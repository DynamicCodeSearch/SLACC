import sys
sys.path.append('/home/george2/Raise/ProgramRepair/CodeSeer/projects/src/main/python')
from CodeJam.Y11R5P1.dozingcat.a import *

def func_1245f2f08535453f872c2285f96375f6(unext, lnext):
    xnext = lnext[0]
    h2 = unext[1] - lnext[1]
    return h2


def func_ed34f69a133249f291c9df79931a2a58(unext, lnext):
    xnext = lnext[0]
    h2 = unext[1] - lnext[1]
    return xnext


def func_6aaa612a9bf6438ab02c14a528f029fa(unext, lnext):
    h2 = unext[1] - lnext[1]
    li += 1
    return li


def func_367650f43bef41e997c3710d2143af68(unext, lnext):
    h2 = unext[1] - lnext[1]
    li += 1
    return h2


def func_1b810be99af8441b96cb7010373e5f11():
    li += 1
    ui += 1
    return li


def func_ce495825b8ee4d54b4531b6e531922eb():
    li += 1
    ui += 1
    return ui


def func_68f78691d1e546db8e11b575bfa75692(unext, lnext):
    xnext = lnext[0]
    h2 = unext[1] - lnext[1]
    li += 1
    return li


def func_5209889e8f804538839d536ef6d62322(unext, lnext):
    xnext = lnext[0]
    h2 = unext[1] - lnext[1]
    li += 1
    return h2


def func_84b7956e48154c15b68ec81b9068d1fa(unext, lnext):
    xnext = lnext[0]
    h2 = unext[1] - lnext[1]
    li += 1
    return xnext


def func_5e5ef93f246d4e92bc6e096ec58551a2(unext, lnext):
    h2 = unext[1] - lnext[1]
    li += 1
    ui += 1
    return h2


def func_57e344634c99495abd60950361fd63f1(unext, lnext):
    h2 = unext[1] - lnext[1]
    li += 1
    ui += 1
    return ui


def func_7abc950a58cc4d7c8fc1cc4c7e83daee(unext, lnext):
    h2 = unext[1] - lnext[1]
    li += 1
    ui += 1
    return li


def func_527b25ca75de41818b6b5e4efa638801(unext, lnext):
    xnext = lnext[0]
    h2 = unext[1] - lnext[1]
    li += 1
    ui += 1
    return li


def func_d276ce2a83d848d0bea54cd053323a2d(unext, lnext):
    xnext = lnext[0]
    h2 = unext[1] - lnext[1]
    li += 1
    ui += 1
    return h2


def func_fe62dadc176e45269ad45f7b31be66d1(unext, lnext):
    xnext = lnext[0]
    h2 = unext[1] - lnext[1]
    li += 1
    ui += 1
    return xnext


def func_e08fde3e6ada4c03be33101f5ee656e2(unext, lnext):
    xnext = lnext[0]
    h2 = unext[1] - lnext[1]
    li += 1
    ui += 1
    return ui


def func_09c69bf917274587a08fda41b58177c7(ui, unext, lnext, upper):
    xnext = lnext[0]
    frac = 1.0 * (xnext - upper[ui][0]) / (unext[0] - upper[ui][0])
    return xnext


def func_48017061f23c4682a47e782085cb0089(ui, unext, lnext, upper):
    xnext = lnext[0]
    frac = 1.0 * (xnext - upper[ui][0]) / (unext[0] - upper[ui][0])
    return frac


def func_2945691b86504e058d943ef32614d42a(ui, unext, xnext, upper):
    frac = 1.0 * (xnext - upper[ui][0]) / (unext[0] - upper[ui][0])
    yupper = upper[ui][1] + frac * (unext[1] - upper[ui][1])
    return yupper


def func_562923ab6612479597e8bfbe7a0a476c(ui, unext, xnext, upper):
    frac = 1.0 * (xnext - upper[ui][0]) / (unext[0] - upper[ui][0])
    yupper = upper[ui][1] + frac * (unext[1] - upper[ui][1])
    return frac


def func_9182c7e2b026412b96956e7e9315b8ab(ui, frac, unext, lnext, upper):
    yupper = upper[ui][1] + frac * (unext[1] - upper[ui][1])
    h2 = yupper - lnext[1]
    return h2


def func_79b6f86c954d4bbcb62f7a6f1669a070(ui, frac, unext, lnext, upper):
    yupper = upper[ui][1] + frac * (unext[1] - upper[ui][1])
    h2 = yupper - lnext[1]
    return yupper


def func_e361df5133024ce2a0580075c6a5a715(yupper, lnext):
    h2 = yupper - lnext[1]
    li += 1
    return li


def func_ab4666f7338e47389479510f4ad19f62(yupper, lnext):
    h2 = yupper - lnext[1]
    li += 1
    return h2


def func_9ba335fa926843839b17f96d241b3e7e(ui, unext, lnext, upper):
    xnext = lnext[0]
    frac = 1.0 * (xnext - upper[ui][0]) / (unext[0] - upper[ui][0])
    yupper = upper[ui][1] + frac * (unext[1] - upper[ui][1])
    return xnext


def func_49581721a811412a98677c84a44a213f(ui, unext, lnext, upper):
    xnext = lnext[0]
    frac = 1.0 * (xnext - upper[ui][0]) / (unext[0] - upper[ui][0])
    yupper = upper[ui][1] + frac * (unext[1] - upper[ui][1])
    return yupper


def func_59e09f5aa94b4af881d9e10ae0a4fe26(ui, unext, lnext, upper):
    xnext = lnext[0]
    frac = 1.0 * (xnext - upper[ui][0]) / (unext[0] - upper[ui][0])
    yupper = upper[ui][1] + frac * (unext[1] - upper[ui][1])
    return frac


def func_25643b3838ef46eaa0819094ad26d482(ui, unext, lnext, xnext, upper):
    frac = 1.0 * (xnext - upper[ui][0]) / (unext[0] - upper[ui][0])
    yupper = upper[ui][1] + frac * (unext[1] - upper[ui][1])
    h2 = yupper - lnext[1]
    return frac


def func_30aa54146b43401182b2484c74bec093(ui, unext, lnext, xnext, upper):
    frac = 1.0 * (xnext - upper[ui][0]) / (unext[0] - upper[ui][0])
    yupper = upper[ui][1] + frac * (unext[1] - upper[ui][1])
    h2 = yupper - lnext[1]
    return h2


def func_84d90324983f4ba993d94a2593ebed08(ui, unext, lnext, xnext, upper):
    frac = 1.0 * (xnext - upper[ui][0]) / (unext[0] - upper[ui][0])
    yupper = upper[ui][1] + frac * (unext[1] - upper[ui][1])
    h2 = yupper - lnext[1]
    return yupper


def func_6dd7d725941d4234b0c26253c346d907(ui, frac, unext, lnext, upper):
    yupper = upper[ui][1] + frac * (unext[1] - upper[ui][1])
    h2 = yupper - lnext[1]
    li += 1
    return h2


def func_18f612c6d9874e59b81f0f2f49f3e6a0(ui, frac, unext, lnext, upper):
    yupper = upper[ui][1] + frac * (unext[1] - upper[ui][1])
    h2 = yupper - lnext[1]
    li += 1
    return yupper


def func_48d139ac1f28410d8417b3dfe710be27(ui, frac, unext, lnext, upper):
    yupper = upper[ui][1] + frac * (unext[1] - upper[ui][1])
    h2 = yupper - lnext[1]
    li += 1
    return li


def func_937bb14a391c456eb7bd09f5b9d7f3a8(ui, unext, lnext, upper):
    xnext = lnext[0]
    frac = 1.0 * (xnext - upper[ui][0]) / (unext[0] - upper[ui][0])
    yupper = upper[ui][1] + frac * (unext[1] - upper[ui][1])
    h2 = yupper - lnext[1]
    return yupper


def func_0086b7dfb7ad43269dc956a440514f2a(ui, unext, lnext, upper):
    xnext = lnext[0]
    frac = 1.0 * (xnext - upper[ui][0]) / (unext[0] - upper[ui][0])
    yupper = upper[ui][1] + frac * (unext[1] - upper[ui][1])
    h2 = yupper - lnext[1]
    return xnext


def func_387b20e32ed14fd7b64e58d3a7936055(ui, unext, lnext, upper):
    xnext = lnext[0]
    frac = 1.0 * (xnext - upper[ui][0]) / (unext[0] - upper[ui][0])
    yupper = upper[ui][1] + frac * (unext[1] - upper[ui][1])
    h2 = yupper - lnext[1]
    return frac


def func_34ef55291a08495482ac7460f320808b(ui, unext, lnext, upper):
    xnext = lnext[0]
    frac = 1.0 * (xnext - upper[ui][0]) / (unext[0] - upper[ui][0])
    yupper = upper[ui][1] + frac * (unext[1] - upper[ui][1])
    h2 = yupper - lnext[1]
    return h2


def func_b9e3258c47ef492e9b87f24b7a1f5b95(ui, unext, lnext, xnext, upper):
    frac = 1.0 * (xnext - upper[ui][0]) / (unext[0] - upper[ui][0])
    yupper = upper[ui][1] + frac * (unext[1] - upper[ui][1])
    h2 = yupper - lnext[1]
    li += 1
    return frac


def func_6f6516e2e959491abc514aa51766ebb6(ui, unext, lnext, xnext, upper):
    frac = 1.0 * (xnext - upper[ui][0]) / (unext[0] - upper[ui][0])
    yupper = upper[ui][1] + frac * (unext[1] - upper[ui][1])
    h2 = yupper - lnext[1]
    li += 1
    return li


def func_6bc5af432f4a4d4cbad013660461e166(ui, unext, lnext, xnext, upper):
    frac = 1.0 * (xnext - upper[ui][0]) / (unext[0] - upper[ui][0])
    yupper = upper[ui][1] + frac * (unext[1] - upper[ui][1])
    h2 = yupper - lnext[1]
    li += 1
    return yupper


def func_4371baa4f0ef497296d9d3fc684e38b5(ui, unext, lnext, xnext, upper):
    frac = 1.0 * (xnext - upper[ui][0]) / (unext[0] - upper[ui][0])
    yupper = upper[ui][1] + frac * (unext[1] - upper[ui][1])
    h2 = yupper - lnext[1]
    li += 1
    return h2


def func_c13625fb2b8644cb8561b933e152e945(ui, unext, lnext, upper):
    xnext = lnext[0]
    frac = 1.0 * (xnext - upper[ui][0]) / (unext[0] - upper[ui][0])
    yupper = upper[ui][1] + frac * (unext[1] - upper[ui][1])
    h2 = yupper - lnext[1]
    li += 1
    return h2


def func_60ed366f27aa4893aac56a0188785abe(ui, unext, lnext, upper):
    xnext = lnext[0]
    frac = 1.0 * (xnext - upper[ui][0]) / (unext[0] - upper[ui][0])
    yupper = upper[ui][1] + frac * (unext[1] - upper[ui][1])
    h2 = yupper - lnext[1]
    li += 1
    return xnext


def func_77e3a360247a4dcdbbdd3fa4e4bf099d(ui, unext, lnext, upper):
    xnext = lnext[0]
    frac = 1.0 * (xnext - upper[ui][0]) / (unext[0] - upper[ui][0])
    yupper = upper[ui][1] + frac * (unext[1] - upper[ui][1])
    h2 = yupper - lnext[1]
    li += 1
    return li


def func_458de8b4f689490b813dc940210b19a9(ui, unext, lnext, upper):
    xnext = lnext[0]
    frac = 1.0 * (xnext - upper[ui][0]) / (unext[0] - upper[ui][0])
    yupper = upper[ui][1] + frac * (unext[1] - upper[ui][1])
    h2 = yupper - lnext[1]
    li += 1
    return frac


def func_0a70696da0fc43019ef464467b316d0a(ui, unext, lnext, upper):
    xnext = lnext[0]
    frac = 1.0 * (xnext - upper[ui][0]) / (unext[0] - upper[ui][0])
    yupper = upper[ui][1] + frac * (unext[1] - upper[ui][1])
    h2 = yupper - lnext[1]
    li += 1
    return yupper


def func_402bfa18e45e4bd18c4b645ea7ca3f70(li, unext, lnext, lower):
    xnext = unext[0]
    frac = 1.0 * (xnext - lower[li][0]) / (lnext[0] - lower[li][0])
    return frac


def func_60980bc41ca8414db1569b995ef90518(li, unext, lnext, lower):
    xnext = unext[0]
    frac = 1.0 * (xnext - lower[li][0]) / (lnext[0] - lower[li][0])
    return xnext


def func_9380b44da33a4045ace7fc1564f09e75(li, lnext, xnext, lower):
    frac = 1.0 * (xnext - lower[li][0]) / (lnext[0] - lower[li][0])
    ylower = lower[li][1] + frac * (lnext[1] - lower[li][1])
    return ylower


def func_8de35058abc0479b912f3d79d1aea8c4(li, lnext, xnext, lower):
    frac = 1.0 * (xnext - lower[li][0]) / (lnext[0] - lower[li][0])
    ylower = lower[li][1] + frac * (lnext[1] - lower[li][1])
    return frac


def func_2792e914cce94f6a99ba9006f7d16038(li, frac, unext, lnext, lower):
    ylower = lower[li][1] + frac * (lnext[1] - lower[li][1])
    h2 = unext[1] - ylower
    return h2


def func_c430fe5adb804ca5bc0ccc0c7be3b23a(li, frac, unext, lnext, lower):
    ylower = lower[li][1] + frac * (lnext[1] - lower[li][1])
    h2 = unext[1] - ylower
    return ylower


def func_8fca224b817c410db36922269e46f38d(unext, ylower):
    h2 = unext[1] - ylower
    ui += 1
    return ui


def func_598ced9142924586a6ca468703b1c216(unext, ylower):
    h2 = unext[1] - ylower
    ui += 1
    return h2


def func_e82e29bca9ce441c81e4de2ada2207d2(li, unext, lnext, lower):
    xnext = unext[0]
    frac = 1.0 * (xnext - lower[li][0]) / (lnext[0] - lower[li][0])
    ylower = lower[li][1] + frac * (lnext[1] - lower[li][1])
    return xnext


def func_78cafb3f55af4821b4ab185596e77daf(li, unext, lnext, lower):
    xnext = unext[0]
    frac = 1.0 * (xnext - lower[li][0]) / (lnext[0] - lower[li][0])
    ylower = lower[li][1] + frac * (lnext[1] - lower[li][1])
    return frac


def func_1de6f325b48b4c2eb848027b942ddccc(li, unext, lnext, lower):
    xnext = unext[0]
    frac = 1.0 * (xnext - lower[li][0]) / (lnext[0] - lower[li][0])
    ylower = lower[li][1] + frac * (lnext[1] - lower[li][1])
    return ylower


def func_06d2eee39bff4319a01d802082235d31(li, unext, lnext, xnext, lower):
    frac = 1.0 * (xnext - lower[li][0]) / (lnext[0] - lower[li][0])
    ylower = lower[li][1] + frac * (lnext[1] - lower[li][1])
    h2 = unext[1] - ylower
    return ylower


def func_de223f7766814ab486223883b850334e(li, unext, lnext, xnext, lower):
    frac = 1.0 * (xnext - lower[li][0]) / (lnext[0] - lower[li][0])
    ylower = lower[li][1] + frac * (lnext[1] - lower[li][1])
    h2 = unext[1] - ylower
    return h2


def func_7745a84b6d30440ab598465094891377(li, unext, lnext, xnext, lower):
    frac = 1.0 * (xnext - lower[li][0]) / (lnext[0] - lower[li][0])
    ylower = lower[li][1] + frac * (lnext[1] - lower[li][1])
    h2 = unext[1] - ylower
    return frac


def func_71eaa3d83e8b4437be7ff83366658fed(li, frac, unext, lnext, lower):
    ylower = lower[li][1] + frac * (lnext[1] - lower[li][1])
    h2 = unext[1] - ylower
    ui += 1
    return ui


def func_405bfda1d08549e386569a8579825ebb(li, frac, unext, lnext, lower):
    ylower = lower[li][1] + frac * (lnext[1] - lower[li][1])
    h2 = unext[1] - ylower
    ui += 1
    return h2


def func_835ffb291b15465688cadf05371704d6(li, frac, unext, lnext, lower):
    ylower = lower[li][1] + frac * (lnext[1] - lower[li][1])
    h2 = unext[1] - ylower
    ui += 1
    return ylower


def func_02b7045761be4c569715f5009733f9c0(li, unext, lnext, lower):
    xnext = unext[0]
    frac = 1.0 * (xnext - lower[li][0]) / (lnext[0] - lower[li][0])
    ylower = lower[li][1] + frac * (lnext[1] - lower[li][1])
    h2 = unext[1] - ylower
    return ylower


def func_738223841ada4beb839291e2bab629a5(li, unext, lnext, lower):
    xnext = unext[0]
    frac = 1.0 * (xnext - lower[li][0]) / (lnext[0] - lower[li][0])
    ylower = lower[li][1] + frac * (lnext[1] - lower[li][1])
    h2 = unext[1] - ylower
    return xnext


def func_547180f651d84e1f98010ec35d8ae306(li, unext, lnext, lower):
    xnext = unext[0]
    frac = 1.0 * (xnext - lower[li][0]) / (lnext[0] - lower[li][0])
    ylower = lower[li][1] + frac * (lnext[1] - lower[li][1])
    h2 = unext[1] - ylower
    return frac


def func_dce2fe5516a8419aad7c944e070e4376(li, unext, lnext, lower):
    xnext = unext[0]
    frac = 1.0 * (xnext - lower[li][0]) / (lnext[0] - lower[li][0])
    ylower = lower[li][1] + frac * (lnext[1] - lower[li][1])
    h2 = unext[1] - ylower
    return h2


def func_ea12da6d49214a37a989be0a9d440491(li, unext, lnext, xnext, lower):
    frac = 1.0 * (xnext - lower[li][0]) / (lnext[0] - lower[li][0])
    ylower = lower[li][1] + frac * (lnext[1] - lower[li][1])
    h2 = unext[1] - ylower
    ui += 1
    return ui


def func_00bcdfdce7d0440892755d9076192840(li, unext, lnext, xnext, lower):
    frac = 1.0 * (xnext - lower[li][0]) / (lnext[0] - lower[li][0])
    ylower = lower[li][1] + frac * (lnext[1] - lower[li][1])
    h2 = unext[1] - ylower
    ui += 1
    return frac


def func_7646456bc6774d6e9ec0350241564073(li, unext, lnext, xnext, lower):
    frac = 1.0 * (xnext - lower[li][0]) / (lnext[0] - lower[li][0])
    ylower = lower[li][1] + frac * (lnext[1] - lower[li][1])
    h2 = unext[1] - ylower
    ui += 1
    return h2


def func_173edb86cf6047a19b07c898ac7497e3(li, unext, lnext, xnext, lower):
    frac = 1.0 * (xnext - lower[li][0]) / (lnext[0] - lower[li][0])
    ylower = lower[li][1] + frac * (lnext[1] - lower[li][1])
    h2 = unext[1] - ylower
    ui += 1
    return ylower


def func_22949779e1054f73af6ad6010f924187(li, unext, lnext, lower):
    xnext = unext[0]
    frac = 1.0 * (xnext - lower[li][0]) / (lnext[0] - lower[li][0])
    ylower = lower[li][1] + frac * (lnext[1] - lower[li][1])
    h2 = unext[1] - ylower
    ui += 1
    return ylower


def func_080c419843f544bcad882a4d3924c476(li, unext, lnext, lower):
    xnext = unext[0]
    frac = 1.0 * (xnext - lower[li][0]) / (lnext[0] - lower[li][0])
    ylower = lower[li][1] + frac * (lnext[1] - lower[li][1])
    h2 = unext[1] - ylower
    ui += 1
    return h2


def func_72db9684fca1499e8ea0af8890799fd2(li, unext, lnext, lower):
    xnext = unext[0]
    frac = 1.0 * (xnext - lower[li][0]) / (lnext[0] - lower[li][0])
    ylower = lower[li][1] + frac * (lnext[1] - lower[li][1])
    h2 = unext[1] - ylower
    ui += 1
    return frac


def func_01d21ecec0a94cdf9a2f541a44f4ba70(li, unext, lnext, lower):
    xnext = unext[0]
    frac = 1.0 * (xnext - lower[li][0]) / (lnext[0] - lower[li][0])
    ylower = lower[li][1] + frac * (lnext[1] - lower[li][1])
    h2 = unext[1] - ylower
    ui += 1
    return xnext


def func_d3833033353144beaaf3f73eba4c9b80(li, unext, lnext, lower):
    xnext = unext[0]
    frac = 1.0 * (xnext - lower[li][0]) / (lnext[0] - lower[li][0])
    ylower = lower[li][1] + frac * (lnext[1] - lower[li][1])
    h2 = unext[1] - ylower
    ui += 1
    return ui


def func_04d4818806de4e34921847df3451b277(ui, li, upper, lower):
    lnext = lower[li + 1]
    unext = upper[ui + 1]
    return lnext


def func_2f94794af44c40ea844081582af6008f(ui, li, upper, lower):
    lnext = lower[li + 1]
    unext = upper[ui + 1]
    return unext


def func_04b627d8366e4c5e83a112bae6ebb479(lnext, upper, lower):
    unext = upper[ui + 1]
    if lnext[0] == unext[0]:
        xnext = lnext[0]
        h2 = unext[1] - lnext[1]
        li += 1
        ui += 1
    elif lnext[0] < upper[ui + 1][0]:
        xnext = lnext[0]
        frac = 1.0 * (xnext - upper[ui][0]) / (unext[0] - upper[ui][0])
        yupper = upper[ui][1] + frac * (unext[1] - upper[ui][1])
        h2 = yupper - lnext[1]
        li += 1
    else:
        xnext = unext[0]
        frac = 1.0 * (xnext - lower[li][0]) / (lnext[0] - lower[li][0])
        ylower = lower[li][1] + frac * (lnext[1] - lower[li][1])
        h2 = unext[1] - ylower
        ui += 1
    return frac


def func_4e7e5c47f9ba418b88a5e8aedb8de95c(lnext, upper, lower):
    unext = upper[ui + 1]
    if lnext[0] == unext[0]:
        xnext = lnext[0]
        h2 = unext[1] - lnext[1]
        li += 1
        ui += 1
    elif lnext[0] < upper[ui + 1][0]:
        xnext = lnext[0]
        frac = 1.0 * (xnext - upper[ui][0]) / (unext[0] - upper[ui][0])
        yupper = upper[ui][1] + frac * (unext[1] - upper[ui][1])
        h2 = yupper - lnext[1]
        li += 1
    else:
        xnext = unext[0]
        frac = 1.0 * (xnext - lower[li][0]) / (lnext[0] - lower[li][0])
        ylower = lower[li][1] + frac * (lnext[1] - lower[li][1])
        h2 = unext[1] - ylower
        ui += 1
    return xnext


def func_9e752507439e471a8540f19657497480(lnext, upper, lower):
    unext = upper[ui + 1]
    if lnext[0] == unext[0]:
        xnext = lnext[0]
        h2 = unext[1] - lnext[1]
        li += 1
        ui += 1
    elif lnext[0] < upper[ui + 1][0]:
        xnext = lnext[0]
        frac = 1.0 * (xnext - upper[ui][0]) / (unext[0] - upper[ui][0])
        yupper = upper[ui][1] + frac * (unext[1] - upper[ui][1])
        h2 = yupper - lnext[1]
        li += 1
    else:
        xnext = unext[0]
        frac = 1.0 * (xnext - lower[li][0]) / (lnext[0] - lower[li][0])
        ylower = lower[li][1] + frac * (lnext[1] - lower[li][1])
        h2 = unext[1] - ylower
        ui += 1
    return h2


def func_b4ab78e1e0dc4daf9daa63384412fa48(lnext, upper, lower):
    unext = upper[ui + 1]
    if lnext[0] == unext[0]:
        xnext = lnext[0]
        h2 = unext[1] - lnext[1]
        li += 1
        ui += 1
    elif lnext[0] < upper[ui + 1][0]:
        xnext = lnext[0]
        frac = 1.0 * (xnext - upper[ui][0]) / (unext[0] - upper[ui][0])
        yupper = upper[ui][1] + frac * (unext[1] - upper[ui][1])
        h2 = yupper - lnext[1]
        li += 1
    else:
        xnext = unext[0]
        frac = 1.0 * (xnext - lower[li][0]) / (lnext[0] - lower[li][0])
        ylower = lower[li][1] + frac * (lnext[1] - lower[li][1])
        h2 = unext[1] - ylower
        ui += 1
    return ui


def func_20ec254bfbd9463999ceaaccaa521d55(lnext, upper, lower):
    unext = upper[ui + 1]
    if lnext[0] == unext[0]:
        xnext = lnext[0]
        h2 = unext[1] - lnext[1]
        li += 1
        ui += 1
    elif lnext[0] < upper[ui + 1][0]:
        xnext = lnext[0]
        frac = 1.0 * (xnext - upper[ui][0]) / (unext[0] - upper[ui][0])
        yupper = upper[ui][1] + frac * (unext[1] - upper[ui][1])
        h2 = yupper - lnext[1]
        li += 1
    else:
        xnext = unext[0]
        frac = 1.0 * (xnext - lower[li][0]) / (lnext[0] - lower[li][0])
        ylower = lower[li][1] + frac * (lnext[1] - lower[li][1])
        h2 = unext[1] - ylower
        ui += 1
    return unext


def func_d1d4de159dcd4ce594db05e3214c4138(lnext, upper, lower):
    unext = upper[ui + 1]
    if lnext[0] == unext[0]:
        xnext = lnext[0]
        h2 = unext[1] - lnext[1]
        li += 1
        ui += 1
    elif lnext[0] < upper[ui + 1][0]:
        xnext = lnext[0]
        frac = 1.0 * (xnext - upper[ui][0]) / (unext[0] - upper[ui][0])
        yupper = upper[ui][1] + frac * (unext[1] - upper[ui][1])
        h2 = yupper - lnext[1]
        li += 1
    else:
        xnext = unext[0]
        frac = 1.0 * (xnext - lower[li][0]) / (lnext[0] - lower[li][0])
        ylower = lower[li][1] + frac * (lnext[1] - lower[li][1])
        h2 = unext[1] - ylower
        ui += 1
    return li


def func_12c9e32011f9400598f420772f8a505a(lnext, upper, lower):
    unext = upper[ui + 1]
    if lnext[0] == unext[0]:
        xnext = lnext[0]
        h2 = unext[1] - lnext[1]
        li += 1
        ui += 1
    elif lnext[0] < upper[ui + 1][0]:
        xnext = lnext[0]
        frac = 1.0 * (xnext - upper[ui][0]) / (unext[0] - upper[ui][0])
        yupper = upper[ui][1] + frac * (unext[1] - upper[ui][1])
        h2 = yupper - lnext[1]
        li += 1
    else:
        xnext = unext[0]
        frac = 1.0 * (xnext - lower[li][0]) / (lnext[0] - lower[li][0])
        ylower = lower[li][1] + frac * (lnext[1] - lower[li][1])
        h2 = unext[1] - ylower
        ui += 1
    return yupper


def func_004c3e2a2dd14665bbe5d0e7c28f55bd(lnext, upper, lower):
    unext = upper[ui + 1]
    if lnext[0] == unext[0]:
        xnext = lnext[0]
        h2 = unext[1] - lnext[1]
        li += 1
        ui += 1
    elif lnext[0] < upper[ui + 1][0]:
        xnext = lnext[0]
        frac = 1.0 * (xnext - upper[ui][0]) / (unext[0] - upper[ui][0])
        yupper = upper[ui][1] + frac * (unext[1] - upper[ui][1])
        h2 = yupper - lnext[1]
        li += 1
    else:
        xnext = unext[0]
        frac = 1.0 * (xnext - lower[li][0]) / (lnext[0] - lower[li][0])
        ylower = lower[li][1] + frac * (lnext[1] - lower[li][1])
        h2 = unext[1] - ylower
        ui += 1
    return ylower


def func_c375d244f98c4fa090ecf8c77d8bc0df(unext, h1, lnext, x, upper, lower):
    if lnext[0] == unext[0]:
        xnext = lnext[0]
        h2 = unext[1] - lnext[1]
        li += 1
        ui += 1
    elif lnext[0] < upper[ui + 1][0]:
        xnext = lnext[0]
        frac = 1.0 * (xnext - upper[ui][0]) / (unext[0] - upper[ui][0])
        yupper = upper[ui][1] + frac * (unext[1] - upper[ui][1])
        h2 = yupper - lnext[1]
        li += 1
    else:
        xnext = unext[0]
        frac = 1.0 * (xnext - lower[li][0]) / (lnext[0] - lower[li][0])
        ylower = lower[li][1] + frac * (lnext[1] - lower[li][1])
        h2 = unext[1] - ylower
        ui += 1
    da = (xnext - x) * (h1 + h2) / 2.0
    return da


def func_1024311bff39483096c870beaac98626(unext, h1, lnext, x, upper, lower):
    if lnext[0] == unext[0]:
        xnext = lnext[0]
        h2 = unext[1] - lnext[1]
        li += 1
        ui += 1
    elif lnext[0] < upper[ui + 1][0]:
        xnext = lnext[0]
        frac = 1.0 * (xnext - upper[ui][0]) / (unext[0] - upper[ui][0])
        yupper = upper[ui][1] + frac * (unext[1] - upper[ui][1])
        h2 = yupper - lnext[1]
        li += 1
    else:
        xnext = unext[0]
        frac = 1.0 * (xnext - lower[li][0]) / (lnext[0] - lower[li][0])
        ylower = lower[li][1] + frac * (lnext[1] - lower[li][1])
        h2 = unext[1] - ylower
        ui += 1
    da = (xnext - x) * (h1 + h2) / 2.0
    return ylower


def func_9a336d48c577459ea2bf4d38579fc477(unext, h1, lnext, x, upper, lower):
    if lnext[0] == unext[0]:
        xnext = lnext[0]
        h2 = unext[1] - lnext[1]
        li += 1
        ui += 1
    elif lnext[0] < upper[ui + 1][0]:
        xnext = lnext[0]
        frac = 1.0 * (xnext - upper[ui][0]) / (unext[0] - upper[ui][0])
        yupper = upper[ui][1] + frac * (unext[1] - upper[ui][1])
        h2 = yupper - lnext[1]
        li += 1
    else:
        xnext = unext[0]
        frac = 1.0 * (xnext - lower[li][0]) / (lnext[0] - lower[li][0])
        ylower = lower[li][1] + frac * (lnext[1] - lower[li][1])
        h2 = unext[1] - ylower
        ui += 1
    da = (xnext - x) * (h1 + h2) / 2.0
    return xnext


def func_b079d446ea1b4667aecbc05be79bafba(unext, h1, lnext, x, upper, lower):
    if lnext[0] == unext[0]:
        xnext = lnext[0]
        h2 = unext[1] - lnext[1]
        li += 1
        ui += 1
    elif lnext[0] < upper[ui + 1][0]:
        xnext = lnext[0]
        frac = 1.0 * (xnext - upper[ui][0]) / (unext[0] - upper[ui][0])
        yupper = upper[ui][1] + frac * (unext[1] - upper[ui][1])
        h2 = yupper - lnext[1]
        li += 1
    else:
        xnext = unext[0]
        frac = 1.0 * (xnext - lower[li][0]) / (lnext[0] - lower[li][0])
        ylower = lower[li][1] + frac * (lnext[1] - lower[li][1])
        h2 = unext[1] - ylower
        ui += 1
    da = (xnext - x) * (h1 + h2) / 2.0
    return h2


def func_9e6071a1e9fa4f43ab7c0eacc9c9efc3(unext, h1, lnext, x, upper, lower):
    if lnext[0] == unext[0]:
        xnext = lnext[0]
        h2 = unext[1] - lnext[1]
        li += 1
        ui += 1
    elif lnext[0] < upper[ui + 1][0]:
        xnext = lnext[0]
        frac = 1.0 * (xnext - upper[ui][0]) / (unext[0] - upper[ui][0])
        yupper = upper[ui][1] + frac * (unext[1] - upper[ui][1])
        h2 = yupper - lnext[1]
        li += 1
    else:
        xnext = unext[0]
        frac = 1.0 * (xnext - lower[li][0]) / (lnext[0] - lower[li][0])
        ylower = lower[li][1] + frac * (lnext[1] - lower[li][1])
        h2 = unext[1] - ylower
        ui += 1
    da = (xnext - x) * (h1 + h2) / 2.0
    return li


def func_bda07ae4d3fc40bc84f9d2dc3d96e6f9(unext, h1, lnext, x, upper, lower):
    if lnext[0] == unext[0]:
        xnext = lnext[0]
        h2 = unext[1] - lnext[1]
        li += 1
        ui += 1
    elif lnext[0] < upper[ui + 1][0]:
        xnext = lnext[0]
        frac = 1.0 * (xnext - upper[ui][0]) / (unext[0] - upper[ui][0])
        yupper = upper[ui][1] + frac * (unext[1] - upper[ui][1])
        h2 = yupper - lnext[1]
        li += 1
    else:
        xnext = unext[0]
        frac = 1.0 * (xnext - lower[li][0]) / (lnext[0] - lower[li][0])
        ylower = lower[li][1] + frac * (lnext[1] - lower[li][1])
        h2 = unext[1] - ylower
        ui += 1
    da = (xnext - x) * (h1 + h2) / 2.0
    return yupper


def func_3b1f25eb234241848c89d8e105bdbff0(unext, h1, lnext, x, upper, lower):
    if lnext[0] == unext[0]:
        xnext = lnext[0]
        h2 = unext[1] - lnext[1]
        li += 1
        ui += 1
    elif lnext[0] < upper[ui + 1][0]:
        xnext = lnext[0]
        frac = 1.0 * (xnext - upper[ui][0]) / (unext[0] - upper[ui][0])
        yupper = upper[ui][1] + frac * (unext[1] - upper[ui][1])
        h2 = yupper - lnext[1]
        li += 1
    else:
        xnext = unext[0]
        frac = 1.0 * (xnext - lower[li][0]) / (lnext[0] - lower[li][0])
        ylower = lower[li][1] + frac * (lnext[1] - lower[li][1])
        h2 = unext[1] - ylower
        ui += 1
    da = (xnext - x) * (h1 + h2) / 2.0
    return frac


def func_bebd652db02f4d099a85339c5160bd1b(unext, h1, lnext, x, upper, lower):
    if lnext[0] == unext[0]:
        xnext = lnext[0]
        h2 = unext[1] - lnext[1]
        li += 1
        ui += 1
    elif lnext[0] < upper[ui + 1][0]:
        xnext = lnext[0]
        frac = 1.0 * (xnext - upper[ui][0]) / (unext[0] - upper[ui][0])
        yupper = upper[ui][1] + frac * (unext[1] - upper[ui][1])
        h2 = yupper - lnext[1]
        li += 1
    else:
        xnext = unext[0]
        frac = 1.0 * (xnext - lower[li][0]) / (lnext[0] - lower[li][0])
        ylower = lower[li][1] + frac * (lnext[1] - lower[li][1])
        h2 = unext[1] - ylower
        ui += 1
    da = (xnext - x) * (h1 + h2) / 2.0
    return ui


def func_c81156ecbaed402c89f25159c2f3a2af(h1, xnext, x, h2):
    da = (xnext - x) * (h1 + h2) / 2.0
    total_area += da
    return total_area


def func_e1ddd9f89b3d4f758cf6f8a1e6a3577c(h1, xnext, x, h2):
    da = (xnext - x) * (h1 + h2) / 2.0
    total_area += da
    return da


def func_d3f7f227d68f43a0a1fef21f15cb2ba2(xnext, h2):
    x = xnext
    h1 = h2
    return h1


def func_8e7011861ed44719ab51fd7370623b3a(xnext, h2):
    x = xnext
    h1 = h2
    return x


def func_5e6efa0322cc484badaeaca3dd3a7d0d(upper, lower):
    lnext = lower[li + 1]
    unext = upper[ui + 1]
    if lnext[0] == unext[0]:
        xnext = lnext[0]
        h2 = unext[1] - lnext[1]
        li += 1
        ui += 1
    elif lnext[0] < upper[ui + 1][0]:
        xnext = lnext[0]
        frac = 1.0 * (xnext - upper[ui][0]) / (unext[0] - upper[ui][0])
        yupper = upper[ui][1] + frac * (unext[1] - upper[ui][1])
        h2 = yupper - lnext[1]
        li += 1
    else:
        xnext = unext[0]
        frac = 1.0 * (xnext - lower[li][0]) / (lnext[0] - lower[li][0])
        ylower = lower[li][1] + frac * (lnext[1] - lower[li][1])
        h2 = unext[1] - ylower
        ui += 1
    return ui


def func_3ada975b3fc54a19bc699e2f99794bb9(upper, lower):
    lnext = lower[li + 1]
    unext = upper[ui + 1]
    if lnext[0] == unext[0]:
        xnext = lnext[0]
        h2 = unext[1] - lnext[1]
        li += 1
        ui += 1
    elif lnext[0] < upper[ui + 1][0]:
        xnext = lnext[0]
        frac = 1.0 * (xnext - upper[ui][0]) / (unext[0] - upper[ui][0])
        yupper = upper[ui][1] + frac * (unext[1] - upper[ui][1])
        h2 = yupper - lnext[1]
        li += 1
    else:
        xnext = unext[0]
        frac = 1.0 * (xnext - lower[li][0]) / (lnext[0] - lower[li][0])
        ylower = lower[li][1] + frac * (lnext[1] - lower[li][1])
        h2 = unext[1] - ylower
        ui += 1
    return xnext


def func_c8a0c2b2c50e41c38fcbe7bffdb70f1a(upper, lower):
    lnext = lower[li + 1]
    unext = upper[ui + 1]
    if lnext[0] == unext[0]:
        xnext = lnext[0]
        h2 = unext[1] - lnext[1]
        li += 1
        ui += 1
    elif lnext[0] < upper[ui + 1][0]:
        xnext = lnext[0]
        frac = 1.0 * (xnext - upper[ui][0]) / (unext[0] - upper[ui][0])
        yupper = upper[ui][1] + frac * (unext[1] - upper[ui][1])
        h2 = yupper - lnext[1]
        li += 1
    else:
        xnext = unext[0]
        frac = 1.0 * (xnext - lower[li][0]) / (lnext[0] - lower[li][0])
        ylower = lower[li][1] + frac * (lnext[1] - lower[li][1])
        h2 = unext[1] - ylower
        ui += 1
    return frac


def func_cda761df4924452e8af7afb8fbda42ed(upper, lower):
    lnext = lower[li + 1]
    unext = upper[ui + 1]
    if lnext[0] == unext[0]:
        xnext = lnext[0]
        h2 = unext[1] - lnext[1]
        li += 1
        ui += 1
    elif lnext[0] < upper[ui + 1][0]:
        xnext = lnext[0]
        frac = 1.0 * (xnext - upper[ui][0]) / (unext[0] - upper[ui][0])
        yupper = upper[ui][1] + frac * (unext[1] - upper[ui][1])
        h2 = yupper - lnext[1]
        li += 1
    else:
        xnext = unext[0]
        frac = 1.0 * (xnext - lower[li][0]) / (lnext[0] - lower[li][0])
        ylower = lower[li][1] + frac * (lnext[1] - lower[li][1])
        h2 = unext[1] - ylower
        ui += 1
    return yupper


def func_1da2cc1ac86742b28dfcc1c71196ecd3(upper, lower):
    lnext = lower[li + 1]
    unext = upper[ui + 1]
    if lnext[0] == unext[0]:
        xnext = lnext[0]
        h2 = unext[1] - lnext[1]
        li += 1
        ui += 1
    elif lnext[0] < upper[ui + 1][0]:
        xnext = lnext[0]
        frac = 1.0 * (xnext - upper[ui][0]) / (unext[0] - upper[ui][0])
        yupper = upper[ui][1] + frac * (unext[1] - upper[ui][1])
        h2 = yupper - lnext[1]
        li += 1
    else:
        xnext = unext[0]
        frac = 1.0 * (xnext - lower[li][0]) / (lnext[0] - lower[li][0])
        ylower = lower[li][1] + frac * (lnext[1] - lower[li][1])
        h2 = unext[1] - ylower
        ui += 1
    return unext


def func_1cc13dfdc9b84b028dab485514d592fa(upper, lower):
    lnext = lower[li + 1]
    unext = upper[ui + 1]
    if lnext[0] == unext[0]:
        xnext = lnext[0]
        h2 = unext[1] - lnext[1]
        li += 1
        ui += 1
    elif lnext[0] < upper[ui + 1][0]:
        xnext = lnext[0]
        frac = 1.0 * (xnext - upper[ui][0]) / (unext[0] - upper[ui][0])
        yupper = upper[ui][1] + frac * (unext[1] - upper[ui][1])
        h2 = yupper - lnext[1]
        li += 1
    else:
        xnext = unext[0]
        frac = 1.0 * (xnext - lower[li][0]) / (lnext[0] - lower[li][0])
        ylower = lower[li][1] + frac * (lnext[1] - lower[li][1])
        h2 = unext[1] - ylower
        ui += 1
    return lnext


def func_f870256fee60443b8e3d16f1f82b7cad(upper, lower):
    lnext = lower[li + 1]
    unext = upper[ui + 1]
    if lnext[0] == unext[0]:
        xnext = lnext[0]
        h2 = unext[1] - lnext[1]
        li += 1
        ui += 1
    elif lnext[0] < upper[ui + 1][0]:
        xnext = lnext[0]
        frac = 1.0 * (xnext - upper[ui][0]) / (unext[0] - upper[ui][0])
        yupper = upper[ui][1] + frac * (unext[1] - upper[ui][1])
        h2 = yupper - lnext[1]
        li += 1
    else:
        xnext = unext[0]
        frac = 1.0 * (xnext - lower[li][0]) / (lnext[0] - lower[li][0])
        ylower = lower[li][1] + frac * (lnext[1] - lower[li][1])
        h2 = unext[1] - ylower
        ui += 1
    return ylower


def func_247269f88d0047df8ddea52f607782d5(upper, lower):
    lnext = lower[li + 1]
    unext = upper[ui + 1]
    if lnext[0] == unext[0]:
        xnext = lnext[0]
        h2 = unext[1] - lnext[1]
        li += 1
        ui += 1
    elif lnext[0] < upper[ui + 1][0]:
        xnext = lnext[0]
        frac = 1.0 * (xnext - upper[ui][0]) / (unext[0] - upper[ui][0])
        yupper = upper[ui][1] + frac * (unext[1] - upper[ui][1])
        h2 = yupper - lnext[1]
        li += 1
    else:
        xnext = unext[0]
        frac = 1.0 * (xnext - lower[li][0]) / (lnext[0] - lower[li][0])
        ylower = lower[li][1] + frac * (lnext[1] - lower[li][1])
        h2 = unext[1] - ylower
        ui += 1
    return h2


def func_808cb4ec0f0742fb9655c537ec0ad5b0(upper, lower):
    lnext = lower[li + 1]
    unext = upper[ui + 1]
    if lnext[0] == unext[0]:
        xnext = lnext[0]
        h2 = unext[1] - lnext[1]
        li += 1
        ui += 1
    elif lnext[0] < upper[ui + 1][0]:
        xnext = lnext[0]
        frac = 1.0 * (xnext - upper[ui][0]) / (unext[0] - upper[ui][0])
        yupper = upper[ui][1] + frac * (unext[1] - upper[ui][1])
        h2 = yupper - lnext[1]
        li += 1
    else:
        xnext = unext[0]
        frac = 1.0 * (xnext - lower[li][0]) / (lnext[0] - lower[li][0])
        ylower = lower[li][1] + frac * (lnext[1] - lower[li][1])
        h2 = unext[1] - ylower
        ui += 1
    return li


def func_b95d4dc8bb4748a28744884a8cb79c07(h1, lnext, x, upper, lower):
    unext = upper[ui + 1]
    if lnext[0] == unext[0]:
        xnext = lnext[0]
        h2 = unext[1] - lnext[1]
        li += 1
        ui += 1
    elif lnext[0] < upper[ui + 1][0]:
        xnext = lnext[0]
        frac = 1.0 * (xnext - upper[ui][0]) / (unext[0] - upper[ui][0])
        yupper = upper[ui][1] + frac * (unext[1] - upper[ui][1])
        h2 = yupper - lnext[1]
        li += 1
    else:
        xnext = unext[0]
        frac = 1.0 * (xnext - lower[li][0]) / (lnext[0] - lower[li][0])
        ylower = lower[li][1] + frac * (lnext[1] - lower[li][1])
        h2 = unext[1] - ylower
        ui += 1
    da = (xnext - x) * (h1 + h2) / 2.0
    return unext


def func_e14cf1bbdd0a4ef8a21e3c19829bb1f7(h1, lnext, x, upper, lower):
    unext = upper[ui + 1]
    if lnext[0] == unext[0]:
        xnext = lnext[0]
        h2 = unext[1] - lnext[1]
        li += 1
        ui += 1
    elif lnext[0] < upper[ui + 1][0]:
        xnext = lnext[0]
        frac = 1.0 * (xnext - upper[ui][0]) / (unext[0] - upper[ui][0])
        yupper = upper[ui][1] + frac * (unext[1] - upper[ui][1])
        h2 = yupper - lnext[1]
        li += 1
    else:
        xnext = unext[0]
        frac = 1.0 * (xnext - lower[li][0]) / (lnext[0] - lower[li][0])
        ylower = lower[li][1] + frac * (lnext[1] - lower[li][1])
        h2 = unext[1] - ylower
        ui += 1
    da = (xnext - x) * (h1 + h2) / 2.0
    return da


def func_9a8312438ff743948e0caf97e13c9802(h1, lnext, x, upper, lower):
    unext = upper[ui + 1]
    if lnext[0] == unext[0]:
        xnext = lnext[0]
        h2 = unext[1] - lnext[1]
        li += 1
        ui += 1
    elif lnext[0] < upper[ui + 1][0]:
        xnext = lnext[0]
        frac = 1.0 * (xnext - upper[ui][0]) / (unext[0] - upper[ui][0])
        yupper = upper[ui][1] + frac * (unext[1] - upper[ui][1])
        h2 = yupper - lnext[1]
        li += 1
    else:
        xnext = unext[0]
        frac = 1.0 * (xnext - lower[li][0]) / (lnext[0] - lower[li][0])
        ylower = lower[li][1] + frac * (lnext[1] - lower[li][1])
        h2 = unext[1] - ylower
        ui += 1
    da = (xnext - x) * (h1 + h2) / 2.0
    return li


def func_ee0c3ebc66d84c47b0aedd388d4c0965(h1, lnext, x, upper, lower):
    unext = upper[ui + 1]
    if lnext[0] == unext[0]:
        xnext = lnext[0]
        h2 = unext[1] - lnext[1]
        li += 1
        ui += 1
    elif lnext[0] < upper[ui + 1][0]:
        xnext = lnext[0]
        frac = 1.0 * (xnext - upper[ui][0]) / (unext[0] - upper[ui][0])
        yupper = upper[ui][1] + frac * (unext[1] - upper[ui][1])
        h2 = yupper - lnext[1]
        li += 1
    else:
        xnext = unext[0]
        frac = 1.0 * (xnext - lower[li][0]) / (lnext[0] - lower[li][0])
        ylower = lower[li][1] + frac * (lnext[1] - lower[li][1])
        h2 = unext[1] - ylower
        ui += 1
    da = (xnext - x) * (h1 + h2) / 2.0
    return ylower


def func_65d6e5e089f9479a8147b26d146ed969(h1, lnext, x, upper, lower):
    unext = upper[ui + 1]
    if lnext[0] == unext[0]:
        xnext = lnext[0]
        h2 = unext[1] - lnext[1]
        li += 1
        ui += 1
    elif lnext[0] < upper[ui + 1][0]:
        xnext = lnext[0]
        frac = 1.0 * (xnext - upper[ui][0]) / (unext[0] - upper[ui][0])
        yupper = upper[ui][1] + frac * (unext[1] - upper[ui][1])
        h2 = yupper - lnext[1]
        li += 1
    else:
        xnext = unext[0]
        frac = 1.0 * (xnext - lower[li][0]) / (lnext[0] - lower[li][0])
        ylower = lower[li][1] + frac * (lnext[1] - lower[li][1])
        h2 = unext[1] - ylower
        ui += 1
    da = (xnext - x) * (h1 + h2) / 2.0
    return h2


def func_dd48c4f7a7be4c3cbfbdff8e85d620e0(h1, lnext, x, upper, lower):
    unext = upper[ui + 1]
    if lnext[0] == unext[0]:
        xnext = lnext[0]
        h2 = unext[1] - lnext[1]
        li += 1
        ui += 1
    elif lnext[0] < upper[ui + 1][0]:
        xnext = lnext[0]
        frac = 1.0 * (xnext - upper[ui][0]) / (unext[0] - upper[ui][0])
        yupper = upper[ui][1] + frac * (unext[1] - upper[ui][1])
        h2 = yupper - lnext[1]
        li += 1
    else:
        xnext = unext[0]
        frac = 1.0 * (xnext - lower[li][0]) / (lnext[0] - lower[li][0])
        ylower = lower[li][1] + frac * (lnext[1] - lower[li][1])
        h2 = unext[1] - ylower
        ui += 1
    da = (xnext - x) * (h1 + h2) / 2.0
    return xnext


def func_3097b2f6448d4cb7b1bb15c4071c02d9(h1, lnext, x, upper, lower):
    unext = upper[ui + 1]
    if lnext[0] == unext[0]:
        xnext = lnext[0]
        h2 = unext[1] - lnext[1]
        li += 1
        ui += 1
    elif lnext[0] < upper[ui + 1][0]:
        xnext = lnext[0]
        frac = 1.0 * (xnext - upper[ui][0]) / (unext[0] - upper[ui][0])
        yupper = upper[ui][1] + frac * (unext[1] - upper[ui][1])
        h2 = yupper - lnext[1]
        li += 1
    else:
        xnext = unext[0]
        frac = 1.0 * (xnext - lower[li][0]) / (lnext[0] - lower[li][0])
        ylower = lower[li][1] + frac * (lnext[1] - lower[li][1])
        h2 = unext[1] - ylower
        ui += 1
    da = (xnext - x) * (h1 + h2) / 2.0
    return yupper


def func_3d0fc74778274f29ad69a0a4ae9df782(h1, lnext, x, upper, lower):
    unext = upper[ui + 1]
    if lnext[0] == unext[0]:
        xnext = lnext[0]
        h2 = unext[1] - lnext[1]
        li += 1
        ui += 1
    elif lnext[0] < upper[ui + 1][0]:
        xnext = lnext[0]
        frac = 1.0 * (xnext - upper[ui][0]) / (unext[0] - upper[ui][0])
        yupper = upper[ui][1] + frac * (unext[1] - upper[ui][1])
        h2 = yupper - lnext[1]
        li += 1
    else:
        xnext = unext[0]
        frac = 1.0 * (xnext - lower[li][0]) / (lnext[0] - lower[li][0])
        ylower = lower[li][1] + frac * (lnext[1] - lower[li][1])
        h2 = unext[1] - ylower
        ui += 1
    da = (xnext - x) * (h1 + h2) / 2.0
    return ui


def func_5c52c131e77d48b4ba78f44a233338f1(h1, lnext, x, upper, lower):
    unext = upper[ui + 1]
    if lnext[0] == unext[0]:
        xnext = lnext[0]
        h2 = unext[1] - lnext[1]
        li += 1
        ui += 1
    elif lnext[0] < upper[ui + 1][0]:
        xnext = lnext[0]
        frac = 1.0 * (xnext - upper[ui][0]) / (unext[0] - upper[ui][0])
        yupper = upper[ui][1] + frac * (unext[1] - upper[ui][1])
        h2 = yupper - lnext[1]
        li += 1
    else:
        xnext = unext[0]
        frac = 1.0 * (xnext - lower[li][0]) / (lnext[0] - lower[li][0])
        ylower = lower[li][1] + frac * (lnext[1] - lower[li][1])
        h2 = unext[1] - ylower
        ui += 1
    da = (xnext - x) * (h1 + h2) / 2.0
    return frac


def func_1ca6e73270d445bb981935cb111e0e0a(unext, h1, lnext, x, upper, lower):
    if lnext[0] == unext[0]:
        xnext = lnext[0]
        h2 = unext[1] - lnext[1]
        li += 1
        ui += 1
    elif lnext[0] < upper[ui + 1][0]:
        xnext = lnext[0]
        frac = 1.0 * (xnext - upper[ui][0]) / (unext[0] - upper[ui][0])
        yupper = upper[ui][1] + frac * (unext[1] - upper[ui][1])
        h2 = yupper - lnext[1]
        li += 1
    else:
        xnext = unext[0]
        frac = 1.0 * (xnext - lower[li][0]) / (lnext[0] - lower[li][0])
        ylower = lower[li][1] + frac * (lnext[1] - lower[li][1])
        h2 = unext[1] - ylower
        ui += 1
    da = (xnext - x) * (h1 + h2) / 2.0
    total_area += da
    return h2


def func_fd8f28ba49ca44f397e3765aea8392fd(unext, h1, lnext, x, upper, lower):
    if lnext[0] == unext[0]:
        xnext = lnext[0]
        h2 = unext[1] - lnext[1]
        li += 1
        ui += 1
    elif lnext[0] < upper[ui + 1][0]:
        xnext = lnext[0]
        frac = 1.0 * (xnext - upper[ui][0]) / (unext[0] - upper[ui][0])
        yupper = upper[ui][1] + frac * (unext[1] - upper[ui][1])
        h2 = yupper - lnext[1]
        li += 1
    else:
        xnext = unext[0]
        frac = 1.0 * (xnext - lower[li][0]) / (lnext[0] - lower[li][0])
        ylower = lower[li][1] + frac * (lnext[1] - lower[li][1])
        h2 = unext[1] - ylower
        ui += 1
    da = (xnext - x) * (h1 + h2) / 2.0
    total_area += da
    return frac


def func_20717a1896b7497ea5cf515a5de77192(unext, h1, lnext, x, upper, lower):
    if lnext[0] == unext[0]:
        xnext = lnext[0]
        h2 = unext[1] - lnext[1]
        li += 1
        ui += 1
    elif lnext[0] < upper[ui + 1][0]:
        xnext = lnext[0]
        frac = 1.0 * (xnext - upper[ui][0]) / (unext[0] - upper[ui][0])
        yupper = upper[ui][1] + frac * (unext[1] - upper[ui][1])
        h2 = yupper - lnext[1]
        li += 1
    else:
        xnext = unext[0]
        frac = 1.0 * (xnext - lower[li][0]) / (lnext[0] - lower[li][0])
        ylower = lower[li][1] + frac * (lnext[1] - lower[li][1])
        h2 = unext[1] - ylower
        ui += 1
    da = (xnext - x) * (h1 + h2) / 2.0
    total_area += da
    return da


def func_28de90d6867e4e0dbff25a8ed2de394e(unext, h1, lnext, x, upper, lower):
    if lnext[0] == unext[0]:
        xnext = lnext[0]
        h2 = unext[1] - lnext[1]
        li += 1
        ui += 1
    elif lnext[0] < upper[ui + 1][0]:
        xnext = lnext[0]
        frac = 1.0 * (xnext - upper[ui][0]) / (unext[0] - upper[ui][0])
        yupper = upper[ui][1] + frac * (unext[1] - upper[ui][1])
        h2 = yupper - lnext[1]
        li += 1
    else:
        xnext = unext[0]
        frac = 1.0 * (xnext - lower[li][0]) / (lnext[0] - lower[li][0])
        ylower = lower[li][1] + frac * (lnext[1] - lower[li][1])
        h2 = unext[1] - ylower
        ui += 1
    da = (xnext - x) * (h1 + h2) / 2.0
    total_area += da
    return xnext


def func_2b860f28047b4bfa9a8a2bec5ba6b6e4(unext, h1, lnext, x, upper, lower):
    if lnext[0] == unext[0]:
        xnext = lnext[0]
        h2 = unext[1] - lnext[1]
        li += 1
        ui += 1
    elif lnext[0] < upper[ui + 1][0]:
        xnext = lnext[0]
        frac = 1.0 * (xnext - upper[ui][0]) / (unext[0] - upper[ui][0])
        yupper = upper[ui][1] + frac * (unext[1] - upper[ui][1])
        h2 = yupper - lnext[1]
        li += 1
    else:
        xnext = unext[0]
        frac = 1.0 * (xnext - lower[li][0]) / (lnext[0] - lower[li][0])
        ylower = lower[li][1] + frac * (lnext[1] - lower[li][1])
        h2 = unext[1] - ylower
        ui += 1
    da = (xnext - x) * (h1 + h2) / 2.0
    total_area += da
    return total_area


def func_0b8d823e44e8490298fdcc3f4204e4f9(unext, h1, lnext, x, upper, lower):
    if lnext[0] == unext[0]:
        xnext = lnext[0]
        h2 = unext[1] - lnext[1]
        li += 1
        ui += 1
    elif lnext[0] < upper[ui + 1][0]:
        xnext = lnext[0]
        frac = 1.0 * (xnext - upper[ui][0]) / (unext[0] - upper[ui][0])
        yupper = upper[ui][1] + frac * (unext[1] - upper[ui][1])
        h2 = yupper - lnext[1]
        li += 1
    else:
        xnext = unext[0]
        frac = 1.0 * (xnext - lower[li][0]) / (lnext[0] - lower[li][0])
        ylower = lower[li][1] + frac * (lnext[1] - lower[li][1])
        h2 = unext[1] - ylower
        ui += 1
    da = (xnext - x) * (h1 + h2) / 2.0
    total_area += da
    return li


def func_bdcf6009506e4531b559e2304ee366e3(unext, h1, lnext, x, upper, lower):
    if lnext[0] == unext[0]:
        xnext = lnext[0]
        h2 = unext[1] - lnext[1]
        li += 1
        ui += 1
    elif lnext[0] < upper[ui + 1][0]:
        xnext = lnext[0]
        frac = 1.0 * (xnext - upper[ui][0]) / (unext[0] - upper[ui][0])
        yupper = upper[ui][1] + frac * (unext[1] - upper[ui][1])
        h2 = yupper - lnext[1]
        li += 1
    else:
        xnext = unext[0]
        frac = 1.0 * (xnext - lower[li][0]) / (lnext[0] - lower[li][0])
        ylower = lower[li][1] + frac * (lnext[1] - lower[li][1])
        h2 = unext[1] - ylower
        ui += 1
    da = (xnext - x) * (h1 + h2) / 2.0
    total_area += da
    return yupper


def func_b45de3e6722f4687a0fb9f40559a5f30(unext, h1, lnext, x, upper, lower):
    if lnext[0] == unext[0]:
        xnext = lnext[0]
        h2 = unext[1] - lnext[1]
        li += 1
        ui += 1
    elif lnext[0] < upper[ui + 1][0]:
        xnext = lnext[0]
        frac = 1.0 * (xnext - upper[ui][0]) / (unext[0] - upper[ui][0])
        yupper = upper[ui][1] + frac * (unext[1] - upper[ui][1])
        h2 = yupper - lnext[1]
        li += 1
    else:
        xnext = unext[0]
        frac = 1.0 * (xnext - lower[li][0]) / (lnext[0] - lower[li][0])
        ylower = lower[li][1] + frac * (lnext[1] - lower[li][1])
        h2 = unext[1] - ylower
        ui += 1
    da = (xnext - x) * (h1 + h2) / 2.0
    total_area += da
    return ylower


def func_565fb875217b4c3ca3a5b4c83be708d2(unext, h1, lnext, x, upper, lower):
    if lnext[0] == unext[0]:
        xnext = lnext[0]
        h2 = unext[1] - lnext[1]
        li += 1
        ui += 1
    elif lnext[0] < upper[ui + 1][0]:
        xnext = lnext[0]
        frac = 1.0 * (xnext - upper[ui][0]) / (unext[0] - upper[ui][0])
        yupper = upper[ui][1] + frac * (unext[1] - upper[ui][1])
        h2 = yupper - lnext[1]
        li += 1
    else:
        xnext = unext[0]
        frac = 1.0 * (xnext - lower[li][0]) / (lnext[0] - lower[li][0])
        ylower = lower[li][1] + frac * (lnext[1] - lower[li][1])
        h2 = unext[1] - ylower
        ui += 1
    da = (xnext - x) * (h1 + h2) / 2.0
    total_area += da
    return ui


def func_31fad08b7c1e4d389d4d7fb5bcf24db8(h1, x, upper, lower):
    lnext = lower[li + 1]
    unext = upper[ui + 1]
    if lnext[0] == unext[0]:
        xnext = lnext[0]
        h2 = unext[1] - lnext[1]
        li += 1
        ui += 1
    elif lnext[0] < upper[ui + 1][0]:
        xnext = lnext[0]
        frac = 1.0 * (xnext - upper[ui][0]) / (unext[0] - upper[ui][0])
        yupper = upper[ui][1] + frac * (unext[1] - upper[ui][1])
        h2 = yupper - lnext[1]
        li += 1
    else:
        xnext = unext[0]
        frac = 1.0 * (xnext - lower[li][0]) / (lnext[0] - lower[li][0])
        ylower = lower[li][1] + frac * (lnext[1] - lower[li][1])
        h2 = unext[1] - ylower
        ui += 1
    da = (xnext - x) * (h1 + h2) / 2.0
    return h2


def func_56168a1b547a43c4b5670c6679fa72e5(h1, x, upper, lower):
    lnext = lower[li + 1]
    unext = upper[ui + 1]
    if lnext[0] == unext[0]:
        xnext = lnext[0]
        h2 = unext[1] - lnext[1]
        li += 1
        ui += 1
    elif lnext[0] < upper[ui + 1][0]:
        xnext = lnext[0]
        frac = 1.0 * (xnext - upper[ui][0]) / (unext[0] - upper[ui][0])
        yupper = upper[ui][1] + frac * (unext[1] - upper[ui][1])
        h2 = yupper - lnext[1]
        li += 1
    else:
        xnext = unext[0]
        frac = 1.0 * (xnext - lower[li][0]) / (lnext[0] - lower[li][0])
        ylower = lower[li][1] + frac * (lnext[1] - lower[li][1])
        h2 = unext[1] - ylower
        ui += 1
    da = (xnext - x) * (h1 + h2) / 2.0
    return da


def func_932ea8a8f0084c3bbeae8e0f3c70161a(h1, x, upper, lower):
    lnext = lower[li + 1]
    unext = upper[ui + 1]
    if lnext[0] == unext[0]:
        xnext = lnext[0]
        h2 = unext[1] - lnext[1]
        li += 1
        ui += 1
    elif lnext[0] < upper[ui + 1][0]:
        xnext = lnext[0]
        frac = 1.0 * (xnext - upper[ui][0]) / (unext[0] - upper[ui][0])
        yupper = upper[ui][1] + frac * (unext[1] - upper[ui][1])
        h2 = yupper - lnext[1]
        li += 1
    else:
        xnext = unext[0]
        frac = 1.0 * (xnext - lower[li][0]) / (lnext[0] - lower[li][0])
        ylower = lower[li][1] + frac * (lnext[1] - lower[li][1])
        h2 = unext[1] - ylower
        ui += 1
    da = (xnext - x) * (h1 + h2) / 2.0
    return yupper


def func_08da356ed91e4f14895af59419f9de54(h1, x, upper, lower):
    lnext = lower[li + 1]
    unext = upper[ui + 1]
    if lnext[0] == unext[0]:
        xnext = lnext[0]
        h2 = unext[1] - lnext[1]
        li += 1
        ui += 1
    elif lnext[0] < upper[ui + 1][0]:
        xnext = lnext[0]
        frac = 1.0 * (xnext - upper[ui][0]) / (unext[0] - upper[ui][0])
        yupper = upper[ui][1] + frac * (unext[1] - upper[ui][1])
        h2 = yupper - lnext[1]
        li += 1
    else:
        xnext = unext[0]
        frac = 1.0 * (xnext - lower[li][0]) / (lnext[0] - lower[li][0])
        ylower = lower[li][1] + frac * (lnext[1] - lower[li][1])
        h2 = unext[1] - ylower
        ui += 1
    da = (xnext - x) * (h1 + h2) / 2.0
    return ui


def func_e47f8f8ee63f42fab438f13ebaebf55c(h1, x, upper, lower):
    lnext = lower[li + 1]
    unext = upper[ui + 1]
    if lnext[0] == unext[0]:
        xnext = lnext[0]
        h2 = unext[1] - lnext[1]
        li += 1
        ui += 1
    elif lnext[0] < upper[ui + 1][0]:
        xnext = lnext[0]
        frac = 1.0 * (xnext - upper[ui][0]) / (unext[0] - upper[ui][0])
        yupper = upper[ui][1] + frac * (unext[1] - upper[ui][1])
        h2 = yupper - lnext[1]
        li += 1
    else:
        xnext = unext[0]
        frac = 1.0 * (xnext - lower[li][0]) / (lnext[0] - lower[li][0])
        ylower = lower[li][1] + frac * (lnext[1] - lower[li][1])
        h2 = unext[1] - ylower
        ui += 1
    da = (xnext - x) * (h1 + h2) / 2.0
    return xnext


def func_7df29e6857574e59850a83336c942d03(h1, x, upper, lower):
    lnext = lower[li + 1]
    unext = upper[ui + 1]
    if lnext[0] == unext[0]:
        xnext = lnext[0]
        h2 = unext[1] - lnext[1]
        li += 1
        ui += 1
    elif lnext[0] < upper[ui + 1][0]:
        xnext = lnext[0]
        frac = 1.0 * (xnext - upper[ui][0]) / (unext[0] - upper[ui][0])
        yupper = upper[ui][1] + frac * (unext[1] - upper[ui][1])
        h2 = yupper - lnext[1]
        li += 1
    else:
        xnext = unext[0]
        frac = 1.0 * (xnext - lower[li][0]) / (lnext[0] - lower[li][0])
        ylower = lower[li][1] + frac * (lnext[1] - lower[li][1])
        h2 = unext[1] - ylower
        ui += 1
    da = (xnext - x) * (h1 + h2) / 2.0
    return lnext


def func_08f64f6c1dc341c4bdda1222caeaf2bb(h1, x, upper, lower):
    lnext = lower[li + 1]
    unext = upper[ui + 1]
    if lnext[0] == unext[0]:
        xnext = lnext[0]
        h2 = unext[1] - lnext[1]
        li += 1
        ui += 1
    elif lnext[0] < upper[ui + 1][0]:
        xnext = lnext[0]
        frac = 1.0 * (xnext - upper[ui][0]) / (unext[0] - upper[ui][0])
        yupper = upper[ui][1] + frac * (unext[1] - upper[ui][1])
        h2 = yupper - lnext[1]
        li += 1
    else:
        xnext = unext[0]
        frac = 1.0 * (xnext - lower[li][0]) / (lnext[0] - lower[li][0])
        ylower = lower[li][1] + frac * (lnext[1] - lower[li][1])
        h2 = unext[1] - ylower
        ui += 1
    da = (xnext - x) * (h1 + h2) / 2.0
    return unext


def func_48b91687d9794f9f84bf12e280f91bfa(h1, x, upper, lower):
    lnext = lower[li + 1]
    unext = upper[ui + 1]
    if lnext[0] == unext[0]:
        xnext = lnext[0]
        h2 = unext[1] - lnext[1]
        li += 1
        ui += 1
    elif lnext[0] < upper[ui + 1][0]:
        xnext = lnext[0]
        frac = 1.0 * (xnext - upper[ui][0]) / (unext[0] - upper[ui][0])
        yupper = upper[ui][1] + frac * (unext[1] - upper[ui][1])
        h2 = yupper - lnext[1]
        li += 1
    else:
        xnext = unext[0]
        frac = 1.0 * (xnext - lower[li][0]) / (lnext[0] - lower[li][0])
        ylower = lower[li][1] + frac * (lnext[1] - lower[li][1])
        h2 = unext[1] - ylower
        ui += 1
    da = (xnext - x) * (h1 + h2) / 2.0
    return frac


def func_b12c18bc4a1e46d6ab2248d61a4968fd(h1, x, upper, lower):
    lnext = lower[li + 1]
    unext = upper[ui + 1]
    if lnext[0] == unext[0]:
        xnext = lnext[0]
        h2 = unext[1] - lnext[1]
        li += 1
        ui += 1
    elif lnext[0] < upper[ui + 1][0]:
        xnext = lnext[0]
        frac = 1.0 * (xnext - upper[ui][0]) / (unext[0] - upper[ui][0])
        yupper = upper[ui][1] + frac * (unext[1] - upper[ui][1])
        h2 = yupper - lnext[1]
        li += 1
    else:
        xnext = unext[0]
        frac = 1.0 * (xnext - lower[li][0]) / (lnext[0] - lower[li][0])
        ylower = lower[li][1] + frac * (lnext[1] - lower[li][1])
        h2 = unext[1] - ylower
        ui += 1
    da = (xnext - x) * (h1 + h2) / 2.0
    return li


def func_a665e4fe5a404bf2bd2ec42809606db8(h1, x, upper, lower):
    lnext = lower[li + 1]
    unext = upper[ui + 1]
    if lnext[0] == unext[0]:
        xnext = lnext[0]
        h2 = unext[1] - lnext[1]
        li += 1
        ui += 1
    elif lnext[0] < upper[ui + 1][0]:
        xnext = lnext[0]
        frac = 1.0 * (xnext - upper[ui][0]) / (unext[0] - upper[ui][0])
        yupper = upper[ui][1] + frac * (unext[1] - upper[ui][1])
        h2 = yupper - lnext[1]
        li += 1
    else:
        xnext = unext[0]
        frac = 1.0 * (xnext - lower[li][0]) / (lnext[0] - lower[li][0])
        ylower = lower[li][1] + frac * (lnext[1] - lower[li][1])
        h2 = unext[1] - ylower
        ui += 1
    da = (xnext - x) * (h1 + h2) / 2.0
    return ylower


def func_dd73fddc83334c84aad23a5b99c495e3(h1, lnext, x, upper, lower):
    unext = upper[ui + 1]
    if lnext[0] == unext[0]:
        xnext = lnext[0]
        h2 = unext[1] - lnext[1]
        li += 1
        ui += 1
    elif lnext[0] < upper[ui + 1][0]:
        xnext = lnext[0]
        frac = 1.0 * (xnext - upper[ui][0]) / (unext[0] - upper[ui][0])
        yupper = upper[ui][1] + frac * (unext[1] - upper[ui][1])
        h2 = yupper - lnext[1]
        li += 1
    else:
        xnext = unext[0]
        frac = 1.0 * (xnext - lower[li][0]) / (lnext[0] - lower[li][0])
        ylower = lower[li][1] + frac * (lnext[1] - lower[li][1])
        h2 = unext[1] - ylower
        ui += 1
    da = (xnext - x) * (h1 + h2) / 2.0
    total_area += da
    return li


def func_2111e5ba4f1a469bb9b28232b4b5497e(h1, lnext, x, upper, lower):
    unext = upper[ui + 1]
    if lnext[0] == unext[0]:
        xnext = lnext[0]
        h2 = unext[1] - lnext[1]
        li += 1
        ui += 1
    elif lnext[0] < upper[ui + 1][0]:
        xnext = lnext[0]
        frac = 1.0 * (xnext - upper[ui][0]) / (unext[0] - upper[ui][0])
        yupper = upper[ui][1] + frac * (unext[1] - upper[ui][1])
        h2 = yupper - lnext[1]
        li += 1
    else:
        xnext = unext[0]
        frac = 1.0 * (xnext - lower[li][0]) / (lnext[0] - lower[li][0])
        ylower = lower[li][1] + frac * (lnext[1] - lower[li][1])
        h2 = unext[1] - ylower
        ui += 1
    da = (xnext - x) * (h1 + h2) / 2.0
    total_area += da
    return ylower


def func_c74b93e37ebf4077bcd0ffaff2e6c4f3(h1, lnext, x, upper, lower):
    unext = upper[ui + 1]
    if lnext[0] == unext[0]:
        xnext = lnext[0]
        h2 = unext[1] - lnext[1]
        li += 1
        ui += 1
    elif lnext[0] < upper[ui + 1][0]:
        xnext = lnext[0]
        frac = 1.0 * (xnext - upper[ui][0]) / (unext[0] - upper[ui][0])
        yupper = upper[ui][1] + frac * (unext[1] - upper[ui][1])
        h2 = yupper - lnext[1]
        li += 1
    else:
        xnext = unext[0]
        frac = 1.0 * (xnext - lower[li][0]) / (lnext[0] - lower[li][0])
        ylower = lower[li][1] + frac * (lnext[1] - lower[li][1])
        h2 = unext[1] - ylower
        ui += 1
    da = (xnext - x) * (h1 + h2) / 2.0
    total_area += da
    return frac


def func_fd737a6945304389927adce84b6803d5(h1, lnext, x, upper, lower):
    unext = upper[ui + 1]
    if lnext[0] == unext[0]:
        xnext = lnext[0]
        h2 = unext[1] - lnext[1]
        li += 1
        ui += 1
    elif lnext[0] < upper[ui + 1][0]:
        xnext = lnext[0]
        frac = 1.0 * (xnext - upper[ui][0]) / (unext[0] - upper[ui][0])
        yupper = upper[ui][1] + frac * (unext[1] - upper[ui][1])
        h2 = yupper - lnext[1]
        li += 1
    else:
        xnext = unext[0]
        frac = 1.0 * (xnext - lower[li][0]) / (lnext[0] - lower[li][0])
        ylower = lower[li][1] + frac * (lnext[1] - lower[li][1])
        h2 = unext[1] - ylower
        ui += 1
    da = (xnext - x) * (h1 + h2) / 2.0
    total_area += da
    return da


def func_6d3cc08b32fe45168cac5e02c1bdca37(h1, lnext, x, upper, lower):
    unext = upper[ui + 1]
    if lnext[0] == unext[0]:
        xnext = lnext[0]
        h2 = unext[1] - lnext[1]
        li += 1
        ui += 1
    elif lnext[0] < upper[ui + 1][0]:
        xnext = lnext[0]
        frac = 1.0 * (xnext - upper[ui][0]) / (unext[0] - upper[ui][0])
        yupper = upper[ui][1] + frac * (unext[1] - upper[ui][1])
        h2 = yupper - lnext[1]
        li += 1
    else:
        xnext = unext[0]
        frac = 1.0 * (xnext - lower[li][0]) / (lnext[0] - lower[li][0])
        ylower = lower[li][1] + frac * (lnext[1] - lower[li][1])
        h2 = unext[1] - ylower
        ui += 1
    da = (xnext - x) * (h1 + h2) / 2.0
    total_area += da
    return total_area


def func_1d594e5246014488a6ea6980943bd5f4(h1, lnext, x, upper, lower):
    unext = upper[ui + 1]
    if lnext[0] == unext[0]:
        xnext = lnext[0]
        h2 = unext[1] - lnext[1]
        li += 1
        ui += 1
    elif lnext[0] < upper[ui + 1][0]:
        xnext = lnext[0]
        frac = 1.0 * (xnext - upper[ui][0]) / (unext[0] - upper[ui][0])
        yupper = upper[ui][1] + frac * (unext[1] - upper[ui][1])
        h2 = yupper - lnext[1]
        li += 1
    else:
        xnext = unext[0]
        frac = 1.0 * (xnext - lower[li][0]) / (lnext[0] - lower[li][0])
        ylower = lower[li][1] + frac * (lnext[1] - lower[li][1])
        h2 = unext[1] - ylower
        ui += 1
    da = (xnext - x) * (h1 + h2) / 2.0
    total_area += da
    return yupper


def func_40bb7dd13ad840fc8c7f6e351fb4cc27(h1, lnext, x, upper, lower):
    unext = upper[ui + 1]
    if lnext[0] == unext[0]:
        xnext = lnext[0]
        h2 = unext[1] - lnext[1]
        li += 1
        ui += 1
    elif lnext[0] < upper[ui + 1][0]:
        xnext = lnext[0]
        frac = 1.0 * (xnext - upper[ui][0]) / (unext[0] - upper[ui][0])
        yupper = upper[ui][1] + frac * (unext[1] - upper[ui][1])
        h2 = yupper - lnext[1]
        li += 1
    else:
        xnext = unext[0]
        frac = 1.0 * (xnext - lower[li][0]) / (lnext[0] - lower[li][0])
        ylower = lower[li][1] + frac * (lnext[1] - lower[li][1])
        h2 = unext[1] - ylower
        ui += 1
    da = (xnext - x) * (h1 + h2) / 2.0
    total_area += da
    return unext


def func_ae4054885d99449dbb12781d8f3725d0(h1, lnext, x, upper, lower):
    unext = upper[ui + 1]
    if lnext[0] == unext[0]:
        xnext = lnext[0]
        h2 = unext[1] - lnext[1]
        li += 1
        ui += 1
    elif lnext[0] < upper[ui + 1][0]:
        xnext = lnext[0]
        frac = 1.0 * (xnext - upper[ui][0]) / (unext[0] - upper[ui][0])
        yupper = upper[ui][1] + frac * (unext[1] - upper[ui][1])
        h2 = yupper - lnext[1]
        li += 1
    else:
        xnext = unext[0]
        frac = 1.0 * (xnext - lower[li][0]) / (lnext[0] - lower[li][0])
        ylower = lower[li][1] + frac * (lnext[1] - lower[li][1])
        h2 = unext[1] - ylower
        ui += 1
    da = (xnext - x) * (h1 + h2) / 2.0
    total_area += da
    return ui


def func_674cba3e0f8a490094d4fedcfded4503(h1, lnext, x, upper, lower):
    unext = upper[ui + 1]
    if lnext[0] == unext[0]:
        xnext = lnext[0]
        h2 = unext[1] - lnext[1]
        li += 1
        ui += 1
    elif lnext[0] < upper[ui + 1][0]:
        xnext = lnext[0]
        frac = 1.0 * (xnext - upper[ui][0]) / (unext[0] - upper[ui][0])
        yupper = upper[ui][1] + frac * (unext[1] - upper[ui][1])
        h2 = yupper - lnext[1]
        li += 1
    else:
        xnext = unext[0]
        frac = 1.0 * (xnext - lower[li][0]) / (lnext[0] - lower[li][0])
        ylower = lower[li][1] + frac * (lnext[1] - lower[li][1])
        h2 = unext[1] - ylower
        ui += 1
    da = (xnext - x) * (h1 + h2) / 2.0
    total_area += da
    return h2


def func_79f7b17916aa4902b0e8eed3cdabcc9a(h1, lnext, x, upper, lower):
    unext = upper[ui + 1]
    if lnext[0] == unext[0]:
        xnext = lnext[0]
        h2 = unext[1] - lnext[1]
        li += 1
        ui += 1
    elif lnext[0] < upper[ui + 1][0]:
        xnext = lnext[0]
        frac = 1.0 * (xnext - upper[ui][0]) / (unext[0] - upper[ui][0])
        yupper = upper[ui][1] + frac * (unext[1] - upper[ui][1])
        h2 = yupper - lnext[1]
        li += 1
    else:
        xnext = unext[0]
        frac = 1.0 * (xnext - lower[li][0]) / (lnext[0] - lower[li][0])
        ylower = lower[li][1] + frac * (lnext[1] - lower[li][1])
        h2 = unext[1] - ylower
        ui += 1
    da = (xnext - x) * (h1 + h2) / 2.0
    total_area += da
    return xnext


def func_6a0b75f8ebe64ba2b736cf5b8bd7f0cf(h1, x, upper, lower):
    lnext = lower[li + 1]
    unext = upper[ui + 1]
    if lnext[0] == unext[0]:
        xnext = lnext[0]
        h2 = unext[1] - lnext[1]
        li += 1
        ui += 1
    elif lnext[0] < upper[ui + 1][0]:
        xnext = lnext[0]
        frac = 1.0 * (xnext - upper[ui][0]) / (unext[0] - upper[ui][0])
        yupper = upper[ui][1] + frac * (unext[1] - upper[ui][1])
        h2 = yupper - lnext[1]
        li += 1
    else:
        xnext = unext[0]
        frac = 1.0 * (xnext - lower[li][0]) / (lnext[0] - lower[li][0])
        ylower = lower[li][1] + frac * (lnext[1] - lower[li][1])
        h2 = unext[1] - ylower
        ui += 1
    da = (xnext - x) * (h1 + h2) / 2.0
    total_area += da
    return h2


def func_3109ad9c27e24972bcfcdc25bb4a4433(h1, x, upper, lower):
    lnext = lower[li + 1]
    unext = upper[ui + 1]
    if lnext[0] == unext[0]:
        xnext = lnext[0]
        h2 = unext[1] - lnext[1]
        li += 1
        ui += 1
    elif lnext[0] < upper[ui + 1][0]:
        xnext = lnext[0]
        frac = 1.0 * (xnext - upper[ui][0]) / (unext[0] - upper[ui][0])
        yupper = upper[ui][1] + frac * (unext[1] - upper[ui][1])
        h2 = yupper - lnext[1]
        li += 1
    else:
        xnext = unext[0]
        frac = 1.0 * (xnext - lower[li][0]) / (lnext[0] - lower[li][0])
        ylower = lower[li][1] + frac * (lnext[1] - lower[li][1])
        h2 = unext[1] - ylower
        ui += 1
    da = (xnext - x) * (h1 + h2) / 2.0
    total_area += da
    return frac


def func_9a1ca9b9e57d4eab9baeb405c0efc2a8(h1, x, upper, lower):
    lnext = lower[li + 1]
    unext = upper[ui + 1]
    if lnext[0] == unext[0]:
        xnext = lnext[0]
        h2 = unext[1] - lnext[1]
        li += 1
        ui += 1
    elif lnext[0] < upper[ui + 1][0]:
        xnext = lnext[0]
        frac = 1.0 * (xnext - upper[ui][0]) / (unext[0] - upper[ui][0])
        yupper = upper[ui][1] + frac * (unext[1] - upper[ui][1])
        h2 = yupper - lnext[1]
        li += 1
    else:
        xnext = unext[0]
        frac = 1.0 * (xnext - lower[li][0]) / (lnext[0] - lower[li][0])
        ylower = lower[li][1] + frac * (lnext[1] - lower[li][1])
        h2 = unext[1] - ylower
        ui += 1
    da = (xnext - x) * (h1 + h2) / 2.0
    total_area += da
    return da


def func_1ca492d5cc394879b93de0b5160d728b(h1, x, upper, lower):
    lnext = lower[li + 1]
    unext = upper[ui + 1]
    if lnext[0] == unext[0]:
        xnext = lnext[0]
        h2 = unext[1] - lnext[1]
        li += 1
        ui += 1
    elif lnext[0] < upper[ui + 1][0]:
        xnext = lnext[0]
        frac = 1.0 * (xnext - upper[ui][0]) / (unext[0] - upper[ui][0])
        yupper = upper[ui][1] + frac * (unext[1] - upper[ui][1])
        h2 = yupper - lnext[1]
        li += 1
    else:
        xnext = unext[0]
        frac = 1.0 * (xnext - lower[li][0]) / (lnext[0] - lower[li][0])
        ylower = lower[li][1] + frac * (lnext[1] - lower[li][1])
        h2 = unext[1] - ylower
        ui += 1
    da = (xnext - x) * (h1 + h2) / 2.0
    total_area += da
    return xnext


def func_abe2d6e816ff48baab4d7f67e893d9d7(h1, x, upper, lower):
    lnext = lower[li + 1]
    unext = upper[ui + 1]
    if lnext[0] == unext[0]:
        xnext = lnext[0]
        h2 = unext[1] - lnext[1]
        li += 1
        ui += 1
    elif lnext[0] < upper[ui + 1][0]:
        xnext = lnext[0]
        frac = 1.0 * (xnext - upper[ui][0]) / (unext[0] - upper[ui][0])
        yupper = upper[ui][1] + frac * (unext[1] - upper[ui][1])
        h2 = yupper - lnext[1]
        li += 1
    else:
        xnext = unext[0]
        frac = 1.0 * (xnext - lower[li][0]) / (lnext[0] - lower[li][0])
        ylower = lower[li][1] + frac * (lnext[1] - lower[li][1])
        h2 = unext[1] - ylower
        ui += 1
    da = (xnext - x) * (h1 + h2) / 2.0
    total_area += da
    return lnext


def func_2e562a76098d445c9b9937b03e5238c2(h1, x, upper, lower):
    lnext = lower[li + 1]
    unext = upper[ui + 1]
    if lnext[0] == unext[0]:
        xnext = lnext[0]
        h2 = unext[1] - lnext[1]
        li += 1
        ui += 1
    elif lnext[0] < upper[ui + 1][0]:
        xnext = lnext[0]
        frac = 1.0 * (xnext - upper[ui][0]) / (unext[0] - upper[ui][0])
        yupper = upper[ui][1] + frac * (unext[1] - upper[ui][1])
        h2 = yupper - lnext[1]
        li += 1
    else:
        xnext = unext[0]
        frac = 1.0 * (xnext - lower[li][0]) / (lnext[0] - lower[li][0])
        ylower = lower[li][1] + frac * (lnext[1] - lower[li][1])
        h2 = unext[1] - ylower
        ui += 1
    da = (xnext - x) * (h1 + h2) / 2.0
    total_area += da
    return ui


def func_58ab29c950394b6aacfd905bad35418e(h1, x, upper, lower):
    lnext = lower[li + 1]
    unext = upper[ui + 1]
    if lnext[0] == unext[0]:
        xnext = lnext[0]
        h2 = unext[1] - lnext[1]
        li += 1
        ui += 1
    elif lnext[0] < upper[ui + 1][0]:
        xnext = lnext[0]
        frac = 1.0 * (xnext - upper[ui][0]) / (unext[0] - upper[ui][0])
        yupper = upper[ui][1] + frac * (unext[1] - upper[ui][1])
        h2 = yupper - lnext[1]
        li += 1
    else:
        xnext = unext[0]
        frac = 1.0 * (xnext - lower[li][0]) / (lnext[0] - lower[li][0])
        ylower = lower[li][1] + frac * (lnext[1] - lower[li][1])
        h2 = unext[1] - ylower
        ui += 1
    da = (xnext - x) * (h1 + h2) / 2.0
    total_area += da
    return total_area


def func_32ce7ba25cc4443f9a1a09dde8286b6f(h1, x, upper, lower):
    lnext = lower[li + 1]
    unext = upper[ui + 1]
    if lnext[0] == unext[0]:
        xnext = lnext[0]
        h2 = unext[1] - lnext[1]
        li += 1
        ui += 1
    elif lnext[0] < upper[ui + 1][0]:
        xnext = lnext[0]
        frac = 1.0 * (xnext - upper[ui][0]) / (unext[0] - upper[ui][0])
        yupper = upper[ui][1] + frac * (unext[1] - upper[ui][1])
        h2 = yupper - lnext[1]
        li += 1
    else:
        xnext = unext[0]
        frac = 1.0 * (xnext - lower[li][0]) / (lnext[0] - lower[li][0])
        ylower = lower[li][1] + frac * (lnext[1] - lower[li][1])
        h2 = unext[1] - ylower
        ui += 1
    da = (xnext - x) * (h1 + h2) / 2.0
    total_area += da
    return yupper


def func_29abe66ef4f8413aacff03ad34af3c32(h1, x, upper, lower):
    lnext = lower[li + 1]
    unext = upper[ui + 1]
    if lnext[0] == unext[0]:
        xnext = lnext[0]
        h2 = unext[1] - lnext[1]
        li += 1
        ui += 1
    elif lnext[0] < upper[ui + 1][0]:
        xnext = lnext[0]
        frac = 1.0 * (xnext - upper[ui][0]) / (unext[0] - upper[ui][0])
        yupper = upper[ui][1] + frac * (unext[1] - upper[ui][1])
        h2 = yupper - lnext[1]
        li += 1
    else:
        xnext = unext[0]
        frac = 1.0 * (xnext - lower[li][0]) / (lnext[0] - lower[li][0])
        ylower = lower[li][1] + frac * (lnext[1] - lower[li][1])
        h2 = unext[1] - ylower
        ui += 1
    da = (xnext - x) * (h1 + h2) / 2.0
    total_area += da
    return unext


def func_500a996cf87a4ccdb5445bea8e959c3d(h1, x, upper, lower):
    lnext = lower[li + 1]
    unext = upper[ui + 1]
    if lnext[0] == unext[0]:
        xnext = lnext[0]
        h2 = unext[1] - lnext[1]
        li += 1
        ui += 1
    elif lnext[0] < upper[ui + 1][0]:
        xnext = lnext[0]
        frac = 1.0 * (xnext - upper[ui][0]) / (unext[0] - upper[ui][0])
        yupper = upper[ui][1] + frac * (unext[1] - upper[ui][1])
        h2 = yupper - lnext[1]
        li += 1
    else:
        xnext = unext[0]
        frac = 1.0 * (xnext - lower[li][0]) / (lnext[0] - lower[li][0])
        ylower = lower[li][1] + frac * (lnext[1] - lower[li][1])
        h2 = unext[1] - ylower
        ui += 1
    da = (xnext - x) * (h1 + h2) / 2.0
    total_area += da
    return ylower


def func_50c5d3ad9c624b7fb6915fc3242e9d63(h1, x, upper, lower):
    lnext = lower[li + 1]
    unext = upper[ui + 1]
    if lnext[0] == unext[0]:
        xnext = lnext[0]
        h2 = unext[1] - lnext[1]
        li += 1
        ui += 1
    elif lnext[0] < upper[ui + 1][0]:
        xnext = lnext[0]
        frac = 1.0 * (xnext - upper[ui][0]) / (unext[0] - upper[ui][0])
        yupper = upper[ui][1] + frac * (unext[1] - upper[ui][1])
        h2 = yupper - lnext[1]
        li += 1
    else:
        xnext = unext[0]
        frac = 1.0 * (xnext - lower[li][0]) / (lnext[0] - lower[li][0])
        ylower = lower[li][1] + frac * (lnext[1] - lower[li][1])
        h2 = unext[1] - ylower
        ui += 1
    da = (xnext - x) * (h1 + h2) / 2.0
    total_area += da
    return li


def func_8ce3b3b22c034fe5afddca8240c749e5(h1, width, h2):
    qa = (h2 - h1) / (2 * width)
    qb = h1
    return qa


def func_0f7e2b6cf7ed441ba38b83f395a6fdb0(h1, width, h2):
    qa = (h2 - h1) / (2 * width)
    qb = h1
    return qb


def func_063e6a1f1c3d47fb985cd173eefe76ab(h1, area_from_left):
    qb = h1
    qc = -area_from_left
    return qc


def func_641693850ad746c38c54a3aff9f17b57(h1, area_from_left):
    qb = h1
    qc = -area_from_left
    return qb


def func_b8335e23cf8c469782dfd8e754c6f3dd(qa, qb, area_from_left):
    qc = -area_from_left
    x = (-qb + (qb ** 2 - 4 * qa * qc) ** 0.5) / (2 * qa)
    return qc


def func_c162cc96dcae4473873434d02787c09e(qa, qb, area_from_left):
    qc = -area_from_left
    x = (-qb + (qb ** 2 - 4 * qa * qc) ** 0.5) / (2 * qa)
    return x


def func_097ea418d65649cebd6fdd24741a9090(h1, width, h2, area_from_left):
    qa = (h2 - h1) / (2 * width)
    qb = h1
    qc = -area_from_left
    return qc


def func_3e725aae5c66455dac3fbf5cd94f8cba(h1, width, h2, area_from_left):
    qa = (h2 - h1) / (2 * width)
    qb = h1
    qc = -area_from_left
    return qa


def func_71637a6ffb12471fbd846d119b4e9e7f(h1, width, h2, area_from_left):
    qa = (h2 - h1) / (2 * width)
    qb = h1
    qc = -area_from_left
    return qb


def func_bfa672e3956940e2bb3b0029ea05bb4e(h1, qa, area_from_left):
    qb = h1
    qc = -area_from_left
    x = (-qb + (qb ** 2 - 4 * qa * qc) ** 0.5) / (2 * qa)
    return qc


def func_591dfeb184af440d97b6f582b8dd96b2(h1, qa, area_from_left):
    qb = h1
    qc = -area_from_left
    x = (-qb + (qb ** 2 - 4 * qa * qc) ** 0.5) / (2 * qa)
    return x


def func_809e8586f3aa49d69871a8da17e6d71c(h1, qa, area_from_left):
    qb = h1
    qc = -area_from_left
    x = (-qb + (qb ** 2 - 4 * qa * qc) ** 0.5) / (2 * qa)
    return qb


def func_24d864684457482b9896b3aa2a777e28(h1, width, h2, area_from_left):
    qa = (h2 - h1) / (2 * width)
    qb = h1
    qc = -area_from_left
    x = (-qb + (qb ** 2 - 4 * qa * qc) ** 0.5) / (2 * qa)
    return qb


def func_cea490766c9f41c99e52867ff700d1f7(h1, width, h2, area_from_left):
    qa = (h2 - h1) / (2 * width)
    qb = h1
    qc = -area_from_left
    x = (-qb + (qb ** 2 - 4 * qa * qc) ** 0.5) / (2 * qa)
    return qa


def func_75c1bd82a6c84e639473701f7a4d9fff(h1, width, h2, area_from_left):
    qa = (h2 - h1) / (2 * width)
    qb = h1
    qc = -area_from_left
    x = (-qb + (qb ** 2 - 4 * qa * qc) ** 0.5) / (2 * qa)
    return qc


def func_ded69eb52401423ab4743a678fff2214(h1, width, h2, area_from_left):
    qa = (h2 - h1) / (2 * width)
    qb = h1
    qc = -area_from_left
    x = (-qb + (qb ** 2 - 4 * qa * qc) ** 0.5) / (2 * qa)
    return x


def func_21305fdcfb1f4efb996244c0afb1b821(x2, x1, needed):
    area_from_left += needed
    width = x2 - x1
    return width


def func_467b01df18a3413d91dacbf154e92354(x2, x1, needed):
    area_from_left += needed
    width = x2 - x1
    return area_from_left


def func_f52ffcd319474fff811d317c77085b56(x2, h1, x1, h2, area_from_left):
    width = x2 - x1
    if h1 == h2:
        x = area_from_left / h1
    else:
        qa = (h2 - h1) / (2 * width)
        qb = h1
        qc = -area_from_left
        x = (-qb + (qb ** 2 - 4 * qa * qc) ** 0.5) / (2 * qa)
    return x


def func_750d0581ee0749b78002c50583fae4cd(x2, h1, x1, h2, area_from_left):
    width = x2 - x1
    if h1 == h2:
        x = area_from_left / h1
    else:
        qa = (h2 - h1) / (2 * width)
        qb = h1
        qc = -area_from_left
        x = (-qb + (qb ** 2 - 4 * qa * qc) ** 0.5) / (2 * qa)
    return qa


def func_f33f93ac123f499ca0d8e1ea7441802b(x2, h1, x1, h2, area_from_left):
    width = x2 - x1
    if h1 == h2:
        x = area_from_left / h1
    else:
        qa = (h2 - h1) / (2 * width)
        qb = h1
        qc = -area_from_left
        x = (-qb + (qb ** 2 - 4 * qa * qc) ** 0.5) / (2 * qa)
    return qb


def func_fe219e02c3264be4b08057a0b8750545(x2, h1, x1, h2, area_from_left):
    width = x2 - x1
    if h1 == h2:
        x = area_from_left / h1
    else:
        qa = (h2 - h1) / (2 * width)
        qb = h1
        qc = -area_from_left
        x = (-qb + (qb ** 2 - 4 * qa * qc) ** 0.5) / (2 * qa)
    return qc


def func_89f0687928fb45cea0fd47592f46f5e3(x2, h1, x1, h2, area_from_left):
    width = x2 - x1
    if h1 == h2:
        x = area_from_left / h1
    else:
        qa = (h2 - h1) / (2 * width)
        qb = h1
        qc = -area_from_left
        x = (-qb + (qb ** 2 - 4 * qa * qc) ** 0.5) / (2 * qa)
    return width


def func_6501e80d1a6b46c9926303a7848e9a89(h1, x1, cuts, width, h2,
    area_from_left):
    if h1 == h2:
        x = area_from_left / h1
    else:
        qa = (h2 - h1) / (2 * width)
        qb = h1
        qc = -area_from_left
        x = (-qb + (qb ** 2 - 4 * qa * qc) ** 0.5) / (2 * qa)
    cuts.append(x1 + x)
    return qa


def func_8252b23c5cc848d58b34c1418324f39e(h1, x1, cuts, width, h2,
    area_from_left):
    if h1 == h2:
        x = area_from_left / h1
    else:
        qa = (h2 - h1) / (2 * width)
        qb = h1
        qc = -area_from_left
        x = (-qb + (qb ** 2 - 4 * qa * qc) ** 0.5) / (2 * qa)
    cuts.append(x1 + x)
    return qc


def func_2febcfc5eb4b40d88ec1890dab729ab7(h1, x1, cuts, width, h2,
    area_from_left):
    if h1 == h2:
        x = area_from_left / h1
    else:
        qa = (h2 - h1) / (2 * width)
        qb = h1
        qc = -area_from_left
        x = (-qb + (qb ** 2 - 4 * qa * qc) ** 0.5) / (2 * qa)
    cuts.append(x1 + x)
    return qb


def func_7890c85df6594a09ac7145f385c14ebb(h1, x1, cuts, width, h2,
    area_from_left):
    if h1 == h2:
        x = area_from_left / h1
    else:
        qa = (h2 - h1) / (2 * width)
        qb = h1
        qc = -area_from_left
        x = (-qb + (qb ** 2 - 4 * qa * qc) ** 0.5) / (2 * qa)
    cuts.append(x1 + x)
    return x


def func_d1a968f16ae348ada0af5e47eeed907f(npieces, needed, cuts):
    if len(cuts) == npieces - 1:
        return cuts
    segment_remaining -= needed
    return segment_remaining


def func_fc1662353c8e42ada2ae40f5f235037b(needed, size):
    segment_remaining -= needed
    needed = size
    return segment_remaining


def func_6eeb08e37b2149dcae22416ca6b0d45d(x2, h1, x1, needed, h2):
    area_from_left += needed
    width = x2 - x1
    if h1 == h2:
        x = area_from_left / h1
    else:
        qa = (h2 - h1) / (2 * width)
        qb = h1
        qc = -area_from_left
        x = (-qb + (qb ** 2 - 4 * qa * qc) ** 0.5) / (2 * qa)
    return x


def func_6ed1b9ed2ea34ce591734e8cef98a0ae(x2, h1, x1, needed, h2):
    area_from_left += needed
    width = x2 - x1
    if h1 == h2:
        x = area_from_left / h1
    else:
        qa = (h2 - h1) / (2 * width)
        qb = h1
        qc = -area_from_left
        x = (-qb + (qb ** 2 - 4 * qa * qc) ** 0.5) / (2 * qa)
    return qc


def func_b04856bee7004a939fdcf79dce51ec0c(x2, h1, x1, needed, h2):
    area_from_left += needed
    width = x2 - x1
    if h1 == h2:
        x = area_from_left / h1
    else:
        qa = (h2 - h1) / (2 * width)
        qb = h1
        qc = -area_from_left
        x = (-qb + (qb ** 2 - 4 * qa * qc) ** 0.5) / (2 * qa)
    return width


def func_4cf81b5e62f848318a0db6531bde69b1(x2, h1, x1, needed, h2):
    area_from_left += needed
    width = x2 - x1
    if h1 == h2:
        x = area_from_left / h1
    else:
        qa = (h2 - h1) / (2 * width)
        qb = h1
        qc = -area_from_left
        x = (-qb + (qb ** 2 - 4 * qa * qc) ** 0.5) / (2 * qa)
    return qa


def func_fcaff677979e4480b67b7f96a41f8699(x2, h1, x1, needed, h2):
    area_from_left += needed
    width = x2 - x1
    if h1 == h2:
        x = area_from_left / h1
    else:
        qa = (h2 - h1) / (2 * width)
        qb = h1
        qc = -area_from_left
        x = (-qb + (qb ** 2 - 4 * qa * qc) ** 0.5) / (2 * qa)
    return qb


def func_5a2a850d23e0440f92d44c2b5c05463a(x2, h1, x1, needed, h2):
    area_from_left += needed
    width = x2 - x1
    if h1 == h2:
        x = area_from_left / h1
    else:
        qa = (h2 - h1) / (2 * width)
        qb = h1
        qc = -area_from_left
        x = (-qb + (qb ** 2 - 4 * qa * qc) ** 0.5) / (2 * qa)
    return area_from_left


def func_78f0acbd0033447bab715c209997acda(x2, h1, x1, cuts, h2, area_from_left
    ):
    width = x2 - x1
    if h1 == h2:
        x = area_from_left / h1
    else:
        qa = (h2 - h1) / (2 * width)
        qb = h1
        qc = -area_from_left
        x = (-qb + (qb ** 2 - 4 * qa * qc) ** 0.5) / (2 * qa)
    cuts.append(x1 + x)
    return x


def func_c9b03e0617ce45559abf8def18ab4183(x2, h1, x1, cuts, h2, area_from_left
    ):
    width = x2 - x1
    if h1 == h2:
        x = area_from_left / h1
    else:
        qa = (h2 - h1) / (2 * width)
        qb = h1
        qc = -area_from_left
        x = (-qb + (qb ** 2 - 4 * qa * qc) ** 0.5) / (2 * qa)
    cuts.append(x1 + x)
    return width


def func_16a5c07c60284c9980d5f8125172ea11(x2, h1, x1, cuts, h2, area_from_left
    ):
    width = x2 - x1
    if h1 == h2:
        x = area_from_left / h1
    else:
        qa = (h2 - h1) / (2 * width)
        qb = h1
        qc = -area_from_left
        x = (-qb + (qb ** 2 - 4 * qa * qc) ** 0.5) / (2 * qa)
    cuts.append(x1 + x)
    return qa


def func_d726edaa858f4badac586c1786dc889c(x2, h1, x1, cuts, h2, area_from_left
    ):
    width = x2 - x1
    if h1 == h2:
        x = area_from_left / h1
    else:
        qa = (h2 - h1) / (2 * width)
        qb = h1
        qc = -area_from_left
        x = (-qb + (qb ** 2 - 4 * qa * qc) ** 0.5) / (2 * qa)
    cuts.append(x1 + x)
    return qb


def func_4ddb0a5ac70b48dea94cc3ab62e522d3(x2, h1, x1, cuts, h2, area_from_left
    ):
    width = x2 - x1
    if h1 == h2:
        x = area_from_left / h1
    else:
        qa = (h2 - h1) / (2 * width)
        qb = h1
        qc = -area_from_left
        x = (-qb + (qb ** 2 - 4 * qa * qc) ** 0.5) / (2 * qa)
    cuts.append(x1 + x)
    return qc


def func_834cfa2585814a7cb1b386cc48d83985(npieces, h1, x1, cuts, width, h2,
    area_from_left):
    if h1 == h2:
        x = area_from_left / h1
    else:
        qa = (h2 - h1) / (2 * width)
        qb = h1
        qc = -area_from_left
        x = (-qb + (qb ** 2 - 4 * qa * qc) ** 0.5) / (2 * qa)
    cuts.append(x1 + x)
    if len(cuts) == npieces - 1:
        return cuts
    return x


def func_0839050df110409ca4b785284242da82(npieces, h1, x1, cuts, width, h2,
    area_from_left):
    if h1 == h2:
        x = area_from_left / h1
    else:
        qa = (h2 - h1) / (2 * width)
        qb = h1
        qc = -area_from_left
        x = (-qb + (qb ** 2 - 4 * qa * qc) ** 0.5) / (2 * qa)
    cuts.append(x1 + x)
    if len(cuts) == npieces - 1:
        return cuts
    return qc


def func_c84ead4213094502997b62c9e78e5bf4(npieces, h1, x1, cuts, width, h2,
    area_from_left):
    if h1 == h2:
        x = area_from_left / h1
    else:
        qa = (h2 - h1) / (2 * width)
        qb = h1
        qc = -area_from_left
        x = (-qb + (qb ** 2 - 4 * qa * qc) ** 0.5) / (2 * qa)
    cuts.append(x1 + x)
    if len(cuts) == npieces - 1:
        return cuts
    return qb


def func_bb26ed6d8fed49e8ae0a3303287826b4(npieces, h1, x1, cuts, width, h2,
    area_from_left):
    if h1 == h2:
        x = area_from_left / h1
    else:
        qa = (h2 - h1) / (2 * width)
        qb = h1
        qc = -area_from_left
        x = (-qb + (qb ** 2 - 4 * qa * qc) ** 0.5) / (2 * qa)
    cuts.append(x1 + x)
    if len(cuts) == npieces - 1:
        return cuts
    return qa


def func_541a6d6c48714d57a565e920ff237fbb(npieces, x1, needed, cuts, x):
    cuts.append(x1 + x)
    if len(cuts) == npieces - 1:
        return cuts
    segment_remaining -= needed
    return segment_remaining


def func_437f2bce8b9c425eab5b7c31c6bdf1f6(npieces, needed, cuts, size):
    if len(cuts) == npieces - 1:
        return cuts
    segment_remaining -= needed
    needed = size
    return segment_remaining


def func_b35cf0bc6f27490ca803c15c9a48a647(x2, h1, x1, needed, cuts, h2):
    area_from_left += needed
    width = x2 - x1
    if h1 == h2:
        x = area_from_left / h1
    else:
        qa = (h2 - h1) / (2 * width)
        qb = h1
        qc = -area_from_left
        x = (-qb + (qb ** 2 - 4 * qa * qc) ** 0.5) / (2 * qa)
    cuts.append(x1 + x)
    return qb


def func_80e2a24213b74c8a92fc9352325ee9ca(x2, h1, x1, needed, cuts, h2):
    area_from_left += needed
    width = x2 - x1
    if h1 == h2:
        x = area_from_left / h1
    else:
        qa = (h2 - h1) / (2 * width)
        qb = h1
        qc = -area_from_left
        x = (-qb + (qb ** 2 - 4 * qa * qc) ** 0.5) / (2 * qa)
    cuts.append(x1 + x)
    return area_from_left


def func_c8860072b1244b278665abb12d7ce971(x2, h1, x1, needed, cuts, h2):
    area_from_left += needed
    width = x2 - x1
    if h1 == h2:
        x = area_from_left / h1
    else:
        qa = (h2 - h1) / (2 * width)
        qb = h1
        qc = -area_from_left
        x = (-qb + (qb ** 2 - 4 * qa * qc) ** 0.5) / (2 * qa)
    cuts.append(x1 + x)
    return x


def func_38106e5652be4914ab97ab09a546d918(x2, h1, x1, needed, cuts, h2):
    area_from_left += needed
    width = x2 - x1
    if h1 == h2:
        x = area_from_left / h1
    else:
        qa = (h2 - h1) / (2 * width)
        qb = h1
        qc = -area_from_left
        x = (-qb + (qb ** 2 - 4 * qa * qc) ** 0.5) / (2 * qa)
    cuts.append(x1 + x)
    return qa


def func_338ab5313b614703afc83b89e228792c(x2, h1, x1, needed, cuts, h2):
    area_from_left += needed
    width = x2 - x1
    if h1 == h2:
        x = area_from_left / h1
    else:
        qa = (h2 - h1) / (2 * width)
        qb = h1
        qc = -area_from_left
        x = (-qb + (qb ** 2 - 4 * qa * qc) ** 0.5) / (2 * qa)
    cuts.append(x1 + x)
    return width


def func_b1779792e4b14021b3074b9b1958e672(x2, h1, x1, needed, cuts, h2):
    area_from_left += needed
    width = x2 - x1
    if h1 == h2:
        x = area_from_left / h1
    else:
        qa = (h2 - h1) / (2 * width)
        qb = h1
        qc = -area_from_left
        x = (-qb + (qb ** 2 - 4 * qa * qc) ** 0.5) / (2 * qa)
    cuts.append(x1 + x)
    return qc


def func_9a234a88564b45e59492cbd1c4e4d1fe(npieces, x2, h1, x1, cuts, h2,
    area_from_left):
    width = x2 - x1
    if h1 == h2:
        x = area_from_left / h1
    else:
        qa = (h2 - h1) / (2 * width)
        qb = h1
        qc = -area_from_left
        x = (-qb + (qb ** 2 - 4 * qa * qc) ** 0.5) / (2 * qa)
    cuts.append(x1 + x)
    if len(cuts) == npieces - 1:
        return cuts
    return qa


def func_f33aea0f852349d28b8188afe4c04085(npieces, x2, h1, x1, cuts, h2,
    area_from_left):
    width = x2 - x1
    if h1 == h2:
        x = area_from_left / h1
    else:
        qa = (h2 - h1) / (2 * width)
        qb = h1
        qc = -area_from_left
        x = (-qb + (qb ** 2 - 4 * qa * qc) ** 0.5) / (2 * qa)
    cuts.append(x1 + x)
    if len(cuts) == npieces - 1:
        return cuts
    return qb


def func_a4ee46be56e54ae88c4d1c6633dfbd08(npieces, x2, h1, x1, cuts, h2,
    area_from_left):
    width = x2 - x1
    if h1 == h2:
        x = area_from_left / h1
    else:
        qa = (h2 - h1) / (2 * width)
        qb = h1
        qc = -area_from_left
        x = (-qb + (qb ** 2 - 4 * qa * qc) ** 0.5) / (2 * qa)
    cuts.append(x1 + x)
    if len(cuts) == npieces - 1:
        return cuts
    return width


def func_4626c1a5fb504eebabe8278b4e06bb53(npieces, x2, h1, x1, cuts, h2,
    area_from_left):
    width = x2 - x1
    if h1 == h2:
        x = area_from_left / h1
    else:
        qa = (h2 - h1) / (2 * width)
        qb = h1
        qc = -area_from_left
        x = (-qb + (qb ** 2 - 4 * qa * qc) ** 0.5) / (2 * qa)
    cuts.append(x1 + x)
    if len(cuts) == npieces - 1:
        return cuts
    return qc


def func_ffbf2f23e0744325b408595e7734228d(npieces, x2, h1, x1, cuts, h2,
    area_from_left):
    width = x2 - x1
    if h1 == h2:
        x = area_from_left / h1
    else:
        qa = (h2 - h1) / (2 * width)
        qb = h1
        qc = -area_from_left
        x = (-qb + (qb ** 2 - 4 * qa * qc) ** 0.5) / (2 * qa)
    cuts.append(x1 + x)
    if len(cuts) == npieces - 1:
        return cuts
    return x


def func_835ae36d993a43c2a12a898ad89bad72(npieces, h1, x1, needed, cuts,
    width, h2, area_from_left):
    if h1 == h2:
        x = area_from_left / h1
    else:
        qa = (h2 - h1) / (2 * width)
        qb = h1
        qc = -area_from_left
        x = (-qb + (qb ** 2 - 4 * qa * qc) ** 0.5) / (2 * qa)
    cuts.append(x1 + x)
    if len(cuts) == npieces - 1:
        return cuts
    segment_remaining -= needed
    return qc


def func_1a4cb22c294f4d4f91ee51b4c9e1ea32(npieces, h1, x1, needed, cuts,
    width, h2, area_from_left):
    if h1 == h2:
        x = area_from_left / h1
    else:
        qa = (h2 - h1) / (2 * width)
        qb = h1
        qc = -area_from_left
        x = (-qb + (qb ** 2 - 4 * qa * qc) ** 0.5) / (2 * qa)
    cuts.append(x1 + x)
    if len(cuts) == npieces - 1:
        return cuts
    segment_remaining -= needed
    return qb


def func_f758d34b877040f6ba124f09819bdb2e(npieces, h1, x1, needed, cuts,
    width, h2, area_from_left):
    if h1 == h2:
        x = area_from_left / h1
    else:
        qa = (h2 - h1) / (2 * width)
        qb = h1
        qc = -area_from_left
        x = (-qb + (qb ** 2 - 4 * qa * qc) ** 0.5) / (2 * qa)
    cuts.append(x1 + x)
    if len(cuts) == npieces - 1:
        return cuts
    segment_remaining -= needed
    return qa


def func_3e56129eb79c451f9cfa6ce75ae727e3(npieces, h1, x1, needed, cuts,
    width, h2, area_from_left):
    if h1 == h2:
        x = area_from_left / h1
    else:
        qa = (h2 - h1) / (2 * width)
        qb = h1
        qc = -area_from_left
        x = (-qb + (qb ** 2 - 4 * qa * qc) ** 0.5) / (2 * qa)
    cuts.append(x1 + x)
    if len(cuts) == npieces - 1:
        return cuts
    segment_remaining -= needed
    return x


def func_5743b8b1c0d04326838efb5bf32f2cf2(npieces, h1, x1, needed, cuts,
    width, h2, area_from_left):
    if h1 == h2:
        x = area_from_left / h1
    else:
        qa = (h2 - h1) / (2 * width)
        qb = h1
        qc = -area_from_left
        x = (-qb + (qb ** 2 - 4 * qa * qc) ** 0.5) / (2 * qa)
    cuts.append(x1 + x)
    if len(cuts) == npieces - 1:
        return cuts
    segment_remaining -= needed
    return segment_remaining


def func_3b629dcbfc5845d9a9ca90abbd8721b9(npieces, x1, needed, cuts, x, size):
    cuts.append(x1 + x)
    if len(cuts) == npieces - 1:
        return cuts
    segment_remaining -= needed
    needed = size
    return segment_remaining


def func_7a440e311183498083e397b0eb2244e3(npieces, x2, h1, x1, needed, cuts, h2
    ):
    area_from_left += needed
    width = x2 - x1
    if h1 == h2:
        x = area_from_left / h1
    else:
        qa = (h2 - h1) / (2 * width)
        qb = h1
        qc = -area_from_left
        x = (-qb + (qb ** 2 - 4 * qa * qc) ** 0.5) / (2 * qa)
    cuts.append(x1 + x)
    if len(cuts) == npieces - 1:
        return cuts
    return width


def func_1a1311b8b7fd4a86a647bc97823e356a(npieces, x2, h1, x1, needed, cuts, h2
    ):
    area_from_left += needed
    width = x2 - x1
    if h1 == h2:
        x = area_from_left / h1
    else:
        qa = (h2 - h1) / (2 * width)
        qb = h1
        qc = -area_from_left
        x = (-qb + (qb ** 2 - 4 * qa * qc) ** 0.5) / (2 * qa)
    cuts.append(x1 + x)
    if len(cuts) == npieces - 1:
        return cuts
    return qc


def func_8f76b3b27de0453d864bcf6e56987862(npieces, x2, h1, x1, needed, cuts, h2
    ):
    area_from_left += needed
    width = x2 - x1
    if h1 == h2:
        x = area_from_left / h1
    else:
        qa = (h2 - h1) / (2 * width)
        qb = h1
        qc = -area_from_left
        x = (-qb + (qb ** 2 - 4 * qa * qc) ** 0.5) / (2 * qa)
    cuts.append(x1 + x)
    if len(cuts) == npieces - 1:
        return cuts
    return qa


def func_9ecb3c1ebd5e4e72b617bc75a92602fe(npieces, x2, h1, x1, needed, cuts, h2
    ):
    area_from_left += needed
    width = x2 - x1
    if h1 == h2:
        x = area_from_left / h1
    else:
        qa = (h2 - h1) / (2 * width)
        qb = h1
        qc = -area_from_left
        x = (-qb + (qb ** 2 - 4 * qa * qc) ** 0.5) / (2 * qa)
    cuts.append(x1 + x)
    if len(cuts) == npieces - 1:
        return cuts
    return qb


def func_c009023cb67b48c7ad7c7feeba282489(npieces, x2, h1, x1, needed, cuts, h2
    ):
    area_from_left += needed
    width = x2 - x1
    if h1 == h2:
        x = area_from_left / h1
    else:
        qa = (h2 - h1) / (2 * width)
        qb = h1
        qc = -area_from_left
        x = (-qb + (qb ** 2 - 4 * qa * qc) ** 0.5) / (2 * qa)
    cuts.append(x1 + x)
    if len(cuts) == npieces - 1:
        return cuts
    return x


def func_7af19069e0134734959bcee7570d774a(npieces, x2, h1, x1, needed, cuts, h2
    ):
    area_from_left += needed
    width = x2 - x1
    if h1 == h2:
        x = area_from_left / h1
    else:
        qa = (h2 - h1) / (2 * width)
        qb = h1
        qc = -area_from_left
        x = (-qb + (qb ** 2 - 4 * qa * qc) ** 0.5) / (2 * qa)
    cuts.append(x1 + x)
    if len(cuts) == npieces - 1:
        return cuts
    return area_from_left


def func_dc590c9416364709904084e3a5cf7a61(npieces, x2, h1, x1, needed, cuts,
    h2, area_from_left):
    width = x2 - x1
    if h1 == h2:
        x = area_from_left / h1
    else:
        qa = (h2 - h1) / (2 * width)
        qb = h1
        qc = -area_from_left
        x = (-qb + (qb ** 2 - 4 * qa * qc) ** 0.5) / (2 * qa)
    cuts.append(x1 + x)
    if len(cuts) == npieces - 1:
        return cuts
    segment_remaining -= needed
    return x


def func_ef8c890370a4422f8793517624013ae3(npieces, x2, h1, x1, needed, cuts,
    h2, area_from_left):
    width = x2 - x1
    if h1 == h2:
        x = area_from_left / h1
    else:
        qa = (h2 - h1) / (2 * width)
        qb = h1
        qc = -area_from_left
        x = (-qb + (qb ** 2 - 4 * qa * qc) ** 0.5) / (2 * qa)
    cuts.append(x1 + x)
    if len(cuts) == npieces - 1:
        return cuts
    segment_remaining -= needed
    return qc


def func_257a5873ad6649c785a728ddee75d34b(npieces, x2, h1, x1, needed, cuts,
    h2, area_from_left):
    width = x2 - x1
    if h1 == h2:
        x = area_from_left / h1
    else:
        qa = (h2 - h1) / (2 * width)
        qb = h1
        qc = -area_from_left
        x = (-qb + (qb ** 2 - 4 * qa * qc) ** 0.5) / (2 * qa)
    cuts.append(x1 + x)
    if len(cuts) == npieces - 1:
        return cuts
    segment_remaining -= needed
    return qb


def func_667e2f0f3d2c41ffb22c9ccce2dbe6a5(npieces, x2, h1, x1, needed, cuts,
    h2, area_from_left):
    width = x2 - x1
    if h1 == h2:
        x = area_from_left / h1
    else:
        qa = (h2 - h1) / (2 * width)
        qb = h1
        qc = -area_from_left
        x = (-qb + (qb ** 2 - 4 * qa * qc) ** 0.5) / (2 * qa)
    cuts.append(x1 + x)
    if len(cuts) == npieces - 1:
        return cuts
    segment_remaining -= needed
    return segment_remaining


def func_b562552795194c5192ce8532bad36606(npieces, x2, h1, x1, needed, cuts,
    h2, area_from_left):
    width = x2 - x1
    if h1 == h2:
        x = area_from_left / h1
    else:
        qa = (h2 - h1) / (2 * width)
        qb = h1
        qc = -area_from_left
        x = (-qb + (qb ** 2 - 4 * qa * qc) ** 0.5) / (2 * qa)
    cuts.append(x1 + x)
    if len(cuts) == npieces - 1:
        return cuts
    segment_remaining -= needed
    return qa


def func_4ec5a9742de943d0a740619568f4491f(npieces, x2, h1, x1, needed, cuts,
    h2, area_from_left):
    width = x2 - x1
    if h1 == h2:
        x = area_from_left / h1
    else:
        qa = (h2 - h1) / (2 * width)
        qb = h1
        qc = -area_from_left
        x = (-qb + (qb ** 2 - 4 * qa * qc) ** 0.5) / (2 * qa)
    cuts.append(x1 + x)
    if len(cuts) == npieces - 1:
        return cuts
    segment_remaining -= needed
    return width


def func_7c51ad2fda184e099a3af419e0b3bbc4(npieces, h1, x1, needed, cuts,
    size, width, h2, area_from_left):
    if h1 == h2:
        x = area_from_left / h1
    else:
        qa = (h2 - h1) / (2 * width)
        qb = h1
        qc = -area_from_left
        x = (-qb + (qb ** 2 - 4 * qa * qc) ** 0.5) / (2 * qa)
    cuts.append(x1 + x)
    if len(cuts) == npieces - 1:
        return cuts
    segment_remaining -= needed
    needed = size
    return qa


def func_0fd033036cce4747b98ab299fe09230d(npieces, h1, x1, needed, cuts,
    size, width, h2, area_from_left):
    if h1 == h2:
        x = area_from_left / h1
    else:
        qa = (h2 - h1) / (2 * width)
        qb = h1
        qc = -area_from_left
        x = (-qb + (qb ** 2 - 4 * qa * qc) ** 0.5) / (2 * qa)
    cuts.append(x1 + x)
    if len(cuts) == npieces - 1:
        return cuts
    segment_remaining -= needed
    needed = size
    return qc


def func_7ad1bb5606e946a4b8d1f5c6bf19fed3(npieces, h1, x1, needed, cuts,
    size, width, h2, area_from_left):
    if h1 == h2:
        x = area_from_left / h1
    else:
        qa = (h2 - h1) / (2 * width)
        qb = h1
        qc = -area_from_left
        x = (-qb + (qb ** 2 - 4 * qa * qc) ** 0.5) / (2 * qa)
    cuts.append(x1 + x)
    if len(cuts) == npieces - 1:
        return cuts
    segment_remaining -= needed
    needed = size
    return qb


def func_4c2717047ef74f08adb2273c2198fcbc(npieces, h1, x1, needed, cuts,
    size, width, h2, area_from_left):
    if h1 == h2:
        x = area_from_left / h1
    else:
        qa = (h2 - h1) / (2 * width)
        qb = h1
        qc = -area_from_left
        x = (-qb + (qb ** 2 - 4 * qa * qc) ** 0.5) / (2 * qa)
    cuts.append(x1 + x)
    if len(cuts) == npieces - 1:
        return cuts
    segment_remaining -= needed
    needed = size
    return segment_remaining


def func_d39bc9ac12a34ec78eb1ca6724b4c42e(npieces, h1, x1, needed, cuts,
    size, width, h2, area_from_left):
    if h1 == h2:
        x = area_from_left / h1
    else:
        qa = (h2 - h1) / (2 * width)
        qb = h1
        qc = -area_from_left
        x = (-qb + (qb ** 2 - 4 * qa * qc) ** 0.5) / (2 * qa)
    cuts.append(x1 + x)
    if len(cuts) == npieces - 1:
        return cuts
    segment_remaining -= needed
    needed = size
    return x


def func_053f9688784549b09f36fd5bf09865c0(npieces, x2, h1, x1, needed, cuts, h2
    ):
    area_from_left += needed
    width = x2 - x1
    if h1 == h2:
        x = area_from_left / h1
    else:
        qa = (h2 - h1) / (2 * width)
        qb = h1
        qc = -area_from_left
        x = (-qb + (qb ** 2 - 4 * qa * qc) ** 0.5) / (2 * qa)
    cuts.append(x1 + x)
    if len(cuts) == npieces - 1:
        return cuts
    segment_remaining -= needed
    return x


def func_18af76d655af4ca48852dfcdd458d362(npieces, x2, h1, x1, needed, cuts, h2
    ):
    area_from_left += needed
    width = x2 - x1
    if h1 == h2:
        x = area_from_left / h1
    else:
        qa = (h2 - h1) / (2 * width)
        qb = h1
        qc = -area_from_left
        x = (-qb + (qb ** 2 - 4 * qa * qc) ** 0.5) / (2 * qa)
    cuts.append(x1 + x)
    if len(cuts) == npieces - 1:
        return cuts
    segment_remaining -= needed
    return segment_remaining


def func_51836abb6f2e42fab02399cad916136e(npieces, x2, h1, x1, needed, cuts, h2
    ):
    area_from_left += needed
    width = x2 - x1
    if h1 == h2:
        x = area_from_left / h1
    else:
        qa = (h2 - h1) / (2 * width)
        qb = h1
        qc = -area_from_left
        x = (-qb + (qb ** 2 - 4 * qa * qc) ** 0.5) / (2 * qa)
    cuts.append(x1 + x)
    if len(cuts) == npieces - 1:
        return cuts
    segment_remaining -= needed
    return area_from_left


def func_629f4657c45d43f595b22371d61b4ed0(npieces, x2, h1, x1, needed, cuts, h2
    ):
    area_from_left += needed
    width = x2 - x1
    if h1 == h2:
        x = area_from_left / h1
    else:
        qa = (h2 - h1) / (2 * width)
        qb = h1
        qc = -area_from_left
        x = (-qb + (qb ** 2 - 4 * qa * qc) ** 0.5) / (2 * qa)
    cuts.append(x1 + x)
    if len(cuts) == npieces - 1:
        return cuts
    segment_remaining -= needed
    return qa


def func_3597f0fbec1d4814b5d7a18f37aa8b94(npieces, x2, h1, x1, needed, cuts, h2
    ):
    area_from_left += needed
    width = x2 - x1
    if h1 == h2:
        x = area_from_left / h1
    else:
        qa = (h2 - h1) / (2 * width)
        qb = h1
        qc = -area_from_left
        x = (-qb + (qb ** 2 - 4 * qa * qc) ** 0.5) / (2 * qa)
    cuts.append(x1 + x)
    if len(cuts) == npieces - 1:
        return cuts
    segment_remaining -= needed
    return qc


def func_28191c469072443aa390db3d5110b310(npieces, x2, h1, x1, needed, cuts, h2
    ):
    area_from_left += needed
    width = x2 - x1
    if h1 == h2:
        x = area_from_left / h1
    else:
        qa = (h2 - h1) / (2 * width)
        qb = h1
        qc = -area_from_left
        x = (-qb + (qb ** 2 - 4 * qa * qc) ** 0.5) / (2 * qa)
    cuts.append(x1 + x)
    if len(cuts) == npieces - 1:
        return cuts
    segment_remaining -= needed
    return width


def func_eb9a50c723564419bd3b4a5c218c9c59(npieces, x2, h1, x1, needed, cuts, h2
    ):
    area_from_left += needed
    width = x2 - x1
    if h1 == h2:
        x = area_from_left / h1
    else:
        qa = (h2 - h1) / (2 * width)
        qb = h1
        qc = -area_from_left
        x = (-qb + (qb ** 2 - 4 * qa * qc) ** 0.5) / (2 * qa)
    cuts.append(x1 + x)
    if len(cuts) == npieces - 1:
        return cuts
    segment_remaining -= needed
    return qb


def func_874ce4ed174448998eea2584469b1b67(npieces, x2, h1, x1, needed, cuts,
    size, h2, area_from_left):
    width = x2 - x1
    if h1 == h2:
        x = area_from_left / h1
    else:
        qa = (h2 - h1) / (2 * width)
        qb = h1
        qc = -area_from_left
        x = (-qb + (qb ** 2 - 4 * qa * qc) ** 0.5) / (2 * qa)
    cuts.append(x1 + x)
    if len(cuts) == npieces - 1:
        return cuts
    segment_remaining -= needed
    needed = size
    return x


def func_7d7f5dd443ce43bfa354f9ff5e5abc37(npieces, x2, h1, x1, needed, cuts,
    size, h2, area_from_left):
    width = x2 - x1
    if h1 == h2:
        x = area_from_left / h1
    else:
        qa = (h2 - h1) / (2 * width)
        qb = h1
        qc = -area_from_left
        x = (-qb + (qb ** 2 - 4 * qa * qc) ** 0.5) / (2 * qa)
    cuts.append(x1 + x)
    if len(cuts) == npieces - 1:
        return cuts
    segment_remaining -= needed
    needed = size
    return qa


def func_8f7e7fa6a5354ff8bccacbdeefb8bb9f(npieces, x2, h1, x1, needed, cuts,
    size, h2, area_from_left):
    width = x2 - x1
    if h1 == h2:
        x = area_from_left / h1
    else:
        qa = (h2 - h1) / (2 * width)
        qb = h1
        qc = -area_from_left
        x = (-qb + (qb ** 2 - 4 * qa * qc) ** 0.5) / (2 * qa)
    cuts.append(x1 + x)
    if len(cuts) == npieces - 1:
        return cuts
    segment_remaining -= needed
    needed = size
    return segment_remaining


def func_247c73fc17344005b377d41ef6cba1aa(npieces, x2, h1, x1, needed, cuts,
    size, h2, area_from_left):
    width = x2 - x1
    if h1 == h2:
        x = area_from_left / h1
    else:
        qa = (h2 - h1) / (2 * width)
        qb = h1
        qc = -area_from_left
        x = (-qb + (qb ** 2 - 4 * qa * qc) ** 0.5) / (2 * qa)
    cuts.append(x1 + x)
    if len(cuts) == npieces - 1:
        return cuts
    segment_remaining -= needed
    needed = size
    return qc


def func_299e4db0da264f7da3d1e6214b13df96(npieces, x2, h1, x1, needed, cuts,
    size, h2, area_from_left):
    width = x2 - x1
    if h1 == h2:
        x = area_from_left / h1
    else:
        qa = (h2 - h1) / (2 * width)
        qb = h1
        qc = -area_from_left
        x = (-qb + (qb ** 2 - 4 * qa * qc) ** 0.5) / (2 * qa)
    cuts.append(x1 + x)
    if len(cuts) == npieces - 1:
        return cuts
    segment_remaining -= needed
    needed = size
    return width


def func_39638a228df04a089ef3a20a9c7f3dc0(npieces, x2, h1, x1, needed, cuts,
    size, h2, area_from_left):
    width = x2 - x1
    if h1 == h2:
        x = area_from_left / h1
    else:
        qa = (h2 - h1) / (2 * width)
        qb = h1
        qc = -area_from_left
        x = (-qb + (qb ** 2 - 4 * qa * qc) ** 0.5) / (2 * qa)
    cuts.append(x1 + x)
    if len(cuts) == npieces - 1:
        return cuts
    segment_remaining -= needed
    needed = size
    return qb


def func_a95cb57b432c495f9ea61358ecb327e7(npieces, x2, h1, x1, needed, cuts,
    size, h2):
    area_from_left += needed
    width = x2 - x1
    if h1 == h2:
        x = area_from_left / h1
    else:
        qa = (h2 - h1) / (2 * width)
        qb = h1
        qc = -area_from_left
        x = (-qb + (qb ** 2 - 4 * qa * qc) ** 0.5) / (2 * qa)
    cuts.append(x1 + x)
    if len(cuts) == npieces - 1:
        return cuts
    segment_remaining -= needed
    needed = size
    return qa


def func_b68ab371bce5425fac08418a67eec26c(npieces, x2, h1, x1, needed, cuts,
    size, h2):
    area_from_left += needed
    width = x2 - x1
    if h1 == h2:
        x = area_from_left / h1
    else:
        qa = (h2 - h1) / (2 * width)
        qb = h1
        qc = -area_from_left
        x = (-qb + (qb ** 2 - 4 * qa * qc) ** 0.5) / (2 * qa)
    cuts.append(x1 + x)
    if len(cuts) == npieces - 1:
        return cuts
    segment_remaining -= needed
    needed = size
    return area_from_left


def func_353b3b8ef376473a8968549de95e5170(npieces, x2, h1, x1, needed, cuts,
    size, h2):
    area_from_left += needed
    width = x2 - x1
    if h1 == h2:
        x = area_from_left / h1
    else:
        qa = (h2 - h1) / (2 * width)
        qb = h1
        qc = -area_from_left
        x = (-qb + (qb ** 2 - 4 * qa * qc) ** 0.5) / (2 * qa)
    cuts.append(x1 + x)
    if len(cuts) == npieces - 1:
        return cuts
    segment_remaining -= needed
    needed = size
    return x


def func_0cea390ecd8c4ec0a98905ec52962c1f(npieces, x2, h1, x1, needed, cuts,
    size, h2):
    area_from_left += needed
    width = x2 - x1
    if h1 == h2:
        x = area_from_left / h1
    else:
        qa = (h2 - h1) / (2 * width)
        qb = h1
        qc = -area_from_left
        x = (-qb + (qb ** 2 - 4 * qa * qc) ** 0.5) / (2 * qa)
    cuts.append(x1 + x)
    if len(cuts) == npieces - 1:
        return cuts
    segment_remaining -= needed
    needed = size
    return width


def func_a16220058d3f4985bc5f4c9f26f15794(npieces, x2, h1, x1, needed, cuts,
    size, h2):
    area_from_left += needed
    width = x2 - x1
    if h1 == h2:
        x = area_from_left / h1
    else:
        qa = (h2 - h1) / (2 * width)
        qb = h1
        qc = -area_from_left
        x = (-qb + (qb ** 2 - 4 * qa * qc) ** 0.5) / (2 * qa)
    cuts.append(x1 + x)
    if len(cuts) == npieces - 1:
        return cuts
    segment_remaining -= needed
    needed = size
    return qb


def func_f13fb7f040504498baa7f1ab3cf8ffa5(npieces, x2, h1, x1, needed, cuts,
    size, h2):
    area_from_left += needed
    width = x2 - x1
    if h1 == h2:
        x = area_from_left / h1
    else:
        qa = (h2 - h1) / (2 * width)
        qb = h1
        qc = -area_from_left
        x = (-qb + (qb ** 2 - 4 * qa * qc) ** 0.5) / (2 * qa)
    cuts.append(x1 + x)
    if len(cuts) == npieces - 1:
        return cuts
    segment_remaining -= needed
    needed = size
    return qc


def func_c44bbf59669a4e87be531e6ef4dbd32e(npieces, x2, h1, x1, needed, cuts,
    size, h2):
    area_from_left += needed
    width = x2 - x1
    if h1 == h2:
        x = area_from_left / h1
    else:
        qa = (h2 - h1) / (2 * width)
        qb = h1
        qc = -area_from_left
        x = (-qb + (qb ** 2 - 4 * qa * qc) ** 0.5) / (2 * qa)
    cuts.append(x1 + x)
    if len(cuts) == npieces - 1:
        return cuts
    segment_remaining -= needed
    needed = size
    return segment_remaining


def func_de9e2c0df31442b4b9d8bc4f2431129b(segment_area):
    segment_remaining = segment_area
    area_from_left = 0
    return area_from_left


def func_a6db9a9ac1e3496cabc9d9a46a328352(segment_area):
    segment_remaining = segment_area
    area_from_left = 0
    return segment_remaining


def func_08144d1984d948caa16e983acb2f1d3f(npieces, x2, h1, x1, needed, cuts,
    size, h2):
    area_from_left = 0
    while segment_remaining >= needed:
        area_from_left += needed
        width = x2 - x1
        if h1 == h2:
            x = area_from_left / h1
        else:
            qa = (h2 - h1) / (2 * width)
            qb = h1
            qc = -area_from_left
            x = (-qb + (qb ** 2 - 4 * qa * qc) ** 0.5) / (2 * qa)
        cuts.append(x1 + x)
        if len(cuts) == npieces - 1:
            return cuts
        segment_remaining -= needed
        needed = size
    return segment_remaining


def func_9b441593eea945e7b6cfcf19d65bf68a(npieces, x2, h1, x1, needed, cuts,
    size, h2):
    area_from_left = 0
    while segment_remaining >= needed:
        area_from_left += needed
        width = x2 - x1
        if h1 == h2:
            x = area_from_left / h1
        else:
            qa = (h2 - h1) / (2 * width)
            qb = h1
            qc = -area_from_left
            x = (-qb + (qb ** 2 - 4 * qa * qc) ** 0.5) / (2 * qa)
        cuts.append(x1 + x)
        if len(cuts) == npieces - 1:
            return cuts
        segment_remaining -= needed
        needed = size
    return qc


def func_cf7839b8ddd74f478c22117762e17ab5(npieces, x2, h1, x1, needed, cuts,
    size, h2):
    area_from_left = 0
    while segment_remaining >= needed:
        area_from_left += needed
        width = x2 - x1
        if h1 == h2:
            x = area_from_left / h1
        else:
            qa = (h2 - h1) / (2 * width)
            qb = h1
            qc = -area_from_left
            x = (-qb + (qb ** 2 - 4 * qa * qc) ** 0.5) / (2 * qa)
        cuts.append(x1 + x)
        if len(cuts) == npieces - 1:
            return cuts
        segment_remaining -= needed
        needed = size
    return area_from_left


def func_6f7954a0e224499f9f6ab9fe158e1d86(npieces, x2, h1, x1, needed, cuts,
    size, h2):
    area_from_left = 0
    while segment_remaining >= needed:
        area_from_left += needed
        width = x2 - x1
        if h1 == h2:
            x = area_from_left / h1
        else:
            qa = (h2 - h1) / (2 * width)
            qb = h1
            qc = -area_from_left
            x = (-qb + (qb ** 2 - 4 * qa * qc) ** 0.5) / (2 * qa)
        cuts.append(x1 + x)
        if len(cuts) == npieces - 1:
            return cuts
        segment_remaining -= needed
        needed = size
    return x


def func_1fce43f825c340d5a179284a50a50d1a(npieces, x2, h1, x1, needed, cuts,
    size, h2):
    area_from_left = 0
    while segment_remaining >= needed:
        area_from_left += needed
        width = x2 - x1
        if h1 == h2:
            x = area_from_left / h1
        else:
            qa = (h2 - h1) / (2 * width)
            qb = h1
            qc = -area_from_left
            x = (-qb + (qb ** 2 - 4 * qa * qc) ** 0.5) / (2 * qa)
        cuts.append(x1 + x)
        if len(cuts) == npieces - 1:
            return cuts
        segment_remaining -= needed
        needed = size
    return qa


def func_969329cfde2d453c93b4f8853fad8a06(npieces, x2, h1, x1, needed, cuts,
    size, h2):
    area_from_left = 0
    while segment_remaining >= needed:
        area_from_left += needed
        width = x2 - x1
        if h1 == h2:
            x = area_from_left / h1
        else:
            qa = (h2 - h1) / (2 * width)
            qb = h1
            qc = -area_from_left
            x = (-qb + (qb ** 2 - 4 * qa * qc) ** 0.5) / (2 * qa)
        cuts.append(x1 + x)
        if len(cuts) == npieces - 1:
            return cuts
        segment_remaining -= needed
        needed = size
    return qb


def func_09520dc896934e7ebfbb1d2833e5a8be(npieces, x2, h1, x1, needed, cuts,
    size, h2):
    area_from_left = 0
    while segment_remaining >= needed:
        area_from_left += needed
        width = x2 - x1
        if h1 == h2:
            x = area_from_left / h1
        else:
            qa = (h2 - h1) / (2 * width)
            qb = h1
            qc = -area_from_left
            x = (-qb + (qb ** 2 - 4 * qa * qc) ** 0.5) / (2 * qa)
        cuts.append(x1 + x)
        if len(cuts) == npieces - 1:
            return cuts
        segment_remaining -= needed
        needed = size
    return width


def func_53cee627910c456fbbc17bf0effa094f(npieces, x2, h1, x1, cuts, size, h2):
    while segment_remaining >= needed:
        area_from_left += needed
        width = x2 - x1
        if h1 == h2:
            x = area_from_left / h1
        else:
            qa = (h2 - h1) / (2 * width)
            qb = h1
            qc = -area_from_left
            x = (-qb + (qb ** 2 - 4 * qa * qc) ** 0.5) / (2 * qa)
        cuts.append(x1 + x)
        if len(cuts) == npieces - 1:
            return cuts
        segment_remaining -= needed
        needed = size
    needed -= segment_remaining
    return qb


def func_8411e58d874241198ed95bd2dd008f36(npieces, x2, h1, x1, cuts, size, h2):
    while segment_remaining >= needed:
        area_from_left += needed
        width = x2 - x1
        if h1 == h2:
            x = area_from_left / h1
        else:
            qa = (h2 - h1) / (2 * width)
            qb = h1
            qc = -area_from_left
            x = (-qb + (qb ** 2 - 4 * qa * qc) ** 0.5) / (2 * qa)
        cuts.append(x1 + x)
        if len(cuts) == npieces - 1:
            return cuts
        segment_remaining -= needed
        needed = size
    needed -= segment_remaining
    return needed


def func_bddae033f621481a90a84b736fa9d9a6(npieces, x2, h1, x1, cuts, size, h2):
    while segment_remaining >= needed:
        area_from_left += needed
        width = x2 - x1
        if h1 == h2:
            x = area_from_left / h1
        else:
            qa = (h2 - h1) / (2 * width)
            qb = h1
            qc = -area_from_left
            x = (-qb + (qb ** 2 - 4 * qa * qc) ** 0.5) / (2 * qa)
        cuts.append(x1 + x)
        if len(cuts) == npieces - 1:
            return cuts
        segment_remaining -= needed
        needed = size
    needed -= segment_remaining
    return area_from_left


def func_21ab63a3d8694960bf3bc2c2c48b8d2e(npieces, x2, h1, x1, cuts, size, h2):
    while segment_remaining >= needed:
        area_from_left += needed
        width = x2 - x1
        if h1 == h2:
            x = area_from_left / h1
        else:
            qa = (h2 - h1) / (2 * width)
            qb = h1
            qc = -area_from_left
            x = (-qb + (qb ** 2 - 4 * qa * qc) ** 0.5) / (2 * qa)
        cuts.append(x1 + x)
        if len(cuts) == npieces - 1:
            return cuts
        segment_remaining -= needed
        needed = size
    needed -= segment_remaining
    return qc


def func_0432d0954d7f45d89f4390d1cc5db99e(npieces, x2, h1, x1, cuts, size, h2):
    while segment_remaining >= needed:
        area_from_left += needed
        width = x2 - x1
        if h1 == h2:
            x = area_from_left / h1
        else:
            qa = (h2 - h1) / (2 * width)
            qb = h1
            qc = -area_from_left
            x = (-qb + (qb ** 2 - 4 * qa * qc) ** 0.5) / (2 * qa)
        cuts.append(x1 + x)
        if len(cuts) == npieces - 1:
            return cuts
        segment_remaining -= needed
        needed = size
    needed -= segment_remaining
    return qa


def func_3c65abb6ac4b46b289f78eaec3d8b83f(npieces, x2, h1, x1, cuts, size, h2):
    while segment_remaining >= needed:
        area_from_left += needed
        width = x2 - x1
        if h1 == h2:
            x = area_from_left / h1
        else:
            qa = (h2 - h1) / (2 * width)
            qb = h1
            qc = -area_from_left
            x = (-qb + (qb ** 2 - 4 * qa * qc) ** 0.5) / (2 * qa)
        cuts.append(x1 + x)
        if len(cuts) == npieces - 1:
            return cuts
        segment_remaining -= needed
        needed = size
    needed -= segment_remaining
    return width


def func_0937b7afb8cc4c41abd6e66cbeba773e(npieces, x2, h1, x1, cuts, size, h2):
    while segment_remaining >= needed:
        area_from_left += needed
        width = x2 - x1
        if h1 == h2:
            x = area_from_left / h1
        else:
            qa = (h2 - h1) / (2 * width)
            qb = h1
            qc = -area_from_left
            x = (-qb + (qb ** 2 - 4 * qa * qc) ** 0.5) / (2 * qa)
        cuts.append(x1 + x)
        if len(cuts) == npieces - 1:
            return cuts
        segment_remaining -= needed
        needed = size
    needed -= segment_remaining
    return x


def func_6485b91e42d14bc28017b78c4b2512c4(npieces, x2, h1, x1, cuts, size, h2):
    while segment_remaining >= needed:
        area_from_left += needed
        width = x2 - x1
        if h1 == h2:
            x = area_from_left / h1
        else:
            qa = (h2 - h1) / (2 * width)
            qb = h1
            qc = -area_from_left
            x = (-qb + (qb ** 2 - 4 * qa * qc) ** 0.5) / (2 * qa)
        cuts.append(x1 + x)
        if len(cuts) == npieces - 1:
            return cuts
        segment_remaining -= needed
        needed = size
    needed -= segment_remaining
    return segment_remaining


def func_0e07c4c9b11043dc8f40348b355050e7(npieces, x2, h1, x1, segment_area,
    needed, cuts, size, h2):
    segment_remaining = segment_area
    area_from_left = 0
    while segment_remaining >= needed:
        area_from_left += needed
        width = x2 - x1
        if h1 == h2:
            x = area_from_left / h1
        else:
            qa = (h2 - h1) / (2 * width)
            qb = h1
            qc = -area_from_left
            x = (-qb + (qb ** 2 - 4 * qa * qc) ** 0.5) / (2 * qa)
        cuts.append(x1 + x)
        if len(cuts) == npieces - 1:
            return cuts
        segment_remaining -= needed
        needed = size
    return segment_remaining


def func_e4a2ec40aedb405b8c013176bfcbb778(npieces, x2, h1, x1, segment_area,
    needed, cuts, size, h2):
    segment_remaining = segment_area
    area_from_left = 0
    while segment_remaining >= needed:
        area_from_left += needed
        width = x2 - x1
        if h1 == h2:
            x = area_from_left / h1
        else:
            qa = (h2 - h1) / (2 * width)
            qb = h1
            qc = -area_from_left
            x = (-qb + (qb ** 2 - 4 * qa * qc) ** 0.5) / (2 * qa)
        cuts.append(x1 + x)
        if len(cuts) == npieces - 1:
            return cuts
        segment_remaining -= needed
        needed = size
    return area_from_left


def func_28b5f0cea2a84cdc9f8f96744109ac6f(npieces, x2, h1, x1, segment_area,
    needed, cuts, size, h2):
    segment_remaining = segment_area
    area_from_left = 0
    while segment_remaining >= needed:
        area_from_left += needed
        width = x2 - x1
        if h1 == h2:
            x = area_from_left / h1
        else:
            qa = (h2 - h1) / (2 * width)
            qb = h1
            qc = -area_from_left
            x = (-qb + (qb ** 2 - 4 * qa * qc) ** 0.5) / (2 * qa)
        cuts.append(x1 + x)
        if len(cuts) == npieces - 1:
            return cuts
        segment_remaining -= needed
        needed = size
    return qb


def func_74a891a246ed43d489e243bfd78776d1(npieces, x2, h1, x1, segment_area,
    needed, cuts, size, h2):
    segment_remaining = segment_area
    area_from_left = 0
    while segment_remaining >= needed:
        area_from_left += needed
        width = x2 - x1
        if h1 == h2:
            x = area_from_left / h1
        else:
            qa = (h2 - h1) / (2 * width)
            qb = h1
            qc = -area_from_left
            x = (-qb + (qb ** 2 - 4 * qa * qc) ** 0.5) / (2 * qa)
        cuts.append(x1 + x)
        if len(cuts) == npieces - 1:
            return cuts
        segment_remaining -= needed
        needed = size
    return qa


def func_f81b06194dec4c3a9223cd3444e1bc22(npieces, x2, h1, x1, segment_area,
    needed, cuts, size, h2):
    segment_remaining = segment_area
    area_from_left = 0
    while segment_remaining >= needed:
        area_from_left += needed
        width = x2 - x1
        if h1 == h2:
            x = area_from_left / h1
        else:
            qa = (h2 - h1) / (2 * width)
            qb = h1
            qc = -area_from_left
            x = (-qb + (qb ** 2 - 4 * qa * qc) ** 0.5) / (2 * qa)
        cuts.append(x1 + x)
        if len(cuts) == npieces - 1:
            return cuts
        segment_remaining -= needed
        needed = size
    return x


def func_04bf20c998304fc2a83fa331d3003207(npieces, x2, h1, x1, segment_area,
    needed, cuts, size, h2):
    segment_remaining = segment_area
    area_from_left = 0
    while segment_remaining >= needed:
        area_from_left += needed
        width = x2 - x1
        if h1 == h2:
            x = area_from_left / h1
        else:
            qa = (h2 - h1) / (2 * width)
            qb = h1
            qc = -area_from_left
            x = (-qb + (qb ** 2 - 4 * qa * qc) ** 0.5) / (2 * qa)
        cuts.append(x1 + x)
        if len(cuts) == npieces - 1:
            return cuts
        segment_remaining -= needed
        needed = size
    return qc


def func_a25ae32f07714e2591e8b6fa09d021d7(npieces, x2, h1, x1, segment_area,
    needed, cuts, size, h2):
    segment_remaining = segment_area
    area_from_left = 0
    while segment_remaining >= needed:
        area_from_left += needed
        width = x2 - x1
        if h1 == h2:
            x = area_from_left / h1
        else:
            qa = (h2 - h1) / (2 * width)
            qb = h1
            qc = -area_from_left
            x = (-qb + (qb ** 2 - 4 * qa * qc) ** 0.5) / (2 * qa)
        cuts.append(x1 + x)
        if len(cuts) == npieces - 1:
            return cuts
        segment_remaining -= needed
        needed = size
    return width


def func_a35d56cba0254f6a92990a169f19e6c0(npieces, x2, h1, x1, cuts, size, h2):
    area_from_left = 0
    while segment_remaining >= needed:
        area_from_left += needed
        width = x2 - x1
        if h1 == h2:
            x = area_from_left / h1
        else:
            qa = (h2 - h1) / (2 * width)
            qb = h1
            qc = -area_from_left
            x = (-qb + (qb ** 2 - 4 * qa * qc) ** 0.5) / (2 * qa)
        cuts.append(x1 + x)
        if len(cuts) == npieces - 1:
            return cuts
        segment_remaining -= needed
        needed = size
    needed -= segment_remaining
    return qc


def func_23576a1c407d4bfca4fd093d67be1f4f(npieces, x2, h1, x1, cuts, size, h2):
    area_from_left = 0
    while segment_remaining >= needed:
        area_from_left += needed
        width = x2 - x1
        if h1 == h2:
            x = area_from_left / h1
        else:
            qa = (h2 - h1) / (2 * width)
            qb = h1
            qc = -area_from_left
            x = (-qb + (qb ** 2 - 4 * qa * qc) ** 0.5) / (2 * qa)
        cuts.append(x1 + x)
        if len(cuts) == npieces - 1:
            return cuts
        segment_remaining -= needed
        needed = size
    needed -= segment_remaining
    return x


def func_e5d9d276d9864bf892d3e11c4cb342c1(npieces, x2, h1, x1, cuts, size, h2):
    area_from_left = 0
    while segment_remaining >= needed:
        area_from_left += needed
        width = x2 - x1
        if h1 == h2:
            x = area_from_left / h1
        else:
            qa = (h2 - h1) / (2 * width)
            qb = h1
            qc = -area_from_left
            x = (-qb + (qb ** 2 - 4 * qa * qc) ** 0.5) / (2 * qa)
        cuts.append(x1 + x)
        if len(cuts) == npieces - 1:
            return cuts
        segment_remaining -= needed
        needed = size
    needed -= segment_remaining
    return width


def func_142e2ded8668483299c11b71b55833c7(npieces, x2, h1, x1, cuts, size, h2):
    area_from_left = 0
    while segment_remaining >= needed:
        area_from_left += needed
        width = x2 - x1
        if h1 == h2:
            x = area_from_left / h1
        else:
            qa = (h2 - h1) / (2 * width)
            qb = h1
            qc = -area_from_left
            x = (-qb + (qb ** 2 - 4 * qa * qc) ** 0.5) / (2 * qa)
        cuts.append(x1 + x)
        if len(cuts) == npieces - 1:
            return cuts
        segment_remaining -= needed
        needed = size
    needed -= segment_remaining
    return qa


def func_7a4f8abeb261461aa2a63a6409324a5c(npieces, x2, h1, x1, cuts, size, h2):
    area_from_left = 0
    while segment_remaining >= needed:
        area_from_left += needed
        width = x2 - x1
        if h1 == h2:
            x = area_from_left / h1
        else:
            qa = (h2 - h1) / (2 * width)
            qb = h1
            qc = -area_from_left
            x = (-qb + (qb ** 2 - 4 * qa * qc) ** 0.5) / (2 * qa)
        cuts.append(x1 + x)
        if len(cuts) == npieces - 1:
            return cuts
        segment_remaining -= needed
        needed = size
    needed -= segment_remaining
    return needed


def func_0bd74ffe29ef485cb99780a529ba7a06(npieces, x2, h1, x1, cuts, size, h2):
    area_from_left = 0
    while segment_remaining >= needed:
        area_from_left += needed
        width = x2 - x1
        if h1 == h2:
            x = area_from_left / h1
        else:
            qa = (h2 - h1) / (2 * width)
            qb = h1
            qc = -area_from_left
            x = (-qb + (qb ** 2 - 4 * qa * qc) ** 0.5) / (2 * qa)
        cuts.append(x1 + x)
        if len(cuts) == npieces - 1:
            return cuts
        segment_remaining -= needed
        needed = size
    needed -= segment_remaining
    return area_from_left


def func_645b604a84d94fdcbe12614868bdaf6c(npieces, x2, h1, x1, cuts, size, h2):
    area_from_left = 0
    while segment_remaining >= needed:
        area_from_left += needed
        width = x2 - x1
        if h1 == h2:
            x = area_from_left / h1
        else:
            qa = (h2 - h1) / (2 * width)
            qb = h1
            qc = -area_from_left
            x = (-qb + (qb ** 2 - 4 * qa * qc) ** 0.5) / (2 * qa)
        cuts.append(x1 + x)
        if len(cuts) == npieces - 1:
            return cuts
        segment_remaining -= needed
        needed = size
    needed -= segment_remaining
    return segment_remaining


def func_5f1b28a4d8ad48abb0c5631bddc03ed6(npieces, x2, h1, x1, cuts, size, h2):
    area_from_left = 0
    while segment_remaining >= needed:
        area_from_left += needed
        width = x2 - x1
        if h1 == h2:
            x = area_from_left / h1
        else:
            qa = (h2 - h1) / (2 * width)
            qb = h1
            qc = -area_from_left
            x = (-qb + (qb ** 2 - 4 * qa * qc) ** 0.5) / (2 * qa)
        cuts.append(x1 + x)
        if len(cuts) == npieces - 1:
            return cuts
        segment_remaining -= needed
        needed = size
    needed -= segment_remaining
    return qb


def func_b4e8bbd0f98a4651a9b9620f250943ce(npieces, x2, h1, x1, segment_area,
    cuts, size, h2):
    segment_remaining = segment_area
    area_from_left = 0
    while segment_remaining >= needed:
        area_from_left += needed
        width = x2 - x1
        if h1 == h2:
            x = area_from_left / h1
        else:
            qa = (h2 - h1) / (2 * width)
            qb = h1
            qc = -area_from_left
            x = (-qb + (qb ** 2 - 4 * qa * qc) ** 0.5) / (2 * qa)
        cuts.append(x1 + x)
        if len(cuts) == npieces - 1:
            return cuts
        segment_remaining -= needed
        needed = size
    needed -= segment_remaining
    return qb


def func_f849a07ce77b4c019e7fbf9dbfcd6261(npieces, x2, h1, x1, segment_area,
    cuts, size, h2):
    segment_remaining = segment_area
    area_from_left = 0
    while segment_remaining >= needed:
        area_from_left += needed
        width = x2 - x1
        if h1 == h2:
            x = area_from_left / h1
        else:
            qa = (h2 - h1) / (2 * width)
            qb = h1
            qc = -area_from_left
            x = (-qb + (qb ** 2 - 4 * qa * qc) ** 0.5) / (2 * qa)
        cuts.append(x1 + x)
        if len(cuts) == npieces - 1:
            return cuts
        segment_remaining -= needed
        needed = size
    needed -= segment_remaining
    return area_from_left


def func_8fed5ef91e044718bc56b5d8f6d8d36a(npieces, x2, h1, x1, segment_area,
    cuts, size, h2):
    segment_remaining = segment_area
    area_from_left = 0
    while segment_remaining >= needed:
        area_from_left += needed
        width = x2 - x1
        if h1 == h2:
            x = area_from_left / h1
        else:
            qa = (h2 - h1) / (2 * width)
            qb = h1
            qc = -area_from_left
            x = (-qb + (qb ** 2 - 4 * qa * qc) ** 0.5) / (2 * qa)
        cuts.append(x1 + x)
        if len(cuts) == npieces - 1:
            return cuts
        segment_remaining -= needed
        needed = size
    needed -= segment_remaining
    return qa


def func_c68e157ddbbc45d09ad16e6253b134a5(npieces, x2, h1, x1, segment_area,
    cuts, size, h2):
    segment_remaining = segment_area
    area_from_left = 0
    while segment_remaining >= needed:
        area_from_left += needed
        width = x2 - x1
        if h1 == h2:
            x = area_from_left / h1
        else:
            qa = (h2 - h1) / (2 * width)
            qb = h1
            qc = -area_from_left
            x = (-qb + (qb ** 2 - 4 * qa * qc) ** 0.5) / (2 * qa)
        cuts.append(x1 + x)
        if len(cuts) == npieces - 1:
            return cuts
        segment_remaining -= needed
        needed = size
    needed -= segment_remaining
    return width


def func_fad014cd8c53449fb1def762cd640790(npieces, x2, h1, x1, segment_area,
    cuts, size, h2):
    segment_remaining = segment_area
    area_from_left = 0
    while segment_remaining >= needed:
        area_from_left += needed
        width = x2 - x1
        if h1 == h2:
            x = area_from_left / h1
        else:
            qa = (h2 - h1) / (2 * width)
            qb = h1
            qc = -area_from_left
            x = (-qb + (qb ** 2 - 4 * qa * qc) ** 0.5) / (2 * qa)
        cuts.append(x1 + x)
        if len(cuts) == npieces - 1:
            return cuts
        segment_remaining -= needed
        needed = size
    needed -= segment_remaining
    return needed


def func_745a8e9a96ec49fa9fb77777a522228b(npieces, x2, h1, x1, segment_area,
    cuts, size, h2):
    segment_remaining = segment_area
    area_from_left = 0
    while segment_remaining >= needed:
        area_from_left += needed
        width = x2 - x1
        if h1 == h2:
            x = area_from_left / h1
        else:
            qa = (h2 - h1) / (2 * width)
            qb = h1
            qc = -area_from_left
            x = (-qb + (qb ** 2 - 4 * qa * qc) ** 0.5) / (2 * qa)
        cuts.append(x1 + x)
        if len(cuts) == npieces - 1:
            return cuts
        segment_remaining -= needed
        needed = size
    needed -= segment_remaining
    return x


def func_df76a225017b4d7b834707716d86c5f1(npieces, x2, h1, x1, segment_area,
    cuts, size, h2):
    segment_remaining = segment_area
    area_from_left = 0
    while segment_remaining >= needed:
        area_from_left += needed
        width = x2 - x1
        if h1 == h2:
            x = area_from_left / h1
        else:
            qa = (h2 - h1) / (2 * width)
            qb = h1
            qc = -area_from_left
            x = (-qb + (qb ** 2 - 4 * qa * qc) ** 0.5) / (2 * qa)
        cuts.append(x1 + x)
        if len(cuts) == npieces - 1:
            return cuts
        segment_remaining -= needed
        needed = size
    needed -= segment_remaining
    return segment_remaining


def func_229241974a9448d99ce8d3854ef9c969(npieces, x2, h1, x1, segment_area,
    cuts, size, h2):
    segment_remaining = segment_area
    area_from_left = 0
    while segment_remaining >= needed:
        area_from_left += needed
        width = x2 - x1
        if h1 == h2:
            x = area_from_left / h1
        else:
            qa = (h2 - h1) / (2 * width)
            qb = h1
            qc = -area_from_left
            x = (-qb + (qb ** 2 - 4 * qa * qc) ** 0.5) / (2 * qa)
        cuts.append(x1 + x)
        if len(cuts) == npieces - 1:
            return cuts
        segment_remaining -= needed
        needed = size
    needed -= segment_remaining
    return qc


def func_8b417894022c46d28ad5fb9d4b30dfe6():
    li = ui = 0
    areas = []
    return ui


def func_db4f41d6be1341e6be1c4f3f543577da():
    li = ui = 0
    areas = []
    return areas


def func_5819245432e149989a4cadcc752f7d52():
    li = ui = 0
    areas = []
    return li


def func_23515e7ae1b1471e817fa1f69567fc56():
    areas = []
    total_area = 0
    return total_area


def func_683304050d9d4cb0ab0c4aaee010589e():
    areas = []
    total_area = 0
    return areas


def func_629c880b8cfa43b7ba683cdf5553e95f():
    total_area = 0
    x = 0
    return total_area


def func_60d0f689b6bf4607b7954ad2c86a6efe():
    total_area = 0
    x = 0
    return x


def func_3e8e07b4756a4cbcbabb3d54563cf969(upper, lower):
    x = 0
    h1 = upper[0][1] - lower[0][1]
    return x


def func_14485012bd2e4d34a8cec1ea8221ca20(upper, lower):
    x = 0
    h1 = upper[0][1] - lower[0][1]
    return h1


def func_9475a8c81fc74115bc90a91233141262(upper, lower):
    h1 = upper[0][1] - lower[0][1]
    W = lower[-1][0]
    return W


def func_7de47d21ec6e41298c7b2ad3360d72e3(upper, lower):
    h1 = upper[0][1] - lower[0][1]
    W = lower[-1][0]
    return h1


def func_eae09f945198426fbc5ab388e4a1467d(npieces, total_area):
    size = total_area / npieces
    cuts = []
    return size


def func_43e000ef42554446b927188b1433b5a2(npieces, total_area):
    size = total_area / npieces
    cuts = []
    return cuts


def func_7d45398ac6c54e15b511f87b4b8153c2(size):
    cuts = []
    needed = size
    return cuts


def func_1b98bb55c2174a14afcb7b4ee4df708b(size):
    cuts = []
    needed = size
    return needed


def func_5684b541da144241ad9ae0e8afe8b81b():
    li = ui = 0
    areas = []
    total_area = 0
    return ui


def func_dc22568f11c44a63bd5d42c1ce7cc788():
    li = ui = 0
    areas = []
    total_area = 0
    return areas


def func_e50e133abdc14ed1bb5df82c0420f00c():
    li = ui = 0
    areas = []
    total_area = 0
    return total_area


def func_7f9dc411dec641578b7c3734af2fae3c():
    li = ui = 0
    areas = []
    total_area = 0
    return li


def func_3c0ef411797f468ca7466cbaef66b9be():
    areas = []
    total_area = 0
    x = 0
    return areas


def func_f2c0d30b7d7c4322ab0a146d355f3866():
    areas = []
    total_area = 0
    x = 0
    return x


def func_3815e036ad6e42a4bc1be9cc46292d90():
    areas = []
    total_area = 0
    x = 0
    return total_area


def func_c70e68e31221445a8976ffdb6b3b0988(upper, lower):
    total_area = 0
    x = 0
    h1 = upper[0][1] - lower[0][1]
    return total_area


def func_25259abe83f64f8d99cd7eef7f722102(upper, lower):
    total_area = 0
    x = 0
    h1 = upper[0][1] - lower[0][1]
    return h1


def func_71210f8179a247a7b4499f4a10b33e9b(upper, lower):
    total_area = 0
    x = 0
    h1 = upper[0][1] - lower[0][1]
    return x


def func_722bbcd78b084cd692c8c81293642899(upper, lower):
    x = 0
    h1 = upper[0][1] - lower[0][1]
    W = lower[-1][0]
    return h1


def func_746b9cfd177a48ca9423042cf9bc4cc1(upper, lower):
    x = 0
    h1 = upper[0][1] - lower[0][1]
    W = lower[-1][0]
    return W


def func_798624039efb49e69ddb4dc31068e204(upper, lower):
    x = 0
    h1 = upper[0][1] - lower[0][1]
    W = lower[-1][0]
    return x


def func_0510ad09f0bd479f82a5c272256b1460(npieces, total_area):
    size = total_area / npieces
    cuts = []
    needed = size
    return size


def func_013a3e5810ad462983f225130c5a3c89(npieces, total_area):
    size = total_area / npieces
    cuts = []
    needed = size
    return cuts


def func_8170912d640642e2834a0c0777dbfd2e(npieces, total_area):
    size = total_area / npieces
    cuts = []
    needed = size
    return needed


def func_e67545604966466ab9cf302e3f8a995c():
    li = ui = 0
    areas = []
    total_area = 0
    x = 0
    return ui


def func_637b3c58891a4f17a225f2eae58a1475():
    li = ui = 0
    areas = []
    total_area = 0
    x = 0
    return li


def func_dd341eb50366471aad99bd5d8adf4eba():
    li = ui = 0
    areas = []
    total_area = 0
    x = 0
    return areas


def func_ff09ecdf980b42b6a7fdd5bef7a7e81a():
    li = ui = 0
    areas = []
    total_area = 0
    x = 0
    return total_area


def func_bdbbd8981fff428cb00fe2dd81cab2aa():
    li = ui = 0
    areas = []
    total_area = 0
    x = 0
    return x


def func_8a973c469f3843fab8ed4f0b3739ad39(upper, lower):
    areas = []
    total_area = 0
    x = 0
    h1 = upper[0][1] - lower[0][1]
    return total_area


def func_9bb13bb2bb4b464ea5e68774726c0112(upper, lower):
    areas = []
    total_area = 0
    x = 0
    h1 = upper[0][1] - lower[0][1]
    return areas


def func_b6fb5172ea374759a318ed0fa4867b1f(upper, lower):
    areas = []
    total_area = 0
    x = 0
    h1 = upper[0][1] - lower[0][1]
    return h1


def func_0653d1601fb84b279acd919de0320eaf(upper, lower):
    areas = []
    total_area = 0
    x = 0
    h1 = upper[0][1] - lower[0][1]
    return x


def func_d22d689148bd44be968136de669456a2(upper, lower):
    total_area = 0
    x = 0
    h1 = upper[0][1] - lower[0][1]
    W = lower[-1][0]
    return W


def func_6a8bab32e4e84165b095cbd570213cfa(upper, lower):
    total_area = 0
    x = 0
    h1 = upper[0][1] - lower[0][1]
    W = lower[-1][0]
    return x


def func_4a3169839b2341a28a00a24f0b39c81f(upper, lower):
    total_area = 0
    x = 0
    h1 = upper[0][1] - lower[0][1]
    W = lower[-1][0]
    return h1


def func_222676ccd53f486d9b5e2d71e79fa0bf(upper, lower):
    total_area = 0
    x = 0
    h1 = upper[0][1] - lower[0][1]
    W = lower[-1][0]
    return total_area


def func_b9627646feb84c5ba68b8e8a0a879b1b(upper, lower):
    li = ui = 0
    areas = []
    total_area = 0
    x = 0
    h1 = upper[0][1] - lower[0][1]
    return areas


def func_62ac0ec350654eea9fa85e906fe7dfa6(upper, lower):
    li = ui = 0
    areas = []
    total_area = 0
    x = 0
    h1 = upper[0][1] - lower[0][1]
    return x


def func_85beccb41afd49ab95b5c9de87a40303(upper, lower):
    li = ui = 0
    areas = []
    total_area = 0
    x = 0
    h1 = upper[0][1] - lower[0][1]
    return ui


def func_f94d2effa21f47a098d9dbdb551fc80e(upper, lower):
    li = ui = 0
    areas = []
    total_area = 0
    x = 0
    h1 = upper[0][1] - lower[0][1]
    return total_area


def func_022500f0966d4093af99a1eca5eac3c8(upper, lower):
    li = ui = 0
    areas = []
    total_area = 0
    x = 0
    h1 = upper[0][1] - lower[0][1]
    return h1


def func_00363f15b35443b1b4a0c66bb4fc222f(upper, lower):
    li = ui = 0
    areas = []
    total_area = 0
    x = 0
    h1 = upper[0][1] - lower[0][1]
    return li


def func_d7d6a006e8914a7e8062d38216c7bc03(upper, lower):
    areas = []
    total_area = 0
    x = 0
    h1 = upper[0][1] - lower[0][1]
    W = lower[-1][0]
    return areas


def func_a5caca7985054ad99a6fcc469da9399c(upper, lower):
    areas = []
    total_area = 0
    x = 0
    h1 = upper[0][1] - lower[0][1]
    W = lower[-1][0]
    return x


def func_92b8a20df83a431f942a6e283ca6cc44(upper, lower):
    areas = []
    total_area = 0
    x = 0
    h1 = upper[0][1] - lower[0][1]
    W = lower[-1][0]
    return W


def func_9af7a1a1869c458ba3f4e568f5d1cdf1(upper, lower):
    areas = []
    total_area = 0
    x = 0
    h1 = upper[0][1] - lower[0][1]
    W = lower[-1][0]
    return total_area


def func_0976e6cd6ba8480dbc327e88de1b6233(upper, lower):
    areas = []
    total_area = 0
    x = 0
    h1 = upper[0][1] - lower[0][1]
    W = lower[-1][0]
    return h1


def func_433cf3399ccb4ab68133976324dd7d08(upper, lower):
    li = ui = 0
    areas = []
    total_area = 0
    x = 0
    h1 = upper[0][1] - lower[0][1]
    W = lower[-1][0]
    return areas


def func_c4ca717bc6ec4a4a86b6c4a41045e5a1(upper, lower):
    li = ui = 0
    areas = []
    total_area = 0
    x = 0
    h1 = upper[0][1] - lower[0][1]
    W = lower[-1][0]
    return h1


def func_7f2ffc9afc3749078615ecf9a2ce828a(upper, lower):
    li = ui = 0
    areas = []
    total_area = 0
    x = 0
    h1 = upper[0][1] - lower[0][1]
    W = lower[-1][0]
    return ui


def func_433f53398b064f8c972da54fd83acf16(upper, lower):
    li = ui = 0
    areas = []
    total_area = 0
    x = 0
    h1 = upper[0][1] - lower[0][1]
    W = lower[-1][0]
    return W


def func_3d885784ec644e93b1d0215162a79baa(upper, lower):
    li = ui = 0
    areas = []
    total_area = 0
    x = 0
    h1 = upper[0][1] - lower[0][1]
    W = lower[-1][0]
    return x


def func_fda99d8114784ec8b9a69011076135d0(upper, lower):
    li = ui = 0
    areas = []
    total_area = 0
    x = 0
    h1 = upper[0][1] - lower[0][1]
    W = lower[-1][0]
    return li


def func_4c2d4285f40941b8b22c9a7ed8f309be(upper, lower):
    li = ui = 0
    areas = []
    total_area = 0
    x = 0
    h1 = upper[0][1] - lower[0][1]
    W = lower[-1][0]
    return total_area


def func_35d6b33d77a04f6db18576e7e948d631(upper, lower):
    areas = []
    total_area = 0
    x = 0
    h1 = upper[0][1] - lower[0][1]
    W = lower[-1][0]
    while x < W:
        lnext = lower[li + 1]
        unext = upper[ui + 1]
        if lnext[0] == unext[0]:
            xnext = lnext[0]
            h2 = unext[1] - lnext[1]
            li += 1
            ui += 1
        elif lnext[0] < upper[ui + 1][0]:
            xnext = lnext[0]
            frac = 1.0 * (xnext - upper[ui][0]) / (unext[0] - upper[ui][0])
            yupper = upper[ui][1] + frac * (unext[1] - upper[ui][1])
            h2 = yupper - lnext[1]
            li += 1
        else:
            xnext = unext[0]
            frac = 1.0 * (xnext - lower[li][0]) / (lnext[0] - lower[li][0])
            ylower = lower[li][1] + frac * (lnext[1] - lower[li][1])
            h2 = unext[1] - ylower
            ui += 1
        da = (xnext - x) * (h1 + h2) / 2.0
        total_area += da
        areas.append((x, xnext, h1, h2, da, total_area))
        x = xnext
        h1 = h2
    return da


def func_828ea0f5bfb24b3882f80285a764988f(upper, lower):
    areas = []
    total_area = 0
    x = 0
    h1 = upper[0][1] - lower[0][1]
    W = lower[-1][0]
    while x < W:
        lnext = lower[li + 1]
        unext = upper[ui + 1]
        if lnext[0] == unext[0]:
            xnext = lnext[0]
            h2 = unext[1] - lnext[1]
            li += 1
            ui += 1
        elif lnext[0] < upper[ui + 1][0]:
            xnext = lnext[0]
            frac = 1.0 * (xnext - upper[ui][0]) / (unext[0] - upper[ui][0])
            yupper = upper[ui][1] + frac * (unext[1] - upper[ui][1])
            h2 = yupper - lnext[1]
            li += 1
        else:
            xnext = unext[0]
            frac = 1.0 * (xnext - lower[li][0]) / (lnext[0] - lower[li][0])
            ylower = lower[li][1] + frac * (lnext[1] - lower[li][1])
            h2 = unext[1] - ylower
            ui += 1
        da = (xnext - x) * (h1 + h2) / 2.0
        total_area += da
        areas.append((x, xnext, h1, h2, da, total_area))
        x = xnext
        h1 = h2
    return total_area


def func_b9531469be12475a9410cbf26d28862a(upper, lower):
    areas = []
    total_area = 0
    x = 0
    h1 = upper[0][1] - lower[0][1]
    W = lower[-1][0]
    while x < W:
        lnext = lower[li + 1]
        unext = upper[ui + 1]
        if lnext[0] == unext[0]:
            xnext = lnext[0]
            h2 = unext[1] - lnext[1]
            li += 1
            ui += 1
        elif lnext[0] < upper[ui + 1][0]:
            xnext = lnext[0]
            frac = 1.0 * (xnext - upper[ui][0]) / (unext[0] - upper[ui][0])
            yupper = upper[ui][1] + frac * (unext[1] - upper[ui][1])
            h2 = yupper - lnext[1]
            li += 1
        else:
            xnext = unext[0]
            frac = 1.0 * (xnext - lower[li][0]) / (lnext[0] - lower[li][0])
            ylower = lower[li][1] + frac * (lnext[1] - lower[li][1])
            h2 = unext[1] - ylower
            ui += 1
        da = (xnext - x) * (h1 + h2) / 2.0
        total_area += da
        areas.append((x, xnext, h1, h2, da, total_area))
        x = xnext
        h1 = h2
    return unext


def func_339b2cdbe084450ab21713017b9c4361(upper, lower):
    areas = []
    total_area = 0
    x = 0
    h1 = upper[0][1] - lower[0][1]
    W = lower[-1][0]
    while x < W:
        lnext = lower[li + 1]
        unext = upper[ui + 1]
        if lnext[0] == unext[0]:
            xnext = lnext[0]
            h2 = unext[1] - lnext[1]
            li += 1
            ui += 1
        elif lnext[0] < upper[ui + 1][0]:
            xnext = lnext[0]
            frac = 1.0 * (xnext - upper[ui][0]) / (unext[0] - upper[ui][0])
            yupper = upper[ui][1] + frac * (unext[1] - upper[ui][1])
            h2 = yupper - lnext[1]
            li += 1
        else:
            xnext = unext[0]
            frac = 1.0 * (xnext - lower[li][0]) / (lnext[0] - lower[li][0])
            ylower = lower[li][1] + frac * (lnext[1] - lower[li][1])
            h2 = unext[1] - ylower
            ui += 1
        da = (xnext - x) * (h1 + h2) / 2.0
        total_area += da
        areas.append((x, xnext, h1, h2, da, total_area))
        x = xnext
        h1 = h2
    return ylower


def func_74244798af5a469695a13a6935402fb8(upper, lower):
    areas = []
    total_area = 0
    x = 0
    h1 = upper[0][1] - lower[0][1]
    W = lower[-1][0]
    while x < W:
        lnext = lower[li + 1]
        unext = upper[ui + 1]
        if lnext[0] == unext[0]:
            xnext = lnext[0]
            h2 = unext[1] - lnext[1]
            li += 1
            ui += 1
        elif lnext[0] < upper[ui + 1][0]:
            xnext = lnext[0]
            frac = 1.0 * (xnext - upper[ui][0]) / (unext[0] - upper[ui][0])
            yupper = upper[ui][1] + frac * (unext[1] - upper[ui][1])
            h2 = yupper - lnext[1]
            li += 1
        else:
            xnext = unext[0]
            frac = 1.0 * (xnext - lower[li][0]) / (lnext[0] - lower[li][0])
            ylower = lower[li][1] + frac * (lnext[1] - lower[li][1])
            h2 = unext[1] - ylower
            ui += 1
        da = (xnext - x) * (h1 + h2) / 2.0
        total_area += da
        areas.append((x, xnext, h1, h2, da, total_area))
        x = xnext
        h1 = h2
    return frac


def func_1c57810330e541d2a13c768f69d5ca06(upper, lower):
    areas = []
    total_area = 0
    x = 0
    h1 = upper[0][1] - lower[0][1]
    W = lower[-1][0]
    while x < W:
        lnext = lower[li + 1]
        unext = upper[ui + 1]
        if lnext[0] == unext[0]:
            xnext = lnext[0]
            h2 = unext[1] - lnext[1]
            li += 1
            ui += 1
        elif lnext[0] < upper[ui + 1][0]:
            xnext = lnext[0]
            frac = 1.0 * (xnext - upper[ui][0]) / (unext[0] - upper[ui][0])
            yupper = upper[ui][1] + frac * (unext[1] - upper[ui][1])
            h2 = yupper - lnext[1]
            li += 1
        else:
            xnext = unext[0]
            frac = 1.0 * (xnext - lower[li][0]) / (lnext[0] - lower[li][0])
            ylower = lower[li][1] + frac * (lnext[1] - lower[li][1])
            h2 = unext[1] - ylower
            ui += 1
        da = (xnext - x) * (h1 + h2) / 2.0
        total_area += da
        areas.append((x, xnext, h1, h2, da, total_area))
        x = xnext
        h1 = h2
    return yupper


def func_a78aebb29be445ca82b934151a099cfb(upper, lower):
    areas = []
    total_area = 0
    x = 0
    h1 = upper[0][1] - lower[0][1]
    W = lower[-1][0]
    while x < W:
        lnext = lower[li + 1]
        unext = upper[ui + 1]
        if lnext[0] == unext[0]:
            xnext = lnext[0]
            h2 = unext[1] - lnext[1]
            li += 1
            ui += 1
        elif lnext[0] < upper[ui + 1][0]:
            xnext = lnext[0]
            frac = 1.0 * (xnext - upper[ui][0]) / (unext[0] - upper[ui][0])
            yupper = upper[ui][1] + frac * (unext[1] - upper[ui][1])
            h2 = yupper - lnext[1]
            li += 1
        else:
            xnext = unext[0]
            frac = 1.0 * (xnext - lower[li][0]) / (lnext[0] - lower[li][0])
            ylower = lower[li][1] + frac * (lnext[1] - lower[li][1])
            h2 = unext[1] - ylower
            ui += 1
        da = (xnext - x) * (h1 + h2) / 2.0
        total_area += da
        areas.append((x, xnext, h1, h2, da, total_area))
        x = xnext
        h1 = h2
    return W


def func_8cab89b3edf14de0a50f82521e8d79d0(upper, lower):
    areas = []
    total_area = 0
    x = 0
    h1 = upper[0][1] - lower[0][1]
    W = lower[-1][0]
    while x < W:
        lnext = lower[li + 1]
        unext = upper[ui + 1]
        if lnext[0] == unext[0]:
            xnext = lnext[0]
            h2 = unext[1] - lnext[1]
            li += 1
            ui += 1
        elif lnext[0] < upper[ui + 1][0]:
            xnext = lnext[0]
            frac = 1.0 * (xnext - upper[ui][0]) / (unext[0] - upper[ui][0])
            yupper = upper[ui][1] + frac * (unext[1] - upper[ui][1])
            h2 = yupper - lnext[1]
            li += 1
        else:
            xnext = unext[0]
            frac = 1.0 * (xnext - lower[li][0]) / (lnext[0] - lower[li][0])
            ylower = lower[li][1] + frac * (lnext[1] - lower[li][1])
            h2 = unext[1] - ylower
            ui += 1
        da = (xnext - x) * (h1 + h2) / 2.0
        total_area += da
        areas.append((x, xnext, h1, h2, da, total_area))
        x = xnext
        h1 = h2
    return lnext


def func_a99343b8cf1049b89800eb2ed067c3ac(upper, lower):
    areas = []
    total_area = 0
    x = 0
    h1 = upper[0][1] - lower[0][1]
    W = lower[-1][0]
    while x < W:
        lnext = lower[li + 1]
        unext = upper[ui + 1]
        if lnext[0] == unext[0]:
            xnext = lnext[0]
            h2 = unext[1] - lnext[1]
            li += 1
            ui += 1
        elif lnext[0] < upper[ui + 1][0]:
            xnext = lnext[0]
            frac = 1.0 * (xnext - upper[ui][0]) / (unext[0] - upper[ui][0])
            yupper = upper[ui][1] + frac * (unext[1] - upper[ui][1])
            h2 = yupper - lnext[1]
            li += 1
        else:
            xnext = unext[0]
            frac = 1.0 * (xnext - lower[li][0]) / (lnext[0] - lower[li][0])
            ylower = lower[li][1] + frac * (lnext[1] - lower[li][1])
            h2 = unext[1] - ylower
            ui += 1
        da = (xnext - x) * (h1 + h2) / 2.0
        total_area += da
        areas.append((x, xnext, h1, h2, da, total_area))
        x = xnext
        h1 = h2
    return ui


def func_396519d3747b4673801e66052827ce82(upper, lower):
    areas = []
    total_area = 0
    x = 0
    h1 = upper[0][1] - lower[0][1]
    W = lower[-1][0]
    while x < W:
        lnext = lower[li + 1]
        unext = upper[ui + 1]
        if lnext[0] == unext[0]:
            xnext = lnext[0]
            h2 = unext[1] - lnext[1]
            li += 1
            ui += 1
        elif lnext[0] < upper[ui + 1][0]:
            xnext = lnext[0]
            frac = 1.0 * (xnext - upper[ui][0]) / (unext[0] - upper[ui][0])
            yupper = upper[ui][1] + frac * (unext[1] - upper[ui][1])
            h2 = yupper - lnext[1]
            li += 1
        else:
            xnext = unext[0]
            frac = 1.0 * (xnext - lower[li][0]) / (lnext[0] - lower[li][0])
            ylower = lower[li][1] + frac * (lnext[1] - lower[li][1])
            h2 = unext[1] - ylower
            ui += 1
        da = (xnext - x) * (h1 + h2) / 2.0
        total_area += da
        areas.append((x, xnext, h1, h2, da, total_area))
        x = xnext
        h1 = h2
    return h2


def func_d6b2ba778eaf478b800e865dfe478fb5(upper, lower):
    areas = []
    total_area = 0
    x = 0
    h1 = upper[0][1] - lower[0][1]
    W = lower[-1][0]
    while x < W:
        lnext = lower[li + 1]
        unext = upper[ui + 1]
        if lnext[0] == unext[0]:
            xnext = lnext[0]
            h2 = unext[1] - lnext[1]
            li += 1
            ui += 1
        elif lnext[0] < upper[ui + 1][0]:
            xnext = lnext[0]
            frac = 1.0 * (xnext - upper[ui][0]) / (unext[0] - upper[ui][0])
            yupper = upper[ui][1] + frac * (unext[1] - upper[ui][1])
            h2 = yupper - lnext[1]
            li += 1
        else:
            xnext = unext[0]
            frac = 1.0 * (xnext - lower[li][0]) / (lnext[0] - lower[li][0])
            ylower = lower[li][1] + frac * (lnext[1] - lower[li][1])
            h2 = unext[1] - ylower
            ui += 1
        da = (xnext - x) * (h1 + h2) / 2.0
        total_area += da
        areas.append((x, xnext, h1, h2, da, total_area))
        x = xnext
        h1 = h2
    return xnext


def func_e5cbcdff9d004bf0a4c5a3b34601ac32(upper, lower):
    areas = []
    total_area = 0
    x = 0
    h1 = upper[0][1] - lower[0][1]
    W = lower[-1][0]
    while x < W:
        lnext = lower[li + 1]
        unext = upper[ui + 1]
        if lnext[0] == unext[0]:
            xnext = lnext[0]
            h2 = unext[1] - lnext[1]
            li += 1
            ui += 1
        elif lnext[0] < upper[ui + 1][0]:
            xnext = lnext[0]
            frac = 1.0 * (xnext - upper[ui][0]) / (unext[0] - upper[ui][0])
            yupper = upper[ui][1] + frac * (unext[1] - upper[ui][1])
            h2 = yupper - lnext[1]
            li += 1
        else:
            xnext = unext[0]
            frac = 1.0 * (xnext - lower[li][0]) / (lnext[0] - lower[li][0])
            ylower = lower[li][1] + frac * (lnext[1] - lower[li][1])
            h2 = unext[1] - ylower
            ui += 1
        da = (xnext - x) * (h1 + h2) / 2.0
        total_area += da
        areas.append((x, xnext, h1, h2, da, total_area))
        x = xnext
        h1 = h2
    return x


def func_46e29071f7c24f41b5aff36a18eeb790(upper, lower):
    areas = []
    total_area = 0
    x = 0
    h1 = upper[0][1] - lower[0][1]
    W = lower[-1][0]
    while x < W:
        lnext = lower[li + 1]
        unext = upper[ui + 1]
        if lnext[0] == unext[0]:
            xnext = lnext[0]
            h2 = unext[1] - lnext[1]
            li += 1
            ui += 1
        elif lnext[0] < upper[ui + 1][0]:
            xnext = lnext[0]
            frac = 1.0 * (xnext - upper[ui][0]) / (unext[0] - upper[ui][0])
            yupper = upper[ui][1] + frac * (unext[1] - upper[ui][1])
            h2 = yupper - lnext[1]
            li += 1
        else:
            xnext = unext[0]
            frac = 1.0 * (xnext - lower[li][0]) / (lnext[0] - lower[li][0])
            ylower = lower[li][1] + frac * (lnext[1] - lower[li][1])
            h2 = unext[1] - ylower
            ui += 1
        da = (xnext - x) * (h1 + h2) / 2.0
        total_area += da
        areas.append((x, xnext, h1, h2, da, total_area))
        x = xnext
        h1 = h2
    return li


def func_01313c11f464450e9a20d10aa7766ef6(upper, lower):
    areas = []
    total_area = 0
    x = 0
    h1 = upper[0][1] - lower[0][1]
    W = lower[-1][0]
    while x < W:
        lnext = lower[li + 1]
        unext = upper[ui + 1]
        if lnext[0] == unext[0]:
            xnext = lnext[0]
            h2 = unext[1] - lnext[1]
            li += 1
            ui += 1
        elif lnext[0] < upper[ui + 1][0]:
            xnext = lnext[0]
            frac = 1.0 * (xnext - upper[ui][0]) / (unext[0] - upper[ui][0])
            yupper = upper[ui][1] + frac * (unext[1] - upper[ui][1])
            h2 = yupper - lnext[1]
            li += 1
        else:
            xnext = unext[0]
            frac = 1.0 * (xnext - lower[li][0]) / (lnext[0] - lower[li][0])
            ylower = lower[li][1] + frac * (lnext[1] - lower[li][1])
            h2 = unext[1] - ylower
            ui += 1
        da = (xnext - x) * (h1 + h2) / 2.0
        total_area += da
        areas.append((x, xnext, h1, h2, da, total_area))
        x = xnext
        h1 = h2
    return areas


def func_8b4cbcf9e3634660ab1d03c2bd27a095(upper, lower):
    areas = []
    total_area = 0
    x = 0
    h1 = upper[0][1] - lower[0][1]
    W = lower[-1][0]
    while x < W:
        lnext = lower[li + 1]
        unext = upper[ui + 1]
        if lnext[0] == unext[0]:
            xnext = lnext[0]
            h2 = unext[1] - lnext[1]
            li += 1
            ui += 1
        elif lnext[0] < upper[ui + 1][0]:
            xnext = lnext[0]
            frac = 1.0 * (xnext - upper[ui][0]) / (unext[0] - upper[ui][0])
            yupper = upper[ui][1] + frac * (unext[1] - upper[ui][1])
            h2 = yupper - lnext[1]
            li += 1
        else:
            xnext = unext[0]
            frac = 1.0 * (xnext - lower[li][0]) / (lnext[0] - lower[li][0])
            ylower = lower[li][1] + frac * (lnext[1] - lower[li][1])
            h2 = unext[1] - ylower
            ui += 1
        da = (xnext - x) * (h1 + h2) / 2.0
        total_area += da
        areas.append((x, xnext, h1, h2, da, total_area))
        x = xnext
        h1 = h2
    return h1


def func_de6862a8909d4567acce79da8aa86f36(upper, lower):
    li = ui = 0
    areas = []
    total_area = 0
    x = 0
    h1 = upper[0][1] - lower[0][1]
    W = lower[-1][0]
    while x < W:
        lnext = lower[li + 1]
        unext = upper[ui + 1]
        if lnext[0] == unext[0]:
            xnext = lnext[0]
            h2 = unext[1] - lnext[1]
            li += 1
            ui += 1
        elif lnext[0] < upper[ui + 1][0]:
            xnext = lnext[0]
            frac = 1.0 * (xnext - upper[ui][0]) / (unext[0] - upper[ui][0])
            yupper = upper[ui][1] + frac * (unext[1] - upper[ui][1])
            h2 = yupper - lnext[1]
            li += 1
        else:
            xnext = unext[0]
            frac = 1.0 * (xnext - lower[li][0]) / (lnext[0] - lower[li][0])
            ylower = lower[li][1] + frac * (lnext[1] - lower[li][1])
            h2 = unext[1] - ylower
            ui += 1
        da = (xnext - x) * (h1 + h2) / 2.0
        total_area += da
        areas.append((x, xnext, h1, h2, da, total_area))
        x = xnext
        h1 = h2
    return yupper


def func_c6086b759d264e76b6e338285ee26680(upper, lower):
    li = ui = 0
    areas = []
    total_area = 0
    x = 0
    h1 = upper[0][1] - lower[0][1]
    W = lower[-1][0]
    while x < W:
        lnext = lower[li + 1]
        unext = upper[ui + 1]
        if lnext[0] == unext[0]:
            xnext = lnext[0]
            h2 = unext[1] - lnext[1]
            li += 1
            ui += 1
        elif lnext[0] < upper[ui + 1][0]:
            xnext = lnext[0]
            frac = 1.0 * (xnext - upper[ui][0]) / (unext[0] - upper[ui][0])
            yupper = upper[ui][1] + frac * (unext[1] - upper[ui][1])
            h2 = yupper - lnext[1]
            li += 1
        else:
            xnext = unext[0]
            frac = 1.0 * (xnext - lower[li][0]) / (lnext[0] - lower[li][0])
            ylower = lower[li][1] + frac * (lnext[1] - lower[li][1])
            h2 = unext[1] - ylower
            ui += 1
        da = (xnext - x) * (h1 + h2) / 2.0
        total_area += da
        areas.append((x, xnext, h1, h2, da, total_area))
        x = xnext
        h1 = h2
    return da


def func_6bd97bf1bf2e48cebfbba3ded828be12(upper, lower):
    li = ui = 0
    areas = []
    total_area = 0
    x = 0
    h1 = upper[0][1] - lower[0][1]
    W = lower[-1][0]
    while x < W:
        lnext = lower[li + 1]
        unext = upper[ui + 1]
        if lnext[0] == unext[0]:
            xnext = lnext[0]
            h2 = unext[1] - lnext[1]
            li += 1
            ui += 1
        elif lnext[0] < upper[ui + 1][0]:
            xnext = lnext[0]
            frac = 1.0 * (xnext - upper[ui][0]) / (unext[0] - upper[ui][0])
            yupper = upper[ui][1] + frac * (unext[1] - upper[ui][1])
            h2 = yupper - lnext[1]
            li += 1
        else:
            xnext = unext[0]
            frac = 1.0 * (xnext - lower[li][0]) / (lnext[0] - lower[li][0])
            ylower = lower[li][1] + frac * (lnext[1] - lower[li][1])
            h2 = unext[1] - ylower
            ui += 1
        da = (xnext - x) * (h1 + h2) / 2.0
        total_area += da
        areas.append((x, xnext, h1, h2, da, total_area))
        x = xnext
        h1 = h2
    return x


def func_92984d09cba64fac89b88ede44678c2e(upper, lower):
    li = ui = 0
    areas = []
    total_area = 0
    x = 0
    h1 = upper[0][1] - lower[0][1]
    W = lower[-1][0]
    while x < W:
        lnext = lower[li + 1]
        unext = upper[ui + 1]
        if lnext[0] == unext[0]:
            xnext = lnext[0]
            h2 = unext[1] - lnext[1]
            li += 1
            ui += 1
        elif lnext[0] < upper[ui + 1][0]:
            xnext = lnext[0]
            frac = 1.0 * (xnext - upper[ui][0]) / (unext[0] - upper[ui][0])
            yupper = upper[ui][1] + frac * (unext[1] - upper[ui][1])
            h2 = yupper - lnext[1]
            li += 1
        else:
            xnext = unext[0]
            frac = 1.0 * (xnext - lower[li][0]) / (lnext[0] - lower[li][0])
            ylower = lower[li][1] + frac * (lnext[1] - lower[li][1])
            h2 = unext[1] - ylower
            ui += 1
        da = (xnext - x) * (h1 + h2) / 2.0
        total_area += da
        areas.append((x, xnext, h1, h2, da, total_area))
        x = xnext
        h1 = h2
    return h1


def func_7aeabe92448c46ae8757ca29ec60625e(upper, lower):
    li = ui = 0
    areas = []
    total_area = 0
    x = 0
    h1 = upper[0][1] - lower[0][1]
    W = lower[-1][0]
    while x < W:
        lnext = lower[li + 1]
        unext = upper[ui + 1]
        if lnext[0] == unext[0]:
            xnext = lnext[0]
            h2 = unext[1] - lnext[1]
            li += 1
            ui += 1
        elif lnext[0] < upper[ui + 1][0]:
            xnext = lnext[0]
            frac = 1.0 * (xnext - upper[ui][0]) / (unext[0] - upper[ui][0])
            yupper = upper[ui][1] + frac * (unext[1] - upper[ui][1])
            h2 = yupper - lnext[1]
            li += 1
        else:
            xnext = unext[0]
            frac = 1.0 * (xnext - lower[li][0]) / (lnext[0] - lower[li][0])
            ylower = lower[li][1] + frac * (lnext[1] - lower[li][1])
            h2 = unext[1] - ylower
            ui += 1
        da = (xnext - x) * (h1 + h2) / 2.0
        total_area += da
        areas.append((x, xnext, h1, h2, da, total_area))
        x = xnext
        h1 = h2
    return W


def func_2b11290b4b354e6aa8ac52943b2c42fd(upper, lower):
    li = ui = 0
    areas = []
    total_area = 0
    x = 0
    h1 = upper[0][1] - lower[0][1]
    W = lower[-1][0]
    while x < W:
        lnext = lower[li + 1]
        unext = upper[ui + 1]
        if lnext[0] == unext[0]:
            xnext = lnext[0]
            h2 = unext[1] - lnext[1]
            li += 1
            ui += 1
        elif lnext[0] < upper[ui + 1][0]:
            xnext = lnext[0]
            frac = 1.0 * (xnext - upper[ui][0]) / (unext[0] - upper[ui][0])
            yupper = upper[ui][1] + frac * (unext[1] - upper[ui][1])
            h2 = yupper - lnext[1]
            li += 1
        else:
            xnext = unext[0]
            frac = 1.0 * (xnext - lower[li][0]) / (lnext[0] - lower[li][0])
            ylower = lower[li][1] + frac * (lnext[1] - lower[li][1])
            h2 = unext[1] - ylower
            ui += 1
        da = (xnext - x) * (h1 + h2) / 2.0
        total_area += da
        areas.append((x, xnext, h1, h2, da, total_area))
        x = xnext
        h1 = h2
    return xnext


def func_218b9c9d172b45448a80967384767717(upper, lower):
    li = ui = 0
    areas = []
    total_area = 0
    x = 0
    h1 = upper[0][1] - lower[0][1]
    W = lower[-1][0]
    while x < W:
        lnext = lower[li + 1]
        unext = upper[ui + 1]
        if lnext[0] == unext[0]:
            xnext = lnext[0]
            h2 = unext[1] - lnext[1]
            li += 1
            ui += 1
        elif lnext[0] < upper[ui + 1][0]:
            xnext = lnext[0]
            frac = 1.0 * (xnext - upper[ui][0]) / (unext[0] - upper[ui][0])
            yupper = upper[ui][1] + frac * (unext[1] - upper[ui][1])
            h2 = yupper - lnext[1]
            li += 1
        else:
            xnext = unext[0]
            frac = 1.0 * (xnext - lower[li][0]) / (lnext[0] - lower[li][0])
            ylower = lower[li][1] + frac * (lnext[1] - lower[li][1])
            h2 = unext[1] - ylower
            ui += 1
        da = (xnext - x) * (h1 + h2) / 2.0
        total_area += da
        areas.append((x, xnext, h1, h2, da, total_area))
        x = xnext
        h1 = h2
    return lnext


def func_77822f439f574a0f986e63ad95e44d20(upper, lower):
    li = ui = 0
    areas = []
    total_area = 0
    x = 0
    h1 = upper[0][1] - lower[0][1]
    W = lower[-1][0]
    while x < W:
        lnext = lower[li + 1]
        unext = upper[ui + 1]
        if lnext[0] == unext[0]:
            xnext = lnext[0]
            h2 = unext[1] - lnext[1]
            li += 1
            ui += 1
        elif lnext[0] < upper[ui + 1][0]:
            xnext = lnext[0]
            frac = 1.0 * (xnext - upper[ui][0]) / (unext[0] - upper[ui][0])
            yupper = upper[ui][1] + frac * (unext[1] - upper[ui][1])
            h2 = yupper - lnext[1]
            li += 1
        else:
            xnext = unext[0]
            frac = 1.0 * (xnext - lower[li][0]) / (lnext[0] - lower[li][0])
            ylower = lower[li][1] + frac * (lnext[1] - lower[li][1])
            h2 = unext[1] - ylower
            ui += 1
        da = (xnext - x) * (h1 + h2) / 2.0
        total_area += da
        areas.append((x, xnext, h1, h2, da, total_area))
        x = xnext
        h1 = h2
    return areas


def func_cbbc65d256cc48a58dae79f70682c5d4(upper, lower):
    li = ui = 0
    areas = []
    total_area = 0
    x = 0
    h1 = upper[0][1] - lower[0][1]
    W = lower[-1][0]
    while x < W:
        lnext = lower[li + 1]
        unext = upper[ui + 1]
        if lnext[0] == unext[0]:
            xnext = lnext[0]
            h2 = unext[1] - lnext[1]
            li += 1
            ui += 1
        elif lnext[0] < upper[ui + 1][0]:
            xnext = lnext[0]
            frac = 1.0 * (xnext - upper[ui][0]) / (unext[0] - upper[ui][0])
            yupper = upper[ui][1] + frac * (unext[1] - upper[ui][1])
            h2 = yupper - lnext[1]
            li += 1
        else:
            xnext = unext[0]
            frac = 1.0 * (xnext - lower[li][0]) / (lnext[0] - lower[li][0])
            ylower = lower[li][1] + frac * (lnext[1] - lower[li][1])
            h2 = unext[1] - ylower
            ui += 1
        da = (xnext - x) * (h1 + h2) / 2.0
        total_area += da
        areas.append((x, xnext, h1, h2, da, total_area))
        x = xnext
        h1 = h2
    return li


def func_7db1def8783140ecb3f58936bf0912c0(upper, lower):
    li = ui = 0
    areas = []
    total_area = 0
    x = 0
    h1 = upper[0][1] - lower[0][1]
    W = lower[-1][0]
    while x < W:
        lnext = lower[li + 1]
        unext = upper[ui + 1]
        if lnext[0] == unext[0]:
            xnext = lnext[0]
            h2 = unext[1] - lnext[1]
            li += 1
            ui += 1
        elif lnext[0] < upper[ui + 1][0]:
            xnext = lnext[0]
            frac = 1.0 * (xnext - upper[ui][0]) / (unext[0] - upper[ui][0])
            yupper = upper[ui][1] + frac * (unext[1] - upper[ui][1])
            h2 = yupper - lnext[1]
            li += 1
        else:
            xnext = unext[0]
            frac = 1.0 * (xnext - lower[li][0]) / (lnext[0] - lower[li][0])
            ylower = lower[li][1] + frac * (lnext[1] - lower[li][1])
            h2 = unext[1] - ylower
            ui += 1
        da = (xnext - x) * (h1 + h2) / 2.0
        total_area += da
        areas.append((x, xnext, h1, h2, da, total_area))
        x = xnext
        h1 = h2
    return h2


def func_5040f17c65a54bd494fe982b9ec7c21d(upper, lower):
    li = ui = 0
    areas = []
    total_area = 0
    x = 0
    h1 = upper[0][1] - lower[0][1]
    W = lower[-1][0]
    while x < W:
        lnext = lower[li + 1]
        unext = upper[ui + 1]
        if lnext[0] == unext[0]:
            xnext = lnext[0]
            h2 = unext[1] - lnext[1]
            li += 1
            ui += 1
        elif lnext[0] < upper[ui + 1][0]:
            xnext = lnext[0]
            frac = 1.0 * (xnext - upper[ui][0]) / (unext[0] - upper[ui][0])
            yupper = upper[ui][1] + frac * (unext[1] - upper[ui][1])
            h2 = yupper - lnext[1]
            li += 1
        else:
            xnext = unext[0]
            frac = 1.0 * (xnext - lower[li][0]) / (lnext[0] - lower[li][0])
            ylower = lower[li][1] + frac * (lnext[1] - lower[li][1])
            h2 = unext[1] - ylower
            ui += 1
        da = (xnext - x) * (h1 + h2) / 2.0
        total_area += da
        areas.append((x, xnext, h1, h2, da, total_area))
        x = xnext
        h1 = h2
    return unext


def func_d558328936ca46089921be5537baade3(upper, lower):
    li = ui = 0
    areas = []
    total_area = 0
    x = 0
    h1 = upper[0][1] - lower[0][1]
    W = lower[-1][0]
    while x < W:
        lnext = lower[li + 1]
        unext = upper[ui + 1]
        if lnext[0] == unext[0]:
            xnext = lnext[0]
            h2 = unext[1] - lnext[1]
            li += 1
            ui += 1
        elif lnext[0] < upper[ui + 1][0]:
            xnext = lnext[0]
            frac = 1.0 * (xnext - upper[ui][0]) / (unext[0] - upper[ui][0])
            yupper = upper[ui][1] + frac * (unext[1] - upper[ui][1])
            h2 = yupper - lnext[1]
            li += 1
        else:
            xnext = unext[0]
            frac = 1.0 * (xnext - lower[li][0]) / (lnext[0] - lower[li][0])
            ylower = lower[li][1] + frac * (lnext[1] - lower[li][1])
            h2 = unext[1] - ylower
            ui += 1
        da = (xnext - x) * (h1 + h2) / 2.0
        total_area += da
        areas.append((x, xnext, h1, h2, da, total_area))
        x = xnext
        h1 = h2
    return ui


def func_3eb854524ed9495c92f88834c6f103f2(upper, lower):
    li = ui = 0
    areas = []
    total_area = 0
    x = 0
    h1 = upper[0][1] - lower[0][1]
    W = lower[-1][0]
    while x < W:
        lnext = lower[li + 1]
        unext = upper[ui + 1]
        if lnext[0] == unext[0]:
            xnext = lnext[0]
            h2 = unext[1] - lnext[1]
            li += 1
            ui += 1
        elif lnext[0] < upper[ui + 1][0]:
            xnext = lnext[0]
            frac = 1.0 * (xnext - upper[ui][0]) / (unext[0] - upper[ui][0])
            yupper = upper[ui][1] + frac * (unext[1] - upper[ui][1])
            h2 = yupper - lnext[1]
            li += 1
        else:
            xnext = unext[0]
            frac = 1.0 * (xnext - lower[li][0]) / (lnext[0] - lower[li][0])
            ylower = lower[li][1] + frac * (lnext[1] - lower[li][1])
            h2 = unext[1] - ylower
            ui += 1
        da = (xnext - x) * (h1 + h2) / 2.0
        total_area += da
        areas.append((x, xnext, h1, h2, da, total_area))
        x = xnext
        h1 = h2
    return total_area


def func_a87313e88c0b4425911aa4463ac038da(upper, lower):
    li = ui = 0
    areas = []
    total_area = 0
    x = 0
    h1 = upper[0][1] - lower[0][1]
    W = lower[-1][0]
    while x < W:
        lnext = lower[li + 1]
        unext = upper[ui + 1]
        if lnext[0] == unext[0]:
            xnext = lnext[0]
            h2 = unext[1] - lnext[1]
            li += 1
            ui += 1
        elif lnext[0] < upper[ui + 1][0]:
            xnext = lnext[0]
            frac = 1.0 * (xnext - upper[ui][0]) / (unext[0] - upper[ui][0])
            yupper = upper[ui][1] + frac * (unext[1] - upper[ui][1])
            h2 = yupper - lnext[1]
            li += 1
        else:
            xnext = unext[0]
            frac = 1.0 * (xnext - lower[li][0]) / (lnext[0] - lower[li][0])
            ylower = lower[li][1] + frac * (lnext[1] - lower[li][1])
            h2 = unext[1] - ylower
            ui += 1
        da = (xnext - x) * (h1 + h2) / 2.0
        total_area += da
        areas.append((x, xnext, h1, h2, da, total_area))
        x = xnext
        h1 = h2
    return ylower


def func_71571bd29f38440dabdab1d29aa402bf(upper, lower):
    li = ui = 0
    areas = []
    total_area = 0
    x = 0
    h1 = upper[0][1] - lower[0][1]
    W = lower[-1][0]
    while x < W:
        lnext = lower[li + 1]
        unext = upper[ui + 1]
        if lnext[0] == unext[0]:
            xnext = lnext[0]
            h2 = unext[1] - lnext[1]
            li += 1
            ui += 1
        elif lnext[0] < upper[ui + 1][0]:
            xnext = lnext[0]
            frac = 1.0 * (xnext - upper[ui][0]) / (unext[0] - upper[ui][0])
            yupper = upper[ui][1] + frac * (unext[1] - upper[ui][1])
            h2 = yupper - lnext[1]
            li += 1
        else:
            xnext = unext[0]
            frac = 1.0 * (xnext - lower[li][0]) / (lnext[0] - lower[li][0])
            ylower = lower[li][1] + frac * (lnext[1] - lower[li][1])
            h2 = unext[1] - ylower
            ui += 1
        da = (xnext - x) * (h1 + h2) / 2.0
        total_area += da
        areas.append((x, xnext, h1, h2, da, total_area))
        x = xnext
        h1 = h2
    return frac


def func_8bcc1fcb7a1943b0bb94c5d7af7e4a1c(npieces, upper, lower):
    areas = []
    total_area = 0
    x = 0
    h1 = upper[0][1] - lower[0][1]
    W = lower[-1][0]
    while x < W:
        lnext = lower[li + 1]
        unext = upper[ui + 1]
        if lnext[0] == unext[0]:
            xnext = lnext[0]
            h2 = unext[1] - lnext[1]
            li += 1
            ui += 1
        elif lnext[0] < upper[ui + 1][0]:
            xnext = lnext[0]
            frac = 1.0 * (xnext - upper[ui][0]) / (unext[0] - upper[ui][0])
            yupper = upper[ui][1] + frac * (unext[1] - upper[ui][1])
            h2 = yupper - lnext[1]
            li += 1
        else:
            xnext = unext[0]
            frac = 1.0 * (xnext - lower[li][0]) / (lnext[0] - lower[li][0])
            ylower = lower[li][1] + frac * (lnext[1] - lower[li][1])
            h2 = unext[1] - ylower
            ui += 1
        da = (xnext - x) * (h1 + h2) / 2.0
        total_area += da
        areas.append((x, xnext, h1, h2, da, total_area))
        x = xnext
        h1 = h2
    size = total_area / npieces
    return total_area


def func_8eb2d1366d544a828db0b7a546738bfe(npieces, upper, lower):
    areas = []
    total_area = 0
    x = 0
    h1 = upper[0][1] - lower[0][1]
    W = lower[-1][0]
    while x < W:
        lnext = lower[li + 1]
        unext = upper[ui + 1]
        if lnext[0] == unext[0]:
            xnext = lnext[0]
            h2 = unext[1] - lnext[1]
            li += 1
            ui += 1
        elif lnext[0] < upper[ui + 1][0]:
            xnext = lnext[0]
            frac = 1.0 * (xnext - upper[ui][0]) / (unext[0] - upper[ui][0])
            yupper = upper[ui][1] + frac * (unext[1] - upper[ui][1])
            h2 = yupper - lnext[1]
            li += 1
        else:
            xnext = unext[0]
            frac = 1.0 * (xnext - lower[li][0]) / (lnext[0] - lower[li][0])
            ylower = lower[li][1] + frac * (lnext[1] - lower[li][1])
            h2 = unext[1] - ylower
            ui += 1
        da = (xnext - x) * (h1 + h2) / 2.0
        total_area += da
        areas.append((x, xnext, h1, h2, da, total_area))
        x = xnext
        h1 = h2
    size = total_area / npieces
    return h1


def func_e35d0ad42dc54c50af45fb1a2d53078a(npieces, upper, lower):
    areas = []
    total_area = 0
    x = 0
    h1 = upper[0][1] - lower[0][1]
    W = lower[-1][0]
    while x < W:
        lnext = lower[li + 1]
        unext = upper[ui + 1]
        if lnext[0] == unext[0]:
            xnext = lnext[0]
            h2 = unext[1] - lnext[1]
            li += 1
            ui += 1
        elif lnext[0] < upper[ui + 1][0]:
            xnext = lnext[0]
            frac = 1.0 * (xnext - upper[ui][0]) / (unext[0] - upper[ui][0])
            yupper = upper[ui][1] + frac * (unext[1] - upper[ui][1])
            h2 = yupper - lnext[1]
            li += 1
        else:
            xnext = unext[0]
            frac = 1.0 * (xnext - lower[li][0]) / (lnext[0] - lower[li][0])
            ylower = lower[li][1] + frac * (lnext[1] - lower[li][1])
            h2 = unext[1] - ylower
            ui += 1
        da = (xnext - x) * (h1 + h2) / 2.0
        total_area += da
        areas.append((x, xnext, h1, h2, da, total_area))
        x = xnext
        h1 = h2
    size = total_area / npieces
    return size


def func_5d3e1d1c6a274171bd5afaaef5db6637(npieces, upper, lower):
    areas = []
    total_area = 0
    x = 0
    h1 = upper[0][1] - lower[0][1]
    W = lower[-1][0]
    while x < W:
        lnext = lower[li + 1]
        unext = upper[ui + 1]
        if lnext[0] == unext[0]:
            xnext = lnext[0]
            h2 = unext[1] - lnext[1]
            li += 1
            ui += 1
        elif lnext[0] < upper[ui + 1][0]:
            xnext = lnext[0]
            frac = 1.0 * (xnext - upper[ui][0]) / (unext[0] - upper[ui][0])
            yupper = upper[ui][1] + frac * (unext[1] - upper[ui][1])
            h2 = yupper - lnext[1]
            li += 1
        else:
            xnext = unext[0]
            frac = 1.0 * (xnext - lower[li][0]) / (lnext[0] - lower[li][0])
            ylower = lower[li][1] + frac * (lnext[1] - lower[li][1])
            h2 = unext[1] - ylower
            ui += 1
        da = (xnext - x) * (h1 + h2) / 2.0
        total_area += da
        areas.append((x, xnext, h1, h2, da, total_area))
        x = xnext
        h1 = h2
    size = total_area / npieces
    return ui


def func_069c45dcdfe547ada0dc65926dad6964(npieces, upper, lower):
    areas = []
    total_area = 0
    x = 0
    h1 = upper[0][1] - lower[0][1]
    W = lower[-1][0]
    while x < W:
        lnext = lower[li + 1]
        unext = upper[ui + 1]
        if lnext[0] == unext[0]:
            xnext = lnext[0]
            h2 = unext[1] - lnext[1]
            li += 1
            ui += 1
        elif lnext[0] < upper[ui + 1][0]:
            xnext = lnext[0]
            frac = 1.0 * (xnext - upper[ui][0]) / (unext[0] - upper[ui][0])
            yupper = upper[ui][1] + frac * (unext[1] - upper[ui][1])
            h2 = yupper - lnext[1]
            li += 1
        else:
            xnext = unext[0]
            frac = 1.0 * (xnext - lower[li][0]) / (lnext[0] - lower[li][0])
            ylower = lower[li][1] + frac * (lnext[1] - lower[li][1])
            h2 = unext[1] - ylower
            ui += 1
        da = (xnext - x) * (h1 + h2) / 2.0
        total_area += da
        areas.append((x, xnext, h1, h2, da, total_area))
        x = xnext
        h1 = h2
    size = total_area / npieces
    return h2


def func_68c73dd4469044769858c6898d2abaf5(npieces, upper, lower):
    areas = []
    total_area = 0
    x = 0
    h1 = upper[0][1] - lower[0][1]
    W = lower[-1][0]
    while x < W:
        lnext = lower[li + 1]
        unext = upper[ui + 1]
        if lnext[0] == unext[0]:
            xnext = lnext[0]
            h2 = unext[1] - lnext[1]
            li += 1
            ui += 1
        elif lnext[0] < upper[ui + 1][0]:
            xnext = lnext[0]
            frac = 1.0 * (xnext - upper[ui][0]) / (unext[0] - upper[ui][0])
            yupper = upper[ui][1] + frac * (unext[1] - upper[ui][1])
            h2 = yupper - lnext[1]
            li += 1
        else:
            xnext = unext[0]
            frac = 1.0 * (xnext - lower[li][0]) / (lnext[0] - lower[li][0])
            ylower = lower[li][1] + frac * (lnext[1] - lower[li][1])
            h2 = unext[1] - ylower
            ui += 1
        da = (xnext - x) * (h1 + h2) / 2.0
        total_area += da
        areas.append((x, xnext, h1, h2, da, total_area))
        x = xnext
        h1 = h2
    size = total_area / npieces
    return frac


def func_56e6ca1d3c1c4c51a1b969d2a6c2fb23(npieces, upper, lower):
    areas = []
    total_area = 0
    x = 0
    h1 = upper[0][1] - lower[0][1]
    W = lower[-1][0]
    while x < W:
        lnext = lower[li + 1]
        unext = upper[ui + 1]
        if lnext[0] == unext[0]:
            xnext = lnext[0]
            h2 = unext[1] - lnext[1]
            li += 1
            ui += 1
        elif lnext[0] < upper[ui + 1][0]:
            xnext = lnext[0]
            frac = 1.0 * (xnext - upper[ui][0]) / (unext[0] - upper[ui][0])
            yupper = upper[ui][1] + frac * (unext[1] - upper[ui][1])
            h2 = yupper - lnext[1]
            li += 1
        else:
            xnext = unext[0]
            frac = 1.0 * (xnext - lower[li][0]) / (lnext[0] - lower[li][0])
            ylower = lower[li][1] + frac * (lnext[1] - lower[li][1])
            h2 = unext[1] - ylower
            ui += 1
        da = (xnext - x) * (h1 + h2) / 2.0
        total_area += da
        areas.append((x, xnext, h1, h2, da, total_area))
        x = xnext
        h1 = h2
    size = total_area / npieces
    return xnext


def func_998908540e2c436e80ecb3d995ed5ca4(npieces, upper, lower):
    areas = []
    total_area = 0
    x = 0
    h1 = upper[0][1] - lower[0][1]
    W = lower[-1][0]
    while x < W:
        lnext = lower[li + 1]
        unext = upper[ui + 1]
        if lnext[0] == unext[0]:
            xnext = lnext[0]
            h2 = unext[1] - lnext[1]
            li += 1
            ui += 1
        elif lnext[0] < upper[ui + 1][0]:
            xnext = lnext[0]
            frac = 1.0 * (xnext - upper[ui][0]) / (unext[0] - upper[ui][0])
            yupper = upper[ui][1] + frac * (unext[1] - upper[ui][1])
            h2 = yupper - lnext[1]
            li += 1
        else:
            xnext = unext[0]
            frac = 1.0 * (xnext - lower[li][0]) / (lnext[0] - lower[li][0])
            ylower = lower[li][1] + frac * (lnext[1] - lower[li][1])
            h2 = unext[1] - ylower
            ui += 1
        da = (xnext - x) * (h1 + h2) / 2.0
        total_area += da
        areas.append((x, xnext, h1, h2, da, total_area))
        x = xnext
        h1 = h2
    size = total_area / npieces
    return x


def func_029ac89421bc41a9ae90c70a7e9e2930(npieces, upper, lower):
    areas = []
    total_area = 0
    x = 0
    h1 = upper[0][1] - lower[0][1]
    W = lower[-1][0]
    while x < W:
        lnext = lower[li + 1]
        unext = upper[ui + 1]
        if lnext[0] == unext[0]:
            xnext = lnext[0]
            h2 = unext[1] - lnext[1]
            li += 1
            ui += 1
        elif lnext[0] < upper[ui + 1][0]:
            xnext = lnext[0]
            frac = 1.0 * (xnext - upper[ui][0]) / (unext[0] - upper[ui][0])
            yupper = upper[ui][1] + frac * (unext[1] - upper[ui][1])
            h2 = yupper - lnext[1]
            li += 1
        else:
            xnext = unext[0]
            frac = 1.0 * (xnext - lower[li][0]) / (lnext[0] - lower[li][0])
            ylower = lower[li][1] + frac * (lnext[1] - lower[li][1])
            h2 = unext[1] - ylower
            ui += 1
        da = (xnext - x) * (h1 + h2) / 2.0
        total_area += da
        areas.append((x, xnext, h1, h2, da, total_area))
        x = xnext
        h1 = h2
    size = total_area / npieces
    return li


def func_604588140a4b4f5e840b745b408d8c4a(npieces, upper, lower):
    areas = []
    total_area = 0
    x = 0
    h1 = upper[0][1] - lower[0][1]
    W = lower[-1][0]
    while x < W:
        lnext = lower[li + 1]
        unext = upper[ui + 1]
        if lnext[0] == unext[0]:
            xnext = lnext[0]
            h2 = unext[1] - lnext[1]
            li += 1
            ui += 1
        elif lnext[0] < upper[ui + 1][0]:
            xnext = lnext[0]
            frac = 1.0 * (xnext - upper[ui][0]) / (unext[0] - upper[ui][0])
            yupper = upper[ui][1] + frac * (unext[1] - upper[ui][1])
            h2 = yupper - lnext[1]
            li += 1
        else:
            xnext = unext[0]
            frac = 1.0 * (xnext - lower[li][0]) / (lnext[0] - lower[li][0])
            ylower = lower[li][1] + frac * (lnext[1] - lower[li][1])
            h2 = unext[1] - ylower
            ui += 1
        da = (xnext - x) * (h1 + h2) / 2.0
        total_area += da
        areas.append((x, xnext, h1, h2, da, total_area))
        x = xnext
        h1 = h2
    size = total_area / npieces
    return ylower


def func_714d4049216e4f7cb85dc066131e24fa(npieces, upper, lower):
    areas = []
    total_area = 0
    x = 0
    h1 = upper[0][1] - lower[0][1]
    W = lower[-1][0]
    while x < W:
        lnext = lower[li + 1]
        unext = upper[ui + 1]
        if lnext[0] == unext[0]:
            xnext = lnext[0]
            h2 = unext[1] - lnext[1]
            li += 1
            ui += 1
        elif lnext[0] < upper[ui + 1][0]:
            xnext = lnext[0]
            frac = 1.0 * (xnext - upper[ui][0]) / (unext[0] - upper[ui][0])
            yupper = upper[ui][1] + frac * (unext[1] - upper[ui][1])
            h2 = yupper - lnext[1]
            li += 1
        else:
            xnext = unext[0]
            frac = 1.0 * (xnext - lower[li][0]) / (lnext[0] - lower[li][0])
            ylower = lower[li][1] + frac * (lnext[1] - lower[li][1])
            h2 = unext[1] - ylower
            ui += 1
        da = (xnext - x) * (h1 + h2) / 2.0
        total_area += da
        areas.append((x, xnext, h1, h2, da, total_area))
        x = xnext
        h1 = h2
    size = total_area / npieces
    return W


def func_964994eaad654dc985693d70ca26a6f3(npieces, upper, lower):
    areas = []
    total_area = 0
    x = 0
    h1 = upper[0][1] - lower[0][1]
    W = lower[-1][0]
    while x < W:
        lnext = lower[li + 1]
        unext = upper[ui + 1]
        if lnext[0] == unext[0]:
            xnext = lnext[0]
            h2 = unext[1] - lnext[1]
            li += 1
            ui += 1
        elif lnext[0] < upper[ui + 1][0]:
            xnext = lnext[0]
            frac = 1.0 * (xnext - upper[ui][0]) / (unext[0] - upper[ui][0])
            yupper = upper[ui][1] + frac * (unext[1] - upper[ui][1])
            h2 = yupper - lnext[1]
            li += 1
        else:
            xnext = unext[0]
            frac = 1.0 * (xnext - lower[li][0]) / (lnext[0] - lower[li][0])
            ylower = lower[li][1] + frac * (lnext[1] - lower[li][1])
            h2 = unext[1] - ylower
            ui += 1
        da = (xnext - x) * (h1 + h2) / 2.0
        total_area += da
        areas.append((x, xnext, h1, h2, da, total_area))
        x = xnext
        h1 = h2
    size = total_area / npieces
    return yupper


def func_6d5ecae38ceb4346b2b9223a5cbbbb8b(npieces, upper, lower):
    areas = []
    total_area = 0
    x = 0
    h1 = upper[0][1] - lower[0][1]
    W = lower[-1][0]
    while x < W:
        lnext = lower[li + 1]
        unext = upper[ui + 1]
        if lnext[0] == unext[0]:
            xnext = lnext[0]
            h2 = unext[1] - lnext[1]
            li += 1
            ui += 1
        elif lnext[0] < upper[ui + 1][0]:
            xnext = lnext[0]
            frac = 1.0 * (xnext - upper[ui][0]) / (unext[0] - upper[ui][0])
            yupper = upper[ui][1] + frac * (unext[1] - upper[ui][1])
            h2 = yupper - lnext[1]
            li += 1
        else:
            xnext = unext[0]
            frac = 1.0 * (xnext - lower[li][0]) / (lnext[0] - lower[li][0])
            ylower = lower[li][1] + frac * (lnext[1] - lower[li][1])
            h2 = unext[1] - ylower
            ui += 1
        da = (xnext - x) * (h1 + h2) / 2.0
        total_area += da
        areas.append((x, xnext, h1, h2, da, total_area))
        x = xnext
        h1 = h2
    size = total_area / npieces
    return lnext


def func_8a45a0ea6e8e4ec0b96289c369737831(npieces, upper, lower):
    areas = []
    total_area = 0
    x = 0
    h1 = upper[0][1] - lower[0][1]
    W = lower[-1][0]
    while x < W:
        lnext = lower[li + 1]
        unext = upper[ui + 1]
        if lnext[0] == unext[0]:
            xnext = lnext[0]
            h2 = unext[1] - lnext[1]
            li += 1
            ui += 1
        elif lnext[0] < upper[ui + 1][0]:
            xnext = lnext[0]
            frac = 1.0 * (xnext - upper[ui][0]) / (unext[0] - upper[ui][0])
            yupper = upper[ui][1] + frac * (unext[1] - upper[ui][1])
            h2 = yupper - lnext[1]
            li += 1
        else:
            xnext = unext[0]
            frac = 1.0 * (xnext - lower[li][0]) / (lnext[0] - lower[li][0])
            ylower = lower[li][1] + frac * (lnext[1] - lower[li][1])
            h2 = unext[1] - ylower
            ui += 1
        da = (xnext - x) * (h1 + h2) / 2.0
        total_area += da
        areas.append((x, xnext, h1, h2, da, total_area))
        x = xnext
        h1 = h2
    size = total_area / npieces
    return unext


def func_d27cb78638fa4e178c0439c359ae3ae3(npieces, upper, lower):
    areas = []
    total_area = 0
    x = 0
    h1 = upper[0][1] - lower[0][1]
    W = lower[-1][0]
    while x < W:
        lnext = lower[li + 1]
        unext = upper[ui + 1]
        if lnext[0] == unext[0]:
            xnext = lnext[0]
            h2 = unext[1] - lnext[1]
            li += 1
            ui += 1
        elif lnext[0] < upper[ui + 1][0]:
            xnext = lnext[0]
            frac = 1.0 * (xnext - upper[ui][0]) / (unext[0] - upper[ui][0])
            yupper = upper[ui][1] + frac * (unext[1] - upper[ui][1])
            h2 = yupper - lnext[1]
            li += 1
        else:
            xnext = unext[0]
            frac = 1.0 * (xnext - lower[li][0]) / (lnext[0] - lower[li][0])
            ylower = lower[li][1] + frac * (lnext[1] - lower[li][1])
            h2 = unext[1] - ylower
            ui += 1
        da = (xnext - x) * (h1 + h2) / 2.0
        total_area += da
        areas.append((x, xnext, h1, h2, da, total_area))
        x = xnext
        h1 = h2
    size = total_area / npieces
    return da


def func_c20fd8f4a7574dd0b8dd257dd9ea9686(npieces, upper, lower):
    areas = []
    total_area = 0
    x = 0
    h1 = upper[0][1] - lower[0][1]
    W = lower[-1][0]
    while x < W:
        lnext = lower[li + 1]
        unext = upper[ui + 1]
        if lnext[0] == unext[0]:
            xnext = lnext[0]
            h2 = unext[1] - lnext[1]
            li += 1
            ui += 1
        elif lnext[0] < upper[ui + 1][0]:
            xnext = lnext[0]
            frac = 1.0 * (xnext - upper[ui][0]) / (unext[0] - upper[ui][0])
            yupper = upper[ui][1] + frac * (unext[1] - upper[ui][1])
            h2 = yupper - lnext[1]
            li += 1
        else:
            xnext = unext[0]
            frac = 1.0 * (xnext - lower[li][0]) / (lnext[0] - lower[li][0])
            ylower = lower[li][1] + frac * (lnext[1] - lower[li][1])
            h2 = unext[1] - ylower
            ui += 1
        da = (xnext - x) * (h1 + h2) / 2.0
        total_area += da
        areas.append((x, xnext, h1, h2, da, total_area))
        x = xnext
        h1 = h2
    size = total_area / npieces
    return areas


def func_9b1a8b3d414444a7a308b0c7c71361f7(npieces, upper, lower):
    li = ui = 0
    areas = []
    total_area = 0
    x = 0
    h1 = upper[0][1] - lower[0][1]
    W = lower[-1][0]
    while x < W:
        lnext = lower[li + 1]
        unext = upper[ui + 1]
        if lnext[0] == unext[0]:
            xnext = lnext[0]
            h2 = unext[1] - lnext[1]
            li += 1
            ui += 1
        elif lnext[0] < upper[ui + 1][0]:
            xnext = lnext[0]
            frac = 1.0 * (xnext - upper[ui][0]) / (unext[0] - upper[ui][0])
            yupper = upper[ui][1] + frac * (unext[1] - upper[ui][1])
            h2 = yupper - lnext[1]
            li += 1
        else:
            xnext = unext[0]
            frac = 1.0 * (xnext - lower[li][0]) / (lnext[0] - lower[li][0])
            ylower = lower[li][1] + frac * (lnext[1] - lower[li][1])
            h2 = unext[1] - ylower
            ui += 1
        da = (xnext - x) * (h1 + h2) / 2.0
        total_area += da
        areas.append((x, xnext, h1, h2, da, total_area))
        x = xnext
        h1 = h2
    size = total_area / npieces
    return frac


def func_fabbdae5ca85466da2f4ac5dbcc72638(npieces, upper, lower):
    li = ui = 0
    areas = []
    total_area = 0
    x = 0
    h1 = upper[0][1] - lower[0][1]
    W = lower[-1][0]
    while x < W:
        lnext = lower[li + 1]
        unext = upper[ui + 1]
        if lnext[0] == unext[0]:
            xnext = lnext[0]
            h2 = unext[1] - lnext[1]
            li += 1
            ui += 1
        elif lnext[0] < upper[ui + 1][0]:
            xnext = lnext[0]
            frac = 1.0 * (xnext - upper[ui][0]) / (unext[0] - upper[ui][0])
            yupper = upper[ui][1] + frac * (unext[1] - upper[ui][1])
            h2 = yupper - lnext[1]
            li += 1
        else:
            xnext = unext[0]
            frac = 1.0 * (xnext - lower[li][0]) / (lnext[0] - lower[li][0])
            ylower = lower[li][1] + frac * (lnext[1] - lower[li][1])
            h2 = unext[1] - ylower
            ui += 1
        da = (xnext - x) * (h1 + h2) / 2.0
        total_area += da
        areas.append((x, xnext, h1, h2, da, total_area))
        x = xnext
        h1 = h2
    size = total_area / npieces
    return size


def func_b45c16b9b7c24b8e888de6ccccd78610(npieces, upper, lower):
    li = ui = 0
    areas = []
    total_area = 0
    x = 0
    h1 = upper[0][1] - lower[0][1]
    W = lower[-1][0]
    while x < W:
        lnext = lower[li + 1]
        unext = upper[ui + 1]
        if lnext[0] == unext[0]:
            xnext = lnext[0]
            h2 = unext[1] - lnext[1]
            li += 1
            ui += 1
        elif lnext[0] < upper[ui + 1][0]:
            xnext = lnext[0]
            frac = 1.0 * (xnext - upper[ui][0]) / (unext[0] - upper[ui][0])
            yupper = upper[ui][1] + frac * (unext[1] - upper[ui][1])
            h2 = yupper - lnext[1]
            li += 1
        else:
            xnext = unext[0]
            frac = 1.0 * (xnext - lower[li][0]) / (lnext[0] - lower[li][0])
            ylower = lower[li][1] + frac * (lnext[1] - lower[li][1])
            h2 = unext[1] - ylower
            ui += 1
        da = (xnext - x) * (h1 + h2) / 2.0
        total_area += da
        areas.append((x, xnext, h1, h2, da, total_area))
        x = xnext
        h1 = h2
    size = total_area / npieces
    return h2


def func_7e9d998b4a7d48fd9a8c386cbb17eede(npieces, upper, lower):
    li = ui = 0
    areas = []
    total_area = 0
    x = 0
    h1 = upper[0][1] - lower[0][1]
    W = lower[-1][0]
    while x < W:
        lnext = lower[li + 1]
        unext = upper[ui + 1]
        if lnext[0] == unext[0]:
            xnext = lnext[0]
            h2 = unext[1] - lnext[1]
            li += 1
            ui += 1
        elif lnext[0] < upper[ui + 1][0]:
            xnext = lnext[0]
            frac = 1.0 * (xnext - upper[ui][0]) / (unext[0] - upper[ui][0])
            yupper = upper[ui][1] + frac * (unext[1] - upper[ui][1])
            h2 = yupper - lnext[1]
            li += 1
        else:
            xnext = unext[0]
            frac = 1.0 * (xnext - lower[li][0]) / (lnext[0] - lower[li][0])
            ylower = lower[li][1] + frac * (lnext[1] - lower[li][1])
            h2 = unext[1] - ylower
            ui += 1
        da = (xnext - x) * (h1 + h2) / 2.0
        total_area += da
        areas.append((x, xnext, h1, h2, da, total_area))
        x = xnext
        h1 = h2
    size = total_area / npieces
    return unext


def func_8e8cdf82377d4e28a59905a33db64577(npieces, upper, lower):
    li = ui = 0
    areas = []
    total_area = 0
    x = 0
    h1 = upper[0][1] - lower[0][1]
    W = lower[-1][0]
    while x < W:
        lnext = lower[li + 1]
        unext = upper[ui + 1]
        if lnext[0] == unext[0]:
            xnext = lnext[0]
            h2 = unext[1] - lnext[1]
            li += 1
            ui += 1
        elif lnext[0] < upper[ui + 1][0]:
            xnext = lnext[0]
            frac = 1.0 * (xnext - upper[ui][0]) / (unext[0] - upper[ui][0])
            yupper = upper[ui][1] + frac * (unext[1] - upper[ui][1])
            h2 = yupper - lnext[1]
            li += 1
        else:
            xnext = unext[0]
            frac = 1.0 * (xnext - lower[li][0]) / (lnext[0] - lower[li][0])
            ylower = lower[li][1] + frac * (lnext[1] - lower[li][1])
            h2 = unext[1] - ylower
            ui += 1
        da = (xnext - x) * (h1 + h2) / 2.0
        total_area += da
        areas.append((x, xnext, h1, h2, da, total_area))
        x = xnext
        h1 = h2
    size = total_area / npieces
    return lnext


def func_aee021abfb884b439e456ff444f9105f(npieces, upper, lower):
    li = ui = 0
    areas = []
    total_area = 0
    x = 0
    h1 = upper[0][1] - lower[0][1]
    W = lower[-1][0]
    while x < W:
        lnext = lower[li + 1]
        unext = upper[ui + 1]
        if lnext[0] == unext[0]:
            xnext = lnext[0]
            h2 = unext[1] - lnext[1]
            li += 1
            ui += 1
        elif lnext[0] < upper[ui + 1][0]:
            xnext = lnext[0]
            frac = 1.0 * (xnext - upper[ui][0]) / (unext[0] - upper[ui][0])
            yupper = upper[ui][1] + frac * (unext[1] - upper[ui][1])
            h2 = yupper - lnext[1]
            li += 1
        else:
            xnext = unext[0]
            frac = 1.0 * (xnext - lower[li][0]) / (lnext[0] - lower[li][0])
            ylower = lower[li][1] + frac * (lnext[1] - lower[li][1])
            h2 = unext[1] - ylower
            ui += 1
        da = (xnext - x) * (h1 + h2) / 2.0
        total_area += da
        areas.append((x, xnext, h1, h2, da, total_area))
        x = xnext
        h1 = h2
    size = total_area / npieces
    return h1


def func_e307437b1d6a404397946398735e53e6(npieces, upper, lower):
    li = ui = 0
    areas = []
    total_area = 0
    x = 0
    h1 = upper[0][1] - lower[0][1]
    W = lower[-1][0]
    while x < W:
        lnext = lower[li + 1]
        unext = upper[ui + 1]
        if lnext[0] == unext[0]:
            xnext = lnext[0]
            h2 = unext[1] - lnext[1]
            li += 1
            ui += 1
        elif lnext[0] < upper[ui + 1][0]:
            xnext = lnext[0]
            frac = 1.0 * (xnext - upper[ui][0]) / (unext[0] - upper[ui][0])
            yupper = upper[ui][1] + frac * (unext[1] - upper[ui][1])
            h2 = yupper - lnext[1]
            li += 1
        else:
            xnext = unext[0]
            frac = 1.0 * (xnext - lower[li][0]) / (lnext[0] - lower[li][0])
            ylower = lower[li][1] + frac * (lnext[1] - lower[li][1])
            h2 = unext[1] - ylower
            ui += 1
        da = (xnext - x) * (h1 + h2) / 2.0
        total_area += da
        areas.append((x, xnext, h1, h2, da, total_area))
        x = xnext
        h1 = h2
    size = total_area / npieces
    return ylower


def func_2ba476b24a964c5bb07c28d2faf95fd7(npieces, upper, lower):
    li = ui = 0
    areas = []
    total_area = 0
    x = 0
    h1 = upper[0][1] - lower[0][1]
    W = lower[-1][0]
    while x < W:
        lnext = lower[li + 1]
        unext = upper[ui + 1]
        if lnext[0] == unext[0]:
            xnext = lnext[0]
            h2 = unext[1] - lnext[1]
            li += 1
            ui += 1
        elif lnext[0] < upper[ui + 1][0]:
            xnext = lnext[0]
            frac = 1.0 * (xnext - upper[ui][0]) / (unext[0] - upper[ui][0])
            yupper = upper[ui][1] + frac * (unext[1] - upper[ui][1])
            h2 = yupper - lnext[1]
            li += 1
        else:
            xnext = unext[0]
            frac = 1.0 * (xnext - lower[li][0]) / (lnext[0] - lower[li][0])
            ylower = lower[li][1] + frac * (lnext[1] - lower[li][1])
            h2 = unext[1] - ylower
            ui += 1
        da = (xnext - x) * (h1 + h2) / 2.0
        total_area += da
        areas.append((x, xnext, h1, h2, da, total_area))
        x = xnext
        h1 = h2
    size = total_area / npieces
    return da


def func_90bd4f36459a4404abd349c278aa6630(npieces, upper, lower):
    li = ui = 0
    areas = []
    total_area = 0
    x = 0
    h1 = upper[0][1] - lower[0][1]
    W = lower[-1][0]
    while x < W:
        lnext = lower[li + 1]
        unext = upper[ui + 1]
        if lnext[0] == unext[0]:
            xnext = lnext[0]
            h2 = unext[1] - lnext[1]
            li += 1
            ui += 1
        elif lnext[0] < upper[ui + 1][0]:
            xnext = lnext[0]
            frac = 1.0 * (xnext - upper[ui][0]) / (unext[0] - upper[ui][0])
            yupper = upper[ui][1] + frac * (unext[1] - upper[ui][1])
            h2 = yupper - lnext[1]
            li += 1
        else:
            xnext = unext[0]
            frac = 1.0 * (xnext - lower[li][0]) / (lnext[0] - lower[li][0])
            ylower = lower[li][1] + frac * (lnext[1] - lower[li][1])
            h2 = unext[1] - ylower
            ui += 1
        da = (xnext - x) * (h1 + h2) / 2.0
        total_area += da
        areas.append((x, xnext, h1, h2, da, total_area))
        x = xnext
        h1 = h2
    size = total_area / npieces
    return x


def func_f20f108ef2274f6a90ab888f386152cb(npieces, upper, lower):
    li = ui = 0
    areas = []
    total_area = 0
    x = 0
    h1 = upper[0][1] - lower[0][1]
    W = lower[-1][0]
    while x < W:
        lnext = lower[li + 1]
        unext = upper[ui + 1]
        if lnext[0] == unext[0]:
            xnext = lnext[0]
            h2 = unext[1] - lnext[1]
            li += 1
            ui += 1
        elif lnext[0] < upper[ui + 1][0]:
            xnext = lnext[0]
            frac = 1.0 * (xnext - upper[ui][0]) / (unext[0] - upper[ui][0])
            yupper = upper[ui][1] + frac * (unext[1] - upper[ui][1])
            h2 = yupper - lnext[1]
            li += 1
        else:
            xnext = unext[0]
            frac = 1.0 * (xnext - lower[li][0]) / (lnext[0] - lower[li][0])
            ylower = lower[li][1] + frac * (lnext[1] - lower[li][1])
            h2 = unext[1] - ylower
            ui += 1
        da = (xnext - x) * (h1 + h2) / 2.0
        total_area += da
        areas.append((x, xnext, h1, h2, da, total_area))
        x = xnext
        h1 = h2
    size = total_area / npieces
    return yupper


def func_d0ccd2d5662345bc99057b436871cff7(npieces, upper, lower):
    li = ui = 0
    areas = []
    total_area = 0
    x = 0
    h1 = upper[0][1] - lower[0][1]
    W = lower[-1][0]
    while x < W:
        lnext = lower[li + 1]
        unext = upper[ui + 1]
        if lnext[0] == unext[0]:
            xnext = lnext[0]
            h2 = unext[1] - lnext[1]
            li += 1
            ui += 1
        elif lnext[0] < upper[ui + 1][0]:
            xnext = lnext[0]
            frac = 1.0 * (xnext - upper[ui][0]) / (unext[0] - upper[ui][0])
            yupper = upper[ui][1] + frac * (unext[1] - upper[ui][1])
            h2 = yupper - lnext[1]
            li += 1
        else:
            xnext = unext[0]
            frac = 1.0 * (xnext - lower[li][0]) / (lnext[0] - lower[li][0])
            ylower = lower[li][1] + frac * (lnext[1] - lower[li][1])
            h2 = unext[1] - ylower
            ui += 1
        da = (xnext - x) * (h1 + h2) / 2.0
        total_area += da
        areas.append((x, xnext, h1, h2, da, total_area))
        x = xnext
        h1 = h2
    size = total_area / npieces
    return areas


def func_28a0bf3a11cf4677a14fd95008473875(npieces, upper, lower):
    li = ui = 0
    areas = []
    total_area = 0
    x = 0
    h1 = upper[0][1] - lower[0][1]
    W = lower[-1][0]
    while x < W:
        lnext = lower[li + 1]
        unext = upper[ui + 1]
        if lnext[0] == unext[0]:
            xnext = lnext[0]
            h2 = unext[1] - lnext[1]
            li += 1
            ui += 1
        elif lnext[0] < upper[ui + 1][0]:
            xnext = lnext[0]
            frac = 1.0 * (xnext - upper[ui][0]) / (unext[0] - upper[ui][0])
            yupper = upper[ui][1] + frac * (unext[1] - upper[ui][1])
            h2 = yupper - lnext[1]
            li += 1
        else:
            xnext = unext[0]
            frac = 1.0 * (xnext - lower[li][0]) / (lnext[0] - lower[li][0])
            ylower = lower[li][1] + frac * (lnext[1] - lower[li][1])
            h2 = unext[1] - ylower
            ui += 1
        da = (xnext - x) * (h1 + h2) / 2.0
        total_area += da
        areas.append((x, xnext, h1, h2, da, total_area))
        x = xnext
        h1 = h2
    size = total_area / npieces
    return total_area


def func_9018ab6c8e70438b80fb9d6ab561c81c(npieces, upper, lower):
    li = ui = 0
    areas = []
    total_area = 0
    x = 0
    h1 = upper[0][1] - lower[0][1]
    W = lower[-1][0]
    while x < W:
        lnext = lower[li + 1]
        unext = upper[ui + 1]
        if lnext[0] == unext[0]:
            xnext = lnext[0]
            h2 = unext[1] - lnext[1]
            li += 1
            ui += 1
        elif lnext[0] < upper[ui + 1][0]:
            xnext = lnext[0]
            frac = 1.0 * (xnext - upper[ui][0]) / (unext[0] - upper[ui][0])
            yupper = upper[ui][1] + frac * (unext[1] - upper[ui][1])
            h2 = yupper - lnext[1]
            li += 1
        else:
            xnext = unext[0]
            frac = 1.0 * (xnext - lower[li][0]) / (lnext[0] - lower[li][0])
            ylower = lower[li][1] + frac * (lnext[1] - lower[li][1])
            h2 = unext[1] - ylower
            ui += 1
        da = (xnext - x) * (h1 + h2) / 2.0
        total_area += da
        areas.append((x, xnext, h1, h2, da, total_area))
        x = xnext
        h1 = h2
    size = total_area / npieces
    return xnext


def func_4a018e96b16441929b3bb2aa72a80c4c(npieces, upper, lower):
    li = ui = 0
    areas = []
    total_area = 0
    x = 0
    h1 = upper[0][1] - lower[0][1]
    W = lower[-1][0]
    while x < W:
        lnext = lower[li + 1]
        unext = upper[ui + 1]
        if lnext[0] == unext[0]:
            xnext = lnext[0]
            h2 = unext[1] - lnext[1]
            li += 1
            ui += 1
        elif lnext[0] < upper[ui + 1][0]:
            xnext = lnext[0]
            frac = 1.0 * (xnext - upper[ui][0]) / (unext[0] - upper[ui][0])
            yupper = upper[ui][1] + frac * (unext[1] - upper[ui][1])
            h2 = yupper - lnext[1]
            li += 1
        else:
            xnext = unext[0]
            frac = 1.0 * (xnext - lower[li][0]) / (lnext[0] - lower[li][0])
            ylower = lower[li][1] + frac * (lnext[1] - lower[li][1])
            h2 = unext[1] - ylower
            ui += 1
        da = (xnext - x) * (h1 + h2) / 2.0
        total_area += da
        areas.append((x, xnext, h1, h2, da, total_area))
        x = xnext
        h1 = h2
    size = total_area / npieces
    return W


def func_28fb07516bc64458a64ef9e180a12633(npieces, upper, lower):
    li = ui = 0
    areas = []
    total_area = 0
    x = 0
    h1 = upper[0][1] - lower[0][1]
    W = lower[-1][0]
    while x < W:
        lnext = lower[li + 1]
        unext = upper[ui + 1]
        if lnext[0] == unext[0]:
            xnext = lnext[0]
            h2 = unext[1] - lnext[1]
            li += 1
            ui += 1
        elif lnext[0] < upper[ui + 1][0]:
            xnext = lnext[0]
            frac = 1.0 * (xnext - upper[ui][0]) / (unext[0] - upper[ui][0])
            yupper = upper[ui][1] + frac * (unext[1] - upper[ui][1])
            h2 = yupper - lnext[1]
            li += 1
        else:
            xnext = unext[0]
            frac = 1.0 * (xnext - lower[li][0]) / (lnext[0] - lower[li][0])
            ylower = lower[li][1] + frac * (lnext[1] - lower[li][1])
            h2 = unext[1] - ylower
            ui += 1
        da = (xnext - x) * (h1 + h2) / 2.0
        total_area += da
        areas.append((x, xnext, h1, h2, da, total_area))
        x = xnext
        h1 = h2
    size = total_area / npieces
    return li


def func_5f5b06e69db742efbaaa15f023691221(npieces, upper, lower):
    li = ui = 0
    areas = []
    total_area = 0
    x = 0
    h1 = upper[0][1] - lower[0][1]
    W = lower[-1][0]
    while x < W:
        lnext = lower[li + 1]
        unext = upper[ui + 1]
        if lnext[0] == unext[0]:
            xnext = lnext[0]
            h2 = unext[1] - lnext[1]
            li += 1
            ui += 1
        elif lnext[0] < upper[ui + 1][0]:
            xnext = lnext[0]
            frac = 1.0 * (xnext - upper[ui][0]) / (unext[0] - upper[ui][0])
            yupper = upper[ui][1] + frac * (unext[1] - upper[ui][1])
            h2 = yupper - lnext[1]
            li += 1
        else:
            xnext = unext[0]
            frac = 1.0 * (xnext - lower[li][0]) / (lnext[0] - lower[li][0])
            ylower = lower[li][1] + frac * (lnext[1] - lower[li][1])
            h2 = unext[1] - ylower
            ui += 1
        da = (xnext - x) * (h1 + h2) / 2.0
        total_area += da
        areas.append((x, xnext, h1, h2, da, total_area))
        x = xnext
        h1 = h2
    size = total_area / npieces
    return ui


def func_924456d6bf4c4bb19af247f58267508a(npieces, upper, lower):
    areas = []
    total_area = 0
    x = 0
    h1 = upper[0][1] - lower[0][1]
    W = lower[-1][0]
    while x < W:
        lnext = lower[li + 1]
        unext = upper[ui + 1]
        if lnext[0] == unext[0]:
            xnext = lnext[0]
            h2 = unext[1] - lnext[1]
            li += 1
            ui += 1
        elif lnext[0] < upper[ui + 1][0]:
            xnext = lnext[0]
            frac = 1.0 * (xnext - upper[ui][0]) / (unext[0] - upper[ui][0])
            yupper = upper[ui][1] + frac * (unext[1] - upper[ui][1])
            h2 = yupper - lnext[1]
            li += 1
        else:
            xnext = unext[0]
            frac = 1.0 * (xnext - lower[li][0]) / (lnext[0] - lower[li][0])
            ylower = lower[li][1] + frac * (lnext[1] - lower[li][1])
            h2 = unext[1] - ylower
            ui += 1
        da = (xnext - x) * (h1 + h2) / 2.0
        total_area += da
        areas.append((x, xnext, h1, h2, da, total_area))
        x = xnext
        h1 = h2
    size = total_area / npieces
    cuts = []
    return h1


def func_f3ac62c0743a47d0be8d8ecd47a6d82c(npieces, upper, lower):
    areas = []
    total_area = 0
    x = 0
    h1 = upper[0][1] - lower[0][1]
    W = lower[-1][0]
    while x < W:
        lnext = lower[li + 1]
        unext = upper[ui + 1]
        if lnext[0] == unext[0]:
            xnext = lnext[0]
            h2 = unext[1] - lnext[1]
            li += 1
            ui += 1
        elif lnext[0] < upper[ui + 1][0]:
            xnext = lnext[0]
            frac = 1.0 * (xnext - upper[ui][0]) / (unext[0] - upper[ui][0])
            yupper = upper[ui][1] + frac * (unext[1] - upper[ui][1])
            h2 = yupper - lnext[1]
            li += 1
        else:
            xnext = unext[0]
            frac = 1.0 * (xnext - lower[li][0]) / (lnext[0] - lower[li][0])
            ylower = lower[li][1] + frac * (lnext[1] - lower[li][1])
            h2 = unext[1] - ylower
            ui += 1
        da = (xnext - x) * (h1 + h2) / 2.0
        total_area += da
        areas.append((x, xnext, h1, h2, da, total_area))
        x = xnext
        h1 = h2
    size = total_area / npieces
    cuts = []
    return W


def func_578a6349e914472ea21a05335352fd02(npieces, upper, lower):
    areas = []
    total_area = 0
    x = 0
    h1 = upper[0][1] - lower[0][1]
    W = lower[-1][0]
    while x < W:
        lnext = lower[li + 1]
        unext = upper[ui + 1]
        if lnext[0] == unext[0]:
            xnext = lnext[0]
            h2 = unext[1] - lnext[1]
            li += 1
            ui += 1
        elif lnext[0] < upper[ui + 1][0]:
            xnext = lnext[0]
            frac = 1.0 * (xnext - upper[ui][0]) / (unext[0] - upper[ui][0])
            yupper = upper[ui][1] + frac * (unext[1] - upper[ui][1])
            h2 = yupper - lnext[1]
            li += 1
        else:
            xnext = unext[0]
            frac = 1.0 * (xnext - lower[li][0]) / (lnext[0] - lower[li][0])
            ylower = lower[li][1] + frac * (lnext[1] - lower[li][1])
            h2 = unext[1] - ylower
            ui += 1
        da = (xnext - x) * (h1 + h2) / 2.0
        total_area += da
        areas.append((x, xnext, h1, h2, da, total_area))
        x = xnext
        h1 = h2
    size = total_area / npieces
    cuts = []
    return x


def func_0b68597821f74729800debf09d3b6872(npieces, upper, lower):
    areas = []
    total_area = 0
    x = 0
    h1 = upper[0][1] - lower[0][1]
    W = lower[-1][0]
    while x < W:
        lnext = lower[li + 1]
        unext = upper[ui + 1]
        if lnext[0] == unext[0]:
            xnext = lnext[0]
            h2 = unext[1] - lnext[1]
            li += 1
            ui += 1
        elif lnext[0] < upper[ui + 1][0]:
            xnext = lnext[0]
            frac = 1.0 * (xnext - upper[ui][0]) / (unext[0] - upper[ui][0])
            yupper = upper[ui][1] + frac * (unext[1] - upper[ui][1])
            h2 = yupper - lnext[1]
            li += 1
        else:
            xnext = unext[0]
            frac = 1.0 * (xnext - lower[li][0]) / (lnext[0] - lower[li][0])
            ylower = lower[li][1] + frac * (lnext[1] - lower[li][1])
            h2 = unext[1] - ylower
            ui += 1
        da = (xnext - x) * (h1 + h2) / 2.0
        total_area += da
        areas.append((x, xnext, h1, h2, da, total_area))
        x = xnext
        h1 = h2
    size = total_area / npieces
    cuts = []
    return size


def func_8bb400d628ac41eea896678c685885fc(npieces, upper, lower):
    areas = []
    total_area = 0
    x = 0
    h1 = upper[0][1] - lower[0][1]
    W = lower[-1][0]
    while x < W:
        lnext = lower[li + 1]
        unext = upper[ui + 1]
        if lnext[0] == unext[0]:
            xnext = lnext[0]
            h2 = unext[1] - lnext[1]
            li += 1
            ui += 1
        elif lnext[0] < upper[ui + 1][0]:
            xnext = lnext[0]
            frac = 1.0 * (xnext - upper[ui][0]) / (unext[0] - upper[ui][0])
            yupper = upper[ui][1] + frac * (unext[1] - upper[ui][1])
            h2 = yupper - lnext[1]
            li += 1
        else:
            xnext = unext[0]
            frac = 1.0 * (xnext - lower[li][0]) / (lnext[0] - lower[li][0])
            ylower = lower[li][1] + frac * (lnext[1] - lower[li][1])
            h2 = unext[1] - ylower
            ui += 1
        da = (xnext - x) * (h1 + h2) / 2.0
        total_area += da
        areas.append((x, xnext, h1, h2, da, total_area))
        x = xnext
        h1 = h2
    size = total_area / npieces
    cuts = []
    return yupper


def func_79923073a6664bffbd1f6cd619b52492(npieces, upper, lower):
    areas = []
    total_area = 0
    x = 0
    h1 = upper[0][1] - lower[0][1]
    W = lower[-1][0]
    while x < W:
        lnext = lower[li + 1]
        unext = upper[ui + 1]
        if lnext[0] == unext[0]:
            xnext = lnext[0]
            h2 = unext[1] - lnext[1]
            li += 1
            ui += 1
        elif lnext[0] < upper[ui + 1][0]:
            xnext = lnext[0]
            frac = 1.0 * (xnext - upper[ui][0]) / (unext[0] - upper[ui][0])
            yupper = upper[ui][1] + frac * (unext[1] - upper[ui][1])
            h2 = yupper - lnext[1]
            li += 1
        else:
            xnext = unext[0]
            frac = 1.0 * (xnext - lower[li][0]) / (lnext[0] - lower[li][0])
            ylower = lower[li][1] + frac * (lnext[1] - lower[li][1])
            h2 = unext[1] - ylower
            ui += 1
        da = (xnext - x) * (h1 + h2) / 2.0
        total_area += da
        areas.append((x, xnext, h1, h2, da, total_area))
        x = xnext
        h1 = h2
    size = total_area / npieces
    cuts = []
    return h2


def func_bff075a6e6374cc88e08efd3d97e6772(npieces, upper, lower):
    areas = []
    total_area = 0
    x = 0
    h1 = upper[0][1] - lower[0][1]
    W = lower[-1][0]
    while x < W:
        lnext = lower[li + 1]
        unext = upper[ui + 1]
        if lnext[0] == unext[0]:
            xnext = lnext[0]
            h2 = unext[1] - lnext[1]
            li += 1
            ui += 1
        elif lnext[0] < upper[ui + 1][0]:
            xnext = lnext[0]
            frac = 1.0 * (xnext - upper[ui][0]) / (unext[0] - upper[ui][0])
            yupper = upper[ui][1] + frac * (unext[1] - upper[ui][1])
            h2 = yupper - lnext[1]
            li += 1
        else:
            xnext = unext[0]
            frac = 1.0 * (xnext - lower[li][0]) / (lnext[0] - lower[li][0])
            ylower = lower[li][1] + frac * (lnext[1] - lower[li][1])
            h2 = unext[1] - ylower
            ui += 1
        da = (xnext - x) * (h1 + h2) / 2.0
        total_area += da
        areas.append((x, xnext, h1, h2, da, total_area))
        x = xnext
        h1 = h2
    size = total_area / npieces
    cuts = []
    return ui


def func_a6099a9b3bed4200bd17137ed68a9cf3(npieces, upper, lower):
    areas = []
    total_area = 0
    x = 0
    h1 = upper[0][1] - lower[0][1]
    W = lower[-1][0]
    while x < W:
        lnext = lower[li + 1]
        unext = upper[ui + 1]
        if lnext[0] == unext[0]:
            xnext = lnext[0]
            h2 = unext[1] - lnext[1]
            li += 1
            ui += 1
        elif lnext[0] < upper[ui + 1][0]:
            xnext = lnext[0]
            frac = 1.0 * (xnext - upper[ui][0]) / (unext[0] - upper[ui][0])
            yupper = upper[ui][1] + frac * (unext[1] - upper[ui][1])
            h2 = yupper - lnext[1]
            li += 1
        else:
            xnext = unext[0]
            frac = 1.0 * (xnext - lower[li][0]) / (lnext[0] - lower[li][0])
            ylower = lower[li][1] + frac * (lnext[1] - lower[li][1])
            h2 = unext[1] - ylower
            ui += 1
        da = (xnext - x) * (h1 + h2) / 2.0
        total_area += da
        areas.append((x, xnext, h1, h2, da, total_area))
        x = xnext
        h1 = h2
    size = total_area / npieces
    cuts = []
    return li


def func_69480b960e564a3ba04045021904143f(npieces, upper, lower):
    areas = []
    total_area = 0
    x = 0
    h1 = upper[0][1] - lower[0][1]
    W = lower[-1][0]
    while x < W:
        lnext = lower[li + 1]
        unext = upper[ui + 1]
        if lnext[0] == unext[0]:
            xnext = lnext[0]
            h2 = unext[1] - lnext[1]
            li += 1
            ui += 1
        elif lnext[0] < upper[ui + 1][0]:
            xnext = lnext[0]
            frac = 1.0 * (xnext - upper[ui][0]) / (unext[0] - upper[ui][0])
            yupper = upper[ui][1] + frac * (unext[1] - upper[ui][1])
            h2 = yupper - lnext[1]
            li += 1
        else:
            xnext = unext[0]
            frac = 1.0 * (xnext - lower[li][0]) / (lnext[0] - lower[li][0])
            ylower = lower[li][1] + frac * (lnext[1] - lower[li][1])
            h2 = unext[1] - ylower
            ui += 1
        da = (xnext - x) * (h1 + h2) / 2.0
        total_area += da
        areas.append((x, xnext, h1, h2, da, total_area))
        x = xnext
        h1 = h2
    size = total_area / npieces
    cuts = []
    return frac


def func_cae3aeab64b34d2980ae698be4743d9b(npieces, upper, lower):
    areas = []
    total_area = 0
    x = 0
    h1 = upper[0][1] - lower[0][1]
    W = lower[-1][0]
    while x < W:
        lnext = lower[li + 1]
        unext = upper[ui + 1]
        if lnext[0] == unext[0]:
            xnext = lnext[0]
            h2 = unext[1] - lnext[1]
            li += 1
            ui += 1
        elif lnext[0] < upper[ui + 1][0]:
            xnext = lnext[0]
            frac = 1.0 * (xnext - upper[ui][0]) / (unext[0] - upper[ui][0])
            yupper = upper[ui][1] + frac * (unext[1] - upper[ui][1])
            h2 = yupper - lnext[1]
            li += 1
        else:
            xnext = unext[0]
            frac = 1.0 * (xnext - lower[li][0]) / (lnext[0] - lower[li][0])
            ylower = lower[li][1] + frac * (lnext[1] - lower[li][1])
            h2 = unext[1] - ylower
            ui += 1
        da = (xnext - x) * (h1 + h2) / 2.0
        total_area += da
        areas.append((x, xnext, h1, h2, da, total_area))
        x = xnext
        h1 = h2
    size = total_area / npieces
    cuts = []
    return lnext


def func_5953d2922c004be5892a47266c9f4973(npieces, upper, lower):
    areas = []
    total_area = 0
    x = 0
    h1 = upper[0][1] - lower[0][1]
    W = lower[-1][0]
    while x < W:
        lnext = lower[li + 1]
        unext = upper[ui + 1]
        if lnext[0] == unext[0]:
            xnext = lnext[0]
            h2 = unext[1] - lnext[1]
            li += 1
            ui += 1
        elif lnext[0] < upper[ui + 1][0]:
            xnext = lnext[0]
            frac = 1.0 * (xnext - upper[ui][0]) / (unext[0] - upper[ui][0])
            yupper = upper[ui][1] + frac * (unext[1] - upper[ui][1])
            h2 = yupper - lnext[1]
            li += 1
        else:
            xnext = unext[0]
            frac = 1.0 * (xnext - lower[li][0]) / (lnext[0] - lower[li][0])
            ylower = lower[li][1] + frac * (lnext[1] - lower[li][1])
            h2 = unext[1] - ylower
            ui += 1
        da = (xnext - x) * (h1 + h2) / 2.0
        total_area += da
        areas.append((x, xnext, h1, h2, da, total_area))
        x = xnext
        h1 = h2
    size = total_area / npieces
    cuts = []
    return areas


def func_6e9ba9ba2ca045b38db23f98d244dff0(npieces, upper, lower):
    areas = []
    total_area = 0
    x = 0
    h1 = upper[0][1] - lower[0][1]
    W = lower[-1][0]
    while x < W:
        lnext = lower[li + 1]
        unext = upper[ui + 1]
        if lnext[0] == unext[0]:
            xnext = lnext[0]
            h2 = unext[1] - lnext[1]
            li += 1
            ui += 1
        elif lnext[0] < upper[ui + 1][0]:
            xnext = lnext[0]
            frac = 1.0 * (xnext - upper[ui][0]) / (unext[0] - upper[ui][0])
            yupper = upper[ui][1] + frac * (unext[1] - upper[ui][1])
            h2 = yupper - lnext[1]
            li += 1
        else:
            xnext = unext[0]
            frac = 1.0 * (xnext - lower[li][0]) / (lnext[0] - lower[li][0])
            ylower = lower[li][1] + frac * (lnext[1] - lower[li][1])
            h2 = unext[1] - ylower
            ui += 1
        da = (xnext - x) * (h1 + h2) / 2.0
        total_area += da
        areas.append((x, xnext, h1, h2, da, total_area))
        x = xnext
        h1 = h2
    size = total_area / npieces
    cuts = []
    return total_area


def func_a99c084250984caf94d4356f8090957b(npieces, upper, lower):
    areas = []
    total_area = 0
    x = 0
    h1 = upper[0][1] - lower[0][1]
    W = lower[-1][0]
    while x < W:
        lnext = lower[li + 1]
        unext = upper[ui + 1]
        if lnext[0] == unext[0]:
            xnext = lnext[0]
            h2 = unext[1] - lnext[1]
            li += 1
            ui += 1
        elif lnext[0] < upper[ui + 1][0]:
            xnext = lnext[0]
            frac = 1.0 * (xnext - upper[ui][0]) / (unext[0] - upper[ui][0])
            yupper = upper[ui][1] + frac * (unext[1] - upper[ui][1])
            h2 = yupper - lnext[1]
            li += 1
        else:
            xnext = unext[0]
            frac = 1.0 * (xnext - lower[li][0]) / (lnext[0] - lower[li][0])
            ylower = lower[li][1] + frac * (lnext[1] - lower[li][1])
            h2 = unext[1] - ylower
            ui += 1
        da = (xnext - x) * (h1 + h2) / 2.0
        total_area += da
        areas.append((x, xnext, h1, h2, da, total_area))
        x = xnext
        h1 = h2
    size = total_area / npieces
    cuts = []
    return cuts


def func_ce06e0e228194f229bf5a2c445772173(npieces, upper, lower):
    areas = []
    total_area = 0
    x = 0
    h1 = upper[0][1] - lower[0][1]
    W = lower[-1][0]
    while x < W:
        lnext = lower[li + 1]
        unext = upper[ui + 1]
        if lnext[0] == unext[0]:
            xnext = lnext[0]
            h2 = unext[1] - lnext[1]
            li += 1
            ui += 1
        elif lnext[0] < upper[ui + 1][0]:
            xnext = lnext[0]
            frac = 1.0 * (xnext - upper[ui][0]) / (unext[0] - upper[ui][0])
            yupper = upper[ui][1] + frac * (unext[1] - upper[ui][1])
            h2 = yupper - lnext[1]
            li += 1
        else:
            xnext = unext[0]
            frac = 1.0 * (xnext - lower[li][0]) / (lnext[0] - lower[li][0])
            ylower = lower[li][1] + frac * (lnext[1] - lower[li][1])
            h2 = unext[1] - ylower
            ui += 1
        da = (xnext - x) * (h1 + h2) / 2.0
        total_area += da
        areas.append((x, xnext, h1, h2, da, total_area))
        x = xnext
        h1 = h2
    size = total_area / npieces
    cuts = []
    return unext


def func_6ad80814bc1c430c9769fd6e0d7c4a0c(npieces, upper, lower):
    areas = []
    total_area = 0
    x = 0
    h1 = upper[0][1] - lower[0][1]
    W = lower[-1][0]
    while x < W:
        lnext = lower[li + 1]
        unext = upper[ui + 1]
        if lnext[0] == unext[0]:
            xnext = lnext[0]
            h2 = unext[1] - lnext[1]
            li += 1
            ui += 1
        elif lnext[0] < upper[ui + 1][0]:
            xnext = lnext[0]
            frac = 1.0 * (xnext - upper[ui][0]) / (unext[0] - upper[ui][0])
            yupper = upper[ui][1] + frac * (unext[1] - upper[ui][1])
            h2 = yupper - lnext[1]
            li += 1
        else:
            xnext = unext[0]
            frac = 1.0 * (xnext - lower[li][0]) / (lnext[0] - lower[li][0])
            ylower = lower[li][1] + frac * (lnext[1] - lower[li][1])
            h2 = unext[1] - ylower
            ui += 1
        da = (xnext - x) * (h1 + h2) / 2.0
        total_area += da
        areas.append((x, xnext, h1, h2, da, total_area))
        x = xnext
        h1 = h2
    size = total_area / npieces
    cuts = []
    return xnext


def func_99b81ba375fa46c39de6ef19e2a007a5(npieces, upper, lower):
    areas = []
    total_area = 0
    x = 0
    h1 = upper[0][1] - lower[0][1]
    W = lower[-1][0]
    while x < W:
        lnext = lower[li + 1]
        unext = upper[ui + 1]
        if lnext[0] == unext[0]:
            xnext = lnext[0]
            h2 = unext[1] - lnext[1]
            li += 1
            ui += 1
        elif lnext[0] < upper[ui + 1][0]:
            xnext = lnext[0]
            frac = 1.0 * (xnext - upper[ui][0]) / (unext[0] - upper[ui][0])
            yupper = upper[ui][1] + frac * (unext[1] - upper[ui][1])
            h2 = yupper - lnext[1]
            li += 1
        else:
            xnext = unext[0]
            frac = 1.0 * (xnext - lower[li][0]) / (lnext[0] - lower[li][0])
            ylower = lower[li][1] + frac * (lnext[1] - lower[li][1])
            h2 = unext[1] - ylower
            ui += 1
        da = (xnext - x) * (h1 + h2) / 2.0
        total_area += da
        areas.append((x, xnext, h1, h2, da, total_area))
        x = xnext
        h1 = h2
    size = total_area / npieces
    cuts = []
    return ylower


def func_14c88393050c451ea91a5d104ad023da(npieces, upper, lower):
    areas = []
    total_area = 0
    x = 0
    h1 = upper[0][1] - lower[0][1]
    W = lower[-1][0]
    while x < W:
        lnext = lower[li + 1]
        unext = upper[ui + 1]
        if lnext[0] == unext[0]:
            xnext = lnext[0]
            h2 = unext[1] - lnext[1]
            li += 1
            ui += 1
        elif lnext[0] < upper[ui + 1][0]:
            xnext = lnext[0]
            frac = 1.0 * (xnext - upper[ui][0]) / (unext[0] - upper[ui][0])
            yupper = upper[ui][1] + frac * (unext[1] - upper[ui][1])
            h2 = yupper - lnext[1]
            li += 1
        else:
            xnext = unext[0]
            frac = 1.0 * (xnext - lower[li][0]) / (lnext[0] - lower[li][0])
            ylower = lower[li][1] + frac * (lnext[1] - lower[li][1])
            h2 = unext[1] - ylower
            ui += 1
        da = (xnext - x) * (h1 + h2) / 2.0
        total_area += da
        areas.append((x, xnext, h1, h2, da, total_area))
        x = xnext
        h1 = h2
    size = total_area / npieces
    cuts = []
    return da


def func_0637fe0a157b4940b39c5478c940b37b(npieces, upper, lower):
    li = ui = 0
    areas = []
    total_area = 0
    x = 0
    h1 = upper[0][1] - lower[0][1]
    W = lower[-1][0]
    while x < W:
        lnext = lower[li + 1]
        unext = upper[ui + 1]
        if lnext[0] == unext[0]:
            xnext = lnext[0]
            h2 = unext[1] - lnext[1]
            li += 1
            ui += 1
        elif lnext[0] < upper[ui + 1][0]:
            xnext = lnext[0]
            frac = 1.0 * (xnext - upper[ui][0]) / (unext[0] - upper[ui][0])
            yupper = upper[ui][1] + frac * (unext[1] - upper[ui][1])
            h2 = yupper - lnext[1]
            li += 1
        else:
            xnext = unext[0]
            frac = 1.0 * (xnext - lower[li][0]) / (lnext[0] - lower[li][0])
            ylower = lower[li][1] + frac * (lnext[1] - lower[li][1])
            h2 = unext[1] - ylower
            ui += 1
        da = (xnext - x) * (h1 + h2) / 2.0
        total_area += da
        areas.append((x, xnext, h1, h2, da, total_area))
        x = xnext
        h1 = h2
    size = total_area / npieces
    cuts = []
    return total_area


def func_4d05d525a968485d928e531817bc0acb(npieces, upper, lower):
    li = ui = 0
    areas = []
    total_area = 0
    x = 0
    h1 = upper[0][1] - lower[0][1]
    W = lower[-1][0]
    while x < W:
        lnext = lower[li + 1]
        unext = upper[ui + 1]
        if lnext[0] == unext[0]:
            xnext = lnext[0]
            h2 = unext[1] - lnext[1]
            li += 1
            ui += 1
        elif lnext[0] < upper[ui + 1][0]:
            xnext = lnext[0]
            frac = 1.0 * (xnext - upper[ui][0]) / (unext[0] - upper[ui][0])
            yupper = upper[ui][1] + frac * (unext[1] - upper[ui][1])
            h2 = yupper - lnext[1]
            li += 1
        else:
            xnext = unext[0]
            frac = 1.0 * (xnext - lower[li][0]) / (lnext[0] - lower[li][0])
            ylower = lower[li][1] + frac * (lnext[1] - lower[li][1])
            h2 = unext[1] - ylower
            ui += 1
        da = (xnext - x) * (h1 + h2) / 2.0
        total_area += da
        areas.append((x, xnext, h1, h2, da, total_area))
        x = xnext
        h1 = h2
    size = total_area / npieces
    cuts = []
    return yupper


def func_f1f7c11542304c23ab2d64a390608cf5(npieces, upper, lower):
    li = ui = 0
    areas = []
    total_area = 0
    x = 0
    h1 = upper[0][1] - lower[0][1]
    W = lower[-1][0]
    while x < W:
        lnext = lower[li + 1]
        unext = upper[ui + 1]
        if lnext[0] == unext[0]:
            xnext = lnext[0]
            h2 = unext[1] - lnext[1]
            li += 1
            ui += 1
        elif lnext[0] < upper[ui + 1][0]:
            xnext = lnext[0]
            frac = 1.0 * (xnext - upper[ui][0]) / (unext[0] - upper[ui][0])
            yupper = upper[ui][1] + frac * (unext[1] - upper[ui][1])
            h2 = yupper - lnext[1]
            li += 1
        else:
            xnext = unext[0]
            frac = 1.0 * (xnext - lower[li][0]) / (lnext[0] - lower[li][0])
            ylower = lower[li][1] + frac * (lnext[1] - lower[li][1])
            h2 = unext[1] - ylower
            ui += 1
        da = (xnext - x) * (h1 + h2) / 2.0
        total_area += da
        areas.append((x, xnext, h1, h2, da, total_area))
        x = xnext
        h1 = h2
    size = total_area / npieces
    cuts = []
    return size


def func_2e9a81391dc548d58b0dbf2d39b1cc6f(npieces, upper, lower):
    li = ui = 0
    areas = []
    total_area = 0
    x = 0
    h1 = upper[0][1] - lower[0][1]
    W = lower[-1][0]
    while x < W:
        lnext = lower[li + 1]
        unext = upper[ui + 1]
        if lnext[0] == unext[0]:
            xnext = lnext[0]
            h2 = unext[1] - lnext[1]
            li += 1
            ui += 1
        elif lnext[0] < upper[ui + 1][0]:
            xnext = lnext[0]
            frac = 1.0 * (xnext - upper[ui][0]) / (unext[0] - upper[ui][0])
            yupper = upper[ui][1] + frac * (unext[1] - upper[ui][1])
            h2 = yupper - lnext[1]
            li += 1
        else:
            xnext = unext[0]
            frac = 1.0 * (xnext - lower[li][0]) / (lnext[0] - lower[li][0])
            ylower = lower[li][1] + frac * (lnext[1] - lower[li][1])
            h2 = unext[1] - ylower
            ui += 1
        da = (xnext - x) * (h1 + h2) / 2.0
        total_area += da
        areas.append((x, xnext, h1, h2, da, total_area))
        x = xnext
        h1 = h2
    size = total_area / npieces
    cuts = []
    return li


def func_93218df47449454fb8cb11f3c42cfe3b(npieces, upper, lower):
    li = ui = 0
    areas = []
    total_area = 0
    x = 0
    h1 = upper[0][1] - lower[0][1]
    W = lower[-1][0]
    while x < W:
        lnext = lower[li + 1]
        unext = upper[ui + 1]
        if lnext[0] == unext[0]:
            xnext = lnext[0]
            h2 = unext[1] - lnext[1]
            li += 1
            ui += 1
        elif lnext[0] < upper[ui + 1][0]:
            xnext = lnext[0]
            frac = 1.0 * (xnext - upper[ui][0]) / (unext[0] - upper[ui][0])
            yupper = upper[ui][1] + frac * (unext[1] - upper[ui][1])
            h2 = yupper - lnext[1]
            li += 1
        else:
            xnext = unext[0]
            frac = 1.0 * (xnext - lower[li][0]) / (lnext[0] - lower[li][0])
            ylower = lower[li][1] + frac * (lnext[1] - lower[li][1])
            h2 = unext[1] - ylower
            ui += 1
        da = (xnext - x) * (h1 + h2) / 2.0
        total_area += da
        areas.append((x, xnext, h1, h2, da, total_area))
        x = xnext
        h1 = h2
    size = total_area / npieces
    cuts = []
    return h2


def func_432e2cc394704544bc8f25551eef15af(npieces, upper, lower):
    li = ui = 0
    areas = []
    total_area = 0
    x = 0
    h1 = upper[0][1] - lower[0][1]
    W = lower[-1][0]
    while x < W:
        lnext = lower[li + 1]
        unext = upper[ui + 1]
        if lnext[0] == unext[0]:
            xnext = lnext[0]
            h2 = unext[1] - lnext[1]
            li += 1
            ui += 1
        elif lnext[0] < upper[ui + 1][0]:
            xnext = lnext[0]
            frac = 1.0 * (xnext - upper[ui][0]) / (unext[0] - upper[ui][0])
            yupper = upper[ui][1] + frac * (unext[1] - upper[ui][1])
            h2 = yupper - lnext[1]
            li += 1
        else:
            xnext = unext[0]
            frac = 1.0 * (xnext - lower[li][0]) / (lnext[0] - lower[li][0])
            ylower = lower[li][1] + frac * (lnext[1] - lower[li][1])
            h2 = unext[1] - ylower
            ui += 1
        da = (xnext - x) * (h1 + h2) / 2.0
        total_area += da
        areas.append((x, xnext, h1, h2, da, total_area))
        x = xnext
        h1 = h2
    size = total_area / npieces
    cuts = []
    return ui


def func_d377c9bf443a458fb08823f9a2f25212(npieces, upper, lower):
    li = ui = 0
    areas = []
    total_area = 0
    x = 0
    h1 = upper[0][1] - lower[0][1]
    W = lower[-1][0]
    while x < W:
        lnext = lower[li + 1]
        unext = upper[ui + 1]
        if lnext[0] == unext[0]:
            xnext = lnext[0]
            h2 = unext[1] - lnext[1]
            li += 1
            ui += 1
        elif lnext[0] < upper[ui + 1][0]:
            xnext = lnext[0]
            frac = 1.0 * (xnext - upper[ui][0]) / (unext[0] - upper[ui][0])
            yupper = upper[ui][1] + frac * (unext[1] - upper[ui][1])
            h2 = yupper - lnext[1]
            li += 1
        else:
            xnext = unext[0]
            frac = 1.0 * (xnext - lower[li][0]) / (lnext[0] - lower[li][0])
            ylower = lower[li][1] + frac * (lnext[1] - lower[li][1])
            h2 = unext[1] - ylower
            ui += 1
        da = (xnext - x) * (h1 + h2) / 2.0
        total_area += da
        areas.append((x, xnext, h1, h2, da, total_area))
        x = xnext
        h1 = h2
    size = total_area / npieces
    cuts = []
    return x


def func_f6136353e5d746a69b3ab8d4afbb18c2(npieces, upper, lower):
    li = ui = 0
    areas = []
    total_area = 0
    x = 0
    h1 = upper[0][1] - lower[0][1]
    W = lower[-1][0]
    while x < W:
        lnext = lower[li + 1]
        unext = upper[ui + 1]
        if lnext[0] == unext[0]:
            xnext = lnext[0]
            h2 = unext[1] - lnext[1]
            li += 1
            ui += 1
        elif lnext[0] < upper[ui + 1][0]:
            xnext = lnext[0]
            frac = 1.0 * (xnext - upper[ui][0]) / (unext[0] - upper[ui][0])
            yupper = upper[ui][1] + frac * (unext[1] - upper[ui][1])
            h2 = yupper - lnext[1]
            li += 1
        else:
            xnext = unext[0]
            frac = 1.0 * (xnext - lower[li][0]) / (lnext[0] - lower[li][0])
            ylower = lower[li][1] + frac * (lnext[1] - lower[li][1])
            h2 = unext[1] - ylower
            ui += 1
        da = (xnext - x) * (h1 + h2) / 2.0
        total_area += da
        areas.append((x, xnext, h1, h2, da, total_area))
        x = xnext
        h1 = h2
    size = total_area / npieces
    cuts = []
    return frac


def func_b60cf29b3fa34c28a27ec25c840fbd08(npieces, upper, lower):
    li = ui = 0
    areas = []
    total_area = 0
    x = 0
    h1 = upper[0][1] - lower[0][1]
    W = lower[-1][0]
    while x < W:
        lnext = lower[li + 1]
        unext = upper[ui + 1]
        if lnext[0] == unext[0]:
            xnext = lnext[0]
            h2 = unext[1] - lnext[1]
            li += 1
            ui += 1
        elif lnext[0] < upper[ui + 1][0]:
            xnext = lnext[0]
            frac = 1.0 * (xnext - upper[ui][0]) / (unext[0] - upper[ui][0])
            yupper = upper[ui][1] + frac * (unext[1] - upper[ui][1])
            h2 = yupper - lnext[1]
            li += 1
        else:
            xnext = unext[0]
            frac = 1.0 * (xnext - lower[li][0]) / (lnext[0] - lower[li][0])
            ylower = lower[li][1] + frac * (lnext[1] - lower[li][1])
            h2 = unext[1] - ylower
            ui += 1
        da = (xnext - x) * (h1 + h2) / 2.0
        total_area += da
        areas.append((x, xnext, h1, h2, da, total_area))
        x = xnext
        h1 = h2
    size = total_area / npieces
    cuts = []
    return da


def func_0cb0b4aef6f24f36baac6378da1aba44(npieces, upper, lower):
    li = ui = 0
    areas = []
    total_area = 0
    x = 0
    h1 = upper[0][1] - lower[0][1]
    W = lower[-1][0]
    while x < W:
        lnext = lower[li + 1]
        unext = upper[ui + 1]
        if lnext[0] == unext[0]:
            xnext = lnext[0]
            h2 = unext[1] - lnext[1]
            li += 1
            ui += 1
        elif lnext[0] < upper[ui + 1][0]:
            xnext = lnext[0]
            frac = 1.0 * (xnext - upper[ui][0]) / (unext[0] - upper[ui][0])
            yupper = upper[ui][1] + frac * (unext[1] - upper[ui][1])
            h2 = yupper - lnext[1]
            li += 1
        else:
            xnext = unext[0]
            frac = 1.0 * (xnext - lower[li][0]) / (lnext[0] - lower[li][0])
            ylower = lower[li][1] + frac * (lnext[1] - lower[li][1])
            h2 = unext[1] - ylower
            ui += 1
        da = (xnext - x) * (h1 + h2) / 2.0
        total_area += da
        areas.append((x, xnext, h1, h2, da, total_area))
        x = xnext
        h1 = h2
    size = total_area / npieces
    cuts = []
    return ylower


def func_a37f4780647f4cd6a64aab0b571d2ba7(npieces, upper, lower):
    li = ui = 0
    areas = []
    total_area = 0
    x = 0
    h1 = upper[0][1] - lower[0][1]
    W = lower[-1][0]
    while x < W:
        lnext = lower[li + 1]
        unext = upper[ui + 1]
        if lnext[0] == unext[0]:
            xnext = lnext[0]
            h2 = unext[1] - lnext[1]
            li += 1
            ui += 1
        elif lnext[0] < upper[ui + 1][0]:
            xnext = lnext[0]
            frac = 1.0 * (xnext - upper[ui][0]) / (unext[0] - upper[ui][0])
            yupper = upper[ui][1] + frac * (unext[1] - upper[ui][1])
            h2 = yupper - lnext[1]
            li += 1
        else:
            xnext = unext[0]
            frac = 1.0 * (xnext - lower[li][0]) / (lnext[0] - lower[li][0])
            ylower = lower[li][1] + frac * (lnext[1] - lower[li][1])
            h2 = unext[1] - ylower
            ui += 1
        da = (xnext - x) * (h1 + h2) / 2.0
        total_area += da
        areas.append((x, xnext, h1, h2, da, total_area))
        x = xnext
        h1 = h2
    size = total_area / npieces
    cuts = []
    return areas


def func_b7bc3090e74b485f85f5c190ca4f6e62(npieces, upper, lower):
    li = ui = 0
    areas = []
    total_area = 0
    x = 0
    h1 = upper[0][1] - lower[0][1]
    W = lower[-1][0]
    while x < W:
        lnext = lower[li + 1]
        unext = upper[ui + 1]
        if lnext[0] == unext[0]:
            xnext = lnext[0]
            h2 = unext[1] - lnext[1]
            li += 1
            ui += 1
        elif lnext[0] < upper[ui + 1][0]:
            xnext = lnext[0]
            frac = 1.0 * (xnext - upper[ui][0]) / (unext[0] - upper[ui][0])
            yupper = upper[ui][1] + frac * (unext[1] - upper[ui][1])
            h2 = yupper - lnext[1]
            li += 1
        else:
            xnext = unext[0]
            frac = 1.0 * (xnext - lower[li][0]) / (lnext[0] - lower[li][0])
            ylower = lower[li][1] + frac * (lnext[1] - lower[li][1])
            h2 = unext[1] - ylower
            ui += 1
        da = (xnext - x) * (h1 + h2) / 2.0
        total_area += da
        areas.append((x, xnext, h1, h2, da, total_area))
        x = xnext
        h1 = h2
    size = total_area / npieces
    cuts = []
    return unext


def func_66fa8a8c429c438591118fda819c43f3(npieces, upper, lower):
    li = ui = 0
    areas = []
    total_area = 0
    x = 0
    h1 = upper[0][1] - lower[0][1]
    W = lower[-1][0]
    while x < W:
        lnext = lower[li + 1]
        unext = upper[ui + 1]
        if lnext[0] == unext[0]:
            xnext = lnext[0]
            h2 = unext[1] - lnext[1]
            li += 1
            ui += 1
        elif lnext[0] < upper[ui + 1][0]:
            xnext = lnext[0]
            frac = 1.0 * (xnext - upper[ui][0]) / (unext[0] - upper[ui][0])
            yupper = upper[ui][1] + frac * (unext[1] - upper[ui][1])
            h2 = yupper - lnext[1]
            li += 1
        else:
            xnext = unext[0]
            frac = 1.0 * (xnext - lower[li][0]) / (lnext[0] - lower[li][0])
            ylower = lower[li][1] + frac * (lnext[1] - lower[li][1])
            h2 = unext[1] - ylower
            ui += 1
        da = (xnext - x) * (h1 + h2) / 2.0
        total_area += da
        areas.append((x, xnext, h1, h2, da, total_area))
        x = xnext
        h1 = h2
    size = total_area / npieces
    cuts = []
    return W


def func_5f21b98da5864d83ae19d05cb233d2c7(npieces, upper, lower):
    li = ui = 0
    areas = []
    total_area = 0
    x = 0
    h1 = upper[0][1] - lower[0][1]
    W = lower[-1][0]
    while x < W:
        lnext = lower[li + 1]
        unext = upper[ui + 1]
        if lnext[0] == unext[0]:
            xnext = lnext[0]
            h2 = unext[1] - lnext[1]
            li += 1
            ui += 1
        elif lnext[0] < upper[ui + 1][0]:
            xnext = lnext[0]
            frac = 1.0 * (xnext - upper[ui][0]) / (unext[0] - upper[ui][0])
            yupper = upper[ui][1] + frac * (unext[1] - upper[ui][1])
            h2 = yupper - lnext[1]
            li += 1
        else:
            xnext = unext[0]
            frac = 1.0 * (xnext - lower[li][0]) / (lnext[0] - lower[li][0])
            ylower = lower[li][1] + frac * (lnext[1] - lower[li][1])
            h2 = unext[1] - ylower
            ui += 1
        da = (xnext - x) * (h1 + h2) / 2.0
        total_area += da
        areas.append((x, xnext, h1, h2, da, total_area))
        x = xnext
        h1 = h2
    size = total_area / npieces
    cuts = []
    return cuts


def func_4daa494d1c5d4c0eaae9e17b404fa932(npieces, upper, lower):
    li = ui = 0
    areas = []
    total_area = 0
    x = 0
    h1 = upper[0][1] - lower[0][1]
    W = lower[-1][0]
    while x < W:
        lnext = lower[li + 1]
        unext = upper[ui + 1]
        if lnext[0] == unext[0]:
            xnext = lnext[0]
            h2 = unext[1] - lnext[1]
            li += 1
            ui += 1
        elif lnext[0] < upper[ui + 1][0]:
            xnext = lnext[0]
            frac = 1.0 * (xnext - upper[ui][0]) / (unext[0] - upper[ui][0])
            yupper = upper[ui][1] + frac * (unext[1] - upper[ui][1])
            h2 = yupper - lnext[1]
            li += 1
        else:
            xnext = unext[0]
            frac = 1.0 * (xnext - lower[li][0]) / (lnext[0] - lower[li][0])
            ylower = lower[li][1] + frac * (lnext[1] - lower[li][1])
            h2 = unext[1] - ylower
            ui += 1
        da = (xnext - x) * (h1 + h2) / 2.0
        total_area += da
        areas.append((x, xnext, h1, h2, da, total_area))
        x = xnext
        h1 = h2
    size = total_area / npieces
    cuts = []
    return lnext


def func_1c29c8b01efa48738f2f4ee4e1df90c0(npieces, upper, lower):
    li = ui = 0
    areas = []
    total_area = 0
    x = 0
    h1 = upper[0][1] - lower[0][1]
    W = lower[-1][0]
    while x < W:
        lnext = lower[li + 1]
        unext = upper[ui + 1]
        if lnext[0] == unext[0]:
            xnext = lnext[0]
            h2 = unext[1] - lnext[1]
            li += 1
            ui += 1
        elif lnext[0] < upper[ui + 1][0]:
            xnext = lnext[0]
            frac = 1.0 * (xnext - upper[ui][0]) / (unext[0] - upper[ui][0])
            yupper = upper[ui][1] + frac * (unext[1] - upper[ui][1])
            h2 = yupper - lnext[1]
            li += 1
        else:
            xnext = unext[0]
            frac = 1.0 * (xnext - lower[li][0]) / (lnext[0] - lower[li][0])
            ylower = lower[li][1] + frac * (lnext[1] - lower[li][1])
            h2 = unext[1] - ylower
            ui += 1
        da = (xnext - x) * (h1 + h2) / 2.0
        total_area += da
        areas.append((x, xnext, h1, h2, da, total_area))
        x = xnext
        h1 = h2
    size = total_area / npieces
    cuts = []
    return xnext


def func_0b0e8a43042c441b9b81019d5debde14(npieces, upper, lower):
    li = ui = 0
    areas = []
    total_area = 0
    x = 0
    h1 = upper[0][1] - lower[0][1]
    W = lower[-1][0]
    while x < W:
        lnext = lower[li + 1]
        unext = upper[ui + 1]
        if lnext[0] == unext[0]:
            xnext = lnext[0]
            h2 = unext[1] - lnext[1]
            li += 1
            ui += 1
        elif lnext[0] < upper[ui + 1][0]:
            xnext = lnext[0]
            frac = 1.0 * (xnext - upper[ui][0]) / (unext[0] - upper[ui][0])
            yupper = upper[ui][1] + frac * (unext[1] - upper[ui][1])
            h2 = yupper - lnext[1]
            li += 1
        else:
            xnext = unext[0]
            frac = 1.0 * (xnext - lower[li][0]) / (lnext[0] - lower[li][0])
            ylower = lower[li][1] + frac * (lnext[1] - lower[li][1])
            h2 = unext[1] - ylower
            ui += 1
        da = (xnext - x) * (h1 + h2) / 2.0
        total_area += da
        areas.append((x, xnext, h1, h2, da, total_area))
        x = xnext
        h1 = h2
    size = total_area / npieces
    cuts = []
    return h1


def func_22d9e231361b41f098ab79829d2f1e9a(npieces, upper, lower):
    areas = []
    total_area = 0
    x = 0
    h1 = upper[0][1] - lower[0][1]
    W = lower[-1][0]
    while x < W:
        lnext = lower[li + 1]
        unext = upper[ui + 1]
        if lnext[0] == unext[0]:
            xnext = lnext[0]
            h2 = unext[1] - lnext[1]
            li += 1
            ui += 1
        elif lnext[0] < upper[ui + 1][0]:
            xnext = lnext[0]
            frac = 1.0 * (xnext - upper[ui][0]) / (unext[0] - upper[ui][0])
            yupper = upper[ui][1] + frac * (unext[1] - upper[ui][1])
            h2 = yupper - lnext[1]
            li += 1
        else:
            xnext = unext[0]
            frac = 1.0 * (xnext - lower[li][0]) / (lnext[0] - lower[li][0])
            ylower = lower[li][1] + frac * (lnext[1] - lower[li][1])
            h2 = unext[1] - ylower
            ui += 1
        da = (xnext - x) * (h1 + h2) / 2.0
        total_area += da
        areas.append((x, xnext, h1, h2, da, total_area))
        x = xnext
        h1 = h2
    size = total_area / npieces
    cuts = []
    needed = size
    return unext


def func_28ba656bc77d412688dd2f213e1eb791(npieces, upper, lower):
    areas = []
    total_area = 0
    x = 0
    h1 = upper[0][1] - lower[0][1]
    W = lower[-1][0]
    while x < W:
        lnext = lower[li + 1]
        unext = upper[ui + 1]
        if lnext[0] == unext[0]:
            xnext = lnext[0]
            h2 = unext[1] - lnext[1]
            li += 1
            ui += 1
        elif lnext[0] < upper[ui + 1][0]:
            xnext = lnext[0]
            frac = 1.0 * (xnext - upper[ui][0]) / (unext[0] - upper[ui][0])
            yupper = upper[ui][1] + frac * (unext[1] - upper[ui][1])
            h2 = yupper - lnext[1]
            li += 1
        else:
            xnext = unext[0]
            frac = 1.0 * (xnext - lower[li][0]) / (lnext[0] - lower[li][0])
            ylower = lower[li][1] + frac * (lnext[1] - lower[li][1])
            h2 = unext[1] - ylower
            ui += 1
        da = (xnext - x) * (h1 + h2) / 2.0
        total_area += da
        areas.append((x, xnext, h1, h2, da, total_area))
        x = xnext
        h1 = h2
    size = total_area / npieces
    cuts = []
    needed = size
    return x


def func_6bd2190ed4ed403788bfacc574c4af99(npieces, upper, lower):
    areas = []
    total_area = 0
    x = 0
    h1 = upper[0][1] - lower[0][1]
    W = lower[-1][0]
    while x < W:
        lnext = lower[li + 1]
        unext = upper[ui + 1]
        if lnext[0] == unext[0]:
            xnext = lnext[0]
            h2 = unext[1] - lnext[1]
            li += 1
            ui += 1
        elif lnext[0] < upper[ui + 1][0]:
            xnext = lnext[0]
            frac = 1.0 * (xnext - upper[ui][0]) / (unext[0] - upper[ui][0])
            yupper = upper[ui][1] + frac * (unext[1] - upper[ui][1])
            h2 = yupper - lnext[1]
            li += 1
        else:
            xnext = unext[0]
            frac = 1.0 * (xnext - lower[li][0]) / (lnext[0] - lower[li][0])
            ylower = lower[li][1] + frac * (lnext[1] - lower[li][1])
            h2 = unext[1] - ylower
            ui += 1
        da = (xnext - x) * (h1 + h2) / 2.0
        total_area += da
        areas.append((x, xnext, h1, h2, da, total_area))
        x = xnext
        h1 = h2
    size = total_area / npieces
    cuts = []
    needed = size
    return da


def func_20f1c574930748a5bb6203d38f9cc5b1(npieces, upper, lower):
    areas = []
    total_area = 0
    x = 0
    h1 = upper[0][1] - lower[0][1]
    W = lower[-1][0]
    while x < W:
        lnext = lower[li + 1]
        unext = upper[ui + 1]
        if lnext[0] == unext[0]:
            xnext = lnext[0]
            h2 = unext[1] - lnext[1]
            li += 1
            ui += 1
        elif lnext[0] < upper[ui + 1][0]:
            xnext = lnext[0]
            frac = 1.0 * (xnext - upper[ui][0]) / (unext[0] - upper[ui][0])
            yupper = upper[ui][1] + frac * (unext[1] - upper[ui][1])
            h2 = yupper - lnext[1]
            li += 1
        else:
            xnext = unext[0]
            frac = 1.0 * (xnext - lower[li][0]) / (lnext[0] - lower[li][0])
            ylower = lower[li][1] + frac * (lnext[1] - lower[li][1])
            h2 = unext[1] - ylower
            ui += 1
        da = (xnext - x) * (h1 + h2) / 2.0
        total_area += da
        areas.append((x, xnext, h1, h2, da, total_area))
        x = xnext
        h1 = h2
    size = total_area / npieces
    cuts = []
    needed = size
    return W


def func_4ad5e81c8ed346e0a72ffa940441e697(npieces, upper, lower):
    areas = []
    total_area = 0
    x = 0
    h1 = upper[0][1] - lower[0][1]
    W = lower[-1][0]
    while x < W:
        lnext = lower[li + 1]
        unext = upper[ui + 1]
        if lnext[0] == unext[0]:
            xnext = lnext[0]
            h2 = unext[1] - lnext[1]
            li += 1
            ui += 1
        elif lnext[0] < upper[ui + 1][0]:
            xnext = lnext[0]
            frac = 1.0 * (xnext - upper[ui][0]) / (unext[0] - upper[ui][0])
            yupper = upper[ui][1] + frac * (unext[1] - upper[ui][1])
            h2 = yupper - lnext[1]
            li += 1
        else:
            xnext = unext[0]
            frac = 1.0 * (xnext - lower[li][0]) / (lnext[0] - lower[li][0])
            ylower = lower[li][1] + frac * (lnext[1] - lower[li][1])
            h2 = unext[1] - ylower
            ui += 1
        da = (xnext - x) * (h1 + h2) / 2.0
        total_area += da
        areas.append((x, xnext, h1, h2, da, total_area))
        x = xnext
        h1 = h2
    size = total_area / npieces
    cuts = []
    needed = size
    return cuts


def func_52192b383b8b4cd7aa92bf1a32a43dcc(npieces, upper, lower):
    areas = []
    total_area = 0
    x = 0
    h1 = upper[0][1] - lower[0][1]
    W = lower[-1][0]
    while x < W:
        lnext = lower[li + 1]
        unext = upper[ui + 1]
        if lnext[0] == unext[0]:
            xnext = lnext[0]
            h2 = unext[1] - lnext[1]
            li += 1
            ui += 1
        elif lnext[0] < upper[ui + 1][0]:
            xnext = lnext[0]
            frac = 1.0 * (xnext - upper[ui][0]) / (unext[0] - upper[ui][0])
            yupper = upper[ui][1] + frac * (unext[1] - upper[ui][1])
            h2 = yupper - lnext[1]
            li += 1
        else:
            xnext = unext[0]
            frac = 1.0 * (xnext - lower[li][0]) / (lnext[0] - lower[li][0])
            ylower = lower[li][1] + frac * (lnext[1] - lower[li][1])
            h2 = unext[1] - ylower
            ui += 1
        da = (xnext - x) * (h1 + h2) / 2.0
        total_area += da
        areas.append((x, xnext, h1, h2, da, total_area))
        x = xnext
        h1 = h2
    size = total_area / npieces
    cuts = []
    needed = size
    return h2


def func_6a58c088da074de2813934f2c4fef6e9(npieces, upper, lower):
    areas = []
    total_area = 0
    x = 0
    h1 = upper[0][1] - lower[0][1]
    W = lower[-1][0]
    while x < W:
        lnext = lower[li + 1]
        unext = upper[ui + 1]
        if lnext[0] == unext[0]:
            xnext = lnext[0]
            h2 = unext[1] - lnext[1]
            li += 1
            ui += 1
        elif lnext[0] < upper[ui + 1][0]:
            xnext = lnext[0]
            frac = 1.0 * (xnext - upper[ui][0]) / (unext[0] - upper[ui][0])
            yupper = upper[ui][1] + frac * (unext[1] - upper[ui][1])
            h2 = yupper - lnext[1]
            li += 1
        else:
            xnext = unext[0]
            frac = 1.0 * (xnext - lower[li][0]) / (lnext[0] - lower[li][0])
            ylower = lower[li][1] + frac * (lnext[1] - lower[li][1])
            h2 = unext[1] - ylower
            ui += 1
        da = (xnext - x) * (h1 + h2) / 2.0
        total_area += da
        areas.append((x, xnext, h1, h2, da, total_area))
        x = xnext
        h1 = h2
    size = total_area / npieces
    cuts = []
    needed = size
    return ylower


def func_8ab932662878460783c50d08e4d2f893(npieces, upper, lower):
    areas = []
    total_area = 0
    x = 0
    h1 = upper[0][1] - lower[0][1]
    W = lower[-1][0]
    while x < W:
        lnext = lower[li + 1]
        unext = upper[ui + 1]
        if lnext[0] == unext[0]:
            xnext = lnext[0]
            h2 = unext[1] - lnext[1]
            li += 1
            ui += 1
        elif lnext[0] < upper[ui + 1][0]:
            xnext = lnext[0]
            frac = 1.0 * (xnext - upper[ui][0]) / (unext[0] - upper[ui][0])
            yupper = upper[ui][1] + frac * (unext[1] - upper[ui][1])
            h2 = yupper - lnext[1]
            li += 1
        else:
            xnext = unext[0]
            frac = 1.0 * (xnext - lower[li][0]) / (lnext[0] - lower[li][0])
            ylower = lower[li][1] + frac * (lnext[1] - lower[li][1])
            h2 = unext[1] - ylower
            ui += 1
        da = (xnext - x) * (h1 + h2) / 2.0
        total_area += da
        areas.append((x, xnext, h1, h2, da, total_area))
        x = xnext
        h1 = h2
    size = total_area / npieces
    cuts = []
    needed = size
    return li


def func_3b84d7b9e0d14db5a02ba91354873af9(npieces, upper, lower):
    areas = []
    total_area = 0
    x = 0
    h1 = upper[0][1] - lower[0][1]
    W = lower[-1][0]
    while x < W:
        lnext = lower[li + 1]
        unext = upper[ui + 1]
        if lnext[0] == unext[0]:
            xnext = lnext[0]
            h2 = unext[1] - lnext[1]
            li += 1
            ui += 1
        elif lnext[0] < upper[ui + 1][0]:
            xnext = lnext[0]
            frac = 1.0 * (xnext - upper[ui][0]) / (unext[0] - upper[ui][0])
            yupper = upper[ui][1] + frac * (unext[1] - upper[ui][1])
            h2 = yupper - lnext[1]
            li += 1
        else:
            xnext = unext[0]
            frac = 1.0 * (xnext - lower[li][0]) / (lnext[0] - lower[li][0])
            ylower = lower[li][1] + frac * (lnext[1] - lower[li][1])
            h2 = unext[1] - ylower
            ui += 1
        da = (xnext - x) * (h1 + h2) / 2.0
        total_area += da
        areas.append((x, xnext, h1, h2, da, total_area))
        x = xnext
        h1 = h2
    size = total_area / npieces
    cuts = []
    needed = size
    return size


def func_935475fe2ffd4a969886d49739954d8f(npieces, upper, lower):
    areas = []
    total_area = 0
    x = 0
    h1 = upper[0][1] - lower[0][1]
    W = lower[-1][0]
    while x < W:
        lnext = lower[li + 1]
        unext = upper[ui + 1]
        if lnext[0] == unext[0]:
            xnext = lnext[0]
            h2 = unext[1] - lnext[1]
            li += 1
            ui += 1
        elif lnext[0] < upper[ui + 1][0]:
            xnext = lnext[0]
            frac = 1.0 * (xnext - upper[ui][0]) / (unext[0] - upper[ui][0])
            yupper = upper[ui][1] + frac * (unext[1] - upper[ui][1])
            h2 = yupper - lnext[1]
            li += 1
        else:
            xnext = unext[0]
            frac = 1.0 * (xnext - lower[li][0]) / (lnext[0] - lower[li][0])
            ylower = lower[li][1] + frac * (lnext[1] - lower[li][1])
            h2 = unext[1] - ylower
            ui += 1
        da = (xnext - x) * (h1 + h2) / 2.0
        total_area += da
        areas.append((x, xnext, h1, h2, da, total_area))
        x = xnext
        h1 = h2
    size = total_area / npieces
    cuts = []
    needed = size
    return yupper


def func_b760d3526475405fb4328af5d98baa4e(npieces, upper, lower):
    areas = []
    total_area = 0
    x = 0
    h1 = upper[0][1] - lower[0][1]
    W = lower[-1][0]
    while x < W:
        lnext = lower[li + 1]
        unext = upper[ui + 1]
        if lnext[0] == unext[0]:
            xnext = lnext[0]
            h2 = unext[1] - lnext[1]
            li += 1
            ui += 1
        elif lnext[0] < upper[ui + 1][0]:
            xnext = lnext[0]
            frac = 1.0 * (xnext - upper[ui][0]) / (unext[0] - upper[ui][0])
            yupper = upper[ui][1] + frac * (unext[1] - upper[ui][1])
            h2 = yupper - lnext[1]
            li += 1
        else:
            xnext = unext[0]
            frac = 1.0 * (xnext - lower[li][0]) / (lnext[0] - lower[li][0])
            ylower = lower[li][1] + frac * (lnext[1] - lower[li][1])
            h2 = unext[1] - ylower
            ui += 1
        da = (xnext - x) * (h1 + h2) / 2.0
        total_area += da
        areas.append((x, xnext, h1, h2, da, total_area))
        x = xnext
        h1 = h2
    size = total_area / npieces
    cuts = []
    needed = size
    return h1


def func_85cb2beaf170477d96fbec65a32acb53(npieces, upper, lower):
    areas = []
    total_area = 0
    x = 0
    h1 = upper[0][1] - lower[0][1]
    W = lower[-1][0]
    while x < W:
        lnext = lower[li + 1]
        unext = upper[ui + 1]
        if lnext[0] == unext[0]:
            xnext = lnext[0]
            h2 = unext[1] - lnext[1]
            li += 1
            ui += 1
        elif lnext[0] < upper[ui + 1][0]:
            xnext = lnext[0]
            frac = 1.0 * (xnext - upper[ui][0]) / (unext[0] - upper[ui][0])
            yupper = upper[ui][1] + frac * (unext[1] - upper[ui][1])
            h2 = yupper - lnext[1]
            li += 1
        else:
            xnext = unext[0]
            frac = 1.0 * (xnext - lower[li][0]) / (lnext[0] - lower[li][0])
            ylower = lower[li][1] + frac * (lnext[1] - lower[li][1])
            h2 = unext[1] - ylower
            ui += 1
        da = (xnext - x) * (h1 + h2) / 2.0
        total_area += da
        areas.append((x, xnext, h1, h2, da, total_area))
        x = xnext
        h1 = h2
    size = total_area / npieces
    cuts = []
    needed = size
    return total_area


def func_1d32518c57404d0c9116ad03e57f5ad4(npieces, upper, lower):
    areas = []
    total_area = 0
    x = 0
    h1 = upper[0][1] - lower[0][1]
    W = lower[-1][0]
    while x < W:
        lnext = lower[li + 1]
        unext = upper[ui + 1]
        if lnext[0] == unext[0]:
            xnext = lnext[0]
            h2 = unext[1] - lnext[1]
            li += 1
            ui += 1
        elif lnext[0] < upper[ui + 1][0]:
            xnext = lnext[0]
            frac = 1.0 * (xnext - upper[ui][0]) / (unext[0] - upper[ui][0])
            yupper = upper[ui][1] + frac * (unext[1] - upper[ui][1])
            h2 = yupper - lnext[1]
            li += 1
        else:
            xnext = unext[0]
            frac = 1.0 * (xnext - lower[li][0]) / (lnext[0] - lower[li][0])
            ylower = lower[li][1] + frac * (lnext[1] - lower[li][1])
            h2 = unext[1] - ylower
            ui += 1
        da = (xnext - x) * (h1 + h2) / 2.0
        total_area += da
        areas.append((x, xnext, h1, h2, da, total_area))
        x = xnext
        h1 = h2
    size = total_area / npieces
    cuts = []
    needed = size
    return needed


def func_d5efa18751824256bbd1082d602e4f87(npieces, upper, lower):
    areas = []
    total_area = 0
    x = 0
    h1 = upper[0][1] - lower[0][1]
    W = lower[-1][0]
    while x < W:
        lnext = lower[li + 1]
        unext = upper[ui + 1]
        if lnext[0] == unext[0]:
            xnext = lnext[0]
            h2 = unext[1] - lnext[1]
            li += 1
            ui += 1
        elif lnext[0] < upper[ui + 1][0]:
            xnext = lnext[0]
            frac = 1.0 * (xnext - upper[ui][0]) / (unext[0] - upper[ui][0])
            yupper = upper[ui][1] + frac * (unext[1] - upper[ui][1])
            h2 = yupper - lnext[1]
            li += 1
        else:
            xnext = unext[0]
            frac = 1.0 * (xnext - lower[li][0]) / (lnext[0] - lower[li][0])
            ylower = lower[li][1] + frac * (lnext[1] - lower[li][1])
            h2 = unext[1] - ylower
            ui += 1
        da = (xnext - x) * (h1 + h2) / 2.0
        total_area += da
        areas.append((x, xnext, h1, h2, da, total_area))
        x = xnext
        h1 = h2
    size = total_area / npieces
    cuts = []
    needed = size
    return xnext


def func_f5f9e9ec69df4edb90b285d7df4f39cc(npieces, upper, lower):
    areas = []
    total_area = 0
    x = 0
    h1 = upper[0][1] - lower[0][1]
    W = lower[-1][0]
    while x < W:
        lnext = lower[li + 1]
        unext = upper[ui + 1]
        if lnext[0] == unext[0]:
            xnext = lnext[0]
            h2 = unext[1] - lnext[1]
            li += 1
            ui += 1
        elif lnext[0] < upper[ui + 1][0]:
            xnext = lnext[0]
            frac = 1.0 * (xnext - upper[ui][0]) / (unext[0] - upper[ui][0])
            yupper = upper[ui][1] + frac * (unext[1] - upper[ui][1])
            h2 = yupper - lnext[1]
            li += 1
        else:
            xnext = unext[0]
            frac = 1.0 * (xnext - lower[li][0]) / (lnext[0] - lower[li][0])
            ylower = lower[li][1] + frac * (lnext[1] - lower[li][1])
            h2 = unext[1] - ylower
            ui += 1
        da = (xnext - x) * (h1 + h2) / 2.0
        total_area += da
        areas.append((x, xnext, h1, h2, da, total_area))
        x = xnext
        h1 = h2
    size = total_area / npieces
    cuts = []
    needed = size
    return lnext


def func_2307dd49b9e144ee92b72e6d43875eb5(npieces, upper, lower):
    areas = []
    total_area = 0
    x = 0
    h1 = upper[0][1] - lower[0][1]
    W = lower[-1][0]
    while x < W:
        lnext = lower[li + 1]
        unext = upper[ui + 1]
        if lnext[0] == unext[0]:
            xnext = lnext[0]
            h2 = unext[1] - lnext[1]
            li += 1
            ui += 1
        elif lnext[0] < upper[ui + 1][0]:
            xnext = lnext[0]
            frac = 1.0 * (xnext - upper[ui][0]) / (unext[0] - upper[ui][0])
            yupper = upper[ui][1] + frac * (unext[1] - upper[ui][1])
            h2 = yupper - lnext[1]
            li += 1
        else:
            xnext = unext[0]
            frac = 1.0 * (xnext - lower[li][0]) / (lnext[0] - lower[li][0])
            ylower = lower[li][1] + frac * (lnext[1] - lower[li][1])
            h2 = unext[1] - ylower
            ui += 1
        da = (xnext - x) * (h1 + h2) / 2.0
        total_area += da
        areas.append((x, xnext, h1, h2, da, total_area))
        x = xnext
        h1 = h2
    size = total_area / npieces
    cuts = []
    needed = size
    return ui


def func_c873502025434f659de4ff963b7df582(npieces, upper, lower):
    areas = []
    total_area = 0
    x = 0
    h1 = upper[0][1] - lower[0][1]
    W = lower[-1][0]
    while x < W:
        lnext = lower[li + 1]
        unext = upper[ui + 1]
        if lnext[0] == unext[0]:
            xnext = lnext[0]
            h2 = unext[1] - lnext[1]
            li += 1
            ui += 1
        elif lnext[0] < upper[ui + 1][0]:
            xnext = lnext[0]
            frac = 1.0 * (xnext - upper[ui][0]) / (unext[0] - upper[ui][0])
            yupper = upper[ui][1] + frac * (unext[1] - upper[ui][1])
            h2 = yupper - lnext[1]
            li += 1
        else:
            xnext = unext[0]
            frac = 1.0 * (xnext - lower[li][0]) / (lnext[0] - lower[li][0])
            ylower = lower[li][1] + frac * (lnext[1] - lower[li][1])
            h2 = unext[1] - ylower
            ui += 1
        da = (xnext - x) * (h1 + h2) / 2.0
        total_area += da
        areas.append((x, xnext, h1, h2, da, total_area))
        x = xnext
        h1 = h2
    size = total_area / npieces
    cuts = []
    needed = size
    return areas


def func_a69c6f24d24d4159afa22ced195c4fc3(npieces, upper, lower):
    areas = []
    total_area = 0
    x = 0
    h1 = upper[0][1] - lower[0][1]
    W = lower[-1][0]
    while x < W:
        lnext = lower[li + 1]
        unext = upper[ui + 1]
        if lnext[0] == unext[0]:
            xnext = lnext[0]
            h2 = unext[1] - lnext[1]
            li += 1
            ui += 1
        elif lnext[0] < upper[ui + 1][0]:
            xnext = lnext[0]
            frac = 1.0 * (xnext - upper[ui][0]) / (unext[0] - upper[ui][0])
            yupper = upper[ui][1] + frac * (unext[1] - upper[ui][1])
            h2 = yupper - lnext[1]
            li += 1
        else:
            xnext = unext[0]
            frac = 1.0 * (xnext - lower[li][0]) / (lnext[0] - lower[li][0])
            ylower = lower[li][1] + frac * (lnext[1] - lower[li][1])
            h2 = unext[1] - ylower
            ui += 1
        da = (xnext - x) * (h1 + h2) / 2.0
        total_area += da
        areas.append((x, xnext, h1, h2, da, total_area))
        x = xnext
        h1 = h2
    size = total_area / npieces
    cuts = []
    needed = size
    return frac


def func_3e15508892604d7db95919625914185e(npieces, upper, lower):
    li = ui = 0
    areas = []
    total_area = 0
    x = 0
    h1 = upper[0][1] - lower[0][1]
    W = lower[-1][0]
    while x < W:
        lnext = lower[li + 1]
        unext = upper[ui + 1]
        if lnext[0] == unext[0]:
            xnext = lnext[0]
            h2 = unext[1] - lnext[1]
            li += 1
            ui += 1
        elif lnext[0] < upper[ui + 1][0]:
            xnext = lnext[0]
            frac = 1.0 * (xnext - upper[ui][0]) / (unext[0] - upper[ui][0])
            yupper = upper[ui][1] + frac * (unext[1] - upper[ui][1])
            h2 = yupper - lnext[1]
            li += 1
        else:
            xnext = unext[0]
            frac = 1.0 * (xnext - lower[li][0]) / (lnext[0] - lower[li][0])
            ylower = lower[li][1] + frac * (lnext[1] - lower[li][1])
            h2 = unext[1] - ylower
            ui += 1
        da = (xnext - x) * (h1 + h2) / 2.0
        total_area += da
        areas.append((x, xnext, h1, h2, da, total_area))
        x = xnext
        h1 = h2
    size = total_area / npieces
    cuts = []
    needed = size
    return ui


def func_fd053951cd434607b86e44ee1ee4bbe6(npieces, upper, lower):
    li = ui = 0
    areas = []
    total_area = 0
    x = 0
    h1 = upper[0][1] - lower[0][1]
    W = lower[-1][0]
    while x < W:
        lnext = lower[li + 1]
        unext = upper[ui + 1]
        if lnext[0] == unext[0]:
            xnext = lnext[0]
            h2 = unext[1] - lnext[1]
            li += 1
            ui += 1
        elif lnext[0] < upper[ui + 1][0]:
            xnext = lnext[0]
            frac = 1.0 * (xnext - upper[ui][0]) / (unext[0] - upper[ui][0])
            yupper = upper[ui][1] + frac * (unext[1] - upper[ui][1])
            h2 = yupper - lnext[1]
            li += 1
        else:
            xnext = unext[0]
            frac = 1.0 * (xnext - lower[li][0]) / (lnext[0] - lower[li][0])
            ylower = lower[li][1] + frac * (lnext[1] - lower[li][1])
            h2 = unext[1] - ylower
            ui += 1
        da = (xnext - x) * (h1 + h2) / 2.0
        total_area += da
        areas.append((x, xnext, h1, h2, da, total_area))
        x = xnext
        h1 = h2
    size = total_area / npieces
    cuts = []
    needed = size
    return lnext


def func_65cc721e885349edad062254680ef2cf(npieces, upper, lower):
    li = ui = 0
    areas = []
    total_area = 0
    x = 0
    h1 = upper[0][1] - lower[0][1]
    W = lower[-1][0]
    while x < W:
        lnext = lower[li + 1]
        unext = upper[ui + 1]
        if lnext[0] == unext[0]:
            xnext = lnext[0]
            h2 = unext[1] - lnext[1]
            li += 1
            ui += 1
        elif lnext[0] < upper[ui + 1][0]:
            xnext = lnext[0]
            frac = 1.0 * (xnext - upper[ui][0]) / (unext[0] - upper[ui][0])
            yupper = upper[ui][1] + frac * (unext[1] - upper[ui][1])
            h2 = yupper - lnext[1]
            li += 1
        else:
            xnext = unext[0]
            frac = 1.0 * (xnext - lower[li][0]) / (lnext[0] - lower[li][0])
            ylower = lower[li][1] + frac * (lnext[1] - lower[li][1])
            h2 = unext[1] - ylower
            ui += 1
        da = (xnext - x) * (h1 + h2) / 2.0
        total_area += da
        areas.append((x, xnext, h1, h2, da, total_area))
        x = xnext
        h1 = h2
    size = total_area / npieces
    cuts = []
    needed = size
    return needed


def func_6e211365ae4640baadc74cfa33adcabc(npieces, upper, lower):
    li = ui = 0
    areas = []
    total_area = 0
    x = 0
    h1 = upper[0][1] - lower[0][1]
    W = lower[-1][0]
    while x < W:
        lnext = lower[li + 1]
        unext = upper[ui + 1]
        if lnext[0] == unext[0]:
            xnext = lnext[0]
            h2 = unext[1] - lnext[1]
            li += 1
            ui += 1
        elif lnext[0] < upper[ui + 1][0]:
            xnext = lnext[0]
            frac = 1.0 * (xnext - upper[ui][0]) / (unext[0] - upper[ui][0])
            yupper = upper[ui][1] + frac * (unext[1] - upper[ui][1])
            h2 = yupper - lnext[1]
            li += 1
        else:
            xnext = unext[0]
            frac = 1.0 * (xnext - lower[li][0]) / (lnext[0] - lower[li][0])
            ylower = lower[li][1] + frac * (lnext[1] - lower[li][1])
            h2 = unext[1] - ylower
            ui += 1
        da = (xnext - x) * (h1 + h2) / 2.0
        total_area += da
        areas.append((x, xnext, h1, h2, da, total_area))
        x = xnext
        h1 = h2
    size = total_area / npieces
    cuts = []
    needed = size
    return W


def func_582d141101ae41c0b72276814eccaa0d(npieces, upper, lower):
    li = ui = 0
    areas = []
    total_area = 0
    x = 0
    h1 = upper[0][1] - lower[0][1]
    W = lower[-1][0]
    while x < W:
        lnext = lower[li + 1]
        unext = upper[ui + 1]
        if lnext[0] == unext[0]:
            xnext = lnext[0]
            h2 = unext[1] - lnext[1]
            li += 1
            ui += 1
        elif lnext[0] < upper[ui + 1][0]:
            xnext = lnext[0]
            frac = 1.0 * (xnext - upper[ui][0]) / (unext[0] - upper[ui][0])
            yupper = upper[ui][1] + frac * (unext[1] - upper[ui][1])
            h2 = yupper - lnext[1]
            li += 1
        else:
            xnext = unext[0]
            frac = 1.0 * (xnext - lower[li][0]) / (lnext[0] - lower[li][0])
            ylower = lower[li][1] + frac * (lnext[1] - lower[li][1])
            h2 = unext[1] - ylower
            ui += 1
        da = (xnext - x) * (h1 + h2) / 2.0
        total_area += da
        areas.append((x, xnext, h1, h2, da, total_area))
        x = xnext
        h1 = h2
    size = total_area / npieces
    cuts = []
    needed = size
    return total_area


def func_b15552ce32d142d591434e67a5590d08(npieces, upper, lower):
    li = ui = 0
    areas = []
    total_area = 0
    x = 0
    h1 = upper[0][1] - lower[0][1]
    W = lower[-1][0]
    while x < W:
        lnext = lower[li + 1]
        unext = upper[ui + 1]
        if lnext[0] == unext[0]:
            xnext = lnext[0]
            h2 = unext[1] - lnext[1]
            li += 1
            ui += 1
        elif lnext[0] < upper[ui + 1][0]:
            xnext = lnext[0]
            frac = 1.0 * (xnext - upper[ui][0]) / (unext[0] - upper[ui][0])
            yupper = upper[ui][1] + frac * (unext[1] - upper[ui][1])
            h2 = yupper - lnext[1]
            li += 1
        else:
            xnext = unext[0]
            frac = 1.0 * (xnext - lower[li][0]) / (lnext[0] - lower[li][0])
            ylower = lower[li][1] + frac * (lnext[1] - lower[li][1])
            h2 = unext[1] - ylower
            ui += 1
        da = (xnext - x) * (h1 + h2) / 2.0
        total_area += da
        areas.append((x, xnext, h1, h2, da, total_area))
        x = xnext
        h1 = h2
    size = total_area / npieces
    cuts = []
    needed = size
    return xnext


def func_4378c2aa966748879ab1dc5f133b8c61(npieces, upper, lower):
    li = ui = 0
    areas = []
    total_area = 0
    x = 0
    h1 = upper[0][1] - lower[0][1]
    W = lower[-1][0]
    while x < W:
        lnext = lower[li + 1]
        unext = upper[ui + 1]
        if lnext[0] == unext[0]:
            xnext = lnext[0]
            h2 = unext[1] - lnext[1]
            li += 1
            ui += 1
        elif lnext[0] < upper[ui + 1][0]:
            xnext = lnext[0]
            frac = 1.0 * (xnext - upper[ui][0]) / (unext[0] - upper[ui][0])
            yupper = upper[ui][1] + frac * (unext[1] - upper[ui][1])
            h2 = yupper - lnext[1]
            li += 1
        else:
            xnext = unext[0]
            frac = 1.0 * (xnext - lower[li][0]) / (lnext[0] - lower[li][0])
            ylower = lower[li][1] + frac * (lnext[1] - lower[li][1])
            h2 = unext[1] - ylower
            ui += 1
        da = (xnext - x) * (h1 + h2) / 2.0
        total_area += da
        areas.append((x, xnext, h1, h2, da, total_area))
        x = xnext
        h1 = h2
    size = total_area / npieces
    cuts = []
    needed = size
    return x


def func_d412696b82ae476295595541156d8de3(npieces, upper, lower):
    li = ui = 0
    areas = []
    total_area = 0
    x = 0
    h1 = upper[0][1] - lower[0][1]
    W = lower[-1][0]
    while x < W:
        lnext = lower[li + 1]
        unext = upper[ui + 1]
        if lnext[0] == unext[0]:
            xnext = lnext[0]
            h2 = unext[1] - lnext[1]
            li += 1
            ui += 1
        elif lnext[0] < upper[ui + 1][0]:
            xnext = lnext[0]
            frac = 1.0 * (xnext - upper[ui][0]) / (unext[0] - upper[ui][0])
            yupper = upper[ui][1] + frac * (unext[1] - upper[ui][1])
            h2 = yupper - lnext[1]
            li += 1
        else:
            xnext = unext[0]
            frac = 1.0 * (xnext - lower[li][0]) / (lnext[0] - lower[li][0])
            ylower = lower[li][1] + frac * (lnext[1] - lower[li][1])
            h2 = unext[1] - ylower
            ui += 1
        da = (xnext - x) * (h1 + h2) / 2.0
        total_area += da
        areas.append((x, xnext, h1, h2, da, total_area))
        x = xnext
        h1 = h2
    size = total_area / npieces
    cuts = []
    needed = size
    return li


def func_de14fe88c5f7446a8f7d72126c795494(npieces, upper, lower):
    li = ui = 0
    areas = []
    total_area = 0
    x = 0
    h1 = upper[0][1] - lower[0][1]
    W = lower[-1][0]
    while x < W:
        lnext = lower[li + 1]
        unext = upper[ui + 1]
        if lnext[0] == unext[0]:
            xnext = lnext[0]
            h2 = unext[1] - lnext[1]
            li += 1
            ui += 1
        elif lnext[0] < upper[ui + 1][0]:
            xnext = lnext[0]
            frac = 1.0 * (xnext - upper[ui][0]) / (unext[0] - upper[ui][0])
            yupper = upper[ui][1] + frac * (unext[1] - upper[ui][1])
            h2 = yupper - lnext[1]
            li += 1
        else:
            xnext = unext[0]
            frac = 1.0 * (xnext - lower[li][0]) / (lnext[0] - lower[li][0])
            ylower = lower[li][1] + frac * (lnext[1] - lower[li][1])
            h2 = unext[1] - ylower
            ui += 1
        da = (xnext - x) * (h1 + h2) / 2.0
        total_area += da
        areas.append((x, xnext, h1, h2, da, total_area))
        x = xnext
        h1 = h2
    size = total_area / npieces
    cuts = []
    needed = size
    return h1


def func_ab62ba35381648d38eb95b81f3768f03(npieces, upper, lower):
    li = ui = 0
    areas = []
    total_area = 0
    x = 0
    h1 = upper[0][1] - lower[0][1]
    W = lower[-1][0]
    while x < W:
        lnext = lower[li + 1]
        unext = upper[ui + 1]
        if lnext[0] == unext[0]:
            xnext = lnext[0]
            h2 = unext[1] - lnext[1]
            li += 1
            ui += 1
        elif lnext[0] < upper[ui + 1][0]:
            xnext = lnext[0]
            frac = 1.0 * (xnext - upper[ui][0]) / (unext[0] - upper[ui][0])
            yupper = upper[ui][1] + frac * (unext[1] - upper[ui][1])
            h2 = yupper - lnext[1]
            li += 1
        else:
            xnext = unext[0]
            frac = 1.0 * (xnext - lower[li][0]) / (lnext[0] - lower[li][0])
            ylower = lower[li][1] + frac * (lnext[1] - lower[li][1])
            h2 = unext[1] - ylower
            ui += 1
        da = (xnext - x) * (h1 + h2) / 2.0
        total_area += da
        areas.append((x, xnext, h1, h2, da, total_area))
        x = xnext
        h1 = h2
    size = total_area / npieces
    cuts = []
    needed = size
    return cuts


def func_732fdc559d90418881f7bc5ede659739(npieces, upper, lower):
    li = ui = 0
    areas = []
    total_area = 0
    x = 0
    h1 = upper[0][1] - lower[0][1]
    W = lower[-1][0]
    while x < W:
        lnext = lower[li + 1]
        unext = upper[ui + 1]
        if lnext[0] == unext[0]:
            xnext = lnext[0]
            h2 = unext[1] - lnext[1]
            li += 1
            ui += 1
        elif lnext[0] < upper[ui + 1][0]:
            xnext = lnext[0]
            frac = 1.0 * (xnext - upper[ui][0]) / (unext[0] - upper[ui][0])
            yupper = upper[ui][1] + frac * (unext[1] - upper[ui][1])
            h2 = yupper - lnext[1]
            li += 1
        else:
            xnext = unext[0]
            frac = 1.0 * (xnext - lower[li][0]) / (lnext[0] - lower[li][0])
            ylower = lower[li][1] + frac * (lnext[1] - lower[li][1])
            h2 = unext[1] - ylower
            ui += 1
        da = (xnext - x) * (h1 + h2) / 2.0
        total_area += da
        areas.append((x, xnext, h1, h2, da, total_area))
        x = xnext
        h1 = h2
    size = total_area / npieces
    cuts = []
    needed = size
    return da


def func_f1c2d36ebdf84ad0a1876b50a5b4c8cf(npieces, upper, lower):
    li = ui = 0
    areas = []
    total_area = 0
    x = 0
    h1 = upper[0][1] - lower[0][1]
    W = lower[-1][0]
    while x < W:
        lnext = lower[li + 1]
        unext = upper[ui + 1]
        if lnext[0] == unext[0]:
            xnext = lnext[0]
            h2 = unext[1] - lnext[1]
            li += 1
            ui += 1
        elif lnext[0] < upper[ui + 1][0]:
            xnext = lnext[0]
            frac = 1.0 * (xnext - upper[ui][0]) / (unext[0] - upper[ui][0])
            yupper = upper[ui][1] + frac * (unext[1] - upper[ui][1])
            h2 = yupper - lnext[1]
            li += 1
        else:
            xnext = unext[0]
            frac = 1.0 * (xnext - lower[li][0]) / (lnext[0] - lower[li][0])
            ylower = lower[li][1] + frac * (lnext[1] - lower[li][1])
            h2 = unext[1] - ylower
            ui += 1
        da = (xnext - x) * (h1 + h2) / 2.0
        total_area += da
        areas.append((x, xnext, h1, h2, da, total_area))
        x = xnext
        h1 = h2
    size = total_area / npieces
    cuts = []
    needed = size
    return areas


def func_38fabf5d7502471e9390fb060712abf1(npieces, upper, lower):
    li = ui = 0
    areas = []
    total_area = 0
    x = 0
    h1 = upper[0][1] - lower[0][1]
    W = lower[-1][0]
    while x < W:
        lnext = lower[li + 1]
        unext = upper[ui + 1]
        if lnext[0] == unext[0]:
            xnext = lnext[0]
            h2 = unext[1] - lnext[1]
            li += 1
            ui += 1
        elif lnext[0] < upper[ui + 1][0]:
            xnext = lnext[0]
            frac = 1.0 * (xnext - upper[ui][0]) / (unext[0] - upper[ui][0])
            yupper = upper[ui][1] + frac * (unext[1] - upper[ui][1])
            h2 = yupper - lnext[1]
            li += 1
        else:
            xnext = unext[0]
            frac = 1.0 * (xnext - lower[li][0]) / (lnext[0] - lower[li][0])
            ylower = lower[li][1] + frac * (lnext[1] - lower[li][1])
            h2 = unext[1] - ylower
            ui += 1
        da = (xnext - x) * (h1 + h2) / 2.0
        total_area += da
        areas.append((x, xnext, h1, h2, da, total_area))
        x = xnext
        h1 = h2
    size = total_area / npieces
    cuts = []
    needed = size
    return ylower


def func_405e858f85eb47afba179ecc463e2eae(npieces, upper, lower):
    li = ui = 0
    areas = []
    total_area = 0
    x = 0
    h1 = upper[0][1] - lower[0][1]
    W = lower[-1][0]
    while x < W:
        lnext = lower[li + 1]
        unext = upper[ui + 1]
        if lnext[0] == unext[0]:
            xnext = lnext[0]
            h2 = unext[1] - lnext[1]
            li += 1
            ui += 1
        elif lnext[0] < upper[ui + 1][0]:
            xnext = lnext[0]
            frac = 1.0 * (xnext - upper[ui][0]) / (unext[0] - upper[ui][0])
            yupper = upper[ui][1] + frac * (unext[1] - upper[ui][1])
            h2 = yupper - lnext[1]
            li += 1
        else:
            xnext = unext[0]
            frac = 1.0 * (xnext - lower[li][0]) / (lnext[0] - lower[li][0])
            ylower = lower[li][1] + frac * (lnext[1] - lower[li][1])
            h2 = unext[1] - ylower
            ui += 1
        da = (xnext - x) * (h1 + h2) / 2.0
        total_area += da
        areas.append((x, xnext, h1, h2, da, total_area))
        x = xnext
        h1 = h2
    size = total_area / npieces
    cuts = []
    needed = size
    return frac


def func_7f6e276160854b0cb9de32cb83316e06(npieces, upper, lower):
    li = ui = 0
    areas = []
    total_area = 0
    x = 0
    h1 = upper[0][1] - lower[0][1]
    W = lower[-1][0]
    while x < W:
        lnext = lower[li + 1]
        unext = upper[ui + 1]
        if lnext[0] == unext[0]:
            xnext = lnext[0]
            h2 = unext[1] - lnext[1]
            li += 1
            ui += 1
        elif lnext[0] < upper[ui + 1][0]:
            xnext = lnext[0]
            frac = 1.0 * (xnext - upper[ui][0]) / (unext[0] - upper[ui][0])
            yupper = upper[ui][1] + frac * (unext[1] - upper[ui][1])
            h2 = yupper - lnext[1]
            li += 1
        else:
            xnext = unext[0]
            frac = 1.0 * (xnext - lower[li][0]) / (lnext[0] - lower[li][0])
            ylower = lower[li][1] + frac * (lnext[1] - lower[li][1])
            h2 = unext[1] - ylower
            ui += 1
        da = (xnext - x) * (h1 + h2) / 2.0
        total_area += da
        areas.append((x, xnext, h1, h2, da, total_area))
        x = xnext
        h1 = h2
    size = total_area / npieces
    cuts = []
    needed = size
    return h2


def func_34a3aee8069a4184a5cfaf41234621a8(npieces, upper, lower):
    li = ui = 0
    areas = []
    total_area = 0
    x = 0
    h1 = upper[0][1] - lower[0][1]
    W = lower[-1][0]
    while x < W:
        lnext = lower[li + 1]
        unext = upper[ui + 1]
        if lnext[0] == unext[0]:
            xnext = lnext[0]
            h2 = unext[1] - lnext[1]
            li += 1
            ui += 1
        elif lnext[0] < upper[ui + 1][0]:
            xnext = lnext[0]
            frac = 1.0 * (xnext - upper[ui][0]) / (unext[0] - upper[ui][0])
            yupper = upper[ui][1] + frac * (unext[1] - upper[ui][1])
            h2 = yupper - lnext[1]
            li += 1
        else:
            xnext = unext[0]
            frac = 1.0 * (xnext - lower[li][0]) / (lnext[0] - lower[li][0])
            ylower = lower[li][1] + frac * (lnext[1] - lower[li][1])
            h2 = unext[1] - ylower
            ui += 1
        da = (xnext - x) * (h1 + h2) / 2.0
        total_area += da
        areas.append((x, xnext, h1, h2, da, total_area))
        x = xnext
        h1 = h2
    size = total_area / npieces
    cuts = []
    needed = size
    return unext


def func_97bf2facb17a4d04a8613ece44e9d1ef(npieces, upper, lower):
    li = ui = 0
    areas = []
    total_area = 0
    x = 0
    h1 = upper[0][1] - lower[0][1]
    W = lower[-1][0]
    while x < W:
        lnext = lower[li + 1]
        unext = upper[ui + 1]
        if lnext[0] == unext[0]:
            xnext = lnext[0]
            h2 = unext[1] - lnext[1]
            li += 1
            ui += 1
        elif lnext[0] < upper[ui + 1][0]:
            xnext = lnext[0]
            frac = 1.0 * (xnext - upper[ui][0]) / (unext[0] - upper[ui][0])
            yupper = upper[ui][1] + frac * (unext[1] - upper[ui][1])
            h2 = yupper - lnext[1]
            li += 1
        else:
            xnext = unext[0]
            frac = 1.0 * (xnext - lower[li][0]) / (lnext[0] - lower[li][0])
            ylower = lower[li][1] + frac * (lnext[1] - lower[li][1])
            h2 = unext[1] - ylower
            ui += 1
        da = (xnext - x) * (h1 + h2) / 2.0
        total_area += da
        areas.append((x, xnext, h1, h2, da, total_area))
        x = xnext
        h1 = h2
    size = total_area / npieces
    cuts = []
    needed = size
    return yupper


def func_b0c6a9790ac9463c94555e61e46286c5(npieces, upper, lower):
    li = ui = 0
    areas = []
    total_area = 0
    x = 0
    h1 = upper[0][1] - lower[0][1]
    W = lower[-1][0]
    while x < W:
        lnext = lower[li + 1]
        unext = upper[ui + 1]
        if lnext[0] == unext[0]:
            xnext = lnext[0]
            h2 = unext[1] - lnext[1]
            li += 1
            ui += 1
        elif lnext[0] < upper[ui + 1][0]:
            xnext = lnext[0]
            frac = 1.0 * (xnext - upper[ui][0]) / (unext[0] - upper[ui][0])
            yupper = upper[ui][1] + frac * (unext[1] - upper[ui][1])
            h2 = yupper - lnext[1]
            li += 1
        else:
            xnext = unext[0]
            frac = 1.0 * (xnext - lower[li][0]) / (lnext[0] - lower[li][0])
            ylower = lower[li][1] + frac * (lnext[1] - lower[li][1])
            h2 = unext[1] - ylower
            ui += 1
        da = (xnext - x) * (h1 + h2) / 2.0
        total_area += da
        areas.append((x, xnext, h1, h2, da, total_area))
        x = xnext
        h1 = h2
    size = total_area / npieces
    cuts = []
    needed = size
    return size


def func_3cb29863d41147569feb372bde4bd8c4(npieces, upper, lower):
    areas = []
    total_area = 0
    x = 0
    h1 = upper[0][1] - lower[0][1]
    W = lower[-1][0]
    while x < W:
        lnext = lower[li + 1]
        unext = upper[ui + 1]
        if lnext[0] == unext[0]:
            xnext = lnext[0]
            h2 = unext[1] - lnext[1]
            li += 1
            ui += 1
        elif lnext[0] < upper[ui + 1][0]:
            xnext = lnext[0]
            frac = 1.0 * (xnext - upper[ui][0]) / (unext[0] - upper[ui][0])
            yupper = upper[ui][1] + frac * (unext[1] - upper[ui][1])
            h2 = yupper - lnext[1]
            li += 1
        else:
            xnext = unext[0]
            frac = 1.0 * (xnext - lower[li][0]) / (lnext[0] - lower[li][0])
            ylower = lower[li][1] + frac * (lnext[1] - lower[li][1])
            h2 = unext[1] - ylower
            ui += 1
        da = (xnext - x) * (h1 + h2) / 2.0
        total_area += da
        areas.append((x, xnext, h1, h2, da, total_area))
        x = xnext
        h1 = h2
    size = total_area / npieces
    cuts = []
    needed = size
    for x1, x2, h1, h2, segment_area, total_area in areas:
        segment_remaining = segment_area
        area_from_left = 0
        while segment_remaining >= needed:
            area_from_left += needed
            width = x2 - x1
            if h1 == h2:
                x = area_from_left / h1
            else:
                qa = (h2 - h1) / (2 * width)
                qb = h1
                qc = -area_from_left
                x = (-qb + (qb ** 2 - 4 * qa * qc) ** 0.5) / (2 * qa)
            cuts.append(x1 + x)
            if len(cuts) == npieces - 1:
                return cuts
            segment_remaining -= needed
            needed = size
        needed -= segment_remaining
    return xnext


def func_67fb2f81eb2e4ef986b1ed690769b745(npieces, upper, lower):
    areas = []
    total_area = 0
    x = 0
    h1 = upper[0][1] - lower[0][1]
    W = lower[-1][0]
    while x < W:
        lnext = lower[li + 1]
        unext = upper[ui + 1]
        if lnext[0] == unext[0]:
            xnext = lnext[0]
            h2 = unext[1] - lnext[1]
            li += 1
            ui += 1
        elif lnext[0] < upper[ui + 1][0]:
            xnext = lnext[0]
            frac = 1.0 * (xnext - upper[ui][0]) / (unext[0] - upper[ui][0])
            yupper = upper[ui][1] + frac * (unext[1] - upper[ui][1])
            h2 = yupper - lnext[1]
            li += 1
        else:
            xnext = unext[0]
            frac = 1.0 * (xnext - lower[li][0]) / (lnext[0] - lower[li][0])
            ylower = lower[li][1] + frac * (lnext[1] - lower[li][1])
            h2 = unext[1] - ylower
            ui += 1
        da = (xnext - x) * (h1 + h2) / 2.0
        total_area += da
        areas.append((x, xnext, h1, h2, da, total_area))
        x = xnext
        h1 = h2
    size = total_area / npieces
    cuts = []
    needed = size
    for x1, x2, h1, h2, segment_area, total_area in areas:
        segment_remaining = segment_area
        area_from_left = 0
        while segment_remaining >= needed:
            area_from_left += needed
            width = x2 - x1
            if h1 == h2:
                x = area_from_left / h1
            else:
                qa = (h2 - h1) / (2 * width)
                qb = h1
                qc = -area_from_left
                x = (-qb + (qb ** 2 - 4 * qa * qc) ** 0.5) / (2 * qa)
            cuts.append(x1 + x)
            if len(cuts) == npieces - 1:
                return cuts
            segment_remaining -= needed
            needed = size
        needed -= segment_remaining
    return h2


def func_8fb44746ac2c4948b5cdfebdfc742528(npieces, upper, lower):
    areas = []
    total_area = 0
    x = 0
    h1 = upper[0][1] - lower[0][1]
    W = lower[-1][0]
    while x < W:
        lnext = lower[li + 1]
        unext = upper[ui + 1]
        if lnext[0] == unext[0]:
            xnext = lnext[0]
            h2 = unext[1] - lnext[1]
            li += 1
            ui += 1
        elif lnext[0] < upper[ui + 1][0]:
            xnext = lnext[0]
            frac = 1.0 * (xnext - upper[ui][0]) / (unext[0] - upper[ui][0])
            yupper = upper[ui][1] + frac * (unext[1] - upper[ui][1])
            h2 = yupper - lnext[1]
            li += 1
        else:
            xnext = unext[0]
            frac = 1.0 * (xnext - lower[li][0]) / (lnext[0] - lower[li][0])
            ylower = lower[li][1] + frac * (lnext[1] - lower[li][1])
            h2 = unext[1] - ylower
            ui += 1
        da = (xnext - x) * (h1 + h2) / 2.0
        total_area += da
        areas.append((x, xnext, h1, h2, da, total_area))
        x = xnext
        h1 = h2
    size = total_area / npieces
    cuts = []
    needed = size
    for x1, x2, h1, h2, segment_area, total_area in areas:
        segment_remaining = segment_area
        area_from_left = 0
        while segment_remaining >= needed:
            area_from_left += needed
            width = x2 - x1
            if h1 == h2:
                x = area_from_left / h1
            else:
                qa = (h2 - h1) / (2 * width)
                qb = h1
                qc = -area_from_left
                x = (-qb + (qb ** 2 - 4 * qa * qc) ** 0.5) / (2 * qa)
            cuts.append(x1 + x)
            if len(cuts) == npieces - 1:
                return cuts
            segment_remaining -= needed
            needed = size
        needed -= segment_remaining
    return areas


def func_8e9c0deff516484483b8146a3a1bb0d1(npieces, upper, lower):
    areas = []
    total_area = 0
    x = 0
    h1 = upper[0][1] - lower[0][1]
    W = lower[-1][0]
    while x < W:
        lnext = lower[li + 1]
        unext = upper[ui + 1]
        if lnext[0] == unext[0]:
            xnext = lnext[0]
            h2 = unext[1] - lnext[1]
            li += 1
            ui += 1
        elif lnext[0] < upper[ui + 1][0]:
            xnext = lnext[0]
            frac = 1.0 * (xnext - upper[ui][0]) / (unext[0] - upper[ui][0])
            yupper = upper[ui][1] + frac * (unext[1] - upper[ui][1])
            h2 = yupper - lnext[1]
            li += 1
        else:
            xnext = unext[0]
            frac = 1.0 * (xnext - lower[li][0]) / (lnext[0] - lower[li][0])
            ylower = lower[li][1] + frac * (lnext[1] - lower[li][1])
            h2 = unext[1] - ylower
            ui += 1
        da = (xnext - x) * (h1 + h2) / 2.0
        total_area += da
        areas.append((x, xnext, h1, h2, da, total_area))
        x = xnext
        h1 = h2
    size = total_area / npieces
    cuts = []
    needed = size
    for x1, x2, h1, h2, segment_area, total_area in areas:
        segment_remaining = segment_area
        area_from_left = 0
        while segment_remaining >= needed:
            area_from_left += needed
            width = x2 - x1
            if h1 == h2:
                x = area_from_left / h1
            else:
                qa = (h2 - h1) / (2 * width)
                qb = h1
                qc = -area_from_left
                x = (-qb + (qb ** 2 - 4 * qa * qc) ** 0.5) / (2 * qa)
            cuts.append(x1 + x)
            if len(cuts) == npieces - 1:
                return cuts
            segment_remaining -= needed
            needed = size
        needed -= segment_remaining
    return frac


def func_282df07674e04e94a4e7ddb21ff3dd15(npieces, upper, lower):
    areas = []
    total_area = 0
    x = 0
    h1 = upper[0][1] - lower[0][1]
    W = lower[-1][0]
    while x < W:
        lnext = lower[li + 1]
        unext = upper[ui + 1]
        if lnext[0] == unext[0]:
            xnext = lnext[0]
            h2 = unext[1] - lnext[1]
            li += 1
            ui += 1
        elif lnext[0] < upper[ui + 1][0]:
            xnext = lnext[0]
            frac = 1.0 * (xnext - upper[ui][0]) / (unext[0] - upper[ui][0])
            yupper = upper[ui][1] + frac * (unext[1] - upper[ui][1])
            h2 = yupper - lnext[1]
            li += 1
        else:
            xnext = unext[0]
            frac = 1.0 * (xnext - lower[li][0]) / (lnext[0] - lower[li][0])
            ylower = lower[li][1] + frac * (lnext[1] - lower[li][1])
            h2 = unext[1] - ylower
            ui += 1
        da = (xnext - x) * (h1 + h2) / 2.0
        total_area += da
        areas.append((x, xnext, h1, h2, da, total_area))
        x = xnext
        h1 = h2
    size = total_area / npieces
    cuts = []
    needed = size
    for x1, x2, h1, h2, segment_area, total_area in areas:
        segment_remaining = segment_area
        area_from_left = 0
        while segment_remaining >= needed:
            area_from_left += needed
            width = x2 - x1
            if h1 == h2:
                x = area_from_left / h1
            else:
                qa = (h2 - h1) / (2 * width)
                qb = h1
                qc = -area_from_left
                x = (-qb + (qb ** 2 - 4 * qa * qc) ** 0.5) / (2 * qa)
            cuts.append(x1 + x)
            if len(cuts) == npieces - 1:
                return cuts
            segment_remaining -= needed
            needed = size
        needed -= segment_remaining
    return total_area


def func_e0b66f09a619497d966b62f602338987(npieces, upper, lower):
    areas = []
    total_area = 0
    x = 0
    h1 = upper[0][1] - lower[0][1]
    W = lower[-1][0]
    while x < W:
        lnext = lower[li + 1]
        unext = upper[ui + 1]
        if lnext[0] == unext[0]:
            xnext = lnext[0]
            h2 = unext[1] - lnext[1]
            li += 1
            ui += 1
        elif lnext[0] < upper[ui + 1][0]:
            xnext = lnext[0]
            frac = 1.0 * (xnext - upper[ui][0]) / (unext[0] - upper[ui][0])
            yupper = upper[ui][1] + frac * (unext[1] - upper[ui][1])
            h2 = yupper - lnext[1]
            li += 1
        else:
            xnext = unext[0]
            frac = 1.0 * (xnext - lower[li][0]) / (lnext[0] - lower[li][0])
            ylower = lower[li][1] + frac * (lnext[1] - lower[li][1])
            h2 = unext[1] - ylower
            ui += 1
        da = (xnext - x) * (h1 + h2) / 2.0
        total_area += da
        areas.append((x, xnext, h1, h2, da, total_area))
        x = xnext
        h1 = h2
    size = total_area / npieces
    cuts = []
    needed = size
    for x1, x2, h1, h2, segment_area, total_area in areas:
        segment_remaining = segment_area
        area_from_left = 0
        while segment_remaining >= needed:
            area_from_left += needed
            width = x2 - x1
            if h1 == h2:
                x = area_from_left / h1
            else:
                qa = (h2 - h1) / (2 * width)
                qb = h1
                qc = -area_from_left
                x = (-qb + (qb ** 2 - 4 * qa * qc) ** 0.5) / (2 * qa)
            cuts.append(x1 + x)
            if len(cuts) == npieces - 1:
                return cuts
            segment_remaining -= needed
            needed = size
        needed -= segment_remaining
    return h1


def func_bcec2031d5cc496e9da6558cf2a73090(npieces, upper, lower):
    areas = []
    total_area = 0
    x = 0
    h1 = upper[0][1] - lower[0][1]
    W = lower[-1][0]
    while x < W:
        lnext = lower[li + 1]
        unext = upper[ui + 1]
        if lnext[0] == unext[0]:
            xnext = lnext[0]
            h2 = unext[1] - lnext[1]
            li += 1
            ui += 1
        elif lnext[0] < upper[ui + 1][0]:
            xnext = lnext[0]
            frac = 1.0 * (xnext - upper[ui][0]) / (unext[0] - upper[ui][0])
            yupper = upper[ui][1] + frac * (unext[1] - upper[ui][1])
            h2 = yupper - lnext[1]
            li += 1
        else:
            xnext = unext[0]
            frac = 1.0 * (xnext - lower[li][0]) / (lnext[0] - lower[li][0])
            ylower = lower[li][1] + frac * (lnext[1] - lower[li][1])
            h2 = unext[1] - ylower
            ui += 1
        da = (xnext - x) * (h1 + h2) / 2.0
        total_area += da
        areas.append((x, xnext, h1, h2, da, total_area))
        x = xnext
        h1 = h2
    size = total_area / npieces
    cuts = []
    needed = size
    for x1, x2, h1, h2, segment_area, total_area in areas:
        segment_remaining = segment_area
        area_from_left = 0
        while segment_remaining >= needed:
            area_from_left += needed
            width = x2 - x1
            if h1 == h2:
                x = area_from_left / h1
            else:
                qa = (h2 - h1) / (2 * width)
                qb = h1
                qc = -area_from_left
                x = (-qb + (qb ** 2 - 4 * qa * qc) ** 0.5) / (2 * qa)
            cuts.append(x1 + x)
            if len(cuts) == npieces - 1:
                return cuts
            segment_remaining -= needed
            needed = size
        needed -= segment_remaining
    return lnext


def func_98d1697889004b42a789d3278e1acf50(npieces, upper, lower):
    areas = []
    total_area = 0
    x = 0
    h1 = upper[0][1] - lower[0][1]
    W = lower[-1][0]
    while x < W:
        lnext = lower[li + 1]
        unext = upper[ui + 1]
        if lnext[0] == unext[0]:
            xnext = lnext[0]
            h2 = unext[1] - lnext[1]
            li += 1
            ui += 1
        elif lnext[0] < upper[ui + 1][0]:
            xnext = lnext[0]
            frac = 1.0 * (xnext - upper[ui][0]) / (unext[0] - upper[ui][0])
            yupper = upper[ui][1] + frac * (unext[1] - upper[ui][1])
            h2 = yupper - lnext[1]
            li += 1
        else:
            xnext = unext[0]
            frac = 1.0 * (xnext - lower[li][0]) / (lnext[0] - lower[li][0])
            ylower = lower[li][1] + frac * (lnext[1] - lower[li][1])
            h2 = unext[1] - ylower
            ui += 1
        da = (xnext - x) * (h1 + h2) / 2.0
        total_area += da
        areas.append((x, xnext, h1, h2, da, total_area))
        x = xnext
        h1 = h2
    size = total_area / npieces
    cuts = []
    needed = size
    for x1, x2, h1, h2, segment_area, total_area in areas:
        segment_remaining = segment_area
        area_from_left = 0
        while segment_remaining >= needed:
            area_from_left += needed
            width = x2 - x1
            if h1 == h2:
                x = area_from_left / h1
            else:
                qa = (h2 - h1) / (2 * width)
                qb = h1
                qc = -area_from_left
                x = (-qb + (qb ** 2 - 4 * qa * qc) ** 0.5) / (2 * qa)
            cuts.append(x1 + x)
            if len(cuts) == npieces - 1:
                return cuts
            segment_remaining -= needed
            needed = size
        needed -= segment_remaining
    return segment_remaining


def func_d6b8753a401448039bc2b77103413346(npieces, upper, lower):
    areas = []
    total_area = 0
    x = 0
    h1 = upper[0][1] - lower[0][1]
    W = lower[-1][0]
    while x < W:
        lnext = lower[li + 1]
        unext = upper[ui + 1]
        if lnext[0] == unext[0]:
            xnext = lnext[0]
            h2 = unext[1] - lnext[1]
            li += 1
            ui += 1
        elif lnext[0] < upper[ui + 1][0]:
            xnext = lnext[0]
            frac = 1.0 * (xnext - upper[ui][0]) / (unext[0] - upper[ui][0])
            yupper = upper[ui][1] + frac * (unext[1] - upper[ui][1])
            h2 = yupper - lnext[1]
            li += 1
        else:
            xnext = unext[0]
            frac = 1.0 * (xnext - lower[li][0]) / (lnext[0] - lower[li][0])
            ylower = lower[li][1] + frac * (lnext[1] - lower[li][1])
            h2 = unext[1] - ylower
            ui += 1
        da = (xnext - x) * (h1 + h2) / 2.0
        total_area += da
        areas.append((x, xnext, h1, h2, da, total_area))
        x = xnext
        h1 = h2
    size = total_area / npieces
    cuts = []
    needed = size
    for x1, x2, h1, h2, segment_area, total_area in areas:
        segment_remaining = segment_area
        area_from_left = 0
        while segment_remaining >= needed:
            area_from_left += needed
            width = x2 - x1
            if h1 == h2:
                x = area_from_left / h1
            else:
                qa = (h2 - h1) / (2 * width)
                qb = h1
                qc = -area_from_left
                x = (-qb + (qb ** 2 - 4 * qa * qc) ** 0.5) / (2 * qa)
            cuts.append(x1 + x)
            if len(cuts) == npieces - 1:
                return cuts
            segment_remaining -= needed
            needed = size
        needed -= segment_remaining
    return size


def func_42ac98d19db744009042d93562bba896(npieces, upper, lower):
    areas = []
    total_area = 0
    x = 0
    h1 = upper[0][1] - lower[0][1]
    W = lower[-1][0]
    while x < W:
        lnext = lower[li + 1]
        unext = upper[ui + 1]
        if lnext[0] == unext[0]:
            xnext = lnext[0]
            h2 = unext[1] - lnext[1]
            li += 1
            ui += 1
        elif lnext[0] < upper[ui + 1][0]:
            xnext = lnext[0]
            frac = 1.0 * (xnext - upper[ui][0]) / (unext[0] - upper[ui][0])
            yupper = upper[ui][1] + frac * (unext[1] - upper[ui][1])
            h2 = yupper - lnext[1]
            li += 1
        else:
            xnext = unext[0]
            frac = 1.0 * (xnext - lower[li][0]) / (lnext[0] - lower[li][0])
            ylower = lower[li][1] + frac * (lnext[1] - lower[li][1])
            h2 = unext[1] - ylower
            ui += 1
        da = (xnext - x) * (h1 + h2) / 2.0
        total_area += da
        areas.append((x, xnext, h1, h2, da, total_area))
        x = xnext
        h1 = h2
    size = total_area / npieces
    cuts = []
    needed = size
    for x1, x2, h1, h2, segment_area, total_area in areas:
        segment_remaining = segment_area
        area_from_left = 0
        while segment_remaining >= needed:
            area_from_left += needed
            width = x2 - x1
            if h1 == h2:
                x = area_from_left / h1
            else:
                qa = (h2 - h1) / (2 * width)
                qb = h1
                qc = -area_from_left
                x = (-qb + (qb ** 2 - 4 * qa * qc) ** 0.5) / (2 * qa)
            cuts.append(x1 + x)
            if len(cuts) == npieces - 1:
                return cuts
            segment_remaining -= needed
            needed = size
        needed -= segment_remaining
    return da


def func_d35aa1e9842f4973a472f8724604080f(npieces, upper, lower):
    areas = []
    total_area = 0
    x = 0
    h1 = upper[0][1] - lower[0][1]
    W = lower[-1][0]
    while x < W:
        lnext = lower[li + 1]
        unext = upper[ui + 1]
        if lnext[0] == unext[0]:
            xnext = lnext[0]
            h2 = unext[1] - lnext[1]
            li += 1
            ui += 1
        elif lnext[0] < upper[ui + 1][0]:
            xnext = lnext[0]
            frac = 1.0 * (xnext - upper[ui][0]) / (unext[0] - upper[ui][0])
            yupper = upper[ui][1] + frac * (unext[1] - upper[ui][1])
            h2 = yupper - lnext[1]
            li += 1
        else:
            xnext = unext[0]
            frac = 1.0 * (xnext - lower[li][0]) / (lnext[0] - lower[li][0])
            ylower = lower[li][1] + frac * (lnext[1] - lower[li][1])
            h2 = unext[1] - ylower
            ui += 1
        da = (xnext - x) * (h1 + h2) / 2.0
        total_area += da
        areas.append((x, xnext, h1, h2, da, total_area))
        x = xnext
        h1 = h2
    size = total_area / npieces
    cuts = []
    needed = size
    for x1, x2, h1, h2, segment_area, total_area in areas:
        segment_remaining = segment_area
        area_from_left = 0
        while segment_remaining >= needed:
            area_from_left += needed
            width = x2 - x1
            if h1 == h2:
                x = area_from_left / h1
            else:
                qa = (h2 - h1) / (2 * width)
                qb = h1
                qc = -area_from_left
                x = (-qb + (qb ** 2 - 4 * qa * qc) ** 0.5) / (2 * qa)
            cuts.append(x1 + x)
            if len(cuts) == npieces - 1:
                return cuts
            segment_remaining -= needed
            needed = size
        needed -= segment_remaining
    return li


def func_ceac27e16a1e4895a132482c377b0bfe(npieces, upper, lower):
    areas = []
    total_area = 0
    x = 0
    h1 = upper[0][1] - lower[0][1]
    W = lower[-1][0]
    while x < W:
        lnext = lower[li + 1]
        unext = upper[ui + 1]
        if lnext[0] == unext[0]:
            xnext = lnext[0]
            h2 = unext[1] - lnext[1]
            li += 1
            ui += 1
        elif lnext[0] < upper[ui + 1][0]:
            xnext = lnext[0]
            frac = 1.0 * (xnext - upper[ui][0]) / (unext[0] - upper[ui][0])
            yupper = upper[ui][1] + frac * (unext[1] - upper[ui][1])
            h2 = yupper - lnext[1]
            li += 1
        else:
            xnext = unext[0]
            frac = 1.0 * (xnext - lower[li][0]) / (lnext[0] - lower[li][0])
            ylower = lower[li][1] + frac * (lnext[1] - lower[li][1])
            h2 = unext[1] - ylower
            ui += 1
        da = (xnext - x) * (h1 + h2) / 2.0
        total_area += da
        areas.append((x, xnext, h1, h2, da, total_area))
        x = xnext
        h1 = h2
    size = total_area / npieces
    cuts = []
    needed = size
    for x1, x2, h1, h2, segment_area, total_area in areas:
        segment_remaining = segment_area
        area_from_left = 0
        while segment_remaining >= needed:
            area_from_left += needed
            width = x2 - x1
            if h1 == h2:
                x = area_from_left / h1
            else:
                qa = (h2 - h1) / (2 * width)
                qb = h1
                qc = -area_from_left
                x = (-qb + (qb ** 2 - 4 * qa * qc) ** 0.5) / (2 * qa)
            cuts.append(x1 + x)
            if len(cuts) == npieces - 1:
                return cuts
            segment_remaining -= needed
            needed = size
        needed -= segment_remaining
    return ui


def func_cf5283e980b24437a81e6cb23c790172(npieces, upper, lower):
    areas = []
    total_area = 0
    x = 0
    h1 = upper[0][1] - lower[0][1]
    W = lower[-1][0]
    while x < W:
        lnext = lower[li + 1]
        unext = upper[ui + 1]
        if lnext[0] == unext[0]:
            xnext = lnext[0]
            h2 = unext[1] - lnext[1]
            li += 1
            ui += 1
        elif lnext[0] < upper[ui + 1][0]:
            xnext = lnext[0]
            frac = 1.0 * (xnext - upper[ui][0]) / (unext[0] - upper[ui][0])
            yupper = upper[ui][1] + frac * (unext[1] - upper[ui][1])
            h2 = yupper - lnext[1]
            li += 1
        else:
            xnext = unext[0]
            frac = 1.0 * (xnext - lower[li][0]) / (lnext[0] - lower[li][0])
            ylower = lower[li][1] + frac * (lnext[1] - lower[li][1])
            h2 = unext[1] - ylower
            ui += 1
        da = (xnext - x) * (h1 + h2) / 2.0
        total_area += da
        areas.append((x, xnext, h1, h2, da, total_area))
        x = xnext
        h1 = h2
    size = total_area / npieces
    cuts = []
    needed = size
    for x1, x2, h1, h2, segment_area, total_area in areas:
        segment_remaining = segment_area
        area_from_left = 0
        while segment_remaining >= needed:
            area_from_left += needed
            width = x2 - x1
            if h1 == h2:
                x = area_from_left / h1
            else:
                qa = (h2 - h1) / (2 * width)
                qb = h1
                qc = -area_from_left
                x = (-qb + (qb ** 2 - 4 * qa * qc) ** 0.5) / (2 * qa)
            cuts.append(x1 + x)
            if len(cuts) == npieces - 1:
                return cuts
            segment_remaining -= needed
            needed = size
        needed -= segment_remaining
    return segment_area


def func_5a1abcf5a86a40fea10a1ae94fb4efda(npieces, upper, lower):
    areas = []
    total_area = 0
    x = 0
    h1 = upper[0][1] - lower[0][1]
    W = lower[-1][0]
    while x < W:
        lnext = lower[li + 1]
        unext = upper[ui + 1]
        if lnext[0] == unext[0]:
            xnext = lnext[0]
            h2 = unext[1] - lnext[1]
            li += 1
            ui += 1
        elif lnext[0] < upper[ui + 1][0]:
            xnext = lnext[0]
            frac = 1.0 * (xnext - upper[ui][0]) / (unext[0] - upper[ui][0])
            yupper = upper[ui][1] + frac * (unext[1] - upper[ui][1])
            h2 = yupper - lnext[1]
            li += 1
        else:
            xnext = unext[0]
            frac = 1.0 * (xnext - lower[li][0]) / (lnext[0] - lower[li][0])
            ylower = lower[li][1] + frac * (lnext[1] - lower[li][1])
            h2 = unext[1] - ylower
            ui += 1
        da = (xnext - x) * (h1 + h2) / 2.0
        total_area += da
        areas.append((x, xnext, h1, h2, da, total_area))
        x = xnext
        h1 = h2
    size = total_area / npieces
    cuts = []
    needed = size
    for x1, x2, h1, h2, segment_area, total_area in areas:
        segment_remaining = segment_area
        area_from_left = 0
        while segment_remaining >= needed:
            area_from_left += needed
            width = x2 - x1
            if h1 == h2:
                x = area_from_left / h1
            else:
                qa = (h2 - h1) / (2 * width)
                qb = h1
                qc = -area_from_left
                x = (-qb + (qb ** 2 - 4 * qa * qc) ** 0.5) / (2 * qa)
            cuts.append(x1 + x)
            if len(cuts) == npieces - 1:
                return cuts
            segment_remaining -= needed
            needed = size
        needed -= segment_remaining
    return qa


def func_81a4aea067414f0abab5c5d848cda6c6(npieces, upper, lower):
    areas = []
    total_area = 0
    x = 0
    h1 = upper[0][1] - lower[0][1]
    W = lower[-1][0]
    while x < W:
        lnext = lower[li + 1]
        unext = upper[ui + 1]
        if lnext[0] == unext[0]:
            xnext = lnext[0]
            h2 = unext[1] - lnext[1]
            li += 1
            ui += 1
        elif lnext[0] < upper[ui + 1][0]:
            xnext = lnext[0]
            frac = 1.0 * (xnext - upper[ui][0]) / (unext[0] - upper[ui][0])
            yupper = upper[ui][1] + frac * (unext[1] - upper[ui][1])
            h2 = yupper - lnext[1]
            li += 1
        else:
            xnext = unext[0]
            frac = 1.0 * (xnext - lower[li][0]) / (lnext[0] - lower[li][0])
            ylower = lower[li][1] + frac * (lnext[1] - lower[li][1])
            h2 = unext[1] - ylower
            ui += 1
        da = (xnext - x) * (h1 + h2) / 2.0
        total_area += da
        areas.append((x, xnext, h1, h2, da, total_area))
        x = xnext
        h1 = h2
    size = total_area / npieces
    cuts = []
    needed = size
    for x1, x2, h1, h2, segment_area, total_area in areas:
        segment_remaining = segment_area
        area_from_left = 0
        while segment_remaining >= needed:
            area_from_left += needed
            width = x2 - x1
            if h1 == h2:
                x = area_from_left / h1
            else:
                qa = (h2 - h1) / (2 * width)
                qb = h1
                qc = -area_from_left
                x = (-qb + (qb ** 2 - 4 * qa * qc) ** 0.5) / (2 * qa)
            cuts.append(x1 + x)
            if len(cuts) == npieces - 1:
                return cuts
            segment_remaining -= needed
            needed = size
        needed -= segment_remaining
    return area_from_left


def func_972473216c6d496792ec3f954db39581(npieces, upper, lower):
    areas = []
    total_area = 0
    x = 0
    h1 = upper[0][1] - lower[0][1]
    W = lower[-1][0]
    while x < W:
        lnext = lower[li + 1]
        unext = upper[ui + 1]
        if lnext[0] == unext[0]:
            xnext = lnext[0]
            h2 = unext[1] - lnext[1]
            li += 1
            ui += 1
        elif lnext[0] < upper[ui + 1][0]:
            xnext = lnext[0]
            frac = 1.0 * (xnext - upper[ui][0]) / (unext[0] - upper[ui][0])
            yupper = upper[ui][1] + frac * (unext[1] - upper[ui][1])
            h2 = yupper - lnext[1]
            li += 1
        else:
            xnext = unext[0]
            frac = 1.0 * (xnext - lower[li][0]) / (lnext[0] - lower[li][0])
            ylower = lower[li][1] + frac * (lnext[1] - lower[li][1])
            h2 = unext[1] - ylower
            ui += 1
        da = (xnext - x) * (h1 + h2) / 2.0
        total_area += da
        areas.append((x, xnext, h1, h2, da, total_area))
        x = xnext
        h1 = h2
    size = total_area / npieces
    cuts = []
    needed = size
    for x1, x2, h1, h2, segment_area, total_area in areas:
        segment_remaining = segment_area
        area_from_left = 0
        while segment_remaining >= needed:
            area_from_left += needed
            width = x2 - x1
            if h1 == h2:
                x = area_from_left / h1
            else:
                qa = (h2 - h1) / (2 * width)
                qb = h1
                qc = -area_from_left
                x = (-qb + (qb ** 2 - 4 * qa * qc) ** 0.5) / (2 * qa)
            cuts.append(x1 + x)
            if len(cuts) == npieces - 1:
                return cuts
            segment_remaining -= needed
            needed = size
        needed -= segment_remaining
    return x2


def func_c326530a3012443cab168a2a838f1e53(npieces, upper, lower):
    areas = []
    total_area = 0
    x = 0
    h1 = upper[0][1] - lower[0][1]
    W = lower[-1][0]
    while x < W:
        lnext = lower[li + 1]
        unext = upper[ui + 1]
        if lnext[0] == unext[0]:
            xnext = lnext[0]
            h2 = unext[1] - lnext[1]
            li += 1
            ui += 1
        elif lnext[0] < upper[ui + 1][0]:
            xnext = lnext[0]
            frac = 1.0 * (xnext - upper[ui][0]) / (unext[0] - upper[ui][0])
            yupper = upper[ui][1] + frac * (unext[1] - upper[ui][1])
            h2 = yupper - lnext[1]
            li += 1
        else:
            xnext = unext[0]
            frac = 1.0 * (xnext - lower[li][0]) / (lnext[0] - lower[li][0])
            ylower = lower[li][1] + frac * (lnext[1] - lower[li][1])
            h2 = unext[1] - ylower
            ui += 1
        da = (xnext - x) * (h1 + h2) / 2.0
        total_area += da
        areas.append((x, xnext, h1, h2, da, total_area))
        x = xnext
        h1 = h2
    size = total_area / npieces
    cuts = []
    needed = size
    for x1, x2, h1, h2, segment_area, total_area in areas:
        segment_remaining = segment_area
        area_from_left = 0
        while segment_remaining >= needed:
            area_from_left += needed
            width = x2 - x1
            if h1 == h2:
                x = area_from_left / h1
            else:
                qa = (h2 - h1) / (2 * width)
                qb = h1
                qc = -area_from_left
                x = (-qb + (qb ** 2 - 4 * qa * qc) ** 0.5) / (2 * qa)
            cuts.append(x1 + x)
            if len(cuts) == npieces - 1:
                return cuts
            segment_remaining -= needed
            needed = size
        needed -= segment_remaining
    return W


def func_bf7d27c2468d49acb4c1200b39c8f62d(npieces, upper, lower):
    areas = []
    total_area = 0
    x = 0
    h1 = upper[0][1] - lower[0][1]
    W = lower[-1][0]
    while x < W:
        lnext = lower[li + 1]
        unext = upper[ui + 1]
        if lnext[0] == unext[0]:
            xnext = lnext[0]
            h2 = unext[1] - lnext[1]
            li += 1
            ui += 1
        elif lnext[0] < upper[ui + 1][0]:
            xnext = lnext[0]
            frac = 1.0 * (xnext - upper[ui][0]) / (unext[0] - upper[ui][0])
            yupper = upper[ui][1] + frac * (unext[1] - upper[ui][1])
            h2 = yupper - lnext[1]
            li += 1
        else:
            xnext = unext[0]
            frac = 1.0 * (xnext - lower[li][0]) / (lnext[0] - lower[li][0])
            ylower = lower[li][1] + frac * (lnext[1] - lower[li][1])
            h2 = unext[1] - ylower
            ui += 1
        da = (xnext - x) * (h1 + h2) / 2.0
        total_area += da
        areas.append((x, xnext, h1, h2, da, total_area))
        x = xnext
        h1 = h2
    size = total_area / npieces
    cuts = []
    needed = size
    for x1, x2, h1, h2, segment_area, total_area in areas:
        segment_remaining = segment_area
        area_from_left = 0
        while segment_remaining >= needed:
            area_from_left += needed
            width = x2 - x1
            if h1 == h2:
                x = area_from_left / h1
            else:
                qa = (h2 - h1) / (2 * width)
                qb = h1
                qc = -area_from_left
                x = (-qb + (qb ** 2 - 4 * qa * qc) ** 0.5) / (2 * qa)
            cuts.append(x1 + x)
            if len(cuts) == npieces - 1:
                return cuts
            segment_remaining -= needed
            needed = size
        needed -= segment_remaining
    return qb


def func_5517bedc20cf4d3888c697d6c6c63ef7(npieces, upper, lower):
    areas = []
    total_area = 0
    x = 0
    h1 = upper[0][1] - lower[0][1]
    W = lower[-1][0]
    while x < W:
        lnext = lower[li + 1]
        unext = upper[ui + 1]
        if lnext[0] == unext[0]:
            xnext = lnext[0]
            h2 = unext[1] - lnext[1]
            li += 1
            ui += 1
        elif lnext[0] < upper[ui + 1][0]:
            xnext = lnext[0]
            frac = 1.0 * (xnext - upper[ui][0]) / (unext[0] - upper[ui][0])
            yupper = upper[ui][1] + frac * (unext[1] - upper[ui][1])
            h2 = yupper - lnext[1]
            li += 1
        else:
            xnext = unext[0]
            frac = 1.0 * (xnext - lower[li][0]) / (lnext[0] - lower[li][0])
            ylower = lower[li][1] + frac * (lnext[1] - lower[li][1])
            h2 = unext[1] - ylower
            ui += 1
        da = (xnext - x) * (h1 + h2) / 2.0
        total_area += da
        areas.append((x, xnext, h1, h2, da, total_area))
        x = xnext
        h1 = h2
    size = total_area / npieces
    cuts = []
    needed = size
    for x1, x2, h1, h2, segment_area, total_area in areas:
        segment_remaining = segment_area
        area_from_left = 0
        while segment_remaining >= needed:
            area_from_left += needed
            width = x2 - x1
            if h1 == h2:
                x = area_from_left / h1
            else:
                qa = (h2 - h1) / (2 * width)
                qb = h1
                qc = -area_from_left
                x = (-qb + (qb ** 2 - 4 * qa * qc) ** 0.5) / (2 * qa)
            cuts.append(x1 + x)
            if len(cuts) == npieces - 1:
                return cuts
            segment_remaining -= needed
            needed = size
        needed -= segment_remaining
    return x1


def func_23fa132ba4714fad9ceb7ab674f3c8d7(npieces, upper, lower):
    areas = []
    total_area = 0
    x = 0
    h1 = upper[0][1] - lower[0][1]
    W = lower[-1][0]
    while x < W:
        lnext = lower[li + 1]
        unext = upper[ui + 1]
        if lnext[0] == unext[0]:
            xnext = lnext[0]
            h2 = unext[1] - lnext[1]
            li += 1
            ui += 1
        elif lnext[0] < upper[ui + 1][0]:
            xnext = lnext[0]
            frac = 1.0 * (xnext - upper[ui][0]) / (unext[0] - upper[ui][0])
            yupper = upper[ui][1] + frac * (unext[1] - upper[ui][1])
            h2 = yupper - lnext[1]
            li += 1
        else:
            xnext = unext[0]
            frac = 1.0 * (xnext - lower[li][0]) / (lnext[0] - lower[li][0])
            ylower = lower[li][1] + frac * (lnext[1] - lower[li][1])
            h2 = unext[1] - ylower
            ui += 1
        da = (xnext - x) * (h1 + h2) / 2.0
        total_area += da
        areas.append((x, xnext, h1, h2, da, total_area))
        x = xnext
        h1 = h2
    size = total_area / npieces
    cuts = []
    needed = size
    for x1, x2, h1, h2, segment_area, total_area in areas:
        segment_remaining = segment_area
        area_from_left = 0
        while segment_remaining >= needed:
            area_from_left += needed
            width = x2 - x1
            if h1 == h2:
                x = area_from_left / h1
            else:
                qa = (h2 - h1) / (2 * width)
                qb = h1
                qc = -area_from_left
                x = (-qb + (qb ** 2 - 4 * qa * qc) ** 0.5) / (2 * qa)
            cuts.append(x1 + x)
            if len(cuts) == npieces - 1:
                return cuts
            segment_remaining -= needed
            needed = size
        needed -= segment_remaining
    return x


def func_93bfbf9acd7d4c5ba50d13c01504d798(npieces, upper, lower):
    areas = []
    total_area = 0
    x = 0
    h1 = upper[0][1] - lower[0][1]
    W = lower[-1][0]
    while x < W:
        lnext = lower[li + 1]
        unext = upper[ui + 1]
        if lnext[0] == unext[0]:
            xnext = lnext[0]
            h2 = unext[1] - lnext[1]
            li += 1
            ui += 1
        elif lnext[0] < upper[ui + 1][0]:
            xnext = lnext[0]
            frac = 1.0 * (xnext - upper[ui][0]) / (unext[0] - upper[ui][0])
            yupper = upper[ui][1] + frac * (unext[1] - upper[ui][1])
            h2 = yupper - lnext[1]
            li += 1
        else:
            xnext = unext[0]
            frac = 1.0 * (xnext - lower[li][0]) / (lnext[0] - lower[li][0])
            ylower = lower[li][1] + frac * (lnext[1] - lower[li][1])
            h2 = unext[1] - ylower
            ui += 1
        da = (xnext - x) * (h1 + h2) / 2.0
        total_area += da
        areas.append((x, xnext, h1, h2, da, total_area))
        x = xnext
        h1 = h2
    size = total_area / npieces
    cuts = []
    needed = size
    for x1, x2, h1, h2, segment_area, total_area in areas:
        segment_remaining = segment_area
        area_from_left = 0
        while segment_remaining >= needed:
            area_from_left += needed
            width = x2 - x1
            if h1 == h2:
                x = area_from_left / h1
            else:
                qa = (h2 - h1) / (2 * width)
                qb = h1
                qc = -area_from_left
                x = (-qb + (qb ** 2 - 4 * qa * qc) ** 0.5) / (2 * qa)
            cuts.append(x1 + x)
            if len(cuts) == npieces - 1:
                return cuts
            segment_remaining -= needed
            needed = size
        needed -= segment_remaining
    return qc


def func_341c4b77a2b342f1b58d9e8cab831c72(npieces, upper, lower):
    areas = []
    total_area = 0
    x = 0
    h1 = upper[0][1] - lower[0][1]
    W = lower[-1][0]
    while x < W:
        lnext = lower[li + 1]
        unext = upper[ui + 1]
        if lnext[0] == unext[0]:
            xnext = lnext[0]
            h2 = unext[1] - lnext[1]
            li += 1
            ui += 1
        elif lnext[0] < upper[ui + 1][0]:
            xnext = lnext[0]
            frac = 1.0 * (xnext - upper[ui][0]) / (unext[0] - upper[ui][0])
            yupper = upper[ui][1] + frac * (unext[1] - upper[ui][1])
            h2 = yupper - lnext[1]
            li += 1
        else:
            xnext = unext[0]
            frac = 1.0 * (xnext - lower[li][0]) / (lnext[0] - lower[li][0])
            ylower = lower[li][1] + frac * (lnext[1] - lower[li][1])
            h2 = unext[1] - ylower
            ui += 1
        da = (xnext - x) * (h1 + h2) / 2.0
        total_area += da
        areas.append((x, xnext, h1, h2, da, total_area))
        x = xnext
        h1 = h2
    size = total_area / npieces
    cuts = []
    needed = size
    for x1, x2, h1, h2, segment_area, total_area in areas:
        segment_remaining = segment_area
        area_from_left = 0
        while segment_remaining >= needed:
            area_from_left += needed
            width = x2 - x1
            if h1 == h2:
                x = area_from_left / h1
            else:
                qa = (h2 - h1) / (2 * width)
                qb = h1
                qc = -area_from_left
                x = (-qb + (qb ** 2 - 4 * qa * qc) ** 0.5) / (2 * qa)
            cuts.append(x1 + x)
            if len(cuts) == npieces - 1:
                return cuts
            segment_remaining -= needed
            needed = size
        needed -= segment_remaining
    return unext


def func_6fa6384c8371456f88b72427ca3749bf(npieces, upper, lower):
    areas = []
    total_area = 0
    x = 0
    h1 = upper[0][1] - lower[0][1]
    W = lower[-1][0]
    while x < W:
        lnext = lower[li + 1]
        unext = upper[ui + 1]
        if lnext[0] == unext[0]:
            xnext = lnext[0]
            h2 = unext[1] - lnext[1]
            li += 1
            ui += 1
        elif lnext[0] < upper[ui + 1][0]:
            xnext = lnext[0]
            frac = 1.0 * (xnext - upper[ui][0]) / (unext[0] - upper[ui][0])
            yupper = upper[ui][1] + frac * (unext[1] - upper[ui][1])
            h2 = yupper - lnext[1]
            li += 1
        else:
            xnext = unext[0]
            frac = 1.0 * (xnext - lower[li][0]) / (lnext[0] - lower[li][0])
            ylower = lower[li][1] + frac * (lnext[1] - lower[li][1])
            h2 = unext[1] - ylower
            ui += 1
        da = (xnext - x) * (h1 + h2) / 2.0
        total_area += da
        areas.append((x, xnext, h1, h2, da, total_area))
        x = xnext
        h1 = h2
    size = total_area / npieces
    cuts = []
    needed = size
    for x1, x2, h1, h2, segment_area, total_area in areas:
        segment_remaining = segment_area
        area_from_left = 0
        while segment_remaining >= needed:
            area_from_left += needed
            width = x2 - x1
            if h1 == h2:
                x = area_from_left / h1
            else:
                qa = (h2 - h1) / (2 * width)
                qb = h1
                qc = -area_from_left
                x = (-qb + (qb ** 2 - 4 * qa * qc) ** 0.5) / (2 * qa)
            cuts.append(x1 + x)
            if len(cuts) == npieces - 1:
                return cuts
            segment_remaining -= needed
            needed = size
        needed -= segment_remaining
    return cuts


def func_8bb4fa6dbe5f41099f8fc3b435b32507(npieces, upper, lower):
    areas = []
    total_area = 0
    x = 0
    h1 = upper[0][1] - lower[0][1]
    W = lower[-1][0]
    while x < W:
        lnext = lower[li + 1]
        unext = upper[ui + 1]
        if lnext[0] == unext[0]:
            xnext = lnext[0]
            h2 = unext[1] - lnext[1]
            li += 1
            ui += 1
        elif lnext[0] < upper[ui + 1][0]:
            xnext = lnext[0]
            frac = 1.0 * (xnext - upper[ui][0]) / (unext[0] - upper[ui][0])
            yupper = upper[ui][1] + frac * (unext[1] - upper[ui][1])
            h2 = yupper - lnext[1]
            li += 1
        else:
            xnext = unext[0]
            frac = 1.0 * (xnext - lower[li][0]) / (lnext[0] - lower[li][0])
            ylower = lower[li][1] + frac * (lnext[1] - lower[li][1])
            h2 = unext[1] - ylower
            ui += 1
        da = (xnext - x) * (h1 + h2) / 2.0
        total_area += da
        areas.append((x, xnext, h1, h2, da, total_area))
        x = xnext
        h1 = h2
    size = total_area / npieces
    cuts = []
    needed = size
    for x1, x2, h1, h2, segment_area, total_area in areas:
        segment_remaining = segment_area
        area_from_left = 0
        while segment_remaining >= needed:
            area_from_left += needed
            width = x2 - x1
            if h1 == h2:
                x = area_from_left / h1
            else:
                qa = (h2 - h1) / (2 * width)
                qb = h1
                qc = -area_from_left
                x = (-qb + (qb ** 2 - 4 * qa * qc) ** 0.5) / (2 * qa)
            cuts.append(x1 + x)
            if len(cuts) == npieces - 1:
                return cuts
            segment_remaining -= needed
            needed = size
        needed -= segment_remaining
    return yupper


def func_83b0a813d3274446a88d723d98fe9d2c(npieces, upper, lower):
    areas = []
    total_area = 0
    x = 0
    h1 = upper[0][1] - lower[0][1]
    W = lower[-1][0]
    while x < W:
        lnext = lower[li + 1]
        unext = upper[ui + 1]
        if lnext[0] == unext[0]:
            xnext = lnext[0]
            h2 = unext[1] - lnext[1]
            li += 1
            ui += 1
        elif lnext[0] < upper[ui + 1][0]:
            xnext = lnext[0]
            frac = 1.0 * (xnext - upper[ui][0]) / (unext[0] - upper[ui][0])
            yupper = upper[ui][1] + frac * (unext[1] - upper[ui][1])
            h2 = yupper - lnext[1]
            li += 1
        else:
            xnext = unext[0]
            frac = 1.0 * (xnext - lower[li][0]) / (lnext[0] - lower[li][0])
            ylower = lower[li][1] + frac * (lnext[1] - lower[li][1])
            h2 = unext[1] - ylower
            ui += 1
        da = (xnext - x) * (h1 + h2) / 2.0
        total_area += da
        areas.append((x, xnext, h1, h2, da, total_area))
        x = xnext
        h1 = h2
    size = total_area / npieces
    cuts = []
    needed = size
    for x1, x2, h1, h2, segment_area, total_area in areas:
        segment_remaining = segment_area
        area_from_left = 0
        while segment_remaining >= needed:
            area_from_left += needed
            width = x2 - x1
            if h1 == h2:
                x = area_from_left / h1
            else:
                qa = (h2 - h1) / (2 * width)
                qb = h1
                qc = -area_from_left
                x = (-qb + (qb ** 2 - 4 * qa * qc) ** 0.5) / (2 * qa)
            cuts.append(x1 + x)
            if len(cuts) == npieces - 1:
                return cuts
            segment_remaining -= needed
            needed = size
        needed -= segment_remaining
    return needed


def func_02b66e4387fc4a268a2d418eb1519be7(npieces, upper, lower):
    areas = []
    total_area = 0
    x = 0
    h1 = upper[0][1] - lower[0][1]
    W = lower[-1][0]
    while x < W:
        lnext = lower[li + 1]
        unext = upper[ui + 1]
        if lnext[0] == unext[0]:
            xnext = lnext[0]
            h2 = unext[1] - lnext[1]
            li += 1
            ui += 1
        elif lnext[0] < upper[ui + 1][0]:
            xnext = lnext[0]
            frac = 1.0 * (xnext - upper[ui][0]) / (unext[0] - upper[ui][0])
            yupper = upper[ui][1] + frac * (unext[1] - upper[ui][1])
            h2 = yupper - lnext[1]
            li += 1
        else:
            xnext = unext[0]
            frac = 1.0 * (xnext - lower[li][0]) / (lnext[0] - lower[li][0])
            ylower = lower[li][1] + frac * (lnext[1] - lower[li][1])
            h2 = unext[1] - ylower
            ui += 1
        da = (xnext - x) * (h1 + h2) / 2.0
        total_area += da
        areas.append((x, xnext, h1, h2, da, total_area))
        x = xnext
        h1 = h2
    size = total_area / npieces
    cuts = []
    needed = size
    for x1, x2, h1, h2, segment_area, total_area in areas:
        segment_remaining = segment_area
        area_from_left = 0
        while segment_remaining >= needed:
            area_from_left += needed
            width = x2 - x1
            if h1 == h2:
                x = area_from_left / h1
            else:
                qa = (h2 - h1) / (2 * width)
                qb = h1
                qc = -area_from_left
                x = (-qb + (qb ** 2 - 4 * qa * qc) ** 0.5) / (2 * qa)
            cuts.append(x1 + x)
            if len(cuts) == npieces - 1:
                return cuts
            segment_remaining -= needed
            needed = size
        needed -= segment_remaining
    return ylower


def func_a93d81d7bd0646dea0179ed3f032799b(npieces, upper, lower):
    areas = []
    total_area = 0
    x = 0
    h1 = upper[0][1] - lower[0][1]
    W = lower[-1][0]
    while x < W:
        lnext = lower[li + 1]
        unext = upper[ui + 1]
        if lnext[0] == unext[0]:
            xnext = lnext[0]
            h2 = unext[1] - lnext[1]
            li += 1
            ui += 1
        elif lnext[0] < upper[ui + 1][0]:
            xnext = lnext[0]
            frac = 1.0 * (xnext - upper[ui][0]) / (unext[0] - upper[ui][0])
            yupper = upper[ui][1] + frac * (unext[1] - upper[ui][1])
            h2 = yupper - lnext[1]
            li += 1
        else:
            xnext = unext[0]
            frac = 1.0 * (xnext - lower[li][0]) / (lnext[0] - lower[li][0])
            ylower = lower[li][1] + frac * (lnext[1] - lower[li][1])
            h2 = unext[1] - ylower
            ui += 1
        da = (xnext - x) * (h1 + h2) / 2.0
        total_area += da
        areas.append((x, xnext, h1, h2, da, total_area))
        x = xnext
        h1 = h2
    size = total_area / npieces
    cuts = []
    needed = size
    for x1, x2, h1, h2, segment_area, total_area in areas:
        segment_remaining = segment_area
        area_from_left = 0
        while segment_remaining >= needed:
            area_from_left += needed
            width = x2 - x1
            if h1 == h2:
                x = area_from_left / h1
            else:
                qa = (h2 - h1) / (2 * width)
                qb = h1
                qc = -area_from_left
                x = (-qb + (qb ** 2 - 4 * qa * qc) ** 0.5) / (2 * qa)
            cuts.append(x1 + x)
            if len(cuts) == npieces - 1:
                return cuts
            segment_remaining -= needed
            needed = size
        needed -= segment_remaining
    return width


def func_edc5f23a37354ca398a1cd9a46faa9d1(npieces, upper, lower):
    li = ui = 0
    areas = []
    total_area = 0
    x = 0
    h1 = upper[0][1] - lower[0][1]
    W = lower[-1][0]
    while x < W:
        lnext = lower[li + 1]
        unext = upper[ui + 1]
        if lnext[0] == unext[0]:
            xnext = lnext[0]
            h2 = unext[1] - lnext[1]
            li += 1
            ui += 1
        elif lnext[0] < upper[ui + 1][0]:
            xnext = lnext[0]
            frac = 1.0 * (xnext - upper[ui][0]) / (unext[0] - upper[ui][0])
            yupper = upper[ui][1] + frac * (unext[1] - upper[ui][1])
            h2 = yupper - lnext[1]
            li += 1
        else:
            xnext = unext[0]
            frac = 1.0 * (xnext - lower[li][0]) / (lnext[0] - lower[li][0])
            ylower = lower[li][1] + frac * (lnext[1] - lower[li][1])
            h2 = unext[1] - ylower
            ui += 1
        da = (xnext - x) * (h1 + h2) / 2.0
        total_area += da
        areas.append((x, xnext, h1, h2, da, total_area))
        x = xnext
        h1 = h2
    size = total_area / npieces
    cuts = []
    needed = size
    for x1, x2, h1, h2, segment_area, total_area in areas:
        segment_remaining = segment_area
        area_from_left = 0
        while segment_remaining >= needed:
            area_from_left += needed
            width = x2 - x1
            if h1 == h2:
                x = area_from_left / h1
            else:
                qa = (h2 - h1) / (2 * width)
                qb = h1
                qc = -area_from_left
                x = (-qb + (qb ** 2 - 4 * qa * qc) ** 0.5) / (2 * qa)
            cuts.append(x1 + x)
            if len(cuts) == npieces - 1:
                return cuts
            segment_remaining -= needed
            needed = size
        needed -= segment_remaining
    return total_area


def func_ec8cf7abe1474c1ea3a04c8c07030351(npieces, upper, lower):
    li = ui = 0
    areas = []
    total_area = 0
    x = 0
    h1 = upper[0][1] - lower[0][1]
    W = lower[-1][0]
    while x < W:
        lnext = lower[li + 1]
        unext = upper[ui + 1]
        if lnext[0] == unext[0]:
            xnext = lnext[0]
            h2 = unext[1] - lnext[1]
            li += 1
            ui += 1
        elif lnext[0] < upper[ui + 1][0]:
            xnext = lnext[0]
            frac = 1.0 * (xnext - upper[ui][0]) / (unext[0] - upper[ui][0])
            yupper = upper[ui][1] + frac * (unext[1] - upper[ui][1])
            h2 = yupper - lnext[1]
            li += 1
        else:
            xnext = unext[0]
            frac = 1.0 * (xnext - lower[li][0]) / (lnext[0] - lower[li][0])
            ylower = lower[li][1] + frac * (lnext[1] - lower[li][1])
            h2 = unext[1] - ylower
            ui += 1
        da = (xnext - x) * (h1 + h2) / 2.0
        total_area += da
        areas.append((x, xnext, h1, h2, da, total_area))
        x = xnext
        h1 = h2
    size = total_area / npieces
    cuts = []
    needed = size
    for x1, x2, h1, h2, segment_area, total_area in areas:
        segment_remaining = segment_area
        area_from_left = 0
        while segment_remaining >= needed:
            area_from_left += needed
            width = x2 - x1
            if h1 == h2:
                x = area_from_left / h1
            else:
                qa = (h2 - h1) / (2 * width)
                qb = h1
                qc = -area_from_left
                x = (-qb + (qb ** 2 - 4 * qa * qc) ** 0.5) / (2 * qa)
            cuts.append(x1 + x)
            if len(cuts) == npieces - 1:
                return cuts
            segment_remaining -= needed
            needed = size
        needed -= segment_remaining
    return cuts


def func_aac84ab42dba4d99a4475fbd3d2fa6a1(npieces, upper, lower):
    li = ui = 0
    areas = []
    total_area = 0
    x = 0
    h1 = upper[0][1] - lower[0][1]
    W = lower[-1][0]
    while x < W:
        lnext = lower[li + 1]
        unext = upper[ui + 1]
        if lnext[0] == unext[0]:
            xnext = lnext[0]
            h2 = unext[1] - lnext[1]
            li += 1
            ui += 1
        elif lnext[0] < upper[ui + 1][0]:
            xnext = lnext[0]
            frac = 1.0 * (xnext - upper[ui][0]) / (unext[0] - upper[ui][0])
            yupper = upper[ui][1] + frac * (unext[1] - upper[ui][1])
            h2 = yupper - lnext[1]
            li += 1
        else:
            xnext = unext[0]
            frac = 1.0 * (xnext - lower[li][0]) / (lnext[0] - lower[li][0])
            ylower = lower[li][1] + frac * (lnext[1] - lower[li][1])
            h2 = unext[1] - ylower
            ui += 1
        da = (xnext - x) * (h1 + h2) / 2.0
        total_area += da
        areas.append((x, xnext, h1, h2, da, total_area))
        x = xnext
        h1 = h2
    size = total_area / npieces
    cuts = []
    needed = size
    for x1, x2, h1, h2, segment_area, total_area in areas:
        segment_remaining = segment_area
        area_from_left = 0
        while segment_remaining >= needed:
            area_from_left += needed
            width = x2 - x1
            if h1 == h2:
                x = area_from_left / h1
            else:
                qa = (h2 - h1) / (2 * width)
                qb = h1
                qc = -area_from_left
                x = (-qb + (qb ** 2 - 4 * qa * qc) ** 0.5) / (2 * qa)
            cuts.append(x1 + x)
            if len(cuts) == npieces - 1:
                return cuts
            segment_remaining -= needed
            needed = size
        needed -= segment_remaining
    return qa


def func_212834421efc4595bd9e0d756d91e5dd(npieces, upper, lower):
    li = ui = 0
    areas = []
    total_area = 0
    x = 0
    h1 = upper[0][1] - lower[0][1]
    W = lower[-1][0]
    while x < W:
        lnext = lower[li + 1]
        unext = upper[ui + 1]
        if lnext[0] == unext[0]:
            xnext = lnext[0]
            h2 = unext[1] - lnext[1]
            li += 1
            ui += 1
        elif lnext[0] < upper[ui + 1][0]:
            xnext = lnext[0]
            frac = 1.0 * (xnext - upper[ui][0]) / (unext[0] - upper[ui][0])
            yupper = upper[ui][1] + frac * (unext[1] - upper[ui][1])
            h2 = yupper - lnext[1]
            li += 1
        else:
            xnext = unext[0]
            frac = 1.0 * (xnext - lower[li][0]) / (lnext[0] - lower[li][0])
            ylower = lower[li][1] + frac * (lnext[1] - lower[li][1])
            h2 = unext[1] - ylower
            ui += 1
        da = (xnext - x) * (h1 + h2) / 2.0
        total_area += da
        areas.append((x, xnext, h1, h2, da, total_area))
        x = xnext
        h1 = h2
    size = total_area / npieces
    cuts = []
    needed = size
    for x1, x2, h1, h2, segment_area, total_area in areas:
        segment_remaining = segment_area
        area_from_left = 0
        while segment_remaining >= needed:
            area_from_left += needed
            width = x2 - x1
            if h1 == h2:
                x = area_from_left / h1
            else:
                qa = (h2 - h1) / (2 * width)
                qb = h1
                qc = -area_from_left
                x = (-qb + (qb ** 2 - 4 * qa * qc) ** 0.5) / (2 * qa)
            cuts.append(x1 + x)
            if len(cuts) == npieces - 1:
                return cuts
            segment_remaining -= needed
            needed = size
        needed -= segment_remaining
    return segment_area


def func_c58aec3102974673bb6b3d77736a3722(npieces, upper, lower):
    li = ui = 0
    areas = []
    total_area = 0
    x = 0
    h1 = upper[0][1] - lower[0][1]
    W = lower[-1][0]
    while x < W:
        lnext = lower[li + 1]
        unext = upper[ui + 1]
        if lnext[0] == unext[0]:
            xnext = lnext[0]
            h2 = unext[1] - lnext[1]
            li += 1
            ui += 1
        elif lnext[0] < upper[ui + 1][0]:
            xnext = lnext[0]
            frac = 1.0 * (xnext - upper[ui][0]) / (unext[0] - upper[ui][0])
            yupper = upper[ui][1] + frac * (unext[1] - upper[ui][1])
            h2 = yupper - lnext[1]
            li += 1
        else:
            xnext = unext[0]
            frac = 1.0 * (xnext - lower[li][0]) / (lnext[0] - lower[li][0])
            ylower = lower[li][1] + frac * (lnext[1] - lower[li][1])
            h2 = unext[1] - ylower
            ui += 1
        da = (xnext - x) * (h1 + h2) / 2.0
        total_area += da
        areas.append((x, xnext, h1, h2, da, total_area))
        x = xnext
        h1 = h2
    size = total_area / npieces
    cuts = []
    needed = size
    for x1, x2, h1, h2, segment_area, total_area in areas:
        segment_remaining = segment_area
        area_from_left = 0
        while segment_remaining >= needed:
            area_from_left += needed
            width = x2 - x1
            if h1 == h2:
                x = area_from_left / h1
            else:
                qa = (h2 - h1) / (2 * width)
                qb = h1
                qc = -area_from_left
                x = (-qb + (qb ** 2 - 4 * qa * qc) ** 0.5) / (2 * qa)
            cuts.append(x1 + x)
            if len(cuts) == npieces - 1:
                return cuts
            segment_remaining -= needed
            needed = size
        needed -= segment_remaining
    return ylower


def func_f40536f4090c4b24b1e9912d9fe799b1(npieces, upper, lower):
    li = ui = 0
    areas = []
    total_area = 0
    x = 0
    h1 = upper[0][1] - lower[0][1]
    W = lower[-1][0]
    while x < W:
        lnext = lower[li + 1]
        unext = upper[ui + 1]
        if lnext[0] == unext[0]:
            xnext = lnext[0]
            h2 = unext[1] - lnext[1]
            li += 1
            ui += 1
        elif lnext[0] < upper[ui + 1][0]:
            xnext = lnext[0]
            frac = 1.0 * (xnext - upper[ui][0]) / (unext[0] - upper[ui][0])
            yupper = upper[ui][1] + frac * (unext[1] - upper[ui][1])
            h2 = yupper - lnext[1]
            li += 1
        else:
            xnext = unext[0]
            frac = 1.0 * (xnext - lower[li][0]) / (lnext[0] - lower[li][0])
            ylower = lower[li][1] + frac * (lnext[1] - lower[li][1])
            h2 = unext[1] - ylower
            ui += 1
        da = (xnext - x) * (h1 + h2) / 2.0
        total_area += da
        areas.append((x, xnext, h1, h2, da, total_area))
        x = xnext
        h1 = h2
    size = total_area / npieces
    cuts = []
    needed = size
    for x1, x2, h1, h2, segment_area, total_area in areas:
        segment_remaining = segment_area
        area_from_left = 0
        while segment_remaining >= needed:
            area_from_left += needed
            width = x2 - x1
            if h1 == h2:
                x = area_from_left / h1
            else:
                qa = (h2 - h1) / (2 * width)
                qb = h1
                qc = -area_from_left
                x = (-qb + (qb ** 2 - 4 * qa * qc) ** 0.5) / (2 * qa)
            cuts.append(x1 + x)
            if len(cuts) == npieces - 1:
                return cuts
            segment_remaining -= needed
            needed = size
        needed -= segment_remaining
    return h2


def func_27a07dd3bf764627ac98173e708c68b7(npieces, upper, lower):
    li = ui = 0
    areas = []
    total_area = 0
    x = 0
    h1 = upper[0][1] - lower[0][1]
    W = lower[-1][0]
    while x < W:
        lnext = lower[li + 1]
        unext = upper[ui + 1]
        if lnext[0] == unext[0]:
            xnext = lnext[0]
            h2 = unext[1] - lnext[1]
            li += 1
            ui += 1
        elif lnext[0] < upper[ui + 1][0]:
            xnext = lnext[0]
            frac = 1.0 * (xnext - upper[ui][0]) / (unext[0] - upper[ui][0])
            yupper = upper[ui][1] + frac * (unext[1] - upper[ui][1])
            h2 = yupper - lnext[1]
            li += 1
        else:
            xnext = unext[0]
            frac = 1.0 * (xnext - lower[li][0]) / (lnext[0] - lower[li][0])
            ylower = lower[li][1] + frac * (lnext[1] - lower[li][1])
            h2 = unext[1] - ylower
            ui += 1
        da = (xnext - x) * (h1 + h2) / 2.0
        total_area += da
        areas.append((x, xnext, h1, h2, da, total_area))
        x = xnext
        h1 = h2
    size = total_area / npieces
    cuts = []
    needed = size
    for x1, x2, h1, h2, segment_area, total_area in areas:
        segment_remaining = segment_area
        area_from_left = 0
        while segment_remaining >= needed:
            area_from_left += needed
            width = x2 - x1
            if h1 == h2:
                x = area_from_left / h1
            else:
                qa = (h2 - h1) / (2 * width)
                qb = h1
                qc = -area_from_left
                x = (-qb + (qb ** 2 - 4 * qa * qc) ** 0.5) / (2 * qa)
            cuts.append(x1 + x)
            if len(cuts) == npieces - 1:
                return cuts
            segment_remaining -= needed
            needed = size
        needed -= segment_remaining
    return yupper


def func_8f2743a2aec349989ee3c2b119a495da(npieces, upper, lower):
    li = ui = 0
    areas = []
    total_area = 0
    x = 0
    h1 = upper[0][1] - lower[0][1]
    W = lower[-1][0]
    while x < W:
        lnext = lower[li + 1]
        unext = upper[ui + 1]
        if lnext[0] == unext[0]:
            xnext = lnext[0]
            h2 = unext[1] - lnext[1]
            li += 1
            ui += 1
        elif lnext[0] < upper[ui + 1][0]:
            xnext = lnext[0]
            frac = 1.0 * (xnext - upper[ui][0]) / (unext[0] - upper[ui][0])
            yupper = upper[ui][1] + frac * (unext[1] - upper[ui][1])
            h2 = yupper - lnext[1]
            li += 1
        else:
            xnext = unext[0]
            frac = 1.0 * (xnext - lower[li][0]) / (lnext[0] - lower[li][0])
            ylower = lower[li][1] + frac * (lnext[1] - lower[li][1])
            h2 = unext[1] - ylower
            ui += 1
        da = (xnext - x) * (h1 + h2) / 2.0
        total_area += da
        areas.append((x, xnext, h1, h2, da, total_area))
        x = xnext
        h1 = h2
    size = total_area / npieces
    cuts = []
    needed = size
    for x1, x2, h1, h2, segment_area, total_area in areas:
        segment_remaining = segment_area
        area_from_left = 0
        while segment_remaining >= needed:
            area_from_left += needed
            width = x2 - x1
            if h1 == h2:
                x = area_from_left / h1
            else:
                qa = (h2 - h1) / (2 * width)
                qb = h1
                qc = -area_from_left
                x = (-qb + (qb ** 2 - 4 * qa * qc) ** 0.5) / (2 * qa)
            cuts.append(x1 + x)
            if len(cuts) == npieces - 1:
                return cuts
            segment_remaining -= needed
            needed = size
        needed -= segment_remaining
    return ui


def func_4c941530bde24f5c829f7fefc5e23b44(npieces, upper, lower):
    li = ui = 0
    areas = []
    total_area = 0
    x = 0
    h1 = upper[0][1] - lower[0][1]
    W = lower[-1][0]
    while x < W:
        lnext = lower[li + 1]
        unext = upper[ui + 1]
        if lnext[0] == unext[0]:
            xnext = lnext[0]
            h2 = unext[1] - lnext[1]
            li += 1
            ui += 1
        elif lnext[0] < upper[ui + 1][0]:
            xnext = lnext[0]
            frac = 1.0 * (xnext - upper[ui][0]) / (unext[0] - upper[ui][0])
            yupper = upper[ui][1] + frac * (unext[1] - upper[ui][1])
            h2 = yupper - lnext[1]
            li += 1
        else:
            xnext = unext[0]
            frac = 1.0 * (xnext - lower[li][0]) / (lnext[0] - lower[li][0])
            ylower = lower[li][1] + frac * (lnext[1] - lower[li][1])
            h2 = unext[1] - ylower
            ui += 1
        da = (xnext - x) * (h1 + h2) / 2.0
        total_area += da
        areas.append((x, xnext, h1, h2, da, total_area))
        x = xnext
        h1 = h2
    size = total_area / npieces
    cuts = []
    needed = size
    for x1, x2, h1, h2, segment_area, total_area in areas:
        segment_remaining = segment_area
        area_from_left = 0
        while segment_remaining >= needed:
            area_from_left += needed
            width = x2 - x1
            if h1 == h2:
                x = area_from_left / h1
            else:
                qa = (h2 - h1) / (2 * width)
                qb = h1
                qc = -area_from_left
                x = (-qb + (qb ** 2 - 4 * qa * qc) ** 0.5) / (2 * qa)
            cuts.append(x1 + x)
            if len(cuts) == npieces - 1:
                return cuts
            segment_remaining -= needed
            needed = size
        needed -= segment_remaining
    return da


def func_3eb7df749ef94918b0d411fd608835c9(npieces, upper, lower):
    li = ui = 0
    areas = []
    total_area = 0
    x = 0
    h1 = upper[0][1] - lower[0][1]
    W = lower[-1][0]
    while x < W:
        lnext = lower[li + 1]
        unext = upper[ui + 1]
        if lnext[0] == unext[0]:
            xnext = lnext[0]
            h2 = unext[1] - lnext[1]
            li += 1
            ui += 1
        elif lnext[0] < upper[ui + 1][0]:
            xnext = lnext[0]
            frac = 1.0 * (xnext - upper[ui][0]) / (unext[0] - upper[ui][0])
            yupper = upper[ui][1] + frac * (unext[1] - upper[ui][1])
            h2 = yupper - lnext[1]
            li += 1
        else:
            xnext = unext[0]
            frac = 1.0 * (xnext - lower[li][0]) / (lnext[0] - lower[li][0])
            ylower = lower[li][1] + frac * (lnext[1] - lower[li][1])
            h2 = unext[1] - ylower
            ui += 1
        da = (xnext - x) * (h1 + h2) / 2.0
        total_area += da
        areas.append((x, xnext, h1, h2, da, total_area))
        x = xnext
        h1 = h2
    size = total_area / npieces
    cuts = []
    needed = size
    for x1, x2, h1, h2, segment_area, total_area in areas:
        segment_remaining = segment_area
        area_from_left = 0
        while segment_remaining >= needed:
            area_from_left += needed
            width = x2 - x1
            if h1 == h2:
                x = area_from_left / h1
            else:
                qa = (h2 - h1) / (2 * width)
                qb = h1
                qc = -area_from_left
                x = (-qb + (qb ** 2 - 4 * qa * qc) ** 0.5) / (2 * qa)
            cuts.append(x1 + x)
            if len(cuts) == npieces - 1:
                return cuts
            segment_remaining -= needed
            needed = size
        needed -= segment_remaining
    return size


def func_c724af5bf4dd49f385d8d95f8883fb0d(npieces, upper, lower):
    li = ui = 0
    areas = []
    total_area = 0
    x = 0
    h1 = upper[0][1] - lower[0][1]
    W = lower[-1][0]
    while x < W:
        lnext = lower[li + 1]
        unext = upper[ui + 1]
        if lnext[0] == unext[0]:
            xnext = lnext[0]
            h2 = unext[1] - lnext[1]
            li += 1
            ui += 1
        elif lnext[0] < upper[ui + 1][0]:
            xnext = lnext[0]
            frac = 1.0 * (xnext - upper[ui][0]) / (unext[0] - upper[ui][0])
            yupper = upper[ui][1] + frac * (unext[1] - upper[ui][1])
            h2 = yupper - lnext[1]
            li += 1
        else:
            xnext = unext[0]
            frac = 1.0 * (xnext - lower[li][0]) / (lnext[0] - lower[li][0])
            ylower = lower[li][1] + frac * (lnext[1] - lower[li][1])
            h2 = unext[1] - ylower
            ui += 1
        da = (xnext - x) * (h1 + h2) / 2.0
        total_area += da
        areas.append((x, xnext, h1, h2, da, total_area))
        x = xnext
        h1 = h2
    size = total_area / npieces
    cuts = []
    needed = size
    for x1, x2, h1, h2, segment_area, total_area in areas:
        segment_remaining = segment_area
        area_from_left = 0
        while segment_remaining >= needed:
            area_from_left += needed
            width = x2 - x1
            if h1 == h2:
                x = area_from_left / h1
            else:
                qa = (h2 - h1) / (2 * width)
                qb = h1
                qc = -area_from_left
                x = (-qb + (qb ** 2 - 4 * qa * qc) ** 0.5) / (2 * qa)
            cuts.append(x1 + x)
            if len(cuts) == npieces - 1:
                return cuts
            segment_remaining -= needed
            needed = size
        needed -= segment_remaining
    return unext


def func_f30f2284e91e42a2b7fa7411b9b3cfab(npieces, upper, lower):
    li = ui = 0
    areas = []
    total_area = 0
    x = 0
    h1 = upper[0][1] - lower[0][1]
    W = lower[-1][0]
    while x < W:
        lnext = lower[li + 1]
        unext = upper[ui + 1]
        if lnext[0] == unext[0]:
            xnext = lnext[0]
            h2 = unext[1] - lnext[1]
            li += 1
            ui += 1
        elif lnext[0] < upper[ui + 1][0]:
            xnext = lnext[0]
            frac = 1.0 * (xnext - upper[ui][0]) / (unext[0] - upper[ui][0])
            yupper = upper[ui][1] + frac * (unext[1] - upper[ui][1])
            h2 = yupper - lnext[1]
            li += 1
        else:
            xnext = unext[0]
            frac = 1.0 * (xnext - lower[li][0]) / (lnext[0] - lower[li][0])
            ylower = lower[li][1] + frac * (lnext[1] - lower[li][1])
            h2 = unext[1] - ylower
            ui += 1
        da = (xnext - x) * (h1 + h2) / 2.0
        total_area += da
        areas.append((x, xnext, h1, h2, da, total_area))
        x = xnext
        h1 = h2
    size = total_area / npieces
    cuts = []
    needed = size
    for x1, x2, h1, h2, segment_area, total_area in areas:
        segment_remaining = segment_area
        area_from_left = 0
        while segment_remaining >= needed:
            area_from_left += needed
            width = x2 - x1
            if h1 == h2:
                x = area_from_left / h1
            else:
                qa = (h2 - h1) / (2 * width)
                qb = h1
                qc = -area_from_left
                x = (-qb + (qb ** 2 - 4 * qa * qc) ** 0.5) / (2 * qa)
            cuts.append(x1 + x)
            if len(cuts) == npieces - 1:
                return cuts
            segment_remaining -= needed
            needed = size
        needed -= segment_remaining
    return frac


def func_dee83475815e4605b17677c20bab6966(npieces, upper, lower):
    li = ui = 0
    areas = []
    total_area = 0
    x = 0
    h1 = upper[0][1] - lower[0][1]
    W = lower[-1][0]
    while x < W:
        lnext = lower[li + 1]
        unext = upper[ui + 1]
        if lnext[0] == unext[0]:
            xnext = lnext[0]
            h2 = unext[1] - lnext[1]
            li += 1
            ui += 1
        elif lnext[0] < upper[ui + 1][0]:
            xnext = lnext[0]
            frac = 1.0 * (xnext - upper[ui][0]) / (unext[0] - upper[ui][0])
            yupper = upper[ui][1] + frac * (unext[1] - upper[ui][1])
            h2 = yupper - lnext[1]
            li += 1
        else:
            xnext = unext[0]
            frac = 1.0 * (xnext - lower[li][0]) / (lnext[0] - lower[li][0])
            ylower = lower[li][1] + frac * (lnext[1] - lower[li][1])
            h2 = unext[1] - ylower
            ui += 1
        da = (xnext - x) * (h1 + h2) / 2.0
        total_area += da
        areas.append((x, xnext, h1, h2, da, total_area))
        x = xnext
        h1 = h2
    size = total_area / npieces
    cuts = []
    needed = size
    for x1, x2, h1, h2, segment_area, total_area in areas:
        segment_remaining = segment_area
        area_from_left = 0
        while segment_remaining >= needed:
            area_from_left += needed
            width = x2 - x1
            if h1 == h2:
                x = area_from_left / h1
            else:
                qa = (h2 - h1) / (2 * width)
                qb = h1
                qc = -area_from_left
                x = (-qb + (qb ** 2 - 4 * qa * qc) ** 0.5) / (2 * qa)
            cuts.append(x1 + x)
            if len(cuts) == npieces - 1:
                return cuts
            segment_remaining -= needed
            needed = size
        needed -= segment_remaining
    return segment_remaining


def func_c6428341c3744b9e909529d55607310c(npieces, upper, lower):
    li = ui = 0
    areas = []
    total_area = 0
    x = 0
    h1 = upper[0][1] - lower[0][1]
    W = lower[-1][0]
    while x < W:
        lnext = lower[li + 1]
        unext = upper[ui + 1]
        if lnext[0] == unext[0]:
            xnext = lnext[0]
            h2 = unext[1] - lnext[1]
            li += 1
            ui += 1
        elif lnext[0] < upper[ui + 1][0]:
            xnext = lnext[0]
            frac = 1.0 * (xnext - upper[ui][0]) / (unext[0] - upper[ui][0])
            yupper = upper[ui][1] + frac * (unext[1] - upper[ui][1])
            h2 = yupper - lnext[1]
            li += 1
        else:
            xnext = unext[0]
            frac = 1.0 * (xnext - lower[li][0]) / (lnext[0] - lower[li][0])
            ylower = lower[li][1] + frac * (lnext[1] - lower[li][1])
            h2 = unext[1] - ylower
            ui += 1
        da = (xnext - x) * (h1 + h2) / 2.0
        total_area += da
        areas.append((x, xnext, h1, h2, da, total_area))
        x = xnext
        h1 = h2
    size = total_area / npieces
    cuts = []
    needed = size
    for x1, x2, h1, h2, segment_area, total_area in areas:
        segment_remaining = segment_area
        area_from_left = 0
        while segment_remaining >= needed:
            area_from_left += needed
            width = x2 - x1
            if h1 == h2:
                x = area_from_left / h1
            else:
                qa = (h2 - h1) / (2 * width)
                qb = h1
                qc = -area_from_left
                x = (-qb + (qb ** 2 - 4 * qa * qc) ** 0.5) / (2 * qa)
            cuts.append(x1 + x)
            if len(cuts) == npieces - 1:
                return cuts
            segment_remaining -= needed
            needed = size
        needed -= segment_remaining
    return qb


def func_8cce00fb4c24413db38f183fb14f3679(npieces, upper, lower):
    li = ui = 0
    areas = []
    total_area = 0
    x = 0
    h1 = upper[0][1] - lower[0][1]
    W = lower[-1][0]
    while x < W:
        lnext = lower[li + 1]
        unext = upper[ui + 1]
        if lnext[0] == unext[0]:
            xnext = lnext[0]
            h2 = unext[1] - lnext[1]
            li += 1
            ui += 1
        elif lnext[0] < upper[ui + 1][0]:
            xnext = lnext[0]
            frac = 1.0 * (xnext - upper[ui][0]) / (unext[0] - upper[ui][0])
            yupper = upper[ui][1] + frac * (unext[1] - upper[ui][1])
            h2 = yupper - lnext[1]
            li += 1
        else:
            xnext = unext[0]
            frac = 1.0 * (xnext - lower[li][0]) / (lnext[0] - lower[li][0])
            ylower = lower[li][1] + frac * (lnext[1] - lower[li][1])
            h2 = unext[1] - ylower
            ui += 1
        da = (xnext - x) * (h1 + h2) / 2.0
        total_area += da
        areas.append((x, xnext, h1, h2, da, total_area))
        x = xnext
        h1 = h2
    size = total_area / npieces
    cuts = []
    needed = size
    for x1, x2, h1, h2, segment_area, total_area in areas:
        segment_remaining = segment_area
        area_from_left = 0
        while segment_remaining >= needed:
            area_from_left += needed
            width = x2 - x1
            if h1 == h2:
                x = area_from_left / h1
            else:
                qa = (h2 - h1) / (2 * width)
                qb = h1
                qc = -area_from_left
                x = (-qb + (qb ** 2 - 4 * qa * qc) ** 0.5) / (2 * qa)
            cuts.append(x1 + x)
            if len(cuts) == npieces - 1:
                return cuts
            segment_remaining -= needed
            needed = size
        needed -= segment_remaining
    return qc


def func_d94432a818fe4ca1ae1d5b9ba0fbf2ce(npieces, upper, lower):
    li = ui = 0
    areas = []
    total_area = 0
    x = 0
    h1 = upper[0][1] - lower[0][1]
    W = lower[-1][0]
    while x < W:
        lnext = lower[li + 1]
        unext = upper[ui + 1]
        if lnext[0] == unext[0]:
            xnext = lnext[0]
            h2 = unext[1] - lnext[1]
            li += 1
            ui += 1
        elif lnext[0] < upper[ui + 1][0]:
            xnext = lnext[0]
            frac = 1.0 * (xnext - upper[ui][0]) / (unext[0] - upper[ui][0])
            yupper = upper[ui][1] + frac * (unext[1] - upper[ui][1])
            h2 = yupper - lnext[1]
            li += 1
        else:
            xnext = unext[0]
            frac = 1.0 * (xnext - lower[li][0]) / (lnext[0] - lower[li][0])
            ylower = lower[li][1] + frac * (lnext[1] - lower[li][1])
            h2 = unext[1] - ylower
            ui += 1
        da = (xnext - x) * (h1 + h2) / 2.0
        total_area += da
        areas.append((x, xnext, h1, h2, da, total_area))
        x = xnext
        h1 = h2
    size = total_area / npieces
    cuts = []
    needed = size
    for x1, x2, h1, h2, segment_area, total_area in areas:
        segment_remaining = segment_area
        area_from_left = 0
        while segment_remaining >= needed:
            area_from_left += needed
            width = x2 - x1
            if h1 == h2:
                x = area_from_left / h1
            else:
                qa = (h2 - h1) / (2 * width)
                qb = h1
                qc = -area_from_left
                x = (-qb + (qb ** 2 - 4 * qa * qc) ** 0.5) / (2 * qa)
            cuts.append(x1 + x)
            if len(cuts) == npieces - 1:
                return cuts
            segment_remaining -= needed
            needed = size
        needed -= segment_remaining
    return x2


def func_163d22756907491c9f224e2551291da8(npieces, upper, lower):
    li = ui = 0
    areas = []
    total_area = 0
    x = 0
    h1 = upper[0][1] - lower[0][1]
    W = lower[-1][0]
    while x < W:
        lnext = lower[li + 1]
        unext = upper[ui + 1]
        if lnext[0] == unext[0]:
            xnext = lnext[0]
            h2 = unext[1] - lnext[1]
            li += 1
            ui += 1
        elif lnext[0] < upper[ui + 1][0]:
            xnext = lnext[0]
            frac = 1.0 * (xnext - upper[ui][0]) / (unext[0] - upper[ui][0])
            yupper = upper[ui][1] + frac * (unext[1] - upper[ui][1])
            h2 = yupper - lnext[1]
            li += 1
        else:
            xnext = unext[0]
            frac = 1.0 * (xnext - lower[li][0]) / (lnext[0] - lower[li][0])
            ylower = lower[li][1] + frac * (lnext[1] - lower[li][1])
            h2 = unext[1] - ylower
            ui += 1
        da = (xnext - x) * (h1 + h2) / 2.0
        total_area += da
        areas.append((x, xnext, h1, h2, da, total_area))
        x = xnext
        h1 = h2
    size = total_area / npieces
    cuts = []
    needed = size
    for x1, x2, h1, h2, segment_area, total_area in areas:
        segment_remaining = segment_area
        area_from_left = 0
        while segment_remaining >= needed:
            area_from_left += needed
            width = x2 - x1
            if h1 == h2:
                x = area_from_left / h1
            else:
                qa = (h2 - h1) / (2 * width)
                qb = h1
                qc = -area_from_left
                x = (-qb + (qb ** 2 - 4 * qa * qc) ** 0.5) / (2 * qa)
            cuts.append(x1 + x)
            if len(cuts) == npieces - 1:
                return cuts
            segment_remaining -= needed
            needed = size
        needed -= segment_remaining
    return xnext


def func_ed93ad8549d5404caa27773bab649b4c(npieces, upper, lower):
    li = ui = 0
    areas = []
    total_area = 0
    x = 0
    h1 = upper[0][1] - lower[0][1]
    W = lower[-1][0]
    while x < W:
        lnext = lower[li + 1]
        unext = upper[ui + 1]
        if lnext[0] == unext[0]:
            xnext = lnext[0]
            h2 = unext[1] - lnext[1]
            li += 1
            ui += 1
        elif lnext[0] < upper[ui + 1][0]:
            xnext = lnext[0]
            frac = 1.0 * (xnext - upper[ui][0]) / (unext[0] - upper[ui][0])
            yupper = upper[ui][1] + frac * (unext[1] - upper[ui][1])
            h2 = yupper - lnext[1]
            li += 1
        else:
            xnext = unext[0]
            frac = 1.0 * (xnext - lower[li][0]) / (lnext[0] - lower[li][0])
            ylower = lower[li][1] + frac * (lnext[1] - lower[li][1])
            h2 = unext[1] - ylower
            ui += 1
        da = (xnext - x) * (h1 + h2) / 2.0
        total_area += da
        areas.append((x, xnext, h1, h2, da, total_area))
        x = xnext
        h1 = h2
    size = total_area / npieces
    cuts = []
    needed = size
    for x1, x2, h1, h2, segment_area, total_area in areas:
        segment_remaining = segment_area
        area_from_left = 0
        while segment_remaining >= needed:
            area_from_left += needed
            width = x2 - x1
            if h1 == h2:
                x = area_from_left / h1
            else:
                qa = (h2 - h1) / (2 * width)
                qb = h1
                qc = -area_from_left
                x = (-qb + (qb ** 2 - 4 * qa * qc) ** 0.5) / (2 * qa)
            cuts.append(x1 + x)
            if len(cuts) == npieces - 1:
                return cuts
            segment_remaining -= needed
            needed = size
        needed -= segment_remaining
    return areas


def func_b47aad6298b542cbb925e3c9067dde17(npieces, upper, lower):
    li = ui = 0
    areas = []
    total_area = 0
    x = 0
    h1 = upper[0][1] - lower[0][1]
    W = lower[-1][0]
    while x < W:
        lnext = lower[li + 1]
        unext = upper[ui + 1]
        if lnext[0] == unext[0]:
            xnext = lnext[0]
            h2 = unext[1] - lnext[1]
            li += 1
            ui += 1
        elif lnext[0] < upper[ui + 1][0]:
            xnext = lnext[0]
            frac = 1.0 * (xnext - upper[ui][0]) / (unext[0] - upper[ui][0])
            yupper = upper[ui][1] + frac * (unext[1] - upper[ui][1])
            h2 = yupper - lnext[1]
            li += 1
        else:
            xnext = unext[0]
            frac = 1.0 * (xnext - lower[li][0]) / (lnext[0] - lower[li][0])
            ylower = lower[li][1] + frac * (lnext[1] - lower[li][1])
            h2 = unext[1] - ylower
            ui += 1
        da = (xnext - x) * (h1 + h2) / 2.0
        total_area += da
        areas.append((x, xnext, h1, h2, da, total_area))
        x = xnext
        h1 = h2
    size = total_area / npieces
    cuts = []
    needed = size
    for x1, x2, h1, h2, segment_area, total_area in areas:
        segment_remaining = segment_area
        area_from_left = 0
        while segment_remaining >= needed:
            area_from_left += needed
            width = x2 - x1
            if h1 == h2:
                x = area_from_left / h1
            else:
                qa = (h2 - h1) / (2 * width)
                qb = h1
                qc = -area_from_left
                x = (-qb + (qb ** 2 - 4 * qa * qc) ** 0.5) / (2 * qa)
            cuts.append(x1 + x)
            if len(cuts) == npieces - 1:
                return cuts
            segment_remaining -= needed
            needed = size
        needed -= segment_remaining
    return h1


def func_c53da0f69daa41eb81a9a59148f29c5f(npieces, upper, lower):
    li = ui = 0
    areas = []
    total_area = 0
    x = 0
    h1 = upper[0][1] - lower[0][1]
    W = lower[-1][0]
    while x < W:
        lnext = lower[li + 1]
        unext = upper[ui + 1]
        if lnext[0] == unext[0]:
            xnext = lnext[0]
            h2 = unext[1] - lnext[1]
            li += 1
            ui += 1
        elif lnext[0] < upper[ui + 1][0]:
            xnext = lnext[0]
            frac = 1.0 * (xnext - upper[ui][0]) / (unext[0] - upper[ui][0])
            yupper = upper[ui][1] + frac * (unext[1] - upper[ui][1])
            h2 = yupper - lnext[1]
            li += 1
        else:
            xnext = unext[0]
            frac = 1.0 * (xnext - lower[li][0]) / (lnext[0] - lower[li][0])
            ylower = lower[li][1] + frac * (lnext[1] - lower[li][1])
            h2 = unext[1] - ylower
            ui += 1
        da = (xnext - x) * (h1 + h2) / 2.0
        total_area += da
        areas.append((x, xnext, h1, h2, da, total_area))
        x = xnext
        h1 = h2
    size = total_area / npieces
    cuts = []
    needed = size
    for x1, x2, h1, h2, segment_area, total_area in areas:
        segment_remaining = segment_area
        area_from_left = 0
        while segment_remaining >= needed:
            area_from_left += needed
            width = x2 - x1
            if h1 == h2:
                x = area_from_left / h1
            else:
                qa = (h2 - h1) / (2 * width)
                qb = h1
                qc = -area_from_left
                x = (-qb + (qb ** 2 - 4 * qa * qc) ** 0.5) / (2 * qa)
            cuts.append(x1 + x)
            if len(cuts) == npieces - 1:
                return cuts
            segment_remaining -= needed
            needed = size
        needed -= segment_remaining
    return lnext


def func_47fad57f15494356b939066e226d5a7e(npieces, upper, lower):
    li = ui = 0
    areas = []
    total_area = 0
    x = 0
    h1 = upper[0][1] - lower[0][1]
    W = lower[-1][0]
    while x < W:
        lnext = lower[li + 1]
        unext = upper[ui + 1]
        if lnext[0] == unext[0]:
            xnext = lnext[0]
            h2 = unext[1] - lnext[1]
            li += 1
            ui += 1
        elif lnext[0] < upper[ui + 1][0]:
            xnext = lnext[0]
            frac = 1.0 * (xnext - upper[ui][0]) / (unext[0] - upper[ui][0])
            yupper = upper[ui][1] + frac * (unext[1] - upper[ui][1])
            h2 = yupper - lnext[1]
            li += 1
        else:
            xnext = unext[0]
            frac = 1.0 * (xnext - lower[li][0]) / (lnext[0] - lower[li][0])
            ylower = lower[li][1] + frac * (lnext[1] - lower[li][1])
            h2 = unext[1] - ylower
            ui += 1
        da = (xnext - x) * (h1 + h2) / 2.0
        total_area += da
        areas.append((x, xnext, h1, h2, da, total_area))
        x = xnext
        h1 = h2
    size = total_area / npieces
    cuts = []
    needed = size
    for x1, x2, h1, h2, segment_area, total_area in areas:
        segment_remaining = segment_area
        area_from_left = 0
        while segment_remaining >= needed:
            area_from_left += needed
            width = x2 - x1
            if h1 == h2:
                x = area_from_left / h1
            else:
                qa = (h2 - h1) / (2 * width)
                qb = h1
                qc = -area_from_left
                x = (-qb + (qb ** 2 - 4 * qa * qc) ** 0.5) / (2 * qa)
            cuts.append(x1 + x)
            if len(cuts) == npieces - 1:
                return cuts
            segment_remaining -= needed
            needed = size
        needed -= segment_remaining
    return width


def func_9fce09243c8d443d8008c7ad5757c072(npieces, upper, lower):
    li = ui = 0
    areas = []
    total_area = 0
    x = 0
    h1 = upper[0][1] - lower[0][1]
    W = lower[-1][0]
    while x < W:
        lnext = lower[li + 1]
        unext = upper[ui + 1]
        if lnext[0] == unext[0]:
            xnext = lnext[0]
            h2 = unext[1] - lnext[1]
            li += 1
            ui += 1
        elif lnext[0] < upper[ui + 1][0]:
            xnext = lnext[0]
            frac = 1.0 * (xnext - upper[ui][0]) / (unext[0] - upper[ui][0])
            yupper = upper[ui][1] + frac * (unext[1] - upper[ui][1])
            h2 = yupper - lnext[1]
            li += 1
        else:
            xnext = unext[0]
            frac = 1.0 * (xnext - lower[li][0]) / (lnext[0] - lower[li][0])
            ylower = lower[li][1] + frac * (lnext[1] - lower[li][1])
            h2 = unext[1] - ylower
            ui += 1
        da = (xnext - x) * (h1 + h2) / 2.0
        total_area += da
        areas.append((x, xnext, h1, h2, da, total_area))
        x = xnext
        h1 = h2
    size = total_area / npieces
    cuts = []
    needed = size
    for x1, x2, h1, h2, segment_area, total_area in areas:
        segment_remaining = segment_area
        area_from_left = 0
        while segment_remaining >= needed:
            area_from_left += needed
            width = x2 - x1
            if h1 == h2:
                x = area_from_left / h1
            else:
                qa = (h2 - h1) / (2 * width)
                qb = h1
                qc = -area_from_left
                x = (-qb + (qb ** 2 - 4 * qa * qc) ** 0.5) / (2 * qa)
            cuts.append(x1 + x)
            if len(cuts) == npieces - 1:
                return cuts
            segment_remaining -= needed
            needed = size
        needed -= segment_remaining
    return x1


def func_ea4eb3eef93f480f8dbbe06029737c98(npieces, upper, lower):
    li = ui = 0
    areas = []
    total_area = 0
    x = 0
    h1 = upper[0][1] - lower[0][1]
    W = lower[-1][0]
    while x < W:
        lnext = lower[li + 1]
        unext = upper[ui + 1]
        if lnext[0] == unext[0]:
            xnext = lnext[0]
            h2 = unext[1] - lnext[1]
            li += 1
            ui += 1
        elif lnext[0] < upper[ui + 1][0]:
            xnext = lnext[0]
            frac = 1.0 * (xnext - upper[ui][0]) / (unext[0] - upper[ui][0])
            yupper = upper[ui][1] + frac * (unext[1] - upper[ui][1])
            h2 = yupper - lnext[1]
            li += 1
        else:
            xnext = unext[0]
            frac = 1.0 * (xnext - lower[li][0]) / (lnext[0] - lower[li][0])
            ylower = lower[li][1] + frac * (lnext[1] - lower[li][1])
            h2 = unext[1] - ylower
            ui += 1
        da = (xnext - x) * (h1 + h2) / 2.0
        total_area += da
        areas.append((x, xnext, h1, h2, da, total_area))
        x = xnext
        h1 = h2
    size = total_area / npieces
    cuts = []
    needed = size
    for x1, x2, h1, h2, segment_area, total_area in areas:
        segment_remaining = segment_area
        area_from_left = 0
        while segment_remaining >= needed:
            area_from_left += needed
            width = x2 - x1
            if h1 == h2:
                x = area_from_left / h1
            else:
                qa = (h2 - h1) / (2 * width)
                qb = h1
                qc = -area_from_left
                x = (-qb + (qb ** 2 - 4 * qa * qc) ** 0.5) / (2 * qa)
            cuts.append(x1 + x)
            if len(cuts) == npieces - 1:
                return cuts
            segment_remaining -= needed
            needed = size
        needed -= segment_remaining
    return needed


def func_7f0198e143c047f5854e210e55de0ab7(npieces, upper, lower):
    li = ui = 0
    areas = []
    total_area = 0
    x = 0
    h1 = upper[0][1] - lower[0][1]
    W = lower[-1][0]
    while x < W:
        lnext = lower[li + 1]
        unext = upper[ui + 1]
        if lnext[0] == unext[0]:
            xnext = lnext[0]
            h2 = unext[1] - lnext[1]
            li += 1
            ui += 1
        elif lnext[0] < upper[ui + 1][0]:
            xnext = lnext[0]
            frac = 1.0 * (xnext - upper[ui][0]) / (unext[0] - upper[ui][0])
            yupper = upper[ui][1] + frac * (unext[1] - upper[ui][1])
            h2 = yupper - lnext[1]
            li += 1
        else:
            xnext = unext[0]
            frac = 1.0 * (xnext - lower[li][0]) / (lnext[0] - lower[li][0])
            ylower = lower[li][1] + frac * (lnext[1] - lower[li][1])
            h2 = unext[1] - ylower
            ui += 1
        da = (xnext - x) * (h1 + h2) / 2.0
        total_area += da
        areas.append((x, xnext, h1, h2, da, total_area))
        x = xnext
        h1 = h2
    size = total_area / npieces
    cuts = []
    needed = size
    for x1, x2, h1, h2, segment_area, total_area in areas:
        segment_remaining = segment_area
        area_from_left = 0
        while segment_remaining >= needed:
            area_from_left += needed
            width = x2 - x1
            if h1 == h2:
                x = area_from_left / h1
            else:
                qa = (h2 - h1) / (2 * width)
                qb = h1
                qc = -area_from_left
                x = (-qb + (qb ** 2 - 4 * qa * qc) ** 0.5) / (2 * qa)
            cuts.append(x1 + x)
            if len(cuts) == npieces - 1:
                return cuts
            segment_remaining -= needed
            needed = size
        needed -= segment_remaining
    return W


def func_65abc3cbaf334111a1904ca6068f4323(npieces, upper, lower):
    li = ui = 0
    areas = []
    total_area = 0
    x = 0
    h1 = upper[0][1] - lower[0][1]
    W = lower[-1][0]
    while x < W:
        lnext = lower[li + 1]
        unext = upper[ui + 1]
        if lnext[0] == unext[0]:
            xnext = lnext[0]
            h2 = unext[1] - lnext[1]
            li += 1
            ui += 1
        elif lnext[0] < upper[ui + 1][0]:
            xnext = lnext[0]
            frac = 1.0 * (xnext - upper[ui][0]) / (unext[0] - upper[ui][0])
            yupper = upper[ui][1] + frac * (unext[1] - upper[ui][1])
            h2 = yupper - lnext[1]
            li += 1
        else:
            xnext = unext[0]
            frac = 1.0 * (xnext - lower[li][0]) / (lnext[0] - lower[li][0])
            ylower = lower[li][1] + frac * (lnext[1] - lower[li][1])
            h2 = unext[1] - ylower
            ui += 1
        da = (xnext - x) * (h1 + h2) / 2.0
        total_area += da
        areas.append((x, xnext, h1, h2, da, total_area))
        x = xnext
        h1 = h2
    size = total_area / npieces
    cuts = []
    needed = size
    for x1, x2, h1, h2, segment_area, total_area in areas:
        segment_remaining = segment_area
        area_from_left = 0
        while segment_remaining >= needed:
            area_from_left += needed
            width = x2 - x1
            if h1 == h2:
                x = area_from_left / h1
            else:
                qa = (h2 - h1) / (2 * width)
                qb = h1
                qc = -area_from_left
                x = (-qb + (qb ** 2 - 4 * qa * qc) ** 0.5) / (2 * qa)
            cuts.append(x1 + x)
            if len(cuts) == npieces - 1:
                return cuts
            segment_remaining -= needed
            needed = size
        needed -= segment_remaining
    return li


def func_72619dff26fd49b1b3f769acede8a49b(npieces, upper, lower):
    li = ui = 0
    areas = []
    total_area = 0
    x = 0
    h1 = upper[0][1] - lower[0][1]
    W = lower[-1][0]
    while x < W:
        lnext = lower[li + 1]
        unext = upper[ui + 1]
        if lnext[0] == unext[0]:
            xnext = lnext[0]
            h2 = unext[1] - lnext[1]
            li += 1
            ui += 1
        elif lnext[0] < upper[ui + 1][0]:
            xnext = lnext[0]
            frac = 1.0 * (xnext - upper[ui][0]) / (unext[0] - upper[ui][0])
            yupper = upper[ui][1] + frac * (unext[1] - upper[ui][1])
            h2 = yupper - lnext[1]
            li += 1
        else:
            xnext = unext[0]
            frac = 1.0 * (xnext - lower[li][0]) / (lnext[0] - lower[li][0])
            ylower = lower[li][1] + frac * (lnext[1] - lower[li][1])
            h2 = unext[1] - ylower
            ui += 1
        da = (xnext - x) * (h1 + h2) / 2.0
        total_area += da
        areas.append((x, xnext, h1, h2, da, total_area))
        x = xnext
        h1 = h2
    size = total_area / npieces
    cuts = []
    needed = size
    for x1, x2, h1, h2, segment_area, total_area in areas:
        segment_remaining = segment_area
        area_from_left = 0
        while segment_remaining >= needed:
            area_from_left += needed
            width = x2 - x1
            if h1 == h2:
                x = area_from_left / h1
            else:
                qa = (h2 - h1) / (2 * width)
                qb = h1
                qc = -area_from_left
                x = (-qb + (qb ** 2 - 4 * qa * qc) ** 0.5) / (2 * qa)
            cuts.append(x1 + x)
            if len(cuts) == npieces - 1:
                return cuts
            segment_remaining -= needed
            needed = size
        needed -= segment_remaining
    return area_from_left


def func_acc1cf2e160140b1949af7872e98cfc2(npieces, upper, lower):
    li = ui = 0
    areas = []
    total_area = 0
    x = 0
    h1 = upper[0][1] - lower[0][1]
    W = lower[-1][0]
    while x < W:
        lnext = lower[li + 1]
        unext = upper[ui + 1]
        if lnext[0] == unext[0]:
            xnext = lnext[0]
            h2 = unext[1] - lnext[1]
            li += 1
            ui += 1
        elif lnext[0] < upper[ui + 1][0]:
            xnext = lnext[0]
            frac = 1.0 * (xnext - upper[ui][0]) / (unext[0] - upper[ui][0])
            yupper = upper[ui][1] + frac * (unext[1] - upper[ui][1])
            h2 = yupper - lnext[1]
            li += 1
        else:
            xnext = unext[0]
            frac = 1.0 * (xnext - lower[li][0]) / (lnext[0] - lower[li][0])
            ylower = lower[li][1] + frac * (lnext[1] - lower[li][1])
            h2 = unext[1] - ylower
            ui += 1
        da = (xnext - x) * (h1 + h2) / 2.0
        total_area += da
        areas.append((x, xnext, h1, h2, da, total_area))
        x = xnext
        h1 = h2
    size = total_area / npieces
    cuts = []
    needed = size
    for x1, x2, h1, h2, segment_area, total_area in areas:
        segment_remaining = segment_area
        area_from_left = 0
        while segment_remaining >= needed:
            area_from_left += needed
            width = x2 - x1
            if h1 == h2:
                x = area_from_left / h1
            else:
                qa = (h2 - h1) / (2 * width)
                qb = h1
                qc = -area_from_left
                x = (-qb + (qb ** 2 - 4 * qa * qc) ** 0.5) / (2 * qa)
            cuts.append(x1 + x)
            if len(cuts) == npieces - 1:
                return cuts
            segment_remaining -= needed
            needed = size
        needed -= segment_remaining
    return x


def func_c2929ae2c19149a88a70128c25e6ca9f(infile):
    W, L, U, G = map(int, infile.readline().split())
    lower = []
    return lower


def func_3edd52e9c31f4b048aac383659844c00(infile):
    W, L, U, G = map(int, infile.readline().split())
    lower = []
    return G


def func_c207992cf0b54db69e25fb638c2aa8ff(infile):
    W, L, U, G = map(int, infile.readline().split())
    lower = []
    return L


def func_fdeba186f3d14cc4b2b8f67e2e8b24a9(infile):
    W, L, U, G = map(int, infile.readline().split())
    lower = []
    return U


def func_1a7f14da77a447acbd515901f41fc06a(infile):
    W, L, U, G = map(int, infile.readline().split())
    lower = []
    return W


def func_c2f97cad34ff4d66b9798e2842ac939a():
    lower = []
    upper = []
    return upper


def func_a88ffd3e0f5c4f21b12d3c93bbd7351d():
    lower = []
    upper = []
    return lower


def func_c6eea036fc1b4389a7dee548c2920420(infile, lower, L):
    upper = []
    for j in xrange(L):
        lower.append(map(float, infile.readline().split()))
    return j


def func_a0ae016003904ceaa29a2abbd9d28a8c(infile, lower, L):
    upper = []
    for j in xrange(L):
        lower.append(map(float, infile.readline().split()))
    return upper


def func_7eea82bcad1e432a965314393d14ebd3(upper, infile, U, lower, L):
    for j in xrange(L):
        lower.append(map(float, infile.readline().split()))
    for j in xrange(U):
        upper.append(map(float, infile.readline().split()))
    return j


def func_905603ec298b4ae29616d69552bf423d(upper, infile, U, lower, G):
    for j in xrange(U):
        upper.append(map(float, infile.readline().split()))
    result = get_cuts(lower, upper, G)
    return j


def func_56f5b2de7ad4417c88b4a769128d9947(upper, infile, U, lower, G):
    for j in xrange(U):
        upper.append(map(float, infile.readline().split()))
    result = get_cuts(lower, upper, G)
    return result


def func_fb1d70d62b8e4427805275234a248016(upper, lower, i, G):
    result = get_cuts(lower, upper, G)('Case #%d:' % (i + 1,))
    return result


def func_dd3a4292bd1b40d69d778d2080829a67(infile):
    W, L, U, G = map(int, infile.readline().split())
    lower = []
    upper = []
    return G


def func_2d7990088bb4499db82d212b5ad32963(infile):
    W, L, U, G = map(int, infile.readline().split())
    lower = []
    upper = []
    return lower


def func_592122217fda4547b201be134aec0e9b(infile):
    W, L, U, G = map(int, infile.readline().split())
    lower = []
    upper = []
    return W


def func_fe330482e2934cacb1429b4bbc7fd5a3(infile):
    W, L, U, G = map(int, infile.readline().split())
    lower = []
    upper = []
    return U


def func_b67ed09e9a24498296d97e63e42220e5(infile):
    W, L, U, G = map(int, infile.readline().split())
    lower = []
    upper = []
    return L


def func_ba785f6a2cda4fc5b1dc667b89eb67f2(infile):
    W, L, U, G = map(int, infile.readline().split())
    lower = []
    upper = []
    return upper


def func_b646d29772c14b919709948a193afae7(infile, L):
    lower = []
    upper = []
    for j in xrange(L):
        lower.append(map(float, infile.readline().split()))
    return lower


def func_283b50d07f4c44b9b5a1982e937b70c1(infile, L):
    lower = []
    upper = []
    for j in xrange(L):
        lower.append(map(float, infile.readline().split()))
    return j


def func_dac79fe5fa4f4d74979ac3430d0bb0c9(infile, L):
    lower = []
    upper = []
    for j in xrange(L):
        lower.append(map(float, infile.readline().split()))
    return upper


def func_8af1ccd3a5fb42338425e3a0b0bcef5e(infile, U, lower, L):
    upper = []
    for j in xrange(L):
        lower.append(map(float, infile.readline().split()))
    for j in xrange(U):
        upper.append(map(float, infile.readline().split()))
    return j


def func_d70332c5193c4b3bba5e2bfb5cf018d4(infile, U, lower, L):
    upper = []
    for j in xrange(L):
        lower.append(map(float, infile.readline().split()))
    for j in xrange(U):
        upper.append(map(float, infile.readline().split()))
    return upper


def func_1822f7c51775480eb96bd06a6ea46fad(upper, infile, U, lower, G, L):
    for j in xrange(L):
        lower.append(map(float, infile.readline().split()))
    for j in xrange(U):
        upper.append(map(float, infile.readline().split()))
    result = get_cuts(lower, upper, G)
    return j


def func_bf934646ed1d4ab7b3b4050b18e210ba(upper, infile, U, lower, G, L):
    for j in xrange(L):
        lower.append(map(float, infile.readline().split()))
    for j in xrange(U):
        upper.append(map(float, infile.readline().split()))
    result = get_cuts(lower, upper, G)
    return result


def func_35a100d212d041d9946026646fa91f20(upper, infile, U, lower, i, G):
    for j in xrange(U):
        upper.append(map(float, infile.readline().split()))
    result = get_cuts(lower, upper, G)('Case #%d:' % (i + 1,))
    return j


def func_b44e7f19f3924edea944a068381cd9ed(upper, infile, U, lower, i, G):
    for j in xrange(U):
        upper.append(map(float, infile.readline().split()))
    result = get_cuts(lower, upper, G)('Case #%d:' % (i + 1,))
    return result


def func_7eacac7843414d3dafe646f94fd6541c(upper, lower, i, G):
    result = get_cuts(lower, upper, G)('Case #%d:' % (i + 1,))
    for r in result:
        print r
    return result


def func_57ddc6c398cd48a088a169536f119cbe(upper, lower, i, G):
    result = get_cuts(lower, upper, G)('Case #%d:' % (i + 1,))
    for r in result:
        print r
    return r


def func_cf7fa3ecd3e54639b82c4a3a777232f1(infile):
    W, L, U, G = map(int, infile.readline().split())
    lower = []
    upper = []
    for j in xrange(L):
        lower.append(map(float, infile.readline().split()))
    return upper


def func_8ca7cbc9db7545c69c2de7e686300390(infile):
    W, L, U, G = map(int, infile.readline().split())
    lower = []
    upper = []
    for j in xrange(L):
        lower.append(map(float, infile.readline().split()))
    return j


def func_00622b59f2744d3680fdf33f4d789c80(infile):
    W, L, U, G = map(int, infile.readline().split())
    lower = []
    upper = []
    for j in xrange(L):
        lower.append(map(float, infile.readline().split()))
    return lower


def func_0d29969860614dfba87cc5798a5495d1(infile):
    W, L, U, G = map(int, infile.readline().split())
    lower = []
    upper = []
    for j in xrange(L):
        lower.append(map(float, infile.readline().split()))
    return W


def func_f5221ee06136443884ab662ee74214bc(infile):
    W, L, U, G = map(int, infile.readline().split())
    lower = []
    upper = []
    for j in xrange(L):
        lower.append(map(float, infile.readline().split()))
    return L


def func_fa5d3758629749aca42b92d9b48661c3(infile):
    W, L, U, G = map(int, infile.readline().split())
    lower = []
    upper = []
    for j in xrange(L):
        lower.append(map(float, infile.readline().split()))
    return G


def func_09b3121e949a4be48644eec938c8fb1f(infile):
    W, L, U, G = map(int, infile.readline().split())
    lower = []
    upper = []
    for j in xrange(L):
        lower.append(map(float, infile.readline().split()))
    return U


def func_0a67924249fd41908f49d5802e019e3a(infile, U, L):
    lower = []
    upper = []
    for j in xrange(L):
        lower.append(map(float, infile.readline().split()))
    for j in xrange(U):
        upper.append(map(float, infile.readline().split()))
    return lower


def func_bc377ad646c940ff921c88f1c3a2e112(infile, U, L):
    lower = []
    upper = []
    for j in xrange(L):
        lower.append(map(float, infile.readline().split()))
    for j in xrange(U):
        upper.append(map(float, infile.readline().split()))
    return j


def func_df91a02760a3457ebaf242ce3038eb06(infile, U, L):
    lower = []
    upper = []
    for j in xrange(L):
        lower.append(map(float, infile.readline().split()))
    for j in xrange(U):
        upper.append(map(float, infile.readline().split()))
    return upper


def func_d6edc645ae1e45de8ae89ffa551d14b9(infile, U, lower, G, L):
    upper = []
    for j in xrange(L):
        lower.append(map(float, infile.readline().split()))
    for j in xrange(U):
        upper.append(map(float, infile.readline().split()))
    result = get_cuts(lower, upper, G)
    return j


def func_fe58e2a016f844e79b723df041c343ce(infile, U, lower, G, L):
    upper = []
    for j in xrange(L):
        lower.append(map(float, infile.readline().split()))
    for j in xrange(U):
        upper.append(map(float, infile.readline().split()))
    result = get_cuts(lower, upper, G)
    return result


def func_680df7e235494aec995a25feda70811e(infile, U, lower, G, L):
    upper = []
    for j in xrange(L):
        lower.append(map(float, infile.readline().split()))
    for j in xrange(U):
        upper.append(map(float, infile.readline().split()))
    result = get_cuts(lower, upper, G)
    return upper


def func_12f6d2e7b9e34f2294d80ec837bae109(upper, infile, U, lower, i, G, L):
    for j in xrange(L):
        lower.append(map(float, infile.readline().split()))
    for j in xrange(U):
        upper.append(map(float, infile.readline().split()))
    result = get_cuts(lower, upper, G)('Case #%d:' % (i + 1,))
    return j


def func_7f614533679f486882037a7c9f1dee27(upper, infile, U, lower, i, G, L):
    for j in xrange(L):
        lower.append(map(float, infile.readline().split()))
    for j in xrange(U):
        upper.append(map(float, infile.readline().split()))
    result = get_cuts(lower, upper, G)('Case #%d:' % (i + 1,))
    return result


def func_b2f82a5d39464369ab32016ea03cac3c(upper, infile, U, lower, i, G):
    for j in xrange(U):
        upper.append(map(float, infile.readline().split()))
    result = get_cuts(lower, upper, G)('Case #%d:' % (i + 1,))
    for r in result:
        print r
    return result


def func_ecb42c4154ad413284ace9564d45bd17(upper, infile, U, lower, i, G):
    for j in xrange(U):
        upper.append(map(float, infile.readline().split()))
    result = get_cuts(lower, upper, G)('Case #%d:' % (i + 1,))
    for r in result:
        print r
    return r


def func_5adf9e5247134cd7b9190d0839c0be02(upper, infile, U, lower, i, G):
    for j in xrange(U):
        upper.append(map(float, infile.readline().split()))
    result = get_cuts(lower, upper, G)('Case #%d:' % (i + 1,))
    for r in result:
        print r
    return j


def func_4be4bfc0f7c54ce1a51de4a283350ed5(infile):
    W, L, U, G = map(int, infile.readline().split())
    lower = []
    upper = []
    for j in xrange(L):
        lower.append(map(float, infile.readline().split()))
    for j in xrange(U):
        upper.append(map(float, infile.readline().split()))
    return L


def func_12211c2522e74a929e8dd71466d01b3f(infile):
    W, L, U, G = map(int, infile.readline().split())
    lower = []
    upper = []
    for j in xrange(L):
        lower.append(map(float, infile.readline().split()))
    for j in xrange(U):
        upper.append(map(float, infile.readline().split()))
    return G


def func_d4c0aa80a078438eb61819dd0d8ef972(infile):
    W, L, U, G = map(int, infile.readline().split())
    lower = []
    upper = []
    for j in xrange(L):
        lower.append(map(float, infile.readline().split()))
    for j in xrange(U):
        upper.append(map(float, infile.readline().split()))
    return U


def func_b3fe6e7bb5a449ebba68e713bc8d4af0(infile):
    W, L, U, G = map(int, infile.readline().split())
    lower = []
    upper = []
    for j in xrange(L):
        lower.append(map(float, infile.readline().split()))
    for j in xrange(U):
        upper.append(map(float, infile.readline().split()))
    return lower


def func_fff5db50d3b04eff92cadab056813b6a(infile):
    W, L, U, G = map(int, infile.readline().split())
    lower = []
    upper = []
    for j in xrange(L):
        lower.append(map(float, infile.readline().split()))
    for j in xrange(U):
        upper.append(map(float, infile.readline().split()))
    return W


def func_6acd221046ca4395ba68b912f00ccc03(infile):
    W, L, U, G = map(int, infile.readline().split())
    lower = []
    upper = []
    for j in xrange(L):
        lower.append(map(float, infile.readline().split()))
    for j in xrange(U):
        upper.append(map(float, infile.readline().split()))
    return upper


def func_52d9697b7b55402f8f82115f178ee155(infile):
    W, L, U, G = map(int, infile.readline().split())
    lower = []
    upper = []
    for j in xrange(L):
        lower.append(map(float, infile.readline().split()))
    for j in xrange(U):
        upper.append(map(float, infile.readline().split()))
    return j


def func_6b531f8da76f4acda8f390018d254fae(infile, U, G, L):
    lower = []
    upper = []
    for j in xrange(L):
        lower.append(map(float, infile.readline().split()))
    for j in xrange(U):
        upper.append(map(float, infile.readline().split()))
    result = get_cuts(lower, upper, G)
    return upper


def func_a75a59a6f1db4b898da6e80335cd206c(infile, U, G, L):
    lower = []
    upper = []
    for j in xrange(L):
        lower.append(map(float, infile.readline().split()))
    for j in xrange(U):
        upper.append(map(float, infile.readline().split()))
    result = get_cuts(lower, upper, G)
    return j


def func_eba324cb929a4450a3f1915ee7b82b93(infile, U, G, L):
    lower = []
    upper = []
    for j in xrange(L):
        lower.append(map(float, infile.readline().split()))
    for j in xrange(U):
        upper.append(map(float, infile.readline().split()))
    result = get_cuts(lower, upper, G)
    return result


def func_3cd04026451642ff8df1bf0e1d3bdf16(infile, U, G, L):
    lower = []
    upper = []
    for j in xrange(L):
        lower.append(map(float, infile.readline().split()))
    for j in xrange(U):
        upper.append(map(float, infile.readline().split()))
    result = get_cuts(lower, upper, G)
    return lower


def func_a4943a91dc4c43f3878a561ba7d74ad8(infile, U, lower, i, G, L):
    upper = []
    for j in xrange(L):
        lower.append(map(float, infile.readline().split()))
    for j in xrange(U):
        upper.append(map(float, infile.readline().split()))
    result = get_cuts(lower, upper, G)('Case #%d:' % (i + 1,))
    return upper


def func_28e2e5d733d845ebb2b8b5c0959777d8(infile, U, lower, i, G, L):
    upper = []
    for j in xrange(L):
        lower.append(map(float, infile.readline().split()))
    for j in xrange(U):
        upper.append(map(float, infile.readline().split()))
    result = get_cuts(lower, upper, G)('Case #%d:' % (i + 1,))
    return result


def func_dc2460ec1d1340b9aa99a60ba0f6dcff(infile, U, lower, i, G, L):
    upper = []
    for j in xrange(L):
        lower.append(map(float, infile.readline().split()))
    for j in xrange(U):
        upper.append(map(float, infile.readline().split()))
    result = get_cuts(lower, upper, G)('Case #%d:' % (i + 1,))
    return j


def func_9f8e605909f64cb28023a7bb7d5feb37(upper, infile, U, lower, i, G, L):
    for j in xrange(L):
        lower.append(map(float, infile.readline().split()))
    for j in xrange(U):
        upper.append(map(float, infile.readline().split()))
    result = get_cuts(lower, upper, G)('Case #%d:' % (i + 1,))
    for r in result:
        print r
    return r


def func_34a082c6c44a469b882dfce01e9b8449(upper, infile, U, lower, i, G, L):
    for j in xrange(L):
        lower.append(map(float, infile.readline().split()))
    for j in xrange(U):
        upper.append(map(float, infile.readline().split()))
    result = get_cuts(lower, upper, G)('Case #%d:' % (i + 1,))
    for r in result:
        print r
    return result


def func_435233b1fc81420aa1a6cdd318311c0e(upper, infile, U, lower, i, G, L):
    for j in xrange(L):
        lower.append(map(float, infile.readline().split()))
    for j in xrange(U):
        upper.append(map(float, infile.readline().split()))
    result = get_cuts(lower, upper, G)('Case #%d:' % (i + 1,))
    for r in result:
        print r
    return j


def func_537d7c1ed53e4d62be3602f4f6102295(infile):
    W, L, U, G = map(int, infile.readline().split())
    lower = []
    upper = []
    for j in xrange(L):
        lower.append(map(float, infile.readline().split()))
    for j in xrange(U):
        upper.append(map(float, infile.readline().split()))
    result = get_cuts(lower, upper, G)
    return upper


def func_171f1b90a09b456ca225672ea479a753(infile):
    W, L, U, G = map(int, infile.readline().split())
    lower = []
    upper = []
    for j in xrange(L):
        lower.append(map(float, infile.readline().split()))
    for j in xrange(U):
        upper.append(map(float, infile.readline().split()))
    result = get_cuts(lower, upper, G)
    return W


def func_a260516cb61b44efafd8331addc6450f(infile):
    W, L, U, G = map(int, infile.readline().split())
    lower = []
    upper = []
    for j in xrange(L):
        lower.append(map(float, infile.readline().split()))
    for j in xrange(U):
        upper.append(map(float, infile.readline().split()))
    result = get_cuts(lower, upper, G)
    return U


def func_5e978c1f472146cf854550ac62f0be7e(infile):
    W, L, U, G = map(int, infile.readline().split())
    lower = []
    upper = []
    for j in xrange(L):
        lower.append(map(float, infile.readline().split()))
    for j in xrange(U):
        upper.append(map(float, infile.readline().split()))
    result = get_cuts(lower, upper, G)
    return G


def func_840018fafa7d49f2bc1af10f840486f9(infile):
    W, L, U, G = map(int, infile.readline().split())
    lower = []
    upper = []
    for j in xrange(L):
        lower.append(map(float, infile.readline().split()))
    for j in xrange(U):
        upper.append(map(float, infile.readline().split()))
    result = get_cuts(lower, upper, G)
    return L


def func_0901e9fdfa164f48ace1e06f7f18aac7(infile):
    W, L, U, G = map(int, infile.readline().split())
    lower = []
    upper = []
    for j in xrange(L):
        lower.append(map(float, infile.readline().split()))
    for j in xrange(U):
        upper.append(map(float, infile.readline().split()))
    result = get_cuts(lower, upper, G)
    return j


def func_b32ad3d1c27d4947a47975f7e3721bc1(infile):
    W, L, U, G = map(int, infile.readline().split())
    lower = []
    upper = []
    for j in xrange(L):
        lower.append(map(float, infile.readline().split()))
    for j in xrange(U):
        upper.append(map(float, infile.readline().split()))
    result = get_cuts(lower, upper, G)
    return result


def func_12a964c91bc54fbbaed0cbc9e801b451(infile):
    W, L, U, G = map(int, infile.readline().split())
    lower = []
    upper = []
    for j in xrange(L):
        lower.append(map(float, infile.readline().split()))
    for j in xrange(U):
        upper.append(map(float, infile.readline().split()))
    result = get_cuts(lower, upper, G)
    return lower


def func_1a1be1054a9949699cbc189336e460ef(infile, U, i, G, L):
    lower = []
    upper = []
    for j in xrange(L):
        lower.append(map(float, infile.readline().split()))
    for j in xrange(U):
        upper.append(map(float, infile.readline().split()))
    result = get_cuts(lower, upper, G)('Case #%d:' % (i + 1,))
    return lower


def func_561243cdfded464e952993b60533aeec(infile, U, i, G, L):
    lower = []
    upper = []
    for j in xrange(L):
        lower.append(map(float, infile.readline().split()))
    for j in xrange(U):
        upper.append(map(float, infile.readline().split()))
    result = get_cuts(lower, upper, G)('Case #%d:' % (i + 1,))
    return j


def func_2115e2c16be84867a8a212942ba4e770(infile, U, i, G, L):
    lower = []
    upper = []
    for j in xrange(L):
        lower.append(map(float, infile.readline().split()))
    for j in xrange(U):
        upper.append(map(float, infile.readline().split()))
    result = get_cuts(lower, upper, G)('Case #%d:' % (i + 1,))
    return upper


def func_4f23c6e5a133499fa97e4b2b032482b8(infile, U, i, G, L):
    lower = []
    upper = []
    for j in xrange(L):
        lower.append(map(float, infile.readline().split()))
    for j in xrange(U):
        upper.append(map(float, infile.readline().split()))
    result = get_cuts(lower, upper, G)('Case #%d:' % (i + 1,))
    return result


def func_3f55fddc1e3342d8b913270f5ac70c97(infile, U, lower, i, G, L):
    upper = []
    for j in xrange(L):
        lower.append(map(float, infile.readline().split()))
    for j in xrange(U):
        upper.append(map(float, infile.readline().split()))
    result = get_cuts(lower, upper, G)('Case #%d:' % (i + 1,))
    for r in result:
        print r
    return r


def func_8ac1476e631645b888f1cf76913d5e93(infile, U, lower, i, G, L):
    upper = []
    for j in xrange(L):
        lower.append(map(float, infile.readline().split()))
    for j in xrange(U):
        upper.append(map(float, infile.readline().split()))
    result = get_cuts(lower, upper, G)('Case #%d:' % (i + 1,))
    for r in result:
        print r
    return result


def func_34e92b493e0d4154bb9909dd7bf6d497(infile, U, lower, i, G, L):
    upper = []
    for j in xrange(L):
        lower.append(map(float, infile.readline().split()))
    for j in xrange(U):
        upper.append(map(float, infile.readline().split()))
    result = get_cuts(lower, upper, G)('Case #%d:' % (i + 1,))
    for r in result:
        print r
    return j


def func_cb96db8b94604ab88daf2cfdc3390940(infile, U, lower, i, G, L):
    upper = []
    for j in xrange(L):
        lower.append(map(float, infile.readline().split()))
    for j in xrange(U):
        upper.append(map(float, infile.readline().split()))
    result = get_cuts(lower, upper, G)('Case #%d:' % (i + 1,))
    for r in result:
        print r
    return upper


def func_2d9736a3a4bf46be866821af9e328870(infile, i):
    W, L, U, G = map(int, infile.readline().split())
    lower = []
    upper = []
    for j in xrange(L):
        lower.append(map(float, infile.readline().split()))
    for j in xrange(U):
        upper.append(map(float, infile.readline().split()))
    result = get_cuts(lower, upper, G)('Case #%d:' % (i + 1,))
    return U


def func_ce4dad4b3b2b46f3870a751eb8dfb9fa(infile, i):
    W, L, U, G = map(int, infile.readline().split())
    lower = []
    upper = []
    for j in xrange(L):
        lower.append(map(float, infile.readline().split()))
    for j in xrange(U):
        upper.append(map(float, infile.readline().split()))
    result = get_cuts(lower, upper, G)('Case #%d:' % (i + 1,))
    return upper


def func_c4c317c492f2457096b4e68c1085d240(infile, i):
    W, L, U, G = map(int, infile.readline().split())
    lower = []
    upper = []
    for j in xrange(L):
        lower.append(map(float, infile.readline().split()))
    for j in xrange(U):
        upper.append(map(float, infile.readline().split()))
    result = get_cuts(lower, upper, G)('Case #%d:' % (i + 1,))
    return j


def func_48eca5218fe0491bb8318637305e428d(infile, i):
    W, L, U, G = map(int, infile.readline().split())
    lower = []
    upper = []
    for j in xrange(L):
        lower.append(map(float, infile.readline().split()))
    for j in xrange(U):
        upper.append(map(float, infile.readline().split()))
    result = get_cuts(lower, upper, G)('Case #%d:' % (i + 1,))
    return W


def func_c537ea5a98b843c483385813b5580105(infile, i):
    W, L, U, G = map(int, infile.readline().split())
    lower = []
    upper = []
    for j in xrange(L):
        lower.append(map(float, infile.readline().split()))
    for j in xrange(U):
        upper.append(map(float, infile.readline().split()))
    result = get_cuts(lower, upper, G)('Case #%d:' % (i + 1,))
    return L


def func_60859f79464c42dea9b45c05d7b0deb2(infile, i):
    W, L, U, G = map(int, infile.readline().split())
    lower = []
    upper = []
    for j in xrange(L):
        lower.append(map(float, infile.readline().split()))
    for j in xrange(U):
        upper.append(map(float, infile.readline().split()))
    result = get_cuts(lower, upper, G)('Case #%d:' % (i + 1,))
    return G


def func_8257864126c847a8835be2a1298c9177(infile, i):
    W, L, U, G = map(int, infile.readline().split())
    lower = []
    upper = []
    for j in xrange(L):
        lower.append(map(float, infile.readline().split()))
    for j in xrange(U):
        upper.append(map(float, infile.readline().split()))
    result = get_cuts(lower, upper, G)('Case #%d:' % (i + 1,))
    return lower


def func_a14785910a4542ce93409e3497460c38(infile, i):
    W, L, U, G = map(int, infile.readline().split())
    lower = []
    upper = []
    for j in xrange(L):
        lower.append(map(float, infile.readline().split()))
    for j in xrange(U):
        upper.append(map(float, infile.readline().split()))
    result = get_cuts(lower, upper, G)('Case #%d:' % (i + 1,))
    return result


def func_7f2f90e0469f4876a9de9b9b509e7419(infile, U, i, G, L):
    lower = []
    upper = []
    for j in xrange(L):
        lower.append(map(float, infile.readline().split()))
    for j in xrange(U):
        upper.append(map(float, infile.readline().split()))
    result = get_cuts(lower, upper, G)('Case #%d:' % (i + 1,))
    for r in result:
        print r
    return j


def func_265015a5b06746ec9510fc000b103f73(infile, U, i, G, L):
    lower = []
    upper = []
    for j in xrange(L):
        lower.append(map(float, infile.readline().split()))
    for j in xrange(U):
        upper.append(map(float, infile.readline().split()))
    result = get_cuts(lower, upper, G)('Case #%d:' % (i + 1,))
    for r in result:
        print r
    return r


def func_99a9f4c377484e9a8a6bbb07dd2d652b(infile, U, i, G, L):
    lower = []
    upper = []
    for j in xrange(L):
        lower.append(map(float, infile.readline().split()))
    for j in xrange(U):
        upper.append(map(float, infile.readline().split()))
    result = get_cuts(lower, upper, G)('Case #%d:' % (i + 1,))
    for r in result:
        print r
    return result


def func_68240da46d7f4ae687d5553519283e26(infile, U, i, G, L):
    lower = []
    upper = []
    for j in xrange(L):
        lower.append(map(float, infile.readline().split()))
    for j in xrange(U):
        upper.append(map(float, infile.readline().split()))
    result = get_cuts(lower, upper, G)('Case #%d:' % (i + 1,))
    for r in result:
        print r
    return upper


def func_f80f60de0cb5488db3c134e3a40432b3(infile, U, i, G, L):
    lower = []
    upper = []
    for j in xrange(L):
        lower.append(map(float, infile.readline().split()))
    for j in xrange(U):
        upper.append(map(float, infile.readline().split()))
    result = get_cuts(lower, upper, G)('Case #%d:' % (i + 1,))
    for r in result:
        print r
    return lower


def func_a226fe08a7b148f49be89c6732363243(infile, i):
    W, L, U, G = map(int, infile.readline().split())
    lower = []
    upper = []
    for j in xrange(L):
        lower.append(map(float, infile.readline().split()))
    for j in xrange(U):
        upper.append(map(float, infile.readline().split()))
    result = get_cuts(lower, upper, G)('Case #%d:' % (i + 1,))
    for r in result:
        print r
    return L


def func_2f4e173efd1042db80a33a087d29327c(infile, i):
    W, L, U, G = map(int, infile.readline().split())
    lower = []
    upper = []
    for j in xrange(L):
        lower.append(map(float, infile.readline().split()))
    for j in xrange(U):
        upper.append(map(float, infile.readline().split()))
    result = get_cuts(lower, upper, G)('Case #%d:' % (i + 1,))
    for r in result:
        print r
    return U


def func_0e451aa7d50f48448f92417ac85cdbce(infile, i):
    W, L, U, G = map(int, infile.readline().split())
    lower = []
    upper = []
    for j in xrange(L):
        lower.append(map(float, infile.readline().split()))
    for j in xrange(U):
        upper.append(map(float, infile.readline().split()))
    result = get_cuts(lower, upper, G)('Case #%d:' % (i + 1,))
    for r in result:
        print r
    return j


def func_06f09ba744774122be077b91635aa29e(infile, i):
    W, L, U, G = map(int, infile.readline().split())
    lower = []
    upper = []
    for j in xrange(L):
        lower.append(map(float, infile.readline().split()))
    for j in xrange(U):
        upper.append(map(float, infile.readline().split()))
    result = get_cuts(lower, upper, G)('Case #%d:' % (i + 1,))
    for r in result:
        print r
    return G


def func_70d88a6833f1431b92eec6d8291ec2a1(infile, i):
    W, L, U, G = map(int, infile.readline().split())
    lower = []
    upper = []
    for j in xrange(L):
        lower.append(map(float, infile.readline().split()))
    for j in xrange(U):
        upper.append(map(float, infile.readline().split()))
    result = get_cuts(lower, upper, G)('Case #%d:' % (i + 1,))
    for r in result:
        print r
    return r


def func_d8b2f965d5944f679a6359c1ffbeda97(infile, i):
    W, L, U, G = map(int, infile.readline().split())
    lower = []
    upper = []
    for j in xrange(L):
        lower.append(map(float, infile.readline().split()))
    for j in xrange(U):
        upper.append(map(float, infile.readline().split()))
    result = get_cuts(lower, upper, G)('Case #%d:' % (i + 1,))
    for r in result:
        print r
    return upper


def func_dd47f6def49e4f69990e06695c6b7cc1(infile, i):
    W, L, U, G = map(int, infile.readline().split())
    lower = []
    upper = []
    for j in xrange(L):
        lower.append(map(float, infile.readline().split()))
    for j in xrange(U):
        upper.append(map(float, infile.readline().split()))
    result = get_cuts(lower, upper, G)('Case #%d:' % (i + 1,))
    for r in result:
        print r
    return result


def func_409fdd14800643bcac1f459b027ee722(infile, i):
    W, L, U, G = map(int, infile.readline().split())
    lower = []
    upper = []
    for j in xrange(L):
        lower.append(map(float, infile.readline().split()))
    for j in xrange(U):
        upper.append(map(float, infile.readline().split()))
    result = get_cuts(lower, upper, G)('Case #%d:' % (i + 1,))
    for r in result:
        print r
    return lower


def func_e101ec91276a49ca9539f8400d8943e1(infile, i):
    W, L, U, G = map(int, infile.readline().split())
    lower = []
    upper = []
    for j in xrange(L):
        lower.append(map(float, infile.readline().split()))
    for j in xrange(U):
        upper.append(map(float, infile.readline().split()))
    result = get_cuts(lower, upper, G)('Case #%d:' % (i + 1,))
    for r in result:
        print r
    return W


def func_799542d1d777436dab8043bb6ce5d269():
    infile = open('codejam/test_files/Y11R5P1/A.in')
    T = int(infile.readline())
    return infile


def func_c8a117bf814945af8a262eabaae8f314():
    infile = open('codejam/test_files/Y11R5P1/A.in')
    T = int(infile.readline())
    return T


def func_19bb57bfd34a46cc8337e91906e66533(infile):
    T = int(infile.readline())
    for i in xrange(T):
        W, L, U, G = map(int, infile.readline().split())
        lower = []
        upper = []
        for j in xrange(L):
            lower.append(map(float, infile.readline().split()))
        for j in xrange(U):
            upper.append(map(float, infile.readline().split()))
        result = get_cuts(lower, upper, G)
        print 'Case #%d:' % (i + 1,)
        for r in result:
            print r
    return lower


def func_7371948bda084855a3ecff1f71aac2b4(infile):
    T = int(infile.readline())
    for i in xrange(T):
        W, L, U, G = map(int, infile.readline().split())
        lower = []
        upper = []
        for j in xrange(L):
            lower.append(map(float, infile.readline().split()))
        for j in xrange(U):
            upper.append(map(float, infile.readline().split()))
        result = get_cuts(lower, upper, G)
        print 'Case #%d:' % (i + 1,)
        for r in result:
            print r
    return result


def func_592e55c47efb43ba9168c557b58d233f(infile):
    T = int(infile.readline())
    for i in xrange(T):
        W, L, U, G = map(int, infile.readline().split())
        lower = []
        upper = []
        for j in xrange(L):
            lower.append(map(float, infile.readline().split()))
        for j in xrange(U):
            upper.append(map(float, infile.readline().split()))
        result = get_cuts(lower, upper, G)
        print 'Case #%d:' % (i + 1,)
        for r in result:
            print r
    return W


def func_320aa3228cf0422eb6bbdb5959e0308c(infile):
    T = int(infile.readline())
    for i in xrange(T):
        W, L, U, G = map(int, infile.readline().split())
        lower = []
        upper = []
        for j in xrange(L):
            lower.append(map(float, infile.readline().split()))
        for j in xrange(U):
            upper.append(map(float, infile.readline().split()))
        result = get_cuts(lower, upper, G)
        print 'Case #%d:' % (i + 1,)
        for r in result:
            print r
    return r


def func_ec7fe42cb86f423398e2284b0852f975(infile):
    T = int(infile.readline())
    for i in xrange(T):
        W, L, U, G = map(int, infile.readline().split())
        lower = []
        upper = []
        for j in xrange(L):
            lower.append(map(float, infile.readline().split()))
        for j in xrange(U):
            upper.append(map(float, infile.readline().split()))
        result = get_cuts(lower, upper, G)
        print 'Case #%d:' % (i + 1,)
        for r in result:
            print r
    return j


def func_21b043b597db4d5eb122eaf621f491cb(infile):
    T = int(infile.readline())
    for i in xrange(T):
        W, L, U, G = map(int, infile.readline().split())
        lower = []
        upper = []
        for j in xrange(L):
            lower.append(map(float, infile.readline().split()))
        for j in xrange(U):
            upper.append(map(float, infile.readline().split()))
        result = get_cuts(lower, upper, G)
        print 'Case #%d:' % (i + 1,)
        for r in result:
            print r
    return L


def func_e8eaf83224a447b3b00d4dcc42e493be(infile):
    T = int(infile.readline())
    for i in xrange(T):
        W, L, U, G = map(int, infile.readline().split())
        lower = []
        upper = []
        for j in xrange(L):
            lower.append(map(float, infile.readline().split()))
        for j in xrange(U):
            upper.append(map(float, infile.readline().split()))
        result = get_cuts(lower, upper, G)
        print 'Case #%d:' % (i + 1,)
        for r in result:
            print r
    return T


def func_0902df9499c2461c99d49877168832ca(infile):
    T = int(infile.readline())
    for i in xrange(T):
        W, L, U, G = map(int, infile.readline().split())
        lower = []
        upper = []
        for j in xrange(L):
            lower.append(map(float, infile.readline().split()))
        for j in xrange(U):
            upper.append(map(float, infile.readline().split()))
        result = get_cuts(lower, upper, G)
        print 'Case #%d:' % (i + 1,)
        for r in result:
            print r
    return upper


def func_afb14cdd87d240a0bdbbd6e51fe9522b(infile):
    T = int(infile.readline())
    for i in xrange(T):
        W, L, U, G = map(int, infile.readline().split())
        lower = []
        upper = []
        for j in xrange(L):
            lower.append(map(float, infile.readline().split()))
        for j in xrange(U):
            upper.append(map(float, infile.readline().split()))
        result = get_cuts(lower, upper, G)
        print 'Case #%d:' % (i + 1,)
        for r in result:
            print r
    return G


def func_34e81dd622214f92b5edd045ccbf60b5(infile):
    T = int(infile.readline())
    for i in xrange(T):
        W, L, U, G = map(int, infile.readline().split())
        lower = []
        upper = []
        for j in xrange(L):
            lower.append(map(float, infile.readline().split()))
        for j in xrange(U):
            upper.append(map(float, infile.readline().split()))
        result = get_cuts(lower, upper, G)
        print 'Case #%d:' % (i + 1,)
        for r in result:
            print r
    return U


def func_cd64a9eecc5f4778b5beb8edd7fad4c8(infile):
    T = int(infile.readline())
    for i in xrange(T):
        W, L, U, G = map(int, infile.readline().split())
        lower = []
        upper = []
        for j in xrange(L):
            lower.append(map(float, infile.readline().split()))
        for j in xrange(U):
            upper.append(map(float, infile.readline().split()))
        result = get_cuts(lower, upper, G)
        print 'Case #%d:' % (i + 1,)
        for r in result:
            print r
    return i


def func_89386cd01b4c4480815d0230c53ae7bd(infile, T):
    for i in xrange(T):
        W, L, U, G = map(int, infile.readline().split())
        lower = []
        upper = []
        for j in xrange(L):
            lower.append(map(float, infile.readline().split()))
        for j in xrange(U):
            upper.append(map(float, infile.readline().split()))
        result = get_cuts(lower, upper, G)
        print 'Case #%d:' % (i + 1,)
        for r in result:
            print r
    infile.close()
    return G


def func_8a6cc42a8ac94e698c9a0c49c64eca2e(infile, T):
    for i in xrange(T):
        W, L, U, G = map(int, infile.readline().split())
        lower = []
        upper = []
        for j in xrange(L):
            lower.append(map(float, infile.readline().split()))
        for j in xrange(U):
            upper.append(map(float, infile.readline().split()))
        result = get_cuts(lower, upper, G)
        print 'Case #%d:' % (i + 1,)
        for r in result:
            print r
    infile.close()
    return lower


def func_0455ee9d3aef405e8d9340aac4e8411f(infile, T):
    for i in xrange(T):
        W, L, U, G = map(int, infile.readline().split())
        lower = []
        upper = []
        for j in xrange(L):
            lower.append(map(float, infile.readline().split()))
        for j in xrange(U):
            upper.append(map(float, infile.readline().split()))
        result = get_cuts(lower, upper, G)
        print 'Case #%d:' % (i + 1,)
        for r in result:
            print r
    infile.close()
    return r


def func_2f898563e9f64c079d18d99cef97f6ce(infile, T):
    for i in xrange(T):
        W, L, U, G = map(int, infile.readline().split())
        lower = []
        upper = []
        for j in xrange(L):
            lower.append(map(float, infile.readline().split()))
        for j in xrange(U):
            upper.append(map(float, infile.readline().split()))
        result = get_cuts(lower, upper, G)
        print 'Case #%d:' % (i + 1,)
        for r in result:
            print r
    infile.close()
    return L


def func_361b6f20d71742b2a215a18a30f8516f(infile, T):
    for i in xrange(T):
        W, L, U, G = map(int, infile.readline().split())
        lower = []
        upper = []
        for j in xrange(L):
            lower.append(map(float, infile.readline().split()))
        for j in xrange(U):
            upper.append(map(float, infile.readline().split()))
        result = get_cuts(lower, upper, G)
        print 'Case #%d:' % (i + 1,)
        for r in result:
            print r
    infile.close()
    return U


def func_8370f37d814449b9b8643683457d26ee(infile, T):
    for i in xrange(T):
        W, L, U, G = map(int, infile.readline().split())
        lower = []
        upper = []
        for j in xrange(L):
            lower.append(map(float, infile.readline().split()))
        for j in xrange(U):
            upper.append(map(float, infile.readline().split()))
        result = get_cuts(lower, upper, G)
        print 'Case #%d:' % (i + 1,)
        for r in result:
            print r
    infile.close()
    return W


def func_20cf55fb83c940d0b045fa2125c2343c(infile, T):
    for i in xrange(T):
        W, L, U, G = map(int, infile.readline().split())
        lower = []
        upper = []
        for j in xrange(L):
            lower.append(map(float, infile.readline().split()))
        for j in xrange(U):
            upper.append(map(float, infile.readline().split()))
        result = get_cuts(lower, upper, G)
        print 'Case #%d:' % (i + 1,)
        for r in result:
            print r
    infile.close()
    return result


def func_f1bcb2b6496548e29d0dab39e593589e(infile, T):
    for i in xrange(T):
        W, L, U, G = map(int, infile.readline().split())
        lower = []
        upper = []
        for j in xrange(L):
            lower.append(map(float, infile.readline().split()))
        for j in xrange(U):
            upper.append(map(float, infile.readline().split()))
        result = get_cuts(lower, upper, G)
        print 'Case #%d:' % (i + 1,)
        for r in result:
            print r
    infile.close()
    return j


def func_898546ded4dd4c858529c5db66b4d067(infile, T):
    for i in xrange(T):
        W, L, U, G = map(int, infile.readline().split())
        lower = []
        upper = []
        for j in xrange(L):
            lower.append(map(float, infile.readline().split()))
        for j in xrange(U):
            upper.append(map(float, infile.readline().split()))
        result = get_cuts(lower, upper, G)
        print 'Case #%d:' % (i + 1,)
        for r in result:
            print r
    infile.close()
    return i


def func_4807ad0ff9864e7e8fda23f6346c40ee(infile, T):
    for i in xrange(T):
        W, L, U, G = map(int, infile.readline().split())
        lower = []
        upper = []
        for j in xrange(L):
            lower.append(map(float, infile.readline().split()))
        for j in xrange(U):
            upper.append(map(float, infile.readline().split()))
        result = get_cuts(lower, upper, G)
        print 'Case #%d:' % (i + 1,)
        for r in result:
            print r
    infile.close()
    return upper


def func_3c2f65d29c10478298f4ed9240e651c0():
    infile = open('codejam/test_files/Y11R5P1/A.in')
    T = int(infile.readline())
    for i in xrange(T):
        W, L, U, G = map(int, infile.readline().split())
        lower = []
        upper = []
        for j in xrange(L):
            lower.append(map(float, infile.readline().split()))
        for j in xrange(U):
            upper.append(map(float, infile.readline().split()))
        result = get_cuts(lower, upper, G)
        print 'Case #%d:' % (i + 1,)
        for r in result:
            print r
    return U


def func_e4265611aa2347639d02fe4c6f1fb420():
    infile = open('codejam/test_files/Y11R5P1/A.in')
    T = int(infile.readline())
    for i in xrange(T):
        W, L, U, G = map(int, infile.readline().split())
        lower = []
        upper = []
        for j in xrange(L):
            lower.append(map(float, infile.readline().split()))
        for j in xrange(U):
            upper.append(map(float, infile.readline().split()))
        result = get_cuts(lower, upper, G)
        print 'Case #%d:' % (i + 1,)
        for r in result:
            print r
    return i


def func_c64284022a744b6b9b551d21928851f2():
    infile = open('codejam/test_files/Y11R5P1/A.in')
    T = int(infile.readline())
    for i in xrange(T):
        W, L, U, G = map(int, infile.readline().split())
        lower = []
        upper = []
        for j in xrange(L):
            lower.append(map(float, infile.readline().split()))
        for j in xrange(U):
            upper.append(map(float, infile.readline().split()))
        result = get_cuts(lower, upper, G)
        print 'Case #%d:' % (i + 1,)
        for r in result:
            print r
    return j


def func_826ac01168934564a5d5d5825efbb2bc():
    infile = open('codejam/test_files/Y11R5P1/A.in')
    T = int(infile.readline())
    for i in xrange(T):
        W, L, U, G = map(int, infile.readline().split())
        lower = []
        upper = []
        for j in xrange(L):
            lower.append(map(float, infile.readline().split()))
        for j in xrange(U):
            upper.append(map(float, infile.readline().split()))
        result = get_cuts(lower, upper, G)
        print 'Case #%d:' % (i + 1,)
        for r in result:
            print r
    return result


def func_7f07bdb26b8343dcbf46d8416ded674e():
    infile = open('codejam/test_files/Y11R5P1/A.in')
    T = int(infile.readline())
    for i in xrange(T):
        W, L, U, G = map(int, infile.readline().split())
        lower = []
        upper = []
        for j in xrange(L):
            lower.append(map(float, infile.readline().split()))
        for j in xrange(U):
            upper.append(map(float, infile.readline().split()))
        result = get_cuts(lower, upper, G)
        print 'Case #%d:' % (i + 1,)
        for r in result:
            print r
    return upper


def func_7b515b06d4d04fd88001cbffc371eec6():
    infile = open('codejam/test_files/Y11R5P1/A.in')
    T = int(infile.readline())
    for i in xrange(T):
        W, L, U, G = map(int, infile.readline().split())
        lower = []
        upper = []
        for j in xrange(L):
            lower.append(map(float, infile.readline().split()))
        for j in xrange(U):
            upper.append(map(float, infile.readline().split()))
        result = get_cuts(lower, upper, G)
        print 'Case #%d:' % (i + 1,)
        for r in result:
            print r
    return G


def func_9204ad7b39e441b1b367564a6dfbf65c():
    infile = open('codejam/test_files/Y11R5P1/A.in')
    T = int(infile.readline())
    for i in xrange(T):
        W, L, U, G = map(int, infile.readline().split())
        lower = []
        upper = []
        for j in xrange(L):
            lower.append(map(float, infile.readline().split()))
        for j in xrange(U):
            upper.append(map(float, infile.readline().split()))
        result = get_cuts(lower, upper, G)
        print 'Case #%d:' % (i + 1,)
        for r in result:
            print r
    return lower


def func_0a3b6f4dd37a4c3c88eca08df048fcd5():
    infile = open('codejam/test_files/Y11R5P1/A.in')
    T = int(infile.readline())
    for i in xrange(T):
        W, L, U, G = map(int, infile.readline().split())
        lower = []
        upper = []
        for j in xrange(L):
            lower.append(map(float, infile.readline().split()))
        for j in xrange(U):
            upper.append(map(float, infile.readline().split()))
        result = get_cuts(lower, upper, G)
        print 'Case #%d:' % (i + 1,)
        for r in result:
            print r
    return L


def func_c76dbc11c6ee441c8ed3e17ff7c4225a():
    infile = open('codejam/test_files/Y11R5P1/A.in')
    T = int(infile.readline())
    for i in xrange(T):
        W, L, U, G = map(int, infile.readline().split())
        lower = []
        upper = []
        for j in xrange(L):
            lower.append(map(float, infile.readline().split()))
        for j in xrange(U):
            upper.append(map(float, infile.readline().split()))
        result = get_cuts(lower, upper, G)
        print 'Case #%d:' % (i + 1,)
        for r in result:
            print r
    return T


def func_62686a46dbbc4eef80cb8a5f4d67f6e7():
    infile = open('codejam/test_files/Y11R5P1/A.in')
    T = int(infile.readline())
    for i in xrange(T):
        W, L, U, G = map(int, infile.readline().split())
        lower = []
        upper = []
        for j in xrange(L):
            lower.append(map(float, infile.readline().split()))
        for j in xrange(U):
            upper.append(map(float, infile.readline().split()))
        result = get_cuts(lower, upper, G)
        print 'Case #%d:' % (i + 1,)
        for r in result:
            print r
    return r


def func_08beae9599f2445aa786363d3f3e74c7():
    infile = open('codejam/test_files/Y11R5P1/A.in')
    T = int(infile.readline())
    for i in xrange(T):
        W, L, U, G = map(int, infile.readline().split())
        lower = []
        upper = []
        for j in xrange(L):
            lower.append(map(float, infile.readline().split()))
        for j in xrange(U):
            upper.append(map(float, infile.readline().split()))
        result = get_cuts(lower, upper, G)
        print 'Case #%d:' % (i + 1,)
        for r in result:
            print r
    return infile


def func_bd10c2e8d82a4c6bb016965a5da43edb():
    infile = open('codejam/test_files/Y11R5P1/A.in')
    T = int(infile.readline())
    for i in xrange(T):
        W, L, U, G = map(int, infile.readline().split())
        lower = []
        upper = []
        for j in xrange(L):
            lower.append(map(float, infile.readline().split()))
        for j in xrange(U):
            upper.append(map(float, infile.readline().split()))
        result = get_cuts(lower, upper, G)
        print 'Case #%d:' % (i + 1,)
        for r in result:
            print r
    return W


def func_0aac6c5d1c774093954907e754d84289(infile):
    T = int(infile.readline())
    for i in xrange(T):
        W, L, U, G = map(int, infile.readline().split())
        lower = []
        upper = []
        for j in xrange(L):
            lower.append(map(float, infile.readline().split()))
        for j in xrange(U):
            upper.append(map(float, infile.readline().split()))
        result = get_cuts(lower, upper, G)
        print 'Case #%d:' % (i + 1,)
        for r in result:
            print r
    infile.close()
    return G


def func_79b8fb12fb5446368372908bc8f48e71(infile):
    T = int(infile.readline())
    for i in xrange(T):
        W, L, U, G = map(int, infile.readline().split())
        lower = []
        upper = []
        for j in xrange(L):
            lower.append(map(float, infile.readline().split()))
        for j in xrange(U):
            upper.append(map(float, infile.readline().split()))
        result = get_cuts(lower, upper, G)
        print 'Case #%d:' % (i + 1,)
        for r in result:
            print r
    infile.close()
    return j


def func_8de7576f77654cbb8ae12c4ba7fbea0c(infile):
    T = int(infile.readline())
    for i in xrange(T):
        W, L, U, G = map(int, infile.readline().split())
        lower = []
        upper = []
        for j in xrange(L):
            lower.append(map(float, infile.readline().split()))
        for j in xrange(U):
            upper.append(map(float, infile.readline().split()))
        result = get_cuts(lower, upper, G)
        print 'Case #%d:' % (i + 1,)
        for r in result:
            print r
    infile.close()
    return upper


def func_6dd14599388642d5b14fbd5fb1254688(infile):
    T = int(infile.readline())
    for i in xrange(T):
        W, L, U, G = map(int, infile.readline().split())
        lower = []
        upper = []
        for j in xrange(L):
            lower.append(map(float, infile.readline().split()))
        for j in xrange(U):
            upper.append(map(float, infile.readline().split()))
        result = get_cuts(lower, upper, G)
        print 'Case #%d:' % (i + 1,)
        for r in result:
            print r
    infile.close()
    return result


def func_b8c35b61c7bf481ca3143283daabeec3(infile):
    T = int(infile.readline())
    for i in xrange(T):
        W, L, U, G = map(int, infile.readline().split())
        lower = []
        upper = []
        for j in xrange(L):
            lower.append(map(float, infile.readline().split()))
        for j in xrange(U):
            upper.append(map(float, infile.readline().split()))
        result = get_cuts(lower, upper, G)
        print 'Case #%d:' % (i + 1,)
        for r in result:
            print r
    infile.close()
    return T


def func_6fcaee8ace8a4324a792ea0fce5d4496(infile):
    T = int(infile.readline())
    for i in xrange(T):
        W, L, U, G = map(int, infile.readline().split())
        lower = []
        upper = []
        for j in xrange(L):
            lower.append(map(float, infile.readline().split()))
        for j in xrange(U):
            upper.append(map(float, infile.readline().split()))
        result = get_cuts(lower, upper, G)
        print 'Case #%d:' % (i + 1,)
        for r in result:
            print r
    infile.close()
    return U


def func_f2a0ad3d66ca4923b97e625c3a116867(infile):
    T = int(infile.readline())
    for i in xrange(T):
        W, L, U, G = map(int, infile.readline().split())
        lower = []
        upper = []
        for j in xrange(L):
            lower.append(map(float, infile.readline().split()))
        for j in xrange(U):
            upper.append(map(float, infile.readline().split()))
        result = get_cuts(lower, upper, G)
        print 'Case #%d:' % (i + 1,)
        for r in result:
            print r
    infile.close()
    return lower


def func_6851ebd60c0a4c37ac0b670d6c6837f0(infile):
    T = int(infile.readline())
    for i in xrange(T):
        W, L, U, G = map(int, infile.readline().split())
        lower = []
        upper = []
        for j in xrange(L):
            lower.append(map(float, infile.readline().split()))
        for j in xrange(U):
            upper.append(map(float, infile.readline().split()))
        result = get_cuts(lower, upper, G)
        print 'Case #%d:' % (i + 1,)
        for r in result:
            print r
    infile.close()
    return W


def func_57f8fe8fbed54f28a960fcd5a41df58d(infile):
    T = int(infile.readline())
    for i in xrange(T):
        W, L, U, G = map(int, infile.readline().split())
        lower = []
        upper = []
        for j in xrange(L):
            lower.append(map(float, infile.readline().split()))
        for j in xrange(U):
            upper.append(map(float, infile.readline().split()))
        result = get_cuts(lower, upper, G)
        print 'Case #%d:' % (i + 1,)
        for r in result:
            print r
    infile.close()
    return L


def func_28d36367668d4e4b916df3526b34f2d2(infile):
    T = int(infile.readline())
    for i in xrange(T):
        W, L, U, G = map(int, infile.readline().split())
        lower = []
        upper = []
        for j in xrange(L):
            lower.append(map(float, infile.readline().split()))
        for j in xrange(U):
            upper.append(map(float, infile.readline().split()))
        result = get_cuts(lower, upper, G)
        print 'Case #%d:' % (i + 1,)
        for r in result:
            print r
    infile.close()
    return i


def func_865f083c13554b25b9148eda1bf0b074(infile):
    T = int(infile.readline())
    for i in xrange(T):
        W, L, U, G = map(int, infile.readline().split())
        lower = []
        upper = []
        for j in xrange(L):
            lower.append(map(float, infile.readline().split()))
        for j in xrange(U):
            upper.append(map(float, infile.readline().split()))
        result = get_cuts(lower, upper, G)
        print 'Case #%d:' % (i + 1,)
        for r in result:
            print r
    infile.close()
    return r


def func_a4982d9ee28b4d03a7ecf944d5d81c02():
    infile = open('codejam/test_files/Y11R5P1/A.in')
    T = int(infile.readline())
    for i in xrange(T):
        W, L, U, G = map(int, infile.readline().split())
        lower = []
        upper = []
        for j in xrange(L):
            lower.append(map(float, infile.readline().split()))
        for j in xrange(U):
            upper.append(map(float, infile.readline().split()))
        result = get_cuts(lower, upper, G)
        print 'Case #%d:' % (i + 1,)
        for r in result:
            print r
    infile.close()
    return L


def func_a43990879b3b4d47a40857878304000f():
    infile = open('codejam/test_files/Y11R5P1/A.in')
    T = int(infile.readline())
    for i in xrange(T):
        W, L, U, G = map(int, infile.readline().split())
        lower = []
        upper = []
        for j in xrange(L):
            lower.append(map(float, infile.readline().split()))
        for j in xrange(U):
            upper.append(map(float, infile.readline().split()))
        result = get_cuts(lower, upper, G)
        print 'Case #%d:' % (i + 1,)
        for r in result:
            print r
    infile.close()
    return infile


def func_12ad727156ef4e5780bc12ca3ae3ab45():
    infile = open('codejam/test_files/Y11R5P1/A.in')
    T = int(infile.readline())
    for i in xrange(T):
        W, L, U, G = map(int, infile.readline().split())
        lower = []
        upper = []
        for j in xrange(L):
            lower.append(map(float, infile.readline().split()))
        for j in xrange(U):
            upper.append(map(float, infile.readline().split()))
        result = get_cuts(lower, upper, G)
        print 'Case #%d:' % (i + 1,)
        for r in result:
            print r
    infile.close()
    return r


def func_5583ae150dff463abdf3a5fa4a6ce2f8():
    infile = open('codejam/test_files/Y11R5P1/A.in')
    T = int(infile.readline())
    for i in xrange(T):
        W, L, U, G = map(int, infile.readline().split())
        lower = []
        upper = []
        for j in xrange(L):
            lower.append(map(float, infile.readline().split()))
        for j in xrange(U):
            upper.append(map(float, infile.readline().split()))
        result = get_cuts(lower, upper, G)
        print 'Case #%d:' % (i + 1,)
        for r in result:
            print r
    infile.close()
    return i


def func_b68b16a54ebb488eab8a972344f85812():
    infile = open('codejam/test_files/Y11R5P1/A.in')
    T = int(infile.readline())
    for i in xrange(T):
        W, L, U, G = map(int, infile.readline().split())
        lower = []
        upper = []
        for j in xrange(L):
            lower.append(map(float, infile.readline().split()))
        for j in xrange(U):
            upper.append(map(float, infile.readline().split()))
        result = get_cuts(lower, upper, G)
        print 'Case #%d:' % (i + 1,)
        for r in result:
            print r
    infile.close()
    return U


def func_7cfe0301f91b4cb7908d9f5304a694ce():
    infile = open('codejam/test_files/Y11R5P1/A.in')
    T = int(infile.readline())
    for i in xrange(T):
        W, L, U, G = map(int, infile.readline().split())
        lower = []
        upper = []
        for j in xrange(L):
            lower.append(map(float, infile.readline().split()))
        for j in xrange(U):
            upper.append(map(float, infile.readline().split()))
        result = get_cuts(lower, upper, G)
        print 'Case #%d:' % (i + 1,)
        for r in result:
            print r
    infile.close()
    return lower


def func_4bdd6c0f7f8b4b7fb4d099bd72003531():
    infile = open('codejam/test_files/Y11R5P1/A.in')
    T = int(infile.readline())
    for i in xrange(T):
        W, L, U, G = map(int, infile.readline().split())
        lower = []
        upper = []
        for j in xrange(L):
            lower.append(map(float, infile.readline().split()))
        for j in xrange(U):
            upper.append(map(float, infile.readline().split()))
        result = get_cuts(lower, upper, G)
        print 'Case #%d:' % (i + 1,)
        for r in result:
            print r
    infile.close()
    return upper


def func_1cb2f6aa89744e08bc1f2e1983990763():
    infile = open('codejam/test_files/Y11R5P1/A.in')
    T = int(infile.readline())
    for i in xrange(T):
        W, L, U, G = map(int, infile.readline().split())
        lower = []
        upper = []
        for j in xrange(L):
            lower.append(map(float, infile.readline().split()))
        for j in xrange(U):
            upper.append(map(float, infile.readline().split()))
        result = get_cuts(lower, upper, G)
        print 'Case #%d:' % (i + 1,)
        for r in result:
            print r
    infile.close()
    return W


def func_1b210b3ece9742e4b23211206b49cd41():
    infile = open('codejam/test_files/Y11R5P1/A.in')
    T = int(infile.readline())
    for i in xrange(T):
        W, L, U, G = map(int, infile.readline().split())
        lower = []
        upper = []
        for j in xrange(L):
            lower.append(map(float, infile.readline().split()))
        for j in xrange(U):
            upper.append(map(float, infile.readline().split()))
        result = get_cuts(lower, upper, G)
        print 'Case #%d:' % (i + 1,)
        for r in result:
            print r
    infile.close()
    return j


def func_a32f2cc035f94ece9f2fbfc13e53c7fa():
    infile = open('codejam/test_files/Y11R5P1/A.in')
    T = int(infile.readline())
    for i in xrange(T):
        W, L, U, G = map(int, infile.readline().split())
        lower = []
        upper = []
        for j in xrange(L):
            lower.append(map(float, infile.readline().split()))
        for j in xrange(U):
            upper.append(map(float, infile.readline().split()))
        result = get_cuts(lower, upper, G)
        print 'Case #%d:' % (i + 1,)
        for r in result:
            print r
    infile.close()
    return T


def func_f92621b72f11481db9179e538076a12d():
    infile = open('codejam/test_files/Y11R5P1/A.in')
    T = int(infile.readline())
    for i in xrange(T):
        W, L, U, G = map(int, infile.readline().split())
        lower = []
        upper = []
        for j in xrange(L):
            lower.append(map(float, infile.readline().split()))
        for j in xrange(U):
            upper.append(map(float, infile.readline().split()))
        result = get_cuts(lower, upper, G)
        print 'Case #%d:' % (i + 1,)
        for r in result:
            print r
    infile.close()
    return G


def func_d186fbab2d844299b6e8038b8c315a6c():
    infile = open('codejam/test_files/Y11R5P1/A.in')
    T = int(infile.readline())
    for i in xrange(T):
        W, L, U, G = map(int, infile.readline().split())
        lower = []
        upper = []
        for j in xrange(L):
            lower.append(map(float, infile.readline().split()))
        for j in xrange(U):
            upper.append(map(float, infile.readline().split()))
        result = get_cuts(lower, upper, G)
        print 'Case #%d:' % (i + 1,)
        for r in result:
            print r
    infile.close()
    return result
