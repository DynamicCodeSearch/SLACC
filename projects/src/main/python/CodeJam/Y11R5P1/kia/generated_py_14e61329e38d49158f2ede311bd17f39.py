import sys
sys.path.append('/home/george2/Raise/ProgramRepair/CodeSeer/projects/src/main/python')
from CodeJam.Y11R5P1.kia.a import *

def func_ed352bbef05049cfac57f363227945de(P, qx):
    i = 0
    while i + 2 < len(P) and P[i + 1][0] <= qx:
        i += 1
    return i


def func_cd93a56af15c4570bee2665063f2350c(P, qx):
    while i + 2 < len(P) and P[i + 1][0] <= qx:
        i += 1
    x0, y0 = P[i]
    return y0


def func_ed8e13b902d748929ff97f6839e595b5(P, qx):
    while i + 2 < len(P) and P[i + 1][0] <= qx:
        i += 1
    x0, y0 = P[i]
    return x0


def func_a5ea22d882ae4e12abf8e57a9aa2ddfb(P, qx):
    while i + 2 < len(P) and P[i + 1][0] <= qx:
        i += 1
    x0, y0 = P[i]
    return i


def func_e468f3834e6b4650a4f1936c9d36749e(P, i):
    x0, y0 = P[i]
    x1, y1 = P[i + 1]
    return y1


def func_87967dcc148a47c584623f8edb1557af(P, i):
    x0, y0 = P[i]
    x1, y1 = P[i + 1]
    return x0


def func_e168f1c4b5f74ecab2897495159a799e(P, i):
    x0, y0 = P[i]
    x1, y1 = P[i + 1]
    return x1


def func_9777f6b3067f47329dadbcdf0b9fa4d7(P, i):
    x0, y0 = P[i]
    x1, y1 = P[i + 1]
    return y0


def func_324e5087aa5e446ca7bb618654f26c54(x0, P, y0, i, qx):
    x1, y1 = P[i + 1]
    return y0 + (y1 - y0) * (qx - x0) / float(x1 - x0)


def func_36c26f75ae6d409a8cd6698c8b802ba8(P, qx):
    i = 0
    while i + 2 < len(P) and P[i + 1][0] <= qx:
        i += 1
    x0, y0 = P[i]
    return x0


def func_c9fe78133270435b875e0ab367d06469(P, qx):
    i = 0
    while i + 2 < len(P) and P[i + 1][0] <= qx:
        i += 1
    x0, y0 = P[i]
    return y0


def func_9cf0aebcdb274a04aed316c525af7a9e(P, qx):
    i = 0
    while i + 2 < len(P) and P[i + 1][0] <= qx:
        i += 1
    x0, y0 = P[i]
    return i


def func_956ad288e8554ac988de968309c5d3cc(P, qx):
    while i + 2 < len(P) and P[i + 1][0] <= qx:
        i += 1
    x0, y0 = P[i]
    x1, y1 = P[i + 1]
    return y1


def func_bafcce3a4cf14a2e8b19ff8f97dcdab3(P, qx):
    while i + 2 < len(P) and P[i + 1][0] <= qx:
        i += 1
    x0, y0 = P[i]
    x1, y1 = P[i + 1]
    return x0


def func_37764433760e499898a76fdf2fa48657(P, qx):
    while i + 2 < len(P) and P[i + 1][0] <= qx:
        i += 1
    x0, y0 = P[i]
    x1, y1 = P[i + 1]
    return y0


def func_9f6680482be14926b2e5b9a8afd5287e(P, qx):
    while i + 2 < len(P) and P[i + 1][0] <= qx:
        i += 1
    x0, y0 = P[i]
    x1, y1 = P[i + 1]
    return i


def func_529bf3426361459c8efd1c4f442029d3(P, qx):
    while i + 2 < len(P) and P[i + 1][0] <= qx:
        i += 1
    x0, y0 = P[i]
    x1, y1 = P[i + 1]
    return x1


def func_6ac47db3a67e47d3a9b28c718bbbbb19(P, i, qx):
    x0, y0 = P[i]
    x1, y1 = P[i + 1]
    return y0 + (y1 - y0) * (qx - x0) / float(x1 - x0)


def func_89d2784d2159465eba528492377911de(P, qx):
    i = 0
    while i + 2 < len(P) and P[i + 1][0] <= qx:
        i += 1
    x0, y0 = P[i]
    x1, y1 = P[i + 1]
    return i


def func_07f42b924f6641eeafb0e012949b5e3f(P, qx):
    i = 0
    while i + 2 < len(P) and P[i + 1][0] <= qx:
        i += 1
    x0, y0 = P[i]
    x1, y1 = P[i + 1]
    return x1


def func_a9d859b029d64122911728d28293ef90(P, qx):
    i = 0
    while i + 2 < len(P) and P[i + 1][0] <= qx:
        i += 1
    x0, y0 = P[i]
    x1, y1 = P[i + 1]
    return y0


def func_4a71d6565e9847c1942f17d4e33c38aa(P, qx):
    i = 0
    while i + 2 < len(P) and P[i + 1][0] <= qx:
        i += 1
    x0, y0 = P[i]
    x1, y1 = P[i + 1]
    return y1


def func_bdc055ad02db48d5a27f1437390c16a6(P, qx):
    i = 0
    while i + 2 < len(P) and P[i + 1][0] <= qx:
        i += 1
    x0, y0 = P[i]
    x1, y1 = P[i + 1]
    return x0


def func_261cad62091e444495c165c8512e43bb(P, qx):
    while i + 2 < len(P) and P[i + 1][0] <= qx:
        i += 1
    x0, y0 = P[i]
    x1, y1 = P[i + 1]
    return y0 + (y1 - y0) * (qx - x0) / float(x1 - x0)


def func_a97f7ba8242c4372b1a1c15efdc60cde(P, qx):
    i = 0
    while i + 2 < len(P) and P[i + 1][0] <= qx:
        i += 1
    x0, y0 = P[i]
    x1, y1 = P[i + 1]
    return y0 + (y1 - y0) * (qx - x0) / float(x1 - x0)


def func_a7cb7f2d5fdc4ab29947a9020e36d1f2(L, U, x0, x1):
    side0 = abs(get_y(L, x0) - get_y(U, x0))
    side1 = abs(get_y(L, x1) - get_y(U, x1))
    return side0


def func_fb139a2c36f448ff9026d038aaf97e3e(L, U, x0, x1):
    side0 = abs(get_y(L, x0) - get_y(U, x0))
    side1 = abs(get_y(L, x1) - get_y(U, x1))
    return side1


def func_9535af5ef4d2496bac284fb97aca182c(L, U, x0, x1):
    side1 = abs(get_y(L, x1) - get_y(U, x1))
    h = abs(x0 - x1)
    return side1


def func_53dcf8df90aa46e4ad78b55daa56619d(L, U, x0, x1):
    side1 = abs(get_y(L, x1) - get_y(U, x1))
    h = abs(x0 - x1)
    return h


def func_3483e3f3113c41f9a12c3d8322f7b1e8(side1, x0, x1, side0):
    h = abs(x0 - x1)
    res += (side0 + side1) * h / 2.0
    return res


def func_869889515e024e0fa35a37ea72e2a1c0(side1, x0, x1, side0):
    h = abs(x0 - x1)
    res += (side0 + side1) * h / 2.0
    return h


def func_b803651a5bf446b69b7f6777da2ed63e(L, U, x0, x1):
    side0 = abs(get_y(L, x0) - get_y(U, x0))
    side1 = abs(get_y(L, x1) - get_y(U, x1))
    h = abs(x0 - x1)
    return side0


def func_71f35341c709413f8fbb7162080bc0f4(L, U, x0, x1):
    side0 = abs(get_y(L, x0) - get_y(U, x0))
    side1 = abs(get_y(L, x1) - get_y(U, x1))
    h = abs(x0 - x1)
    return h


def func_b21a93ee4b3a41ae957b571efb07cfc0(L, U, x0, x1):
    side0 = abs(get_y(L, x0) - get_y(U, x0))
    side1 = abs(get_y(L, x1) - get_y(U, x1))
    h = abs(x0 - x1)
    return side1


def func_5f54fb1254b04387bd6f2346208550d7(L, U, x0, x1, side0):
    side1 = abs(get_y(L, x1) - get_y(U, x1))
    h = abs(x0 - x1)
    res += (side0 + side1) * h / 2.0
    return side1


def func_bd7087bcfeda4f258d29d653ddc9f4ba(L, U, x0, x1, side0):
    side1 = abs(get_y(L, x1) - get_y(U, x1))
    h = abs(x0 - x1)
    res += (side0 + side1) * h / 2.0
    return h


def func_d8251b2e521a4844a02e0add08ee0824(L, U, x0, x1, side0):
    side1 = abs(get_y(L, x1) - get_y(U, x1))
    h = abs(x0 - x1)
    res += (side0 + side1) * h / 2.0
    return res


def func_6f90c964a6b6403584f05e4c9d6a6ea1(L, U, x0, x1):
    side0 = abs(get_y(L, x0) - get_y(U, x0))
    side1 = abs(get_y(L, x1) - get_y(U, x1))
    h = abs(x0 - x1)
    res += (side0 + side1) * h / 2.0
    return h


def func_5dac1ef7dfb04fd683c9a760225a5d7a(L, U, x0, x1):
    side0 = abs(get_y(L, x0) - get_y(U, x0))
    side1 = abs(get_y(L, x1) - get_y(U, x1))
    h = abs(x0 - x1)
    res += (side0 + side1) * h / 2.0
    return res


def func_70c52e615c1a4e418f10d5e0e9485b37(L, U, x0, x1):
    side0 = abs(get_y(L, x0) - get_y(U, x0))
    side1 = abs(get_y(L, x1) - get_y(U, x1))
    h = abs(x0 - x1)
    res += (side0 + side1) * h / 2.0
    return side1


def func_2b191290c1a44e5f9513c7063b86d2a2(L, U, x0, x1):
    side0 = abs(get_y(L, x0) - get_y(U, x0))
    side1 = abs(get_y(L, x1) - get_y(U, x1))
    h = abs(x0 - x1)
    res += (side0 + side1) * h / 2.0
    return side0


def func_d8642ffb5fa04d42853d4044e16215d5(L, U):
    xs = [x for x, y in L]
    xs += [x for x, y in U]
    return y


def func_93afc30fb03642178ac13ab0cd19d0be(L, U):
    xs = [x for x, y in L]
    xs += [x for x, y in U]
    return xs


def func_4463bbb6cd73432fa3e8d263d83f802a(L, U):
    xs = [x for x, y in L]
    xs += [x for x, y in U]
    return x


def func_ddfc57e4a15c47778551c9dc691b3845(left, right):
    xs += [left, right]
    xs = sorted(set(xs))
    return xs


def func_9154cb4f67004ad0933b56b81e00555b(left, right):
    xs = sorted(set(xs))
    xs = [x for x in xs if left - 1e-09 <= x <= right + 1e-09]
    return xs


def func_0e7af835e314447d83a16b7b9c22075d(left, right):
    xs = sorted(set(xs))
    xs = [x for x in xs if left - 1e-09 <= x <= right + 1e-09]
    return x


def func_ecf6eee0757949a48080476407861a09(left, right):
    xs = [x for x in xs if left - 1e-09 <= x <= right + 1e-09]
    res = 0
    return x


def func_e9e46cc50e0f46e8bd0494fa3b407700(left, right):
    xs = [x for x in xs if left - 1e-09 <= x <= right + 1e-09]
    res = 0
    return res


def func_289beb60481245b2a55619ff2330b9d0(left, right):
    xs = [x for x in xs if left - 1e-09 <= x <= right + 1e-09]
    res = 0
    return xs


def func_6261ec33045b469f8763b46244f19124(L, left, U, right):
    xs = [x for x, y in L]
    xs += [x for x, y in U]
    xs += [left, right]
    return y


def func_2321317f672344bab95c2baf3ca481d2(L, left, U, right):
    xs = [x for x, y in L]
    xs += [x for x, y in U]
    xs += [left, right]
    return x


def func_d221b7c8175043738bfbde401b70de19(L, left, U, right):
    xs = [x for x, y in L]
    xs += [x for x, y in U]
    xs += [left, right]
    return xs


def func_c3282250596047b2843414494d1b8a26(left, U, right):
    xs += [x for x, y in U]
    xs += [left, right]
    xs = sorted(set(xs))
    return y


def func_5b542204280743ad813ead09f956b65a(left, U, right):
    xs += [x for x, y in U]
    xs += [left, right]
    xs = sorted(set(xs))
    return xs


def func_99b95d0dace1469ba02f25c841ee11cc(left, U, right):
    xs += [x for x, y in U]
    xs += [left, right]
    xs = sorted(set(xs))
    return x


def func_934f9b0cb8fe4c31bb46ebe4e0a03e17(left, right):
    xs += [left, right]
    xs = sorted(set(xs))
    xs = [x for x in xs if left - 1e-09 <= x <= right + 1e-09]
    return xs


def func_46eb78de3fbc403b97c7c6349a8fd1de(left, right):
    xs += [left, right]
    xs = sorted(set(xs))
    xs = [x for x in xs if left - 1e-09 <= x <= right + 1e-09]
    return x


def func_57a4f3b03f094d6a96b80ab60efaf006(left, right):
    xs = sorted(set(xs))
    xs = [x for x in xs if left - 1e-09 <= x <= right + 1e-09]
    res = 0
    return xs


def func_27bc6192f63a46dfb35d9543a6a13416(left, right):
    xs = sorted(set(xs))
    xs = [x for x in xs if left - 1e-09 <= x <= right + 1e-09]
    res = 0
    return x


def func_62abb5eb30454b949df758a1aafc7216(left, right):
    xs = sorted(set(xs))
    xs = [x for x in xs if left - 1e-09 <= x <= right + 1e-09]
    res = 0
    return res


def func_b5c3c0fa03914bf282475f824586a168(L, left, U, right):
    xs = [x for x in xs if left - 1e-09 <= x <= right + 1e-09]
    res = 0
    for i in range(1, len(xs)):
        x0 = xs[i - 1]
        x1 = xs[i]
        side0 = abs(get_y(L, x0) - get_y(U, x0))
        side1 = abs(get_y(L, x1) - get_y(U, x1))
        h = abs(x0 - x1)
        res += (side0 + side1) * h / 2.0
    return x1


def func_9e5b0fb2034948789c32be477581f607(L, left, U, right):
    xs = [x for x in xs if left - 1e-09 <= x <= right + 1e-09]
    res = 0
    for i in range(1, len(xs)):
        x0 = xs[i - 1]
        x1 = xs[i]
        side0 = abs(get_y(L, x0) - get_y(U, x0))
        side1 = abs(get_y(L, x1) - get_y(U, x1))
        h = abs(x0 - x1)
        res += (side0 + side1) * h / 2.0
    return side0


def func_b837d255a37141669db5a3e2df789e8f(L, left, U, right):
    xs = [x for x in xs if left - 1e-09 <= x <= right + 1e-09]
    res = 0
    for i in range(1, len(xs)):
        x0 = xs[i - 1]
        x1 = xs[i]
        side0 = abs(get_y(L, x0) - get_y(U, x0))
        side1 = abs(get_y(L, x1) - get_y(U, x1))
        h = abs(x0 - x1)
        res += (side0 + side1) * h / 2.0
    return x


def func_6691a9f1543c4062ae087af88640306f(L, left, U, right):
    xs = [x for x in xs if left - 1e-09 <= x <= right + 1e-09]
    res = 0
    for i in range(1, len(xs)):
        x0 = xs[i - 1]
        x1 = xs[i]
        side0 = abs(get_y(L, x0) - get_y(U, x0))
        side1 = abs(get_y(L, x1) - get_y(U, x1))
        h = abs(x0 - x1)
        res += (side0 + side1) * h / 2.0
    return xs


def func_9983b9ae9c7e4b3abf23134092e755e5(L, left, U, right):
    xs = [x for x in xs if left - 1e-09 <= x <= right + 1e-09]
    res = 0
    for i in range(1, len(xs)):
        x0 = xs[i - 1]
        x1 = xs[i]
        side0 = abs(get_y(L, x0) - get_y(U, x0))
        side1 = abs(get_y(L, x1) - get_y(U, x1))
        h = abs(x0 - x1)
        res += (side0 + side1) * h / 2.0
    return i


def func_3a84de93e02f427eab3c2ce0a729a0c7(L, left, U, right):
    xs = [x for x in xs if left - 1e-09 <= x <= right + 1e-09]
    res = 0
    for i in range(1, len(xs)):
        x0 = xs[i - 1]
        x1 = xs[i]
        side0 = abs(get_y(L, x0) - get_y(U, x0))
        side1 = abs(get_y(L, x1) - get_y(U, x1))
        h = abs(x0 - x1)
        res += (side0 + side1) * h / 2.0
    return x0


def func_e9a0b1f1d3294214b9fbf090587ab827(L, left, U, right):
    xs = [x for x in xs if left - 1e-09 <= x <= right + 1e-09]
    res = 0
    for i in range(1, len(xs)):
        x0 = xs[i - 1]
        x1 = xs[i]
        side0 = abs(get_y(L, x0) - get_y(U, x0))
        side1 = abs(get_y(L, x1) - get_y(U, x1))
        h = abs(x0 - x1)
        res += (side0 + side1) * h / 2.0
    return res


def func_a96921b6c1974a178d7cb7be24269712(L, left, U, right):
    xs = [x for x in xs if left - 1e-09 <= x <= right + 1e-09]
    res = 0
    for i in range(1, len(xs)):
        x0 = xs[i - 1]
        x1 = xs[i]
        side0 = abs(get_y(L, x0) - get_y(U, x0))
        side1 = abs(get_y(L, x1) - get_y(U, x1))
        h = abs(x0 - x1)
        res += (side0 + side1) * h / 2.0
    return h


def func_b51529ad8e0142059129204915689800(L, left, U, right):
    xs = [x for x in xs if left - 1e-09 <= x <= right + 1e-09]
    res = 0
    for i in range(1, len(xs)):
        x0 = xs[i - 1]
        x1 = xs[i]
        side0 = abs(get_y(L, x0) - get_y(U, x0))
        side1 = abs(get_y(L, x1) - get_y(U, x1))
        h = abs(x0 - x1)
        res += (side0 + side1) * h / 2.0
    return side1


def func_299a0bbab2e54b1f8de8d8fc6900ea16(L, left, U, right):
    xs = [x for x, y in L]
    xs += [x for x, y in U]
    xs += [left, right]
    xs = sorted(set(xs))
    return xs


def func_e29d782d2aaf4509b13c8f08dfdebc54(L, left, U, right):
    xs = [x for x, y in L]
    xs += [x for x, y in U]
    xs += [left, right]
    xs = sorted(set(xs))
    return x


def func_e2c097e64feb48e6a64dd63f669bec21(L, left, U, right):
    xs = [x for x, y in L]
    xs += [x for x, y in U]
    xs += [left, right]
    xs = sorted(set(xs))
    return y


def func_6c5b99c331fc4d528cdde7f7cc9ddf0a(left, U, right):
    xs += [x for x, y in U]
    xs += [left, right]
    xs = sorted(set(xs))
    xs = [x for x in xs if left - 1e-09 <= x <= right + 1e-09]
    return xs


def func_2d01bb81ac4c41a1946e73b887670a04(left, U, right):
    xs += [x for x, y in U]
    xs += [left, right]
    xs = sorted(set(xs))
    xs = [x for x in xs if left - 1e-09 <= x <= right + 1e-09]
    return y


def func_b67b2b02fb7443b8b7add89f4f00c883(left, U, right):
    xs += [x for x, y in U]
    xs += [left, right]
    xs = sorted(set(xs))
    xs = [x for x in xs if left - 1e-09 <= x <= right + 1e-09]
    return x


def func_8cf5edc35de44f688725d4b177a5b615(left, right):
    xs += [left, right]
    xs = sorted(set(xs))
    xs = [x for x in xs if left - 1e-09 <= x <= right + 1e-09]
    res = 0
    return res


def func_a577e610db49414ab472aec264b87bee(left, right):
    xs += [left, right]
    xs = sorted(set(xs))
    xs = [x for x in xs if left - 1e-09 <= x <= right + 1e-09]
    res = 0
    return xs


def func_36e7924379ea4c4caca2c9a888a77fb9(left, right):
    xs += [left, right]
    xs = sorted(set(xs))
    xs = [x for x in xs if left - 1e-09 <= x <= right + 1e-09]
    res = 0
    return x


def func_35f94142bcdf423991b6a088a147ebbe(L, left, U, right):
    xs = sorted(set(xs))
    xs = [x for x in xs if left - 1e-09 <= x <= right + 1e-09]
    res = 0
    for i in range(1, len(xs)):
        x0 = xs[i - 1]
        x1 = xs[i]
        side0 = abs(get_y(L, x0) - get_y(U, x0))
        side1 = abs(get_y(L, x1) - get_y(U, x1))
        h = abs(x0 - x1)
        res += (side0 + side1) * h / 2.0
    return side0


def func_ec2b9b6a0f704e2ea6d8cc4064e9ec6f(L, left, U, right):
    xs = sorted(set(xs))
    xs = [x for x in xs if left - 1e-09 <= x <= right + 1e-09]
    res = 0
    for i in range(1, len(xs)):
        x0 = xs[i - 1]
        x1 = xs[i]
        side0 = abs(get_y(L, x0) - get_y(U, x0))
        side1 = abs(get_y(L, x1) - get_y(U, x1))
        h = abs(x0 - x1)
        res += (side0 + side1) * h / 2.0
    return x1


def func_2410b32fd45247f7b9552fb9f069c372(L, left, U, right):
    xs = sorted(set(xs))
    xs = [x for x in xs if left - 1e-09 <= x <= right + 1e-09]
    res = 0
    for i in range(1, len(xs)):
        x0 = xs[i - 1]
        x1 = xs[i]
        side0 = abs(get_y(L, x0) - get_y(U, x0))
        side1 = abs(get_y(L, x1) - get_y(U, x1))
        h = abs(x0 - x1)
        res += (side0 + side1) * h / 2.0
    return i


def func_2d806f4d7ad2494c8b0dad7a7d6d6555(L, left, U, right):
    xs = sorted(set(xs))
    xs = [x for x in xs if left - 1e-09 <= x <= right + 1e-09]
    res = 0
    for i in range(1, len(xs)):
        x0 = xs[i - 1]
        x1 = xs[i]
        side0 = abs(get_y(L, x0) - get_y(U, x0))
        side1 = abs(get_y(L, x1) - get_y(U, x1))
        h = abs(x0 - x1)
        res += (side0 + side1) * h / 2.0
    return x0


def func_dc4b3e63c5bd4627a5073a8c65b5508d(L, left, U, right):
    xs = sorted(set(xs))
    xs = [x for x in xs if left - 1e-09 <= x <= right + 1e-09]
    res = 0
    for i in range(1, len(xs)):
        x0 = xs[i - 1]
        x1 = xs[i]
        side0 = abs(get_y(L, x0) - get_y(U, x0))
        side1 = abs(get_y(L, x1) - get_y(U, x1))
        h = abs(x0 - x1)
        res += (side0 + side1) * h / 2.0
    return h


def func_c8583437e1da4cec9af761b32881fca4(L, left, U, right):
    xs = sorted(set(xs))
    xs = [x for x in xs if left - 1e-09 <= x <= right + 1e-09]
    res = 0
    for i in range(1, len(xs)):
        x0 = xs[i - 1]
        x1 = xs[i]
        side0 = abs(get_y(L, x0) - get_y(U, x0))
        side1 = abs(get_y(L, x1) - get_y(U, x1))
        h = abs(x0 - x1)
        res += (side0 + side1) * h / 2.0
    return side1


def func_38af814e2b954889b5b3a3d1afc8b9b9(L, left, U, right):
    xs = sorted(set(xs))
    xs = [x for x in xs if left - 1e-09 <= x <= right + 1e-09]
    res = 0
    for i in range(1, len(xs)):
        x0 = xs[i - 1]
        x1 = xs[i]
        side0 = abs(get_y(L, x0) - get_y(U, x0))
        side1 = abs(get_y(L, x1) - get_y(U, x1))
        h = abs(x0 - x1)
        res += (side0 + side1) * h / 2.0
    return res


def func_a5b42bdd43c042e4952af91b9f91566b(L, left, U, right):
    xs = sorted(set(xs))
    xs = [x for x in xs if left - 1e-09 <= x <= right + 1e-09]
    res = 0
    for i in range(1, len(xs)):
        x0 = xs[i - 1]
        x1 = xs[i]
        side0 = abs(get_y(L, x0) - get_y(U, x0))
        side1 = abs(get_y(L, x1) - get_y(U, x1))
        h = abs(x0 - x1)
        res += (side0 + side1) * h / 2.0
    return xs


def func_0c246e6c1c7c405eb0a6b6d207473101(L, left, U, right):
    xs = sorted(set(xs))
    xs = [x for x in xs if left - 1e-09 <= x <= right + 1e-09]
    res = 0
    for i in range(1, len(xs)):
        x0 = xs[i - 1]
        x1 = xs[i]
        side0 = abs(get_y(L, x0) - get_y(U, x0))
        side1 = abs(get_y(L, x1) - get_y(U, x1))
        h = abs(x0 - x1)
        res += (side0 + side1) * h / 2.0
    return x


def func_d67466468bd24ddaaa7938a7d43190d7(L, left, U, right):
    xs = [x for x in xs if left - 1e-09 <= x <= right + 1e-09]
    res = 0
    for i in range(1, len(xs)):
        x0 = xs[i - 1]
        x1 = xs[i]
        side0 = abs(get_y(L, x0) - get_y(U, x0))
        side1 = abs(get_y(L, x1) - get_y(U, x1))
        h = abs(x0 - x1)
        res += (side0 + side1) * h / 2.0
    return res


def func_e46d82dc4cc241d9a02e150e42147119(L, left, U, right):
    xs = [x for x, y in L]
    xs += [x for x, y in U]
    xs += [left, right]
    xs = sorted(set(xs))
    xs = [x for x in xs if left - 1e-09 <= x <= right + 1e-09]
    return x


def func_3fe37a02360549809f8012b37a244d21(L, left, U, right):
    xs = [x for x, y in L]
    xs += [x for x, y in U]
    xs += [left, right]
    xs = sorted(set(xs))
    xs = [x for x in xs if left - 1e-09 <= x <= right + 1e-09]
    return xs


def func_6bd6ea33ec7041df944636aae944085a(L, left, U, right):
    xs = [x for x, y in L]
    xs += [x for x, y in U]
    xs += [left, right]
    xs = sorted(set(xs))
    xs = [x for x in xs if left - 1e-09 <= x <= right + 1e-09]
    return y


def func_1ba9b47e852c49e8bfead6b77ca9a2d4(left, U, right):
    xs += [x for x, y in U]
    xs += [left, right]
    xs = sorted(set(xs))
    xs = [x for x in xs if left - 1e-09 <= x <= right + 1e-09]
    res = 0
    return y


def func_cc9fb48478dc4dc69929e63627cea4d1(left, U, right):
    xs += [x for x, y in U]
    xs += [left, right]
    xs = sorted(set(xs))
    xs = [x for x in xs if left - 1e-09 <= x <= right + 1e-09]
    res = 0
    return xs


def func_a9b182a4a34b4eb985befde0cc034bdb(left, U, right):
    xs += [x for x, y in U]
    xs += [left, right]
    xs = sorted(set(xs))
    xs = [x for x in xs if left - 1e-09 <= x <= right + 1e-09]
    res = 0
    return x


def func_9ffb46db81e7466e809556e79a8ddf0e(left, U, right):
    xs += [x for x, y in U]
    xs += [left, right]
    xs = sorted(set(xs))
    xs = [x for x in xs if left - 1e-09 <= x <= right + 1e-09]
    res = 0
    return res


def func_b989792bf1d34152b320194f5c902a79(L, left, U, right):
    xs += [left, right]
    xs = sorted(set(xs))
    xs = [x for x in xs if left - 1e-09 <= x <= right + 1e-09]
    res = 0
    for i in range(1, len(xs)):
        x0 = xs[i - 1]
        x1 = xs[i]
        side0 = abs(get_y(L, x0) - get_y(U, x0))
        side1 = abs(get_y(L, x1) - get_y(U, x1))
        h = abs(x0 - x1)
        res += (side0 + side1) * h / 2.0
    return res


def func_5272e679d38e4f41bebd3fd0c571e3fc(L, left, U, right):
    xs += [left, right]
    xs = sorted(set(xs))
    xs = [x for x in xs if left - 1e-09 <= x <= right + 1e-09]
    res = 0
    for i in range(1, len(xs)):
        x0 = xs[i - 1]
        x1 = xs[i]
        side0 = abs(get_y(L, x0) - get_y(U, x0))
        side1 = abs(get_y(L, x1) - get_y(U, x1))
        h = abs(x0 - x1)
        res += (side0 + side1) * h / 2.0
    return x


def func_9eac592159d54fe0a3d1e2429c31f045(L, left, U, right):
    xs += [left, right]
    xs = sorted(set(xs))
    xs = [x for x in xs if left - 1e-09 <= x <= right + 1e-09]
    res = 0
    for i in range(1, len(xs)):
        x0 = xs[i - 1]
        x1 = xs[i]
        side0 = abs(get_y(L, x0) - get_y(U, x0))
        side1 = abs(get_y(L, x1) - get_y(U, x1))
        h = abs(x0 - x1)
        res += (side0 + side1) * h / 2.0
    return x0


def func_d011261ed3b74614a5e3de01e9a22c3f(L, left, U, right):
    xs += [left, right]
    xs = sorted(set(xs))
    xs = [x for x in xs if left - 1e-09 <= x <= right + 1e-09]
    res = 0
    for i in range(1, len(xs)):
        x0 = xs[i - 1]
        x1 = xs[i]
        side0 = abs(get_y(L, x0) - get_y(U, x0))
        side1 = abs(get_y(L, x1) - get_y(U, x1))
        h = abs(x0 - x1)
        res += (side0 + side1) * h / 2.0
    return i


def func_23d7aa3d16f44a729be49caa85d60033(L, left, U, right):
    xs += [left, right]
    xs = sorted(set(xs))
    xs = [x for x in xs if left - 1e-09 <= x <= right + 1e-09]
    res = 0
    for i in range(1, len(xs)):
        x0 = xs[i - 1]
        x1 = xs[i]
        side0 = abs(get_y(L, x0) - get_y(U, x0))
        side1 = abs(get_y(L, x1) - get_y(U, x1))
        h = abs(x0 - x1)
        res += (side0 + side1) * h / 2.0
    return h


def func_c1b9b462d0204e3894eaa31c5c813ec5(L, left, U, right):
    xs += [left, right]
    xs = sorted(set(xs))
    xs = [x for x in xs if left - 1e-09 <= x <= right + 1e-09]
    res = 0
    for i in range(1, len(xs)):
        x0 = xs[i - 1]
        x1 = xs[i]
        side0 = abs(get_y(L, x0) - get_y(U, x0))
        side1 = abs(get_y(L, x1) - get_y(U, x1))
        h = abs(x0 - x1)
        res += (side0 + side1) * h / 2.0
    return x1


def func_5e14ab9202254735af928d9ec54ec5d8(L, left, U, right):
    xs += [left, right]
    xs = sorted(set(xs))
    xs = [x for x in xs if left - 1e-09 <= x <= right + 1e-09]
    res = 0
    for i in range(1, len(xs)):
        x0 = xs[i - 1]
        x1 = xs[i]
        side0 = abs(get_y(L, x0) - get_y(U, x0))
        side1 = abs(get_y(L, x1) - get_y(U, x1))
        h = abs(x0 - x1)
        res += (side0 + side1) * h / 2.0
    return side0


def func_189d62fe01da42f8a0cf189322066cf1(L, left, U, right):
    xs += [left, right]
    xs = sorted(set(xs))
    xs = [x for x in xs if left - 1e-09 <= x <= right + 1e-09]
    res = 0
    for i in range(1, len(xs)):
        x0 = xs[i - 1]
        x1 = xs[i]
        side0 = abs(get_y(L, x0) - get_y(U, x0))
        side1 = abs(get_y(L, x1) - get_y(U, x1))
        h = abs(x0 - x1)
        res += (side0 + side1) * h / 2.0
    return xs


def func_c3ec6243bddd46868244521b0381b010(L, left, U, right):
    xs += [left, right]
    xs = sorted(set(xs))
    xs = [x for x in xs if left - 1e-09 <= x <= right + 1e-09]
    res = 0
    for i in range(1, len(xs)):
        x0 = xs[i - 1]
        x1 = xs[i]
        side0 = abs(get_y(L, x0) - get_y(U, x0))
        side1 = abs(get_y(L, x1) - get_y(U, x1))
        h = abs(x0 - x1)
        res += (side0 + side1) * h / 2.0
    return side1


def func_a6ecbbdee2a840eabf29746e9f2e40ec(L, left, U, right):
    xs = sorted(set(xs))
    xs = [x for x in xs if left - 1e-09 <= x <= right + 1e-09]
    res = 0
    for i in range(1, len(xs)):
        x0 = xs[i - 1]
        x1 = xs[i]
        side0 = abs(get_y(L, x0) - get_y(U, x0))
        side1 = abs(get_y(L, x1) - get_y(U, x1))
        h = abs(x0 - x1)
        res += (side0 + side1) * h / 2.0
    return res


def func_7797fe6a05bc43f785eda5e008788cef(L, left, U, right):
    xs = [x for x, y in L]
    xs += [x for x, y in U]
    xs += [left, right]
    xs = sorted(set(xs))
    xs = [x for x in xs if left - 1e-09 <= x <= right + 1e-09]
    res = 0
    return xs


def func_d99097030cdb4aefb17150ca1c352727(L, left, U, right):
    xs = [x for x, y in L]
    xs += [x for x, y in U]
    xs += [left, right]
    xs = sorted(set(xs))
    xs = [x for x in xs if left - 1e-09 <= x <= right + 1e-09]
    res = 0
    return res


def func_0edc9bcb279342b783035cf8fe576c90(L, left, U, right):
    xs = [x for x, y in L]
    xs += [x for x, y in U]
    xs += [left, right]
    xs = sorted(set(xs))
    xs = [x for x in xs if left - 1e-09 <= x <= right + 1e-09]
    res = 0
    return x


def func_3fc6069763bd4ae2acb182ed68d5ee0a(L, left, U, right):
    xs = [x for x, y in L]
    xs += [x for x, y in U]
    xs += [left, right]
    xs = sorted(set(xs))
    xs = [x for x in xs if left - 1e-09 <= x <= right + 1e-09]
    res = 0
    return y


def func_7f0a7b251e294ce1b4a45a7323fa37a4(L, left, U, right):
    xs += [x for x, y in U]
    xs += [left, right]
    xs = sorted(set(xs))
    xs = [x for x in xs if left - 1e-09 <= x <= right + 1e-09]
    res = 0
    for i in range(1, len(xs)):
        x0 = xs[i - 1]
        x1 = xs[i]
        side0 = abs(get_y(L, x0) - get_y(U, x0))
        side1 = abs(get_y(L, x1) - get_y(U, x1))
        h = abs(x0 - x1)
        res += (side0 + side1) * h / 2.0
    return x


def func_eaee7cc4123b4f51a5097de31b2b166c(L, left, U, right):
    xs += [x for x, y in U]
    xs += [left, right]
    xs = sorted(set(xs))
    xs = [x for x in xs if left - 1e-09 <= x <= right + 1e-09]
    res = 0
    for i in range(1, len(xs)):
        x0 = xs[i - 1]
        x1 = xs[i]
        side0 = abs(get_y(L, x0) - get_y(U, x0))
        side1 = abs(get_y(L, x1) - get_y(U, x1))
        h = abs(x0 - x1)
        res += (side0 + side1) * h / 2.0
    return res


def func_30600ca8741540ffaf22d698fe735d74(L, left, U, right):
    xs += [x for x, y in U]
    xs += [left, right]
    xs = sorted(set(xs))
    xs = [x for x in xs if left - 1e-09 <= x <= right + 1e-09]
    res = 0
    for i in range(1, len(xs)):
        x0 = xs[i - 1]
        x1 = xs[i]
        side0 = abs(get_y(L, x0) - get_y(U, x0))
        side1 = abs(get_y(L, x1) - get_y(U, x1))
        h = abs(x0 - x1)
        res += (side0 + side1) * h / 2.0
    return x0


def func_44b3a8ff5ad245c78758daec8385c5d6(L, left, U, right):
    xs += [x for x, y in U]
    xs += [left, right]
    xs = sorted(set(xs))
    xs = [x for x in xs if left - 1e-09 <= x <= right + 1e-09]
    res = 0
    for i in range(1, len(xs)):
        x0 = xs[i - 1]
        x1 = xs[i]
        side0 = abs(get_y(L, x0) - get_y(U, x0))
        side1 = abs(get_y(L, x1) - get_y(U, x1))
        h = abs(x0 - x1)
        res += (side0 + side1) * h / 2.0
    return side0


def func_7ba5219508e54237b191eb9512d227f3(L, left, U, right):
    xs += [x for x, y in U]
    xs += [left, right]
    xs = sorted(set(xs))
    xs = [x for x in xs if left - 1e-09 <= x <= right + 1e-09]
    res = 0
    for i in range(1, len(xs)):
        x0 = xs[i - 1]
        x1 = xs[i]
        side0 = abs(get_y(L, x0) - get_y(U, x0))
        side1 = abs(get_y(L, x1) - get_y(U, x1))
        h = abs(x0 - x1)
        res += (side0 + side1) * h / 2.0
    return i


def func_ce919d30383d4c87a3a78d2f55262d94(L, left, U, right):
    xs += [x for x, y in U]
    xs += [left, right]
    xs = sorted(set(xs))
    xs = [x for x in xs if left - 1e-09 <= x <= right + 1e-09]
    res = 0
    for i in range(1, len(xs)):
        x0 = xs[i - 1]
        x1 = xs[i]
        side0 = abs(get_y(L, x0) - get_y(U, x0))
        side1 = abs(get_y(L, x1) - get_y(U, x1))
        h = abs(x0 - x1)
        res += (side0 + side1) * h / 2.0
    return y


def func_ad632d7a87724eb391c76eaad533ccd2(L, left, U, right):
    xs += [x for x, y in U]
    xs += [left, right]
    xs = sorted(set(xs))
    xs = [x for x in xs if left - 1e-09 <= x <= right + 1e-09]
    res = 0
    for i in range(1, len(xs)):
        x0 = xs[i - 1]
        x1 = xs[i]
        side0 = abs(get_y(L, x0) - get_y(U, x0))
        side1 = abs(get_y(L, x1) - get_y(U, x1))
        h = abs(x0 - x1)
        res += (side0 + side1) * h / 2.0
    return side1


def func_7378902233d34672bdc4711297f39ffd(L, left, U, right):
    xs += [x for x, y in U]
    xs += [left, right]
    xs = sorted(set(xs))
    xs = [x for x in xs if left - 1e-09 <= x <= right + 1e-09]
    res = 0
    for i in range(1, len(xs)):
        x0 = xs[i - 1]
        x1 = xs[i]
        side0 = abs(get_y(L, x0) - get_y(U, x0))
        side1 = abs(get_y(L, x1) - get_y(U, x1))
        h = abs(x0 - x1)
        res += (side0 + side1) * h / 2.0
    return h


def func_7a2a63496d294ead9bc74f9d2d6b52c7(L, left, U, right):
    xs += [x for x, y in U]
    xs += [left, right]
    xs = sorted(set(xs))
    xs = [x for x in xs if left - 1e-09 <= x <= right + 1e-09]
    res = 0
    for i in range(1, len(xs)):
        x0 = xs[i - 1]
        x1 = xs[i]
        side0 = abs(get_y(L, x0) - get_y(U, x0))
        side1 = abs(get_y(L, x1) - get_y(U, x1))
        h = abs(x0 - x1)
        res += (side0 + side1) * h / 2.0
    return xs


def func_f080f2ea17bf4e7da7c8ee49874ecdf6(L, left, U, right):
    xs += [x for x, y in U]
    xs += [left, right]
    xs = sorted(set(xs))
    xs = [x for x in xs if left - 1e-09 <= x <= right + 1e-09]
    res = 0
    for i in range(1, len(xs)):
        x0 = xs[i - 1]
        x1 = xs[i]
        side0 = abs(get_y(L, x0) - get_y(U, x0))
        side1 = abs(get_y(L, x1) - get_y(U, x1))
        h = abs(x0 - x1)
        res += (side0 + side1) * h / 2.0
    return x1


def func_8312d52f9372422cbbe47e7a0dde22cc(L, left, U, right):
    xs += [left, right]
    xs = sorted(set(xs))
    xs = [x for x in xs if left - 1e-09 <= x <= right + 1e-09]
    res = 0
    for i in range(1, len(xs)):
        x0 = xs[i - 1]
        x1 = xs[i]
        side0 = abs(get_y(L, x0) - get_y(U, x0))
        side1 = abs(get_y(L, x1) - get_y(U, x1))
        h = abs(x0 - x1)
        res += (side0 + side1) * h / 2.0
    return res


def func_8095efc77f9f4c2f97ebfa4b545d97c4(L, left, U, right):
    xs = [x for x, y in L]
    xs += [x for x, y in U]
    xs += [left, right]
    xs = sorted(set(xs))
    xs = [x for x in xs if left - 1e-09 <= x <= right + 1e-09]
    res = 0
    for i in range(1, len(xs)):
        x0 = xs[i - 1]
        x1 = xs[i]
        side0 = abs(get_y(L, x0) - get_y(U, x0))
        side1 = abs(get_y(L, x1) - get_y(U, x1))
        h = abs(x0 - x1)
        res += (side0 + side1) * h / 2.0
    return side1


def func_9985e86e32af4c039989ffbe3fdda527(L, left, U, right):
    xs = [x for x, y in L]
    xs += [x for x, y in U]
    xs += [left, right]
    xs = sorted(set(xs))
    xs = [x for x in xs if left - 1e-09 <= x <= right + 1e-09]
    res = 0
    for i in range(1, len(xs)):
        x0 = xs[i - 1]
        x1 = xs[i]
        side0 = abs(get_y(L, x0) - get_y(U, x0))
        side1 = abs(get_y(L, x1) - get_y(U, x1))
        h = abs(x0 - x1)
        res += (side0 + side1) * h / 2.0
    return x1


def func_0f653d4d548a459f97deeff629991fe0(L, left, U, right):
    xs = [x for x, y in L]
    xs += [x for x, y in U]
    xs += [left, right]
    xs = sorted(set(xs))
    xs = [x for x in xs if left - 1e-09 <= x <= right + 1e-09]
    res = 0
    for i in range(1, len(xs)):
        x0 = xs[i - 1]
        x1 = xs[i]
        side0 = abs(get_y(L, x0) - get_y(U, x0))
        side1 = abs(get_y(L, x1) - get_y(U, x1))
        h = abs(x0 - x1)
        res += (side0 + side1) * h / 2.0
    return y


def func_33dd8f1a0f8a409381321ac4c5d224cf(L, left, U, right):
    xs = [x for x, y in L]
    xs += [x for x, y in U]
    xs += [left, right]
    xs = sorted(set(xs))
    xs = [x for x in xs if left - 1e-09 <= x <= right + 1e-09]
    res = 0
    for i in range(1, len(xs)):
        x0 = xs[i - 1]
        x1 = xs[i]
        side0 = abs(get_y(L, x0) - get_y(U, x0))
        side1 = abs(get_y(L, x1) - get_y(U, x1))
        h = abs(x0 - x1)
        res += (side0 + side1) * h / 2.0
    return h


def func_a06c3b3650474b09b16e16eb1fbb9ff3(L, left, U, right):
    xs = [x for x, y in L]
    xs += [x for x, y in U]
    xs += [left, right]
    xs = sorted(set(xs))
    xs = [x for x in xs if left - 1e-09 <= x <= right + 1e-09]
    res = 0
    for i in range(1, len(xs)):
        x0 = xs[i - 1]
        x1 = xs[i]
        side0 = abs(get_y(L, x0) - get_y(U, x0))
        side1 = abs(get_y(L, x1) - get_y(U, x1))
        h = abs(x0 - x1)
        res += (side0 + side1) * h / 2.0
    return side0


def func_b887353d17814d1fa6e03ccdd2e8a02a(L, left, U, right):
    xs = [x for x, y in L]
    xs += [x for x, y in U]
    xs += [left, right]
    xs = sorted(set(xs))
    xs = [x for x in xs if left - 1e-09 <= x <= right + 1e-09]
    res = 0
    for i in range(1, len(xs)):
        x0 = xs[i - 1]
        x1 = xs[i]
        side0 = abs(get_y(L, x0) - get_y(U, x0))
        side1 = abs(get_y(L, x1) - get_y(U, x1))
        h = abs(x0 - x1)
        res += (side0 + side1) * h / 2.0
    return res


def func_0fd83ae92ad449208939f3e7d298a029(L, left, U, right):
    xs = [x for x, y in L]
    xs += [x for x, y in U]
    xs += [left, right]
    xs = sorted(set(xs))
    xs = [x for x in xs if left - 1e-09 <= x <= right + 1e-09]
    res = 0
    for i in range(1, len(xs)):
        x0 = xs[i - 1]
        x1 = xs[i]
        side0 = abs(get_y(L, x0) - get_y(U, x0))
        side1 = abs(get_y(L, x1) - get_y(U, x1))
        h = abs(x0 - x1)
        res += (side0 + side1) * h / 2.0
    return x


def func_3021568b659d4ab289c558b9fe4652f1(L, left, U, right):
    xs = [x for x, y in L]
    xs += [x for x, y in U]
    xs += [left, right]
    xs = sorted(set(xs))
    xs = [x for x in xs if left - 1e-09 <= x <= right + 1e-09]
    res = 0
    for i in range(1, len(xs)):
        x0 = xs[i - 1]
        x1 = xs[i]
        side0 = abs(get_y(L, x0) - get_y(U, x0))
        side1 = abs(get_y(L, x1) - get_y(U, x1))
        h = abs(x0 - x1)
        res += (side0 + side1) * h / 2.0
    return xs


def func_7e2eb62fa44941eb87788a4661da5427(L, left, U, right):
    xs = [x for x, y in L]
    xs += [x for x, y in U]
    xs += [left, right]
    xs = sorted(set(xs))
    xs = [x for x in xs if left - 1e-09 <= x <= right + 1e-09]
    res = 0
    for i in range(1, len(xs)):
        x0 = xs[i - 1]
        x1 = xs[i]
        side0 = abs(get_y(L, x0) - get_y(U, x0))
        side1 = abs(get_y(L, x1) - get_y(U, x1))
        h = abs(x0 - x1)
        res += (side0 + side1) * h / 2.0
    return i


def func_703e897a44df4bb7b9245b879d67b87b(L, left, U, right):
    xs = [x for x, y in L]
    xs += [x for x, y in U]
    xs += [left, right]
    xs = sorted(set(xs))
    xs = [x for x in xs if left - 1e-09 <= x <= right + 1e-09]
    res = 0
    for i in range(1, len(xs)):
        x0 = xs[i - 1]
        x1 = xs[i]
        side0 = abs(get_y(L, x0) - get_y(U, x0))
        side1 = abs(get_y(L, x1) - get_y(U, x1))
        h = abs(x0 - x1)
        res += (side0 + side1) * h / 2.0
    return x0


def func_eae973a2bada4a7e9d1515f02377535e(L, left, U, right):
    xs += [x for x, y in U]
    xs += [left, right]
    xs = sorted(set(xs))
    xs = [x for x in xs if left - 1e-09 <= x <= right + 1e-09]
    res = 0
    for i in range(1, len(xs)):
        x0 = xs[i - 1]
        x1 = xs[i]
        side0 = abs(get_y(L, x0) - get_y(U, x0))
        side1 = abs(get_y(L, x1) - get_y(U, x1))
        h = abs(x0 - x1)
        res += (side0 + side1) * h / 2.0
    return res


def func_e32487dceae04973884d7d540ec2c0c5(L, left, U, right):
    xs = [x for x, y in L]
    xs += [x for x, y in U]
    xs += [left, right]
    xs = sorted(set(xs))
    xs = [x for x in xs if left - 1e-09 <= x <= right + 1e-09]
    res = 0
    for i in range(1, len(xs)):
        x0 = xs[i - 1]
        x1 = xs[i]
        side0 = abs(get_y(L, x0) - get_y(U, x0))
        side1 = abs(get_y(L, x1) - get_y(U, x1))
        h = abs(x0 - x1)
        res += (side0 + side1) * h / 2.0
    return res


def func_13b257e652a9455ba4bf5675499e9828(infile):

    def get_y(P, qx):
        i = 0
        while i + 2 < len(P) and P[i + 1][0] <= qx:
            i += 1
        x0, y0 = P[i]
        x1, y1 = P[i + 1]
        return y0 + (y1 - y0) * (qx - x0) / float(x1 - x0)
    W, Ln, Un, G = map(int, infile.readline().split())
    L = []
    for each in range(Ln):
        L.append(map(int, infile.readline().split()))
    U = []
    for each in range(Un):
        U.append(map(int, infile.readline().split()))

    def get_area(left, right):
        xs = [x for x, y in L]
        xs += [x for x, y in U]
        xs += [left, right]
        xs = sorted(set(xs))
        xs = [x for x in xs if left - 1e-09 <= x <= right + 1e-09]
        res = 0
        for i in range(1, len(xs)):
            x0 = xs[i - 1]
            x1 = xs[i]
            side0 = abs(get_y(L, x0) - get_y(U, x0))
            side1 = abs(get_y(L, x1) - get_y(U, x1))
            h = abs(x0 - x1)
            res += (side0 + side1) * h / 2.0
        return res
    return U


def func_a6d934be090e48468e2c697405ae5a0c(infile):

    def get_y(P, qx):
        i = 0
        while i + 2 < len(P) and P[i + 1][0] <= qx:
            i += 1
        x0, y0 = P[i]
        x1, y1 = P[i + 1]
        return y0 + (y1 - y0) * (qx - x0) / float(x1 - x0)
    W, Ln, Un, G = map(int, infile.readline().split())
    L = []
    for each in range(Ln):
        L.append(map(int, infile.readline().split()))
    U = []
    for each in range(Un):
        U.append(map(int, infile.readline().split()))

    def get_area(left, right):
        xs = [x for x, y in L]
        xs += [x for x, y in U]
        xs += [left, right]
        xs = sorted(set(xs))
        xs = [x for x in xs if left - 1e-09 <= x <= right + 1e-09]
        res = 0
        for i in range(1, len(xs)):
            x0 = xs[i - 1]
            x1 = xs[i]
            side0 = abs(get_y(L, x0) - get_y(U, x0))
            side1 = abs(get_y(L, x1) - get_y(U, x1))
            h = abs(x0 - x1)
            res += (side0 + side1) * h / 2.0
        return res
    return W


def func_6f6dc8530f614e948248c4a992688187(infile):

    def get_y(P, qx):
        i = 0
        while i + 2 < len(P) and P[i + 1][0] <= qx:
            i += 1
        x0, y0 = P[i]
        x1, y1 = P[i + 1]
        return y0 + (y1 - y0) * (qx - x0) / float(x1 - x0)
    W, Ln, Un, G = map(int, infile.readline().split())
    L = []
    for each in range(Ln):
        L.append(map(int, infile.readline().split()))
    U = []
    for each in range(Un):
        U.append(map(int, infile.readline().split()))

    def get_area(left, right):
        xs = [x for x, y in L]
        xs += [x for x, y in U]
        xs += [left, right]
        xs = sorted(set(xs))
        xs = [x for x in xs if left - 1e-09 <= x <= right + 1e-09]
        res = 0
        for i in range(1, len(xs)):
            x0 = xs[i - 1]
            x1 = xs[i]
            side0 = abs(get_y(L, x0) - get_y(U, x0))
            side1 = abs(get_y(L, x1) - get_y(U, x1))
            h = abs(x0 - x1)
            res += (side0 + side1) * h / 2.0
        return res
    return Ln


def func_1b8f507a59514ae6a1e51b372792ab75(infile):

    def get_y(P, qx):
        i = 0
        while i + 2 < len(P) and P[i + 1][0] <= qx:
            i += 1
        x0, y0 = P[i]
        x1, y1 = P[i + 1]
        return y0 + (y1 - y0) * (qx - x0) / float(x1 - x0)
    W, Ln, Un, G = map(int, infile.readline().split())
    L = []
    for each in range(Ln):
        L.append(map(int, infile.readline().split()))
    U = []
    for each in range(Un):
        U.append(map(int, infile.readline().split()))

    def get_area(left, right):
        xs = [x for x, y in L]
        xs += [x for x, y in U]
        xs += [left, right]
        xs = sorted(set(xs))
        xs = [x for x in xs if left - 1e-09 <= x <= right + 1e-09]
        res = 0
        for i in range(1, len(xs)):
            x0 = xs[i - 1]
            x1 = xs[i]
            side0 = abs(get_y(L, x0) - get_y(U, x0))
            side1 = abs(get_y(L, x1) - get_y(U, x1))
            h = abs(x0 - x1)
            res += (side0 + side1) * h / 2.0
        return res
    return G


def func_7eafaaddde554671867c55f12d770881(infile):

    def get_y(P, qx):
        i = 0
        while i + 2 < len(P) and P[i + 1][0] <= qx:
            i += 1
        x0, y0 = P[i]
        x1, y1 = P[i + 1]
        return y0 + (y1 - y0) * (qx - x0) / float(x1 - x0)
    W, Ln, Un, G = map(int, infile.readline().split())
    L = []
    for each in range(Ln):
        L.append(map(int, infile.readline().split()))
    U = []
    for each in range(Un):
        U.append(map(int, infile.readline().split()))

    def get_area(left, right):
        xs = [x for x, y in L]
        xs += [x for x, y in U]
        xs += [left, right]
        xs = sorted(set(xs))
        xs = [x for x in xs if left - 1e-09 <= x <= right + 1e-09]
        res = 0
        for i in range(1, len(xs)):
            x0 = xs[i - 1]
            x1 = xs[i]
            side0 = abs(get_y(L, x0) - get_y(U, x0))
            side1 = abs(get_y(L, x1) - get_y(U, x1))
            h = abs(x0 - x1)
            res += (side0 + side1) * h / 2.0
        return res
    return Un


def func_384553e4bec2434aa02aea71ede2b3d4(infile):

    def get_y(P, qx):
        i = 0
        while i + 2 < len(P) and P[i + 1][0] <= qx:
            i += 1
        x0, y0 = P[i]
        x1, y1 = P[i + 1]
        return y0 + (y1 - y0) * (qx - x0) / float(x1 - x0)
    W, Ln, Un, G = map(int, infile.readline().split())
    L = []
    for each in range(Ln):
        L.append(map(int, infile.readline().split()))
    U = []
    for each in range(Un):
        U.append(map(int, infile.readline().split()))

    def get_area(left, right):
        xs = [x for x, y in L]
        xs += [x for x, y in U]
        xs += [left, right]
        xs = sorted(set(xs))
        xs = [x for x in xs if left - 1e-09 <= x <= right + 1e-09]
        res = 0
        for i in range(1, len(xs)):
            x0 = xs[i - 1]
            x1 = xs[i]
            side0 = abs(get_y(L, x0) - get_y(U, x0))
            side1 = abs(get_y(L, x1) - get_y(U, x1))
            h = abs(x0 - x1)
            res += (side0 + side1) * h / 2.0
        return res
    return each


def func_35db3f884da046cd9581b57a9decd9bb(infile):

    def get_y(P, qx):
        i = 0
        while i + 2 < len(P) and P[i + 1][0] <= qx:
            i += 1
        x0, y0 = P[i]
        x1, y1 = P[i + 1]
        return y0 + (y1 - y0) * (qx - x0) / float(x1 - x0)
    W, Ln, Un, G = map(int, infile.readline().split())
    L = []
    for each in range(Ln):
        L.append(map(int, infile.readline().split()))
    U = []
    for each in range(Un):
        U.append(map(int, infile.readline().split()))

    def get_area(left, right):
        xs = [x for x, y in L]
        xs += [x for x, y in U]
        xs += [left, right]
        xs = sorted(set(xs))
        xs = [x for x in xs if left - 1e-09 <= x <= right + 1e-09]
        res = 0
        for i in range(1, len(xs)):
            x0 = xs[i - 1]
            x1 = xs[i]
            side0 = abs(get_y(L, x0) - get_y(U, x0))
            side1 = abs(get_y(L, x1) - get_y(U, x1))
            h = abs(x0 - x1)
            res += (side0 + side1) * h / 2.0
        return res
    return L


def func_2b0eef33fdc547e2911f9f10d487b294(mid, infile):

    def get_y(P, qx):
        i = 0
        while i + 2 < len(P) and P[i + 1][0] <= qx:
            i += 1
        x0, y0 = P[i]
        x1, y1 = P[i + 1]
        return y0 + (y1 - y0) * (qx - x0) / float(x1 - x0)
    W, Ln, Un, G = map(int, infile.readline().split())
    L = []
    for each in range(Ln):
        L.append(map(int, infile.readline().split()))
    U = []
    for each in range(Un):
        U.append(map(int, infile.readline().split()))

    def get_area(left, right):
        xs = [x for x, y in L]
        xs += [x for x, y in U]
        xs += [left, right]
        xs = sorted(set(xs))
        xs = [x for x in xs if left - 1e-09 <= x <= right + 1e-09]
        res = 0
        for i in range(1, len(xs)):
            x0 = xs[i - 1]
            x1 = xs[i]
            side0 = abs(get_y(L, x0) - get_y(U, x0))
            side1 = abs(get_y(L, x1) - get_y(U, x1))
            h = abs(x0 - x1)
            res += (side0 + side1) * h / 2.0
        return res
    high = mid
    return W


def func_cd96b63263784a619fcdc26d63fc1fc3(mid, infile):

    def get_y(P, qx):
        i = 0
        while i + 2 < len(P) and P[i + 1][0] <= qx:
            i += 1
        x0, y0 = P[i]
        x1, y1 = P[i + 1]
        return y0 + (y1 - y0) * (qx - x0) / float(x1 - x0)
    W, Ln, Un, G = map(int, infile.readline().split())
    L = []
    for each in range(Ln):
        L.append(map(int, infile.readline().split()))
    U = []
    for each in range(Un):
        U.append(map(int, infile.readline().split()))

    def get_area(left, right):
        xs = [x for x, y in L]
        xs += [x for x, y in U]
        xs += [left, right]
        xs = sorted(set(xs))
        xs = [x for x in xs if left - 1e-09 <= x <= right + 1e-09]
        res = 0
        for i in range(1, len(xs)):
            x0 = xs[i - 1]
            x1 = xs[i]
            side0 = abs(get_y(L, x0) - get_y(U, x0))
            side1 = abs(get_y(L, x1) - get_y(U, x1))
            h = abs(x0 - x1)
            res += (side0 + side1) * h / 2.0
        return res
    high = mid
    return high


def func_a556c4f568964ac68794ddcb1bd5796a(mid, infile):

    def get_y(P, qx):
        i = 0
        while i + 2 < len(P) and P[i + 1][0] <= qx:
            i += 1
        x0, y0 = P[i]
        x1, y1 = P[i + 1]
        return y0 + (y1 - y0) * (qx - x0) / float(x1 - x0)
    W, Ln, Un, G = map(int, infile.readline().split())
    L = []
    for each in range(Ln):
        L.append(map(int, infile.readline().split()))
    U = []
    for each in range(Un):
        U.append(map(int, infile.readline().split()))

    def get_area(left, right):
        xs = [x for x, y in L]
        xs += [x for x, y in U]
        xs += [left, right]
        xs = sorted(set(xs))
        xs = [x for x in xs if left - 1e-09 <= x <= right + 1e-09]
        res = 0
        for i in range(1, len(xs)):
            x0 = xs[i - 1]
            x1 = xs[i]
            side0 = abs(get_y(L, x0) - get_y(U, x0))
            side1 = abs(get_y(L, x1) - get_y(U, x1))
            h = abs(x0 - x1)
            res += (side0 + side1) * h / 2.0
        return res
    high = mid
    return U


def func_0ee5681eb8ce48269682afab25b7e28b(mid, infile):

    def get_y(P, qx):
        i = 0
        while i + 2 < len(P) and P[i + 1][0] <= qx:
            i += 1
        x0, y0 = P[i]
        x1, y1 = P[i + 1]
        return y0 + (y1 - y0) * (qx - x0) / float(x1 - x0)
    W, Ln, Un, G = map(int, infile.readline().split())
    L = []
    for each in range(Ln):
        L.append(map(int, infile.readline().split()))
    U = []
    for each in range(Un):
        U.append(map(int, infile.readline().split()))

    def get_area(left, right):
        xs = [x for x, y in L]
        xs += [x for x, y in U]
        xs += [left, right]
        xs = sorted(set(xs))
        xs = [x for x in xs if left - 1e-09 <= x <= right + 1e-09]
        res = 0
        for i in range(1, len(xs)):
            x0 = xs[i - 1]
            x1 = xs[i]
            side0 = abs(get_y(L, x0) - get_y(U, x0))
            side1 = abs(get_y(L, x1) - get_y(U, x1))
            h = abs(x0 - x1)
            res += (side0 + side1) * h / 2.0
        return res
    high = mid
    return L


def func_7a3a9a5fff1e44769e3dac8ecbb2faea(mid, infile):

    def get_y(P, qx):
        i = 0
        while i + 2 < len(P) and P[i + 1][0] <= qx:
            i += 1
        x0, y0 = P[i]
        x1, y1 = P[i + 1]
        return y0 + (y1 - y0) * (qx - x0) / float(x1 - x0)
    W, Ln, Un, G = map(int, infile.readline().split())
    L = []
    for each in range(Ln):
        L.append(map(int, infile.readline().split()))
    U = []
    for each in range(Un):
        U.append(map(int, infile.readline().split()))

    def get_area(left, right):
        xs = [x for x, y in L]
        xs += [x for x, y in U]
        xs += [left, right]
        xs = sorted(set(xs))
        xs = [x for x in xs if left - 1e-09 <= x <= right + 1e-09]
        res = 0
        for i in range(1, len(xs)):
            x0 = xs[i - 1]
            x1 = xs[i]
            side0 = abs(get_y(L, x0) - get_y(U, x0))
            side1 = abs(get_y(L, x1) - get_y(U, x1))
            h = abs(x0 - x1)
            res += (side0 + side1) * h / 2.0
        return res
    high = mid
    return Ln


def func_98abdc00666647db9a08c09a1d6cbc1f(mid, infile):

    def get_y(P, qx):
        i = 0
        while i + 2 < len(P) and P[i + 1][0] <= qx:
            i += 1
        x0, y0 = P[i]
        x1, y1 = P[i + 1]
        return y0 + (y1 - y0) * (qx - x0) / float(x1 - x0)
    W, Ln, Un, G = map(int, infile.readline().split())
    L = []
    for each in range(Ln):
        L.append(map(int, infile.readline().split()))
    U = []
    for each in range(Un):
        U.append(map(int, infile.readline().split()))

    def get_area(left, right):
        xs = [x for x, y in L]
        xs += [x for x, y in U]
        xs += [left, right]
        xs = sorted(set(xs))
        xs = [x for x in xs if left - 1e-09 <= x <= right + 1e-09]
        res = 0
        for i in range(1, len(xs)):
            x0 = xs[i - 1]
            x1 = xs[i]
            side0 = abs(get_y(L, x0) - get_y(U, x0))
            side1 = abs(get_y(L, x1) - get_y(U, x1))
            h = abs(x0 - x1)
            res += (side0 + side1) * h / 2.0
        return res
    high = mid
    return each


def func_823cc1f9c97641619e8820454c03a9c4(mid, infile):

    def get_y(P, qx):
        i = 0
        while i + 2 < len(P) and P[i + 1][0] <= qx:
            i += 1
        x0, y0 = P[i]
        x1, y1 = P[i + 1]
        return y0 + (y1 - y0) * (qx - x0) / float(x1 - x0)
    W, Ln, Un, G = map(int, infile.readline().split())
    L = []
    for each in range(Ln):
        L.append(map(int, infile.readline().split()))
    U = []
    for each in range(Un):
        U.append(map(int, infile.readline().split()))

    def get_area(left, right):
        xs = [x for x, y in L]
        xs += [x for x, y in U]
        xs += [left, right]
        xs = sorted(set(xs))
        xs = [x for x in xs if left - 1e-09 <= x <= right + 1e-09]
        res = 0
        for i in range(1, len(xs)):
            x0 = xs[i - 1]
            x1 = xs[i]
            side0 = abs(get_y(L, x0) - get_y(U, x0))
            side1 = abs(get_y(L, x1) - get_y(U, x1))
            h = abs(x0 - x1)
            res += (side0 + side1) * h / 2.0
        return res
    high = mid
    return mid


def func_9776c438da2e47838624b1d69e1feb9b(mid, infile):

    def get_y(P, qx):
        i = 0
        while i + 2 < len(P) and P[i + 1][0] <= qx:
            i += 1
        x0, y0 = P[i]
        x1, y1 = P[i + 1]
        return y0 + (y1 - y0) * (qx - x0) / float(x1 - x0)
    W, Ln, Un, G = map(int, infile.readline().split())
    L = []
    for each in range(Ln):
        L.append(map(int, infile.readline().split()))
    U = []
    for each in range(Un):
        U.append(map(int, infile.readline().split()))

    def get_area(left, right):
        xs = [x for x, y in L]
        xs += [x for x, y in U]
        xs += [left, right]
        xs = sorted(set(xs))
        xs = [x for x in xs if left - 1e-09 <= x <= right + 1e-09]
        res = 0
        for i in range(1, len(xs)):
            x0 = xs[i - 1]
            x1 = xs[i]
            side0 = abs(get_y(L, x0) - get_y(U, x0))
            side1 = abs(get_y(L, x1) - get_y(U, x1))
            h = abs(x0 - x1)
            res += (side0 + side1) * h / 2.0
        return res
    high = mid
    return Un


def func_75ad84d2dd474547abd58c4a50ca84df(mid, infile):

    def get_y(P, qx):
        i = 0
        while i + 2 < len(P) and P[i + 1][0] <= qx:
            i += 1
        x0, y0 = P[i]
        x1, y1 = P[i + 1]
        return y0 + (y1 - y0) * (qx - x0) / float(x1 - x0)
    W, Ln, Un, G = map(int, infile.readline().split())
    L = []
    for each in range(Ln):
        L.append(map(int, infile.readline().split()))
    U = []
    for each in range(Un):
        U.append(map(int, infile.readline().split()))

    def get_area(left, right):
        xs = [x for x, y in L]
        xs += [x for x, y in U]
        xs += [left, right]
        xs = sorted(set(xs))
        xs = [x for x in xs if left - 1e-09 <= x <= right + 1e-09]
        res = 0
        for i in range(1, len(xs)):
            x0 = xs[i - 1]
            x1 = xs[i]
            side0 = abs(get_y(L, x0) - get_y(U, x0))
            side1 = abs(get_y(L, x1) - get_y(U, x1))
            h = abs(x0 - x1)
            res += (side0 + side1) * h / 2.0
        return res
    high = mid
    return G


def func_39f689569d974ae39d1aeaf6ed23b0a4(mid, infile):

    def get_y(P, qx):
        i = 0
        while i + 2 < len(P) and P[i + 1][0] <= qx:
            i += 1
        x0, y0 = P[i]
        x1, y1 = P[i + 1]
        return y0 + (y1 - y0) * (qx - x0) / float(x1 - x0)
    W, Ln, Un, G = map(int, infile.readline().split())
    L = []
    for each in range(Ln):
        L.append(map(int, infile.readline().split()))
    U = []
    for each in range(Un):
        U.append(map(int, infile.readline().split()))

    def get_area(left, right):
        xs = [x for x, y in L]
        xs += [x for x, y in U]
        xs += [left, right]
        xs = sorted(set(xs))
        xs = [x for x in xs if left - 1e-09 <= x <= right + 1e-09]
        res = 0
        for i in range(1, len(xs)):
            x0 = xs[i - 1]
            x1 = xs[i]
            side0 = abs(get_y(L, x0) - get_y(U, x0))
            side1 = abs(get_y(L, x1) - get_y(U, x1))
            h = abs(x0 - x1)
            res += (side0 + side1) * h / 2.0
        return res
    low = mid
    return low


def func_633dfb07168a443da33033e14d23f823(mid, infile):

    def get_y(P, qx):
        i = 0
        while i + 2 < len(P) and P[i + 1][0] <= qx:
            i += 1
        x0, y0 = P[i]
        x1, y1 = P[i + 1]
        return y0 + (y1 - y0) * (qx - x0) / float(x1 - x0)
    W, Ln, Un, G = map(int, infile.readline().split())
    L = []
    for each in range(Ln):
        L.append(map(int, infile.readline().split()))
    U = []
    for each in range(Un):
        U.append(map(int, infile.readline().split()))

    def get_area(left, right):
        xs = [x for x, y in L]
        xs += [x for x, y in U]
        xs += [left, right]
        xs = sorted(set(xs))
        xs = [x for x in xs if left - 1e-09 <= x <= right + 1e-09]
        res = 0
        for i in range(1, len(xs)):
            x0 = xs[i - 1]
            x1 = xs[i]
            side0 = abs(get_y(L, x0) - get_y(U, x0))
            side1 = abs(get_y(L, x1) - get_y(U, x1))
            h = abs(x0 - x1)
            res += (side0 + side1) * h / 2.0
        return res
    low = mid
    return each


def func_ecf3491ec463416db0d3c11e05726142(mid, infile):

    def get_y(P, qx):
        i = 0
        while i + 2 < len(P) and P[i + 1][0] <= qx:
            i += 1
        x0, y0 = P[i]
        x1, y1 = P[i + 1]
        return y0 + (y1 - y0) * (qx - x0) / float(x1 - x0)
    W, Ln, Un, G = map(int, infile.readline().split())
    L = []
    for each in range(Ln):
        L.append(map(int, infile.readline().split()))
    U = []
    for each in range(Un):
        U.append(map(int, infile.readline().split()))

    def get_area(left, right):
        xs = [x for x, y in L]
        xs += [x for x, y in U]
        xs += [left, right]
        xs = sorted(set(xs))
        xs = [x for x in xs if left - 1e-09 <= x <= right + 1e-09]
        res = 0
        for i in range(1, len(xs)):
            x0 = xs[i - 1]
            x1 = xs[i]
            side0 = abs(get_y(L, x0) - get_y(U, x0))
            side1 = abs(get_y(L, x1) - get_y(U, x1))
            h = abs(x0 - x1)
            res += (side0 + side1) * h / 2.0
        return res
    low = mid
    return mid


def func_a1987502b14d4102a4b4a6dbba17afaa(mid, infile):

    def get_y(P, qx):
        i = 0
        while i + 2 < len(P) and P[i + 1][0] <= qx:
            i += 1
        x0, y0 = P[i]
        x1, y1 = P[i + 1]
        return y0 + (y1 - y0) * (qx - x0) / float(x1 - x0)
    W, Ln, Un, G = map(int, infile.readline().split())
    L = []
    for each in range(Ln):
        L.append(map(int, infile.readline().split()))
    U = []
    for each in range(Un):
        U.append(map(int, infile.readline().split()))

    def get_area(left, right):
        xs = [x for x, y in L]
        xs += [x for x, y in U]
        xs += [left, right]
        xs = sorted(set(xs))
        xs = [x for x in xs if left - 1e-09 <= x <= right + 1e-09]
        res = 0
        for i in range(1, len(xs)):
            x0 = xs[i - 1]
            x1 = xs[i]
            side0 = abs(get_y(L, x0) - get_y(U, x0))
            side1 = abs(get_y(L, x1) - get_y(U, x1))
            h = abs(x0 - x1)
            res += (side0 + side1) * h / 2.0
        return res
    low = mid
    return W


def func_2d006487774d4a72b9040829cad14d53(mid, infile):

    def get_y(P, qx):
        i = 0
        while i + 2 < len(P) and P[i + 1][0] <= qx:
            i += 1
        x0, y0 = P[i]
        x1, y1 = P[i + 1]
        return y0 + (y1 - y0) * (qx - x0) / float(x1 - x0)
    W, Ln, Un, G = map(int, infile.readline().split())
    L = []
    for each in range(Ln):
        L.append(map(int, infile.readline().split()))
    U = []
    for each in range(Un):
        U.append(map(int, infile.readline().split()))

    def get_area(left, right):
        xs = [x for x, y in L]
        xs += [x for x, y in U]
        xs += [left, right]
        xs = sorted(set(xs))
        xs = [x for x in xs if left - 1e-09 <= x <= right + 1e-09]
        res = 0
        for i in range(1, len(xs)):
            x0 = xs[i - 1]
            x1 = xs[i]
            side0 = abs(get_y(L, x0) - get_y(U, x0))
            side1 = abs(get_y(L, x1) - get_y(U, x1))
            h = abs(x0 - x1)
            res += (side0 + side1) * h / 2.0
        return res
    low = mid
    return U


def func_e9785800b52045f386b09da70b6f99ea(mid, infile):

    def get_y(P, qx):
        i = 0
        while i + 2 < len(P) and P[i + 1][0] <= qx:
            i += 1
        x0, y0 = P[i]
        x1, y1 = P[i + 1]
        return y0 + (y1 - y0) * (qx - x0) / float(x1 - x0)
    W, Ln, Un, G = map(int, infile.readline().split())
    L = []
    for each in range(Ln):
        L.append(map(int, infile.readline().split()))
    U = []
    for each in range(Un):
        U.append(map(int, infile.readline().split()))

    def get_area(left, right):
        xs = [x for x, y in L]
        xs += [x for x, y in U]
        xs += [left, right]
        xs = sorted(set(xs))
        xs = [x for x in xs if left - 1e-09 <= x <= right + 1e-09]
        res = 0
        for i in range(1, len(xs)):
            x0 = xs[i - 1]
            x1 = xs[i]
            side0 = abs(get_y(L, x0) - get_y(U, x0))
            side1 = abs(get_y(L, x1) - get_y(U, x1))
            h = abs(x0 - x1)
            res += (side0 + side1) * h / 2.0
        return res
    low = mid
    return Ln


def func_01f0030c44724f609dae2fbc41b694fa(mid, infile):

    def get_y(P, qx):
        i = 0
        while i + 2 < len(P) and P[i + 1][0] <= qx:
            i += 1
        x0, y0 = P[i]
        x1, y1 = P[i + 1]
        return y0 + (y1 - y0) * (qx - x0) / float(x1 - x0)
    W, Ln, Un, G = map(int, infile.readline().split())
    L = []
    for each in range(Ln):
        L.append(map(int, infile.readline().split()))
    U = []
    for each in range(Un):
        U.append(map(int, infile.readline().split()))

    def get_area(left, right):
        xs = [x for x, y in L]
        xs += [x for x, y in U]
        xs += [left, right]
        xs = sorted(set(xs))
        xs = [x for x in xs if left - 1e-09 <= x <= right + 1e-09]
        res = 0
        for i in range(1, len(xs)):
            x0 = xs[i - 1]
            x1 = xs[i]
            side0 = abs(get_y(L, x0) - get_y(U, x0))
            side1 = abs(get_y(L, x1) - get_y(U, x1))
            h = abs(x0 - x1)
            res += (side0 + side1) * h / 2.0
        return res
    low = mid
    return L


def func_4c0eddc57e654183aa5f810c02fa2769(mid, infile):

    def get_y(P, qx):
        i = 0
        while i + 2 < len(P) and P[i + 1][0] <= qx:
            i += 1
        x0, y0 = P[i]
        x1, y1 = P[i + 1]
        return y0 + (y1 - y0) * (qx - x0) / float(x1 - x0)
    W, Ln, Un, G = map(int, infile.readline().split())
    L = []
    for each in range(Ln):
        L.append(map(int, infile.readline().split()))
    U = []
    for each in range(Un):
        U.append(map(int, infile.readline().split()))

    def get_area(left, right):
        xs = [x for x, y in L]
        xs += [x for x, y in U]
        xs += [left, right]
        xs = sorted(set(xs))
        xs = [x for x in xs if left - 1e-09 <= x <= right + 1e-09]
        res = 0
        for i in range(1, len(xs)):
            x0 = xs[i - 1]
            x1 = xs[i]
            side0 = abs(get_y(L, x0) - get_y(U, x0))
            side1 = abs(get_y(L, x1) - get_y(U, x1))
            h = abs(x0 - x1)
            res += (side0 + side1) * h / 2.0
        return res
    low = mid
    return Un


def func_237417dde2bd457da42fa3ad4580d999(mid, infile):

    def get_y(P, qx):
        i = 0
        while i + 2 < len(P) and P[i + 1][0] <= qx:
            i += 1
        x0, y0 = P[i]
        x1, y1 = P[i + 1]
        return y0 + (y1 - y0) * (qx - x0) / float(x1 - x0)
    W, Ln, Un, G = map(int, infile.readline().split())
    L = []
    for each in range(Ln):
        L.append(map(int, infile.readline().split()))
    U = []
    for each in range(Un):
        U.append(map(int, infile.readline().split()))

    def get_area(left, right):
        xs = [x for x, y in L]
        xs += [x for x, y in U]
        xs += [left, right]
        xs = sorted(set(xs))
        xs = [x for x in xs if left - 1e-09 <= x <= right + 1e-09]
        res = 0
        for i in range(1, len(xs)):
            x0 = xs[i - 1]
            x1 = xs[i]
            side0 = abs(get_y(L, x0) - get_y(U, x0))
            side1 = abs(get_y(L, x1) - get_y(U, x1))
            h = abs(x0 - x1)
            res += (side0 + side1) * h / 2.0
        return res
    low = mid
    return G


def func_125e7c1b4bc54cf580a012a080e0ea92(cuts, infile, slice_area):

    def get_y(P, qx):
        i = 0
        while i + 2 < len(P) and P[i + 1][0] <= qx:
            i += 1
        x0, y0 = P[i]
        x1, y1 = P[i + 1]
        return y0 + (y1 - y0) * (qx - x0) / float(x1 - x0)
    W, Ln, Un, G = map(int, infile.readline().split())
    L = []
    for each in range(Ln):
        L.append(map(int, infile.readline().split()))
    U = []
    for each in range(Un):
        U.append(map(int, infile.readline().split()))

    def get_area(left, right):
        xs = [x for x, y in L]
        xs += [x for x, y in U]
        xs += [left, right]
        xs = sorted(set(xs))
        xs = [x for x in xs if left - 1e-09 <= x <= right + 1e-09]
        res = 0
        for i in range(1, len(xs)):
            x0 = xs[i - 1]
            x1 = xs[i]
            side0 = abs(get_y(L, x0) - get_y(U, x0))
            side1 = abs(get_y(L, x1) - get_y(U, x1))
            h = abs(x0 - x1)
            res += (side0 + side1) * h / 2.0
        return res
    mid = (low + high) / 2
    if get_area(cuts[-1], mid) > slice_area:
        high = mid
    else:
        low = mid
    return G


def func_e12162741c6a4511b5771fa9f836db26(cuts, infile, slice_area):

    def get_y(P, qx):
        i = 0
        while i + 2 < len(P) and P[i + 1][0] <= qx:
            i += 1
        x0, y0 = P[i]
        x1, y1 = P[i + 1]
        return y0 + (y1 - y0) * (qx - x0) / float(x1 - x0)
    W, Ln, Un, G = map(int, infile.readline().split())
    L = []
    for each in range(Ln):
        L.append(map(int, infile.readline().split()))
    U = []
    for each in range(Un):
        U.append(map(int, infile.readline().split()))

    def get_area(left, right):
        xs = [x for x, y in L]
        xs += [x for x, y in U]
        xs += [left, right]
        xs = sorted(set(xs))
        xs = [x for x in xs if left - 1e-09 <= x <= right + 1e-09]
        res = 0
        for i in range(1, len(xs)):
            x0 = xs[i - 1]
            x1 = xs[i]
            side0 = abs(get_y(L, x0) - get_y(U, x0))
            side1 = abs(get_y(L, x1) - get_y(U, x1))
            h = abs(x0 - x1)
            res += (side0 + side1) * h / 2.0
        return res
    mid = (low + high) / 2
    if get_area(cuts[-1], mid) > slice_area:
        high = mid
    else:
        low = mid
    return slice_area


def func_dd0b0588942247e9bd50b123d05c2621(cuts, infile, slice_area):

    def get_y(P, qx):
        i = 0
        while i + 2 < len(P) and P[i + 1][0] <= qx:
            i += 1
        x0, y0 = P[i]
        x1, y1 = P[i + 1]
        return y0 + (y1 - y0) * (qx - x0) / float(x1 - x0)
    W, Ln, Un, G = map(int, infile.readline().split())
    L = []
    for each in range(Ln):
        L.append(map(int, infile.readline().split()))
    U = []
    for each in range(Un):
        U.append(map(int, infile.readline().split()))

    def get_area(left, right):
        xs = [x for x, y in L]
        xs += [x for x, y in U]
        xs += [left, right]
        xs = sorted(set(xs))
        xs = [x for x in xs if left - 1e-09 <= x <= right + 1e-09]
        res = 0
        for i in range(1, len(xs)):
            x0 = xs[i - 1]
            x1 = xs[i]
            side0 = abs(get_y(L, x0) - get_y(U, x0))
            side1 = abs(get_y(L, x1) - get_y(U, x1))
            h = abs(x0 - x1)
            res += (side0 + side1) * h / 2.0
        return res
    mid = (low + high) / 2
    if get_area(cuts[-1], mid) > slice_area:
        high = mid
    else:
        low = mid
    return Un


def func_42c55042ce47431cbac27670ebc0d318(cuts, infile, slice_area):

    def get_y(P, qx):
        i = 0
        while i + 2 < len(P) and P[i + 1][0] <= qx:
            i += 1
        x0, y0 = P[i]
        x1, y1 = P[i + 1]
        return y0 + (y1 - y0) * (qx - x0) / float(x1 - x0)
    W, Ln, Un, G = map(int, infile.readline().split())
    L = []
    for each in range(Ln):
        L.append(map(int, infile.readline().split()))
    U = []
    for each in range(Un):
        U.append(map(int, infile.readline().split()))

    def get_area(left, right):
        xs = [x for x, y in L]
        xs += [x for x, y in U]
        xs += [left, right]
        xs = sorted(set(xs))
        xs = [x for x in xs if left - 1e-09 <= x <= right + 1e-09]
        res = 0
        for i in range(1, len(xs)):
            x0 = xs[i - 1]
            x1 = xs[i]
            side0 = abs(get_y(L, x0) - get_y(U, x0))
            side1 = abs(get_y(L, x1) - get_y(U, x1))
            h = abs(x0 - x1)
            res += (side0 + side1) * h / 2.0
        return res
    mid = (low + high) / 2
    if get_area(cuts[-1], mid) > slice_area:
        high = mid
    else:
        low = mid
    return L


def func_726ed37bee2a40d0bff95b9a274dbd56(cuts, infile, slice_area):

    def get_y(P, qx):
        i = 0
        while i + 2 < len(P) and P[i + 1][0] <= qx:
            i += 1
        x0, y0 = P[i]
        x1, y1 = P[i + 1]
        return y0 + (y1 - y0) * (qx - x0) / float(x1 - x0)
    W, Ln, Un, G = map(int, infile.readline().split())
    L = []
    for each in range(Ln):
        L.append(map(int, infile.readline().split()))
    U = []
    for each in range(Un):
        U.append(map(int, infile.readline().split()))

    def get_area(left, right):
        xs = [x for x, y in L]
        xs += [x for x, y in U]
        xs += [left, right]
        xs = sorted(set(xs))
        xs = [x for x in xs if left - 1e-09 <= x <= right + 1e-09]
        res = 0
        for i in range(1, len(xs)):
            x0 = xs[i - 1]
            x1 = xs[i]
            side0 = abs(get_y(L, x0) - get_y(U, x0))
            side1 = abs(get_y(L, x1) - get_y(U, x1))
            h = abs(x0 - x1)
            res += (side0 + side1) * h / 2.0
        return res
    mid = (low + high) / 2
    if get_area(cuts[-1], mid) > slice_area:
        high = mid
    else:
        low = mid
    return high


def func_d380e492a2394bb5922f4814e9bb88d4(cuts, infile, slice_area):

    def get_y(P, qx):
        i = 0
        while i + 2 < len(P) and P[i + 1][0] <= qx:
            i += 1
        x0, y0 = P[i]
        x1, y1 = P[i + 1]
        return y0 + (y1 - y0) * (qx - x0) / float(x1 - x0)
    W, Ln, Un, G = map(int, infile.readline().split())
    L = []
    for each in range(Ln):
        L.append(map(int, infile.readline().split()))
    U = []
    for each in range(Un):
        U.append(map(int, infile.readline().split()))

    def get_area(left, right):
        xs = [x for x, y in L]
        xs += [x for x, y in U]
        xs += [left, right]
        xs = sorted(set(xs))
        xs = [x for x in xs if left - 1e-09 <= x <= right + 1e-09]
        res = 0
        for i in range(1, len(xs)):
            x0 = xs[i - 1]
            x1 = xs[i]
            side0 = abs(get_y(L, x0) - get_y(U, x0))
            side1 = abs(get_y(L, x1) - get_y(U, x1))
            h = abs(x0 - x1)
            res += (side0 + side1) * h / 2.0
        return res
    mid = (low + high) / 2
    if get_area(cuts[-1], mid) > slice_area:
        high = mid
    else:
        low = mid
    return each


def func_25195736d87e44aab52590b0582dd447(cuts, infile, slice_area):

    def get_y(P, qx):
        i = 0
        while i + 2 < len(P) and P[i + 1][0] <= qx:
            i += 1
        x0, y0 = P[i]
        x1, y1 = P[i + 1]
        return y0 + (y1 - y0) * (qx - x0) / float(x1 - x0)
    W, Ln, Un, G = map(int, infile.readline().split())
    L = []
    for each in range(Ln):
        L.append(map(int, infile.readline().split()))
    U = []
    for each in range(Un):
        U.append(map(int, infile.readline().split()))

    def get_area(left, right):
        xs = [x for x, y in L]
        xs += [x for x, y in U]
        xs += [left, right]
        xs = sorted(set(xs))
        xs = [x for x in xs if left - 1e-09 <= x <= right + 1e-09]
        res = 0
        for i in range(1, len(xs)):
            x0 = xs[i - 1]
            x1 = xs[i]
            side0 = abs(get_y(L, x0) - get_y(U, x0))
            side1 = abs(get_y(L, x1) - get_y(U, x1))
            h = abs(x0 - x1)
            res += (side0 + side1) * h / 2.0
        return res
    mid = (low + high) / 2
    if get_area(cuts[-1], mid) > slice_area:
        high = mid
    else:
        low = mid
    return U


def func_2d689267a79c4d35bebb198d2f39766a(cuts, infile, slice_area):

    def get_y(P, qx):
        i = 0
        while i + 2 < len(P) and P[i + 1][0] <= qx:
            i += 1
        x0, y0 = P[i]
        x1, y1 = P[i + 1]
        return y0 + (y1 - y0) * (qx - x0) / float(x1 - x0)
    W, Ln, Un, G = map(int, infile.readline().split())
    L = []
    for each in range(Ln):
        L.append(map(int, infile.readline().split()))
    U = []
    for each in range(Un):
        U.append(map(int, infile.readline().split()))

    def get_area(left, right):
        xs = [x for x, y in L]
        xs += [x for x, y in U]
        xs += [left, right]
        xs = sorted(set(xs))
        xs = [x for x in xs if left - 1e-09 <= x <= right + 1e-09]
        res = 0
        for i in range(1, len(xs)):
            x0 = xs[i - 1]
            x1 = xs[i]
            side0 = abs(get_y(L, x0) - get_y(U, x0))
            side1 = abs(get_y(L, x1) - get_y(U, x1))
            h = abs(x0 - x1)
            res += (side0 + side1) * h / 2.0
        return res
    mid = (low + high) / 2
    if get_area(cuts[-1], mid) > slice_area:
        high = mid
    else:
        low = mid
    return low


def func_ae15b851b12a4830a724ce6eb6d4a23f(cuts, infile, slice_area):

    def get_y(P, qx):
        i = 0
        while i + 2 < len(P) and P[i + 1][0] <= qx:
            i += 1
        x0, y0 = P[i]
        x1, y1 = P[i + 1]
        return y0 + (y1 - y0) * (qx - x0) / float(x1 - x0)
    W, Ln, Un, G = map(int, infile.readline().split())
    L = []
    for each in range(Ln):
        L.append(map(int, infile.readline().split()))
    U = []
    for each in range(Un):
        U.append(map(int, infile.readline().split()))

    def get_area(left, right):
        xs = [x for x, y in L]
        xs += [x for x, y in U]
        xs += [left, right]
        xs = sorted(set(xs))
        xs = [x for x in xs if left - 1e-09 <= x <= right + 1e-09]
        res = 0
        for i in range(1, len(xs)):
            x0 = xs[i - 1]
            x1 = xs[i]
            side0 = abs(get_y(L, x0) - get_y(U, x0))
            side1 = abs(get_y(L, x1) - get_y(U, x1))
            h = abs(x0 - x1)
            res += (side0 + side1) * h / 2.0
        return res
    mid = (low + high) / 2
    if get_area(cuts[-1], mid) > slice_area:
        high = mid
    else:
        low = mid
    return cuts


def func_d50fbdbcb1024e7f917675b00018491a(cuts, infile, slice_area):

    def get_y(P, qx):
        i = 0
        while i + 2 < len(P) and P[i + 1][0] <= qx:
            i += 1
        x0, y0 = P[i]
        x1, y1 = P[i + 1]
        return y0 + (y1 - y0) * (qx - x0) / float(x1 - x0)
    W, Ln, Un, G = map(int, infile.readline().split())
    L = []
    for each in range(Ln):
        L.append(map(int, infile.readline().split()))
    U = []
    for each in range(Un):
        U.append(map(int, infile.readline().split()))

    def get_area(left, right):
        xs = [x for x, y in L]
        xs += [x for x, y in U]
        xs += [left, right]
        xs = sorted(set(xs))
        xs = [x for x in xs if left - 1e-09 <= x <= right + 1e-09]
        res = 0
        for i in range(1, len(xs)):
            x0 = xs[i - 1]
            x1 = xs[i]
            side0 = abs(get_y(L, x0) - get_y(U, x0))
            side1 = abs(get_y(L, x1) - get_y(U, x1))
            h = abs(x0 - x1)
            res += (side0 + side1) * h / 2.0
        return res
    mid = (low + high) / 2
    if get_area(cuts[-1], mid) > slice_area:
        high = mid
    else:
        low = mid
    return Ln


def func_02b4ea44afec474bb3e60e28f276e42e(cuts, infile, slice_area):

    def get_y(P, qx):
        i = 0
        while i + 2 < len(P) and P[i + 1][0] <= qx:
            i += 1
        x0, y0 = P[i]
        x1, y1 = P[i + 1]
        return y0 + (y1 - y0) * (qx - x0) / float(x1 - x0)
    W, Ln, Un, G = map(int, infile.readline().split())
    L = []
    for each in range(Ln):
        L.append(map(int, infile.readline().split()))
    U = []
    for each in range(Un):
        U.append(map(int, infile.readline().split()))

    def get_area(left, right):
        xs = [x for x, y in L]
        xs += [x for x, y in U]
        xs += [left, right]
        xs = sorted(set(xs))
        xs = [x for x in xs if left - 1e-09 <= x <= right + 1e-09]
        res = 0
        for i in range(1, len(xs)):
            x0 = xs[i - 1]
            x1 = xs[i]
            side0 = abs(get_y(L, x0) - get_y(U, x0))
            side1 = abs(get_y(L, x1) - get_y(U, x1))
            h = abs(x0 - x1)
            res += (side0 + side1) * h / 2.0
        return res
    mid = (low + high) / 2
    if get_area(cuts[-1], mid) > slice_area:
        high = mid
    else:
        low = mid
    return mid


def func_37811d9204be4528a89d252f8cdc7e4e(cuts, infile, slice_area):

    def get_y(P, qx):
        i = 0
        while i + 2 < len(P) and P[i + 1][0] <= qx:
            i += 1
        x0, y0 = P[i]
        x1, y1 = P[i + 1]
        return y0 + (y1 - y0) * (qx - x0) / float(x1 - x0)
    W, Ln, Un, G = map(int, infile.readline().split())
    L = []
    for each in range(Ln):
        L.append(map(int, infile.readline().split()))
    U = []
    for each in range(Un):
        U.append(map(int, infile.readline().split()))

    def get_area(left, right):
        xs = [x for x, y in L]
        xs += [x for x, y in U]
        xs += [left, right]
        xs = sorted(set(xs))
        xs = [x for x in xs if left - 1e-09 <= x <= right + 1e-09]
        res = 0
        for i in range(1, len(xs)):
            x0 = xs[i - 1]
            x1 = xs[i]
            side0 = abs(get_y(L, x0) - get_y(U, x0))
            side1 = abs(get_y(L, x1) - get_y(U, x1))
            h = abs(x0 - x1)
            res += (side0 + side1) * h / 2.0
        return res
    mid = (low + high) / 2
    if get_area(cuts[-1], mid) > slice_area:
        high = mid
    else:
        low = mid
    return W


def func_026f1e854ada41aab9f5b0ece80db845(cuts, infile):

    def get_y(P, qx):
        i = 0
        while i + 2 < len(P) and P[i + 1][0] <= qx:
            i += 1
        x0, y0 = P[i]
        x1, y1 = P[i + 1]
        return y0 + (y1 - y0) * (qx - x0) / float(x1 - x0)
    W, Ln, Un, G = map(int, infile.readline().split())
    L = []
    for each in range(Ln):
        L.append(map(int, infile.readline().split()))
    U = []
    for each in range(Un):
        U.append(map(int, infile.readline().split()))

    def get_area(left, right):
        xs = [x for x, y in L]
        xs += [x for x, y in U]
        xs += [left, right]
        xs = sorted(set(xs))
        xs = [x for x in xs if left - 1e-09 <= x <= right + 1e-09]
        res = 0
        for i in range(1, len(xs)):
            x0 = xs[i - 1]
            x1 = xs[i]
            side0 = abs(get_y(L, x0) - get_y(U, x0))
            side1 = abs(get_y(L, x1) - get_y(U, x1))
            h = abs(x0 - x1)
            res += (side0 + side1) * h / 2.0
        return res
    low = cuts[-1]
    high = W
    return G


def func_719db1ffcab3411e8dc247c82bfaed2e(cuts, infile):

    def get_y(P, qx):
        i = 0
        while i + 2 < len(P) and P[i + 1][0] <= qx:
            i += 1
        x0, y0 = P[i]
        x1, y1 = P[i + 1]
        return y0 + (y1 - y0) * (qx - x0) / float(x1 - x0)
    W, Ln, Un, G = map(int, infile.readline().split())
    L = []
    for each in range(Ln):
        L.append(map(int, infile.readline().split()))
    U = []
    for each in range(Un):
        U.append(map(int, infile.readline().split()))

    def get_area(left, right):
        xs = [x for x, y in L]
        xs += [x for x, y in U]
        xs += [left, right]
        xs = sorted(set(xs))
        xs = [x for x in xs if left - 1e-09 <= x <= right + 1e-09]
        res = 0
        for i in range(1, len(xs)):
            x0 = xs[i - 1]
            x1 = xs[i]
            side0 = abs(get_y(L, x0) - get_y(U, x0))
            side1 = abs(get_y(L, x1) - get_y(U, x1))
            h = abs(x0 - x1)
            res += (side0 + side1) * h / 2.0
        return res
    low = cuts[-1]
    high = W
    return Un


def func_1927660d81fc452b94ff5bd98d5da7a4(cuts, infile):

    def get_y(P, qx):
        i = 0
        while i + 2 < len(P) and P[i + 1][0] <= qx:
            i += 1
        x0, y0 = P[i]
        x1, y1 = P[i + 1]
        return y0 + (y1 - y0) * (qx - x0) / float(x1 - x0)
    W, Ln, Un, G = map(int, infile.readline().split())
    L = []
    for each in range(Ln):
        L.append(map(int, infile.readline().split()))
    U = []
    for each in range(Un):
        U.append(map(int, infile.readline().split()))

    def get_area(left, right):
        xs = [x for x, y in L]
        xs += [x for x, y in U]
        xs += [left, right]
        xs = sorted(set(xs))
        xs = [x for x in xs if left - 1e-09 <= x <= right + 1e-09]
        res = 0
        for i in range(1, len(xs)):
            x0 = xs[i - 1]
            x1 = xs[i]
            side0 = abs(get_y(L, x0) - get_y(U, x0))
            side1 = abs(get_y(L, x1) - get_y(U, x1))
            h = abs(x0 - x1)
            res += (side0 + side1) * h / 2.0
        return res
    low = cuts[-1]
    high = W
    return cuts


def func_e5150f280896422a993f72172f3f1eec(cuts, infile):

    def get_y(P, qx):
        i = 0
        while i + 2 < len(P) and P[i + 1][0] <= qx:
            i += 1
        x0, y0 = P[i]
        x1, y1 = P[i + 1]
        return y0 + (y1 - y0) * (qx - x0) / float(x1 - x0)
    W, Ln, Un, G = map(int, infile.readline().split())
    L = []
    for each in range(Ln):
        L.append(map(int, infile.readline().split()))
    U = []
    for each in range(Un):
        U.append(map(int, infile.readline().split()))

    def get_area(left, right):
        xs = [x for x, y in L]
        xs += [x for x, y in U]
        xs += [left, right]
        xs = sorted(set(xs))
        xs = [x for x in xs if left - 1e-09 <= x <= right + 1e-09]
        res = 0
        for i in range(1, len(xs)):
            x0 = xs[i - 1]
            x1 = xs[i]
            side0 = abs(get_y(L, x0) - get_y(U, x0))
            side1 = abs(get_y(L, x1) - get_y(U, x1))
            h = abs(x0 - x1)
            res += (side0 + side1) * h / 2.0
        return res
    low = cuts[-1]
    high = W
    return each


def func_43b0652674754f9f958a069341f987f8(cuts, infile):

    def get_y(P, qx):
        i = 0
        while i + 2 < len(P) and P[i + 1][0] <= qx:
            i += 1
        x0, y0 = P[i]
        x1, y1 = P[i + 1]
        return y0 + (y1 - y0) * (qx - x0) / float(x1 - x0)
    W, Ln, Un, G = map(int, infile.readline().split())
    L = []
    for each in range(Ln):
        L.append(map(int, infile.readline().split()))
    U = []
    for each in range(Un):
        U.append(map(int, infile.readline().split()))

    def get_area(left, right):
        xs = [x for x, y in L]
        xs += [x for x, y in U]
        xs += [left, right]
        xs = sorted(set(xs))
        xs = [x for x in xs if left - 1e-09 <= x <= right + 1e-09]
        res = 0
        for i in range(1, len(xs)):
            x0 = xs[i - 1]
            x1 = xs[i]
            side0 = abs(get_y(L, x0) - get_y(U, x0))
            side1 = abs(get_y(L, x1) - get_y(U, x1))
            h = abs(x0 - x1)
            res += (side0 + side1) * h / 2.0
        return res
    low = cuts[-1]
    high = W
    return low


def func_05f545f7f2224802ba0a2554e37e527b(cuts, infile):

    def get_y(P, qx):
        i = 0
        while i + 2 < len(P) and P[i + 1][0] <= qx:
            i += 1
        x0, y0 = P[i]
        x1, y1 = P[i + 1]
        return y0 + (y1 - y0) * (qx - x0) / float(x1 - x0)
    W, Ln, Un, G = map(int, infile.readline().split())
    L = []
    for each in range(Ln):
        L.append(map(int, infile.readline().split()))
    U = []
    for each in range(Un):
        U.append(map(int, infile.readline().split()))

    def get_area(left, right):
        xs = [x for x, y in L]
        xs += [x for x, y in U]
        xs += [left, right]
        xs = sorted(set(xs))
        xs = [x for x in xs if left - 1e-09 <= x <= right + 1e-09]
        res = 0
        for i in range(1, len(xs)):
            x0 = xs[i - 1]
            x1 = xs[i]
            side0 = abs(get_y(L, x0) - get_y(U, x0))
            side1 = abs(get_y(L, x1) - get_y(U, x1))
            h = abs(x0 - x1)
            res += (side0 + side1) * h / 2.0
        return res
    low = cuts[-1]
    high = W
    return U


def func_0643c4a16b374b3cbd9938a240ba4e5f(cuts, infile):

    def get_y(P, qx):
        i = 0
        while i + 2 < len(P) and P[i + 1][0] <= qx:
            i += 1
        x0, y0 = P[i]
        x1, y1 = P[i + 1]
        return y0 + (y1 - y0) * (qx - x0) / float(x1 - x0)
    W, Ln, Un, G = map(int, infile.readline().split())
    L = []
    for each in range(Ln):
        L.append(map(int, infile.readline().split()))
    U = []
    for each in range(Un):
        U.append(map(int, infile.readline().split()))

    def get_area(left, right):
        xs = [x for x, y in L]
        xs += [x for x, y in U]
        xs += [left, right]
        xs = sorted(set(xs))
        xs = [x for x in xs if left - 1e-09 <= x <= right + 1e-09]
        res = 0
        for i in range(1, len(xs)):
            x0 = xs[i - 1]
            x1 = xs[i]
            side0 = abs(get_y(L, x0) - get_y(U, x0))
            side1 = abs(get_y(L, x1) - get_y(U, x1))
            h = abs(x0 - x1)
            res += (side0 + side1) * h / 2.0
        return res
    low = cuts[-1]
    high = W
    return high


def func_5a1f2439f01d446bb5e191c530075432(cuts, infile):

    def get_y(P, qx):
        i = 0
        while i + 2 < len(P) and P[i + 1][0] <= qx:
            i += 1
        x0, y0 = P[i]
        x1, y1 = P[i + 1]
        return y0 + (y1 - y0) * (qx - x0) / float(x1 - x0)
    W, Ln, Un, G = map(int, infile.readline().split())
    L = []
    for each in range(Ln):
        L.append(map(int, infile.readline().split()))
    U = []
    for each in range(Un):
        U.append(map(int, infile.readline().split()))

    def get_area(left, right):
        xs = [x for x, y in L]
        xs += [x for x, y in U]
        xs += [left, right]
        xs = sorted(set(xs))
        xs = [x for x in xs if left - 1e-09 <= x <= right + 1e-09]
        res = 0
        for i in range(1, len(xs)):
            x0 = xs[i - 1]
            x1 = xs[i]
            side0 = abs(get_y(L, x0) - get_y(U, x0))
            side1 = abs(get_y(L, x1) - get_y(U, x1))
            h = abs(x0 - x1)
            res += (side0 + side1) * h / 2.0
        return res
    low = cuts[-1]
    high = W
    return L


def func_6debef636c4642c583ed7f9bca642f89(cuts, infile):

    def get_y(P, qx):
        i = 0
        while i + 2 < len(P) and P[i + 1][0] <= qx:
            i += 1
        x0, y0 = P[i]
        x1, y1 = P[i + 1]
        return y0 + (y1 - y0) * (qx - x0) / float(x1 - x0)
    W, Ln, Un, G = map(int, infile.readline().split())
    L = []
    for each in range(Ln):
        L.append(map(int, infile.readline().split()))
    U = []
    for each in range(Un):
        U.append(map(int, infile.readline().split()))

    def get_area(left, right):
        xs = [x for x, y in L]
        xs += [x for x, y in U]
        xs += [left, right]
        xs = sorted(set(xs))
        xs = [x for x in xs if left - 1e-09 <= x <= right + 1e-09]
        res = 0
        for i in range(1, len(xs)):
            x0 = xs[i - 1]
            x1 = xs[i]
            side0 = abs(get_y(L, x0) - get_y(U, x0))
            side1 = abs(get_y(L, x1) - get_y(U, x1))
            h = abs(x0 - x1)
            res += (side0 + side1) * h / 2.0
        return res
    low = cuts[-1]
    high = W
    return W


def func_33a47ef11059405981be49f5cf23c9e2(cuts, infile):

    def get_y(P, qx):
        i = 0
        while i + 2 < len(P) and P[i + 1][0] <= qx:
            i += 1
        x0, y0 = P[i]
        x1, y1 = P[i + 1]
        return y0 + (y1 - y0) * (qx - x0) / float(x1 - x0)
    W, Ln, Un, G = map(int, infile.readline().split())
    L = []
    for each in range(Ln):
        L.append(map(int, infile.readline().split()))
    U = []
    for each in range(Un):
        U.append(map(int, infile.readline().split()))

    def get_area(left, right):
        xs = [x for x, y in L]
        xs += [x for x, y in U]
        xs += [left, right]
        xs = sorted(set(xs))
        xs = [x for x in xs if left - 1e-09 <= x <= right + 1e-09]
        res = 0
        for i in range(1, len(xs)):
            x0 = xs[i - 1]
            x1 = xs[i]
            side0 = abs(get_y(L, x0) - get_y(U, x0))
            side1 = abs(get_y(L, x1) - get_y(U, x1))
            h = abs(x0 - x1)
            res += (side0 + side1) * h / 2.0
        return res
    low = cuts[-1]
    high = W
    return Ln


def func_454fd5bf0f7845fb926570c27e3d5f60(cuts, infile, slice_area):

    def get_y(P, qx):
        i = 0
        while i + 2 < len(P) and P[i + 1][0] <= qx:
            i += 1
        x0, y0 = P[i]
        x1, y1 = P[i + 1]
        return y0 + (y1 - y0) * (qx - x0) / float(x1 - x0)
    W, Ln, Un, G = map(int, infile.readline().split())
    L = []
    for each in range(Ln):
        L.append(map(int, infile.readline().split()))
    U = []
    for each in range(Un):
        U.append(map(int, infile.readline().split()))

    def get_area(left, right):
        xs = [x for x, y in L]
        xs += [x for x, y in U]
        xs += [left, right]
        xs = sorted(set(xs))
        xs = [x for x in xs if left - 1e-09 <= x <= right + 1e-09]
        res = 0
        for i in range(1, len(xs)):
            x0 = xs[i - 1]
            x1 = xs[i]
            side0 = abs(get_y(L, x0) - get_y(U, x0))
            side1 = abs(get_y(L, x1) - get_y(U, x1))
            h = abs(x0 - x1)
            res += (side0 + side1) * h / 2.0
        return res
    high = W
    for i in range(50):
        mid = (low + high) / 2
        if get_area(cuts[-1], mid) > slice_area:
            high = mid
        else:
            low = mid
    return slice_area


def func_623dacb33877415dbe3da7dc2d5b3c1e(cuts, infile, slice_area):

    def get_y(P, qx):
        i = 0
        while i + 2 < len(P) and P[i + 1][0] <= qx:
            i += 1
        x0, y0 = P[i]
        x1, y1 = P[i + 1]
        return y0 + (y1 - y0) * (qx - x0) / float(x1 - x0)
    W, Ln, Un, G = map(int, infile.readline().split())
    L = []
    for each in range(Ln):
        L.append(map(int, infile.readline().split()))
    U = []
    for each in range(Un):
        U.append(map(int, infile.readline().split()))

    def get_area(left, right):
        xs = [x for x, y in L]
        xs += [x for x, y in U]
        xs += [left, right]
        xs = sorted(set(xs))
        xs = [x for x in xs if left - 1e-09 <= x <= right + 1e-09]
        res = 0
        for i in range(1, len(xs)):
            x0 = xs[i - 1]
            x1 = xs[i]
            side0 = abs(get_y(L, x0) - get_y(U, x0))
            side1 = abs(get_y(L, x1) - get_y(U, x1))
            h = abs(x0 - x1)
            res += (side0 + side1) * h / 2.0
        return res
    high = W
    for i in range(50):
        mid = (low + high) / 2
        if get_area(cuts[-1], mid) > slice_area:
            high = mid
        else:
            low = mid
    return high


def func_8ab7b30e8a42462aa5bdfead92ec2edd(cuts, infile, slice_area):

    def get_y(P, qx):
        i = 0
        while i + 2 < len(P) and P[i + 1][0] <= qx:
            i += 1
        x0, y0 = P[i]
        x1, y1 = P[i + 1]
        return y0 + (y1 - y0) * (qx - x0) / float(x1 - x0)
    W, Ln, Un, G = map(int, infile.readline().split())
    L = []
    for each in range(Ln):
        L.append(map(int, infile.readline().split()))
    U = []
    for each in range(Un):
        U.append(map(int, infile.readline().split()))

    def get_area(left, right):
        xs = [x for x, y in L]
        xs += [x for x, y in U]
        xs += [left, right]
        xs = sorted(set(xs))
        xs = [x for x in xs if left - 1e-09 <= x <= right + 1e-09]
        res = 0
        for i in range(1, len(xs)):
            x0 = xs[i - 1]
            x1 = xs[i]
            side0 = abs(get_y(L, x0) - get_y(U, x0))
            side1 = abs(get_y(L, x1) - get_y(U, x1))
            h = abs(x0 - x1)
            res += (side0 + side1) * h / 2.0
        return res
    high = W
    for i in range(50):
        mid = (low + high) / 2
        if get_area(cuts[-1], mid) > slice_area:
            high = mid
        else:
            low = mid
    return Un


def func_e7de02f6919f4e4a85f27e89c9042004(cuts, infile, slice_area):

    def get_y(P, qx):
        i = 0
        while i + 2 < len(P) and P[i + 1][0] <= qx:
            i += 1
        x0, y0 = P[i]
        x1, y1 = P[i + 1]
        return y0 + (y1 - y0) * (qx - x0) / float(x1 - x0)
    W, Ln, Un, G = map(int, infile.readline().split())
    L = []
    for each in range(Ln):
        L.append(map(int, infile.readline().split()))
    U = []
    for each in range(Un):
        U.append(map(int, infile.readline().split()))

    def get_area(left, right):
        xs = [x for x, y in L]
        xs += [x for x, y in U]
        xs += [left, right]
        xs = sorted(set(xs))
        xs = [x for x in xs if left - 1e-09 <= x <= right + 1e-09]
        res = 0
        for i in range(1, len(xs)):
            x0 = xs[i - 1]
            x1 = xs[i]
            side0 = abs(get_y(L, x0) - get_y(U, x0))
            side1 = abs(get_y(L, x1) - get_y(U, x1))
            h = abs(x0 - x1)
            res += (side0 + side1) * h / 2.0
        return res
    high = W
    for i in range(50):
        mid = (low + high) / 2
        if get_area(cuts[-1], mid) > slice_area:
            high = mid
        else:
            low = mid
    return W


def func_03ffb865eec142248421dbf6d2f5ba6b(cuts, infile, slice_area):

    def get_y(P, qx):
        i = 0
        while i + 2 < len(P) and P[i + 1][0] <= qx:
            i += 1
        x0, y0 = P[i]
        x1, y1 = P[i + 1]
        return y0 + (y1 - y0) * (qx - x0) / float(x1 - x0)
    W, Ln, Un, G = map(int, infile.readline().split())
    L = []
    for each in range(Ln):
        L.append(map(int, infile.readline().split()))
    U = []
    for each in range(Un):
        U.append(map(int, infile.readline().split()))

    def get_area(left, right):
        xs = [x for x, y in L]
        xs += [x for x, y in U]
        xs += [left, right]
        xs = sorted(set(xs))
        xs = [x for x in xs if left - 1e-09 <= x <= right + 1e-09]
        res = 0
        for i in range(1, len(xs)):
            x0 = xs[i - 1]
            x1 = xs[i]
            side0 = abs(get_y(L, x0) - get_y(U, x0))
            side1 = abs(get_y(L, x1) - get_y(U, x1))
            h = abs(x0 - x1)
            res += (side0 + side1) * h / 2.0
        return res
    high = W
    for i in range(50):
        mid = (low + high) / 2
        if get_area(cuts[-1], mid) > slice_area:
            high = mid
        else:
            low = mid
    return low


def func_da1b34083e7e4782b7d06c0d36373abb(cuts, infile, slice_area):

    def get_y(P, qx):
        i = 0
        while i + 2 < len(P) and P[i + 1][0] <= qx:
            i += 1
        x0, y0 = P[i]
        x1, y1 = P[i + 1]
        return y0 + (y1 - y0) * (qx - x0) / float(x1 - x0)
    W, Ln, Un, G = map(int, infile.readline().split())
    L = []
    for each in range(Ln):
        L.append(map(int, infile.readline().split()))
    U = []
    for each in range(Un):
        U.append(map(int, infile.readline().split()))

    def get_area(left, right):
        xs = [x for x, y in L]
        xs += [x for x, y in U]
        xs += [left, right]
        xs = sorted(set(xs))
        xs = [x for x in xs if left - 1e-09 <= x <= right + 1e-09]
        res = 0
        for i in range(1, len(xs)):
            x0 = xs[i - 1]
            x1 = xs[i]
            side0 = abs(get_y(L, x0) - get_y(U, x0))
            side1 = abs(get_y(L, x1) - get_y(U, x1))
            h = abs(x0 - x1)
            res += (side0 + side1) * h / 2.0
        return res
    high = W
    for i in range(50):
        mid = (low + high) / 2
        if get_area(cuts[-1], mid) > slice_area:
            high = mid
        else:
            low = mid
    return L


def func_b5e7603c2ecf4663895c6703fc2043af(cuts, infile, slice_area):

    def get_y(P, qx):
        i = 0
        while i + 2 < len(P) and P[i + 1][0] <= qx:
            i += 1
        x0, y0 = P[i]
        x1, y1 = P[i + 1]
        return y0 + (y1 - y0) * (qx - x0) / float(x1 - x0)
    W, Ln, Un, G = map(int, infile.readline().split())
    L = []
    for each in range(Ln):
        L.append(map(int, infile.readline().split()))
    U = []
    for each in range(Un):
        U.append(map(int, infile.readline().split()))

    def get_area(left, right):
        xs = [x for x, y in L]
        xs += [x for x, y in U]
        xs += [left, right]
        xs = sorted(set(xs))
        xs = [x for x in xs if left - 1e-09 <= x <= right + 1e-09]
        res = 0
        for i in range(1, len(xs)):
            x0 = xs[i - 1]
            x1 = xs[i]
            side0 = abs(get_y(L, x0) - get_y(U, x0))
            side1 = abs(get_y(L, x1) - get_y(U, x1))
            h = abs(x0 - x1)
            res += (side0 + side1) * h / 2.0
        return res
    high = W
    for i in range(50):
        mid = (low + high) / 2
        if get_area(cuts[-1], mid) > slice_area:
            high = mid
        else:
            low = mid
    return G


def func_0fcd512b01e14fd6a975f056eae20d01(cuts, infile, slice_area):

    def get_y(P, qx):
        i = 0
        while i + 2 < len(P) and P[i + 1][0] <= qx:
            i += 1
        x0, y0 = P[i]
        x1, y1 = P[i + 1]
        return y0 + (y1 - y0) * (qx - x0) / float(x1 - x0)
    W, Ln, Un, G = map(int, infile.readline().split())
    L = []
    for each in range(Ln):
        L.append(map(int, infile.readline().split()))
    U = []
    for each in range(Un):
        U.append(map(int, infile.readline().split()))

    def get_area(left, right):
        xs = [x for x, y in L]
        xs += [x for x, y in U]
        xs += [left, right]
        xs = sorted(set(xs))
        xs = [x for x in xs if left - 1e-09 <= x <= right + 1e-09]
        res = 0
        for i in range(1, len(xs)):
            x0 = xs[i - 1]
            x1 = xs[i]
            side0 = abs(get_y(L, x0) - get_y(U, x0))
            side1 = abs(get_y(L, x1) - get_y(U, x1))
            h = abs(x0 - x1)
            res += (side0 + side1) * h / 2.0
        return res
    high = W
    for i in range(50):
        mid = (low + high) / 2
        if get_area(cuts[-1], mid) > slice_area:
            high = mid
        else:
            low = mid
    return U


def func_b9dae646913e4546957af5520dcff186(cuts, infile, slice_area):

    def get_y(P, qx):
        i = 0
        while i + 2 < len(P) and P[i + 1][0] <= qx:
            i += 1
        x0, y0 = P[i]
        x1, y1 = P[i + 1]
        return y0 + (y1 - y0) * (qx - x0) / float(x1 - x0)
    W, Ln, Un, G = map(int, infile.readline().split())
    L = []
    for each in range(Ln):
        L.append(map(int, infile.readline().split()))
    U = []
    for each in range(Un):
        U.append(map(int, infile.readline().split()))

    def get_area(left, right):
        xs = [x for x, y in L]
        xs += [x for x, y in U]
        xs += [left, right]
        xs = sorted(set(xs))
        xs = [x for x in xs if left - 1e-09 <= x <= right + 1e-09]
        res = 0
        for i in range(1, len(xs)):
            x0 = xs[i - 1]
            x1 = xs[i]
            side0 = abs(get_y(L, x0) - get_y(U, x0))
            side1 = abs(get_y(L, x1) - get_y(U, x1))
            h = abs(x0 - x1)
            res += (side0 + side1) * h / 2.0
        return res
    high = W
    for i in range(50):
        mid = (low + high) / 2
        if get_area(cuts[-1], mid) > slice_area:
            high = mid
        else:
            low = mid
    return cuts


def func_f4c5d25300eb430ab9382f5c9c8fe637(cuts, infile, slice_area):

    def get_y(P, qx):
        i = 0
        while i + 2 < len(P) and P[i + 1][0] <= qx:
            i += 1
        x0, y0 = P[i]
        x1, y1 = P[i + 1]
        return y0 + (y1 - y0) * (qx - x0) / float(x1 - x0)
    W, Ln, Un, G = map(int, infile.readline().split())
    L = []
    for each in range(Ln):
        L.append(map(int, infile.readline().split()))
    U = []
    for each in range(Un):
        U.append(map(int, infile.readline().split()))

    def get_area(left, right):
        xs = [x for x, y in L]
        xs += [x for x, y in U]
        xs += [left, right]
        xs = sorted(set(xs))
        xs = [x for x in xs if left - 1e-09 <= x <= right + 1e-09]
        res = 0
        for i in range(1, len(xs)):
            x0 = xs[i - 1]
            x1 = xs[i]
            side0 = abs(get_y(L, x0) - get_y(U, x0))
            side1 = abs(get_y(L, x1) - get_y(U, x1))
            h = abs(x0 - x1)
            res += (side0 + side1) * h / 2.0
        return res
    high = W
    for i in range(50):
        mid = (low + high) / 2
        if get_area(cuts[-1], mid) > slice_area:
            high = mid
        else:
            low = mid
    return each


def func_6805ee38cede47b5b95ce79947f142bb(cuts, infile, slice_area):

    def get_y(P, qx):
        i = 0
        while i + 2 < len(P) and P[i + 1][0] <= qx:
            i += 1
        x0, y0 = P[i]
        x1, y1 = P[i + 1]
        return y0 + (y1 - y0) * (qx - x0) / float(x1 - x0)
    W, Ln, Un, G = map(int, infile.readline().split())
    L = []
    for each in range(Ln):
        L.append(map(int, infile.readline().split()))
    U = []
    for each in range(Un):
        U.append(map(int, infile.readline().split()))

    def get_area(left, right):
        xs = [x for x, y in L]
        xs += [x for x, y in U]
        xs += [left, right]
        xs = sorted(set(xs))
        xs = [x for x in xs if left - 1e-09 <= x <= right + 1e-09]
        res = 0
        for i in range(1, len(xs)):
            x0 = xs[i - 1]
            x1 = xs[i]
            side0 = abs(get_y(L, x0) - get_y(U, x0))
            side1 = abs(get_y(L, x1) - get_y(U, x1))
            h = abs(x0 - x1)
            res += (side0 + side1) * h / 2.0
        return res
    high = W
    for i in range(50):
        mid = (low + high) / 2
        if get_area(cuts[-1], mid) > slice_area:
            high = mid
        else:
            low = mid
    return mid


def func_78bdaa8744ae4ab2a2adedd508b23ccc(cuts, infile, slice_area):

    def get_y(P, qx):
        i = 0
        while i + 2 < len(P) and P[i + 1][0] <= qx:
            i += 1
        x0, y0 = P[i]
        x1, y1 = P[i + 1]
        return y0 + (y1 - y0) * (qx - x0) / float(x1 - x0)
    W, Ln, Un, G = map(int, infile.readline().split())
    L = []
    for each in range(Ln):
        L.append(map(int, infile.readline().split()))
    U = []
    for each in range(Un):
        U.append(map(int, infile.readline().split()))

    def get_area(left, right):
        xs = [x for x, y in L]
        xs += [x for x, y in U]
        xs += [left, right]
        xs = sorted(set(xs))
        xs = [x for x in xs if left - 1e-09 <= x <= right + 1e-09]
        res = 0
        for i in range(1, len(xs)):
            x0 = xs[i - 1]
            x1 = xs[i]
            side0 = abs(get_y(L, x0) - get_y(U, x0))
            side1 = abs(get_y(L, x1) - get_y(U, x1))
            h = abs(x0 - x1)
            res += (side0 + side1) * h / 2.0
        return res
    high = W
    for i in range(50):
        mid = (low + high) / 2
        if get_area(cuts[-1], mid) > slice_area:
            high = mid
        else:
            low = mid
    return Ln


def func_13faa6487db64f4ba4bfa5d35ecc5aff(cuts, infile, slice_area):

    def get_y(P, qx):
        i = 0
        while i + 2 < len(P) and P[i + 1][0] <= qx:
            i += 1
        x0, y0 = P[i]
        x1, y1 = P[i + 1]
        return y0 + (y1 - y0) * (qx - x0) / float(x1 - x0)
    W, Ln, Un, G = map(int, infile.readline().split())
    L = []
    for each in range(Ln):
        L.append(map(int, infile.readline().split()))
    U = []
    for each in range(Un):
        U.append(map(int, infile.readline().split()))

    def get_area(left, right):
        xs = [x for x, y in L]
        xs += [x for x, y in U]
        xs += [left, right]
        xs = sorted(set(xs))
        xs = [x for x in xs if left - 1e-09 <= x <= right + 1e-09]
        res = 0
        for i in range(1, len(xs)):
            x0 = xs[i - 1]
            x1 = xs[i]
            side0 = abs(get_y(L, x0) - get_y(U, x0))
            side1 = abs(get_y(L, x1) - get_y(U, x1))
            h = abs(x0 - x1)
            res += (side0 + side1) * h / 2.0
        return res
    high = W
    for i in range(50):
        mid = (low + high) / 2
        if get_area(cuts[-1], mid) > slice_area:
            high = mid
        else:
            low = mid
    return i


def func_975d852ad53f4999b0be4f851ddeb84a(cuts, infile, slice_area):

    def get_y(P, qx):
        i = 0
        while i + 2 < len(P) and P[i + 1][0] <= qx:
            i += 1
        x0, y0 = P[i]
        x1, y1 = P[i + 1]
        return y0 + (y1 - y0) * (qx - x0) / float(x1 - x0)
    W, Ln, Un, G = map(int, infile.readline().split())
    L = []
    for each in range(Ln):
        L.append(map(int, infile.readline().split()))
    U = []
    for each in range(Un):
        U.append(map(int, infile.readline().split()))

    def get_area(left, right):
        xs = [x for x, y in L]
        xs += [x for x, y in U]
        xs += [left, right]
        xs = sorted(set(xs))
        xs = [x for x in xs if left - 1e-09 <= x <= right + 1e-09]
        res = 0
        for i in range(1, len(xs)):
            x0 = xs[i - 1]
            x1 = xs[i]
            side0 = abs(get_y(L, x0) - get_y(U, x0))
            side1 = abs(get_y(L, x1) - get_y(U, x1))
            h = abs(x0 - x1)
            res += (side0 + side1) * h / 2.0
        return res
    for i in range(50):
        mid = (low + high) / 2
        if get_area(cuts[-1], mid) > slice_area:
            high = mid
        else:
            low = mid
    cuts.append(mid)
    return U


def func_c7e699b80c314f2fbc339180c24d5cc5(cuts, infile, slice_area):

    def get_y(P, qx):
        i = 0
        while i + 2 < len(P) and P[i + 1][0] <= qx:
            i += 1
        x0, y0 = P[i]
        x1, y1 = P[i + 1]
        return y0 + (y1 - y0) * (qx - x0) / float(x1 - x0)
    W, Ln, Un, G = map(int, infile.readline().split())
    L = []
    for each in range(Ln):
        L.append(map(int, infile.readline().split()))
    U = []
    for each in range(Un):
        U.append(map(int, infile.readline().split()))

    def get_area(left, right):
        xs = [x for x, y in L]
        xs += [x for x, y in U]
        xs += [left, right]
        xs = sorted(set(xs))
        xs = [x for x in xs if left - 1e-09 <= x <= right + 1e-09]
        res = 0
        for i in range(1, len(xs)):
            x0 = xs[i - 1]
            x1 = xs[i]
            side0 = abs(get_y(L, x0) - get_y(U, x0))
            side1 = abs(get_y(L, x1) - get_y(U, x1))
            h = abs(x0 - x1)
            res += (side0 + side1) * h / 2.0
        return res
    for i in range(50):
        mid = (low + high) / 2
        if get_area(cuts[-1], mid) > slice_area:
            high = mid
        else:
            low = mid
    cuts.append(mid)
    return low


def func_af3bef27e05c438badc0ae6d987a1c94(cuts, infile, slice_area):

    def get_y(P, qx):
        i = 0
        while i + 2 < len(P) and P[i + 1][0] <= qx:
            i += 1
        x0, y0 = P[i]
        x1, y1 = P[i + 1]
        return y0 + (y1 - y0) * (qx - x0) / float(x1 - x0)
    W, Ln, Un, G = map(int, infile.readline().split())
    L = []
    for each in range(Ln):
        L.append(map(int, infile.readline().split()))
    U = []
    for each in range(Un):
        U.append(map(int, infile.readline().split()))

    def get_area(left, right):
        xs = [x for x, y in L]
        xs += [x for x, y in U]
        xs += [left, right]
        xs = sorted(set(xs))
        xs = [x for x in xs if left - 1e-09 <= x <= right + 1e-09]
        res = 0
        for i in range(1, len(xs)):
            x0 = xs[i - 1]
            x1 = xs[i]
            side0 = abs(get_y(L, x0) - get_y(U, x0))
            side1 = abs(get_y(L, x1) - get_y(U, x1))
            h = abs(x0 - x1)
            res += (side0 + side1) * h / 2.0
        return res
    for i in range(50):
        mid = (low + high) / 2
        if get_area(cuts[-1], mid) > slice_area:
            high = mid
        else:
            low = mid
    cuts.append(mid)
    return high


def func_55ed36393f2c434280d8d9919c19920d(cuts, infile, slice_area):

    def get_y(P, qx):
        i = 0
        while i + 2 < len(P) and P[i + 1][0] <= qx:
            i += 1
        x0, y0 = P[i]
        x1, y1 = P[i + 1]
        return y0 + (y1 - y0) * (qx - x0) / float(x1 - x0)
    W, Ln, Un, G = map(int, infile.readline().split())
    L = []
    for each in range(Ln):
        L.append(map(int, infile.readline().split()))
    U = []
    for each in range(Un):
        U.append(map(int, infile.readline().split()))

    def get_area(left, right):
        xs = [x for x, y in L]
        xs += [x for x, y in U]
        xs += [left, right]
        xs = sorted(set(xs))
        xs = [x for x in xs if left - 1e-09 <= x <= right + 1e-09]
        res = 0
        for i in range(1, len(xs)):
            x0 = xs[i - 1]
            x1 = xs[i]
            side0 = abs(get_y(L, x0) - get_y(U, x0))
            side1 = abs(get_y(L, x1) - get_y(U, x1))
            h = abs(x0 - x1)
            res += (side0 + side1) * h / 2.0
        return res
    for i in range(50):
        mid = (low + high) / 2
        if get_area(cuts[-1], mid) > slice_area:
            high = mid
        else:
            low = mid
    cuts.append(mid)
    return G


def func_966f6ff5a99445d9baf1ca463da55dd4(cuts, infile, slice_area):

    def get_y(P, qx):
        i = 0
        while i + 2 < len(P) and P[i + 1][0] <= qx:
            i += 1
        x0, y0 = P[i]
        x1, y1 = P[i + 1]
        return y0 + (y1 - y0) * (qx - x0) / float(x1 - x0)
    W, Ln, Un, G = map(int, infile.readline().split())
    L = []
    for each in range(Ln):
        L.append(map(int, infile.readline().split()))
    U = []
    for each in range(Un):
        U.append(map(int, infile.readline().split()))

    def get_area(left, right):
        xs = [x for x, y in L]
        xs += [x for x, y in U]
        xs += [left, right]
        xs = sorted(set(xs))
        xs = [x for x in xs if left - 1e-09 <= x <= right + 1e-09]
        res = 0
        for i in range(1, len(xs)):
            x0 = xs[i - 1]
            x1 = xs[i]
            side0 = abs(get_y(L, x0) - get_y(U, x0))
            side1 = abs(get_y(L, x1) - get_y(U, x1))
            h = abs(x0 - x1)
            res += (side0 + side1) * h / 2.0
        return res
    for i in range(50):
        mid = (low + high) / 2
        if get_area(cuts[-1], mid) > slice_area:
            high = mid
        else:
            low = mid
    cuts.append(mid)
    return i


def func_e32681694ce946e89c21956e946bbed6(cuts, infile, slice_area):

    def get_y(P, qx):
        i = 0
        while i + 2 < len(P) and P[i + 1][0] <= qx:
            i += 1
        x0, y0 = P[i]
        x1, y1 = P[i + 1]
        return y0 + (y1 - y0) * (qx - x0) / float(x1 - x0)
    W, Ln, Un, G = map(int, infile.readline().split())
    L = []
    for each in range(Ln):
        L.append(map(int, infile.readline().split()))
    U = []
    for each in range(Un):
        U.append(map(int, infile.readline().split()))

    def get_area(left, right):
        xs = [x for x, y in L]
        xs += [x for x, y in U]
        xs += [left, right]
        xs = sorted(set(xs))
        xs = [x for x in xs if left - 1e-09 <= x <= right + 1e-09]
        res = 0
        for i in range(1, len(xs)):
            x0 = xs[i - 1]
            x1 = xs[i]
            side0 = abs(get_y(L, x0) - get_y(U, x0))
            side1 = abs(get_y(L, x1) - get_y(U, x1))
            h = abs(x0 - x1)
            res += (side0 + side1) * h / 2.0
        return res
    for i in range(50):
        mid = (low + high) / 2
        if get_area(cuts[-1], mid) > slice_area:
            high = mid
        else:
            low = mid
    cuts.append(mid)
    return cuts


def func_6925631e1f9448ed9417235df375e7b9(cuts, infile, slice_area):

    def get_y(P, qx):
        i = 0
        while i + 2 < len(P) and P[i + 1][0] <= qx:
            i += 1
        x0, y0 = P[i]
        x1, y1 = P[i + 1]
        return y0 + (y1 - y0) * (qx - x0) / float(x1 - x0)
    W, Ln, Un, G = map(int, infile.readline().split())
    L = []
    for each in range(Ln):
        L.append(map(int, infile.readline().split()))
    U = []
    for each in range(Un):
        U.append(map(int, infile.readline().split()))

    def get_area(left, right):
        xs = [x for x, y in L]
        xs += [x for x, y in U]
        xs += [left, right]
        xs = sorted(set(xs))
        xs = [x for x in xs if left - 1e-09 <= x <= right + 1e-09]
        res = 0
        for i in range(1, len(xs)):
            x0 = xs[i - 1]
            x1 = xs[i]
            side0 = abs(get_y(L, x0) - get_y(U, x0))
            side1 = abs(get_y(L, x1) - get_y(U, x1))
            h = abs(x0 - x1)
            res += (side0 + side1) * h / 2.0
        return res
    for i in range(50):
        mid = (low + high) / 2
        if get_area(cuts[-1], mid) > slice_area:
            high = mid
        else:
            low = mid
    cuts.append(mid)
    return each


def func_6d6a5082e20b417d83eace96b965da97(cuts, infile, slice_area):

    def get_y(P, qx):
        i = 0
        while i + 2 < len(P) and P[i + 1][0] <= qx:
            i += 1
        x0, y0 = P[i]
        x1, y1 = P[i + 1]
        return y0 + (y1 - y0) * (qx - x0) / float(x1 - x0)
    W, Ln, Un, G = map(int, infile.readline().split())
    L = []
    for each in range(Ln):
        L.append(map(int, infile.readline().split()))
    U = []
    for each in range(Un):
        U.append(map(int, infile.readline().split()))

    def get_area(left, right):
        xs = [x for x, y in L]
        xs += [x for x, y in U]
        xs += [left, right]
        xs = sorted(set(xs))
        xs = [x for x in xs if left - 1e-09 <= x <= right + 1e-09]
        res = 0
        for i in range(1, len(xs)):
            x0 = xs[i - 1]
            x1 = xs[i]
            side0 = abs(get_y(L, x0) - get_y(U, x0))
            side1 = abs(get_y(L, x1) - get_y(U, x1))
            h = abs(x0 - x1)
            res += (side0 + side1) * h / 2.0
        return res
    for i in range(50):
        mid = (low + high) / 2
        if get_area(cuts[-1], mid) > slice_area:
            high = mid
        else:
            low = mid
    cuts.append(mid)
    return slice_area


def func_e9ef9c6657634bd5afa4c599db670177(cuts, infile, slice_area):

    def get_y(P, qx):
        i = 0
        while i + 2 < len(P) and P[i + 1][0] <= qx:
            i += 1
        x0, y0 = P[i]
        x1, y1 = P[i + 1]
        return y0 + (y1 - y0) * (qx - x0) / float(x1 - x0)
    W, Ln, Un, G = map(int, infile.readline().split())
    L = []
    for each in range(Ln):
        L.append(map(int, infile.readline().split()))
    U = []
    for each in range(Un):
        U.append(map(int, infile.readline().split()))

    def get_area(left, right):
        xs = [x for x, y in L]
        xs += [x for x, y in U]
        xs += [left, right]
        xs = sorted(set(xs))
        xs = [x for x in xs if left - 1e-09 <= x <= right + 1e-09]
        res = 0
        for i in range(1, len(xs)):
            x0 = xs[i - 1]
            x1 = xs[i]
            side0 = abs(get_y(L, x0) - get_y(U, x0))
            side1 = abs(get_y(L, x1) - get_y(U, x1))
            h = abs(x0 - x1)
            res += (side0 + side1) * h / 2.0
        return res
    for i in range(50):
        mid = (low + high) / 2
        if get_area(cuts[-1], mid) > slice_area:
            high = mid
        else:
            low = mid
    cuts.append(mid)
    return L


def func_10a9ee267f674fbab586309c48986151(cuts, infile, slice_area):

    def get_y(P, qx):
        i = 0
        while i + 2 < len(P) and P[i + 1][0] <= qx:
            i += 1
        x0, y0 = P[i]
        x1, y1 = P[i + 1]
        return y0 + (y1 - y0) * (qx - x0) / float(x1 - x0)
    W, Ln, Un, G = map(int, infile.readline().split())
    L = []
    for each in range(Ln):
        L.append(map(int, infile.readline().split()))
    U = []
    for each in range(Un):
        U.append(map(int, infile.readline().split()))

    def get_area(left, right):
        xs = [x for x, y in L]
        xs += [x for x, y in U]
        xs += [left, right]
        xs = sorted(set(xs))
        xs = [x for x in xs if left - 1e-09 <= x <= right + 1e-09]
        res = 0
        for i in range(1, len(xs)):
            x0 = xs[i - 1]
            x1 = xs[i]
            side0 = abs(get_y(L, x0) - get_y(U, x0))
            side1 = abs(get_y(L, x1) - get_y(U, x1))
            h = abs(x0 - x1)
            res += (side0 + side1) * h / 2.0
        return res
    for i in range(50):
        mid = (low + high) / 2
        if get_area(cuts[-1], mid) > slice_area:
            high = mid
        else:
            low = mid
    cuts.append(mid)
    return Ln


def func_ece13eafcf4c4bbb975f198618b7e738(cuts, infile, slice_area):

    def get_y(P, qx):
        i = 0
        while i + 2 < len(P) and P[i + 1][0] <= qx:
            i += 1
        x0, y0 = P[i]
        x1, y1 = P[i + 1]
        return y0 + (y1 - y0) * (qx - x0) / float(x1 - x0)
    W, Ln, Un, G = map(int, infile.readline().split())
    L = []
    for each in range(Ln):
        L.append(map(int, infile.readline().split()))
    U = []
    for each in range(Un):
        U.append(map(int, infile.readline().split()))

    def get_area(left, right):
        xs = [x for x, y in L]
        xs += [x for x, y in U]
        xs += [left, right]
        xs = sorted(set(xs))
        xs = [x for x in xs if left - 1e-09 <= x <= right + 1e-09]
        res = 0
        for i in range(1, len(xs)):
            x0 = xs[i - 1]
            x1 = xs[i]
            side0 = abs(get_y(L, x0) - get_y(U, x0))
            side1 = abs(get_y(L, x1) - get_y(U, x1))
            h = abs(x0 - x1)
            res += (side0 + side1) * h / 2.0
        return res
    for i in range(50):
        mid = (low + high) / 2
        if get_area(cuts[-1], mid) > slice_area:
            high = mid
        else:
            low = mid
    cuts.append(mid)
    return Un


def func_ab75f7f3b77542ed836183f09ce78ed8(cuts, infile, slice_area):

    def get_y(P, qx):
        i = 0
        while i + 2 < len(P) and P[i + 1][0] <= qx:
            i += 1
        x0, y0 = P[i]
        x1, y1 = P[i + 1]
        return y0 + (y1 - y0) * (qx - x0) / float(x1 - x0)
    W, Ln, Un, G = map(int, infile.readline().split())
    L = []
    for each in range(Ln):
        L.append(map(int, infile.readline().split()))
    U = []
    for each in range(Un):
        U.append(map(int, infile.readline().split()))

    def get_area(left, right):
        xs = [x for x, y in L]
        xs += [x for x, y in U]
        xs += [left, right]
        xs = sorted(set(xs))
        xs = [x for x in xs if left - 1e-09 <= x <= right + 1e-09]
        res = 0
        for i in range(1, len(xs)):
            x0 = xs[i - 1]
            x1 = xs[i]
            side0 = abs(get_y(L, x0) - get_y(U, x0))
            side1 = abs(get_y(L, x1) - get_y(U, x1))
            h = abs(x0 - x1)
            res += (side0 + side1) * h / 2.0
        return res
    for i in range(50):
        mid = (low + high) / 2
        if get_area(cuts[-1], mid) > slice_area:
            high = mid
        else:
            low = mid
    cuts.append(mid)
    return mid


def func_246d1ade807241b78dc83bdedaf72165(cuts, infile, slice_area):

    def get_y(P, qx):
        i = 0
        while i + 2 < len(P) and P[i + 1][0] <= qx:
            i += 1
        x0, y0 = P[i]
        x1, y1 = P[i + 1]
        return y0 + (y1 - y0) * (qx - x0) / float(x1 - x0)
    W, Ln, Un, G = map(int, infile.readline().split())
    L = []
    for each in range(Ln):
        L.append(map(int, infile.readline().split()))
    U = []
    for each in range(Un):
        U.append(map(int, infile.readline().split()))

    def get_area(left, right):
        xs = [x for x, y in L]
        xs += [x for x, y in U]
        xs += [left, right]
        xs = sorted(set(xs))
        xs = [x for x in xs if left - 1e-09 <= x <= right + 1e-09]
        res = 0
        for i in range(1, len(xs)):
            x0 = xs[i - 1]
            x1 = xs[i]
            side0 = abs(get_y(L, x0) - get_y(U, x0))
            side1 = abs(get_y(L, x1) - get_y(U, x1))
            h = abs(x0 - x1)
            res += (side0 + side1) * h / 2.0
        return res
    for i in range(50):
        mid = (low + high) / 2
        if get_area(cuts[-1], mid) > slice_area:
            high = mid
        else:
            low = mid
    cuts.append(mid)
    return W


def func_9fe23c86abd84c29a75b6c24ece46991(cuts, mid, infile):

    def get_y(P, qx):
        i = 0
        while i + 2 < len(P) and P[i + 1][0] <= qx:
            i += 1
        x0, y0 = P[i]
        x1, y1 = P[i + 1]
        return y0 + (y1 - y0) * (qx - x0) / float(x1 - x0)
    W, Ln, Un, G = map(int, infile.readline().split())
    L = []
    for each in range(Ln):
        L.append(map(int, infile.readline().split()))
    U = []
    for each in range(Un):
        U.append(map(int, infile.readline().split()))

    def get_area(left, right):
        xs = [x for x, y in L]
        xs += [x for x, y in U]
        xs += [left, right]
        xs = sorted(set(xs))
        xs = [x for x in xs if left - 1e-09 <= x <= right + 1e-09]
        res = 0
        for i in range(1, len(xs)):
            x0 = xs[i - 1]
            x1 = xs[i]
            side0 = abs(get_y(L, x0) - get_y(U, x0))
            side1 = abs(get_y(L, x1) - get_y(U, x1))
            h = abs(x0 - x1)
            res += (side0 + side1) * h / 2.0
        return res
    cuts.append(mid)('%.10lg' % mid)
    return W


def func_f9c59b08b60d4641a017c4b14681bd23(cuts, mid, infile):

    def get_y(P, qx):
        i = 0
        while i + 2 < len(P) and P[i + 1][0] <= qx:
            i += 1
        x0, y0 = P[i]
        x1, y1 = P[i + 1]
        return y0 + (y1 - y0) * (qx - x0) / float(x1 - x0)
    W, Ln, Un, G = map(int, infile.readline().split())
    L = []
    for each in range(Ln):
        L.append(map(int, infile.readline().split()))
    U = []
    for each in range(Un):
        U.append(map(int, infile.readline().split()))

    def get_area(left, right):
        xs = [x for x, y in L]
        xs += [x for x, y in U]
        xs += [left, right]
        xs = sorted(set(xs))
        xs = [x for x in xs if left - 1e-09 <= x <= right + 1e-09]
        res = 0
        for i in range(1, len(xs)):
            x0 = xs[i - 1]
            x1 = xs[i]
            side0 = abs(get_y(L, x0) - get_y(U, x0))
            side1 = abs(get_y(L, x1) - get_y(U, x1))
            h = abs(x0 - x1)
            res += (side0 + side1) * h / 2.0
        return res
    cuts.append(mid)('%.10lg' % mid)
    return L


def func_61c677cbf6ba4115898bf1d5bd030cbd(cuts, mid, infile):

    def get_y(P, qx):
        i = 0
        while i + 2 < len(P) and P[i + 1][0] <= qx:
            i += 1
        x0, y0 = P[i]
        x1, y1 = P[i + 1]
        return y0 + (y1 - y0) * (qx - x0) / float(x1 - x0)
    W, Ln, Un, G = map(int, infile.readline().split())
    L = []
    for each in range(Ln):
        L.append(map(int, infile.readline().split()))
    U = []
    for each in range(Un):
        U.append(map(int, infile.readline().split()))

    def get_area(left, right):
        xs = [x for x, y in L]
        xs += [x for x, y in U]
        xs += [left, right]
        xs = sorted(set(xs))
        xs = [x for x in xs if left - 1e-09 <= x <= right + 1e-09]
        res = 0
        for i in range(1, len(xs)):
            x0 = xs[i - 1]
            x1 = xs[i]
            side0 = abs(get_y(L, x0) - get_y(U, x0))
            side1 = abs(get_y(L, x1) - get_y(U, x1))
            h = abs(x0 - x1)
            res += (side0 + side1) * h / 2.0
        return res
    cuts.append(mid)('%.10lg' % mid)
    return Ln


def func_9ce28bbfb8ff4ff4a05f7fefa624ad8d(cuts, mid, infile):

    def get_y(P, qx):
        i = 0
        while i + 2 < len(P) and P[i + 1][0] <= qx:
            i += 1
        x0, y0 = P[i]
        x1, y1 = P[i + 1]
        return y0 + (y1 - y0) * (qx - x0) / float(x1 - x0)
    W, Ln, Un, G = map(int, infile.readline().split())
    L = []
    for each in range(Ln):
        L.append(map(int, infile.readline().split()))
    U = []
    for each in range(Un):
        U.append(map(int, infile.readline().split()))

    def get_area(left, right):
        xs = [x for x, y in L]
        xs += [x for x, y in U]
        xs += [left, right]
        xs = sorted(set(xs))
        xs = [x for x in xs if left - 1e-09 <= x <= right + 1e-09]
        res = 0
        for i in range(1, len(xs)):
            x0 = xs[i - 1]
            x1 = xs[i]
            side0 = abs(get_y(L, x0) - get_y(U, x0))
            side1 = abs(get_y(L, x1) - get_y(U, x1))
            h = abs(x0 - x1)
            res += (side0 + side1) * h / 2.0
        return res
    cuts.append(mid)('%.10lg' % mid)
    return each


def func_f5752a1916dd4a72b503ae4e065e2be7(cuts, mid, infile):

    def get_y(P, qx):
        i = 0
        while i + 2 < len(P) and P[i + 1][0] <= qx:
            i += 1
        x0, y0 = P[i]
        x1, y1 = P[i + 1]
        return y0 + (y1 - y0) * (qx - x0) / float(x1 - x0)
    W, Ln, Un, G = map(int, infile.readline().split())
    L = []
    for each in range(Ln):
        L.append(map(int, infile.readline().split()))
    U = []
    for each in range(Un):
        U.append(map(int, infile.readline().split()))

    def get_area(left, right):
        xs = [x for x, y in L]
        xs += [x for x, y in U]
        xs += [left, right]
        xs = sorted(set(xs))
        xs = [x for x in xs if left - 1e-09 <= x <= right + 1e-09]
        res = 0
        for i in range(1, len(xs)):
            x0 = xs[i - 1]
            x1 = xs[i]
            side0 = abs(get_y(L, x0) - get_y(U, x0))
            side1 = abs(get_y(L, x1) - get_y(U, x1))
            h = abs(x0 - x1)
            res += (side0 + side1) * h / 2.0
        return res
    cuts.append(mid)('%.10lg' % mid)
    return U


def func_cea117d1221846eeb142e1e02f3ef570(cuts, mid, infile):

    def get_y(P, qx):
        i = 0
        while i + 2 < len(P) and P[i + 1][0] <= qx:
            i += 1
        x0, y0 = P[i]
        x1, y1 = P[i + 1]
        return y0 + (y1 - y0) * (qx - x0) / float(x1 - x0)
    W, Ln, Un, G = map(int, infile.readline().split())
    L = []
    for each in range(Ln):
        L.append(map(int, infile.readline().split()))
    U = []
    for each in range(Un):
        U.append(map(int, infile.readline().split()))

    def get_area(left, right):
        xs = [x for x, y in L]
        xs += [x for x, y in U]
        xs += [left, right]
        xs = sorted(set(xs))
        xs = [x for x in xs if left - 1e-09 <= x <= right + 1e-09]
        res = 0
        for i in range(1, len(xs)):
            x0 = xs[i - 1]
            x1 = xs[i]
            side0 = abs(get_y(L, x0) - get_y(U, x0))
            side1 = abs(get_y(L, x1) - get_y(U, x1))
            h = abs(x0 - x1)
            res += (side0 + side1) * h / 2.0
        return res
    cuts.append(mid)('%.10lg' % mid)
    return cuts


def func_a7b7aee3cd3a4b38a8a71d14b26580bf(cuts, mid, infile):

    def get_y(P, qx):
        i = 0
        while i + 2 < len(P) and P[i + 1][0] <= qx:
            i += 1
        x0, y0 = P[i]
        x1, y1 = P[i + 1]
        return y0 + (y1 - y0) * (qx - x0) / float(x1 - x0)
    W, Ln, Un, G = map(int, infile.readline().split())
    L = []
    for each in range(Ln):
        L.append(map(int, infile.readline().split()))
    U = []
    for each in range(Un):
        U.append(map(int, infile.readline().split()))

    def get_area(left, right):
        xs = [x for x, y in L]
        xs += [x for x, y in U]
        xs += [left, right]
        xs = sorted(set(xs))
        xs = [x for x in xs if left - 1e-09 <= x <= right + 1e-09]
        res = 0
        for i in range(1, len(xs)):
            x0 = xs[i - 1]
            x1 = xs[i]
            side0 = abs(get_y(L, x0) - get_y(U, x0))
            side1 = abs(get_y(L, x1) - get_y(U, x1))
            h = abs(x0 - x1)
            res += (side0 + side1) * h / 2.0
        return res
    cuts.append(mid)('%.10lg' % mid)
    return mid


def func_2570bcd9eb7243f5be3d91eae83ed8ac(cuts, mid, infile):

    def get_y(P, qx):
        i = 0
        while i + 2 < len(P) and P[i + 1][0] <= qx:
            i += 1
        x0, y0 = P[i]
        x1, y1 = P[i + 1]
        return y0 + (y1 - y0) * (qx - x0) / float(x1 - x0)
    W, Ln, Un, G = map(int, infile.readline().split())
    L = []
    for each in range(Ln):
        L.append(map(int, infile.readline().split()))
    U = []
    for each in range(Un):
        U.append(map(int, infile.readline().split()))

    def get_area(left, right):
        xs = [x for x, y in L]
        xs += [x for x, y in U]
        xs += [left, right]
        xs = sorted(set(xs))
        xs = [x for x in xs if left - 1e-09 <= x <= right + 1e-09]
        res = 0
        for i in range(1, len(xs)):
            x0 = xs[i - 1]
            x1 = xs[i]
            side0 = abs(get_y(L, x0) - get_y(U, x0))
            side1 = abs(get_y(L, x1) - get_y(U, x1))
            h = abs(x0 - x1)
            res += (side0 + side1) * h / 2.0
        return res
    cuts.append(mid)('%.10lg' % mid)
    return Un


def func_551728915fdb49d890f444dcb9e4c57e(cuts, mid, infile):

    def get_y(P, qx):
        i = 0
        while i + 2 < len(P) and P[i + 1][0] <= qx:
            i += 1
        x0, y0 = P[i]
        x1, y1 = P[i + 1]
        return y0 + (y1 - y0) * (qx - x0) / float(x1 - x0)
    W, Ln, Un, G = map(int, infile.readline().split())
    L = []
    for each in range(Ln):
        L.append(map(int, infile.readline().split()))
    U = []
    for each in range(Un):
        U.append(map(int, infile.readline().split()))

    def get_area(left, right):
        xs = [x for x, y in L]
        xs += [x for x, y in U]
        xs += [left, right]
        xs = sorted(set(xs))
        xs = [x for x in xs if left - 1e-09 <= x <= right + 1e-09]
        res = 0
        for i in range(1, len(xs)):
            x0 = xs[i - 1]
            x1 = xs[i]
            side0 = abs(get_y(L, x0) - get_y(U, x0))
            side1 = abs(get_y(L, x1) - get_y(U, x1))
            h = abs(x0 - x1)
            res += (side0 + side1) * h / 2.0
        return res
    cuts.append(mid)('%.10lg' % mid)
    return G


def func_65461c4b671e431c8c0dbbc5c444849b(cuts, infile, slice_area):

    def get_y(P, qx):
        i = 0
        while i + 2 < len(P) and P[i + 1][0] <= qx:
            i += 1
        x0, y0 = P[i]
        x1, y1 = P[i + 1]
        return y0 + (y1 - y0) * (qx - x0) / float(x1 - x0)
    W, Ln, Un, G = map(int, infile.readline().split())
    L = []
    for each in range(Ln):
        L.append(map(int, infile.readline().split()))
    U = []
    for each in range(Un):
        U.append(map(int, infile.readline().split()))

    def get_area(left, right):
        xs = [x for x, y in L]
        xs += [x for x, y in U]
        xs += [left, right]
        xs = sorted(set(xs))
        xs = [x for x in xs if left - 1e-09 <= x <= right + 1e-09]
        res = 0
        for i in range(1, len(xs)):
            x0 = xs[i - 1]
            x1 = xs[i]
            side0 = abs(get_y(L, x0) - get_y(U, x0))
            side1 = abs(get_y(L, x1) - get_y(U, x1))
            h = abs(x0 - x1)
            res += (side0 + side1) * h / 2.0
        return res
    low = cuts[-1]
    high = W
    for i in range(50):
        mid = (low + high) / 2
        if get_area(cuts[-1], mid) > slice_area:
            high = mid
        else:
            low = mid
    return slice_area


def func_45efe6855efd417cb1d5c2a46a86dd90(cuts, infile, slice_area):

    def get_y(P, qx):
        i = 0
        while i + 2 < len(P) and P[i + 1][0] <= qx:
            i += 1
        x0, y0 = P[i]
        x1, y1 = P[i + 1]
        return y0 + (y1 - y0) * (qx - x0) / float(x1 - x0)
    W, Ln, Un, G = map(int, infile.readline().split())
    L = []
    for each in range(Ln):
        L.append(map(int, infile.readline().split()))
    U = []
    for each in range(Un):
        U.append(map(int, infile.readline().split()))

    def get_area(left, right):
        xs = [x for x, y in L]
        xs += [x for x, y in U]
        xs += [left, right]
        xs = sorted(set(xs))
        xs = [x for x in xs if left - 1e-09 <= x <= right + 1e-09]
        res = 0
        for i in range(1, len(xs)):
            x0 = xs[i - 1]
            x1 = xs[i]
            side0 = abs(get_y(L, x0) - get_y(U, x0))
            side1 = abs(get_y(L, x1) - get_y(U, x1))
            h = abs(x0 - x1)
            res += (side0 + side1) * h / 2.0
        return res
    low = cuts[-1]
    high = W
    for i in range(50):
        mid = (low + high) / 2
        if get_area(cuts[-1], mid) > slice_area:
            high = mid
        else:
            low = mid
    return cuts


def func_a9fd206ed816431c89fc789f96ba797a(cuts, infile, slice_area):

    def get_y(P, qx):
        i = 0
        while i + 2 < len(P) and P[i + 1][0] <= qx:
            i += 1
        x0, y0 = P[i]
        x1, y1 = P[i + 1]
        return y0 + (y1 - y0) * (qx - x0) / float(x1 - x0)
    W, Ln, Un, G = map(int, infile.readline().split())
    L = []
    for each in range(Ln):
        L.append(map(int, infile.readline().split()))
    U = []
    for each in range(Un):
        U.append(map(int, infile.readline().split()))

    def get_area(left, right):
        xs = [x for x, y in L]
        xs += [x for x, y in U]
        xs += [left, right]
        xs = sorted(set(xs))
        xs = [x for x in xs if left - 1e-09 <= x <= right + 1e-09]
        res = 0
        for i in range(1, len(xs)):
            x0 = xs[i - 1]
            x1 = xs[i]
            side0 = abs(get_y(L, x0) - get_y(U, x0))
            side1 = abs(get_y(L, x1) - get_y(U, x1))
            h = abs(x0 - x1)
            res += (side0 + side1) * h / 2.0
        return res
    low = cuts[-1]
    high = W
    for i in range(50):
        mid = (low + high) / 2
        if get_area(cuts[-1], mid) > slice_area:
            high = mid
        else:
            low = mid
    return Ln


def func_a5fa6c8555064a648b1f71837f0aa66b(cuts, infile, slice_area):

    def get_y(P, qx):
        i = 0
        while i + 2 < len(P) and P[i + 1][0] <= qx:
            i += 1
        x0, y0 = P[i]
        x1, y1 = P[i + 1]
        return y0 + (y1 - y0) * (qx - x0) / float(x1 - x0)
    W, Ln, Un, G = map(int, infile.readline().split())
    L = []
    for each in range(Ln):
        L.append(map(int, infile.readline().split()))
    U = []
    for each in range(Un):
        U.append(map(int, infile.readline().split()))

    def get_area(left, right):
        xs = [x for x, y in L]
        xs += [x for x, y in U]
        xs += [left, right]
        xs = sorted(set(xs))
        xs = [x for x in xs if left - 1e-09 <= x <= right + 1e-09]
        res = 0
        for i in range(1, len(xs)):
            x0 = xs[i - 1]
            x1 = xs[i]
            side0 = abs(get_y(L, x0) - get_y(U, x0))
            side1 = abs(get_y(L, x1) - get_y(U, x1))
            h = abs(x0 - x1)
            res += (side0 + side1) * h / 2.0
        return res
    low = cuts[-1]
    high = W
    for i in range(50):
        mid = (low + high) / 2
        if get_area(cuts[-1], mid) > slice_area:
            high = mid
        else:
            low = mid
    return G


def func_d31b2d605d804b63a855b12e6eea54df(cuts, infile, slice_area):

    def get_y(P, qx):
        i = 0
        while i + 2 < len(P) and P[i + 1][0] <= qx:
            i += 1
        x0, y0 = P[i]
        x1, y1 = P[i + 1]
        return y0 + (y1 - y0) * (qx - x0) / float(x1 - x0)
    W, Ln, Un, G = map(int, infile.readline().split())
    L = []
    for each in range(Ln):
        L.append(map(int, infile.readline().split()))
    U = []
    for each in range(Un):
        U.append(map(int, infile.readline().split()))

    def get_area(left, right):
        xs = [x for x, y in L]
        xs += [x for x, y in U]
        xs += [left, right]
        xs = sorted(set(xs))
        xs = [x for x in xs if left - 1e-09 <= x <= right + 1e-09]
        res = 0
        for i in range(1, len(xs)):
            x0 = xs[i - 1]
            x1 = xs[i]
            side0 = abs(get_y(L, x0) - get_y(U, x0))
            side1 = abs(get_y(L, x1) - get_y(U, x1))
            h = abs(x0 - x1)
            res += (side0 + side1) * h / 2.0
        return res
    low = cuts[-1]
    high = W
    for i in range(50):
        mid = (low + high) / 2
        if get_area(cuts[-1], mid) > slice_area:
            high = mid
        else:
            low = mid
    return mid


def func_27cc2fa03d1b4cbc9549717e52dc8d9a(cuts, infile, slice_area):

    def get_y(P, qx):
        i = 0
        while i + 2 < len(P) and P[i + 1][0] <= qx:
            i += 1
        x0, y0 = P[i]
        x1, y1 = P[i + 1]
        return y0 + (y1 - y0) * (qx - x0) / float(x1 - x0)
    W, Ln, Un, G = map(int, infile.readline().split())
    L = []
    for each in range(Ln):
        L.append(map(int, infile.readline().split()))
    U = []
    for each in range(Un):
        U.append(map(int, infile.readline().split()))

    def get_area(left, right):
        xs = [x for x, y in L]
        xs += [x for x, y in U]
        xs += [left, right]
        xs = sorted(set(xs))
        xs = [x for x in xs if left - 1e-09 <= x <= right + 1e-09]
        res = 0
        for i in range(1, len(xs)):
            x0 = xs[i - 1]
            x1 = xs[i]
            side0 = abs(get_y(L, x0) - get_y(U, x0))
            side1 = abs(get_y(L, x1) - get_y(U, x1))
            h = abs(x0 - x1)
            res += (side0 + side1) * h / 2.0
        return res
    low = cuts[-1]
    high = W
    for i in range(50):
        mid = (low + high) / 2
        if get_area(cuts[-1], mid) > slice_area:
            high = mid
        else:
            low = mid
    return i


def func_640d31c4d2834d22a43f00a2bcb45085(cuts, infile, slice_area):

    def get_y(P, qx):
        i = 0
        while i + 2 < len(P) and P[i + 1][0] <= qx:
            i += 1
        x0, y0 = P[i]
        x1, y1 = P[i + 1]
        return y0 + (y1 - y0) * (qx - x0) / float(x1 - x0)
    W, Ln, Un, G = map(int, infile.readline().split())
    L = []
    for each in range(Ln):
        L.append(map(int, infile.readline().split()))
    U = []
    for each in range(Un):
        U.append(map(int, infile.readline().split()))

    def get_area(left, right):
        xs = [x for x, y in L]
        xs += [x for x, y in U]
        xs += [left, right]
        xs = sorted(set(xs))
        xs = [x for x in xs if left - 1e-09 <= x <= right + 1e-09]
        res = 0
        for i in range(1, len(xs)):
            x0 = xs[i - 1]
            x1 = xs[i]
            side0 = abs(get_y(L, x0) - get_y(U, x0))
            side1 = abs(get_y(L, x1) - get_y(U, x1))
            h = abs(x0 - x1)
            res += (side0 + side1) * h / 2.0
        return res
    low = cuts[-1]
    high = W
    for i in range(50):
        mid = (low + high) / 2
        if get_area(cuts[-1], mid) > slice_area:
            high = mid
        else:
            low = mid
    return high


def func_d5c906ef7a16473f951b5d32a6eb9113(cuts, infile, slice_area):

    def get_y(P, qx):
        i = 0
        while i + 2 < len(P) and P[i + 1][0] <= qx:
            i += 1
        x0, y0 = P[i]
        x1, y1 = P[i + 1]
        return y0 + (y1 - y0) * (qx - x0) / float(x1 - x0)
    W, Ln, Un, G = map(int, infile.readline().split())
    L = []
    for each in range(Ln):
        L.append(map(int, infile.readline().split()))
    U = []
    for each in range(Un):
        U.append(map(int, infile.readline().split()))

    def get_area(left, right):
        xs = [x for x, y in L]
        xs += [x for x, y in U]
        xs += [left, right]
        xs = sorted(set(xs))
        xs = [x for x in xs if left - 1e-09 <= x <= right + 1e-09]
        res = 0
        for i in range(1, len(xs)):
            x0 = xs[i - 1]
            x1 = xs[i]
            side0 = abs(get_y(L, x0) - get_y(U, x0))
            side1 = abs(get_y(L, x1) - get_y(U, x1))
            h = abs(x0 - x1)
            res += (side0 + side1) * h / 2.0
        return res
    low = cuts[-1]
    high = W
    for i in range(50):
        mid = (low + high) / 2
        if get_area(cuts[-1], mid) > slice_area:
            high = mid
        else:
            low = mid
    return each


def func_48cb63ab336348f2beea2408a487d974(cuts, infile, slice_area):

    def get_y(P, qx):
        i = 0
        while i + 2 < len(P) and P[i + 1][0] <= qx:
            i += 1
        x0, y0 = P[i]
        x1, y1 = P[i + 1]
        return y0 + (y1 - y0) * (qx - x0) / float(x1 - x0)
    W, Ln, Un, G = map(int, infile.readline().split())
    L = []
    for each in range(Ln):
        L.append(map(int, infile.readline().split()))
    U = []
    for each in range(Un):
        U.append(map(int, infile.readline().split()))

    def get_area(left, right):
        xs = [x for x, y in L]
        xs += [x for x, y in U]
        xs += [left, right]
        xs = sorted(set(xs))
        xs = [x for x in xs if left - 1e-09 <= x <= right + 1e-09]
        res = 0
        for i in range(1, len(xs)):
            x0 = xs[i - 1]
            x1 = xs[i]
            side0 = abs(get_y(L, x0) - get_y(U, x0))
            side1 = abs(get_y(L, x1) - get_y(U, x1))
            h = abs(x0 - x1)
            res += (side0 + side1) * h / 2.0
        return res
    low = cuts[-1]
    high = W
    for i in range(50):
        mid = (low + high) / 2
        if get_area(cuts[-1], mid) > slice_area:
            high = mid
        else:
            low = mid
    return L


def func_199de134c9d944248503b9ce93b9eb4d(cuts, infile, slice_area):

    def get_y(P, qx):
        i = 0
        while i + 2 < len(P) and P[i + 1][0] <= qx:
            i += 1
        x0, y0 = P[i]
        x1, y1 = P[i + 1]
        return y0 + (y1 - y0) * (qx - x0) / float(x1 - x0)
    W, Ln, Un, G = map(int, infile.readline().split())
    L = []
    for each in range(Ln):
        L.append(map(int, infile.readline().split()))
    U = []
    for each in range(Un):
        U.append(map(int, infile.readline().split()))

    def get_area(left, right):
        xs = [x for x, y in L]
        xs += [x for x, y in U]
        xs += [left, right]
        xs = sorted(set(xs))
        xs = [x for x in xs if left - 1e-09 <= x <= right + 1e-09]
        res = 0
        for i in range(1, len(xs)):
            x0 = xs[i - 1]
            x1 = xs[i]
            side0 = abs(get_y(L, x0) - get_y(U, x0))
            side1 = abs(get_y(L, x1) - get_y(U, x1))
            h = abs(x0 - x1)
            res += (side0 + side1) * h / 2.0
        return res
    low = cuts[-1]
    high = W
    for i in range(50):
        mid = (low + high) / 2
        if get_area(cuts[-1], mid) > slice_area:
            high = mid
        else:
            low = mid
    return W


def func_eb494e3444594e7a8a4adb77eec4d675(cuts, infile, slice_area):

    def get_y(P, qx):
        i = 0
        while i + 2 < len(P) and P[i + 1][0] <= qx:
            i += 1
        x0, y0 = P[i]
        x1, y1 = P[i + 1]
        return y0 + (y1 - y0) * (qx - x0) / float(x1 - x0)
    W, Ln, Un, G = map(int, infile.readline().split())
    L = []
    for each in range(Ln):
        L.append(map(int, infile.readline().split()))
    U = []
    for each in range(Un):
        U.append(map(int, infile.readline().split()))

    def get_area(left, right):
        xs = [x for x, y in L]
        xs += [x for x, y in U]
        xs += [left, right]
        xs = sorted(set(xs))
        xs = [x for x in xs if left - 1e-09 <= x <= right + 1e-09]
        res = 0
        for i in range(1, len(xs)):
            x0 = xs[i - 1]
            x1 = xs[i]
            side0 = abs(get_y(L, x0) - get_y(U, x0))
            side1 = abs(get_y(L, x1) - get_y(U, x1))
            h = abs(x0 - x1)
            res += (side0 + side1) * h / 2.0
        return res
    low = cuts[-1]
    high = W
    for i in range(50):
        mid = (low + high) / 2
        if get_area(cuts[-1], mid) > slice_area:
            high = mid
        else:
            low = mid
    return U


def func_aa3638e554074f5d9a5d410897cc5691(cuts, infile, slice_area):

    def get_y(P, qx):
        i = 0
        while i + 2 < len(P) and P[i + 1][0] <= qx:
            i += 1
        x0, y0 = P[i]
        x1, y1 = P[i + 1]
        return y0 + (y1 - y0) * (qx - x0) / float(x1 - x0)
    W, Ln, Un, G = map(int, infile.readline().split())
    L = []
    for each in range(Ln):
        L.append(map(int, infile.readline().split()))
    U = []
    for each in range(Un):
        U.append(map(int, infile.readline().split()))

    def get_area(left, right):
        xs = [x for x, y in L]
        xs += [x for x, y in U]
        xs += [left, right]
        xs = sorted(set(xs))
        xs = [x for x in xs if left - 1e-09 <= x <= right + 1e-09]
        res = 0
        for i in range(1, len(xs)):
            x0 = xs[i - 1]
            x1 = xs[i]
            side0 = abs(get_y(L, x0) - get_y(U, x0))
            side1 = abs(get_y(L, x1) - get_y(U, x1))
            h = abs(x0 - x1)
            res += (side0 + side1) * h / 2.0
        return res
    low = cuts[-1]
    high = W
    for i in range(50):
        mid = (low + high) / 2
        if get_area(cuts[-1], mid) > slice_area:
            high = mid
        else:
            low = mid
    return Un


def func_e7e31432c3f9419899159aac30c700fa(cuts, infile, slice_area):

    def get_y(P, qx):
        i = 0
        while i + 2 < len(P) and P[i + 1][0] <= qx:
            i += 1
        x0, y0 = P[i]
        x1, y1 = P[i + 1]
        return y0 + (y1 - y0) * (qx - x0) / float(x1 - x0)
    W, Ln, Un, G = map(int, infile.readline().split())
    L = []
    for each in range(Ln):
        L.append(map(int, infile.readline().split()))
    U = []
    for each in range(Un):
        U.append(map(int, infile.readline().split()))

    def get_area(left, right):
        xs = [x for x, y in L]
        xs += [x for x, y in U]
        xs += [left, right]
        xs = sorted(set(xs))
        xs = [x for x in xs if left - 1e-09 <= x <= right + 1e-09]
        res = 0
        for i in range(1, len(xs)):
            x0 = xs[i - 1]
            x1 = xs[i]
            side0 = abs(get_y(L, x0) - get_y(U, x0))
            side1 = abs(get_y(L, x1) - get_y(U, x1))
            h = abs(x0 - x1)
            res += (side0 + side1) * h / 2.0
        return res
    low = cuts[-1]
    high = W
    for i in range(50):
        mid = (low + high) / 2
        if get_area(cuts[-1], mid) > slice_area:
            high = mid
        else:
            low = mid
    return low


def func_3ede204a7cf54e339e75439ec6b815cd(cuts, infile, slice_area):

    def get_y(P, qx):
        i = 0
        while i + 2 < len(P) and P[i + 1][0] <= qx:
            i += 1
        x0, y0 = P[i]
        x1, y1 = P[i + 1]
        return y0 + (y1 - y0) * (qx - x0) / float(x1 - x0)
    W, Ln, Un, G = map(int, infile.readline().split())
    L = []
    for each in range(Ln):
        L.append(map(int, infile.readline().split()))
    U = []
    for each in range(Un):
        U.append(map(int, infile.readline().split()))

    def get_area(left, right):
        xs = [x for x, y in L]
        xs += [x for x, y in U]
        xs += [left, right]
        xs = sorted(set(xs))
        xs = [x for x in xs if left - 1e-09 <= x <= right + 1e-09]
        res = 0
        for i in range(1, len(xs)):
            x0 = xs[i - 1]
            x1 = xs[i]
            side0 = abs(get_y(L, x0) - get_y(U, x0))
            side1 = abs(get_y(L, x1) - get_y(U, x1))
            h = abs(x0 - x1)
            res += (side0 + side1) * h / 2.0
        return res
    high = W
    for i in range(50):
        mid = (low + high) / 2
        if get_area(cuts[-1], mid) > slice_area:
            high = mid
        else:
            low = mid
    cuts.append(mid)
    return G


def func_3eff937f5d2144f7998d1d6af59753f2(cuts, infile, slice_area):

    def get_y(P, qx):
        i = 0
        while i + 2 < len(P) and P[i + 1][0] <= qx:
            i += 1
        x0, y0 = P[i]
        x1, y1 = P[i + 1]
        return y0 + (y1 - y0) * (qx - x0) / float(x1 - x0)
    W, Ln, Un, G = map(int, infile.readline().split())
    L = []
    for each in range(Ln):
        L.append(map(int, infile.readline().split()))
    U = []
    for each in range(Un):
        U.append(map(int, infile.readline().split()))

    def get_area(left, right):
        xs = [x for x, y in L]
        xs += [x for x, y in U]
        xs += [left, right]
        xs = sorted(set(xs))
        xs = [x for x in xs if left - 1e-09 <= x <= right + 1e-09]
        res = 0
        for i in range(1, len(xs)):
            x0 = xs[i - 1]
            x1 = xs[i]
            side0 = abs(get_y(L, x0) - get_y(U, x0))
            side1 = abs(get_y(L, x1) - get_y(U, x1))
            h = abs(x0 - x1)
            res += (side0 + side1) * h / 2.0
        return res
    high = W
    for i in range(50):
        mid = (low + high) / 2
        if get_area(cuts[-1], mid) > slice_area:
            high = mid
        else:
            low = mid
    cuts.append(mid)
    return Ln


def func_9af899f63a2144c4b85d1312b9d2d184(cuts, infile, slice_area):

    def get_y(P, qx):
        i = 0
        while i + 2 < len(P) and P[i + 1][0] <= qx:
            i += 1
        x0, y0 = P[i]
        x1, y1 = P[i + 1]
        return y0 + (y1 - y0) * (qx - x0) / float(x1 - x0)
    W, Ln, Un, G = map(int, infile.readline().split())
    L = []
    for each in range(Ln):
        L.append(map(int, infile.readline().split()))
    U = []
    for each in range(Un):
        U.append(map(int, infile.readline().split()))

    def get_area(left, right):
        xs = [x for x, y in L]
        xs += [x for x, y in U]
        xs += [left, right]
        xs = sorted(set(xs))
        xs = [x for x in xs if left - 1e-09 <= x <= right + 1e-09]
        res = 0
        for i in range(1, len(xs)):
            x0 = xs[i - 1]
            x1 = xs[i]
            side0 = abs(get_y(L, x0) - get_y(U, x0))
            side1 = abs(get_y(L, x1) - get_y(U, x1))
            h = abs(x0 - x1)
            res += (side0 + side1) * h / 2.0
        return res
    high = W
    for i in range(50):
        mid = (low + high) / 2
        if get_area(cuts[-1], mid) > slice_area:
            high = mid
        else:
            low = mid
    cuts.append(mid)
    return W


def func_05f958c44a0b483bb7b04cbfa918317e(cuts, infile, slice_area):

    def get_y(P, qx):
        i = 0
        while i + 2 < len(P) and P[i + 1][0] <= qx:
            i += 1
        x0, y0 = P[i]
        x1, y1 = P[i + 1]
        return y0 + (y1 - y0) * (qx - x0) / float(x1 - x0)
    W, Ln, Un, G = map(int, infile.readline().split())
    L = []
    for each in range(Ln):
        L.append(map(int, infile.readline().split()))
    U = []
    for each in range(Un):
        U.append(map(int, infile.readline().split()))

    def get_area(left, right):
        xs = [x for x, y in L]
        xs += [x for x, y in U]
        xs += [left, right]
        xs = sorted(set(xs))
        xs = [x for x in xs if left - 1e-09 <= x <= right + 1e-09]
        res = 0
        for i in range(1, len(xs)):
            x0 = xs[i - 1]
            x1 = xs[i]
            side0 = abs(get_y(L, x0) - get_y(U, x0))
            side1 = abs(get_y(L, x1) - get_y(U, x1))
            h = abs(x0 - x1)
            res += (side0 + side1) * h / 2.0
        return res
    high = W
    for i in range(50):
        mid = (low + high) / 2
        if get_area(cuts[-1], mid) > slice_area:
            high = mid
        else:
            low = mid
    cuts.append(mid)
    return low


def func_f48c32e338cf412691bb959adc0f8fe0(cuts, infile, slice_area):

    def get_y(P, qx):
        i = 0
        while i + 2 < len(P) and P[i + 1][0] <= qx:
            i += 1
        x0, y0 = P[i]
        x1, y1 = P[i + 1]
        return y0 + (y1 - y0) * (qx - x0) / float(x1 - x0)
    W, Ln, Un, G = map(int, infile.readline().split())
    L = []
    for each in range(Ln):
        L.append(map(int, infile.readline().split()))
    U = []
    for each in range(Un):
        U.append(map(int, infile.readline().split()))

    def get_area(left, right):
        xs = [x for x, y in L]
        xs += [x for x, y in U]
        xs += [left, right]
        xs = sorted(set(xs))
        xs = [x for x in xs if left - 1e-09 <= x <= right + 1e-09]
        res = 0
        for i in range(1, len(xs)):
            x0 = xs[i - 1]
            x1 = xs[i]
            side0 = abs(get_y(L, x0) - get_y(U, x0))
            side1 = abs(get_y(L, x1) - get_y(U, x1))
            h = abs(x0 - x1)
            res += (side0 + side1) * h / 2.0
        return res
    high = W
    for i in range(50):
        mid = (low + high) / 2
        if get_area(cuts[-1], mid) > slice_area:
            high = mid
        else:
            low = mid
    cuts.append(mid)
    return L


def func_4be4e1cf74f54e9c9648c4968f8e4210(cuts, infile, slice_area):

    def get_y(P, qx):
        i = 0
        while i + 2 < len(P) and P[i + 1][0] <= qx:
            i += 1
        x0, y0 = P[i]
        x1, y1 = P[i + 1]
        return y0 + (y1 - y0) * (qx - x0) / float(x1 - x0)
    W, Ln, Un, G = map(int, infile.readline().split())
    L = []
    for each in range(Ln):
        L.append(map(int, infile.readline().split()))
    U = []
    for each in range(Un):
        U.append(map(int, infile.readline().split()))

    def get_area(left, right):
        xs = [x for x, y in L]
        xs += [x for x, y in U]
        xs += [left, right]
        xs = sorted(set(xs))
        xs = [x for x in xs if left - 1e-09 <= x <= right + 1e-09]
        res = 0
        for i in range(1, len(xs)):
            x0 = xs[i - 1]
            x1 = xs[i]
            side0 = abs(get_y(L, x0) - get_y(U, x0))
            side1 = abs(get_y(L, x1) - get_y(U, x1))
            h = abs(x0 - x1)
            res += (side0 + side1) * h / 2.0
        return res
    high = W
    for i in range(50):
        mid = (low + high) / 2
        if get_area(cuts[-1], mid) > slice_area:
            high = mid
        else:
            low = mid
    cuts.append(mid)
    return i


def func_866a4a6bb7164ef5bdfe7a64a1ae0726(cuts, infile, slice_area):

    def get_y(P, qx):
        i = 0
        while i + 2 < len(P) and P[i + 1][0] <= qx:
            i += 1
        x0, y0 = P[i]
        x1, y1 = P[i + 1]
        return y0 + (y1 - y0) * (qx - x0) / float(x1 - x0)
    W, Ln, Un, G = map(int, infile.readline().split())
    L = []
    for each in range(Ln):
        L.append(map(int, infile.readline().split()))
    U = []
    for each in range(Un):
        U.append(map(int, infile.readline().split()))

    def get_area(left, right):
        xs = [x for x, y in L]
        xs += [x for x, y in U]
        xs += [left, right]
        xs = sorted(set(xs))
        xs = [x for x in xs if left - 1e-09 <= x <= right + 1e-09]
        res = 0
        for i in range(1, len(xs)):
            x0 = xs[i - 1]
            x1 = xs[i]
            side0 = abs(get_y(L, x0) - get_y(U, x0))
            side1 = abs(get_y(L, x1) - get_y(U, x1))
            h = abs(x0 - x1)
            res += (side0 + side1) * h / 2.0
        return res
    high = W
    for i in range(50):
        mid = (low + high) / 2
        if get_area(cuts[-1], mid) > slice_area:
            high = mid
        else:
            low = mid
    cuts.append(mid)
    return mid


def func_8799665a069c4df0a2fa56de247f5a3e(cuts, infile, slice_area):

    def get_y(P, qx):
        i = 0
        while i + 2 < len(P) and P[i + 1][0] <= qx:
            i += 1
        x0, y0 = P[i]
        x1, y1 = P[i + 1]
        return y0 + (y1 - y0) * (qx - x0) / float(x1 - x0)
    W, Ln, Un, G = map(int, infile.readline().split())
    L = []
    for each in range(Ln):
        L.append(map(int, infile.readline().split()))
    U = []
    for each in range(Un):
        U.append(map(int, infile.readline().split()))

    def get_area(left, right):
        xs = [x for x, y in L]
        xs += [x for x, y in U]
        xs += [left, right]
        xs = sorted(set(xs))
        xs = [x for x in xs if left - 1e-09 <= x <= right + 1e-09]
        res = 0
        for i in range(1, len(xs)):
            x0 = xs[i - 1]
            x1 = xs[i]
            side0 = abs(get_y(L, x0) - get_y(U, x0))
            side1 = abs(get_y(L, x1) - get_y(U, x1))
            h = abs(x0 - x1)
            res += (side0 + side1) * h / 2.0
        return res
    high = W
    for i in range(50):
        mid = (low + high) / 2
        if get_area(cuts[-1], mid) > slice_area:
            high = mid
        else:
            low = mid
    cuts.append(mid)
    return each


def func_b30fa87a73a648288e36ccec1fda2304(cuts, infile, slice_area):

    def get_y(P, qx):
        i = 0
        while i + 2 < len(P) and P[i + 1][0] <= qx:
            i += 1
        x0, y0 = P[i]
        x1, y1 = P[i + 1]
        return y0 + (y1 - y0) * (qx - x0) / float(x1 - x0)
    W, Ln, Un, G = map(int, infile.readline().split())
    L = []
    for each in range(Ln):
        L.append(map(int, infile.readline().split()))
    U = []
    for each in range(Un):
        U.append(map(int, infile.readline().split()))

    def get_area(left, right):
        xs = [x for x, y in L]
        xs += [x for x, y in U]
        xs += [left, right]
        xs = sorted(set(xs))
        xs = [x for x in xs if left - 1e-09 <= x <= right + 1e-09]
        res = 0
        for i in range(1, len(xs)):
            x0 = xs[i - 1]
            x1 = xs[i]
            side0 = abs(get_y(L, x0) - get_y(U, x0))
            side1 = abs(get_y(L, x1) - get_y(U, x1))
            h = abs(x0 - x1)
            res += (side0 + side1) * h / 2.0
        return res
    high = W
    for i in range(50):
        mid = (low + high) / 2
        if get_area(cuts[-1], mid) > slice_area:
            high = mid
        else:
            low = mid
    cuts.append(mid)
    return cuts


def func_5a388a1d95bb4d1b8e28fa65adfb283e(cuts, infile, slice_area):

    def get_y(P, qx):
        i = 0
        while i + 2 < len(P) and P[i + 1][0] <= qx:
            i += 1
        x0, y0 = P[i]
        x1, y1 = P[i + 1]
        return y0 + (y1 - y0) * (qx - x0) / float(x1 - x0)
    W, Ln, Un, G = map(int, infile.readline().split())
    L = []
    for each in range(Ln):
        L.append(map(int, infile.readline().split()))
    U = []
    for each in range(Un):
        U.append(map(int, infile.readline().split()))

    def get_area(left, right):
        xs = [x for x, y in L]
        xs += [x for x, y in U]
        xs += [left, right]
        xs = sorted(set(xs))
        xs = [x for x in xs if left - 1e-09 <= x <= right + 1e-09]
        res = 0
        for i in range(1, len(xs)):
            x0 = xs[i - 1]
            x1 = xs[i]
            side0 = abs(get_y(L, x0) - get_y(U, x0))
            side1 = abs(get_y(L, x1) - get_y(U, x1))
            h = abs(x0 - x1)
            res += (side0 + side1) * h / 2.0
        return res
    high = W
    for i in range(50):
        mid = (low + high) / 2
        if get_area(cuts[-1], mid) > slice_area:
            high = mid
        else:
            low = mid
    cuts.append(mid)
    return Un


def func_a52c259283ab413f976b75499520de21(cuts, infile, slice_area):

    def get_y(P, qx):
        i = 0
        while i + 2 < len(P) and P[i + 1][0] <= qx:
            i += 1
        x0, y0 = P[i]
        x1, y1 = P[i + 1]
        return y0 + (y1 - y0) * (qx - x0) / float(x1 - x0)
    W, Ln, Un, G = map(int, infile.readline().split())
    L = []
    for each in range(Ln):
        L.append(map(int, infile.readline().split()))
    U = []
    for each in range(Un):
        U.append(map(int, infile.readline().split()))

    def get_area(left, right):
        xs = [x for x, y in L]
        xs += [x for x, y in U]
        xs += [left, right]
        xs = sorted(set(xs))
        xs = [x for x in xs if left - 1e-09 <= x <= right + 1e-09]
        res = 0
        for i in range(1, len(xs)):
            x0 = xs[i - 1]
            x1 = xs[i]
            side0 = abs(get_y(L, x0) - get_y(U, x0))
            side1 = abs(get_y(L, x1) - get_y(U, x1))
            h = abs(x0 - x1)
            res += (side0 + side1) * h / 2.0
        return res
    high = W
    for i in range(50):
        mid = (low + high) / 2
        if get_area(cuts[-1], mid) > slice_area:
            high = mid
        else:
            low = mid
    cuts.append(mid)
    return U


def func_109b86e2cf2a480b9f52c14a0a872569(cuts, infile, slice_area):

    def get_y(P, qx):
        i = 0
        while i + 2 < len(P) and P[i + 1][0] <= qx:
            i += 1
        x0, y0 = P[i]
        x1, y1 = P[i + 1]
        return y0 + (y1 - y0) * (qx - x0) / float(x1 - x0)
    W, Ln, Un, G = map(int, infile.readline().split())
    L = []
    for each in range(Ln):
        L.append(map(int, infile.readline().split()))
    U = []
    for each in range(Un):
        U.append(map(int, infile.readline().split()))

    def get_area(left, right):
        xs = [x for x, y in L]
        xs += [x for x, y in U]
        xs += [left, right]
        xs = sorted(set(xs))
        xs = [x for x in xs if left - 1e-09 <= x <= right + 1e-09]
        res = 0
        for i in range(1, len(xs)):
            x0 = xs[i - 1]
            x1 = xs[i]
            side0 = abs(get_y(L, x0) - get_y(U, x0))
            side1 = abs(get_y(L, x1) - get_y(U, x1))
            h = abs(x0 - x1)
            res += (side0 + side1) * h / 2.0
        return res
    high = W
    for i in range(50):
        mid = (low + high) / 2
        if get_area(cuts[-1], mid) > slice_area:
            high = mid
        else:
            low = mid
    cuts.append(mid)
    return slice_area


def func_f902c560c1554eac93de857c544e3fee(cuts, infile, slice_area):

    def get_y(P, qx):
        i = 0
        while i + 2 < len(P) and P[i + 1][0] <= qx:
            i += 1
        x0, y0 = P[i]
        x1, y1 = P[i + 1]
        return y0 + (y1 - y0) * (qx - x0) / float(x1 - x0)
    W, Ln, Un, G = map(int, infile.readline().split())
    L = []
    for each in range(Ln):
        L.append(map(int, infile.readline().split()))
    U = []
    for each in range(Un):
        U.append(map(int, infile.readline().split()))

    def get_area(left, right):
        xs = [x for x, y in L]
        xs += [x for x, y in U]
        xs += [left, right]
        xs = sorted(set(xs))
        xs = [x for x in xs if left - 1e-09 <= x <= right + 1e-09]
        res = 0
        for i in range(1, len(xs)):
            x0 = xs[i - 1]
            x1 = xs[i]
            side0 = abs(get_y(L, x0) - get_y(U, x0))
            side1 = abs(get_y(L, x1) - get_y(U, x1))
            h = abs(x0 - x1)
            res += (side0 + side1) * h / 2.0
        return res
    high = W
    for i in range(50):
        mid = (low + high) / 2
        if get_area(cuts[-1], mid) > slice_area:
            high = mid
        else:
            low = mid
    cuts.append(mid)
    return high


def func_d84ed519317546fc94edfb1e5f7841d4(cuts, infile, slice_area):

    def get_y(P, qx):
        i = 0
        while i + 2 < len(P) and P[i + 1][0] <= qx:
            i += 1
        x0, y0 = P[i]
        x1, y1 = P[i + 1]
        return y0 + (y1 - y0) * (qx - x0) / float(x1 - x0)
    W, Ln, Un, G = map(int, infile.readline().split())
    L = []
    for each in range(Ln):
        L.append(map(int, infile.readline().split()))
    U = []
    for each in range(Un):
        U.append(map(int, infile.readline().split()))

    def get_area(left, right):
        xs = [x for x, y in L]
        xs += [x for x, y in U]
        xs += [left, right]
        xs = sorted(set(xs))
        xs = [x for x in xs if left - 1e-09 <= x <= right + 1e-09]
        res = 0
        for i in range(1, len(xs)):
            x0 = xs[i - 1]
            x1 = xs[i]
            side0 = abs(get_y(L, x0) - get_y(U, x0))
            side1 = abs(get_y(L, x1) - get_y(U, x1))
            h = abs(x0 - x1)
            res += (side0 + side1) * h / 2.0
        return res
    for i in range(50):
        mid = (low + high) / 2
        if get_area(cuts[-1], mid) > slice_area:
            high = mid
        else:
            low = mid
    cuts.append(mid)('%.10lg' % mid)
    return Ln


def func_c232ce7e4cd24190bd8cf804637d5bcc(cuts, infile, slice_area):

    def get_y(P, qx):
        i = 0
        while i + 2 < len(P) and P[i + 1][0] <= qx:
            i += 1
        x0, y0 = P[i]
        x1, y1 = P[i + 1]
        return y0 + (y1 - y0) * (qx - x0) / float(x1 - x0)
    W, Ln, Un, G = map(int, infile.readline().split())
    L = []
    for each in range(Ln):
        L.append(map(int, infile.readline().split()))
    U = []
    for each in range(Un):
        U.append(map(int, infile.readline().split()))

    def get_area(left, right):
        xs = [x for x, y in L]
        xs += [x for x, y in U]
        xs += [left, right]
        xs = sorted(set(xs))
        xs = [x for x in xs if left - 1e-09 <= x <= right + 1e-09]
        res = 0
        for i in range(1, len(xs)):
            x0 = xs[i - 1]
            x1 = xs[i]
            side0 = abs(get_y(L, x0) - get_y(U, x0))
            side1 = abs(get_y(L, x1) - get_y(U, x1))
            h = abs(x0 - x1)
            res += (side0 + side1) * h / 2.0
        return res
    for i in range(50):
        mid = (low + high) / 2
        if get_area(cuts[-1], mid) > slice_area:
            high = mid
        else:
            low = mid
    cuts.append(mid)('%.10lg' % mid)
    return slice_area


def func_1a5bc557f4ad458c966428586aeee41f(cuts, infile, slice_area):

    def get_y(P, qx):
        i = 0
        while i + 2 < len(P) and P[i + 1][0] <= qx:
            i += 1
        x0, y0 = P[i]
        x1, y1 = P[i + 1]
        return y0 + (y1 - y0) * (qx - x0) / float(x1 - x0)
    W, Ln, Un, G = map(int, infile.readline().split())
    L = []
    for each in range(Ln):
        L.append(map(int, infile.readline().split()))
    U = []
    for each in range(Un):
        U.append(map(int, infile.readline().split()))

    def get_area(left, right):
        xs = [x for x, y in L]
        xs += [x for x, y in U]
        xs += [left, right]
        xs = sorted(set(xs))
        xs = [x for x in xs if left - 1e-09 <= x <= right + 1e-09]
        res = 0
        for i in range(1, len(xs)):
            x0 = xs[i - 1]
            x1 = xs[i]
            side0 = abs(get_y(L, x0) - get_y(U, x0))
            side1 = abs(get_y(L, x1) - get_y(U, x1))
            h = abs(x0 - x1)
            res += (side0 + side1) * h / 2.0
        return res
    for i in range(50):
        mid = (low + high) / 2
        if get_area(cuts[-1], mid) > slice_area:
            high = mid
        else:
            low = mid
    cuts.append(mid)('%.10lg' % mid)
    return low


def func_7bcbe84a2fd5449ea1a0021f1b47676c(cuts, infile, slice_area):

    def get_y(P, qx):
        i = 0
        while i + 2 < len(P) and P[i + 1][0] <= qx:
            i += 1
        x0, y0 = P[i]
        x1, y1 = P[i + 1]
        return y0 + (y1 - y0) * (qx - x0) / float(x1 - x0)
    W, Ln, Un, G = map(int, infile.readline().split())
    L = []
    for each in range(Ln):
        L.append(map(int, infile.readline().split()))
    U = []
    for each in range(Un):
        U.append(map(int, infile.readline().split()))

    def get_area(left, right):
        xs = [x for x, y in L]
        xs += [x for x, y in U]
        xs += [left, right]
        xs = sorted(set(xs))
        xs = [x for x in xs if left - 1e-09 <= x <= right + 1e-09]
        res = 0
        for i in range(1, len(xs)):
            x0 = xs[i - 1]
            x1 = xs[i]
            side0 = abs(get_y(L, x0) - get_y(U, x0))
            side1 = abs(get_y(L, x1) - get_y(U, x1))
            h = abs(x0 - x1)
            res += (side0 + side1) * h / 2.0
        return res
    for i in range(50):
        mid = (low + high) / 2
        if get_area(cuts[-1], mid) > slice_area:
            high = mid
        else:
            low = mid
    cuts.append(mid)('%.10lg' % mid)
    return cuts


def func_f73fc2654d684645a1eb46b94dff73d9(cuts, infile, slice_area):

    def get_y(P, qx):
        i = 0
        while i + 2 < len(P) and P[i + 1][0] <= qx:
            i += 1
        x0, y0 = P[i]
        x1, y1 = P[i + 1]
        return y0 + (y1 - y0) * (qx - x0) / float(x1 - x0)
    W, Ln, Un, G = map(int, infile.readline().split())
    L = []
    for each in range(Ln):
        L.append(map(int, infile.readline().split()))
    U = []
    for each in range(Un):
        U.append(map(int, infile.readline().split()))

    def get_area(left, right):
        xs = [x for x, y in L]
        xs += [x for x, y in U]
        xs += [left, right]
        xs = sorted(set(xs))
        xs = [x for x in xs if left - 1e-09 <= x <= right + 1e-09]
        res = 0
        for i in range(1, len(xs)):
            x0 = xs[i - 1]
            x1 = xs[i]
            side0 = abs(get_y(L, x0) - get_y(U, x0))
            side1 = abs(get_y(L, x1) - get_y(U, x1))
            h = abs(x0 - x1)
            res += (side0 + side1) * h / 2.0
        return res
    for i in range(50):
        mid = (low + high) / 2
        if get_area(cuts[-1], mid) > slice_area:
            high = mid
        else:
            low = mid
    cuts.append(mid)('%.10lg' % mid)
    return W


def func_eecd62eabdde4cf185d2ade63ea9f080(cuts, infile, slice_area):

    def get_y(P, qx):
        i = 0
        while i + 2 < len(P) and P[i + 1][0] <= qx:
            i += 1
        x0, y0 = P[i]
        x1, y1 = P[i + 1]
        return y0 + (y1 - y0) * (qx - x0) / float(x1 - x0)
    W, Ln, Un, G = map(int, infile.readline().split())
    L = []
    for each in range(Ln):
        L.append(map(int, infile.readline().split()))
    U = []
    for each in range(Un):
        U.append(map(int, infile.readline().split()))

    def get_area(left, right):
        xs = [x for x, y in L]
        xs += [x for x, y in U]
        xs += [left, right]
        xs = sorted(set(xs))
        xs = [x for x in xs if left - 1e-09 <= x <= right + 1e-09]
        res = 0
        for i in range(1, len(xs)):
            x0 = xs[i - 1]
            x1 = xs[i]
            side0 = abs(get_y(L, x0) - get_y(U, x0))
            side1 = abs(get_y(L, x1) - get_y(U, x1))
            h = abs(x0 - x1)
            res += (side0 + side1) * h / 2.0
        return res
    for i in range(50):
        mid = (low + high) / 2
        if get_area(cuts[-1], mid) > slice_area:
            high = mid
        else:
            low = mid
    cuts.append(mid)('%.10lg' % mid)
    return i


def func_00558614fe23412fa82bb33fc8ab1814(cuts, infile, slice_area):

    def get_y(P, qx):
        i = 0
        while i + 2 < len(P) and P[i + 1][0] <= qx:
            i += 1
        x0, y0 = P[i]
        x1, y1 = P[i + 1]
        return y0 + (y1 - y0) * (qx - x0) / float(x1 - x0)
    W, Ln, Un, G = map(int, infile.readline().split())
    L = []
    for each in range(Ln):
        L.append(map(int, infile.readline().split()))
    U = []
    for each in range(Un):
        U.append(map(int, infile.readline().split()))

    def get_area(left, right):
        xs = [x for x, y in L]
        xs += [x for x, y in U]
        xs += [left, right]
        xs = sorted(set(xs))
        xs = [x for x in xs if left - 1e-09 <= x <= right + 1e-09]
        res = 0
        for i in range(1, len(xs)):
            x0 = xs[i - 1]
            x1 = xs[i]
            side0 = abs(get_y(L, x0) - get_y(U, x0))
            side1 = abs(get_y(L, x1) - get_y(U, x1))
            h = abs(x0 - x1)
            res += (side0 + side1) * h / 2.0
        return res
    for i in range(50):
        mid = (low + high) / 2
        if get_area(cuts[-1], mid) > slice_area:
            high = mid
        else:
            low = mid
    cuts.append(mid)('%.10lg' % mid)
    return G


def func_d51d3964608d41f2b8410fadd23c7730(cuts, infile, slice_area):

    def get_y(P, qx):
        i = 0
        while i + 2 < len(P) and P[i + 1][0] <= qx:
            i += 1
        x0, y0 = P[i]
        x1, y1 = P[i + 1]
        return y0 + (y1 - y0) * (qx - x0) / float(x1 - x0)
    W, Ln, Un, G = map(int, infile.readline().split())
    L = []
    for each in range(Ln):
        L.append(map(int, infile.readline().split()))
    U = []
    for each in range(Un):
        U.append(map(int, infile.readline().split()))

    def get_area(left, right):
        xs = [x for x, y in L]
        xs += [x for x, y in U]
        xs += [left, right]
        xs = sorted(set(xs))
        xs = [x for x in xs if left - 1e-09 <= x <= right + 1e-09]
        res = 0
        for i in range(1, len(xs)):
            x0 = xs[i - 1]
            x1 = xs[i]
            side0 = abs(get_y(L, x0) - get_y(U, x0))
            side1 = abs(get_y(L, x1) - get_y(U, x1))
            h = abs(x0 - x1)
            res += (side0 + side1) * h / 2.0
        return res
    for i in range(50):
        mid = (low + high) / 2
        if get_area(cuts[-1], mid) > slice_area:
            high = mid
        else:
            low = mid
    cuts.append(mid)('%.10lg' % mid)
    return each


def func_bbb8e832b3484a72964727368345263e(cuts, infile, slice_area):

    def get_y(P, qx):
        i = 0
        while i + 2 < len(P) and P[i + 1][0] <= qx:
            i += 1
        x0, y0 = P[i]
        x1, y1 = P[i + 1]
        return y0 + (y1 - y0) * (qx - x0) / float(x1 - x0)
    W, Ln, Un, G = map(int, infile.readline().split())
    L = []
    for each in range(Ln):
        L.append(map(int, infile.readline().split()))
    U = []
    for each in range(Un):
        U.append(map(int, infile.readline().split()))

    def get_area(left, right):
        xs = [x for x, y in L]
        xs += [x for x, y in U]
        xs += [left, right]
        xs = sorted(set(xs))
        xs = [x for x in xs if left - 1e-09 <= x <= right + 1e-09]
        res = 0
        for i in range(1, len(xs)):
            x0 = xs[i - 1]
            x1 = xs[i]
            side0 = abs(get_y(L, x0) - get_y(U, x0))
            side1 = abs(get_y(L, x1) - get_y(U, x1))
            h = abs(x0 - x1)
            res += (side0 + side1) * h / 2.0
        return res
    for i in range(50):
        mid = (low + high) / 2
        if get_area(cuts[-1], mid) > slice_area:
            high = mid
        else:
            low = mid
    cuts.append(mid)('%.10lg' % mid)
    return mid


def func_061c8ebec70440ff97b17489d8972b38(cuts, infile, slice_area):

    def get_y(P, qx):
        i = 0
        while i + 2 < len(P) and P[i + 1][0] <= qx:
            i += 1
        x0, y0 = P[i]
        x1, y1 = P[i + 1]
        return y0 + (y1 - y0) * (qx - x0) / float(x1 - x0)
    W, Ln, Un, G = map(int, infile.readline().split())
    L = []
    for each in range(Ln):
        L.append(map(int, infile.readline().split()))
    U = []
    for each in range(Un):
        U.append(map(int, infile.readline().split()))

    def get_area(left, right):
        xs = [x for x, y in L]
        xs += [x for x, y in U]
        xs += [left, right]
        xs = sorted(set(xs))
        xs = [x for x in xs if left - 1e-09 <= x <= right + 1e-09]
        res = 0
        for i in range(1, len(xs)):
            x0 = xs[i - 1]
            x1 = xs[i]
            side0 = abs(get_y(L, x0) - get_y(U, x0))
            side1 = abs(get_y(L, x1) - get_y(U, x1))
            h = abs(x0 - x1)
            res += (side0 + side1) * h / 2.0
        return res
    for i in range(50):
        mid = (low + high) / 2
        if get_area(cuts[-1], mid) > slice_area:
            high = mid
        else:
            low = mid
    cuts.append(mid)('%.10lg' % mid)
    return L


def func_a33931e738264a359a98a005566b5769(cuts, infile, slice_area):

    def get_y(P, qx):
        i = 0
        while i + 2 < len(P) and P[i + 1][0] <= qx:
            i += 1
        x0, y0 = P[i]
        x1, y1 = P[i + 1]
        return y0 + (y1 - y0) * (qx - x0) / float(x1 - x0)
    W, Ln, Un, G = map(int, infile.readline().split())
    L = []
    for each in range(Ln):
        L.append(map(int, infile.readline().split()))
    U = []
    for each in range(Un):
        U.append(map(int, infile.readline().split()))

    def get_area(left, right):
        xs = [x for x, y in L]
        xs += [x for x, y in U]
        xs += [left, right]
        xs = sorted(set(xs))
        xs = [x for x in xs if left - 1e-09 <= x <= right + 1e-09]
        res = 0
        for i in range(1, len(xs)):
            x0 = xs[i - 1]
            x1 = xs[i]
            side0 = abs(get_y(L, x0) - get_y(U, x0))
            side1 = abs(get_y(L, x1) - get_y(U, x1))
            h = abs(x0 - x1)
            res += (side0 + side1) * h / 2.0
        return res
    for i in range(50):
        mid = (low + high) / 2
        if get_area(cuts[-1], mid) > slice_area:
            high = mid
        else:
            low = mid
    cuts.append(mid)('%.10lg' % mid)
    return Un


def func_e6eca78dc9844135a22551da0fc4eb7d(cuts, infile, slice_area):

    def get_y(P, qx):
        i = 0
        while i + 2 < len(P) and P[i + 1][0] <= qx:
            i += 1
        x0, y0 = P[i]
        x1, y1 = P[i + 1]
        return y0 + (y1 - y0) * (qx - x0) / float(x1 - x0)
    W, Ln, Un, G = map(int, infile.readline().split())
    L = []
    for each in range(Ln):
        L.append(map(int, infile.readline().split()))
    U = []
    for each in range(Un):
        U.append(map(int, infile.readline().split()))

    def get_area(left, right):
        xs = [x for x, y in L]
        xs += [x for x, y in U]
        xs += [left, right]
        xs = sorted(set(xs))
        xs = [x for x in xs if left - 1e-09 <= x <= right + 1e-09]
        res = 0
        for i in range(1, len(xs)):
            x0 = xs[i - 1]
            x1 = xs[i]
            side0 = abs(get_y(L, x0) - get_y(U, x0))
            side1 = abs(get_y(L, x1) - get_y(U, x1))
            h = abs(x0 - x1)
            res += (side0 + side1) * h / 2.0
        return res
    for i in range(50):
        mid = (low + high) / 2
        if get_area(cuts[-1], mid) > slice_area:
            high = mid
        else:
            low = mid
    cuts.append(mid)('%.10lg' % mid)
    return high


def func_228a79b11365423c9b4dd79520c49fed(cuts, infile, slice_area):

    def get_y(P, qx):
        i = 0
        while i + 2 < len(P) and P[i + 1][0] <= qx:
            i += 1
        x0, y0 = P[i]
        x1, y1 = P[i + 1]
        return y0 + (y1 - y0) * (qx - x0) / float(x1 - x0)
    W, Ln, Un, G = map(int, infile.readline().split())
    L = []
    for each in range(Ln):
        L.append(map(int, infile.readline().split()))
    U = []
    for each in range(Un):
        U.append(map(int, infile.readline().split()))

    def get_area(left, right):
        xs = [x for x, y in L]
        xs += [x for x, y in U]
        xs += [left, right]
        xs = sorted(set(xs))
        xs = [x for x in xs if left - 1e-09 <= x <= right + 1e-09]
        res = 0
        for i in range(1, len(xs)):
            x0 = xs[i - 1]
            x1 = xs[i]
            side0 = abs(get_y(L, x0) - get_y(U, x0))
            side1 = abs(get_y(L, x1) - get_y(U, x1))
            h = abs(x0 - x1)
            res += (side0 + side1) * h / 2.0
        return res
    for i in range(50):
        mid = (low + high) / 2
        if get_area(cuts[-1], mid) > slice_area:
            high = mid
        else:
            low = mid
    cuts.append(mid)('%.10lg' % mid)
    return U


def func_ec16c6f1ca2f4b08982f25ab7bbee0a7(cuts, infile, slice_area):

    def get_y(P, qx):
        i = 0
        while i + 2 < len(P) and P[i + 1][0] <= qx:
            i += 1
        x0, y0 = P[i]
        x1, y1 = P[i + 1]
        return y0 + (y1 - y0) * (qx - x0) / float(x1 - x0)
    W, Ln, Un, G = map(int, infile.readline().split())
    L = []
    for each in range(Ln):
        L.append(map(int, infile.readline().split()))
    U = []
    for each in range(Un):
        U.append(map(int, infile.readline().split()))

    def get_area(left, right):
        xs = [x for x, y in L]
        xs += [x for x, y in U]
        xs += [left, right]
        xs = sorted(set(xs))
        xs = [x for x in xs if left - 1e-09 <= x <= right + 1e-09]
        res = 0
        for i in range(1, len(xs)):
            x0 = xs[i - 1]
            x1 = xs[i]
            side0 = abs(get_y(L, x0) - get_y(U, x0))
            side1 = abs(get_y(L, x1) - get_y(U, x1))
            h = abs(x0 - x1)
            res += (side0 + side1) * h / 2.0
        return res
    low = cuts[-1]
    high = W
    for i in range(50):
        mid = (low + high) / 2
        if get_area(cuts[-1], mid) > slice_area:
            high = mid
        else:
            low = mid
    cuts.append(mid)
    return Un


def func_ac53711ec02141a39dcfacf06b9c077c(cuts, infile, slice_area):

    def get_y(P, qx):
        i = 0
        while i + 2 < len(P) and P[i + 1][0] <= qx:
            i += 1
        x0, y0 = P[i]
        x1, y1 = P[i + 1]
        return y0 + (y1 - y0) * (qx - x0) / float(x1 - x0)
    W, Ln, Un, G = map(int, infile.readline().split())
    L = []
    for each in range(Ln):
        L.append(map(int, infile.readline().split()))
    U = []
    for each in range(Un):
        U.append(map(int, infile.readline().split()))

    def get_area(left, right):
        xs = [x for x, y in L]
        xs += [x for x, y in U]
        xs += [left, right]
        xs = sorted(set(xs))
        xs = [x for x in xs if left - 1e-09 <= x <= right + 1e-09]
        res = 0
        for i in range(1, len(xs)):
            x0 = xs[i - 1]
            x1 = xs[i]
            side0 = abs(get_y(L, x0) - get_y(U, x0))
            side1 = abs(get_y(L, x1) - get_y(U, x1))
            h = abs(x0 - x1)
            res += (side0 + side1) * h / 2.0
        return res
    low = cuts[-1]
    high = W
    for i in range(50):
        mid = (low + high) / 2
        if get_area(cuts[-1], mid) > slice_area:
            high = mid
        else:
            low = mid
    cuts.append(mid)
    return cuts


def func_91b704127b22418292f0617efc0f5984(cuts, infile, slice_area):

    def get_y(P, qx):
        i = 0
        while i + 2 < len(P) and P[i + 1][0] <= qx:
            i += 1
        x0, y0 = P[i]
        x1, y1 = P[i + 1]
        return y0 + (y1 - y0) * (qx - x0) / float(x1 - x0)
    W, Ln, Un, G = map(int, infile.readline().split())
    L = []
    for each in range(Ln):
        L.append(map(int, infile.readline().split()))
    U = []
    for each in range(Un):
        U.append(map(int, infile.readline().split()))

    def get_area(left, right):
        xs = [x for x, y in L]
        xs += [x for x, y in U]
        xs += [left, right]
        xs = sorted(set(xs))
        xs = [x for x in xs if left - 1e-09 <= x <= right + 1e-09]
        res = 0
        for i in range(1, len(xs)):
            x0 = xs[i - 1]
            x1 = xs[i]
            side0 = abs(get_y(L, x0) - get_y(U, x0))
            side1 = abs(get_y(L, x1) - get_y(U, x1))
            h = abs(x0 - x1)
            res += (side0 + side1) * h / 2.0
        return res
    low = cuts[-1]
    high = W
    for i in range(50):
        mid = (low + high) / 2
        if get_area(cuts[-1], mid) > slice_area:
            high = mid
        else:
            low = mid
    cuts.append(mid)
    return G


def func_51ede20521d24ca4a7ec545e091eff3e(cuts, infile, slice_area):

    def get_y(P, qx):
        i = 0
        while i + 2 < len(P) and P[i + 1][0] <= qx:
            i += 1
        x0, y0 = P[i]
        x1, y1 = P[i + 1]
        return y0 + (y1 - y0) * (qx - x0) / float(x1 - x0)
    W, Ln, Un, G = map(int, infile.readline().split())
    L = []
    for each in range(Ln):
        L.append(map(int, infile.readline().split()))
    U = []
    for each in range(Un):
        U.append(map(int, infile.readline().split()))

    def get_area(left, right):
        xs = [x for x, y in L]
        xs += [x for x, y in U]
        xs += [left, right]
        xs = sorted(set(xs))
        xs = [x for x in xs if left - 1e-09 <= x <= right + 1e-09]
        res = 0
        for i in range(1, len(xs)):
            x0 = xs[i - 1]
            x1 = xs[i]
            side0 = abs(get_y(L, x0) - get_y(U, x0))
            side1 = abs(get_y(L, x1) - get_y(U, x1))
            h = abs(x0 - x1)
            res += (side0 + side1) * h / 2.0
        return res
    low = cuts[-1]
    high = W
    for i in range(50):
        mid = (low + high) / 2
        if get_area(cuts[-1], mid) > slice_area:
            high = mid
        else:
            low = mid
    cuts.append(mid)
    return mid


def func_0803adc2c8b049ae900dbf48f5fd6442(cuts, infile, slice_area):

    def get_y(P, qx):
        i = 0
        while i + 2 < len(P) and P[i + 1][0] <= qx:
            i += 1
        x0, y0 = P[i]
        x1, y1 = P[i + 1]
        return y0 + (y1 - y0) * (qx - x0) / float(x1 - x0)
    W, Ln, Un, G = map(int, infile.readline().split())
    L = []
    for each in range(Ln):
        L.append(map(int, infile.readline().split()))
    U = []
    for each in range(Un):
        U.append(map(int, infile.readline().split()))

    def get_area(left, right):
        xs = [x for x, y in L]
        xs += [x for x, y in U]
        xs += [left, right]
        xs = sorted(set(xs))
        xs = [x for x in xs if left - 1e-09 <= x <= right + 1e-09]
        res = 0
        for i in range(1, len(xs)):
            x0 = xs[i - 1]
            x1 = xs[i]
            side0 = abs(get_y(L, x0) - get_y(U, x0))
            side1 = abs(get_y(L, x1) - get_y(U, x1))
            h = abs(x0 - x1)
            res += (side0 + side1) * h / 2.0
        return res
    low = cuts[-1]
    high = W
    for i in range(50):
        mid = (low + high) / 2
        if get_area(cuts[-1], mid) > slice_area:
            high = mid
        else:
            low = mid
    cuts.append(mid)
    return W


def func_2eb3072b7542437dafe100a8ca116906(cuts, infile, slice_area):

    def get_y(P, qx):
        i = 0
        while i + 2 < len(P) and P[i + 1][0] <= qx:
            i += 1
        x0, y0 = P[i]
        x1, y1 = P[i + 1]
        return y0 + (y1 - y0) * (qx - x0) / float(x1 - x0)
    W, Ln, Un, G = map(int, infile.readline().split())
    L = []
    for each in range(Ln):
        L.append(map(int, infile.readline().split()))
    U = []
    for each in range(Un):
        U.append(map(int, infile.readline().split()))

    def get_area(left, right):
        xs = [x for x, y in L]
        xs += [x for x, y in U]
        xs += [left, right]
        xs = sorted(set(xs))
        xs = [x for x in xs if left - 1e-09 <= x <= right + 1e-09]
        res = 0
        for i in range(1, len(xs)):
            x0 = xs[i - 1]
            x1 = xs[i]
            side0 = abs(get_y(L, x0) - get_y(U, x0))
            side1 = abs(get_y(L, x1) - get_y(U, x1))
            h = abs(x0 - x1)
            res += (side0 + side1) * h / 2.0
        return res
    low = cuts[-1]
    high = W
    for i in range(50):
        mid = (low + high) / 2
        if get_area(cuts[-1], mid) > slice_area:
            high = mid
        else:
            low = mid
    cuts.append(mid)
    return L


def func_69b4cb94329941b3869b5f9472085071(cuts, infile, slice_area):

    def get_y(P, qx):
        i = 0
        while i + 2 < len(P) and P[i + 1][0] <= qx:
            i += 1
        x0, y0 = P[i]
        x1, y1 = P[i + 1]
        return y0 + (y1 - y0) * (qx - x0) / float(x1 - x0)
    W, Ln, Un, G = map(int, infile.readline().split())
    L = []
    for each in range(Ln):
        L.append(map(int, infile.readline().split()))
    U = []
    for each in range(Un):
        U.append(map(int, infile.readline().split()))

    def get_area(left, right):
        xs = [x for x, y in L]
        xs += [x for x, y in U]
        xs += [left, right]
        xs = sorted(set(xs))
        xs = [x for x in xs if left - 1e-09 <= x <= right + 1e-09]
        res = 0
        for i in range(1, len(xs)):
            x0 = xs[i - 1]
            x1 = xs[i]
            side0 = abs(get_y(L, x0) - get_y(U, x0))
            side1 = abs(get_y(L, x1) - get_y(U, x1))
            h = abs(x0 - x1)
            res += (side0 + side1) * h / 2.0
        return res
    low = cuts[-1]
    high = W
    for i in range(50):
        mid = (low + high) / 2
        if get_area(cuts[-1], mid) > slice_area:
            high = mid
        else:
            low = mid
    cuts.append(mid)
    return each


def func_974661ea29be49339a8b6c1ca92f131d(cuts, infile, slice_area):

    def get_y(P, qx):
        i = 0
        while i + 2 < len(P) and P[i + 1][0] <= qx:
            i += 1
        x0, y0 = P[i]
        x1, y1 = P[i + 1]
        return y0 + (y1 - y0) * (qx - x0) / float(x1 - x0)
    W, Ln, Un, G = map(int, infile.readline().split())
    L = []
    for each in range(Ln):
        L.append(map(int, infile.readline().split()))
    U = []
    for each in range(Un):
        U.append(map(int, infile.readline().split()))

    def get_area(left, right):
        xs = [x for x, y in L]
        xs += [x for x, y in U]
        xs += [left, right]
        xs = sorted(set(xs))
        xs = [x for x in xs if left - 1e-09 <= x <= right + 1e-09]
        res = 0
        for i in range(1, len(xs)):
            x0 = xs[i - 1]
            x1 = xs[i]
            side0 = abs(get_y(L, x0) - get_y(U, x0))
            side1 = abs(get_y(L, x1) - get_y(U, x1))
            h = abs(x0 - x1)
            res += (side0 + side1) * h / 2.0
        return res
    low = cuts[-1]
    high = W
    for i in range(50):
        mid = (low + high) / 2
        if get_area(cuts[-1], mid) > slice_area:
            high = mid
        else:
            low = mid
    cuts.append(mid)
    return slice_area


def func_955bb01b8e4741d6ac52a83ecc0c9eb7(cuts, infile, slice_area):

    def get_y(P, qx):
        i = 0
        while i + 2 < len(P) and P[i + 1][0] <= qx:
            i += 1
        x0, y0 = P[i]
        x1, y1 = P[i + 1]
        return y0 + (y1 - y0) * (qx - x0) / float(x1 - x0)
    W, Ln, Un, G = map(int, infile.readline().split())
    L = []
    for each in range(Ln):
        L.append(map(int, infile.readline().split()))
    U = []
    for each in range(Un):
        U.append(map(int, infile.readline().split()))

    def get_area(left, right):
        xs = [x for x, y in L]
        xs += [x for x, y in U]
        xs += [left, right]
        xs = sorted(set(xs))
        xs = [x for x in xs if left - 1e-09 <= x <= right + 1e-09]
        res = 0
        for i in range(1, len(xs)):
            x0 = xs[i - 1]
            x1 = xs[i]
            side0 = abs(get_y(L, x0) - get_y(U, x0))
            side1 = abs(get_y(L, x1) - get_y(U, x1))
            h = abs(x0 - x1)
            res += (side0 + side1) * h / 2.0
        return res
    low = cuts[-1]
    high = W
    for i in range(50):
        mid = (low + high) / 2
        if get_area(cuts[-1], mid) > slice_area:
            high = mid
        else:
            low = mid
    cuts.append(mid)
    return i


def func_aa056a39ddc64409897d31ce1ffb268c(cuts, infile, slice_area):

    def get_y(P, qx):
        i = 0
        while i + 2 < len(P) and P[i + 1][0] <= qx:
            i += 1
        x0, y0 = P[i]
        x1, y1 = P[i + 1]
        return y0 + (y1 - y0) * (qx - x0) / float(x1 - x0)
    W, Ln, Un, G = map(int, infile.readline().split())
    L = []
    for each in range(Ln):
        L.append(map(int, infile.readline().split()))
    U = []
    for each in range(Un):
        U.append(map(int, infile.readline().split()))

    def get_area(left, right):
        xs = [x for x, y in L]
        xs += [x for x, y in U]
        xs += [left, right]
        xs = sorted(set(xs))
        xs = [x for x in xs if left - 1e-09 <= x <= right + 1e-09]
        res = 0
        for i in range(1, len(xs)):
            x0 = xs[i - 1]
            x1 = xs[i]
            side0 = abs(get_y(L, x0) - get_y(U, x0))
            side1 = abs(get_y(L, x1) - get_y(U, x1))
            h = abs(x0 - x1)
            res += (side0 + side1) * h / 2.0
        return res
    low = cuts[-1]
    high = W
    for i in range(50):
        mid = (low + high) / 2
        if get_area(cuts[-1], mid) > slice_area:
            high = mid
        else:
            low = mid
    cuts.append(mid)
    return high


def func_5a75fb20677348b5ba2ad7cbd03ff24e(cuts, infile, slice_area):

    def get_y(P, qx):
        i = 0
        while i + 2 < len(P) and P[i + 1][0] <= qx:
            i += 1
        x0, y0 = P[i]
        x1, y1 = P[i + 1]
        return y0 + (y1 - y0) * (qx - x0) / float(x1 - x0)
    W, Ln, Un, G = map(int, infile.readline().split())
    L = []
    for each in range(Ln):
        L.append(map(int, infile.readline().split()))
    U = []
    for each in range(Un):
        U.append(map(int, infile.readline().split()))

    def get_area(left, right):
        xs = [x for x, y in L]
        xs += [x for x, y in U]
        xs += [left, right]
        xs = sorted(set(xs))
        xs = [x for x in xs if left - 1e-09 <= x <= right + 1e-09]
        res = 0
        for i in range(1, len(xs)):
            x0 = xs[i - 1]
            x1 = xs[i]
            side0 = abs(get_y(L, x0) - get_y(U, x0))
            side1 = abs(get_y(L, x1) - get_y(U, x1))
            h = abs(x0 - x1)
            res += (side0 + side1) * h / 2.0
        return res
    low = cuts[-1]
    high = W
    for i in range(50):
        mid = (low + high) / 2
        if get_area(cuts[-1], mid) > slice_area:
            high = mid
        else:
            low = mid
    cuts.append(mid)
    return low


def func_45fb5469f5b14b26bf3d5fdfb48a598f(cuts, infile, slice_area):

    def get_y(P, qx):
        i = 0
        while i + 2 < len(P) and P[i + 1][0] <= qx:
            i += 1
        x0, y0 = P[i]
        x1, y1 = P[i + 1]
        return y0 + (y1 - y0) * (qx - x0) / float(x1 - x0)
    W, Ln, Un, G = map(int, infile.readline().split())
    L = []
    for each in range(Ln):
        L.append(map(int, infile.readline().split()))
    U = []
    for each in range(Un):
        U.append(map(int, infile.readline().split()))

    def get_area(left, right):
        xs = [x for x, y in L]
        xs += [x for x, y in U]
        xs += [left, right]
        xs = sorted(set(xs))
        xs = [x for x in xs if left - 1e-09 <= x <= right + 1e-09]
        res = 0
        for i in range(1, len(xs)):
            x0 = xs[i - 1]
            x1 = xs[i]
            side0 = abs(get_y(L, x0) - get_y(U, x0))
            side1 = abs(get_y(L, x1) - get_y(U, x1))
            h = abs(x0 - x1)
            res += (side0 + side1) * h / 2.0
        return res
    low = cuts[-1]
    high = W
    for i in range(50):
        mid = (low + high) / 2
        if get_area(cuts[-1], mid) > slice_area:
            high = mid
        else:
            low = mid
    cuts.append(mid)
    return U


def func_de081d60be7e4ae7b7d650ffe876ed7b(cuts, infile, slice_area):

    def get_y(P, qx):
        i = 0
        while i + 2 < len(P) and P[i + 1][0] <= qx:
            i += 1
        x0, y0 = P[i]
        x1, y1 = P[i + 1]
        return y0 + (y1 - y0) * (qx - x0) / float(x1 - x0)
    W, Ln, Un, G = map(int, infile.readline().split())
    L = []
    for each in range(Ln):
        L.append(map(int, infile.readline().split()))
    U = []
    for each in range(Un):
        U.append(map(int, infile.readline().split()))

    def get_area(left, right):
        xs = [x for x, y in L]
        xs += [x for x, y in U]
        xs += [left, right]
        xs = sorted(set(xs))
        xs = [x for x in xs if left - 1e-09 <= x <= right + 1e-09]
        res = 0
        for i in range(1, len(xs)):
            x0 = xs[i - 1]
            x1 = xs[i]
            side0 = abs(get_y(L, x0) - get_y(U, x0))
            side1 = abs(get_y(L, x1) - get_y(U, x1))
            h = abs(x0 - x1)
            res += (side0 + side1) * h / 2.0
        return res
    low = cuts[-1]
    high = W
    for i in range(50):
        mid = (low + high) / 2
        if get_area(cuts[-1], mid) > slice_area:
            high = mid
        else:
            low = mid
    cuts.append(mid)
    return Ln


def func_ff00d5a126c0450591fbcbdbcaab09ea(cuts, infile, slice_area):

    def get_y(P, qx):
        i = 0
        while i + 2 < len(P) and P[i + 1][0] <= qx:
            i += 1
        x0, y0 = P[i]
        x1, y1 = P[i + 1]
        return y0 + (y1 - y0) * (qx - x0) / float(x1 - x0)
    W, Ln, Un, G = map(int, infile.readline().split())
    L = []
    for each in range(Ln):
        L.append(map(int, infile.readline().split()))
    U = []
    for each in range(Un):
        U.append(map(int, infile.readline().split()))

    def get_area(left, right):
        xs = [x for x, y in L]
        xs += [x for x, y in U]
        xs += [left, right]
        xs = sorted(set(xs))
        xs = [x for x in xs if left - 1e-09 <= x <= right + 1e-09]
        res = 0
        for i in range(1, len(xs)):
            x0 = xs[i - 1]
            x1 = xs[i]
            side0 = abs(get_y(L, x0) - get_y(U, x0))
            side1 = abs(get_y(L, x1) - get_y(U, x1))
            h = abs(x0 - x1)
            res += (side0 + side1) * h / 2.0
        return res
    high = W
    for i in range(50):
        mid = (low + high) / 2
        if get_area(cuts[-1], mid) > slice_area:
            high = mid
        else:
            low = mid
    cuts.append(mid)('%.10lg' % mid)
    return G


def func_6196171912774d518d9021e0ebf07bbe(cuts, infile, slice_area):

    def get_y(P, qx):
        i = 0
        while i + 2 < len(P) and P[i + 1][0] <= qx:
            i += 1
        x0, y0 = P[i]
        x1, y1 = P[i + 1]
        return y0 + (y1 - y0) * (qx - x0) / float(x1 - x0)
    W, Ln, Un, G = map(int, infile.readline().split())
    L = []
    for each in range(Ln):
        L.append(map(int, infile.readline().split()))
    U = []
    for each in range(Un):
        U.append(map(int, infile.readline().split()))

    def get_area(left, right):
        xs = [x for x, y in L]
        xs += [x for x, y in U]
        xs += [left, right]
        xs = sorted(set(xs))
        xs = [x for x in xs if left - 1e-09 <= x <= right + 1e-09]
        res = 0
        for i in range(1, len(xs)):
            x0 = xs[i - 1]
            x1 = xs[i]
            side0 = abs(get_y(L, x0) - get_y(U, x0))
            side1 = abs(get_y(L, x1) - get_y(U, x1))
            h = abs(x0 - x1)
            res += (side0 + side1) * h / 2.0
        return res
    high = W
    for i in range(50):
        mid = (low + high) / 2
        if get_area(cuts[-1], mid) > slice_area:
            high = mid
        else:
            low = mid
    cuts.append(mid)('%.10lg' % mid)
    return each


def func_afd3de3c64484583be9ab0b9008a9c87(cuts, infile, slice_area):

    def get_y(P, qx):
        i = 0
        while i + 2 < len(P) and P[i + 1][0] <= qx:
            i += 1
        x0, y0 = P[i]
        x1, y1 = P[i + 1]
        return y0 + (y1 - y0) * (qx - x0) / float(x1 - x0)
    W, Ln, Un, G = map(int, infile.readline().split())
    L = []
    for each in range(Ln):
        L.append(map(int, infile.readline().split()))
    U = []
    for each in range(Un):
        U.append(map(int, infile.readline().split()))

    def get_area(left, right):
        xs = [x for x, y in L]
        xs += [x for x, y in U]
        xs += [left, right]
        xs = sorted(set(xs))
        xs = [x for x in xs if left - 1e-09 <= x <= right + 1e-09]
        res = 0
        for i in range(1, len(xs)):
            x0 = xs[i - 1]
            x1 = xs[i]
            side0 = abs(get_y(L, x0) - get_y(U, x0))
            side1 = abs(get_y(L, x1) - get_y(U, x1))
            h = abs(x0 - x1)
            res += (side0 + side1) * h / 2.0
        return res
    high = W
    for i in range(50):
        mid = (low + high) / 2
        if get_area(cuts[-1], mid) > slice_area:
            high = mid
        else:
            low = mid
    cuts.append(mid)('%.10lg' % mid)
    return cuts


def func_e40cf48145b145aaa42ffd6b318ba1ec(cuts, infile, slice_area):

    def get_y(P, qx):
        i = 0
        while i + 2 < len(P) and P[i + 1][0] <= qx:
            i += 1
        x0, y0 = P[i]
        x1, y1 = P[i + 1]
        return y0 + (y1 - y0) * (qx - x0) / float(x1 - x0)
    W, Ln, Un, G = map(int, infile.readline().split())
    L = []
    for each in range(Ln):
        L.append(map(int, infile.readline().split()))
    U = []
    for each in range(Un):
        U.append(map(int, infile.readline().split()))

    def get_area(left, right):
        xs = [x for x, y in L]
        xs += [x for x, y in U]
        xs += [left, right]
        xs = sorted(set(xs))
        xs = [x for x in xs if left - 1e-09 <= x <= right + 1e-09]
        res = 0
        for i in range(1, len(xs)):
            x0 = xs[i - 1]
            x1 = xs[i]
            side0 = abs(get_y(L, x0) - get_y(U, x0))
            side1 = abs(get_y(L, x1) - get_y(U, x1))
            h = abs(x0 - x1)
            res += (side0 + side1) * h / 2.0
        return res
    high = W
    for i in range(50):
        mid = (low + high) / 2
        if get_area(cuts[-1], mid) > slice_area:
            high = mid
        else:
            low = mid
    cuts.append(mid)('%.10lg' % mid)
    return i


def func_8a507a8d5d0c4a10a569c32a236dc82a(cuts, infile, slice_area):

    def get_y(P, qx):
        i = 0
        while i + 2 < len(P) and P[i + 1][0] <= qx:
            i += 1
        x0, y0 = P[i]
        x1, y1 = P[i + 1]
        return y0 + (y1 - y0) * (qx - x0) / float(x1 - x0)
    W, Ln, Un, G = map(int, infile.readline().split())
    L = []
    for each in range(Ln):
        L.append(map(int, infile.readline().split()))
    U = []
    for each in range(Un):
        U.append(map(int, infile.readline().split()))

    def get_area(left, right):
        xs = [x for x, y in L]
        xs += [x for x, y in U]
        xs += [left, right]
        xs = sorted(set(xs))
        xs = [x for x in xs if left - 1e-09 <= x <= right + 1e-09]
        res = 0
        for i in range(1, len(xs)):
            x0 = xs[i - 1]
            x1 = xs[i]
            side0 = abs(get_y(L, x0) - get_y(U, x0))
            side1 = abs(get_y(L, x1) - get_y(U, x1))
            h = abs(x0 - x1)
            res += (side0 + side1) * h / 2.0
        return res
    high = W
    for i in range(50):
        mid = (low + high) / 2
        if get_area(cuts[-1], mid) > slice_area:
            high = mid
        else:
            low = mid
    cuts.append(mid)('%.10lg' % mid)
    return mid


def func_5090163822c4499b808678172a13b93a(cuts, infile, slice_area):

    def get_y(P, qx):
        i = 0
        while i + 2 < len(P) and P[i + 1][0] <= qx:
            i += 1
        x0, y0 = P[i]
        x1, y1 = P[i + 1]
        return y0 + (y1 - y0) * (qx - x0) / float(x1 - x0)
    W, Ln, Un, G = map(int, infile.readline().split())
    L = []
    for each in range(Ln):
        L.append(map(int, infile.readline().split()))
    U = []
    for each in range(Un):
        U.append(map(int, infile.readline().split()))

    def get_area(left, right):
        xs = [x for x, y in L]
        xs += [x for x, y in U]
        xs += [left, right]
        xs = sorted(set(xs))
        xs = [x for x in xs if left - 1e-09 <= x <= right + 1e-09]
        res = 0
        for i in range(1, len(xs)):
            x0 = xs[i - 1]
            x1 = xs[i]
            side0 = abs(get_y(L, x0) - get_y(U, x0))
            side1 = abs(get_y(L, x1) - get_y(U, x1))
            h = abs(x0 - x1)
            res += (side0 + side1) * h / 2.0
        return res
    high = W
    for i in range(50):
        mid = (low + high) / 2
        if get_area(cuts[-1], mid) > slice_area:
            high = mid
        else:
            low = mid
    cuts.append(mid)('%.10lg' % mid)
    return high


def func_a92a9e99518449f7ad513f186a4b523d(cuts, infile, slice_area):

    def get_y(P, qx):
        i = 0
        while i + 2 < len(P) and P[i + 1][0] <= qx:
            i += 1
        x0, y0 = P[i]
        x1, y1 = P[i + 1]
        return y0 + (y1 - y0) * (qx - x0) / float(x1 - x0)
    W, Ln, Un, G = map(int, infile.readline().split())
    L = []
    for each in range(Ln):
        L.append(map(int, infile.readline().split()))
    U = []
    for each in range(Un):
        U.append(map(int, infile.readline().split()))

    def get_area(left, right):
        xs = [x for x, y in L]
        xs += [x for x, y in U]
        xs += [left, right]
        xs = sorted(set(xs))
        xs = [x for x in xs if left - 1e-09 <= x <= right + 1e-09]
        res = 0
        for i in range(1, len(xs)):
            x0 = xs[i - 1]
            x1 = xs[i]
            side0 = abs(get_y(L, x0) - get_y(U, x0))
            side1 = abs(get_y(L, x1) - get_y(U, x1))
            h = abs(x0 - x1)
            res += (side0 + side1) * h / 2.0
        return res
    high = W
    for i in range(50):
        mid = (low + high) / 2
        if get_area(cuts[-1], mid) > slice_area:
            high = mid
        else:
            low = mid
    cuts.append(mid)('%.10lg' % mid)
    return Un


def func_fde57e1e9d70444aab20779b1208522e(cuts, infile, slice_area):

    def get_y(P, qx):
        i = 0
        while i + 2 < len(P) and P[i + 1][0] <= qx:
            i += 1
        x0, y0 = P[i]
        x1, y1 = P[i + 1]
        return y0 + (y1 - y0) * (qx - x0) / float(x1 - x0)
    W, Ln, Un, G = map(int, infile.readline().split())
    L = []
    for each in range(Ln):
        L.append(map(int, infile.readline().split()))
    U = []
    for each in range(Un):
        U.append(map(int, infile.readline().split()))

    def get_area(left, right):
        xs = [x for x, y in L]
        xs += [x for x, y in U]
        xs += [left, right]
        xs = sorted(set(xs))
        xs = [x for x in xs if left - 1e-09 <= x <= right + 1e-09]
        res = 0
        for i in range(1, len(xs)):
            x0 = xs[i - 1]
            x1 = xs[i]
            side0 = abs(get_y(L, x0) - get_y(U, x0))
            side1 = abs(get_y(L, x1) - get_y(U, x1))
            h = abs(x0 - x1)
            res += (side0 + side1) * h / 2.0
        return res
    high = W
    for i in range(50):
        mid = (low + high) / 2
        if get_area(cuts[-1], mid) > slice_area:
            high = mid
        else:
            low = mid
    cuts.append(mid)('%.10lg' % mid)
    return W


def func_b66385bd19e54e05ae5b37eebf9a91dd(cuts, infile, slice_area):

    def get_y(P, qx):
        i = 0
        while i + 2 < len(P) and P[i + 1][0] <= qx:
            i += 1
        x0, y0 = P[i]
        x1, y1 = P[i + 1]
        return y0 + (y1 - y0) * (qx - x0) / float(x1 - x0)
    W, Ln, Un, G = map(int, infile.readline().split())
    L = []
    for each in range(Ln):
        L.append(map(int, infile.readline().split()))
    U = []
    for each in range(Un):
        U.append(map(int, infile.readline().split()))

    def get_area(left, right):
        xs = [x for x, y in L]
        xs += [x for x, y in U]
        xs += [left, right]
        xs = sorted(set(xs))
        xs = [x for x in xs if left - 1e-09 <= x <= right + 1e-09]
        res = 0
        for i in range(1, len(xs)):
            x0 = xs[i - 1]
            x1 = xs[i]
            side0 = abs(get_y(L, x0) - get_y(U, x0))
            side1 = abs(get_y(L, x1) - get_y(U, x1))
            h = abs(x0 - x1)
            res += (side0 + side1) * h / 2.0
        return res
    high = W
    for i in range(50):
        mid = (low + high) / 2
        if get_area(cuts[-1], mid) > slice_area:
            high = mid
        else:
            low = mid
    cuts.append(mid)('%.10lg' % mid)
    return slice_area


def func_c3ab1a0d66bb4be09eba40647af2c822(cuts, infile, slice_area):

    def get_y(P, qx):
        i = 0
        while i + 2 < len(P) and P[i + 1][0] <= qx:
            i += 1
        x0, y0 = P[i]
        x1, y1 = P[i + 1]
        return y0 + (y1 - y0) * (qx - x0) / float(x1 - x0)
    W, Ln, Un, G = map(int, infile.readline().split())
    L = []
    for each in range(Ln):
        L.append(map(int, infile.readline().split()))
    U = []
    for each in range(Un):
        U.append(map(int, infile.readline().split()))

    def get_area(left, right):
        xs = [x for x, y in L]
        xs += [x for x, y in U]
        xs += [left, right]
        xs = sorted(set(xs))
        xs = [x for x in xs if left - 1e-09 <= x <= right + 1e-09]
        res = 0
        for i in range(1, len(xs)):
            x0 = xs[i - 1]
            x1 = xs[i]
            side0 = abs(get_y(L, x0) - get_y(U, x0))
            side1 = abs(get_y(L, x1) - get_y(U, x1))
            h = abs(x0 - x1)
            res += (side0 + side1) * h / 2.0
        return res
    high = W
    for i in range(50):
        mid = (low + high) / 2
        if get_area(cuts[-1], mid) > slice_area:
            high = mid
        else:
            low = mid
    cuts.append(mid)('%.10lg' % mid)
    return U


def func_a2f5fab05bb842568d5ac396ea038111(cuts, infile, slice_area):

    def get_y(P, qx):
        i = 0
        while i + 2 < len(P) and P[i + 1][0] <= qx:
            i += 1
        x0, y0 = P[i]
        x1, y1 = P[i + 1]
        return y0 + (y1 - y0) * (qx - x0) / float(x1 - x0)
    W, Ln, Un, G = map(int, infile.readline().split())
    L = []
    for each in range(Ln):
        L.append(map(int, infile.readline().split()))
    U = []
    for each in range(Un):
        U.append(map(int, infile.readline().split()))

    def get_area(left, right):
        xs = [x for x, y in L]
        xs += [x for x, y in U]
        xs += [left, right]
        xs = sorted(set(xs))
        xs = [x for x in xs if left - 1e-09 <= x <= right + 1e-09]
        res = 0
        for i in range(1, len(xs)):
            x0 = xs[i - 1]
            x1 = xs[i]
            side0 = abs(get_y(L, x0) - get_y(U, x0))
            side1 = abs(get_y(L, x1) - get_y(U, x1))
            h = abs(x0 - x1)
            res += (side0 + side1) * h / 2.0
        return res
    high = W
    for i in range(50):
        mid = (low + high) / 2
        if get_area(cuts[-1], mid) > slice_area:
            high = mid
        else:
            low = mid
    cuts.append(mid)('%.10lg' % mid)
    return L


def func_7ec7fbe0695343799f2b513cbea0d0d6(cuts, infile, slice_area):

    def get_y(P, qx):
        i = 0
        while i + 2 < len(P) and P[i + 1][0] <= qx:
            i += 1
        x0, y0 = P[i]
        x1, y1 = P[i + 1]
        return y0 + (y1 - y0) * (qx - x0) / float(x1 - x0)
    W, Ln, Un, G = map(int, infile.readline().split())
    L = []
    for each in range(Ln):
        L.append(map(int, infile.readline().split()))
    U = []
    for each in range(Un):
        U.append(map(int, infile.readline().split()))

    def get_area(left, right):
        xs = [x for x, y in L]
        xs += [x for x, y in U]
        xs += [left, right]
        xs = sorted(set(xs))
        xs = [x for x in xs if left - 1e-09 <= x <= right + 1e-09]
        res = 0
        for i in range(1, len(xs)):
            x0 = xs[i - 1]
            x1 = xs[i]
            side0 = abs(get_y(L, x0) - get_y(U, x0))
            side1 = abs(get_y(L, x1) - get_y(U, x1))
            h = abs(x0 - x1)
            res += (side0 + side1) * h / 2.0
        return res
    high = W
    for i in range(50):
        mid = (low + high) / 2
        if get_area(cuts[-1], mid) > slice_area:
            high = mid
        else:
            low = mid
    cuts.append(mid)('%.10lg' % mid)
    return Ln


def func_6ae411f1918c4261ac207876f732df2c(cuts, infile, slice_area):

    def get_y(P, qx):
        i = 0
        while i + 2 < len(P) and P[i + 1][0] <= qx:
            i += 1
        x0, y0 = P[i]
        x1, y1 = P[i + 1]
        return y0 + (y1 - y0) * (qx - x0) / float(x1 - x0)
    W, Ln, Un, G = map(int, infile.readline().split())
    L = []
    for each in range(Ln):
        L.append(map(int, infile.readline().split()))
    U = []
    for each in range(Un):
        U.append(map(int, infile.readline().split()))

    def get_area(left, right):
        xs = [x for x, y in L]
        xs += [x for x, y in U]
        xs += [left, right]
        xs = sorted(set(xs))
        xs = [x for x in xs if left - 1e-09 <= x <= right + 1e-09]
        res = 0
        for i in range(1, len(xs)):
            x0 = xs[i - 1]
            x1 = xs[i]
            side0 = abs(get_y(L, x0) - get_y(U, x0))
            side1 = abs(get_y(L, x1) - get_y(U, x1))
            h = abs(x0 - x1)
            res += (side0 + side1) * h / 2.0
        return res
    high = W
    for i in range(50):
        mid = (low + high) / 2
        if get_area(cuts[-1], mid) > slice_area:
            high = mid
        else:
            low = mid
    cuts.append(mid)('%.10lg' % mid)
    return low


def func_25bfda7a8e5f43efb2943d16c4f82415(cuts, infile, slice_area):

    def get_y(P, qx):
        i = 0
        while i + 2 < len(P) and P[i + 1][0] <= qx:
            i += 1
        x0, y0 = P[i]
        x1, y1 = P[i + 1]
        return y0 + (y1 - y0) * (qx - x0) / float(x1 - x0)
    W, Ln, Un, G = map(int, infile.readline().split())
    L = []
    for each in range(Ln):
        L.append(map(int, infile.readline().split()))
    U = []
    for each in range(Un):
        U.append(map(int, infile.readline().split()))

    def get_area(left, right):
        xs = [x for x, y in L]
        xs += [x for x, y in U]
        xs += [left, right]
        xs = sorted(set(xs))
        xs = [x for x in xs if left - 1e-09 <= x <= right + 1e-09]
        res = 0
        for i in range(1, len(xs)):
            x0 = xs[i - 1]
            x1 = xs[i]
            side0 = abs(get_y(L, x0) - get_y(U, x0))
            side1 = abs(get_y(L, x1) - get_y(U, x1))
            h = abs(x0 - x1)
            res += (side0 + side1) * h / 2.0
        return res
    low = cuts[-1]
    high = W
    for i in range(50):
        mid = (low + high) / 2
        if get_area(cuts[-1], mid) > slice_area:
            high = mid
        else:
            low = mid
    cuts.append(mid)('%.10lg' % mid)
    return G


def func_e3544f1008534860aec6eb0df48787d3(cuts, infile, slice_area):

    def get_y(P, qx):
        i = 0
        while i + 2 < len(P) and P[i + 1][0] <= qx:
            i += 1
        x0, y0 = P[i]
        x1, y1 = P[i + 1]
        return y0 + (y1 - y0) * (qx - x0) / float(x1 - x0)
    W, Ln, Un, G = map(int, infile.readline().split())
    L = []
    for each in range(Ln):
        L.append(map(int, infile.readline().split()))
    U = []
    for each in range(Un):
        U.append(map(int, infile.readline().split()))

    def get_area(left, right):
        xs = [x for x, y in L]
        xs += [x for x, y in U]
        xs += [left, right]
        xs = sorted(set(xs))
        xs = [x for x in xs if left - 1e-09 <= x <= right + 1e-09]
        res = 0
        for i in range(1, len(xs)):
            x0 = xs[i - 1]
            x1 = xs[i]
            side0 = abs(get_y(L, x0) - get_y(U, x0))
            side1 = abs(get_y(L, x1) - get_y(U, x1))
            h = abs(x0 - x1)
            res += (side0 + side1) * h / 2.0
        return res
    low = cuts[-1]
    high = W
    for i in range(50):
        mid = (low + high) / 2
        if get_area(cuts[-1], mid) > slice_area:
            high = mid
        else:
            low = mid
    cuts.append(mid)('%.10lg' % mid)
    return L


def func_bef4b46607f2442fa1c32b681dea7c7f(cuts, infile, slice_area):

    def get_y(P, qx):
        i = 0
        while i + 2 < len(P) and P[i + 1][0] <= qx:
            i += 1
        x0, y0 = P[i]
        x1, y1 = P[i + 1]
        return y0 + (y1 - y0) * (qx - x0) / float(x1 - x0)
    W, Ln, Un, G = map(int, infile.readline().split())
    L = []
    for each in range(Ln):
        L.append(map(int, infile.readline().split()))
    U = []
    for each in range(Un):
        U.append(map(int, infile.readline().split()))

    def get_area(left, right):
        xs = [x for x, y in L]
        xs += [x for x, y in U]
        xs += [left, right]
        xs = sorted(set(xs))
        xs = [x for x in xs if left - 1e-09 <= x <= right + 1e-09]
        res = 0
        for i in range(1, len(xs)):
            x0 = xs[i - 1]
            x1 = xs[i]
            side0 = abs(get_y(L, x0) - get_y(U, x0))
            side1 = abs(get_y(L, x1) - get_y(U, x1))
            h = abs(x0 - x1)
            res += (side0 + side1) * h / 2.0
        return res
    low = cuts[-1]
    high = W
    for i in range(50):
        mid = (low + high) / 2
        if get_area(cuts[-1], mid) > slice_area:
            high = mid
        else:
            low = mid
    cuts.append(mid)('%.10lg' % mid)
    return mid


def func_ab506ccab5654bd0b6eb7cfe665439a5(cuts, infile, slice_area):

    def get_y(P, qx):
        i = 0
        while i + 2 < len(P) and P[i + 1][0] <= qx:
            i += 1
        x0, y0 = P[i]
        x1, y1 = P[i + 1]
        return y0 + (y1 - y0) * (qx - x0) / float(x1 - x0)
    W, Ln, Un, G = map(int, infile.readline().split())
    L = []
    for each in range(Ln):
        L.append(map(int, infile.readline().split()))
    U = []
    for each in range(Un):
        U.append(map(int, infile.readline().split()))

    def get_area(left, right):
        xs = [x for x, y in L]
        xs += [x for x, y in U]
        xs += [left, right]
        xs = sorted(set(xs))
        xs = [x for x in xs if left - 1e-09 <= x <= right + 1e-09]
        res = 0
        for i in range(1, len(xs)):
            x0 = xs[i - 1]
            x1 = xs[i]
            side0 = abs(get_y(L, x0) - get_y(U, x0))
            side1 = abs(get_y(L, x1) - get_y(U, x1))
            h = abs(x0 - x1)
            res += (side0 + side1) * h / 2.0
        return res
    low = cuts[-1]
    high = W
    for i in range(50):
        mid = (low + high) / 2
        if get_area(cuts[-1], mid) > slice_area:
            high = mid
        else:
            low = mid
    cuts.append(mid)('%.10lg' % mid)
    return each


def func_1c651770a1a948498f3d2cfa2a938d4c(cuts, infile, slice_area):

    def get_y(P, qx):
        i = 0
        while i + 2 < len(P) and P[i + 1][0] <= qx:
            i += 1
        x0, y0 = P[i]
        x1, y1 = P[i + 1]
        return y0 + (y1 - y0) * (qx - x0) / float(x1 - x0)
    W, Ln, Un, G = map(int, infile.readline().split())
    L = []
    for each in range(Ln):
        L.append(map(int, infile.readline().split()))
    U = []
    for each in range(Un):
        U.append(map(int, infile.readline().split()))

    def get_area(left, right):
        xs = [x for x, y in L]
        xs += [x for x, y in U]
        xs += [left, right]
        xs = sorted(set(xs))
        xs = [x for x in xs if left - 1e-09 <= x <= right + 1e-09]
        res = 0
        for i in range(1, len(xs)):
            x0 = xs[i - 1]
            x1 = xs[i]
            side0 = abs(get_y(L, x0) - get_y(U, x0))
            side1 = abs(get_y(L, x1) - get_y(U, x1))
            h = abs(x0 - x1)
            res += (side0 + side1) * h / 2.0
        return res
    low = cuts[-1]
    high = W
    for i in range(50):
        mid = (low + high) / 2
        if get_area(cuts[-1], mid) > slice_area:
            high = mid
        else:
            low = mid
    cuts.append(mid)('%.10lg' % mid)
    return low


def func_a6683dfab91348d59635f643a7a41ab0(cuts, infile, slice_area):

    def get_y(P, qx):
        i = 0
        while i + 2 < len(P) and P[i + 1][0] <= qx:
            i += 1
        x0, y0 = P[i]
        x1, y1 = P[i + 1]
        return y0 + (y1 - y0) * (qx - x0) / float(x1 - x0)
    W, Ln, Un, G = map(int, infile.readline().split())
    L = []
    for each in range(Ln):
        L.append(map(int, infile.readline().split()))
    U = []
    for each in range(Un):
        U.append(map(int, infile.readline().split()))

    def get_area(left, right):
        xs = [x for x, y in L]
        xs += [x for x, y in U]
        xs += [left, right]
        xs = sorted(set(xs))
        xs = [x for x in xs if left - 1e-09 <= x <= right + 1e-09]
        res = 0
        for i in range(1, len(xs)):
            x0 = xs[i - 1]
            x1 = xs[i]
            side0 = abs(get_y(L, x0) - get_y(U, x0))
            side1 = abs(get_y(L, x1) - get_y(U, x1))
            h = abs(x0 - x1)
            res += (side0 + side1) * h / 2.0
        return res
    low = cuts[-1]
    high = W
    for i in range(50):
        mid = (low + high) / 2
        if get_area(cuts[-1], mid) > slice_area:
            high = mid
        else:
            low = mid
    cuts.append(mid)('%.10lg' % mid)
    return cuts


def func_1648d7e99c664d0c916840d62257ad4d(cuts, infile, slice_area):

    def get_y(P, qx):
        i = 0
        while i + 2 < len(P) and P[i + 1][0] <= qx:
            i += 1
        x0, y0 = P[i]
        x1, y1 = P[i + 1]
        return y0 + (y1 - y0) * (qx - x0) / float(x1 - x0)
    W, Ln, Un, G = map(int, infile.readline().split())
    L = []
    for each in range(Ln):
        L.append(map(int, infile.readline().split()))
    U = []
    for each in range(Un):
        U.append(map(int, infile.readline().split()))

    def get_area(left, right):
        xs = [x for x, y in L]
        xs += [x for x, y in U]
        xs += [left, right]
        xs = sorted(set(xs))
        xs = [x for x in xs if left - 1e-09 <= x <= right + 1e-09]
        res = 0
        for i in range(1, len(xs)):
            x0 = xs[i - 1]
            x1 = xs[i]
            side0 = abs(get_y(L, x0) - get_y(U, x0))
            side1 = abs(get_y(L, x1) - get_y(U, x1))
            h = abs(x0 - x1)
            res += (side0 + side1) * h / 2.0
        return res
    low = cuts[-1]
    high = W
    for i in range(50):
        mid = (low + high) / 2
        if get_area(cuts[-1], mid) > slice_area:
            high = mid
        else:
            low = mid
    cuts.append(mid)('%.10lg' % mid)
    return high


def func_dc76bffda5f64e97a37f2f866b784e11(cuts, infile, slice_area):

    def get_y(P, qx):
        i = 0
        while i + 2 < len(P) and P[i + 1][0] <= qx:
            i += 1
        x0, y0 = P[i]
        x1, y1 = P[i + 1]
        return y0 + (y1 - y0) * (qx - x0) / float(x1 - x0)
    W, Ln, Un, G = map(int, infile.readline().split())
    L = []
    for each in range(Ln):
        L.append(map(int, infile.readline().split()))
    U = []
    for each in range(Un):
        U.append(map(int, infile.readline().split()))

    def get_area(left, right):
        xs = [x for x, y in L]
        xs += [x for x, y in U]
        xs += [left, right]
        xs = sorted(set(xs))
        xs = [x for x in xs if left - 1e-09 <= x <= right + 1e-09]
        res = 0
        for i in range(1, len(xs)):
            x0 = xs[i - 1]
            x1 = xs[i]
            side0 = abs(get_y(L, x0) - get_y(U, x0))
            side1 = abs(get_y(L, x1) - get_y(U, x1))
            h = abs(x0 - x1)
            res += (side0 + side1) * h / 2.0
        return res
    low = cuts[-1]
    high = W
    for i in range(50):
        mid = (low + high) / 2
        if get_area(cuts[-1], mid) > slice_area:
            high = mid
        else:
            low = mid
    cuts.append(mid)('%.10lg' % mid)
    return U


def func_2bcee1e8ca904080bed0e73075e2f2c2(cuts, infile, slice_area):

    def get_y(P, qx):
        i = 0
        while i + 2 < len(P) and P[i + 1][0] <= qx:
            i += 1
        x0, y0 = P[i]
        x1, y1 = P[i + 1]
        return y0 + (y1 - y0) * (qx - x0) / float(x1 - x0)
    W, Ln, Un, G = map(int, infile.readline().split())
    L = []
    for each in range(Ln):
        L.append(map(int, infile.readline().split()))
    U = []
    for each in range(Un):
        U.append(map(int, infile.readline().split()))

    def get_area(left, right):
        xs = [x for x, y in L]
        xs += [x for x, y in U]
        xs += [left, right]
        xs = sorted(set(xs))
        xs = [x for x in xs if left - 1e-09 <= x <= right + 1e-09]
        res = 0
        for i in range(1, len(xs)):
            x0 = xs[i - 1]
            x1 = xs[i]
            side0 = abs(get_y(L, x0) - get_y(U, x0))
            side1 = abs(get_y(L, x1) - get_y(U, x1))
            h = abs(x0 - x1)
            res += (side0 + side1) * h / 2.0
        return res
    low = cuts[-1]
    high = W
    for i in range(50):
        mid = (low + high) / 2
        if get_area(cuts[-1], mid) > slice_area:
            high = mid
        else:
            low = mid
    cuts.append(mid)('%.10lg' % mid)
    return i


def func_414fa84306604048a405966f006e0b59(cuts, infile, slice_area):

    def get_y(P, qx):
        i = 0
        while i + 2 < len(P) and P[i + 1][0] <= qx:
            i += 1
        x0, y0 = P[i]
        x1, y1 = P[i + 1]
        return y0 + (y1 - y0) * (qx - x0) / float(x1 - x0)
    W, Ln, Un, G = map(int, infile.readline().split())
    L = []
    for each in range(Ln):
        L.append(map(int, infile.readline().split()))
    U = []
    for each in range(Un):
        U.append(map(int, infile.readline().split()))

    def get_area(left, right):
        xs = [x for x, y in L]
        xs += [x for x, y in U]
        xs += [left, right]
        xs = sorted(set(xs))
        xs = [x for x in xs if left - 1e-09 <= x <= right + 1e-09]
        res = 0
        for i in range(1, len(xs)):
            x0 = xs[i - 1]
            x1 = xs[i]
            side0 = abs(get_y(L, x0) - get_y(U, x0))
            side1 = abs(get_y(L, x1) - get_y(U, x1))
            h = abs(x0 - x1)
            res += (side0 + side1) * h / 2.0
        return res
    low = cuts[-1]
    high = W
    for i in range(50):
        mid = (low + high) / 2
        if get_area(cuts[-1], mid) > slice_area:
            high = mid
        else:
            low = mid
    cuts.append(mid)('%.10lg' % mid)
    return slice_area


def func_56bfe9e6f6ca4cd8ac39c3af8f4c2532(cuts, infile, slice_area):

    def get_y(P, qx):
        i = 0
        while i + 2 < len(P) and P[i + 1][0] <= qx:
            i += 1
        x0, y0 = P[i]
        x1, y1 = P[i + 1]
        return y0 + (y1 - y0) * (qx - x0) / float(x1 - x0)
    W, Ln, Un, G = map(int, infile.readline().split())
    L = []
    for each in range(Ln):
        L.append(map(int, infile.readline().split()))
    U = []
    for each in range(Un):
        U.append(map(int, infile.readline().split()))

    def get_area(left, right):
        xs = [x for x, y in L]
        xs += [x for x, y in U]
        xs += [left, right]
        xs = sorted(set(xs))
        xs = [x for x in xs if left - 1e-09 <= x <= right + 1e-09]
        res = 0
        for i in range(1, len(xs)):
            x0 = xs[i - 1]
            x1 = xs[i]
            side0 = abs(get_y(L, x0) - get_y(U, x0))
            side1 = abs(get_y(L, x1) - get_y(U, x1))
            h = abs(x0 - x1)
            res += (side0 + side1) * h / 2.0
        return res
    low = cuts[-1]
    high = W
    for i in range(50):
        mid = (low + high) / 2
        if get_area(cuts[-1], mid) > slice_area:
            high = mid
        else:
            low = mid
    cuts.append(mid)('%.10lg' % mid)
    return W


def func_3db8015be1d54b608d740690fd319485(cuts, infile, slice_area):

    def get_y(P, qx):
        i = 0
        while i + 2 < len(P) and P[i + 1][0] <= qx:
            i += 1
        x0, y0 = P[i]
        x1, y1 = P[i + 1]
        return y0 + (y1 - y0) * (qx - x0) / float(x1 - x0)
    W, Ln, Un, G = map(int, infile.readline().split())
    L = []
    for each in range(Ln):
        L.append(map(int, infile.readline().split()))
    U = []
    for each in range(Un):
        U.append(map(int, infile.readline().split()))

    def get_area(left, right):
        xs = [x for x, y in L]
        xs += [x for x, y in U]
        xs += [left, right]
        xs = sorted(set(xs))
        xs = [x for x in xs if left - 1e-09 <= x <= right + 1e-09]
        res = 0
        for i in range(1, len(xs)):
            x0 = xs[i - 1]
            x1 = xs[i]
            side0 = abs(get_y(L, x0) - get_y(U, x0))
            side1 = abs(get_y(L, x1) - get_y(U, x1))
            h = abs(x0 - x1)
            res += (side0 + side1) * h / 2.0
        return res
    low = cuts[-1]
    high = W
    for i in range(50):
        mid = (low + high) / 2
        if get_area(cuts[-1], mid) > slice_area:
            high = mid
        else:
            low = mid
    cuts.append(mid)('%.10lg' % mid)
    return Ln


def func_41ab50a54e9749f1bdb292b082328567(cuts, infile, slice_area):

    def get_y(P, qx):
        i = 0
        while i + 2 < len(P) and P[i + 1][0] <= qx:
            i += 1
        x0, y0 = P[i]
        x1, y1 = P[i + 1]
        return y0 + (y1 - y0) * (qx - x0) / float(x1 - x0)
    W, Ln, Un, G = map(int, infile.readline().split())
    L = []
    for each in range(Ln):
        L.append(map(int, infile.readline().split()))
    U = []
    for each in range(Un):
        U.append(map(int, infile.readline().split()))

    def get_area(left, right):
        xs = [x for x, y in L]
        xs += [x for x, y in U]
        xs += [left, right]
        xs = sorted(set(xs))
        xs = [x for x in xs if left - 1e-09 <= x <= right + 1e-09]
        res = 0
        for i in range(1, len(xs)):
            x0 = xs[i - 1]
            x1 = xs[i]
            side0 = abs(get_y(L, x0) - get_y(U, x0))
            side1 = abs(get_y(L, x1) - get_y(U, x1))
            h = abs(x0 - x1)
            res += (side0 + side1) * h / 2.0
        return res
    low = cuts[-1]
    high = W
    for i in range(50):
        mid = (low + high) / 2
        if get_area(cuts[-1], mid) > slice_area:
            high = mid
        else:
            low = mid
    cuts.append(mid)('%.10lg' % mid)
    return Un


def func_695eb27a30014f8085c4b86bfe832d36(infile):

    def get_y(P, qx):
        i = 0
        while i + 2 < len(P) and P[i + 1][0] <= qx:
            i += 1
        x0, y0 = P[i]
        x1, y1 = P[i + 1]
        return y0 + (y1 - y0) * (qx - x0) / float(x1 - x0)
    W, Ln, Un, G = map(int, infile.readline().split())
    L = []
    for each in range(Ln):
        L.append(map(int, infile.readline().split()))
    U = []
    for each in range(Un):
        U.append(map(int, infile.readline().split()))

    def get_area(left, right):
        xs = [x for x, y in L]
        xs += [x for x, y in U]
        xs += [left, right]
        xs = sorted(set(xs))
        xs = [x for x in xs if left - 1e-09 <= x <= right + 1e-09]
        res = 0
        for i in range(1, len(xs)):
            x0 = xs[i - 1]
            x1 = xs[i]
            side0 = abs(get_y(L, x0) - get_y(U, x0))
            side1 = abs(get_y(L, x1) - get_y(U, x1))
            h = abs(x0 - x1)
            res += (side0 + side1) * h / 2.0
        return res
    total_area = get_area(0, W)
    slice_area = total_area / float(G)
    return each


def func_4807637b4fe243fa9bf3c3e264a5d368(infile):

    def get_y(P, qx):
        i = 0
        while i + 2 < len(P) and P[i + 1][0] <= qx:
            i += 1
        x0, y0 = P[i]
        x1, y1 = P[i + 1]
        return y0 + (y1 - y0) * (qx - x0) / float(x1 - x0)
    W, Ln, Un, G = map(int, infile.readline().split())
    L = []
    for each in range(Ln):
        L.append(map(int, infile.readline().split()))
    U = []
    for each in range(Un):
        U.append(map(int, infile.readline().split()))

    def get_area(left, right):
        xs = [x for x, y in L]
        xs += [x for x, y in U]
        xs += [left, right]
        xs = sorted(set(xs))
        xs = [x for x in xs if left - 1e-09 <= x <= right + 1e-09]
        res = 0
        for i in range(1, len(xs)):
            x0 = xs[i - 1]
            x1 = xs[i]
            side0 = abs(get_y(L, x0) - get_y(U, x0))
            side1 = abs(get_y(L, x1) - get_y(U, x1))
            h = abs(x0 - x1)
            res += (side0 + side1) * h / 2.0
        return res
    total_area = get_area(0, W)
    slice_area = total_area / float(G)
    return slice_area


def func_5bf90dfb77cc4577ab5a84683100a3ab(infile):

    def get_y(P, qx):
        i = 0
        while i + 2 < len(P) and P[i + 1][0] <= qx:
            i += 1
        x0, y0 = P[i]
        x1, y1 = P[i + 1]
        return y0 + (y1 - y0) * (qx - x0) / float(x1 - x0)
    W, Ln, Un, G = map(int, infile.readline().split())
    L = []
    for each in range(Ln):
        L.append(map(int, infile.readline().split()))
    U = []
    for each in range(Un):
        U.append(map(int, infile.readline().split()))

    def get_area(left, right):
        xs = [x for x, y in L]
        xs += [x for x, y in U]
        xs += [left, right]
        xs = sorted(set(xs))
        xs = [x for x in xs if left - 1e-09 <= x <= right + 1e-09]
        res = 0
        for i in range(1, len(xs)):
            x0 = xs[i - 1]
            x1 = xs[i]
            side0 = abs(get_y(L, x0) - get_y(U, x0))
            side1 = abs(get_y(L, x1) - get_y(U, x1))
            h = abs(x0 - x1)
            res += (side0 + side1) * h / 2.0
        return res
    total_area = get_area(0, W)
    slice_area = total_area / float(G)
    return U


def func_9de13791b3bd437cb0d3c5f056e8ed6d(infile):

    def get_y(P, qx):
        i = 0
        while i + 2 < len(P) and P[i + 1][0] <= qx:
            i += 1
        x0, y0 = P[i]
        x1, y1 = P[i + 1]
        return y0 + (y1 - y0) * (qx - x0) / float(x1 - x0)
    W, Ln, Un, G = map(int, infile.readline().split())
    L = []
    for each in range(Ln):
        L.append(map(int, infile.readline().split()))
    U = []
    for each in range(Un):
        U.append(map(int, infile.readline().split()))

    def get_area(left, right):
        xs = [x for x, y in L]
        xs += [x for x, y in U]
        xs += [left, right]
        xs = sorted(set(xs))
        xs = [x for x in xs if left - 1e-09 <= x <= right + 1e-09]
        res = 0
        for i in range(1, len(xs)):
            x0 = xs[i - 1]
            x1 = xs[i]
            side0 = abs(get_y(L, x0) - get_y(U, x0))
            side1 = abs(get_y(L, x1) - get_y(U, x1))
            h = abs(x0 - x1)
            res += (side0 + side1) * h / 2.0
        return res
    total_area = get_area(0, W)
    slice_area = total_area / float(G)
    return Ln


def func_4dbd0dcda5364923a900c2ffc393ede3(infile):

    def get_y(P, qx):
        i = 0
        while i + 2 < len(P) and P[i + 1][0] <= qx:
            i += 1
        x0, y0 = P[i]
        x1, y1 = P[i + 1]
        return y0 + (y1 - y0) * (qx - x0) / float(x1 - x0)
    W, Ln, Un, G = map(int, infile.readline().split())
    L = []
    for each in range(Ln):
        L.append(map(int, infile.readline().split()))
    U = []
    for each in range(Un):
        U.append(map(int, infile.readline().split()))

    def get_area(left, right):
        xs = [x for x, y in L]
        xs += [x for x, y in U]
        xs += [left, right]
        xs = sorted(set(xs))
        xs = [x for x in xs if left - 1e-09 <= x <= right + 1e-09]
        res = 0
        for i in range(1, len(xs)):
            x0 = xs[i - 1]
            x1 = xs[i]
            side0 = abs(get_y(L, x0) - get_y(U, x0))
            side1 = abs(get_y(L, x1) - get_y(U, x1))
            h = abs(x0 - x1)
            res += (side0 + side1) * h / 2.0
        return res
    total_area = get_area(0, W)
    slice_area = total_area / float(G)
    return L


def func_bff97a7c84f14787a23cc51fa5dbbb1a(infile):

    def get_y(P, qx):
        i = 0
        while i + 2 < len(P) and P[i + 1][0] <= qx:
            i += 1
        x0, y0 = P[i]
        x1, y1 = P[i + 1]
        return y0 + (y1 - y0) * (qx - x0) / float(x1 - x0)
    W, Ln, Un, G = map(int, infile.readline().split())
    L = []
    for each in range(Ln):
        L.append(map(int, infile.readline().split()))
    U = []
    for each in range(Un):
        U.append(map(int, infile.readline().split()))

    def get_area(left, right):
        xs = [x for x, y in L]
        xs += [x for x, y in U]
        xs += [left, right]
        xs = sorted(set(xs))
        xs = [x for x in xs if left - 1e-09 <= x <= right + 1e-09]
        res = 0
        for i in range(1, len(xs)):
            x0 = xs[i - 1]
            x1 = xs[i]
            side0 = abs(get_y(L, x0) - get_y(U, x0))
            side1 = abs(get_y(L, x1) - get_y(U, x1))
            h = abs(x0 - x1)
            res += (side0 + side1) * h / 2.0
        return res
    total_area = get_area(0, W)
    slice_area = total_area / float(G)
    return G


def func_bfb8c78061b84b1e87e3e077e649019d(infile):

    def get_y(P, qx):
        i = 0
        while i + 2 < len(P) and P[i + 1][0] <= qx:
            i += 1
        x0, y0 = P[i]
        x1, y1 = P[i + 1]
        return y0 + (y1 - y0) * (qx - x0) / float(x1 - x0)
    W, Ln, Un, G = map(int, infile.readline().split())
    L = []
    for each in range(Ln):
        L.append(map(int, infile.readline().split()))
    U = []
    for each in range(Un):
        U.append(map(int, infile.readline().split()))

    def get_area(left, right):
        xs = [x for x, y in L]
        xs += [x for x, y in U]
        xs += [left, right]
        xs = sorted(set(xs))
        xs = [x for x in xs if left - 1e-09 <= x <= right + 1e-09]
        res = 0
        for i in range(1, len(xs)):
            x0 = xs[i - 1]
            x1 = xs[i]
            side0 = abs(get_y(L, x0) - get_y(U, x0))
            side1 = abs(get_y(L, x1) - get_y(U, x1))
            h = abs(x0 - x1)
            res += (side0 + side1) * h / 2.0
        return res
    total_area = get_area(0, W)
    slice_area = total_area / float(G)
    return Un


def func_b95ba86976544c82819ca9bd3c481de8(infile):

    def get_y(P, qx):
        i = 0
        while i + 2 < len(P) and P[i + 1][0] <= qx:
            i += 1
        x0, y0 = P[i]
        x1, y1 = P[i + 1]
        return y0 + (y1 - y0) * (qx - x0) / float(x1 - x0)
    W, Ln, Un, G = map(int, infile.readline().split())
    L = []
    for each in range(Ln):
        L.append(map(int, infile.readline().split()))
    U = []
    for each in range(Un):
        U.append(map(int, infile.readline().split()))

    def get_area(left, right):
        xs = [x for x, y in L]
        xs += [x for x, y in U]
        xs += [left, right]
        xs = sorted(set(xs))
        xs = [x for x in xs if left - 1e-09 <= x <= right + 1e-09]
        res = 0
        for i in range(1, len(xs)):
            x0 = xs[i - 1]
            x1 = xs[i]
            side0 = abs(get_y(L, x0) - get_y(U, x0))
            side1 = abs(get_y(L, x1) - get_y(U, x1))
            h = abs(x0 - x1)
            res += (side0 + side1) * h / 2.0
        return res
    total_area = get_area(0, W)
    slice_area = total_area / float(G)
    return total_area


def func_ffa6a2e8f662497090745f24393d2f04(infile):

    def get_y(P, qx):
        i = 0
        while i + 2 < len(P) and P[i + 1][0] <= qx:
            i += 1
        x0, y0 = P[i]
        x1, y1 = P[i + 1]
        return y0 + (y1 - y0) * (qx - x0) / float(x1 - x0)
    W, Ln, Un, G = map(int, infile.readline().split())
    L = []
    for each in range(Ln):
        L.append(map(int, infile.readline().split()))
    U = []
    for each in range(Un):
        U.append(map(int, infile.readline().split()))

    def get_area(left, right):
        xs = [x for x, y in L]
        xs += [x for x, y in U]
        xs += [left, right]
        xs = sorted(set(xs))
        xs = [x for x in xs if left - 1e-09 <= x <= right + 1e-09]
        res = 0
        for i in range(1, len(xs)):
            x0 = xs[i - 1]
            x1 = xs[i]
            side0 = abs(get_y(L, x0) - get_y(U, x0))
            side1 = abs(get_y(L, x1) - get_y(U, x1))
            h = abs(x0 - x1)
            res += (side0 + side1) * h / 2.0
        return res
    total_area = get_area(0, W)
    slice_area = total_area / float(G)
    return W


def func_17655f40a007462aa63d3a6e57513ed8(total_area, infile):

    def get_y(P, qx):
        i = 0
        while i + 2 < len(P) and P[i + 1][0] <= qx:
            i += 1
        x0, y0 = P[i]
        x1, y1 = P[i + 1]
        return y0 + (y1 - y0) * (qx - x0) / float(x1 - x0)
    W, Ln, Un, G = map(int, infile.readline().split())
    L = []
    for each in range(Ln):
        L.append(map(int, infile.readline().split()))
    U = []
    for each in range(Un):
        U.append(map(int, infile.readline().split()))

    def get_area(left, right):
        xs = [x for x, y in L]
        xs += [x for x, y in U]
        xs += [left, right]
        xs = sorted(set(xs))
        xs = [x for x in xs if left - 1e-09 <= x <= right + 1e-09]
        res = 0
        for i in range(1, len(xs)):
            x0 = xs[i - 1]
            x1 = xs[i]
            side0 = abs(get_y(L, x0) - get_y(U, x0))
            side1 = abs(get_y(L, x1) - get_y(U, x1))
            h = abs(x0 - x1)
            res += (side0 + side1) * h / 2.0
        return res
    slice_area = total_area / float(G)
    cuts = [0.0]
    return cuts


def func_43ab0c6208734869985877633d5ed513(total_area, infile):

    def get_y(P, qx):
        i = 0
        while i + 2 < len(P) and P[i + 1][0] <= qx:
            i += 1
        x0, y0 = P[i]
        x1, y1 = P[i + 1]
        return y0 + (y1 - y0) * (qx - x0) / float(x1 - x0)
    W, Ln, Un, G = map(int, infile.readline().split())
    L = []
    for each in range(Ln):
        L.append(map(int, infile.readline().split()))
    U = []
    for each in range(Un):
        U.append(map(int, infile.readline().split()))

    def get_area(left, right):
        xs = [x for x, y in L]
        xs += [x for x, y in U]
        xs += [left, right]
        xs = sorted(set(xs))
        xs = [x for x in xs if left - 1e-09 <= x <= right + 1e-09]
        res = 0
        for i in range(1, len(xs)):
            x0 = xs[i - 1]
            x1 = xs[i]
            side0 = abs(get_y(L, x0) - get_y(U, x0))
            side1 = abs(get_y(L, x1) - get_y(U, x1))
            h = abs(x0 - x1)
            res += (side0 + side1) * h / 2.0
        return res
    slice_area = total_area / float(G)
    cuts = [0.0]
    return W


def func_a0e3df6d39f5435580d018b626bac634(total_area, infile):

    def get_y(P, qx):
        i = 0
        while i + 2 < len(P) and P[i + 1][0] <= qx:
            i += 1
        x0, y0 = P[i]
        x1, y1 = P[i + 1]
        return y0 + (y1 - y0) * (qx - x0) / float(x1 - x0)
    W, Ln, Un, G = map(int, infile.readline().split())
    L = []
    for each in range(Ln):
        L.append(map(int, infile.readline().split()))
    U = []
    for each in range(Un):
        U.append(map(int, infile.readline().split()))

    def get_area(left, right):
        xs = [x for x, y in L]
        xs += [x for x, y in U]
        xs += [left, right]
        xs = sorted(set(xs))
        xs = [x for x in xs if left - 1e-09 <= x <= right + 1e-09]
        res = 0
        for i in range(1, len(xs)):
            x0 = xs[i - 1]
            x1 = xs[i]
            side0 = abs(get_y(L, x0) - get_y(U, x0))
            side1 = abs(get_y(L, x1) - get_y(U, x1))
            h = abs(x0 - x1)
            res += (side0 + side1) * h / 2.0
        return res
    slice_area = total_area / float(G)
    cuts = [0.0]
    return each


def func_82404fbe15b14291ba9a3dce79aed045(total_area, infile):

    def get_y(P, qx):
        i = 0
        while i + 2 < len(P) and P[i + 1][0] <= qx:
            i += 1
        x0, y0 = P[i]
        x1, y1 = P[i + 1]
        return y0 + (y1 - y0) * (qx - x0) / float(x1 - x0)
    W, Ln, Un, G = map(int, infile.readline().split())
    L = []
    for each in range(Ln):
        L.append(map(int, infile.readline().split()))
    U = []
    for each in range(Un):
        U.append(map(int, infile.readline().split()))

    def get_area(left, right):
        xs = [x for x, y in L]
        xs += [x for x, y in U]
        xs += [left, right]
        xs = sorted(set(xs))
        xs = [x for x in xs if left - 1e-09 <= x <= right + 1e-09]
        res = 0
        for i in range(1, len(xs)):
            x0 = xs[i - 1]
            x1 = xs[i]
            side0 = abs(get_y(L, x0) - get_y(U, x0))
            side1 = abs(get_y(L, x1) - get_y(U, x1))
            h = abs(x0 - x1)
            res += (side0 + side1) * h / 2.0
        return res
    slice_area = total_area / float(G)
    cuts = [0.0]
    return Un


def func_8c13f9471aa140168c02fdbf12834707(total_area, infile):

    def get_y(P, qx):
        i = 0
        while i + 2 < len(P) and P[i + 1][0] <= qx:
            i += 1
        x0, y0 = P[i]
        x1, y1 = P[i + 1]
        return y0 + (y1 - y0) * (qx - x0) / float(x1 - x0)
    W, Ln, Un, G = map(int, infile.readline().split())
    L = []
    for each in range(Ln):
        L.append(map(int, infile.readline().split()))
    U = []
    for each in range(Un):
        U.append(map(int, infile.readline().split()))

    def get_area(left, right):
        xs = [x for x, y in L]
        xs += [x for x, y in U]
        xs += [left, right]
        xs = sorted(set(xs))
        xs = [x for x in xs if left - 1e-09 <= x <= right + 1e-09]
        res = 0
        for i in range(1, len(xs)):
            x0 = xs[i - 1]
            x1 = xs[i]
            side0 = abs(get_y(L, x0) - get_y(U, x0))
            side1 = abs(get_y(L, x1) - get_y(U, x1))
            h = abs(x0 - x1)
            res += (side0 + side1) * h / 2.0
        return res
    slice_area = total_area / float(G)
    cuts = [0.0]
    return slice_area


def func_d25afdb18aa443bd9044d609cff07207(total_area, infile):

    def get_y(P, qx):
        i = 0
        while i + 2 < len(P) and P[i + 1][0] <= qx:
            i += 1
        x0, y0 = P[i]
        x1, y1 = P[i + 1]
        return y0 + (y1 - y0) * (qx - x0) / float(x1 - x0)
    W, Ln, Un, G = map(int, infile.readline().split())
    L = []
    for each in range(Ln):
        L.append(map(int, infile.readline().split()))
    U = []
    for each in range(Un):
        U.append(map(int, infile.readline().split()))

    def get_area(left, right):
        xs = [x for x, y in L]
        xs += [x for x, y in U]
        xs += [left, right]
        xs = sorted(set(xs))
        xs = [x for x in xs if left - 1e-09 <= x <= right + 1e-09]
        res = 0
        for i in range(1, len(xs)):
            x0 = xs[i - 1]
            x1 = xs[i]
            side0 = abs(get_y(L, x0) - get_y(U, x0))
            side1 = abs(get_y(L, x1) - get_y(U, x1))
            h = abs(x0 - x1)
            res += (side0 + side1) * h / 2.0
        return res
    slice_area = total_area / float(G)
    cuts = [0.0]
    return G


def func_7e9e471306cf485490e2045ac3c286c8(total_area, infile):

    def get_y(P, qx):
        i = 0
        while i + 2 < len(P) and P[i + 1][0] <= qx:
            i += 1
        x0, y0 = P[i]
        x1, y1 = P[i + 1]
        return y0 + (y1 - y0) * (qx - x0) / float(x1 - x0)
    W, Ln, Un, G = map(int, infile.readline().split())
    L = []
    for each in range(Ln):
        L.append(map(int, infile.readline().split()))
    U = []
    for each in range(Un):
        U.append(map(int, infile.readline().split()))

    def get_area(left, right):
        xs = [x for x, y in L]
        xs += [x for x, y in U]
        xs += [left, right]
        xs = sorted(set(xs))
        xs = [x for x in xs if left - 1e-09 <= x <= right + 1e-09]
        res = 0
        for i in range(1, len(xs)):
            x0 = xs[i - 1]
            x1 = xs[i]
            side0 = abs(get_y(L, x0) - get_y(U, x0))
            side1 = abs(get_y(L, x1) - get_y(U, x1))
            h = abs(x0 - x1)
            res += (side0 + side1) * h / 2.0
        return res
    slice_area = total_area / float(G)
    cuts = [0.0]
    return total_area


def func_5baa8ea706f1468aa0f272699aa0d256(total_area, infile):

    def get_y(P, qx):
        i = 0
        while i + 2 < len(P) and P[i + 1][0] <= qx:
            i += 1
        x0, y0 = P[i]
        x1, y1 = P[i + 1]
        return y0 + (y1 - y0) * (qx - x0) / float(x1 - x0)
    W, Ln, Un, G = map(int, infile.readline().split())
    L = []
    for each in range(Ln):
        L.append(map(int, infile.readline().split()))
    U = []
    for each in range(Un):
        U.append(map(int, infile.readline().split()))

    def get_area(left, right):
        xs = [x for x, y in L]
        xs += [x for x, y in U]
        xs += [left, right]
        xs = sorted(set(xs))
        xs = [x for x in xs if left - 1e-09 <= x <= right + 1e-09]
        res = 0
        for i in range(1, len(xs)):
            x0 = xs[i - 1]
            x1 = xs[i]
            side0 = abs(get_y(L, x0) - get_y(U, x0))
            side1 = abs(get_y(L, x1) - get_y(U, x1))
            h = abs(x0 - x1)
            res += (side0 + side1) * h / 2.0
        return res
    slice_area = total_area / float(G)
    cuts = [0.0]
    return Ln


def func_57316deee51742358f87656e48e65844(total_area, infile):

    def get_y(P, qx):
        i = 0
        while i + 2 < len(P) and P[i + 1][0] <= qx:
            i += 1
        x0, y0 = P[i]
        x1, y1 = P[i + 1]
        return y0 + (y1 - y0) * (qx - x0) / float(x1 - x0)
    W, Ln, Un, G = map(int, infile.readline().split())
    L = []
    for each in range(Ln):
        L.append(map(int, infile.readline().split()))
    U = []
    for each in range(Un):
        U.append(map(int, infile.readline().split()))

    def get_area(left, right):
        xs = [x for x, y in L]
        xs += [x for x, y in U]
        xs += [left, right]
        xs = sorted(set(xs))
        xs = [x for x in xs if left - 1e-09 <= x <= right + 1e-09]
        res = 0
        for i in range(1, len(xs)):
            x0 = xs[i - 1]
            x1 = xs[i]
            side0 = abs(get_y(L, x0) - get_y(U, x0))
            side1 = abs(get_y(L, x1) - get_y(U, x1))
            h = abs(x0 - x1)
            res += (side0 + side1) * h / 2.0
        return res
    slice_area = total_area / float(G)
    cuts = [0.0]
    return L


def func_a7117f3b53854315848511ce94935840(total_area, infile):

    def get_y(P, qx):
        i = 0
        while i + 2 < len(P) and P[i + 1][0] <= qx:
            i += 1
        x0, y0 = P[i]
        x1, y1 = P[i + 1]
        return y0 + (y1 - y0) * (qx - x0) / float(x1 - x0)
    W, Ln, Un, G = map(int, infile.readline().split())
    L = []
    for each in range(Ln):
        L.append(map(int, infile.readline().split()))
    U = []
    for each in range(Un):
        U.append(map(int, infile.readline().split()))

    def get_area(left, right):
        xs = [x for x, y in L]
        xs += [x for x, y in U]
        xs += [left, right]
        xs = sorted(set(xs))
        xs = [x for x in xs if left - 1e-09 <= x <= right + 1e-09]
        res = 0
        for i in range(1, len(xs)):
            x0 = xs[i - 1]
            x1 = xs[i]
            side0 = abs(get_y(L, x0) - get_y(U, x0))
            side1 = abs(get_y(L, x1) - get_y(U, x1))
            h = abs(x0 - x1)
            res += (side0 + side1) * h / 2.0
        return res
    slice_area = total_area / float(G)
    cuts = [0.0]
    return U


def func_efc4069ce1514c83be2f21989288a5da(infile, slice_area):

    def get_y(P, qx):
        i = 0
        while i + 2 < len(P) and P[i + 1][0] <= qx:
            i += 1
        x0, y0 = P[i]
        x1, y1 = P[i + 1]
        return y0 + (y1 - y0) * (qx - x0) / float(x1 - x0)
    W, Ln, Un, G = map(int, infile.readline().split())
    L = []
    for each in range(Ln):
        L.append(map(int, infile.readline().split()))
    U = []
    for each in range(Un):
        U.append(map(int, infile.readline().split()))

    def get_area(left, right):
        xs = [x for x, y in L]
        xs += [x for x, y in U]
        xs += [left, right]
        xs = sorted(set(xs))
        xs = [x for x in xs if left - 1e-09 <= x <= right + 1e-09]
        res = 0
        for i in range(1, len(xs)):
            x0 = xs[i - 1]
            x1 = xs[i]
            side0 = abs(get_y(L, x0) - get_y(U, x0))
            side1 = abs(get_y(L, x1) - get_y(U, x1))
            h = abs(x0 - x1)
            res += (side0 + side1) * h / 2.0
        return res
    cuts = [0.0]
    while len(cuts) < G:
        low = cuts[-1]
        high = W
        for i in range(50):
            mid = (low + high) / 2
            if get_area(cuts[-1], mid) > slice_area:
                high = mid
            else:
                low = mid
        cuts.append(mid)
        print '%.10lg' % mid
    return i


def func_bde6c4889ce44aafb15c88a7864dc395(infile, slice_area):

    def get_y(P, qx):
        i = 0
        while i + 2 < len(P) and P[i + 1][0] <= qx:
            i += 1
        x0, y0 = P[i]
        x1, y1 = P[i + 1]
        return y0 + (y1 - y0) * (qx - x0) / float(x1 - x0)
    W, Ln, Un, G = map(int, infile.readline().split())
    L = []
    for each in range(Ln):
        L.append(map(int, infile.readline().split()))
    U = []
    for each in range(Un):
        U.append(map(int, infile.readline().split()))

    def get_area(left, right):
        xs = [x for x, y in L]
        xs += [x for x, y in U]
        xs += [left, right]
        xs = sorted(set(xs))
        xs = [x for x in xs if left - 1e-09 <= x <= right + 1e-09]
        res = 0
        for i in range(1, len(xs)):
            x0 = xs[i - 1]
            x1 = xs[i]
            side0 = abs(get_y(L, x0) - get_y(U, x0))
            side1 = abs(get_y(L, x1) - get_y(U, x1))
            h = abs(x0 - x1)
            res += (side0 + side1) * h / 2.0
        return res
    cuts = [0.0]
    while len(cuts) < G:
        low = cuts[-1]
        high = W
        for i in range(50):
            mid = (low + high) / 2
            if get_area(cuts[-1], mid) > slice_area:
                high = mid
            else:
                low = mid
        cuts.append(mid)
        print '%.10lg' % mid
    return L


def func_4f8ffa3ee6284050b4e7eac89a31ac6a(infile, slice_area):

    def get_y(P, qx):
        i = 0
        while i + 2 < len(P) and P[i + 1][0] <= qx:
            i += 1
        x0, y0 = P[i]
        x1, y1 = P[i + 1]
        return y0 + (y1 - y0) * (qx - x0) / float(x1 - x0)
    W, Ln, Un, G = map(int, infile.readline().split())
    L = []
    for each in range(Ln):
        L.append(map(int, infile.readline().split()))
    U = []
    for each in range(Un):
        U.append(map(int, infile.readline().split()))

    def get_area(left, right):
        xs = [x for x, y in L]
        xs += [x for x, y in U]
        xs += [left, right]
        xs = sorted(set(xs))
        xs = [x for x in xs if left - 1e-09 <= x <= right + 1e-09]
        res = 0
        for i in range(1, len(xs)):
            x0 = xs[i - 1]
            x1 = xs[i]
            side0 = abs(get_y(L, x0) - get_y(U, x0))
            side1 = abs(get_y(L, x1) - get_y(U, x1))
            h = abs(x0 - x1)
            res += (side0 + side1) * h / 2.0
        return res
    cuts = [0.0]
    while len(cuts) < G:
        low = cuts[-1]
        high = W
        for i in range(50):
            mid = (low + high) / 2
            if get_area(cuts[-1], mid) > slice_area:
                high = mid
            else:
                low = mid
        cuts.append(mid)
        print '%.10lg' % mid
    return each


def func_0d508ba311664209b252f614ceb50b2f(infile, slice_area):

    def get_y(P, qx):
        i = 0
        while i + 2 < len(P) and P[i + 1][0] <= qx:
            i += 1
        x0, y0 = P[i]
        x1, y1 = P[i + 1]
        return y0 + (y1 - y0) * (qx - x0) / float(x1 - x0)
    W, Ln, Un, G = map(int, infile.readline().split())
    L = []
    for each in range(Ln):
        L.append(map(int, infile.readline().split()))
    U = []
    for each in range(Un):
        U.append(map(int, infile.readline().split()))

    def get_area(left, right):
        xs = [x for x, y in L]
        xs += [x for x, y in U]
        xs += [left, right]
        xs = sorted(set(xs))
        xs = [x for x in xs if left - 1e-09 <= x <= right + 1e-09]
        res = 0
        for i in range(1, len(xs)):
            x0 = xs[i - 1]
            x1 = xs[i]
            side0 = abs(get_y(L, x0) - get_y(U, x0))
            side1 = abs(get_y(L, x1) - get_y(U, x1))
            h = abs(x0 - x1)
            res += (side0 + side1) * h / 2.0
        return res
    cuts = [0.0]
    while len(cuts) < G:
        low = cuts[-1]
        high = W
        for i in range(50):
            mid = (low + high) / 2
            if get_area(cuts[-1], mid) > slice_area:
                high = mid
            else:
                low = mid
        cuts.append(mid)
        print '%.10lg' % mid
    return W


def func_5091029b3daa468691c5326497c7c669(infile, slice_area):

    def get_y(P, qx):
        i = 0
        while i + 2 < len(P) and P[i + 1][0] <= qx:
            i += 1
        x0, y0 = P[i]
        x1, y1 = P[i + 1]
        return y0 + (y1 - y0) * (qx - x0) / float(x1 - x0)
    W, Ln, Un, G = map(int, infile.readline().split())
    L = []
    for each in range(Ln):
        L.append(map(int, infile.readline().split()))
    U = []
    for each in range(Un):
        U.append(map(int, infile.readline().split()))

    def get_area(left, right):
        xs = [x for x, y in L]
        xs += [x for x, y in U]
        xs += [left, right]
        xs = sorted(set(xs))
        xs = [x for x in xs if left - 1e-09 <= x <= right + 1e-09]
        res = 0
        for i in range(1, len(xs)):
            x0 = xs[i - 1]
            x1 = xs[i]
            side0 = abs(get_y(L, x0) - get_y(U, x0))
            side1 = abs(get_y(L, x1) - get_y(U, x1))
            h = abs(x0 - x1)
            res += (side0 + side1) * h / 2.0
        return res
    cuts = [0.0]
    while len(cuts) < G:
        low = cuts[-1]
        high = W
        for i in range(50):
            mid = (low + high) / 2
            if get_area(cuts[-1], mid) > slice_area:
                high = mid
            else:
                low = mid
        cuts.append(mid)
        print '%.10lg' % mid
    return Un


def func_c274f5ce000b4e6297d2fdcf861074bf(infile, slice_area):

    def get_y(P, qx):
        i = 0
        while i + 2 < len(P) and P[i + 1][0] <= qx:
            i += 1
        x0, y0 = P[i]
        x1, y1 = P[i + 1]
        return y0 + (y1 - y0) * (qx - x0) / float(x1 - x0)
    W, Ln, Un, G = map(int, infile.readline().split())
    L = []
    for each in range(Ln):
        L.append(map(int, infile.readline().split()))
    U = []
    for each in range(Un):
        U.append(map(int, infile.readline().split()))

    def get_area(left, right):
        xs = [x for x, y in L]
        xs += [x for x, y in U]
        xs += [left, right]
        xs = sorted(set(xs))
        xs = [x for x in xs if left - 1e-09 <= x <= right + 1e-09]
        res = 0
        for i in range(1, len(xs)):
            x0 = xs[i - 1]
            x1 = xs[i]
            side0 = abs(get_y(L, x0) - get_y(U, x0))
            side1 = abs(get_y(L, x1) - get_y(U, x1))
            h = abs(x0 - x1)
            res += (side0 + side1) * h / 2.0
        return res
    cuts = [0.0]
    while len(cuts) < G:
        low = cuts[-1]
        high = W
        for i in range(50):
            mid = (low + high) / 2
            if get_area(cuts[-1], mid) > slice_area:
                high = mid
            else:
                low = mid
        cuts.append(mid)
        print '%.10lg' % mid
    return slice_area


def func_13cde692f1574eeeb98b97c96bb64b75(infile, slice_area):

    def get_y(P, qx):
        i = 0
        while i + 2 < len(P) and P[i + 1][0] <= qx:
            i += 1
        x0, y0 = P[i]
        x1, y1 = P[i + 1]
        return y0 + (y1 - y0) * (qx - x0) / float(x1 - x0)
    W, Ln, Un, G = map(int, infile.readline().split())
    L = []
    for each in range(Ln):
        L.append(map(int, infile.readline().split()))
    U = []
    for each in range(Un):
        U.append(map(int, infile.readline().split()))

    def get_area(left, right):
        xs = [x for x, y in L]
        xs += [x for x, y in U]
        xs += [left, right]
        xs = sorted(set(xs))
        xs = [x for x in xs if left - 1e-09 <= x <= right + 1e-09]
        res = 0
        for i in range(1, len(xs)):
            x0 = xs[i - 1]
            x1 = xs[i]
            side0 = abs(get_y(L, x0) - get_y(U, x0))
            side1 = abs(get_y(L, x1) - get_y(U, x1))
            h = abs(x0 - x1)
            res += (side0 + side1) * h / 2.0
        return res
    cuts = [0.0]
    while len(cuts) < G:
        low = cuts[-1]
        high = W
        for i in range(50):
            mid = (low + high) / 2
            if get_area(cuts[-1], mid) > slice_area:
                high = mid
            else:
                low = mid
        cuts.append(mid)
        print '%.10lg' % mid
    return Ln


def func_123a8a0a05ed4376a99e13e7a8f189ac(infile, slice_area):

    def get_y(P, qx):
        i = 0
        while i + 2 < len(P) and P[i + 1][0] <= qx:
            i += 1
        x0, y0 = P[i]
        x1, y1 = P[i + 1]
        return y0 + (y1 - y0) * (qx - x0) / float(x1 - x0)
    W, Ln, Un, G = map(int, infile.readline().split())
    L = []
    for each in range(Ln):
        L.append(map(int, infile.readline().split()))
    U = []
    for each in range(Un):
        U.append(map(int, infile.readline().split()))

    def get_area(left, right):
        xs = [x for x, y in L]
        xs += [x for x, y in U]
        xs += [left, right]
        xs = sorted(set(xs))
        xs = [x for x in xs if left - 1e-09 <= x <= right + 1e-09]
        res = 0
        for i in range(1, len(xs)):
            x0 = xs[i - 1]
            x1 = xs[i]
            side0 = abs(get_y(L, x0) - get_y(U, x0))
            side1 = abs(get_y(L, x1) - get_y(U, x1))
            h = abs(x0 - x1)
            res += (side0 + side1) * h / 2.0
        return res
    cuts = [0.0]
    while len(cuts) < G:
        low = cuts[-1]
        high = W
        for i in range(50):
            mid = (low + high) / 2
            if get_area(cuts[-1], mid) > slice_area:
                high = mid
            else:
                low = mid
        cuts.append(mid)
        print '%.10lg' % mid
    return low


def func_09eedef1338148c9a51ec5c96b8067a7(infile, slice_area):

    def get_y(P, qx):
        i = 0
        while i + 2 < len(P) and P[i + 1][0] <= qx:
            i += 1
        x0, y0 = P[i]
        x1, y1 = P[i + 1]
        return y0 + (y1 - y0) * (qx - x0) / float(x1 - x0)
    W, Ln, Un, G = map(int, infile.readline().split())
    L = []
    for each in range(Ln):
        L.append(map(int, infile.readline().split()))
    U = []
    for each in range(Un):
        U.append(map(int, infile.readline().split()))

    def get_area(left, right):
        xs = [x for x, y in L]
        xs += [x for x, y in U]
        xs += [left, right]
        xs = sorted(set(xs))
        xs = [x for x in xs if left - 1e-09 <= x <= right + 1e-09]
        res = 0
        for i in range(1, len(xs)):
            x0 = xs[i - 1]
            x1 = xs[i]
            side0 = abs(get_y(L, x0) - get_y(U, x0))
            side1 = abs(get_y(L, x1) - get_y(U, x1))
            h = abs(x0 - x1)
            res += (side0 + side1) * h / 2.0
        return res
    cuts = [0.0]
    while len(cuts) < G:
        low = cuts[-1]
        high = W
        for i in range(50):
            mid = (low + high) / 2
            if get_area(cuts[-1], mid) > slice_area:
                high = mid
            else:
                low = mid
        cuts.append(mid)
        print '%.10lg' % mid
    return mid


def func_c53cedac7bed4f0586fbe7394617c99e(infile, slice_area):

    def get_y(P, qx):
        i = 0
        while i + 2 < len(P) and P[i + 1][0] <= qx:
            i += 1
        x0, y0 = P[i]
        x1, y1 = P[i + 1]
        return y0 + (y1 - y0) * (qx - x0) / float(x1 - x0)
    W, Ln, Un, G = map(int, infile.readline().split())
    L = []
    for each in range(Ln):
        L.append(map(int, infile.readline().split()))
    U = []
    for each in range(Un):
        U.append(map(int, infile.readline().split()))

    def get_area(left, right):
        xs = [x for x, y in L]
        xs += [x for x, y in U]
        xs += [left, right]
        xs = sorted(set(xs))
        xs = [x for x in xs if left - 1e-09 <= x <= right + 1e-09]
        res = 0
        for i in range(1, len(xs)):
            x0 = xs[i - 1]
            x1 = xs[i]
            side0 = abs(get_y(L, x0) - get_y(U, x0))
            side1 = abs(get_y(L, x1) - get_y(U, x1))
            h = abs(x0 - x1)
            res += (side0 + side1) * h / 2.0
        return res
    cuts = [0.0]
    while len(cuts) < G:
        low = cuts[-1]
        high = W
        for i in range(50):
            mid = (low + high) / 2
            if get_area(cuts[-1], mid) > slice_area:
                high = mid
            else:
                low = mid
        cuts.append(mid)
        print '%.10lg' % mid
    return high


def func_644a5158283a4c2687e970043fd799b4(infile, slice_area):

    def get_y(P, qx):
        i = 0
        while i + 2 < len(P) and P[i + 1][0] <= qx:
            i += 1
        x0, y0 = P[i]
        x1, y1 = P[i + 1]
        return y0 + (y1 - y0) * (qx - x0) / float(x1 - x0)
    W, Ln, Un, G = map(int, infile.readline().split())
    L = []
    for each in range(Ln):
        L.append(map(int, infile.readline().split()))
    U = []
    for each in range(Un):
        U.append(map(int, infile.readline().split()))

    def get_area(left, right):
        xs = [x for x, y in L]
        xs += [x for x, y in U]
        xs += [left, right]
        xs = sorted(set(xs))
        xs = [x for x in xs if left - 1e-09 <= x <= right + 1e-09]
        res = 0
        for i in range(1, len(xs)):
            x0 = xs[i - 1]
            x1 = xs[i]
            side0 = abs(get_y(L, x0) - get_y(U, x0))
            side1 = abs(get_y(L, x1) - get_y(U, x1))
            h = abs(x0 - x1)
            res += (side0 + side1) * h / 2.0
        return res
    cuts = [0.0]
    while len(cuts) < G:
        low = cuts[-1]
        high = W
        for i in range(50):
            mid = (low + high) / 2
            if get_area(cuts[-1], mid) > slice_area:
                high = mid
            else:
                low = mid
        cuts.append(mid)
        print '%.10lg' % mid
    return G


def func_6ee6a7526f2248ba84150bdbef3d0bee(infile, slice_area):

    def get_y(P, qx):
        i = 0
        while i + 2 < len(P) and P[i + 1][0] <= qx:
            i += 1
        x0, y0 = P[i]
        x1, y1 = P[i + 1]
        return y0 + (y1 - y0) * (qx - x0) / float(x1 - x0)
    W, Ln, Un, G = map(int, infile.readline().split())
    L = []
    for each in range(Ln):
        L.append(map(int, infile.readline().split()))
    U = []
    for each in range(Un):
        U.append(map(int, infile.readline().split()))

    def get_area(left, right):
        xs = [x for x, y in L]
        xs += [x for x, y in U]
        xs += [left, right]
        xs = sorted(set(xs))
        xs = [x for x in xs if left - 1e-09 <= x <= right + 1e-09]
        res = 0
        for i in range(1, len(xs)):
            x0 = xs[i - 1]
            x1 = xs[i]
            side0 = abs(get_y(L, x0) - get_y(U, x0))
            side1 = abs(get_y(L, x1) - get_y(U, x1))
            h = abs(x0 - x1)
            res += (side0 + side1) * h / 2.0
        return res
    cuts = [0.0]
    while len(cuts) < G:
        low = cuts[-1]
        high = W
        for i in range(50):
            mid = (low + high) / 2
            if get_area(cuts[-1], mid) > slice_area:
                high = mid
            else:
                low = mid
        cuts.append(mid)
        print '%.10lg' % mid
    return U


def func_6e7b80bce63b4140b1c538ab125a5709(infile, slice_area):

    def get_y(P, qx):
        i = 0
        while i + 2 < len(P) and P[i + 1][0] <= qx:
            i += 1
        x0, y0 = P[i]
        x1, y1 = P[i + 1]
        return y0 + (y1 - y0) * (qx - x0) / float(x1 - x0)
    W, Ln, Un, G = map(int, infile.readline().split())
    L = []
    for each in range(Ln):
        L.append(map(int, infile.readline().split()))
    U = []
    for each in range(Un):
        U.append(map(int, infile.readline().split()))

    def get_area(left, right):
        xs = [x for x, y in L]
        xs += [x for x, y in U]
        xs += [left, right]
        xs = sorted(set(xs))
        xs = [x for x in xs if left - 1e-09 <= x <= right + 1e-09]
        res = 0
        for i in range(1, len(xs)):
            x0 = xs[i - 1]
            x1 = xs[i]
            side0 = abs(get_y(L, x0) - get_y(U, x0))
            side1 = abs(get_y(L, x1) - get_y(U, x1))
            h = abs(x0 - x1)
            res += (side0 + side1) * h / 2.0
        return res
    cuts = [0.0]
    while len(cuts) < G:
        low = cuts[-1]
        high = W
        for i in range(50):
            mid = (low + high) / 2
            if get_area(cuts[-1], mid) > slice_area:
                high = mid
            else:
                low = mid
        cuts.append(mid)
        print '%.10lg' % mid
    return cuts


def func_7dfa47cc858b4132931d8b9526238611(infile):

    def get_y(P, qx):
        i = 0
        while i + 2 < len(P) and P[i + 1][0] <= qx:
            i += 1
        x0, y0 = P[i]
        x1, y1 = P[i + 1]
        return y0 + (y1 - y0) * (qx - x0) / float(x1 - x0)
    W, Ln, Un, G = map(int, infile.readline().split())
    L = []
    for each in range(Ln):
        L.append(map(int, infile.readline().split()))
    U = []
    for each in range(Un):
        U.append(map(int, infile.readline().split()))

    def get_area(left, right):
        xs = [x for x, y in L]
        xs += [x for x, y in U]
        xs += [left, right]
        xs = sorted(set(xs))
        xs = [x for x in xs if left - 1e-09 <= x <= right + 1e-09]
        res = 0
        for i in range(1, len(xs)):
            x0 = xs[i - 1]
            x1 = xs[i]
            side0 = abs(get_y(L, x0) - get_y(U, x0))
            side1 = abs(get_y(L, x1) - get_y(U, x1))
            h = abs(x0 - x1)
            res += (side0 + side1) * h / 2.0
        return res
    total_area = get_area(0, W)
    slice_area = total_area / float(G)
    cuts = [0.0]
    return U


def func_05f757087a1b41c0a96d83a38bcb51bf(infile):

    def get_y(P, qx):
        i = 0
        while i + 2 < len(P) and P[i + 1][0] <= qx:
            i += 1
        x0, y0 = P[i]
        x1, y1 = P[i + 1]
        return y0 + (y1 - y0) * (qx - x0) / float(x1 - x0)
    W, Ln, Un, G = map(int, infile.readline().split())
    L = []
    for each in range(Ln):
        L.append(map(int, infile.readline().split()))
    U = []
    for each in range(Un):
        U.append(map(int, infile.readline().split()))

    def get_area(left, right):
        xs = [x for x, y in L]
        xs += [x for x, y in U]
        xs += [left, right]
        xs = sorted(set(xs))
        xs = [x for x in xs if left - 1e-09 <= x <= right + 1e-09]
        res = 0
        for i in range(1, len(xs)):
            x0 = xs[i - 1]
            x1 = xs[i]
            side0 = abs(get_y(L, x0) - get_y(U, x0))
            side1 = abs(get_y(L, x1) - get_y(U, x1))
            h = abs(x0 - x1)
            res += (side0 + side1) * h / 2.0
        return res
    total_area = get_area(0, W)
    slice_area = total_area / float(G)
    cuts = [0.0]
    return W


def func_a5760df3ecc0440593b4cad61ab48a64(infile):

    def get_y(P, qx):
        i = 0
        while i + 2 < len(P) and P[i + 1][0] <= qx:
            i += 1
        x0, y0 = P[i]
        x1, y1 = P[i + 1]
        return y0 + (y1 - y0) * (qx - x0) / float(x1 - x0)
    W, Ln, Un, G = map(int, infile.readline().split())
    L = []
    for each in range(Ln):
        L.append(map(int, infile.readline().split()))
    U = []
    for each in range(Un):
        U.append(map(int, infile.readline().split()))

    def get_area(left, right):
        xs = [x for x, y in L]
        xs += [x for x, y in U]
        xs += [left, right]
        xs = sorted(set(xs))
        xs = [x for x in xs if left - 1e-09 <= x <= right + 1e-09]
        res = 0
        for i in range(1, len(xs)):
            x0 = xs[i - 1]
            x1 = xs[i]
            side0 = abs(get_y(L, x0) - get_y(U, x0))
            side1 = abs(get_y(L, x1) - get_y(U, x1))
            h = abs(x0 - x1)
            res += (side0 + side1) * h / 2.0
        return res
    total_area = get_area(0, W)
    slice_area = total_area / float(G)
    cuts = [0.0]
    return each


def func_85e749c1bf7d4216b89a1ca1bbba0e6d(infile):

    def get_y(P, qx):
        i = 0
        while i + 2 < len(P) and P[i + 1][0] <= qx:
            i += 1
        x0, y0 = P[i]
        x1, y1 = P[i + 1]
        return y0 + (y1 - y0) * (qx - x0) / float(x1 - x0)
    W, Ln, Un, G = map(int, infile.readline().split())
    L = []
    for each in range(Ln):
        L.append(map(int, infile.readline().split()))
    U = []
    for each in range(Un):
        U.append(map(int, infile.readline().split()))

    def get_area(left, right):
        xs = [x for x, y in L]
        xs += [x for x, y in U]
        xs += [left, right]
        xs = sorted(set(xs))
        xs = [x for x in xs if left - 1e-09 <= x <= right + 1e-09]
        res = 0
        for i in range(1, len(xs)):
            x0 = xs[i - 1]
            x1 = xs[i]
            side0 = abs(get_y(L, x0) - get_y(U, x0))
            side1 = abs(get_y(L, x1) - get_y(U, x1))
            h = abs(x0 - x1)
            res += (side0 + side1) * h / 2.0
        return res
    total_area = get_area(0, W)
    slice_area = total_area / float(G)
    cuts = [0.0]
    return cuts


def func_96413a2a0ed3472ca46bf22796b6e8f0(infile):

    def get_y(P, qx):
        i = 0
        while i + 2 < len(P) and P[i + 1][0] <= qx:
            i += 1
        x0, y0 = P[i]
        x1, y1 = P[i + 1]
        return y0 + (y1 - y0) * (qx - x0) / float(x1 - x0)
    W, Ln, Un, G = map(int, infile.readline().split())
    L = []
    for each in range(Ln):
        L.append(map(int, infile.readline().split()))
    U = []
    for each in range(Un):
        U.append(map(int, infile.readline().split()))

    def get_area(left, right):
        xs = [x for x, y in L]
        xs += [x for x, y in U]
        xs += [left, right]
        xs = sorted(set(xs))
        xs = [x for x in xs if left - 1e-09 <= x <= right + 1e-09]
        res = 0
        for i in range(1, len(xs)):
            x0 = xs[i - 1]
            x1 = xs[i]
            side0 = abs(get_y(L, x0) - get_y(U, x0))
            side1 = abs(get_y(L, x1) - get_y(U, x1))
            h = abs(x0 - x1)
            res += (side0 + side1) * h / 2.0
        return res
    total_area = get_area(0, W)
    slice_area = total_area / float(G)
    cuts = [0.0]
    return L


def func_32b889c5907741bfbd40fe29c0708c3c(infile):

    def get_y(P, qx):
        i = 0
        while i + 2 < len(P) and P[i + 1][0] <= qx:
            i += 1
        x0, y0 = P[i]
        x1, y1 = P[i + 1]
        return y0 + (y1 - y0) * (qx - x0) / float(x1 - x0)
    W, Ln, Un, G = map(int, infile.readline().split())
    L = []
    for each in range(Ln):
        L.append(map(int, infile.readline().split()))
    U = []
    for each in range(Un):
        U.append(map(int, infile.readline().split()))

    def get_area(left, right):
        xs = [x for x, y in L]
        xs += [x for x, y in U]
        xs += [left, right]
        xs = sorted(set(xs))
        xs = [x for x in xs if left - 1e-09 <= x <= right + 1e-09]
        res = 0
        for i in range(1, len(xs)):
            x0 = xs[i - 1]
            x1 = xs[i]
            side0 = abs(get_y(L, x0) - get_y(U, x0))
            side1 = abs(get_y(L, x1) - get_y(U, x1))
            h = abs(x0 - x1)
            res += (side0 + side1) * h / 2.0
        return res
    total_area = get_area(0, W)
    slice_area = total_area / float(G)
    cuts = [0.0]
    return total_area


def func_64148ab28f8c4a8da7bb25d39df6dbe0(infile):

    def get_y(P, qx):
        i = 0
        while i + 2 < len(P) and P[i + 1][0] <= qx:
            i += 1
        x0, y0 = P[i]
        x1, y1 = P[i + 1]
        return y0 + (y1 - y0) * (qx - x0) / float(x1 - x0)
    W, Ln, Un, G = map(int, infile.readline().split())
    L = []
    for each in range(Ln):
        L.append(map(int, infile.readline().split()))
    U = []
    for each in range(Un):
        U.append(map(int, infile.readline().split()))

    def get_area(left, right):
        xs = [x for x, y in L]
        xs += [x for x, y in U]
        xs += [left, right]
        xs = sorted(set(xs))
        xs = [x for x in xs if left - 1e-09 <= x <= right + 1e-09]
        res = 0
        for i in range(1, len(xs)):
            x0 = xs[i - 1]
            x1 = xs[i]
            side0 = abs(get_y(L, x0) - get_y(U, x0))
            side1 = abs(get_y(L, x1) - get_y(U, x1))
            h = abs(x0 - x1)
            res += (side0 + side1) * h / 2.0
        return res
    total_area = get_area(0, W)
    slice_area = total_area / float(G)
    cuts = [0.0]
    return slice_area


def func_21610a3f88a843f1b4287c310a62aeca(infile):

    def get_y(P, qx):
        i = 0
        while i + 2 < len(P) and P[i + 1][0] <= qx:
            i += 1
        x0, y0 = P[i]
        x1, y1 = P[i + 1]
        return y0 + (y1 - y0) * (qx - x0) / float(x1 - x0)
    W, Ln, Un, G = map(int, infile.readline().split())
    L = []
    for each in range(Ln):
        L.append(map(int, infile.readline().split()))
    U = []
    for each in range(Un):
        U.append(map(int, infile.readline().split()))

    def get_area(left, right):
        xs = [x for x, y in L]
        xs += [x for x, y in U]
        xs += [left, right]
        xs = sorted(set(xs))
        xs = [x for x in xs if left - 1e-09 <= x <= right + 1e-09]
        res = 0
        for i in range(1, len(xs)):
            x0 = xs[i - 1]
            x1 = xs[i]
            side0 = abs(get_y(L, x0) - get_y(U, x0))
            side1 = abs(get_y(L, x1) - get_y(U, x1))
            h = abs(x0 - x1)
            res += (side0 + side1) * h / 2.0
        return res
    total_area = get_area(0, W)
    slice_area = total_area / float(G)
    cuts = [0.0]
    return G


def func_fb17015fb0504761b1d447c3dfade3bf(infile):

    def get_y(P, qx):
        i = 0
        while i + 2 < len(P) and P[i + 1][0] <= qx:
            i += 1
        x0, y0 = P[i]
        x1, y1 = P[i + 1]
        return y0 + (y1 - y0) * (qx - x0) / float(x1 - x0)
    W, Ln, Un, G = map(int, infile.readline().split())
    L = []
    for each in range(Ln):
        L.append(map(int, infile.readline().split()))
    U = []
    for each in range(Un):
        U.append(map(int, infile.readline().split()))

    def get_area(left, right):
        xs = [x for x, y in L]
        xs += [x for x, y in U]
        xs += [left, right]
        xs = sorted(set(xs))
        xs = [x for x in xs if left - 1e-09 <= x <= right + 1e-09]
        res = 0
        for i in range(1, len(xs)):
            x0 = xs[i - 1]
            x1 = xs[i]
            side0 = abs(get_y(L, x0) - get_y(U, x0))
            side1 = abs(get_y(L, x1) - get_y(U, x1))
            h = abs(x0 - x1)
            res += (side0 + side1) * h / 2.0
        return res
    total_area = get_area(0, W)
    slice_area = total_area / float(G)
    cuts = [0.0]
    return Ln


def func_72221557496e453fb639d4af8f32334f(infile):

    def get_y(P, qx):
        i = 0
        while i + 2 < len(P) and P[i + 1][0] <= qx:
            i += 1
        x0, y0 = P[i]
        x1, y1 = P[i + 1]
        return y0 + (y1 - y0) * (qx - x0) / float(x1 - x0)
    W, Ln, Un, G = map(int, infile.readline().split())
    L = []
    for each in range(Ln):
        L.append(map(int, infile.readline().split()))
    U = []
    for each in range(Un):
        U.append(map(int, infile.readline().split()))

    def get_area(left, right):
        xs = [x for x, y in L]
        xs += [x for x, y in U]
        xs += [left, right]
        xs = sorted(set(xs))
        xs = [x for x in xs if left - 1e-09 <= x <= right + 1e-09]
        res = 0
        for i in range(1, len(xs)):
            x0 = xs[i - 1]
            x1 = xs[i]
            side0 = abs(get_y(L, x0) - get_y(U, x0))
            side1 = abs(get_y(L, x1) - get_y(U, x1))
            h = abs(x0 - x1)
            res += (side0 + side1) * h / 2.0
        return res
    total_area = get_area(0, W)
    slice_area = total_area / float(G)
    cuts = [0.0]
    return Un


def func_c24d46279fd94000ab29bcb929f087bd(total_area, infile):

    def get_y(P, qx):
        i = 0
        while i + 2 < len(P) and P[i + 1][0] <= qx:
            i += 1
        x0, y0 = P[i]
        x1, y1 = P[i + 1]
        return y0 + (y1 - y0) * (qx - x0) / float(x1 - x0)
    W, Ln, Un, G = map(int, infile.readline().split())
    L = []
    for each in range(Ln):
        L.append(map(int, infile.readline().split()))
    U = []
    for each in range(Un):
        U.append(map(int, infile.readline().split()))

    def get_area(left, right):
        xs = [x for x, y in L]
        xs += [x for x, y in U]
        xs += [left, right]
        xs = sorted(set(xs))
        xs = [x for x in xs if left - 1e-09 <= x <= right + 1e-09]
        res = 0
        for i in range(1, len(xs)):
            x0 = xs[i - 1]
            x1 = xs[i]
            side0 = abs(get_y(L, x0) - get_y(U, x0))
            side1 = abs(get_y(L, x1) - get_y(U, x1))
            h = abs(x0 - x1)
            res += (side0 + side1) * h / 2.0
        return res
    slice_area = total_area / float(G)
    cuts = [0.0]
    while len(cuts) < G:
        low = cuts[-1]
        high = W
        for i in range(50):
            mid = (low + high) / 2
            if get_area(cuts[-1], mid) > slice_area:
                high = mid
            else:
                low = mid
        cuts.append(mid)
        print '%.10lg' % mid
    return i


def func_f38f5717db814e4ab606ae10e7e5ece2(total_area, infile):

    def get_y(P, qx):
        i = 0
        while i + 2 < len(P) and P[i + 1][0] <= qx:
            i += 1
        x0, y0 = P[i]
        x1, y1 = P[i + 1]
        return y0 + (y1 - y0) * (qx - x0) / float(x1 - x0)
    W, Ln, Un, G = map(int, infile.readline().split())
    L = []
    for each in range(Ln):
        L.append(map(int, infile.readline().split()))
    U = []
    for each in range(Un):
        U.append(map(int, infile.readline().split()))

    def get_area(left, right):
        xs = [x for x, y in L]
        xs += [x for x, y in U]
        xs += [left, right]
        xs = sorted(set(xs))
        xs = [x for x in xs if left - 1e-09 <= x <= right + 1e-09]
        res = 0
        for i in range(1, len(xs)):
            x0 = xs[i - 1]
            x1 = xs[i]
            side0 = abs(get_y(L, x0) - get_y(U, x0))
            side1 = abs(get_y(L, x1) - get_y(U, x1))
            h = abs(x0 - x1)
            res += (side0 + side1) * h / 2.0
        return res
    slice_area = total_area / float(G)
    cuts = [0.0]
    while len(cuts) < G:
        low = cuts[-1]
        high = W
        for i in range(50):
            mid = (low + high) / 2
            if get_area(cuts[-1], mid) > slice_area:
                high = mid
            else:
                low = mid
        cuts.append(mid)
        print '%.10lg' % mid
    return slice_area


def func_912c7334866840eb93a381a5d3fc53be(total_area, infile):

    def get_y(P, qx):
        i = 0
        while i + 2 < len(P) and P[i + 1][0] <= qx:
            i += 1
        x0, y0 = P[i]
        x1, y1 = P[i + 1]
        return y0 + (y1 - y0) * (qx - x0) / float(x1 - x0)
    W, Ln, Un, G = map(int, infile.readline().split())
    L = []
    for each in range(Ln):
        L.append(map(int, infile.readline().split()))
    U = []
    for each in range(Un):
        U.append(map(int, infile.readline().split()))

    def get_area(left, right):
        xs = [x for x, y in L]
        xs += [x for x, y in U]
        xs += [left, right]
        xs = sorted(set(xs))
        xs = [x for x in xs if left - 1e-09 <= x <= right + 1e-09]
        res = 0
        for i in range(1, len(xs)):
            x0 = xs[i - 1]
            x1 = xs[i]
            side0 = abs(get_y(L, x0) - get_y(U, x0))
            side1 = abs(get_y(L, x1) - get_y(U, x1))
            h = abs(x0 - x1)
            res += (side0 + side1) * h / 2.0
        return res
    slice_area = total_area / float(G)
    cuts = [0.0]
    while len(cuts) < G:
        low = cuts[-1]
        high = W
        for i in range(50):
            mid = (low + high) / 2
            if get_area(cuts[-1], mid) > slice_area:
                high = mid
            else:
                low = mid
        cuts.append(mid)
        print '%.10lg' % mid
    return L


def func_0a6573f281c14446a36afa21b8445989(total_area, infile):

    def get_y(P, qx):
        i = 0
        while i + 2 < len(P) and P[i + 1][0] <= qx:
            i += 1
        x0, y0 = P[i]
        x1, y1 = P[i + 1]
        return y0 + (y1 - y0) * (qx - x0) / float(x1 - x0)
    W, Ln, Un, G = map(int, infile.readline().split())
    L = []
    for each in range(Ln):
        L.append(map(int, infile.readline().split()))
    U = []
    for each in range(Un):
        U.append(map(int, infile.readline().split()))

    def get_area(left, right):
        xs = [x for x, y in L]
        xs += [x for x, y in U]
        xs += [left, right]
        xs = sorted(set(xs))
        xs = [x for x in xs if left - 1e-09 <= x <= right + 1e-09]
        res = 0
        for i in range(1, len(xs)):
            x0 = xs[i - 1]
            x1 = xs[i]
            side0 = abs(get_y(L, x0) - get_y(U, x0))
            side1 = abs(get_y(L, x1) - get_y(U, x1))
            h = abs(x0 - x1)
            res += (side0 + side1) * h / 2.0
        return res
    slice_area = total_area / float(G)
    cuts = [0.0]
    while len(cuts) < G:
        low = cuts[-1]
        high = W
        for i in range(50):
            mid = (low + high) / 2
            if get_area(cuts[-1], mid) > slice_area:
                high = mid
            else:
                low = mid
        cuts.append(mid)
        print '%.10lg' % mid
    return W


def func_77dcb530979147f7b32ead096a08e628(total_area, infile):

    def get_y(P, qx):
        i = 0
        while i + 2 < len(P) and P[i + 1][0] <= qx:
            i += 1
        x0, y0 = P[i]
        x1, y1 = P[i + 1]
        return y0 + (y1 - y0) * (qx - x0) / float(x1 - x0)
    W, Ln, Un, G = map(int, infile.readline().split())
    L = []
    for each in range(Ln):
        L.append(map(int, infile.readline().split()))
    U = []
    for each in range(Un):
        U.append(map(int, infile.readline().split()))

    def get_area(left, right):
        xs = [x for x, y in L]
        xs += [x for x, y in U]
        xs += [left, right]
        xs = sorted(set(xs))
        xs = [x for x in xs if left - 1e-09 <= x <= right + 1e-09]
        res = 0
        for i in range(1, len(xs)):
            x0 = xs[i - 1]
            x1 = xs[i]
            side0 = abs(get_y(L, x0) - get_y(U, x0))
            side1 = abs(get_y(L, x1) - get_y(U, x1))
            h = abs(x0 - x1)
            res += (side0 + side1) * h / 2.0
        return res
    slice_area = total_area / float(G)
    cuts = [0.0]
    while len(cuts) < G:
        low = cuts[-1]
        high = W
        for i in range(50):
            mid = (low + high) / 2
            if get_area(cuts[-1], mid) > slice_area:
                high = mid
            else:
                low = mid
        cuts.append(mid)
        print '%.10lg' % mid
    return G


def func_4f069d571cc448fe9929aa699fc67e20(total_area, infile):

    def get_y(P, qx):
        i = 0
        while i + 2 < len(P) and P[i + 1][0] <= qx:
            i += 1
        x0, y0 = P[i]
        x1, y1 = P[i + 1]
        return y0 + (y1 - y0) * (qx - x0) / float(x1 - x0)
    W, Ln, Un, G = map(int, infile.readline().split())
    L = []
    for each in range(Ln):
        L.append(map(int, infile.readline().split()))
    U = []
    for each in range(Un):
        U.append(map(int, infile.readline().split()))

    def get_area(left, right):
        xs = [x for x, y in L]
        xs += [x for x, y in U]
        xs += [left, right]
        xs = sorted(set(xs))
        xs = [x for x in xs if left - 1e-09 <= x <= right + 1e-09]
        res = 0
        for i in range(1, len(xs)):
            x0 = xs[i - 1]
            x1 = xs[i]
            side0 = abs(get_y(L, x0) - get_y(U, x0))
            side1 = abs(get_y(L, x1) - get_y(U, x1))
            h = abs(x0 - x1)
            res += (side0 + side1) * h / 2.0
        return res
    slice_area = total_area / float(G)
    cuts = [0.0]
    while len(cuts) < G:
        low = cuts[-1]
        high = W
        for i in range(50):
            mid = (low + high) / 2
            if get_area(cuts[-1], mid) > slice_area:
                high = mid
            else:
                low = mid
        cuts.append(mid)
        print '%.10lg' % mid
    return total_area


def func_03ec5fc81bcd48a3b088472ad8d8b2be(total_area, infile):

    def get_y(P, qx):
        i = 0
        while i + 2 < len(P) and P[i + 1][0] <= qx:
            i += 1
        x0, y0 = P[i]
        x1, y1 = P[i + 1]
        return y0 + (y1 - y0) * (qx - x0) / float(x1 - x0)
    W, Ln, Un, G = map(int, infile.readline().split())
    L = []
    for each in range(Ln):
        L.append(map(int, infile.readline().split()))
    U = []
    for each in range(Un):
        U.append(map(int, infile.readline().split()))

    def get_area(left, right):
        xs = [x for x, y in L]
        xs += [x for x, y in U]
        xs += [left, right]
        xs = sorted(set(xs))
        xs = [x for x in xs if left - 1e-09 <= x <= right + 1e-09]
        res = 0
        for i in range(1, len(xs)):
            x0 = xs[i - 1]
            x1 = xs[i]
            side0 = abs(get_y(L, x0) - get_y(U, x0))
            side1 = abs(get_y(L, x1) - get_y(U, x1))
            h = abs(x0 - x1)
            res += (side0 + side1) * h / 2.0
        return res
    slice_area = total_area / float(G)
    cuts = [0.0]
    while len(cuts) < G:
        low = cuts[-1]
        high = W
        for i in range(50):
            mid = (low + high) / 2
            if get_area(cuts[-1], mid) > slice_area:
                high = mid
            else:
                low = mid
        cuts.append(mid)
        print '%.10lg' % mid
    return Ln


def func_6f517b34a5c24454a689f7d2373e4883(total_area, infile):

    def get_y(P, qx):
        i = 0
        while i + 2 < len(P) and P[i + 1][0] <= qx:
            i += 1
        x0, y0 = P[i]
        x1, y1 = P[i + 1]
        return y0 + (y1 - y0) * (qx - x0) / float(x1 - x0)
    W, Ln, Un, G = map(int, infile.readline().split())
    L = []
    for each in range(Ln):
        L.append(map(int, infile.readline().split()))
    U = []
    for each in range(Un):
        U.append(map(int, infile.readline().split()))

    def get_area(left, right):
        xs = [x for x, y in L]
        xs += [x for x, y in U]
        xs += [left, right]
        xs = sorted(set(xs))
        xs = [x for x in xs if left - 1e-09 <= x <= right + 1e-09]
        res = 0
        for i in range(1, len(xs)):
            x0 = xs[i - 1]
            x1 = xs[i]
            side0 = abs(get_y(L, x0) - get_y(U, x0))
            side1 = abs(get_y(L, x1) - get_y(U, x1))
            h = abs(x0 - x1)
            res += (side0 + side1) * h / 2.0
        return res
    slice_area = total_area / float(G)
    cuts = [0.0]
    while len(cuts) < G:
        low = cuts[-1]
        high = W
        for i in range(50):
            mid = (low + high) / 2
            if get_area(cuts[-1], mid) > slice_area:
                high = mid
            else:
                low = mid
        cuts.append(mid)
        print '%.10lg' % mid
    return Un


def func_05ccd84ed4f844639d3b5cc6a8aca3f2(total_area, infile):

    def get_y(P, qx):
        i = 0
        while i + 2 < len(P) and P[i + 1][0] <= qx:
            i += 1
        x0, y0 = P[i]
        x1, y1 = P[i + 1]
        return y0 + (y1 - y0) * (qx - x0) / float(x1 - x0)
    W, Ln, Un, G = map(int, infile.readline().split())
    L = []
    for each in range(Ln):
        L.append(map(int, infile.readline().split()))
    U = []
    for each in range(Un):
        U.append(map(int, infile.readline().split()))

    def get_area(left, right):
        xs = [x for x, y in L]
        xs += [x for x, y in U]
        xs += [left, right]
        xs = sorted(set(xs))
        xs = [x for x in xs if left - 1e-09 <= x <= right + 1e-09]
        res = 0
        for i in range(1, len(xs)):
            x0 = xs[i - 1]
            x1 = xs[i]
            side0 = abs(get_y(L, x0) - get_y(U, x0))
            side1 = abs(get_y(L, x1) - get_y(U, x1))
            h = abs(x0 - x1)
            res += (side0 + side1) * h / 2.0
        return res
    slice_area = total_area / float(G)
    cuts = [0.0]
    while len(cuts) < G:
        low = cuts[-1]
        high = W
        for i in range(50):
            mid = (low + high) / 2
            if get_area(cuts[-1], mid) > slice_area:
                high = mid
            else:
                low = mid
        cuts.append(mid)
        print '%.10lg' % mid
    return low


def func_218eb6b0fc43472bbcba3004a2785362(total_area, infile):

    def get_y(P, qx):
        i = 0
        while i + 2 < len(P) and P[i + 1][0] <= qx:
            i += 1
        x0, y0 = P[i]
        x1, y1 = P[i + 1]
        return y0 + (y1 - y0) * (qx - x0) / float(x1 - x0)
    W, Ln, Un, G = map(int, infile.readline().split())
    L = []
    for each in range(Ln):
        L.append(map(int, infile.readline().split()))
    U = []
    for each in range(Un):
        U.append(map(int, infile.readline().split()))

    def get_area(left, right):
        xs = [x for x, y in L]
        xs += [x for x, y in U]
        xs += [left, right]
        xs = sorted(set(xs))
        xs = [x for x in xs if left - 1e-09 <= x <= right + 1e-09]
        res = 0
        for i in range(1, len(xs)):
            x0 = xs[i - 1]
            x1 = xs[i]
            side0 = abs(get_y(L, x0) - get_y(U, x0))
            side1 = abs(get_y(L, x1) - get_y(U, x1))
            h = abs(x0 - x1)
            res += (side0 + side1) * h / 2.0
        return res
    slice_area = total_area / float(G)
    cuts = [0.0]
    while len(cuts) < G:
        low = cuts[-1]
        high = W
        for i in range(50):
            mid = (low + high) / 2
            if get_area(cuts[-1], mid) > slice_area:
                high = mid
            else:
                low = mid
        cuts.append(mid)
        print '%.10lg' % mid
    return U


def func_c36e96bd0e1f4519a728c713b014e1bd(total_area, infile):

    def get_y(P, qx):
        i = 0
        while i + 2 < len(P) and P[i + 1][0] <= qx:
            i += 1
        x0, y0 = P[i]
        x1, y1 = P[i + 1]
        return y0 + (y1 - y0) * (qx - x0) / float(x1 - x0)
    W, Ln, Un, G = map(int, infile.readline().split())
    L = []
    for each in range(Ln):
        L.append(map(int, infile.readline().split()))
    U = []
    for each in range(Un):
        U.append(map(int, infile.readline().split()))

    def get_area(left, right):
        xs = [x for x, y in L]
        xs += [x for x, y in U]
        xs += [left, right]
        xs = sorted(set(xs))
        xs = [x for x in xs if left - 1e-09 <= x <= right + 1e-09]
        res = 0
        for i in range(1, len(xs)):
            x0 = xs[i - 1]
            x1 = xs[i]
            side0 = abs(get_y(L, x0) - get_y(U, x0))
            side1 = abs(get_y(L, x1) - get_y(U, x1))
            h = abs(x0 - x1)
            res += (side0 + side1) * h / 2.0
        return res
    slice_area = total_area / float(G)
    cuts = [0.0]
    while len(cuts) < G:
        low = cuts[-1]
        high = W
        for i in range(50):
            mid = (low + high) / 2
            if get_area(cuts[-1], mid) > slice_area:
                high = mid
            else:
                low = mid
        cuts.append(mid)
        print '%.10lg' % mid
    return high


def func_33ec978406254f16aecef7add0fa358e(total_area, infile):

    def get_y(P, qx):
        i = 0
        while i + 2 < len(P) and P[i + 1][0] <= qx:
            i += 1
        x0, y0 = P[i]
        x1, y1 = P[i + 1]
        return y0 + (y1 - y0) * (qx - x0) / float(x1 - x0)
    W, Ln, Un, G = map(int, infile.readline().split())
    L = []
    for each in range(Ln):
        L.append(map(int, infile.readline().split()))
    U = []
    for each in range(Un):
        U.append(map(int, infile.readline().split()))

    def get_area(left, right):
        xs = [x for x, y in L]
        xs += [x for x, y in U]
        xs += [left, right]
        xs = sorted(set(xs))
        xs = [x for x in xs if left - 1e-09 <= x <= right + 1e-09]
        res = 0
        for i in range(1, len(xs)):
            x0 = xs[i - 1]
            x1 = xs[i]
            side0 = abs(get_y(L, x0) - get_y(U, x0))
            side1 = abs(get_y(L, x1) - get_y(U, x1))
            h = abs(x0 - x1)
            res += (side0 + side1) * h / 2.0
        return res
    slice_area = total_area / float(G)
    cuts = [0.0]
    while len(cuts) < G:
        low = cuts[-1]
        high = W
        for i in range(50):
            mid = (low + high) / 2
            if get_area(cuts[-1], mid) > slice_area:
                high = mid
            else:
                low = mid
        cuts.append(mid)
        print '%.10lg' % mid
    return cuts


def func_c8a379c6cbcc437586bc3b60db85d96a(total_area, infile):

    def get_y(P, qx):
        i = 0
        while i + 2 < len(P) and P[i + 1][0] <= qx:
            i += 1
        x0, y0 = P[i]
        x1, y1 = P[i + 1]
        return y0 + (y1 - y0) * (qx - x0) / float(x1 - x0)
    W, Ln, Un, G = map(int, infile.readline().split())
    L = []
    for each in range(Ln):
        L.append(map(int, infile.readline().split()))
    U = []
    for each in range(Un):
        U.append(map(int, infile.readline().split()))

    def get_area(left, right):
        xs = [x for x, y in L]
        xs += [x for x, y in U]
        xs += [left, right]
        xs = sorted(set(xs))
        xs = [x for x in xs if left - 1e-09 <= x <= right + 1e-09]
        res = 0
        for i in range(1, len(xs)):
            x0 = xs[i - 1]
            x1 = xs[i]
            side0 = abs(get_y(L, x0) - get_y(U, x0))
            side1 = abs(get_y(L, x1) - get_y(U, x1))
            h = abs(x0 - x1)
            res += (side0 + side1) * h / 2.0
        return res
    slice_area = total_area / float(G)
    cuts = [0.0]
    while len(cuts) < G:
        low = cuts[-1]
        high = W
        for i in range(50):
            mid = (low + high) / 2
            if get_area(cuts[-1], mid) > slice_area:
                high = mid
            else:
                low = mid
        cuts.append(mid)
        print '%.10lg' % mid
    return each


def func_54311f3e79e2416e86c70f741d1e54ea(total_area, infile):

    def get_y(P, qx):
        i = 0
        while i + 2 < len(P) and P[i + 1][0] <= qx:
            i += 1
        x0, y0 = P[i]
        x1, y1 = P[i + 1]
        return y0 + (y1 - y0) * (qx - x0) / float(x1 - x0)
    W, Ln, Un, G = map(int, infile.readline().split())
    L = []
    for each in range(Ln):
        L.append(map(int, infile.readline().split()))
    U = []
    for each in range(Un):
        U.append(map(int, infile.readline().split()))

    def get_area(left, right):
        xs = [x for x, y in L]
        xs += [x for x, y in U]
        xs += [left, right]
        xs = sorted(set(xs))
        xs = [x for x in xs if left - 1e-09 <= x <= right + 1e-09]
        res = 0
        for i in range(1, len(xs)):
            x0 = xs[i - 1]
            x1 = xs[i]
            side0 = abs(get_y(L, x0) - get_y(U, x0))
            side1 = abs(get_y(L, x1) - get_y(U, x1))
            h = abs(x0 - x1)
            res += (side0 + side1) * h / 2.0
        return res
    slice_area = total_area / float(G)
    cuts = [0.0]
    while len(cuts) < G:
        low = cuts[-1]
        high = W
        for i in range(50):
            mid = (low + high) / 2
            if get_area(cuts[-1], mid) > slice_area:
                high = mid
            else:
                low = mid
        cuts.append(mid)
        print '%.10lg' % mid
    return mid


def func_f2453c0d03e641419c8a0991343b96d1(infile):

    def get_y(P, qx):
        i = 0
        while i + 2 < len(P) and P[i + 1][0] <= qx:
            i += 1
        x0, y0 = P[i]
        x1, y1 = P[i + 1]
        return y0 + (y1 - y0) * (qx - x0) / float(x1 - x0)
    W, Ln, Un, G = map(int, infile.readline().split())
    L = []
    for each in range(Ln):
        L.append(map(int, infile.readline().split()))
    U = []
    for each in range(Un):
        U.append(map(int, infile.readline().split()))

    def get_area(left, right):
        xs = [x for x, y in L]
        xs += [x for x, y in U]
        xs += [left, right]
        xs = sorted(set(xs))
        xs = [x for x in xs if left - 1e-09 <= x <= right + 1e-09]
        res = 0
        for i in range(1, len(xs)):
            x0 = xs[i - 1]
            x1 = xs[i]
            side0 = abs(get_y(L, x0) - get_y(U, x0))
            side1 = abs(get_y(L, x1) - get_y(U, x1))
            h = abs(x0 - x1)
            res += (side0 + side1) * h / 2.0
        return res
    total_area = get_area(0, W)
    slice_area = total_area / float(G)
    cuts = [0.0]
    while len(cuts) < G:
        low = cuts[-1]
        high = W
        for i in range(50):
            mid = (low + high) / 2
            if get_area(cuts[-1], mid) > slice_area:
                high = mid
            else:
                low = mid
        cuts.append(mid)
        print '%.10lg' % mid
    return low


def func_28d17fa0cbf54df7b776721634e4a124(infile):

    def get_y(P, qx):
        i = 0
        while i + 2 < len(P) and P[i + 1][0] <= qx:
            i += 1
        x0, y0 = P[i]
        x1, y1 = P[i + 1]
        return y0 + (y1 - y0) * (qx - x0) / float(x1 - x0)
    W, Ln, Un, G = map(int, infile.readline().split())
    L = []
    for each in range(Ln):
        L.append(map(int, infile.readline().split()))
    U = []
    for each in range(Un):
        U.append(map(int, infile.readline().split()))

    def get_area(left, right):
        xs = [x for x, y in L]
        xs += [x for x, y in U]
        xs += [left, right]
        xs = sorted(set(xs))
        xs = [x for x in xs if left - 1e-09 <= x <= right + 1e-09]
        res = 0
        for i in range(1, len(xs)):
            x0 = xs[i - 1]
            x1 = xs[i]
            side0 = abs(get_y(L, x0) - get_y(U, x0))
            side1 = abs(get_y(L, x1) - get_y(U, x1))
            h = abs(x0 - x1)
            res += (side0 + side1) * h / 2.0
        return res
    total_area = get_area(0, W)
    slice_area = total_area / float(G)
    cuts = [0.0]
    while len(cuts) < G:
        low = cuts[-1]
        high = W
        for i in range(50):
            mid = (low + high) / 2
            if get_area(cuts[-1], mid) > slice_area:
                high = mid
            else:
                low = mid
        cuts.append(mid)
        print '%.10lg' % mid
    return mid


def func_4b28ead38186470e88523057fc53bef7(infile):

    def get_y(P, qx):
        i = 0
        while i + 2 < len(P) and P[i + 1][0] <= qx:
            i += 1
        x0, y0 = P[i]
        x1, y1 = P[i + 1]
        return y0 + (y1 - y0) * (qx - x0) / float(x1 - x0)
    W, Ln, Un, G = map(int, infile.readline().split())
    L = []
    for each in range(Ln):
        L.append(map(int, infile.readline().split()))
    U = []
    for each in range(Un):
        U.append(map(int, infile.readline().split()))

    def get_area(left, right):
        xs = [x for x, y in L]
        xs += [x for x, y in U]
        xs += [left, right]
        xs = sorted(set(xs))
        xs = [x for x in xs if left - 1e-09 <= x <= right + 1e-09]
        res = 0
        for i in range(1, len(xs)):
            x0 = xs[i - 1]
            x1 = xs[i]
            side0 = abs(get_y(L, x0) - get_y(U, x0))
            side1 = abs(get_y(L, x1) - get_y(U, x1))
            h = abs(x0 - x1)
            res += (side0 + side1) * h / 2.0
        return res
    total_area = get_area(0, W)
    slice_area = total_area / float(G)
    cuts = [0.0]
    while len(cuts) < G:
        low = cuts[-1]
        high = W
        for i in range(50):
            mid = (low + high) / 2
            if get_area(cuts[-1], mid) > slice_area:
                high = mid
            else:
                low = mid
        cuts.append(mid)
        print '%.10lg' % mid
    return total_area


def func_b8dddaed0b78446c860d646680073105(infile):

    def get_y(P, qx):
        i = 0
        while i + 2 < len(P) and P[i + 1][0] <= qx:
            i += 1
        x0, y0 = P[i]
        x1, y1 = P[i + 1]
        return y0 + (y1 - y0) * (qx - x0) / float(x1 - x0)
    W, Ln, Un, G = map(int, infile.readline().split())
    L = []
    for each in range(Ln):
        L.append(map(int, infile.readline().split()))
    U = []
    for each in range(Un):
        U.append(map(int, infile.readline().split()))

    def get_area(left, right):
        xs = [x for x, y in L]
        xs += [x for x, y in U]
        xs += [left, right]
        xs = sorted(set(xs))
        xs = [x for x in xs if left - 1e-09 <= x <= right + 1e-09]
        res = 0
        for i in range(1, len(xs)):
            x0 = xs[i - 1]
            x1 = xs[i]
            side0 = abs(get_y(L, x0) - get_y(U, x0))
            side1 = abs(get_y(L, x1) - get_y(U, x1))
            h = abs(x0 - x1)
            res += (side0 + side1) * h / 2.0
        return res
    total_area = get_area(0, W)
    slice_area = total_area / float(G)
    cuts = [0.0]
    while len(cuts) < G:
        low = cuts[-1]
        high = W
        for i in range(50):
            mid = (low + high) / 2
            if get_area(cuts[-1], mid) > slice_area:
                high = mid
            else:
                low = mid
        cuts.append(mid)
        print '%.10lg' % mid
    return Ln


def func_834537ac81a54fc1b104ec10fbba2563(infile):

    def get_y(P, qx):
        i = 0
        while i + 2 < len(P) and P[i + 1][0] <= qx:
            i += 1
        x0, y0 = P[i]
        x1, y1 = P[i + 1]
        return y0 + (y1 - y0) * (qx - x0) / float(x1 - x0)
    W, Ln, Un, G = map(int, infile.readline().split())
    L = []
    for each in range(Ln):
        L.append(map(int, infile.readline().split()))
    U = []
    for each in range(Un):
        U.append(map(int, infile.readline().split()))

    def get_area(left, right):
        xs = [x for x, y in L]
        xs += [x for x, y in U]
        xs += [left, right]
        xs = sorted(set(xs))
        xs = [x for x in xs if left - 1e-09 <= x <= right + 1e-09]
        res = 0
        for i in range(1, len(xs)):
            x0 = xs[i - 1]
            x1 = xs[i]
            side0 = abs(get_y(L, x0) - get_y(U, x0))
            side1 = abs(get_y(L, x1) - get_y(U, x1))
            h = abs(x0 - x1)
            res += (side0 + side1) * h / 2.0
        return res
    total_area = get_area(0, W)
    slice_area = total_area / float(G)
    cuts = [0.0]
    while len(cuts) < G:
        low = cuts[-1]
        high = W
        for i in range(50):
            mid = (low + high) / 2
            if get_area(cuts[-1], mid) > slice_area:
                high = mid
            else:
                low = mid
        cuts.append(mid)
        print '%.10lg' % mid
    return L


def func_9727b862a02349ca93dfc67b13924ebe(infile):

    def get_y(P, qx):
        i = 0
        while i + 2 < len(P) and P[i + 1][0] <= qx:
            i += 1
        x0, y0 = P[i]
        x1, y1 = P[i + 1]
        return y0 + (y1 - y0) * (qx - x0) / float(x1 - x0)
    W, Ln, Un, G = map(int, infile.readline().split())
    L = []
    for each in range(Ln):
        L.append(map(int, infile.readline().split()))
    U = []
    for each in range(Un):
        U.append(map(int, infile.readline().split()))

    def get_area(left, right):
        xs = [x for x, y in L]
        xs += [x for x, y in U]
        xs += [left, right]
        xs = sorted(set(xs))
        xs = [x for x in xs if left - 1e-09 <= x <= right + 1e-09]
        res = 0
        for i in range(1, len(xs)):
            x0 = xs[i - 1]
            x1 = xs[i]
            side0 = abs(get_y(L, x0) - get_y(U, x0))
            side1 = abs(get_y(L, x1) - get_y(U, x1))
            h = abs(x0 - x1)
            res += (side0 + side1) * h / 2.0
        return res
    total_area = get_area(0, W)
    slice_area = total_area / float(G)
    cuts = [0.0]
    while len(cuts) < G:
        low = cuts[-1]
        high = W
        for i in range(50):
            mid = (low + high) / 2
            if get_area(cuts[-1], mid) > slice_area:
                high = mid
            else:
                low = mid
        cuts.append(mid)
        print '%.10lg' % mid
    return slice_area


def func_0e022e962d954fa3b6b2c0558947c67c(infile):

    def get_y(P, qx):
        i = 0
        while i + 2 < len(P) and P[i + 1][0] <= qx:
            i += 1
        x0, y0 = P[i]
        x1, y1 = P[i + 1]
        return y0 + (y1 - y0) * (qx - x0) / float(x1 - x0)
    W, Ln, Un, G = map(int, infile.readline().split())
    L = []
    for each in range(Ln):
        L.append(map(int, infile.readline().split()))
    U = []
    for each in range(Un):
        U.append(map(int, infile.readline().split()))

    def get_area(left, right):
        xs = [x for x, y in L]
        xs += [x for x, y in U]
        xs += [left, right]
        xs = sorted(set(xs))
        xs = [x for x in xs if left - 1e-09 <= x <= right + 1e-09]
        res = 0
        for i in range(1, len(xs)):
            x0 = xs[i - 1]
            x1 = xs[i]
            side0 = abs(get_y(L, x0) - get_y(U, x0))
            side1 = abs(get_y(L, x1) - get_y(U, x1))
            h = abs(x0 - x1)
            res += (side0 + side1) * h / 2.0
        return res
    total_area = get_area(0, W)
    slice_area = total_area / float(G)
    cuts = [0.0]
    while len(cuts) < G:
        low = cuts[-1]
        high = W
        for i in range(50):
            mid = (low + high) / 2
            if get_area(cuts[-1], mid) > slice_area:
                high = mid
            else:
                low = mid
        cuts.append(mid)
        print '%.10lg' % mid
    return G


def func_f3464c4b6c2b4520b553a4aac7fda071(infile):

    def get_y(P, qx):
        i = 0
        while i + 2 < len(P) and P[i + 1][0] <= qx:
            i += 1
        x0, y0 = P[i]
        x1, y1 = P[i + 1]
        return y0 + (y1 - y0) * (qx - x0) / float(x1 - x0)
    W, Ln, Un, G = map(int, infile.readline().split())
    L = []
    for each in range(Ln):
        L.append(map(int, infile.readline().split()))
    U = []
    for each in range(Un):
        U.append(map(int, infile.readline().split()))

    def get_area(left, right):
        xs = [x for x, y in L]
        xs += [x for x, y in U]
        xs += [left, right]
        xs = sorted(set(xs))
        xs = [x for x in xs if left - 1e-09 <= x <= right + 1e-09]
        res = 0
        for i in range(1, len(xs)):
            x0 = xs[i - 1]
            x1 = xs[i]
            side0 = abs(get_y(L, x0) - get_y(U, x0))
            side1 = abs(get_y(L, x1) - get_y(U, x1))
            h = abs(x0 - x1)
            res += (side0 + side1) * h / 2.0
        return res
    total_area = get_area(0, W)
    slice_area = total_area / float(G)
    cuts = [0.0]
    while len(cuts) < G:
        low = cuts[-1]
        high = W
        for i in range(50):
            mid = (low + high) / 2
            if get_area(cuts[-1], mid) > slice_area:
                high = mid
            else:
                low = mid
        cuts.append(mid)
        print '%.10lg' % mid
    return each


def func_707a1045b0694d35b7e66d587e5ae80d(infile):

    def get_y(P, qx):
        i = 0
        while i + 2 < len(P) and P[i + 1][0] <= qx:
            i += 1
        x0, y0 = P[i]
        x1, y1 = P[i + 1]
        return y0 + (y1 - y0) * (qx - x0) / float(x1 - x0)
    W, Ln, Un, G = map(int, infile.readline().split())
    L = []
    for each in range(Ln):
        L.append(map(int, infile.readline().split()))
    U = []
    for each in range(Un):
        U.append(map(int, infile.readline().split()))

    def get_area(left, right):
        xs = [x for x, y in L]
        xs += [x for x, y in U]
        xs += [left, right]
        xs = sorted(set(xs))
        xs = [x for x in xs if left - 1e-09 <= x <= right + 1e-09]
        res = 0
        for i in range(1, len(xs)):
            x0 = xs[i - 1]
            x1 = xs[i]
            side0 = abs(get_y(L, x0) - get_y(U, x0))
            side1 = abs(get_y(L, x1) - get_y(U, x1))
            h = abs(x0 - x1)
            res += (side0 + side1) * h / 2.0
        return res
    total_area = get_area(0, W)
    slice_area = total_area / float(G)
    cuts = [0.0]
    while len(cuts) < G:
        low = cuts[-1]
        high = W
        for i in range(50):
            mid = (low + high) / 2
            if get_area(cuts[-1], mid) > slice_area:
                high = mid
            else:
                low = mid
        cuts.append(mid)
        print '%.10lg' % mid
    return U


def func_aa3ed22c6bf24fc7b07c2cd8de7aa14d(infile):

    def get_y(P, qx):
        i = 0
        while i + 2 < len(P) and P[i + 1][0] <= qx:
            i += 1
        x0, y0 = P[i]
        x1, y1 = P[i + 1]
        return y0 + (y1 - y0) * (qx - x0) / float(x1 - x0)
    W, Ln, Un, G = map(int, infile.readline().split())
    L = []
    for each in range(Ln):
        L.append(map(int, infile.readline().split()))
    U = []
    for each in range(Un):
        U.append(map(int, infile.readline().split()))

    def get_area(left, right):
        xs = [x for x, y in L]
        xs += [x for x, y in U]
        xs += [left, right]
        xs = sorted(set(xs))
        xs = [x for x in xs if left - 1e-09 <= x <= right + 1e-09]
        res = 0
        for i in range(1, len(xs)):
            x0 = xs[i - 1]
            x1 = xs[i]
            side0 = abs(get_y(L, x0) - get_y(U, x0))
            side1 = abs(get_y(L, x1) - get_y(U, x1))
            h = abs(x0 - x1)
            res += (side0 + side1) * h / 2.0
        return res
    total_area = get_area(0, W)
    slice_area = total_area / float(G)
    cuts = [0.0]
    while len(cuts) < G:
        low = cuts[-1]
        high = W
        for i in range(50):
            mid = (low + high) / 2
            if get_area(cuts[-1], mid) > slice_area:
                high = mid
            else:
                low = mid
        cuts.append(mid)
        print '%.10lg' % mid
    return i


def func_5718571ba76a48dc906c13ac412eb5f7(infile):

    def get_y(P, qx):
        i = 0
        while i + 2 < len(P) and P[i + 1][0] <= qx:
            i += 1
        x0, y0 = P[i]
        x1, y1 = P[i + 1]
        return y0 + (y1 - y0) * (qx - x0) / float(x1 - x0)
    W, Ln, Un, G = map(int, infile.readline().split())
    L = []
    for each in range(Ln):
        L.append(map(int, infile.readline().split()))
    U = []
    for each in range(Un):
        U.append(map(int, infile.readline().split()))

    def get_area(left, right):
        xs = [x for x, y in L]
        xs += [x for x, y in U]
        xs += [left, right]
        xs = sorted(set(xs))
        xs = [x for x in xs if left - 1e-09 <= x <= right + 1e-09]
        res = 0
        for i in range(1, len(xs)):
            x0 = xs[i - 1]
            x1 = xs[i]
            side0 = abs(get_y(L, x0) - get_y(U, x0))
            side1 = abs(get_y(L, x1) - get_y(U, x1))
            h = abs(x0 - x1)
            res += (side0 + side1) * h / 2.0
        return res
    total_area = get_area(0, W)
    slice_area = total_area / float(G)
    cuts = [0.0]
    while len(cuts) < G:
        low = cuts[-1]
        high = W
        for i in range(50):
            mid = (low + high) / 2
            if get_area(cuts[-1], mid) > slice_area:
                high = mid
            else:
                low = mid
        cuts.append(mid)
        print '%.10lg' % mid
    return high


def func_018c77c4eedb4652a9a8bbe99fdde479(infile):

    def get_y(P, qx):
        i = 0
        while i + 2 < len(P) and P[i + 1][0] <= qx:
            i += 1
        x0, y0 = P[i]
        x1, y1 = P[i + 1]
        return y0 + (y1 - y0) * (qx - x0) / float(x1 - x0)
    W, Ln, Un, G = map(int, infile.readline().split())
    L = []
    for each in range(Ln):
        L.append(map(int, infile.readline().split()))
    U = []
    for each in range(Un):
        U.append(map(int, infile.readline().split()))

    def get_area(left, right):
        xs = [x for x, y in L]
        xs += [x for x, y in U]
        xs += [left, right]
        xs = sorted(set(xs))
        xs = [x for x in xs if left - 1e-09 <= x <= right + 1e-09]
        res = 0
        for i in range(1, len(xs)):
            x0 = xs[i - 1]
            x1 = xs[i]
            side0 = abs(get_y(L, x0) - get_y(U, x0))
            side1 = abs(get_y(L, x1) - get_y(U, x1))
            h = abs(x0 - x1)
            res += (side0 + side1) * h / 2.0
        return res
    total_area = get_area(0, W)
    slice_area = total_area / float(G)
    cuts = [0.0]
    while len(cuts) < G:
        low = cuts[-1]
        high = W
        for i in range(50):
            mid = (low + high) / 2
            if get_area(cuts[-1], mid) > slice_area:
                high = mid
            else:
                low = mid
        cuts.append(mid)
        print '%.10lg' % mid
    return Un


def func_6baa846988a9461bbcc442dbc45cd3e7(infile):

    def get_y(P, qx):
        i = 0
        while i + 2 < len(P) and P[i + 1][0] <= qx:
            i += 1
        x0, y0 = P[i]
        x1, y1 = P[i + 1]
        return y0 + (y1 - y0) * (qx - x0) / float(x1 - x0)
    W, Ln, Un, G = map(int, infile.readline().split())
    L = []
    for each in range(Ln):
        L.append(map(int, infile.readline().split()))
    U = []
    for each in range(Un):
        U.append(map(int, infile.readline().split()))

    def get_area(left, right):
        xs = [x for x, y in L]
        xs += [x for x, y in U]
        xs += [left, right]
        xs = sorted(set(xs))
        xs = [x for x in xs if left - 1e-09 <= x <= right + 1e-09]
        res = 0
        for i in range(1, len(xs)):
            x0 = xs[i - 1]
            x1 = xs[i]
            side0 = abs(get_y(L, x0) - get_y(U, x0))
            side1 = abs(get_y(L, x1) - get_y(U, x1))
            h = abs(x0 - x1)
            res += (side0 + side1) * h / 2.0
        return res
    total_area = get_area(0, W)
    slice_area = total_area / float(G)
    cuts = [0.0]
    while len(cuts) < G:
        low = cuts[-1]
        high = W
        for i in range(50):
            mid = (low + high) / 2
            if get_area(cuts[-1], mid) > slice_area:
                high = mid
            else:
                low = mid
        cuts.append(mid)
        print '%.10lg' % mid
    return cuts


def func_97596770290c4c5d9886c592c786fd1d(infile):

    def get_y(P, qx):
        i = 0
        while i + 2 < len(P) and P[i + 1][0] <= qx:
            i += 1
        x0, y0 = P[i]
        x1, y1 = P[i + 1]
        return y0 + (y1 - y0) * (qx - x0) / float(x1 - x0)
    W, Ln, Un, G = map(int, infile.readline().split())
    L = []
    for each in range(Ln):
        L.append(map(int, infile.readline().split()))
    U = []
    for each in range(Un):
        U.append(map(int, infile.readline().split()))

    def get_area(left, right):
        xs = [x for x, y in L]
        xs += [x for x, y in U]
        xs += [left, right]
        xs = sorted(set(xs))
        xs = [x for x in xs if left - 1e-09 <= x <= right + 1e-09]
        res = 0
        for i in range(1, len(xs)):
            x0 = xs[i - 1]
            x1 = xs[i]
            side0 = abs(get_y(L, x0) - get_y(U, x0))
            side1 = abs(get_y(L, x1) - get_y(U, x1))
            h = abs(x0 - x1)
            res += (side0 + side1) * h / 2.0
        return res
    total_area = get_area(0, W)
    slice_area = total_area / float(G)
    cuts = [0.0]
    while len(cuts) < G:
        low = cuts[-1]
        high = W
        for i in range(50):
            mid = (low + high) / 2
            if get_area(cuts[-1], mid) > slice_area:
                high = mid
            else:
                low = mid
        cuts.append(mid)
        print '%.10lg' % mid
    return W


def func_ca7ad67d06f14c36a4d8754cdaf4eb4d(cuts, infile, slice_area):

    def get_y(P, qx):
        i = 0
        while i + 2 < len(P) and P[i + 1][0] <= qx:
            i += 1
        x0, y0 = P[i]
        x1, y1 = P[i + 1]
        return y0 + (y1 - y0) * (qx - x0) / float(x1 - x0)
    W, Ln, Un, G = map(int, infile.readline().split())
    L = []
    for each in range(Ln):
        L.append(map(int, infile.readline().split()))
    U = []
    for each in range(Un):
        U.append(map(int, infile.readline().split()))

    def get_area(left, right):
        xs = [x for x, y in L]
        xs += [x for x, y in U]
        xs += [left, right]
        xs = sorted(set(xs))
        xs = [x for x in xs if left - 1e-09 <= x <= right + 1e-09]
        res = 0
        for i in range(1, len(xs)):
            x0 = xs[i - 1]
            x1 = xs[i]
            side0 = abs(get_y(L, x0) - get_y(U, x0))
            side1 = abs(get_y(L, x1) - get_y(U, x1))
            h = abs(x0 - x1)
            res += (side0 + side1) * h / 2.0
        return res
    while len(cuts) < G:
        low = cuts[-1]
        high = W
        for i in range(50):
            mid = (low + high) / 2
            if get_area(cuts[-1], mid) > slice_area:
                high = mid
            else:
                low = mid
        cuts.append(mid)
        print '%.10lg' % mid
    return Un


def func_b50423b17cd44942b3dffa348f05c6b6(cuts, infile, slice_area):

    def get_y(P, qx):
        i = 0
        while i + 2 < len(P) and P[i + 1][0] <= qx:
            i += 1
        x0, y0 = P[i]
        x1, y1 = P[i + 1]
        return y0 + (y1 - y0) * (qx - x0) / float(x1 - x0)
    W, Ln, Un, G = map(int, infile.readline().split())
    L = []
    for each in range(Ln):
        L.append(map(int, infile.readline().split()))
    U = []
    for each in range(Un):
        U.append(map(int, infile.readline().split()))

    def get_area(left, right):
        xs = [x for x, y in L]
        xs += [x for x, y in U]
        xs += [left, right]
        xs = sorted(set(xs))
        xs = [x for x in xs if left - 1e-09 <= x <= right + 1e-09]
        res = 0
        for i in range(1, len(xs)):
            x0 = xs[i - 1]
            x1 = xs[i]
            side0 = abs(get_y(L, x0) - get_y(U, x0))
            side1 = abs(get_y(L, x1) - get_y(U, x1))
            h = abs(x0 - x1)
            res += (side0 + side1) * h / 2.0
        return res
    while len(cuts) < G:
        low = cuts[-1]
        high = W
        for i in range(50):
            mid = (low + high) / 2
            if get_area(cuts[-1], mid) > slice_area:
                high = mid
            else:
                low = mid
        cuts.append(mid)
        print '%.10lg' % mid
    return Ln


def func_230283e52e1945cc8c2cf74faf614abb(cuts, infile, slice_area):

    def get_y(P, qx):
        i = 0
        while i + 2 < len(P) and P[i + 1][0] <= qx:
            i += 1
        x0, y0 = P[i]
        x1, y1 = P[i + 1]
        return y0 + (y1 - y0) * (qx - x0) / float(x1 - x0)
    W, Ln, Un, G = map(int, infile.readline().split())
    L = []
    for each in range(Ln):
        L.append(map(int, infile.readline().split()))
    U = []
    for each in range(Un):
        U.append(map(int, infile.readline().split()))

    def get_area(left, right):
        xs = [x for x, y in L]
        xs += [x for x, y in U]
        xs += [left, right]
        xs = sorted(set(xs))
        xs = [x for x in xs if left - 1e-09 <= x <= right + 1e-09]
        res = 0
        for i in range(1, len(xs)):
            x0 = xs[i - 1]
            x1 = xs[i]
            side0 = abs(get_y(L, x0) - get_y(U, x0))
            side1 = abs(get_y(L, x1) - get_y(U, x1))
            h = abs(x0 - x1)
            res += (side0 + side1) * h / 2.0
        return res
    while len(cuts) < G:
        low = cuts[-1]
        high = W
        for i in range(50):
            mid = (low + high) / 2
            if get_area(cuts[-1], mid) > slice_area:
                high = mid
            else:
                low = mid
        cuts.append(mid)
        print '%.10lg' % mid
    return mid


def func_d156aa46ef8e4f2091b6bd3686bb60f6(cuts, infile, slice_area):

    def get_y(P, qx):
        i = 0
        while i + 2 < len(P) and P[i + 1][0] <= qx:
            i += 1
        x0, y0 = P[i]
        x1, y1 = P[i + 1]
        return y0 + (y1 - y0) * (qx - x0) / float(x1 - x0)
    W, Ln, Un, G = map(int, infile.readline().split())
    L = []
    for each in range(Ln):
        L.append(map(int, infile.readline().split()))
    U = []
    for each in range(Un):
        U.append(map(int, infile.readline().split()))

    def get_area(left, right):
        xs = [x for x, y in L]
        xs += [x for x, y in U]
        xs += [left, right]
        xs = sorted(set(xs))
        xs = [x for x in xs if left - 1e-09 <= x <= right + 1e-09]
        res = 0
        for i in range(1, len(xs)):
            x0 = xs[i - 1]
            x1 = xs[i]
            side0 = abs(get_y(L, x0) - get_y(U, x0))
            side1 = abs(get_y(L, x1) - get_y(U, x1))
            h = abs(x0 - x1)
            res += (side0 + side1) * h / 2.0
        return res
    while len(cuts) < G:
        low = cuts[-1]
        high = W
        for i in range(50):
            mid = (low + high) / 2
            if get_area(cuts[-1], mid) > slice_area:
                high = mid
            else:
                low = mid
        cuts.append(mid)
        print '%.10lg' % mid
    return each


def func_37547b0de9ca4dbd86d13bd22f26b3a2(cuts, infile, slice_area):

    def get_y(P, qx):
        i = 0
        while i + 2 < len(P) and P[i + 1][0] <= qx:
            i += 1
        x0, y0 = P[i]
        x1, y1 = P[i + 1]
        return y0 + (y1 - y0) * (qx - x0) / float(x1 - x0)
    W, Ln, Un, G = map(int, infile.readline().split())
    L = []
    for each in range(Ln):
        L.append(map(int, infile.readline().split()))
    U = []
    for each in range(Un):
        U.append(map(int, infile.readline().split()))

    def get_area(left, right):
        xs = [x for x, y in L]
        xs += [x for x, y in U]
        xs += [left, right]
        xs = sorted(set(xs))
        xs = [x for x in xs if left - 1e-09 <= x <= right + 1e-09]
        res = 0
        for i in range(1, len(xs)):
            x0 = xs[i - 1]
            x1 = xs[i]
            side0 = abs(get_y(L, x0) - get_y(U, x0))
            side1 = abs(get_y(L, x1) - get_y(U, x1))
            h = abs(x0 - x1)
            res += (side0 + side1) * h / 2.0
        return res
    while len(cuts) < G:
        low = cuts[-1]
        high = W
        for i in range(50):
            mid = (low + high) / 2
            if get_area(cuts[-1], mid) > slice_area:
                high = mid
            else:
                low = mid
        cuts.append(mid)
        print '%.10lg' % mid
    return U


def func_957dfbd69f5d4c06b41ca2a134d1689b(cuts, infile, slice_area):

    def get_y(P, qx):
        i = 0
        while i + 2 < len(P) and P[i + 1][0] <= qx:
            i += 1
        x0, y0 = P[i]
        x1, y1 = P[i + 1]
        return y0 + (y1 - y0) * (qx - x0) / float(x1 - x0)
    W, Ln, Un, G = map(int, infile.readline().split())
    L = []
    for each in range(Ln):
        L.append(map(int, infile.readline().split()))
    U = []
    for each in range(Un):
        U.append(map(int, infile.readline().split()))

    def get_area(left, right):
        xs = [x for x, y in L]
        xs += [x for x, y in U]
        xs += [left, right]
        xs = sorted(set(xs))
        xs = [x for x in xs if left - 1e-09 <= x <= right + 1e-09]
        res = 0
        for i in range(1, len(xs)):
            x0 = xs[i - 1]
            x1 = xs[i]
            side0 = abs(get_y(L, x0) - get_y(U, x0))
            side1 = abs(get_y(L, x1) - get_y(U, x1))
            h = abs(x0 - x1)
            res += (side0 + side1) * h / 2.0
        return res
    while len(cuts) < G:
        low = cuts[-1]
        high = W
        for i in range(50):
            mid = (low + high) / 2
            if get_area(cuts[-1], mid) > slice_area:
                high = mid
            else:
                low = mid
        cuts.append(mid)
        print '%.10lg' % mid
    return G


def func_7a0f6062dcf44d3e9b8237de0ca1bbc8(cuts, infile, slice_area):

    def get_y(P, qx):
        i = 0
        while i + 2 < len(P) and P[i + 1][0] <= qx:
            i += 1
        x0, y0 = P[i]
        x1, y1 = P[i + 1]
        return y0 + (y1 - y0) * (qx - x0) / float(x1 - x0)
    W, Ln, Un, G = map(int, infile.readline().split())
    L = []
    for each in range(Ln):
        L.append(map(int, infile.readline().split()))
    U = []
    for each in range(Un):
        U.append(map(int, infile.readline().split()))

    def get_area(left, right):
        xs = [x for x, y in L]
        xs += [x for x, y in U]
        xs += [left, right]
        xs = sorted(set(xs))
        xs = [x for x in xs if left - 1e-09 <= x <= right + 1e-09]
        res = 0
        for i in range(1, len(xs)):
            x0 = xs[i - 1]
            x1 = xs[i]
            side0 = abs(get_y(L, x0) - get_y(U, x0))
            side1 = abs(get_y(L, x1) - get_y(U, x1))
            h = abs(x0 - x1)
            res += (side0 + side1) * h / 2.0
        return res
    while len(cuts) < G:
        low = cuts[-1]
        high = W
        for i in range(50):
            mid = (low + high) / 2
            if get_area(cuts[-1], mid) > slice_area:
                high = mid
            else:
                low = mid
        cuts.append(mid)
        print '%.10lg' % mid
    return high


def func_dcbdda85fef446bb99e02eee967298fb(cuts, infile, slice_area):

    def get_y(P, qx):
        i = 0
        while i + 2 < len(P) and P[i + 1][0] <= qx:
            i += 1
        x0, y0 = P[i]
        x1, y1 = P[i + 1]
        return y0 + (y1 - y0) * (qx - x0) / float(x1 - x0)
    W, Ln, Un, G = map(int, infile.readline().split())
    L = []
    for each in range(Ln):
        L.append(map(int, infile.readline().split()))
    U = []
    for each in range(Un):
        U.append(map(int, infile.readline().split()))

    def get_area(left, right):
        xs = [x for x, y in L]
        xs += [x for x, y in U]
        xs += [left, right]
        xs = sorted(set(xs))
        xs = [x for x in xs if left - 1e-09 <= x <= right + 1e-09]
        res = 0
        for i in range(1, len(xs)):
            x0 = xs[i - 1]
            x1 = xs[i]
            side0 = abs(get_y(L, x0) - get_y(U, x0))
            side1 = abs(get_y(L, x1) - get_y(U, x1))
            h = abs(x0 - x1)
            res += (side0 + side1) * h / 2.0
        return res
    while len(cuts) < G:
        low = cuts[-1]
        high = W
        for i in range(50):
            mid = (low + high) / 2
            if get_area(cuts[-1], mid) > slice_area:
                high = mid
            else:
                low = mid
        cuts.append(mid)
        print '%.10lg' % mid
    return W


def func_5701a4eac5fd4bf2a37fb8af0aea61a5(cuts, infile, slice_area):

    def get_y(P, qx):
        i = 0
        while i + 2 < len(P) and P[i + 1][0] <= qx:
            i += 1
        x0, y0 = P[i]
        x1, y1 = P[i + 1]
        return y0 + (y1 - y0) * (qx - x0) / float(x1 - x0)
    W, Ln, Un, G = map(int, infile.readline().split())
    L = []
    for each in range(Ln):
        L.append(map(int, infile.readline().split()))
    U = []
    for each in range(Un):
        U.append(map(int, infile.readline().split()))

    def get_area(left, right):
        xs = [x for x, y in L]
        xs += [x for x, y in U]
        xs += [left, right]
        xs = sorted(set(xs))
        xs = [x for x in xs if left - 1e-09 <= x <= right + 1e-09]
        res = 0
        for i in range(1, len(xs)):
            x0 = xs[i - 1]
            x1 = xs[i]
            side0 = abs(get_y(L, x0) - get_y(U, x0))
            side1 = abs(get_y(L, x1) - get_y(U, x1))
            h = abs(x0 - x1)
            res += (side0 + side1) * h / 2.0
        return res
    while len(cuts) < G:
        low = cuts[-1]
        high = W
        for i in range(50):
            mid = (low + high) / 2
            if get_area(cuts[-1], mid) > slice_area:
                high = mid
            else:
                low = mid
        cuts.append(mid)
        print '%.10lg' % mid
    return L


def func_d55512a915574987ba7ca7007b22aced(cuts, infile, slice_area):

    def get_y(P, qx):
        i = 0
        while i + 2 < len(P) and P[i + 1][0] <= qx:
            i += 1
        x0, y0 = P[i]
        x1, y1 = P[i + 1]
        return y0 + (y1 - y0) * (qx - x0) / float(x1 - x0)
    W, Ln, Un, G = map(int, infile.readline().split())
    L = []
    for each in range(Ln):
        L.append(map(int, infile.readline().split()))
    U = []
    for each in range(Un):
        U.append(map(int, infile.readline().split()))

    def get_area(left, right):
        xs = [x for x, y in L]
        xs += [x for x, y in U]
        xs += [left, right]
        xs = sorted(set(xs))
        xs = [x for x in xs if left - 1e-09 <= x <= right + 1e-09]
        res = 0
        for i in range(1, len(xs)):
            x0 = xs[i - 1]
            x1 = xs[i]
            side0 = abs(get_y(L, x0) - get_y(U, x0))
            side1 = abs(get_y(L, x1) - get_y(U, x1))
            h = abs(x0 - x1)
            res += (side0 + side1) * h / 2.0
        return res
    while len(cuts) < G:
        low = cuts[-1]
        high = W
        for i in range(50):
            mid = (low + high) / 2
            if get_area(cuts[-1], mid) > slice_area:
                high = mid
            else:
                low = mid
        cuts.append(mid)
        print '%.10lg' % mid
    return low


def func_d52bec9762194fa9adc8c0e0d0988e11(cuts, infile, slice_area):

    def get_y(P, qx):
        i = 0
        while i + 2 < len(P) and P[i + 1][0] <= qx:
            i += 1
        x0, y0 = P[i]
        x1, y1 = P[i + 1]
        return y0 + (y1 - y0) * (qx - x0) / float(x1 - x0)
    W, Ln, Un, G = map(int, infile.readline().split())
    L = []
    for each in range(Ln):
        L.append(map(int, infile.readline().split()))
    U = []
    for each in range(Un):
        U.append(map(int, infile.readline().split()))

    def get_area(left, right):
        xs = [x for x, y in L]
        xs += [x for x, y in U]
        xs += [left, right]
        xs = sorted(set(xs))
        xs = [x for x in xs if left - 1e-09 <= x <= right + 1e-09]
        res = 0
        for i in range(1, len(xs)):
            x0 = xs[i - 1]
            x1 = xs[i]
            side0 = abs(get_y(L, x0) - get_y(U, x0))
            side1 = abs(get_y(L, x1) - get_y(U, x1))
            h = abs(x0 - x1)
            res += (side0 + side1) * h / 2.0
        return res
    while len(cuts) < G:
        low = cuts[-1]
        high = W
        for i in range(50):
            mid = (low + high) / 2
            if get_area(cuts[-1], mid) > slice_area:
                high = mid
            else:
                low = mid
        cuts.append(mid)
        print '%.10lg' % mid
    return slice_area


def func_dbacd6c98f2d43ffb794cfef640d19e9(cuts, infile, slice_area):

    def get_y(P, qx):
        i = 0
        while i + 2 < len(P) and P[i + 1][0] <= qx:
            i += 1
        x0, y0 = P[i]
        x1, y1 = P[i + 1]
        return y0 + (y1 - y0) * (qx - x0) / float(x1 - x0)
    W, Ln, Un, G = map(int, infile.readline().split())
    L = []
    for each in range(Ln):
        L.append(map(int, infile.readline().split()))
    U = []
    for each in range(Un):
        U.append(map(int, infile.readline().split()))

    def get_area(left, right):
        xs = [x for x, y in L]
        xs += [x for x, y in U]
        xs += [left, right]
        xs = sorted(set(xs))
        xs = [x for x in xs if left - 1e-09 <= x <= right + 1e-09]
        res = 0
        for i in range(1, len(xs)):
            x0 = xs[i - 1]
            x1 = xs[i]
            side0 = abs(get_y(L, x0) - get_y(U, x0))
            side1 = abs(get_y(L, x1) - get_y(U, x1))
            h = abs(x0 - x1)
            res += (side0 + side1) * h / 2.0
        return res
    while len(cuts) < G:
        low = cuts[-1]
        high = W
        for i in range(50):
            mid = (low + high) / 2
            if get_area(cuts[-1], mid) > slice_area:
                high = mid
            else:
                low = mid
        cuts.append(mid)
        print '%.10lg' % mid
    return cuts


def func_e931e85c64454b168a32dc06e78b4eb3(cuts, infile, slice_area):

    def get_y(P, qx):
        i = 0
        while i + 2 < len(P) and P[i + 1][0] <= qx:
            i += 1
        x0, y0 = P[i]
        x1, y1 = P[i + 1]
        return y0 + (y1 - y0) * (qx - x0) / float(x1 - x0)
    W, Ln, Un, G = map(int, infile.readline().split())
    L = []
    for each in range(Ln):
        L.append(map(int, infile.readline().split()))
    U = []
    for each in range(Un):
        U.append(map(int, infile.readline().split()))

    def get_area(left, right):
        xs = [x for x, y in L]
        xs += [x for x, y in U]
        xs += [left, right]
        xs = sorted(set(xs))
        xs = [x for x in xs if left - 1e-09 <= x <= right + 1e-09]
        res = 0
        for i in range(1, len(xs)):
            x0 = xs[i - 1]
            x1 = xs[i]
            side0 = abs(get_y(L, x0) - get_y(U, x0))
            side1 = abs(get_y(L, x1) - get_y(U, x1))
            h = abs(x0 - x1)
            res += (side0 + side1) * h / 2.0
        return res
    while len(cuts) < G:
        low = cuts[-1]
        high = W
        for i in range(50):
            mid = (low + high) / 2
            if get_area(cuts[-1], mid) > slice_area:
                high = mid
            else:
                low = mid
        cuts.append(mid)
        print '%.10lg' % mid
    return i


def func_e31298b633834ce4b51d84718023a093():
    infile = open('codejam/test_files/Y11R5P1/A.in')
    T = int(infile.readline())
    return T


def func_2402a80462a94f3b8abb581b3cccaa8c():
    infile = open('codejam/test_files/Y11R5P1/A.in')
    T = int(infile.readline())
    return infile


def func_926b43613fc34c3a8d50a25a665650b7(infile):
    T = int(infile.readline())
    for cs in range(T):
        print 'Case #%d:' % (cs + 1)
        solve(infile)
    return cs


def func_291dbbaff16d456bb01e5d66ff33eee9(infile):
    T = int(infile.readline())
    for cs in range(T):
        print 'Case #%d:' % (cs + 1)
        solve(infile)
    return T


def func_e5a080fa3b3046fe9c87e2a1f396337c():
    infile = open('codejam/test_files/Y11R5P1/A.in')
    T = int(infile.readline())
    for cs in range(T):
        print 'Case #%d:' % (cs + 1)
        solve(infile)
    return cs


def func_8869d00edcfe495cb0e9f15f094bfdbd():
    infile = open('codejam/test_files/Y11R5P1/A.in')
    T = int(infile.readline())
    for cs in range(T):
        print 'Case #%d:' % (cs + 1)
        solve(infile)
    return T


def func_b6773ea2da9743fe9d498c25f9f193b4():
    infile = open('codejam/test_files/Y11R5P1/A.in')
    T = int(infile.readline())
    for cs in range(T):
        print 'Case #%d:' % (cs + 1)
        solve(infile)
    return infile
